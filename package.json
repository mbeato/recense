{
  "name": "brain-memory",
  "version": "0.1.0",
  "description": "Brain-inspired memory engine for AI agents",
  "engines": {
    "node": ">=22"
  },
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "bin": {
    "brain": "dist/src/adapter/brain.js"
  },
  "scripts": {
    "build": "tsc",
    "postbuild": "chmod +x dist/src/adapter/brain.js && node scripts/copy-viz-assets.cjs",
    "pretest": "npm run build",
    "init": "npm run build && node dist/src/adapter/brain.js init",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "tsx src/index.ts",
    "smoke": "tsx scripts/smoke.ts",
    "smoke:dogfood": "tsx scripts/smoke-dogfood.ts",
    "eval:judge": "node scripts/eval/judge-eval-runner.cjs --eval scripts/eval/judge-eval-set.json"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.100.1",
    "@anthropic-ai/vertex-sdk": "^0.17.0",
    "better-sqlite3": "^12.10.0",
    "croner": "^10.0.1",
    "googleapis": "^173.0.0",
    "openai": "^4.103.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.9.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
