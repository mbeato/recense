---
status: complete
task: 260809-1n3
date: 2026-08-09
commit: ec9d6e1
---

# Quick Task 260809-1n3: Fix TS2532 in tests/ambient-recall.test.ts:574 — Summary

**One-liner:** Non-null assert `betaMentions[0]!` in the WR-03 hop-dedup test to clear the TS2532 typecheck blocker; `npm run typecheck` green.

## What was done

Single-line change at `tests/ambient-recall.test.ts:574`:

```diff
- expect(betaMentions[0].startsWith('- ')).toBe(true);
+ expect(betaMentions[0]!.startsWith('- ')).toBe(true);
```

The preceding line already asserts `expect(betaMentions).toHaveLength(1)`, so the element is runtime-guaranteed — the `!` is purely a type-checker guard. Idiom matches the suite-wide post-`toHaveLength` convention (`rows[0]!.id` in action-proposal-emission.test.ts, `rows[0]!.role` in adapter-capture.test.ts).

## Verification

- `npm run typecheck` exit 0 (both `tsconfig.json` and `tests/tsconfig.json`) — in the executor worktree and re-confirmed on main post-merge.
- Targeted WR-03 test: 2 passed, 31 skipped.
- Full file: `npx vitest run tests/ambient-recall.test.ts` — 33/33 passed.

## Commit

- `ec9d6e1` fix(tests): non-null assert betaMentions[0] in WR-03 dedup test to clear TS2532

## Note

This clears the "one typecheck blocker" flagged in the v10.0 milestone audit (eea648c).
