---
phase: quick-260809-1n3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/ambient-recall.test.ts]
autonomous: true
requirements: [QUICK-260809-1n3]

must_haves:
  truths:
    - "npm run typecheck exits 0 (TS2532 at tests/ambient-recall.test.ts:574 resolved)"
    - "The WR-03 dedup test still asserts the beta line is a fact line (starts with '- ')"
  artifacts:
    - path: "tests/ambient-recall.test.ts"
      provides: "Type-safe indexed access at line 574"
      contains: "betaMentions[0]!"
  key_links:
    - from: "tests/ambient-recall.test.ts"
      to: "tsconfig noUncheckedIndexedAccess"
      via: "non-null assertion after toHaveLength(1) runtime guard"
      pattern: "betaMentions\\[0\\]!\\.startsWith"
---

<objective>
Fix the TS2532 "Object is possibly 'undefined'" error at tests/ambient-recall.test.ts:574, where `betaMentions[0].startsWith('- ')` fails typecheck because `betaMentions[0]` is `string | undefined` under `noUncheckedIndexedAccess`.

Purpose: This is the single typecheck blocker flagged in the v10.0 milestone audit — `npm run typecheck` must be green.
Output: One-character-class edit to the test file; typecheck exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@tests/ambient-recall.test.ts (lines 557-575 — the WR-03 hop-dedup test)

Established codebase idiom for indexed access followed by a member call is the
non-null assertion, used pervasively in sibling test files:
- tests/action-proposal-emission.test.ts:320 `const row = rows[0]!;`
- tests/action-proposal-emission.test.ts:636 `expect(rowsAfterReplay[0]!.id).toBe(firstId);`
- tests/adapter-capture.test.ts:102-105 `expect(rows[0]!.role).toBe('user');` etc.

The preceding line (573) already asserts `expect(betaMentions).toHaveLength(1)`, so
the element is runtime-guaranteed to exist; the assertion is purely for the type checker.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard indexed access at tests/ambient-recall.test.ts:574 with non-null assertion</name>
  <files>tests/ambient-recall.test.ts</files>
  <action>Edit line 574 of tests/ambient-recall.test.ts, changing `expect(betaMentions[0].startsWith('- ')).toBe(true);` to `expect(betaMentions[0]!.startsWith('- ')).toBe(true);`. Use the non-null assertion (`[0]!`) — NOT optional chaining or an if-guard — because it is the established idiom for post-length-assertion indexed access across the test suite (action-proposal-emission.test.ts, adapter-capture.test.ts), and the preceding `toHaveLength(1)` assertion makes the element runtime-guaranteed. Do not touch any other line; no side-quest refactors of the other `expect(lines[0])...` sites, which typecheck fine because `expect()` accepts `string | undefined` when no member is dereferenced.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/ambient-recall.test.ts -t "WR-03"</automated>
  </verify>
  <done>`npm run typecheck` exits 0 with no TS2532 (or any other) diagnostics; the WR-03 hop-dedup test still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Test-file-only type-annotation change; no runtime code, no untrusted input paths touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | tests/ambient-recall.test.ts | accept | Non-null assertion could mask a real undefined if the length assertion above it were ever removed — acceptable because line 573 asserts length first, matching suite-wide convention |
</threat_model>

<verification>
- `npm run typecheck` exits 0 (the sole blocker from the v10.0 milestone audit)
- `npx vitest run tests/ambient-recall.test.ts` — full file passes, confirming no behavioral change
</verification>

<success_criteria>
- TS2532 at tests/ambient-recall.test.ts:574 is gone
- No other lines in the file modified
- Typecheck green, WR-03 test green
</success_criteria>

<output>
Create `.planning/quick/260809-1n3-fix-ts2532-in-tests-ambient-recall-test-/260809-1n3-SUMMARY.md` when done
</output>
