---
phase: quick-260809-1vg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/support/dist-build.ts
  - tests/adapter-capture.test.ts
  - tests/adapter-inject.test.ts
  - tests/episodic-dryrun-gate.test.ts
  - tests/eval-harness-smoke.test.ts
  - tests/locomo-harness.test.ts
  - tests/locomo-latency-curve.test.ts
  - tests/locomo-scorer.test.ts
  - tests/drift-05-harness-smoke.test.ts
  - tests/strip-hidden.test.ts
autonomous: true
requirements: [TD-DIST-SKIP, TD-STRIP-PERF]

must_haves:
  truths:
    - "With dist/ absent, the full vitest suite reports ZERO failures attributable to a missing dist/ build — the dist-dependent tests skip with a message naming the missing entrypoint and `npm run build`."
    - "With dist/ present, every test that was passing before this change still runs (not skipped) and still passes — the gate must not silently disable coverage on a built tree."
    - "The 17 absolute wall-clock assertions in tests/strip-hidden.test.ts become calibration-relative against a same-size benign body measured through stripHiddenContent in the same test invocation."
    - "The calibration-relative bounds still fail loudly on a quadratic regression — the pre-fix costs these guard were 10^2-10^4 x the benign reference, far past the chosen multiple."
    - "npm run typecheck stays green; no file under src/ is modified."
  artifacts:
    - path: "tests/support/dist-build.ts"
      provides: "Shared dist/ build-presence gate for CLI-subprocess tests"
      exports: ["hasDistEntries", "distSkipReason"]
    - path: "tests/strip-hidden.test.ts"
      provides: "Calibration-relative cost bounds (benign same-size reference through the same entry point)"
      contains: "benignReferenceElapsedMs"
  key_links:
    - from: "tests/adapter-capture.test.ts"
      to: "tests/support/dist-build.ts"
      via: "describe.skipIf(!hasDistEntries(...))"
      pattern: "hasDistEntries"
    - from: "tests/strip-hidden.test.ts"
      to: "src/source/strip-hidden.ts"
      via: "benign reference body timed through the same stripHiddenContent entry point"
      pattern: "benignReferenceElapsedMs"
---

<objective>
Clear two v10.0-audit tech-debt items, tests-only:

1. **dist-gate.** 24 pre-existing failures across 8 test files that spawn compiled `dist/` code. Fresh worktrees never run `npm run build`, so `vitest run` (bypassing the `pretest` build hook) produces confusing subprocess failures instead of skips. Make them skip gracefully with a message that names the missing entrypoint.
2. **strip-hidden perf flake.** 17 absolute `expect(elapsed).toBeLessThan(1000)` assertions in `tests/strip-hidden.test.ts` flake under full-suite parallel load. Convert them to the calibration-relative form already established in this repo by Phase 62 / WR-07.

Purpose: a fresh clone can run `npx vitest run` and get a clean, honest signal.
Output: one new shared test helper, gates applied to 8 test files, 17 assertions re-expressed as ratios against a measured baseline.

**Scope lock:** `tests/` only. Do NOT modify anything under `src/` or `scripts/`. Do NOT weaken or delete any assertion — the growth-ratio assertions (`t64 / Math.max(t32, 1) <= 8`) are already relative and stay UNCHANGED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Read-first (precedent to copy, do not re-derive):
@tests/recense-dispatch.test.ts        # lines 22-30: the EXISTING SKIP_NO_DIST + describe.skipIf idiom
@tests/gmail-hidden-content.test.ts    # lines 43-62 (helper) and 508-530 (assertion form): the EXISTING WR-07 calibration idiom
@tests/strip-hidden.test.ts            # the file being changed in Task 2
</context>

<measurements_from_planning>
All numbers below were measured during planning on this machine. The executor does not need to re-derive them, only to preserve the properties they establish.

**Dist-gate baseline (measured, `mv dist dist.bak` then `npx vitest run <the 8 files>`):**

With `dist/` PRESENT: 8 files, **54 tests, 54 passed, 0 skipped**.
With `dist/` ABSENT: **24 failed**, distributed:

| File | Fails / total | dist entrypoint the failing tests need |
|---|---|---|
| tests/adapter-capture.test.ts | 8 / 8 | `dist/src/adapter/turn-capture-cli.js`, `dist/src/adapter/stop-cli.js` (`DIST_DIR`, line 29) |
| tests/adapter-inject.test.ts | 5 / 8 (+3 already gated) | `dist/src/adapter/session-start-cli.js` (`COMPILED_CLI`, line 26) |
| tests/eval-harness-smoke.test.ts | 3 / 9 | spawns `scripts/eval/longmemeval-harness.cjs`, which `require`s `dist/src/db/schema` et al (harness lines 65-76) |
| tests/locomo-scorer.test.ts | 3 / 4 | spawns `scripts/eval/locomo-scorer.cjs` → `dist/src/lib/config` (harness line 42) |
| tests/locomo-harness.test.ts | 2 / 7 | spawns `scripts/eval/locomo-harness.cjs` → `dist/src/db/schema` et al (harness lines 50-61) |
| tests/drift-05-harness-smoke.test.ts | 1 / 9 | spawns `scripts/eval/drift-05-dry-run.cjs`; test already resolves `dist/src/source/provenance-key.js` at line 180 but **throws** instead of skipping |
| tests/episodic-dryrun-gate.test.ts | 1 / 8 | spawns `scripts/eval/episodic-dryrun.cjs` → `dist/src/db/schema` et al (harness lines 90-103) |
| tests/locomo-latency-curve.test.ts | 1 / 1 | spawns `scripts/eval/latency-curve.cjs` → `dist/src/db/schema` et al (harness lines 74-80) |

Already correctly gated, leave alone: `tests/recense-dispatch.test.ts`, `tests/recense-viz-no-open.test.ts`.
Investigated and ruled OUT (false positives from a `dist/` grep): `tests/corpus-promoter.test.ts` (the string is fixture data at line 154), `tests/recense-doctor.test.ts` / `tests/recense-init.test.ts` (path strings in config assertions, no spawn), `tests/correctness-harness-smoke.test.ts` (no subprocess), `tests/scheduler.test.ts` (child_process is mocked), `tests/ingest-project-reingest.test.ts` / `tests/pin-node.test.ts` (git/no dist).

**strip-hidden calibration baseline** (median of 5 trials, `stripHiddenContent` via tsx; benign reference = `'<p>text</p>'` padded to the SAME byte length, timed through the SAME entry point):

| Shape @ size | elapsed ms | benign ref ms | ratio |
|---|---|---|---|
| A / B / U @ 64 KB | 0.048-0.066 | 1.49-2.39 | 0.03 |
| A / B / U @ 512 KB | 0.362-0.369 | 13.9-19.5 | 0.02-0.03 |
| S @ 64 KB / 512 KB | 0.046 / 0.356 | 1.49 / 15.9 | 0.02-0.03 |
| T @ 64 KB | 1.403 | 1.664 | 0.84 |
| T62-91 bare `<style>` @ 1 MiB | 15.50 | 31.77 | 0.49 |
| T62-91 four attr pairs @ 1 MiB | 19.71 | 32.68 | 0.60 |
| T62-91 unquoted triple @ 1 MiB | 23.85 | 34.28 | 0.70 |
| T62-91 no-`>`-until-final @ 1 MiB | 10.25 | 30.96 | 0.33 |
| T62-91 well-formed (control) @ 1 MiB | 45.81 | 30.06 | **1.52** (observed max) |
| report shape @ 512 KB | 13.43 | 15.41 | 0.87 |
| V / W / X / X3 / Z @ 512 KB | 2.16-4.27 | 13.8-18.0 | 0.14-0.24 |
| Y @ 512 KB | 7.68 | 15.62 | 0.49 |

Two facts this establishes, both load-bearing for Task 2:

- **A ratio bound is defensible for every one of the 17 assertions here.** Unlike the one case `gmail-hidden-content.test.ts` deliberately left absolute (both sides sub-millisecond, timer-resolution noise), every benign reference here measures 1.5-35 ms — well above timer resolution.
- **10x carries ~6.6x headroom over the observed max ratio (1.52) while staying within one order of magnitude of it.** It is not a bound that cannot fail: the regressions these guard were measured pre-fix at 1,983 / 7,904 / 31,616 / **126,422 ms** (report shape, 64/128/256/512 KB — see the comment at line 1531) and 2,887-156,223 ms (T62-91 shapes, line 1427) against benign references of ~15-32 ms. That is a 10^2-10^4 x ratio. A returning quadratic blows through 10x by orders of magnitude.
</measurements_from_planning>

<tasks>

<task type="auto">
  <name>Task 1: Shared dist/ build gate — 8 CLI-subprocess test files skip instead of failing</name>
  <files>tests/support/dist-build.ts, tests/adapter-capture.test.ts, tests/adapter-inject.test.ts, tests/episodic-dryrun-gate.test.ts, tests/eval-harness-smoke.test.ts, tests/locomo-harness.test.ts, tests/locomo-latency-curve.test.ts, tests/locomo-scorer.test.ts, tests/drift-05-harness-smoke.test.ts</files>
  <action>
Create `tests/support/dist-build.ts` (new file, alongside the existing `css-liveness-oracle.ts` / `differential-helpers.ts` helpers). Root tsconfig is `"module": "commonjs"`, so `__dirname` is available and typechecks — resolve the repo root from `__dirname`, never from `process.cwd()`.

Export exactly two things:

- `hasDistEntries(...relFromRepoRoot: string[]): boolean` — true only when EVERY path exists (`node:fs` `existsSync`), each argument being a repo-root-relative path such as `'dist/src/adapter/stop-cli.js'`. Requiring all of them (not just the `dist/` directory) is what makes the gate survive a partial or stale build.
- `distSkipReason(...relFromRepoRoot: string[]): string` — a single-line reason naming the FIRST missing path plus the remedy, e.g. `dist/ build missing (dist/src/adapter/stop-cli.js) — run "npm run build" first`. Use it in the skipped test/describe name so a skip is self-explaining in CI output rather than a bare "skipped".

Then apply the gate to the 8 files listed in `<measurements_from_planning>`, using the EXISTING repo idiom (`tests/recense-dispatch.test.ts` lines 22-30) — a module-level `const SKIP_NO_DIST = !hasDistEntries(...)` followed by `describe.skipIf(SKIP_NO_DIST)` (or `it.skipIf(SKIP_NO_DIST)` where only some tests in a describe spawn a subprocess). Do NOT invent a second idiom.

Gate granularity rule: gate exactly the tests that invoke a dist-dependent entrypoint (a `spawnSync`/`execFileSync` of `dist/src/**` or of a `scripts/eval/*.cjs` that `require`s `dist/src/**`). Where every test in a describe spawns (adapter-capture, adapter-inject, locomo-latency-curve), gate at describe level. Where only some do (episodic-dryrun-gate, eval-harness-smoke, locomo-harness, locomo-scorer, drift-05-harness-smoke), gate at `it` level so the pure in-process tests keep running on an unbuilt tree.

Two files need more than a mechanical addition:

- `tests/adapter-inject.test.ts` — a local `const hasBuild = existsSync(COMPILED_CLI)` already exists at line 184 but gates only 3 of 8 tests. Replace it with the shared helper and extend the gate to cover the other 5 (the whole `session-start-cli (ADAPT-01)` describe), so the file yields 0 failures on an unbuilt tree.
- `tests/drift-05-harness-smoke.test.ts` — lines 177-186 already resolve `dist/src/source/provenance-key.js` and **throw** a descriptive error when it is missing. Replace the throw with the shared skip gate, preserving the existing message content in the skip reason. Delete the now-dead `existsSync`/`distEntry` locals only if nothing else in the file uses them.

Do not touch `tests/recense-dispatch.test.ts` or `tests/recense-viz-no-open.test.ts` — they are already correct and are the precedent being followed; retrofitting them is out of scope.

Add a one-line comment above each new gate stating WHY (spawns compiled `dist/` output; `pretest` builds it for `npm test`, but a bare `vitest run` in a fresh worktree does not). Keep it to one line — this file already carries heavy commentary elsewhere and the point is a gate, not an essay.
  </action>
  <verify>
    <automated>cd /Users/vtx/brain-memory &amp;&amp; npx vitest run tests/adapter-capture.test.ts tests/adapter-inject.test.ts tests/episodic-dryrun-gate.test.ts tests/eval-harness-smoke.test.ts tests/locomo-harness.test.ts tests/locomo-latency-curve.test.ts tests/locomo-scorer.test.ts tests/drift-05-harness-smoke.test.ts 2>&amp;1 | tail -8</automated>
  </verify>
  <done>
Two-sided gate, both sides required:

1. **Built tree (`dist/` present):** the command above reports `Tests  54 passed (54)` with **0 skipped**. A skip here means the gate over-fires and silently disabled coverage — that is a failure of this task, not a pass.
2. **Unbuilt tree:** with `dist/` temporarily moved aside, the same command reports **0 failed**. Baseline was 24 failed; expect >= 24 skipped. If the skipped count exceeds 24, that is acceptable ONLY if each extra skip is a test that genuinely spawns a dist-dependent entrypoint and previously passed for the wrong reason (e.g. asserted a non-zero exit that a missing-module crash also satisfies) — name each such test explicitly in the SUMMARY.

Run the unbuilt check with a guaranteed restore, e.g.: `cd /Users/vtx/brain-memory && mv dist dist.bak && (npx vitest run tests/adapter-capture.test.ts tests/adapter-inject.test.ts tests/episodic-dryrun-gate.test.ts tests/eval-harness-smoke.test.ts tests/locomo-harness.test.ts tests/locomo-latency-curve.test.ts tests/locomo-scorer.test.ts tests/drift-05-harness-smoke.test.ts 2>&1 | tail -8); mv dist.bak dist; ls -d dist`

Record BOTH result lines verbatim in the SUMMARY. `dist/` must be restored before moving on.
  </done>
</task>

<task type="auto">
  <name>Task 2: strip-hidden.test.ts — 17 absolute wall-clock bounds become calibration-relative</name>
  <files>tests/strip-hidden.test.ts</files>
  <action>
Add ONE file-local helper near the top of `tests/strip-hidden.test.ts` (module scope, above the first describe that uses it), mirroring `e2eReferenceElapsedMs` in `tests/gmail-hidden-content.test.ts` lines 43-62 — file-local, NOT in `tests/support/`, because the precedent is file-local and the helper is meaningless outside this entry point:

`function benignReferenceElapsedMs(byteLength: number): number` — build a benign body by repeating `'<p>text</p>'` and slicing to exactly `byteLength`, then time ONE `stripHiddenContent(referenceBody)` call with `performance.now()` and return the elapsed ms. It must go through the SAME entry point as the assertion it calibrates, and be called INSIDE the test body (not hoisted to module init), so both sides share whatever JIT/GC/parallel-fork load is in effect at that moment.

Then convert all 17 `expect(elapsed).toBeLessThan(1000)` occurrences (lines 1263, 1271, 1279, 1290, 1298, 1306, 1385, 1393, 1405, 1483, 1588, 1612, 1620, 1628, 1636, 1644, 1652) to:

  `const referenceElapsed = benignReferenceElapsedMs(<the same byte length as the shape under test>);`
  `expect(elapsed).toBeLessThanOrEqual(10 * Math.max(referenceElapsed, 1));`

The `Math.max(referenceElapsed, 1)` floor and the `10x` multiple are both taken from the existing precedent (`gmail-hidden-content.test.ts` line 528). Use the shape's OWN size for the reference — 64 KB shapes calibrate against a 64 KB benign body, 512 KB against 512 KB, the T62-91 `it.each` block against exactly `CAP` (1,048,576). For the shapes wrapped in `<style>...</style>` (report/V/W/X/X3/Y/Z) calibrate against the nominal byte argument passed to the shape builder; the wrapper's few extra bytes are noise at these sizes.

Update each test NAME to the precedent's form — "…costs at most 10x a same-size benign body (documented budget: 1000ms)…" — so the budget the assertion used to enforce stays visible in CI output rather than disappearing into a comment.

Update the four block-level rationale comments that currently justify the 1000 ms ceilings (above line 1258, above line 1380, above line 1426, and the block at lines 1531-1559) to state the new construction, citing: this plan's measured ratios (0.02-1.52, max 1.52 on the well-formed control at 1 MiB), the 6.6x headroom the 10x bound carries over that max, and the 10^2-10^4 x ratio a returning quadratic produces against the same reference. Do not delete the existing pre-fix millisecond numbers — they are the evidence the bound is not a rubber stamp.

Explicitly UNCHANGED, do not touch:
- every growth-ratio assertion (`t64 / Math.max(t32, 1) <= 8`, `t256 / Math.max(t128, 1) <= 8`, the CR-02 `t64k/t16k` pair) — already relative, and named in-file as "the ReDoS instrument of record"
- the `timeMin` min-of-5 helper at lines 1314-1322
- the `20000` per-describe timeouts
- every correctness assertion (totality, idempotence, parity, leakage, the `liveHidingSelectors` oracle cross-checks)
- `expect(scan.comments[0]!.end).toBeLessThanOrEqual(...)` at lines 959-961 — an ordering assertion, not a perf bound
  </action>
  <verify>
    <automated>cd /Users/vtx/brain-memory &amp;&amp; grep -c "toBeLessThan(1000)" tests/strip-hidden.test.ts; npx vitest run tests/strip-hidden.test.ts 2>&amp;1 | tail -8</automated>
  </verify>
  <done>
- `grep -c "toBeLessThan(1000)" tests/strip-hidden.test.ts` returns **0** (all 17 converted; the count is over assertion text, and the surviving `1000` references live in test names/comments as `budget: 1000ms`, which does not match this pattern).
- `grep -c "benignReferenceElapsedMs" tests/strip-hidden.test.ts` returns **18** (1 definition + 17 call sites).
- `npx vitest run tests/strip-hidden.test.ts` passes with the same test count as before the change (no test added, removed, or skipped).
- Contention check: the file still passes when the whole suite is running around it — `npx vitest run` (Task 3's gate) covers this.
- `grep -c "Math.max(t" tests/strip-hidden.test.ts` is unchanged from before the edit (growth-ratio assertions untouched).
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + typecheck regression gate</name>
  <files>(no files modified — verification only)</files>
  <action>
Verify the two changes hold together and nothing outside `tests/` moved.

1. `npm run typecheck` — must be clean (it typechecks both `tsconfig.json` and `tests/tsconfig.json`, so the new `tests/support/dist-build.ts` is covered).
2. `git diff --stat` — confirm zero files under `src/` or `scripts/` are touched. If any are, revert them; this task is tests-only per the objective's scope lock.
3. Full suite on a BUILT tree: `npm run build && npx vitest run`. Compare the failure list against the pre-change baseline recorded in STATE.md (suite 4029, tsc clean). No NEW failures, and no drop in passing count beyond tests that were already failing for unrelated reasons.
4. Full suite on an UNBUILT tree — the actual point of Task 1. Move `dist/` aside, run `npx vitest run`, restore. The 24 dist-attributable failures must be gone. Any residual failure must be triaged and named in the SUMMARY as either (a) unrelated pre-existing, or (b) a dist-dependent test Task 1 missed — and if (b), gate it using the same helper before closing this task.

Use a restore-guaranteed form for step 4 so a crash cannot leave the tree without `dist/`: `cd /Users/vtx/brain-memory && mv dist dist.bak && (npx vitest run 2>&1 | tail -20); mv dist.bak dist; ls -d dist`
  </action>
  <verify>
    <automated>cd /Users/vtx/brain-memory &amp;&amp; npm run typecheck &amp;&amp; git diff --stat -- src scripts | tail -3 &amp;&amp; npx vitest run 2>&amp;1 | tail -8</automated>
  </verify>
  <done>
- `npm run typecheck` exits 0.
- `git diff --stat -- src scripts` produces NO output (no source or script file modified).
- Built-tree full suite shows no new failures versus the STATE.md baseline; the pass count and the exact `Test Files` / `Tests` summary lines are recorded verbatim in the SUMMARY.
- Unbuilt-tree full suite shows zero dist-attributable failures; the summary lines are recorded verbatim, and every residual failure is named with its triage verdict.
- `dist/` exists on disk when the task ends.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Tests-only change. No new runtime input path, no new dependency, no network or filesystem surface added to shipped code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1vg-01 | Tampering (coverage erosion) | `tests/support/dist-build.ts` gate | mitigate | The gate can silently disable real coverage on a built tree. Task 1's `<done>` requires `0 skipped` when `dist/` is present — an over-firing gate fails the task. |
| T-1vg-02 | Tampering (assertion erosion) | `tests/strip-hidden.test.ts` bounds | mitigate | A relative bound can be made unfalsifiable. Multiple fixed at 10x against a measured max ratio of 1.52 (6.6x headroom); the guarded regressions measured 10^2-10^4 x the same reference, so the bound still fires. Growth-ratio assertions left UNCHANGED as the independent ReDoS instrument. |
| T-1vg-03 | Denial of Service | `stripHiddenContent` on attacker-supplied email HTML | accept | Unchanged by this plan — the ReDoS surface and its bounds remain; only the assertion's frame of reference changes. Residual T-62-54 and its cap remain as previously documented. |
| T-1vg-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. No `package.json` change. |
</threat_model>

<verification>
1. `npm run typecheck` — clean.
2. `git diff --stat -- src scripts` — empty.
3. `npx vitest run` on a built tree — no new failures vs. the STATE.md baseline.
4. `npx vitest run` on an unbuilt tree (`dist/` moved aside, then restored) — zero dist-attributable failures; the 8 files report skips with reasons naming the missing entrypoint and `npm run build`.
5. `grep -c "toBeLessThan(1000)" tests/strip-hidden.test.ts` → 0.
6. `npx vitest run tests/strip-hidden.test.ts` — passes standalone with an unchanged test count.
</verification>

<success_criteria>
- Fresh worktree, no build: `npx vitest run` produces zero failures caused by a missing `dist/` — dist-dependent tests skip with an actionable message.
- Built tree: the 8 gated files still report 54 passed / 0 skipped — no coverage lost.
- All 17 strip-hidden wall-clock bounds are ratios against a same-size benign body measured through `stripHiddenContent` in the same invocation, at 10x with a `Math.max(ref, 1)` floor, with the 1000 ms budget preserved in the test names.
- Growth-ratio assertions, correctness assertions, and `timeMin` are untouched.
- `npm run typecheck` green; nothing under `src/` or `scripts/` modified.
</success_criteria>

<output>
Create `.planning/quick/260809-1vg-test-suite-hygiene-dist-dependent-tests-/260809-1vg-SUMMARY.md` when done.

The SUMMARY must record verbatim:
- the built-tree and unbuilt-tree `Test Files` / `Tests` summary lines for the 8 gated files (Task 1)
- the built-tree and unbuilt-tree full-suite summary lines (Task 3)
- any skip beyond the 24-failure baseline, named and justified
- any residual unbuilt-tree failure, named with its triage verdict
</output>
