---
type: quick
slug: 260714-g0s-move-stats-entry-point-from-settings-pan
wave: 1
autonomous: true
files_modified:
  - src/viz/index.html
  - src/viz/modules/hud.js
  - src/viz/modules/settings.js
  - src/viz/css/styles.css
  - tests/viz-settings-panel.test.ts
  - tests/viz-frontend-static.test.ts
must_haves:
  truths:
    - "A histogram-icon button in the HUD rail opens the stats dashboard"
    - "The settings panel no longer shows the 'View usage stats →' link"
    - "The settings panel keeps its per-toggle usage lines (appendUsageLines) unchanged"
    - "The new rail button is auto-hidden in the compact tray popover"
  artifacts:
    - path: src/viz/index.html
      provides: "#rail-stats button between #rail-corpus and #rail-recenter"
    - path: src/viz/modules/hud.js
      provides: "#rail-stats click → ctx.openStatsDashboard()"
  key_links:
    - from: src/viz/modules/hud.js
      to: ctx.openStatsDashboard
      via: "click listener on #rail-stats"
      pattern: "rail-stats"
---

<objective>
Move the stats-dashboard entry point out of the settings panel and into the HUD rail
as a histogram-icon button (founder D-04 revision, 2026-07-14: "usage is fine, lets
move it out of settings and add it to the rail maybe with like histogram icon").

- ADD: `#rail-stats` icon button in `#hud-rail`, wired to `ctx.openStatsDashboard()`.
- REMOVE: the `.settings-usage-link` ("View usage stats →") creation + wiring from
  settings.js and its CSS rule.
- KEEP: per-toggle usage lines (`appendUsageLines`) untouched; the ⌘K "Open stats"
  palette command stays as-is.
- UPDATE: tests to assert the link is GONE and the rail button exists + wires correctly.

Purpose: single, discoverable stats entry point in the view-opening rail cluster,
not buried in settings.
Output: rail button, cleaned settings panel, updated test coverage.
</objective>

<context>
@.planning/STATE.md
@./CLAUDE.md

Design constraints (load-bearing, Phase-59 rail conventions):
- Icon-only button, 16x16 inline SVG, viewBox `0 0 24 24`, `stroke="currentColor"`,
  `stroke-width="2"` — histogram = three vertical bars of differing heights.
- `aria-label` + `title` = "Open usage stats". No new colors, no amber, no new deps.
- textContent-only for any dynamic value (none expected here).
- Compact tray popover must NOT show the button — the existing
  `#hud-rail button { display: none; }` compact rule (styles.css ~562) already covers
  any descendant button; only `#rail-recenter` is re-shown. VERIFY, do not add CSS.
- Placement: adjacent to `#rail-corpus` (view-opening cluster), BEFORE `#rail-recenter`.

Existing rail markup (index.html ~100-136): `#btn-settings`, `#rail-corpus`,
`#rail-recenter`, `#btn-search` — inline outline SVG icons.

Existing rail wiring precedent (hud.js ~29, ~135-140):
```
const btnSearch = document.getElementById('btn-search');
if (btnSearch) {
  btnSearch.addEventListener('click', () => {
    if (typeof ctx.openPalette === 'function') ctx.openPalette();
  });
}
```

Stats-dashboard ctx surface (stats-dashboard.js ~140-142): exposes
`ctx.openStatsDashboard(tab)` / `ctx.closeStatsDashboard()` / `ctx.isStatsDashboardOpen()`.
The click handler reads `ctx.openStatsDashboard` at click time, so init ordering is
irrelevant (guard with `typeof … === 'function'`).

Removal target (settings.js ~258-267): the `.settings-usage-link` `<a>` block created
after the last `appendDivider`, before the save button. CSS rule at styles.css ~1182-1192.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add #rail-stats histogram button and wire it to ctx.openStatsDashboard</name>
  <read_first>
    - src/viz/index.html (~92-136 — #hud-rail block, icon SVG convention)
    - src/viz/modules/hud.js (~25-29 getElementById block, ~135-140 btn-search wiring)
    - src/viz/css/styles.css (~205 #hud-rail button base, ~550-578 compact popover block)
  </read_first>
  <action>
    In src/viz/index.html, insert a new button in `#hud-rail` immediately AFTER the
    `#rail-corpus` button's closing `</button>` (line ~116) and BEFORE the
    `#rail-recenter` comment/button (line ~117). Match the surrounding markup exactly:

      `<button id="rail-stats" type="button" aria-label="Open usage stats" title="Open usage stats">`
      containing a 16x16 inline SVG, `viewBox="0 0 24 24" fill="none" stroke="currentColor"
      stroke-width="2" stroke-linecap="round" stroke-linejoin="round"`, drawing a histogram
      of three vertical bars of differing heights, e.g. three `<line>` elements:
      `x1=6 y1=20 x2=6 y2=13`, `x1=12 y1=20 x2=12 y2=7`, `x1=18 y1=20 x2=18 y2=16`,
      plus a baseline `<line x1=3 y1=20 x2=21 y2=20>` if it reads cleaner. Bars only is fine.
      Add a brief comment noting D-04 revision (stats moved from settings into the rail).

    In src/viz/modules/hud.js, add `const btnStats = document.getElementById('rail-stats');`
    alongside the existing `btnSearch` lookup (~29), and add a click-wiring block mirroring
    the btn-search precedent (~135-140):
      guard `if (btnStats)`, add a `'click'` listener that calls
      `ctx.openStatsDashboard()` behind a `typeof ctx.openStatsDashboard === 'function'` guard.
    Do NOT pass a tab arg — `openStatsDashboard()` defaults to the 'usage' tab.

    Do NOT add any CSS. Verify (read-only) that the compact rule
    `#hud-rail button { display: none; }` (styles.css ~562) already hides the new button
    and that only `#rail-recenter` is re-shown below it — no new rule needed.
  </action>
  <verify>
    grep -n 'id="rail-stats"' src/viz/index.html
    grep -n 'rail-stats' src/viz/modules/hud.js
    npx vitest run tests/viz-frontend-static.test.ts
  </verify>
  <done>
    #rail-stats button exists in index.html between #rail-corpus and #rail-recenter with
    aria-label/title "Open usage stats" and a histogram SVG; hud.js wires its click to
    ctx.openStatsDashboard() with a typeof guard; no CSS changes made.
  </done>
  <acceptance_criteria>
    - `grep -c 'id="rail-stats"' src/viz/index.html` returns 1
    - `grep -q 'aria-label="Open usage stats"' src/viz/index.html` succeeds
    - index.html: #rail-stats appears AFTER #rail-corpus and BEFORE #rail-recenter
      (`grep -n 'id="rail-corpus"\|id="rail-stats"\|id="rail-recenter"' src/viz/index.html`
       shows line order corpus < stats < recenter)
    - `grep -q "getElementById('rail-stats')" src/viz/modules/hud.js` succeeds
    - `grep -q 'openStatsDashboard' src/viz/modules/hud.js` succeeds
    - `git diff --stat src/viz/css/styles.css` shows no changes from this task
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove the .settings-usage-link from settings.js and its CSS rule</name>
  <read_first>
    - src/viz/modules/settings.js (~16-18 header D-04 note, ~256-277 divider/link/save block)
    - src/viz/css/styles.css (~1182-1193 .settings-usage-link rule + comment)
  </read_first>
  <action>
    In src/viz/modules/settings.js, delete the "Stats dashboard link (D-04)" block
    (~258-267): the `usageLink` `<a>` element creation, its `className`/`href`/`textContent`,
    its click listener calling `ctx.openStatsDashboard`, and the `bodyEl.appendChild(usageLink)`.
    Leave the `appendDivider(bodyEl)` above it AND the save button below it intact.
    Do NOT touch `appendUsageLines` or any of its call sites (core/schema/corpus per-toggle
    lines stay). Update the module header comment (~16-18) so it no longer claims a
    "View usage stats →" link exists — note the entry point moved to the HUD rail (D-04 revision).

    In src/viz/css/styles.css, delete the `.settings-usage-link` and
    `.settings-usage-link:hover` rules plus their block comment (~1182-1192).
  </action>
  <verify>
    grep -c 'settings-usage-link' src/viz/modules/settings.js src/viz/css/styles.css
    grep -c 'appendUsageLines' src/viz/modules/settings.js
  </verify>
  <done>
    No `.settings-usage-link` creation or CSS rule remains; appendUsageLines and its three
    call sites are unchanged; the save button still renders after the final divider.
  </done>
  <acceptance_criteria>
    - `grep -c 'settings-usage-link' src/viz/modules/settings.js` returns 0
    - `grep -c 'settings-usage-link' src/viz/css/styles.css` returns 0
    - `grep -c 'openStatsDashboard' src/viz/modules/settings.js` returns 0
    - `grep -c 'appendUsageLines(bodyEl' src/viz/modules/settings.js` returns 3
    - `grep -q 'settings-save-btn' src/viz/modules/settings.js` succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update tests — assert link is GONE, assert rail button exists + wires</name>
  <read_first>
    - tests/viz-settings-panel.test.ts (file header ~15-21; D-04 tests ~513-570)
    - tests/viz-frontend-static.test.ts (~44-73 corpus/hudSrc setup, ~115-143 chrome id skeleton)
  </read_first>
  <action>
    In tests/viz-settings-panel.test.ts:
    - Replace the test `renders "View usage stats →" link that opens the stats takeover (D-04)`
      (~539-556) with a test that opens/renders the panel and asserts the link is GONE:
      `findFirst(fakeBody, '.settings-usage-link')` is `null`, and `collectText(fakeBody)`
      does NOT contain `'View usage stats'`.
    - Remove the now-moot test `does not throw when clicking the usage link and
      ctx.openStatsDashboard is absent` (~558-563) — there is no link to click.
    - Keep the per-toggle usage-line test (~530-537) and the
      `does not render the old standalone 30d/all-time readout block` test (~565-570) intact.
    - Update the file header comment (~15-21) and the describe title (~515) to reflect that
      the D-04 in-settings link was removed (entry point moved to the HUD rail).

    In tests/viz-frontend-static.test.ts, add assertions (the suite already exposes `html`
    and `hudSrc`). In or next to the `contains the full HTML chrome id skeleton` block (~115):
    - `expect(html).toContain('id="rail-stats"')`
    - `expect(html).toMatch(/<button[^>]*id="rail-stats"[^>]*aria-label="Open usage stats"/)`
    - `expect(hudSrc).toContain('rail-stats')` and `expect(hudSrc).toContain('openStatsDashboard')`
      (rail button is wired to the stats dashboard in hud.js).
    Follow the existing `it(...)`/`expect` style; no new imports needed.
  </action>
  <verify>
    npx vitest run tests/viz-settings-panel.test.ts tests/viz-frontend-static.test.ts
  </verify>
  <done>
    Both test files pass. Settings-panel suite asserts the usage link is absent; frontend-static
    suite asserts #rail-stats exists in index.html with the correct aria-label and that hud.js
    wires it to openStatsDashboard.
  </done>
  <acceptance_criteria>
    - `npx vitest run tests/viz-settings-panel.test.ts tests/viz-frontend-static.test.ts` exits 0
    - `grep -c "View usage stats" tests/viz-settings-panel.test.ts` returns 0 OR only appears in
      a negative assertion (`not.toContain`/`toBeNull` context) — no positive render assertion remains
    - `grep -q "id=\"rail-stats\"" tests/viz-frontend-static.test.ts` succeeds
    - `grep -q 'openStatsDashboard' tests/viz-frontend-static.test.ts` succeeds
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npx vitest run` (full suite) passes — no regressions in viz-settings-panel,
  viz-frontend-static, viz-hud-palette, or viz-stats-routes.
- Manual (optional): open the viz, confirm the histogram button sits between the corpus
  and recenter icons, opens the stats dashboard on click, and is absent in the compact
  tray popover; confirm the settings panel no longer shows "View usage stats →".
</verification>

<success_criteria>
- Histogram rail button (`#rail-stats`) opens the stats dashboard via ctx.openStatsDashboard().
- `.settings-usage-link` and its CSS rule are fully removed; per-toggle usage lines intact.
- Compact popover hides the new button via the existing `#hud-rail button` rule (no new CSS).
- Tests updated: link-gone assertion + rail-button-exists/wired assertions; full suite green.
</success_criteria>

<output>
Create `.planning/quick/260714-g0s-move-stats-entry-point-from-settings-pan/260714-g0s-SUMMARY.md` when done.
</output>
