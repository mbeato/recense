---
phase: 35-recency-strength-retrieval-ranking
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - scripts/eval/replay-ku-harness.cjs
  - scripts/eval/longmemeval-harness.cjs
  - scripts/eval/35-strength-sweep.cjs
autonomous: false
requirements: [RANK-02]

must_haves:
  truths:
    - "The KU harness passes queryText so retrieveRanked routes through hybridTopk — different w values now produce different scores"
    - "Both harnesses accept --strength-weight, threading it into config (KU) and the direct hybridTopk call (LME)"
    - "A sweep over w ∈ {0,0.25,0.5,1.0,2.0} produces a comparison table and names a winning w"
    - "The phase passes if EITHER token-saving OR precision clears the ~1–2pt judge-noise band, with no regression on the other (D-06/D-07)"
  artifacts:
    - path: "scripts/eval/replay-ku-harness.cjs"
      provides: "queryText fix (Pitfall 3) + --strength-weight flag"
      contains: "strength-weight"
    - path: "scripts/eval/longmemeval-harness.cjs"
      provides: "--strength-weight threaded into the direct hybridTopk call"
      contains: "strength-weight"
    - path: "scripts/eval/35-strength-sweep.cjs"
      provides: "w-sweep driver producing a comparison table + winning w"
      contains: "0.25"
  key_links:
    - from: "scripts/eval/35-strength-sweep.cjs"
      to: "harness --strength-weight"
      via: "spawns each harness per w in the grid"
      pattern: "strength-weight"
    - from: "scripts/eval/replay-ku-harness.cjs retrieveRanked"
      to: "hybridTopk (via queryText)"
      via: "kuCase.question passed as 4th arg"
      pattern: "kuCase.question"
---

<objective>
Make RANK-02 measurable and then measure it. First fix the silent no-op (the KU harness calls
`retrieveRanked` without `queryText`, so the strength fusion is never exercised — RESEARCH Critical
Gap / Pitfall 3), add a `--strength-weight` flag to both harnesses, and build a sweep driver. Then
run the baseline + sweep on the paid KU/LongMemEval harness, record results, and report the winning
`w` with a pass/fail verdict against D-06/D-07.

Purpose: D-04 ships the mechanism dark; RANK-02 demands an eval-backed win (no regression + a token
or precision gain) before anyone flips the default. This plan produces that evidence.
Output: Patched harnesses, a sweep script, recorded result JSONs, and a verdict in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-recency-strength-retrieval-ranking/35-CONTEXT.md
@.planning/phases/35-recency-strength-retrieval-ranking/35-RESEARCH.md
@.planning/phases/35-recency-strength-retrieval-ranking/35-VALIDATION.md
@.planning/phases/35-recency-strength-retrieval-ranking/35-01-SUMMARY.md

<interfaces>
<!-- Verified against live source 2026-06-20. Plan 35-01 must be merged first (depends_on). -->

scripts/eval/replay-ku-harness.cjs:239 — the no-op call to fix (Pitfall 3):
  const results = engine.retrieveRanked(queryVec, scratch.config.rankedRetrievalK, scratch.config.rankedRetrievalFloor);
  // MISSING 4th arg → routes through pure cosine topk → strength fusion NEVER runs.
  // kuCase.question is in scope (used at L~230 for the embed). Pass it as queryText.

scripts/eval/longmemeval-harness.cjs:683-685 — the direct hybridTopk call:
  const topkResults = IS_HYBRID
    ? evalRetriever.hybridTopk(queryVec, questionForEmbed, TOP_K)
    : evalRetriever.topk(queryVec, TOP_K);
  // After 35-01, append: , undefined, STRENGTH_WEIGHT, Date.now(), DEFAULT_CONFIG.lambda

Config injection pattern (both harnesses today):
  const config = { ...DEFAULT_CONFIG, dbPath };
  // KU: route w through engine.config by adding rankStrengthWeight: STRENGTH_WEIGHT here.
  // LME: hybridTopk is called directly (not via engine), so pass STRENGTH_WEIGHT to hybridTopk too.

Hybrid signature after 35-01 (from 35-01-SUMMARY / topk.ts):
  hybridTopk(queryVec, queryText, k, preK?, strengthWeight=0, nowMs?, lambda?)

Metrics emitted:
  KU  → { ku_score, ku_correct, ku_scored_cases, ... }  (replay-ku-harness.cjs)
  LME → scorer { scores: { headline, by_category{ knowledge-update, ... } } }  (longmemeval-scorer.cjs)

Existing sweep-style precedents to mirror: scripts/eval/eval02-sweep.sh, scripts/eval/sweep-all.sh.
Baseline/sweep commands: 35-RESEARCH.md Focus Area 4 "Baseline commands" + "W-sweep approach".
Requires OPENAI_API_KEY + ANTHROPIC_API_KEY and the uncommitted scripts/eval/longmemeval-s.jsonl.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix KU harness queryText, add --strength-weight to both harnesses, build the sweep driver</name>
  <files>scripts/eval/replay-ku-harness.cjs, scripts/eval/longmemeval-harness.cjs, scripts/eval/35-strength-sweep.cjs</files>
  <read_first>
    - scripts/eval/replay-ku-harness.cjs (CLI parsing top of file; config build L~97; the retrieveRanked call L239; kuCase.question usage)
    - scripts/eval/longmemeval-harness.cjs (CLI parsing + --topk/--hybrid flags L~150; config build L~185; the hybridTopk call L683-685)
    - scripts/eval/eval02-sweep.sh and scripts/eval/sweep-all.sh (existing sweep style to mirror)
    - 35-RESEARCH.md Focus Area 4 (Critical Gap, remediation option 1, config injection pattern, W-sweep approach), Pitfall 3
    - 35-VALIDATION.md rows `harness-fix` + `w-sweep`
  </read_first>
  <action>
    In scripts/eval/replay-ku-harness.cjs: pass `kuCase.question` as the 4th `queryText` arg to
    `engine.retrieveRanked(queryVec, k, floor, kuCase.question)` at L239 (Pitfall 3 — without this the sweep is
    a silent no-op). Add a `--strength-weight <w>` CLI flag (default 0), parse it near the existing arg parsing,
    and inject it into the config build as `rankStrengthWeight: STRENGTH_WEIGHT` (so engine.retrieveRanked →
    hybridTopk reads it from engine.config).

    In scripts/eval/longmemeval-harness.cjs: add the same `--strength-weight <w>` flag (default 0). Because the
    harness calls `hybridTopk` directly on `evalRetriever` (not through the engine), thread the value into that
    call: when IS_HYBRID, `evalRetriever.hybridTopk(queryVec, questionForEmbed, TOP_K, undefined,
    STRENGTH_WEIGHT, Date.now(), DEFAULT_CONFIG.lambda)`. Leave the non-hybrid `topk` branch unchanged.

    Create scripts/eval/35-strength-sweep.cjs: a small Node driver (mirror eval02-sweep.sh's structure) that,
    for each `w` in `{0, 0.25, 0.5, 1.0, 2.0}` (D-05 grid; Claude's-discretion per CONTEXT), spawns the chosen
    harness with `--strength-weight w` (KU by default; `--lme` switch to drive the LongMemEval harness +
    scorer), writes each run's JSON into scripts/eval/results/35-sweep-w<value>.json, and prints a comparison
    table (w, headline/ku_score, and for the LME path the knowledge-update sub-score) plus the argmax `w`. The
    driver must NOT hardcode API keys — it reads them from the environment. See 35-RESEARCH.md Focus Area 4 for
    the baseline/sweep commands; do NOT inline a code block in this action.
  </action>
  <verify>
    <automated>node --check scripts/eval/replay-ku-harness.cjs && node --check scripts/eval/longmemeval-harness.cjs && node --check scripts/eval/35-strength-sweep.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Source (Pitfall 3 fix, VALIDATION `harness-fix`): `grep -n "kuCase.question" scripts/eval/replay-ku-harness.cjs` shows it passed to retrieveRanked (4th arg, not only the embed line).
    - Source: `grep -n "strength-weight" scripts/eval/replay-ku-harness.cjs scripts/eval/longmemeval-harness.cjs` shows the flag parsed in both.
    - Source: `grep -n "rankStrengthWeight" scripts/eval/replay-ku-harness.cjs` shows it injected into the config build.
    - Source: `grep -n "STRENGTH_WEIGHT" scripts/eval/longmemeval-harness.cjs` shows it threaded into the direct hybridTopk call (5th positional arg).
    - Source: `grep -n "0.25" scripts/eval/35-strength-sweep.cjs` shows the w grid.
    - Syntax: `node --check` passes on all three files (no paid run yet).
    - No new runtime deps added (D-11): `git diff --stat package.json` shows package.json unchanged.
  </acceptance_criteria>
  <done>KU harness exercises hybridTopk via queryText; both harnesses accept --strength-weight and thread it correctly; sweep driver exists and parses; net-zero deps.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run the paid baseline + w-sweep, record results, capture the D-06/D-07 verdict</name>
  <files>scripts/eval/results/35-sweep-w0.json, .planning/phases/35-recency-strength-retrieval-ranking/35-02-SUMMARY.md</files>
  <read_first>
    - 35-RESEARCH.md Focus Area 4 ("Baseline commands" + "Mapping to D-06 measurements" + "W-sweep approach")
    - 35-CONTEXT.md D-05/D-06/D-07 (sweep + either-wins + noise band)
    - 35-VALIDATION.md `w-sweep` row + "Manual-Only Verifications" table
    - .planning/STATE.md "Budget Constraints" (~$14–15 remaining; ≥$3 needs explicit approval)
  </read_first>
  <action>
    Pause for the founder — this run is paid and needs their API keys plus the uncommitted dataset; it cannot
    run autonomously. After Task 1's `--strength-weight` plumbing is merged, the founder runs the KU sweep first
    (cheap), then optionally the LME arms if budget is approved. On resume, the executor reads the result JSONs,
    writes the comparison table + winning w + the D-06/D-07 pass/fail verdict into 35-02-SUMMARY.md. Do NOT
    proceed past this checkpoint without the recorded results.
  </action>
  <what-built>
    Plan 35-01 merged the dark-default strength mechanism; Task 1 above fixed the KU harness no-op, added
    `--strength-weight` to both harnesses, and built scripts/eval/35-strength-sweep.cjs. Everything is ready to
    measure RANK-02. The actual run is paid (OpenAI embed + Haiku/Sonnet judge on subscription/API) and needs
    the founder's API keys plus the uncommitted scripts/eval/longmemeval-s.jsonl — it cannot run autonomously.
  </what-built>
  <how-to-verify>
    Budget note: STATE.md records ~$14–15 remaining and "explicit approval required for any run ≥$3". Run the
    cheaper KU sweep first; only run the LME sweep if the KU signal is inconclusive and budget allows.

    1. Confirm keys are exported: `OPENAI_API_KEY` and `ANTHROPIC_API_KEY` set in the shell.
    2. Build: `npm run build`
    3. KU baseline + sweep (cheaper): run `node scripts/eval/35-strength-sweep.cjs` (default KU path). It writes
       scripts/eval/results/35-sweep-w<value>.json for w ∈ {0,0.25,0.5,1.0,2.0} and prints the comparison table.
       Sanity check (Pitfall 3): the w values must NOT all produce the same ku_score — differing scores confirm
       the queryText fix actually exercises strength fusion.
    4. (Optional, if budget approved ≥$3) LME precision/token arms (D-06): probe + score at two budgets, e.g.
       `--topk 10` (precision) and `--topk 5` (token-saving), at w=0 baseline vs the KU-winning w. See
       35-RESEARCH.md Focus Area 4 "Baseline commands" for the exact probe/scorer invocations, or drive via
       `node scripts/eval/35-strength-sweep.cjs --lme`.
    5. Paste the comparison table(s) / result JSON paths back here, including the winning w and the headline
       metric (token-saving vs precision).

    Pass bar (D-06/D-07): the phase passes if EITHER (a) equal quality at a smaller inject budget (token-saving)
    OR (b) higher accuracy at the same budget (precision) clears the ~1–2pt judge-noise band decisively, AND the
    non-winning metric does not regress beyond that band.
  </how-to-verify>
  <verify>
    <human-check>Founder ran the KU sweep (and optionally LME); result JSONs exist under scripts/eval/results/ and show non-uniform scores across w; the winning w (or "no win") and verdict are pasted back.</human-check>
  </verify>
  <resume-signal>Paste the sweep table + winning w (or "no win — all within noise"), then type "recorded" to have the verdict written into the SUMMARY.</resume-signal>
  <acceptance_criteria>
    - Result files exist under scripts/eval/results/ for each swept w (the executor records the paths after resume).
    - The recorded sweep shows differing scores across w (confirms the queryText fix is live — not a silent no-op).
    - A winning w is named, OR an honest "no decisive win within noise" verdict is recorded (D-07).
    - The SUMMARY states the headline metric (token-saving OR precision) and the pass/fail decision against D-06/D-07.
  </acceptance_criteria>
  <done>Baseline + sweep run with the founder's keys; results recorded under scripts/eval/results/; winning w (or no-win) and a D-06/D-07 verdict captured in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment (API keys) → eval harness | OPENAI_API_KEY / ANTHROPIC_API_KEY are read from env for the paid run; risk is accidental disclosure (logging/transcript), not injection. |
| uncommitted dataset (longmemeval-s.jsonl) → harness | Local trusted file; no external input crosses into engine code under test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Information disclosure | API keys in harness/sweep output | mitigate | Sweep driver reads keys from env and never logs or echoes them; no key is written into result JSONs. (STATE.md already flags a prior OPENAI key exposure → rotate; do not print keys.) |
| T-35-05 | Tampering / measurement integrity | sweep producing a false "win" | accept | The harness math is unchanged code under test; the queryText-fix sanity check (differing scores across w) guards against a silent no-op. Run-to-run judge variance is handled by the D-07 noise band, not a code mitigation. |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No package installs — D-11 net-zero new runtime deps; eval scripts use existing better-sqlite3/openai/@anthropic-ai/sdk only. |
</threat_model>

<verification>
- `node --check` passes on all three modified/created scripts (no paid run needed for the code gate).
- KU sweep produces non-uniform ku_score across w (proves the Pitfall 3 fix is live).
- Recorded result JSONs exist under scripts/eval/results/ for each swept w.
- A winning w (or an honest no-win) and a D-06/D-07 pass/fail verdict are captured in the SUMMARY.
</verification>

<success_criteria>
- RANK-02 measurable: the strength weight actually moves harness scores (no silent no-op).
- A sweep over {0,0.25,0.5,1.0,2.0} reports a winning w.
- Phase passes if EITHER token-saving OR precision clears the judge-noise band with no regression on the other (D-06/D-07).
- Net-zero new runtime deps (D-11); no API keys leaked.
</success_criteria>

<output>
Create `.planning/phases/35-recency-strength-retrieval-ranking/35-02-SUMMARY.md` when done (include the sweep table, winning w, and the D-06/D-07 verdict).
</output>
