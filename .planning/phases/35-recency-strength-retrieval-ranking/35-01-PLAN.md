---
phase: 35-recency-strength-retrieval-ranking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/retrieval/topk.ts
  - src/strength/decay.ts
  - src/lib/config.ts
  - src/retrieval/engine.ts
  - tests/fts-retrieval.test.ts
autonomous: true
requirements: [RANK-01]

must_haves:
  truths:
    - "rrfFuse accepts an optional per-list weight; w=0 (or omitted) reproduces today's exact fused ranking"
    - "hybridTopk builds a strength-ranked third list ONLY from the cosine∪BM25 pool, and only when strengthWeight > 0"
    - "A cue-based retrieval read never mutates s or last_access (no self-confirmation)"
    - "rankStrengthWeight ships at 0 (dark) — zero behavior change on the hot path at merge (D-04)"
  artifacts:
    - path: "src/retrieval/topk.ts"
      provides: "rrfFuse weights param; hybridTopk strengthWeight/nowMs/lambda params; stmtPoolStrength prepared statement"
      contains: "weights?: number[]"
    - path: "src/strength/decay.ts"
      provides: "single shared pure effective_s computation callable from topk.ts (no inline re-derivation)"
      contains: "effectiveStrength"
    - path: "src/lib/config.ts"
      provides: "rankStrengthWeight knob, default 0"
      contains: "rankStrengthWeight"
    - path: "src/retrieval/engine.ts"
      provides: "retrieveRanked passes strength weight only on the hybrid (queryText) branch"
      contains: "rankStrengthWeight"
    - path: "tests/fts-retrieval.test.ts"
      provides: "T1..T5 + no-self-strengthen unit/integration tests"
      contains: "strengthWeight"
  key_links:
    - from: "src/retrieval/topk.ts"
      to: "src/strength/decay.ts effectiveStrength"
      via: "pure shared computation (no inline s·exp(−λΔt) copy)"
      pattern: "effectiveStrength"
    - from: "src/retrieval/engine.ts retrieveRanked"
      to: "hybridTopk(strengthWeight, nowMs, lambda)"
      via: "queryText branch only"
      pattern: "rankStrengthWeight"
---

<objective>
Build the strength-fusion mechanism (RANK-01) entirely in-engine: extend `rrfFuse` with an
optional per-list weight, add a strength-ranked third RRF list inside `hybridTopk` sourced
ONLY from the existing candidate pool (D-02), add the `rankStrengthWeight: 0` config knob (D-04),
wire it through `retrieveRanked` on the cue-based path only (D-08), and lock the behavior with
unit/integration tests T1..T5 + a no-self-strengthen assertion.

Purpose: Today `effective_s` is computed but only used for eviction; it never enters cue-based
ranking. This gives a strongly-reinforced recent belief a tunable, scale-agnostic, rank-based
nudge over a stale weak one at equal similarity — LLM-free, net-zero new deps, dark by default.
Output: A merged mechanism that is byte-identical to today's ranking until `rankStrengthWeight`
is raised (proving the win is Plan 02's job).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-recency-strength-retrieval-ranking/35-CONTEXT.md
@.planning/phases/35-recency-strength-retrieval-ranking/35-RESEARCH.md
@.planning/phases/35-recency-strength-retrieval-ranking/35-VALIDATION.md

<interfaces>
<!-- Exact contracts the executor needs. Verified against live source 2026-06-20. Do not re-explore. -->

src/retrieval/topk.ts:58 — current rrfFuse (NO weights param today):
  export function rrfFuse(lists: Array<Array<{ id: string }>>, k = 60, topK = 10):
    Array<{ id: string; rrfScore: number }>
  Body: per list, scores += 1 / (k + rank + 1). Sort desc, slice topK.

src/retrieval/topk.ts:181 — current hybridTopk (3 params + preK):
  hybridTopk(queryVec: Float32Array, queryText: string, k: number, preK = k * 3):
    Array<{ id: string; score: number }>
  Builds cosineList = this.topk(queryVec, preK); bm25List from stmtBm25 (preK);
  fused = rrfFuse([cosineList, bm25List], 60, k);
  cosineScoreMap = new Map(cosineList.map(h => [h.id, h.score]));
  returns fused.map(f => ({ id, score: cosineScoreMap.get(f.id) ?? 0 }))  // PRESERVE EXACTLY (Pitfall 2)

src/strength/decay.ts:93 — the ONLY legal strength call from retrieval (pure, no DB writes):
  effectiveStrength(s, lastAccessMs, nowMs, lambda): number
    deltaDays = Math.max(0, nowMs - lastAccessMs) / 86_400_000   // L-8 clamp is load-bearing
    return s * Math.exp(-lambda * deltaDays)
  NOTE: currently an INSTANCE method on StrengthDecayManager but uses no `this`. CandidateRetriever
  has no StrengthDecayManager instance — see Task 2 action for how to share it without re-deriving.
  DO NOT call materializeDecay (decay.ts:106) — it mutates s/last_access (Pitfall 4).

src/retrieval/engine.ts:388 — retrieveRanked routing (the wire-up site):
  const hits = queryText
    ? this.retriever.hybridTopk(queryVec, queryText, k)   // ← strength params added here
    : this.retriever.topk(queryVec, k);                   // ← pure-cosine branch UNCHANGED (D-08)
  Floor gate at engine.ts:406-414 gates on hit.score (the cosine component) — must stay intact.
  Engine has this.clock (nowMs()) and this.config (lambda, weights).

src/lib/config.ts — DEFAULT_CONFIG anchors: lambda:0.05 (L608), candidateK:5 (L634),
  rankWeightS:1.0 (L642), rankWeightR:0.0 (L643 — D-24 caveat: held at 0), injectionTokenBudget:500 (L644).
  EngineConfig interface: rankWeightS (L310), rankWeightR (L318), injectionTokenBudget (L325).

src/retrieval/engine.ts:246 — reference only, DO NOT MODIFY (D-08, cue-less path):
  effectiveStrength(row.s, row.last_access, nowMs, this.config.lambda) usage pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend rrfFuse with an optional per-list weight + lock w=0 regression</name>
  <files>src/retrieval/topk.ts, tests/fts-retrieval.test.ts</files>
  <read_first>
    - src/retrieval/topk.ts:52-73 (rrfFuse — the exact body to extend)
    - tests/fts-retrieval.test.ts (existing rrfFuse unit tests ~L127-158 — match their assertion style)
    - 35-RESEARCH.md Focus Area 1 (the literal diff shape for the weights param + dark-default math proof)
    - 35-VALIDATION.md Per-Task Verification Map rows `mechanism` + `dark-default`
  </read_first>
  <behavior>
    - T1 (w=0 regression): rrfFuse([listA, listB, []], 60, 10, [1,1,0]) maps to the SAME id order as rrfFuse([listA, listB], 60, 10) (no weights). Deep-equal on the id arrays.
    - T2 (weighted boost): given cosineList=[low,high], bm25List=[], strengthList=[high,low], the unweighted fuse puts `low` first; rrfFuse([cosine,bm25,strength],60,10,[1,1,0.5]) puts `high` first.
  </behavior>
  <action>
    Add an optional 4th param `weights?: number[]` to `rrfFuse` (topk.ts:58). Change the inner loop to a
    weighted contribution: for list index `li`, `const w = weights?.[li] ?? 1;` then
    `scores.set(hit.id, (scores.get(hit.id) ?? 0) + w / (k + rank + 1));`. Omitted `weights` ⇒ every list
    weight 1 ⇒ byte-identical to today (D-04 dark invariant at the primitive level). Do NOT change the
    sort/slice/return shape. See 35-RESEARCH.md Focus Area 1 for the exact diff; do NOT inline a code block here.
    Then add tests T1 and T2 to tests/fts-retrieval.test.ts using the existing rrfFuse test style.
  </action>
  <verify>
    <automated>npm test -- tests/fts-retrieval.test.ts -t "rrfFuse"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "weights?: number\[\]" src/retrieval/topk.ts` returns the rrfFuse signature line.
    - Source: `grep -n "weights?.\[" src/retrieval/topk.ts` shows the per-list weight lookup inside the loop.
    - Behavior (T1, VALIDATION `dark-default` row): w=0 third-list output id-array deep-equals the unweighted call. `npm test -- tests/fts-retrieval.test.ts -t "rrfFuse"` passes.
    - Behavior (T2, VALIDATION `mechanism` row): weighted strength list reorders `high` to rank 0.
    - No existing rrfFuse caller signature broke: `npm test -- tests/fts-retrieval.test.ts` green.
  </acceptance_criteria>
  <done>rrfFuse takes optional weights; omitted/[..,0] reproduces today's order; weighted list reorders. T1+T2 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strength-list assembly in hybridTopk (pool-only, pure effective_s) + D-02/D-10/no-self-strengthen tests</name>
  <files>src/retrieval/topk.ts, src/strength/decay.ts, tests/fts-retrieval.test.ts</files>
  <read_first>
    - src/retrieval/topk.ts:85-211 (CandidateRetriever: constructor stmts, topk, stmtBm25, hybridTopk + cosineScoreMap)
    - src/strength/decay.ts:85-104 (effectiveStrength — pure; the single math site to share)
    - src/retrieval/engine.ts:246-257 (reference effective_s scoring pattern — DO NOT modify, D-08)
    - 35-RESEARCH.md Focus Area 2 (stmtPoolStrength json_each pattern + hybridTopk new-signature shape), Example 3 (stmtLatestSupportTs json_each precedent), Pitfalls 1/2/4
    - 35-VALIDATION.md rows `mechanism`, `pool-only (D-02)`, `tombstone (D-10)`, `no-self-strengthen`
  </read_first>
  <behavior>
    - T4 (w=0 hybridTopk regression): hybridTopk(queryVec,'q',5, undefined, 0) id-array deep-equals hybridTopk(queryVec,'q',5). No DB strength query issued when strengthWeight=0.
    - T3 (D-02 pool enforcement): a high-strength node whose embedding is far from the query (NOT in cosine pool) and absent from BM25 does NOT appear in hybridTopk output even at strengthWeight=2.0.
    - T5 (D-10 tombstone): a tombstoned high-strength node never surfaces via the strength list (inherited from pool source queries).
    - no-self-strengthen: after a hybridTopk call with strengthWeight>0, the `s` and `last_access` columns of every scored node are unchanged (read SELECT before/after, assert equal).
  </behavior>
  <action>
    Share the pure strength math so topk.ts never re-derives `s·exp(−λΔt)` (project hard-rule + RESEARCH
    "Don't Hand-Roll"): in src/strength/decay.ts, expose the existing effectiveStrength computation as a
    module-level pure helper (or make the method `static`) and have the current instance method delegate to it
    — DO NOT change its formula or the `Math.max(0, …)` L-8 clamp, and keep the existing engine.ts:246 caller
    working. Import that one helper into topk.ts.

    In CandidateRetriever (topk.ts): add a private prepared statement `stmtPoolStrength` in the constructor —
    `SELECT id, s, last_access FROM node WHERE id IN (SELECT value FROM json_each(?))` (parameterized; mirror
    the engine.ts stmtLatestSupportTs json_each pattern, RESEARCH Example 3). Pool ids are internal UUIDs from
    the cosine/BM25 scan — never user strings (D-02, no injection surface).

    Extend `hybridTopk` signature to `(queryVec, queryText, k, preK = k*3, strengthWeight = 0, nowMs?, lambda?)`.
    Keep all existing logic. When `strengthWeight > 0`: build `poolIds = [...new Set([...cosineList ids,
    ...bm25List ids])]`; fetch rows via `stmtPoolStrength.all(JSON.stringify(poolIds))`; map each to
    `{ id, effS: effectiveStrength(s, last_access, nowMs ?? Date.now(), lambda ?? 0.05) }`; sort desc by effS to
    get `strengthList` (id-only for fusion); call `rrfFuse([cosineList, bm25List, strengthList], 60, k,
    [1, 1, strengthWeight])`. When `strengthWeight === 0`: skip the DB query entirely and call
    `rrfFuse([cosineList, bm25List], 60, k)` exactly as today (D-04). PRESERVE the `cosineScoreMap` floor-gate
    output map verbatim (Pitfall 2) — the strength list changes fusion ORDER only, never the returned `score`
    (which stays the cosine component). NEVER call materializeDecay (Pitfall 4). See 35-RESEARCH.md Focus Area 2
    for the literal shape; no fenced code here. The strength-list map MUST call the shared `effectiveStrength`
    helper (as written above) — do NOT re-derive `s·exp(−λΔt)` inline; `grep -c "Math.exp(-" src/retrieval/topk.ts`
    must return 0.

    Add tests T3, T4, T5, and no-self-strengthen to tests/fts-retrieval.test.ts using the existing DB-backed
    hybridTopk test fixture style (~L160-200).
  </action>
  <verify>
    <automated>npm test -- tests/fts-retrieval.test.ts -t "strengthWeight|tombstone|last_access"</automated>
  </verify>
  <acceptance_criteria>
    - Verify command filter `strengthWeight|tombstone|last_access` captures all of T3/T4/T5 + no-self-strengthen (name each test so it matches one of these tokens).
    - Source: `grep -n "strengthWeight" src/retrieval/topk.ts` shows the new hybridTopk param.
    - Source: `grep -n "json_each" src/retrieval/topk.ts` shows the parameterized stmtPoolStrength (D-02; mechanism row).
    - Source (one-place math): `grep -c "Math.exp(-" src/retrieval/topk.ts` returns 0 — topk.ts re-derives nothing; the formula stays in decay.ts only.
    - Source (no mutation): `grep -n "materializeDecay" src/retrieval/topk.ts` returns nothing (Pitfall 4).
    - Behavior (T4, VALIDATION `dark-default`): hybridTopk strengthWeight=0 deep-equals the unweighted call.
    - Behavior (T3, VALIDATION `pool-only (D-02)`): off-pool high-strength node absent from output at w=2.0.
    - Behavior (T5, VALIDATION `tombstone (D-10)`): tombstoned high-strength node never surfaces.
    - Behavior (no-self-strengthen row): node `s`/`last_access` unchanged after a w>0 read.
    - `npm test -- tests/fts-retrieval.test.ts -t "strengthWeight"` and `-t "tombstone"` pass; existing doc-gather/hybrid callers unbroken (full file green).
  </acceptance_criteria>
  <done>hybridTopk fuses a pool-only, tombstone-excluded, pure-effective_s strength list under a weight; w=0 is identical to today; reads never mutate strength; T3/T4/T5/no-self-strengthen green.</done>
</task>

<task type="auto">
  <name>Task 3: Add rankStrengthWeight knob (default 0) and wire it through retrieveRanked (hybrid branch only)</name>
  <files>src/lib/config.ts, src/retrieval/engine.ts</files>
  <read_first>
    - src/lib/config.ts:303-325 (EngineConfig interface — rankWeightS/rankWeightR/injectionTokenBudget) and L607-649 (DEFAULT_CONFIG block)
    - src/retrieval/engine.ts:388-414 (retrieveRanked routing + floor gate)
    - 35-RESEARCH.md Focus Area 3 (interface + DEFAULT_CONFIG + retrieveRanked wire-up; 4-site thread-through)
    - 35-CONTEXT.md D-03, D-04, D-08
  </read_first>
  <action>
    In src/lib/config.ts: add `rankStrengthWeight: number;` to the EngineConfig interface (place it after
    `rankWeightR`, ~L318) with a doc comment noting it is the cue-based hybridTopk strength weight, ships dark at
    0 (D-04), is a sibling of rankWeightS/rankWeightR but applies to the CUE-BASED path only (D-08), and that
    effective_s already folds recency so this is one-signal-one-knob (D-03). Add `rankStrengthWeight: 0,` to
    DEFAULT_CONFIG after `rankWeightR: 0.0` (~L643).

    In src/retrieval/engine.ts:398-400 (retrieveRanked): on the `queryText` (hybrid) branch ONLY, change the
    call to `this.retriever.hybridTopk(queryVec, queryText, k, undefined, this.config.rankStrengthWeight,
    this.clock.nowMs(), this.config.lambda)`. Leave the pure-cosine `this.retriever.topk(queryVec, k)` fallback
    branch UNTOUCHED (D-08 — ambient-recall and other no-queryText callers stay no-fusion). Do not touch the
    floor gate or stale-entity filter. See 35-RESEARCH.md Focus Area 3 for the literal wire-up.
  </action>
  <verify>
    <automated>npm run build && npm test -- tests/fts-retrieval.test.ts tests/retrieval.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "rankStrengthWeight" src/lib/config.ts` shows BOTH the interface field and the DEFAULT_CONFIG `: 0` entry.
    - Source: `grep -n "rankStrengthWeight" src/retrieval/engine.ts` shows it passed inside the queryText branch of retrieveRanked.
    - Source (D-08 cue-less untouched): the `: this.retriever.topk(queryVec, k)` fallback at engine.ts:400 is unchanged (no strength args).
    - Build: `npm run build` exits 0 (tsc clean — interface + default both present, no type error).
    - Regression: `npm test -- tests/retrieval.test.ts` green — default config (w=0) leaves retrieveRanked behavior identical (D-04).
  </acceptance_criteria>
  <done>rankStrengthWeight exists in interface + DEFAULT_CONFIG at 0; retrieveRanked threads it (with clock.nowMs + config.lambda) into hybridTopk on the queryText branch only; tsc clean; retrieval suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB rows (`node.s`, `node.last_access`) → retrieval ranking | Internal trusted data crosses into the ranking computation; the risk is a read path that WRITES back (integrity), not external input. |
| query cue → FTS/cosine scan → pool ids → json_each | Pool ids are internal UUIDs derived from scan results, NOT user-supplied strings; the only user string (queryText) is already sanitized by ftsQueryFromText before MATCH. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering / Integrity | strength list in `hybridTopk` (topk.ts) | mitigate | Retrieval calls the PURE `effectiveStrength` only; `materializeDecay` is forbidden on this path. Asserted by the no-self-strengthen test (s/last_access unchanged after a w>0 read) + `grep` gate that topk.ts contains no `materializeDecay` and no inline `Math.exp(-`. |
| T-35-02 | Injection (SQLi) | `stmtPoolStrength` json_each query | mitigate | Parameterized prepared statement; pool ids are internal UUIDs (JSON-stringified), never raw user text. Mirrors the existing stmtLatestSupportTs pattern. |
| T-35-03 | Information disclosure | off-topic high-strength belief leaking into results | mitigate | D-02: strength list is a strict subset of the cosine∪BM25 pool — it can only re-order relevant candidates, never pull in an irrelevant node. Enforced by the T3 pool-only test. |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — D-11 net-zero new runtime deps; nothing new to audit. |
</threat_model>

<verification>
- `npm run build` clean (tsc).
- `npm test -- tests/fts-retrieval.test.ts` fully green (T1..T5 + no-self-strengthen).
- `npm test -- tests/retrieval.test.ts tests/responder.test.ts` green — default-config (w=0) behavior unchanged.
- Dark-default proof: with `rankStrengthWeight: 0` (the committed default), retrieveRanked output is identical to pre-phase behavior (D-04).
</verification>

<success_criteria>
- RANK-01 mechanism merged: a tunable, LLM-free, rank-based strength term fuses into the cue-based path.
- w=0 reproduces today's exact ranking at every level (rrfFuse, hybridTopk, retrieveRanked) — D-04.
- Strength list is pool-only (D-02), tombstone-excluded (D-10), and never mutates strength (self-confirmation guard).
- Net-zero new runtime deps (D-11).
</success_criteria>

<output>
Create `.planning/phases/35-recency-strength-retrieval-ranking/35-01-SUMMARY.md` when done.
</output>
