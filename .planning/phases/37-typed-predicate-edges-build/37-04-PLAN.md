---
phase: 37-typed-predicate-edges-build
plan: 04
type: execute
wave: 3
depends_on: ["37-02", "37-03"]
files_modified:
  - scripts/eval/37-precision-harness.cjs
  - scripts/eval/queries-37.json
autonomous: false
requirements: [TYPED-02]
must_haves:
  truths:
    - "The harness reproduces the deterministic PRIMARY metric (NTA / answer-in-top-3) on the re-derived query set"
    - "The harness records payload size (typed ~N nodes vs neighborhood K=20) — the interview-defensible token-win claim"
    - "The harness records the SECONDARY 3x-majority LLM-compose rate as confirmation only, never as the gate"
    - "The founder signs off the re-derived query set BEFORE the precision gate binds (D-05)"
    - "The PRIMARY gate is the merge decision: typed top-3% >= 75% AND lift over untyped >= +20pts (D-04)"
  artifacts:
    - path: "scripts/eval/37-precision-harness.cjs"
      provides: "deterministic NTA/top-3 + payload-size + secondary compose harness with gate"
      contains: "top-3"
    - path: "scripts/eval/queries-37.json"
      provides: "re-derived, predicate-balanced query set (founder-signed)"
      contains: "predicate_path"
  key_links:
    - from: "scripts/eval/37-precision-harness.cjs"
      to: "typedReach / untypedTopK (built engine)"
      via: "require dist traversal + payload-size + NTA scoring"
      pattern: "typedReach"
    - from: "harness PRIMARY metric"
      to: "merge gate"
      via: "typed top-3% >= 75% AND lift >= +20pts (deterministic, not the compose)"
      pattern: "top3\\|top-3"
---

<objective>
Build the Phase 37 precision harness and run the gate. Model it on `replay-ku-harness.cjs`: re-derive a predicate-balanced query set from the post-Wave-1 live-DB typed edges, get explicit founder per-query sign-off (D-05), then run the PRIMARY deterministic metric (nodes-to-answer / answer-in-top-3), record payload size (the token win), and the SECONDARY 3×-majority LLM-compose (confirmation only). Gate the merge on the PRIMARY metric (D-04): typed top-3% >= 75% AND lift >= +20pts.

Purpose: prove the +29.5pt precision win and the token reduction hold on the live engine with an interview-defensible, founder-signed query set — no inflated metrics.
Output: `37-precision-harness.cjs`, `queries-37.json`, a results JSON, a founder GO/NO-GO.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-typed-predicate-edges-build/37-CONTEXT.md
@.planning/phases/37-typed-predicate-edges-build/37-RESEARCH.md
@.planning/phases/37-typed-predicate-edges-build/37-VALIDATION.md

@scripts/eval/replay-ku-harness.cjs

<interfaces>
<!-- Built engine the harness consumes (compile first: npm run build) -->
.planning/spikes/004-typed-predicate-edges/05-precision.ts  — the metric template:
  NTA = gold rank in arm payload; top-3 = NTA<=3; payload size; 3x-majority compose
.planning/spikes/004-typed-predicate-edges/lib/traverse.ts:
  typedReach(db, anchor, path, K), untypedTopK(db, anchor, depth, K)  — the two arms
src/recall/typed-traversal.ts (Wave 2, built) — live typedReach for the typed arm

Harness conventions (scripts/eval/replay-ku-harness.cjs):
  --dry-run (zero LLM/API calls, validate wiring), results dir scripts/eval/results/,
  RECENSE_MODEL_PROVIDER=claude-headless for subscription-billed compose calls,
  --out <path>, arg(k,d) parser, per-case dry-run stats.

Query set shape (spike queries.json):
  { _meta: { K }, queries: [{ id, dilution, anchor, predicate_path, gold, question }] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 37-precision-harness (PRIMARY deterministic + payload size + secondary compose)</name>
  <files>scripts/eval/37-precision-harness.cjs</files>
  <read_first>
    - scripts/eval/replay-ku-harness.cjs (scaffold: arg parser, --dry-run, results/ output, headless provider gate, per-case loop)
    - .planning/spikes/004-typed-predicate-edges/05-precision.ts (NTA/rankOf, top1/top3, payload-size reporting, typedPayloadLines/untypedPayloadLines, composeCorrectRate 3x-majority — port the metric logic)
    - .planning/spikes/004-typed-predicate-edges/lib/traverse.ts (typedReach / untypedTopK — the two arms)
    - .planning/phases/37-typed-predicate-edges-build/37-RESEARCH.md §5 (the three measurement buckets + the gate values) and Pitfall 5 (PRIMARY = deterministic; compose NEVER the gate)
    - .planning/phases/37-typed-predicate-edges-build/37-VALIDATION.md (TYPED-01d --regression-only mode; TYPED-02f gate command)
  </read_first>
  <action>
    Create `scripts/eval/37-precision-harness.cjs` mirroring `replay-ku-harness.cjs` structure. It reads `scripts/eval/queries-37.json`, opens the live (or a copied) recense.db read-only, and for each query computes: PRIMARY — typed-arm NTA via `typedReach(db, anchor, predicate_path, BIG)` and untyped-arm NTA via `untypedTopK(db, anchor, depth, BIG)`, aggregate `answer-in-top-3 %` per arm and the lift; PAYLOAD SIZE — mean typed payload length vs K=20 (report "typed ~N nodes vs neighborhood 20"); SECONDARY — 3×-majority LLM-compose (labeled triples for typed, predicate-stripped for untyped) gated behind a non-dry-run run with RECENSE_MODEL_PROVIDER=claude-headless. Support flags: `--dry-run` (validate wiring + query-set parse, ZERO LLM calls), `--regression-only` (the D-03 TYPED-01d check stub: compare merged-vs-baseline claim count on a sample, no compose), `--threshold-sweep` (optional: iterate predicateGlossThreshold over 0.25/0.35/0.45/0.55 for D-05 calibration). Write results to `scripts/eval/results/37-precision-<ts>.json` with per-query detail + aggregates. CRITICAL (Pitfall 5 / D-04): the gate is the PRIMARY deterministic metric ONLY — print the compose rate as labeled "SECONDARY (confirmation only)" and never let it pass/fail the run. Require `npm run build` first (it requires the built engine). Net-zero new deps.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/eval/37-precision-harness.cjs --dry-run --out /tmp/37-dry.json &amp;&amp; node -e "const r=require('/tmp/37-dry.json');if(r.mode!=='dry-run')process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/eval/37-precision-harness.cjs --dry-run` exits 0 with ZERO LLM/API calls and writes a dry-run results skeleton.
    - Source assertion: `grep -c "typedReach\|untypedTopK" scripts/eval/37-precision-harness.cjs` >= 1 (both arms present).
    - Source assertion: `grep -ci "secondary\|confirmation only" scripts/eval/37-precision-harness.cjs` >= 1 AND the gate computation references the deterministic top-3, not the compose rate (verify by reading — Pitfall 5).
    - `grep -c "payload" scripts/eval/37-precision-harness.cjs` >= 1 (token-win measurement present).
    - `--regression-only --dry-run` exits 0 (TYPED-01d wiring).
  </acceptance_criteria>
  <done>Harness runs dry with zero LLM calls; computes deterministic NTA/top-3 + payload size for both arms; compose is secondary/non-gating; results JSON written.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive the predicate-balanced query set from live typed edges</name>
  <files>scripts/eval/queries-37.json</files>
  <read_first>
    - .planning/spikes/004-typed-predicate-edges/queries.json (the shape: _meta.K + queries[] with id/dilution/anchor/predicate_path/gold/question)
    - .planning/phases/37-typed-predicate-edges-build/37-RESEARCH.md §5 "D-05 Query Set Re-derivation" (re-derive from live typed edges post-Wave-1; >= 1 per predicate, ideally 2; anti-circularity: hi/mid authored blind, lo controls from confirmed chains; N≈20-30; K=20) and §2 tuning protocol (>= 2 expected-match queries per predicate)
    - .planning/phases/37-typed-predicate-edges-build/37-CONTEXT.md D-05 (re-derive with founder sign-off) + the no-inflated-metrics project rule (interview-defensible golds)
  </read_first>
  <action>
    After Wave 1 extraction has populated typed edges in a copy of the live recense.db, draft `scripts/eval/queries-37.json` matching the spike shape: `_meta.K = 20`, and 20-30 queries balanced across the 12 predicates (>= 1 each, ideally 2 — RESEARCH §2 needs >= 2 expected-match per predicate to validate the threshold). Each query: `id`, `dilution` (hi/mid/lo tier), `anchor` (a real node value present in the live DB), `predicate_path` (single predicate — v1 D-07), `gold` (the actual reachable node value via that typed edge), and a natural-language `question`. Anti-circularity discipline (RESEARCH §5): author hi/mid questions BLIND to the graph then confirm reachability; draw lo-tier controls from confirmed-reachable chains. Golds must be real and interview-defensible (no fabricated answers — project no-inflated-metrics rule). Do NOT run the gate yet — that is gated on the founder sign-off checkpoint (Task 3).
  </action>
  <verify>
    <automated>node -e "const q=require('./scripts/eval/queries-37.json');const P=new Set(q.queries.flatMap(x=>x.predicate_path));if(q._meta.K!==20||q.queries.length<20||P.size<12)process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `queries-37.json` parses; `_meta.K === 20`; >= 20 queries; all 12 predicates appear across the set (the node one-liner enforces P.size >= 12).
    - `node scripts/eval/37-precision-harness.cjs --dry-run` ingests queries-37.json with zero parse errors.
    - Every query's `predicate_path` is a single-element array (v1 single-hop, D-07): `node -e "const q=require('./scripts/eval/queries-37.json');if(q.queries.some(x=>x.predicate_path.length!==1))process.exit(1)"`.
  </acceptance_criteria>
  <done>queries-37.json exists: 20-30 predicate-balanced single-hop queries with real anchors/golds, parses cleanly in the harness dry-run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Founder sign-off on the re-derived query set (D-05) — BEFORE the gate binds</name>
  <action>Present scripts/eval/queries-37.json to the founder for per-query sign-off; pause the phase until approval. The precision gate (Task 4) MUST NOT run until the founder approves (D-05).</action>
  <what-built>scripts/eval/queries-37.json — a re-derived, predicate-balanced query set (20-30 queries, real anchors/golds from the post-Wave-1 live DB).</what-built>
  <how-to-verify>
    1. Open `scripts/eval/queries-37.json` and review each query's `question`, `anchor`, `predicate_path`, and `gold`.
    2. Confirm each gold answer is a REAL, reachable fact (interview-defensible — project no-inflated-metrics rule), not a fabricated/aspirational answer.
    3. Confirm hi/mid-tier questions read as natural user questions authored blind to the graph (anti-circularity, RESEARCH §5).
    4. Confirm balance: at least 1 query per predicate, ideally 2 (RESEARCH §2 threshold validation needs >= 2 expected-match per predicate).
    5. Approve, or list the queries to revise/replace. The precision gate (Task 4) MUST NOT run until you approve — D-05.
  </how-to-verify>
  <resume-signal>Type "approved" to bind the gate, or list queries to revise (Task 2 re-runs, then re-review).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Run the precision gate (PRIMARY deterministic) + record the token win</name>
  <files>scripts/eval/results/37-precision-LATEST.json</files>
  <read_first>
    - scripts/eval/37-precision-harness.cjs (Task 1) and scripts/eval/queries-37.json (founder-signed, Task 3)
    - .planning/phases/37-typed-predicate-edges-build/37-RESEARCH.md §5 (gate: typed top-3% >= 75% AND lift >= +20pts; spike anchor +29.5pts) and §7 Wave 3 + Pitfall 5
    - .planning/phases/37-typed-predicate-edges-build/37-VALIDATION.md (TYPED-02f command + the GO/NO-GO manual verification)
  </read_first>
  <action>
    Run `node scripts/eval/37-precision-harness.cjs` (with `RECENSE_MODEL_PROVIDER=claude-headless` for the SECONDARY compose) against the founder-signed query set on the live (or copied) DB with typed edges. Record the PRIMARY deterministic result: typed vs untyped answer-in-top-3 % and the lift; the PAYLOAD SIZE (typed mean nodes vs neighborhood 20 — the token-win claim); and the SECONDARY 3×-majority compose as confirmation only. Write the canonical results to `scripts/eval/results/37-precision-LATEST.json`. Surface the numbers; the GO/NO-GO merge decision (gate: typed top-3% >= 75% AND lift >= +20pts) is the founder's call in the next checkpoint. Do NOT gate on the compose rate (Pitfall 5).
  </action>
  <verify>
    <automated>node -e "const r=require('./scripts/eval/results/37-precision-LATEST.json');if(typeof r.both!=='number')process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Results JSON written with per-query detail + aggregates (typed/untyped top-3 %, lift, payload size, secondary compose).
    - The reported gate metric is the deterministic top-3 lift, NOT the compose rate (verify the printed "GATE" line references PRIMARY).
    - Payload-size line present and shows typed mean << 20 (the token reduction).
  </acceptance_criteria>
  <done>Precision gate run on the signed query set; PRIMARY top-3 lift + payload size + secondary compose recorded; numbers ready for the founder GO/NO-GO.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 5: Founder GO/NO-GO merge decision (D-04 PRIMARY gate)</name>
  <action>Present the PRIMARY deterministic numbers (typed top-3 %, lift, payload size) and the SECONDARY compose to the founder; pause for the GO/NO-GO merge decision per the gate (typed top-3 >= 75% AND lift >= +20pts, D-04). Do NOT proceed to merge without an explicit decision.</action>
  <decision>Does the live precision win clear the merge gate — typed answer-in-top-3 >= 75% AND lift over untyped >= +20pts (deterministic PRIMARY, D-04)?</decision>
  <context>Spike anchor was +29.5pts deterministic / +22.8pts compose. The PRIMARY deterministic metric is the gate; the LLM-compose is confirmation only (Pitfall 5 — never let the noisy judge gate on small N). The payload-size reduction (typed ~N vs neighborhood 20) is the token-win evidence and must be interview-defensible (no-inflated-metrics).</context>
  <options>
    <option id="go-merge">
      <name>GO — gate cleared</name>
      <pros>Typed top-3 >= 75% AND lift >= +20pts on the signed query set; payload win confirmed. Promote typed recall as default.</pros>
      <cons>None if the numbers hold; keep RECENSE_TYPED_EXTRACTION_MODE as the documented revert.</cons>
    </option>
    <option id="tune-threshold">
      <name>TUNE — re-run --threshold-sweep before deciding</name>
      <pros>If top-3 is borderline or false-matches appear, the sweep (0.25/0.35/0.45/0.55) may recover the win without code changes.</pros>
      <cons>Adds a calibration loop before merge.</cons>
    </option>
    <option id="no-go">
      <name>NO-GO — gate missed</name>
      <pros>Honest stop; revisit glosses (RESEARCH A1) or predicate granularity (D-09 deferred lever) rather than ship a soft win.</pros>
      <cons>Phase 37 recall benefit not realized this pass.</cons>
    </option>
  </options>
  <resume-signal>Select: go-merge, tune-threshold, or no-go.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query set golds → gate | the metric's correctness depends on golds being real/reachable |
| compose call → gate | the noisy LLM judge must not drive the merge decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-14 | Repudiation | inflated/fabricated golds inflating the win | mitigate | founder per-query sign-off (D-05, Task 3); golds drawn from real reachable live edges; no-inflated-metrics rule |
| T-37-15 | Tampering | compose rate used as the gate (variance on small N) | mitigate | PRIMARY = deterministic NTA/top-3; compose labeled secondary, never gates (D-04, Pitfall 5) |
| T-37-16 | Information disclosure | harness running compose with subscription transport | accept | RECENSE_MODEL_PROVIDER=claude-headless, founder's own Max subscription, local single-tenant engine |
</threat_model>

<verification>
- `node scripts/eval/37-precision-harness.cjs --dry-run` exits 0, zero LLM calls
- `queries-37.json` parses, 12 predicates covered, all single-hop
- Founder sign-off recorded BEFORE the gate run (D-05)
- Results JSON has deterministic top-3 + lift + payload size; gate references PRIMARY not compose
- Founder GO/NO-GO decision recorded
</verification>

<success_criteria>
The build harness reproduces the deterministic precision metric and the payload-size token win on a founder-signed, predicate-balanced query set; the merge decision is made on the PRIMARY deterministic gate (typed top-3 >= 75% AND lift >= +20pts), with the LLM-compose as confirmation only. No inflated metrics; D-05 sign-off precedes the gate.
</success_criteria>

<output>
Create `.planning/phases/37-typed-predicate-edges-build/37-04-SUMMARY.md` when done.
</output>
