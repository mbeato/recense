---
phase: 37-typed-predicate-edges-build
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - src/recall/typed-traversal.ts
  - src/recall/index.ts
  - tests/typed-traversal.test.ts
  - tests/recall-engine.test.ts
autonomous: true
requirements: [TYPED-02]
must_haves:
  truths:
    - "typedReach follows exactly one predicate from the anchor and returns only nodes reachable via that predicate"
    - "Typed traversal filters to PREDICATES_SET.has(edge.rel) && kind==='relation' (links_to/extends excluded)"
    - "Recall returns the small typed-path payload when query→predicate cosine >= predicateGlossThreshold"
    - "Recall falls back to the UNCHANGED schema-neighborhood path when cosine < threshold"
    - "The typed-path recall branch never calls upsertEdge or strengthen (D-08 self-confirmation guard)"
  artifacts:
    - path: "src/recall/typed-traversal.ts"
      provides: "typedReach (single-hop predicate-filtered) + matchPredicate (12-way cosine)"
      contains: "typedReach"
    - path: "src/recall/index.ts"
      provides: "D-06 augment-with-fallback branch using pre-loaded gloss embeddings"
      contains: "predicateGlossThreshold"
  key_links:
    - from: "src/recall/index.ts (after bestMatch, before neighborhood)"
      to: "matchPredicate(cueVec, glossEmbeddings, threshold)"
      via: "12-way cosine on the already-computed query vec (LLM-free)"
      pattern: "matchPredicate"
    - from: "src/recall/typed-traversal.ts::typedReach"
      to: "store.getOutEdgesWithRel"
      via: "rel-carrying edge read filtered by PREDICATES_SET + kind"
      pattern: "getOutEdgesWithRel"
---

<objective>
Build the TYPED-02 recall half: port `typedReach` (single-hop, predicate-filtered) into `src/recall/typed-traversal.ts`, add `matchPredicate` (12-way cosine of the already-computed query vec against the pre-loaded gloss embeddings), and augment `RecallEngine.recall()` with the D-06 branch — typed-path mode when the cosine match clears `predicateGlossThreshold`, else the existing schema-neighborhood assembly UNCHANGED. Online path stays LLM-free; the typed branch never writes to the graph (D-08).

Purpose: the token win — a ~2-node typed payload replaces the ~20-node neighborhood when a query confidently maps to a predicate.
Output: typed-traversal module, recall augmentation, unit tests (incl. D-08 grep guard).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-typed-predicate-edges-build/37-CONTEXT.md
@.planning/phases/37-typed-predicate-edges-build/37-RESEARCH.md

@src/recall/index.ts

<interfaces>
<!-- From Wave 0 (37-01) — import, do not redefine -->
src/model/typed-predicates.ts:
  export const PREDICATES: readonly [...12...]; export const PRED_SET: Set<string>;
src/db/semantic-store.ts:
  getOutEdgesWithRel(nodeId): Array<{ dst; rel; w; kind }>   // USE THIS for typed traversal (rel present)
  getOutEdges(nodeId): Array<{ dst; w; kind }>               // LANDMINE 1: no rel — do NOT use here
src/lib/config.ts:
  predicateGlossThreshold: number  (default 0.35)
src/consolidation/gloss-embeddings.ts:
  loadGlossEmbeddings(store): Record<Predicate, Float32Array> | null

Spike traversal to port (.planning/spikes/004-typed-predicate-edges/lib/traverse.ts):
  typedReach(db, anchor, predicatePath: string[], K): string[]   // path-weight ranked, top-K
  NOTE: spike reads `SELECT dst, rel, w` directly; live port MUST use store.getOutEdgesWithRel
        AND add the PREDICATES_SET.has(e.rel) && e.kind==='relation' filter (LANDMINE 2).

RecallEngine.recall() integration point (src/recall/index.ts):
  :129 cueVec = await provider.embed([boundedQuery])   // reuse — no new embed
  :134 bestMatch = topHits[0]
  :151-175 schema resolution (Case A/B)
  :180-242 neighborhood assembly  ← D-06 fallback = this block, UNCHANGED
  Insert the D-07/D-06 decision BETWEEN bestMatch (:134) and neighborhood (:180).
  HARD INVARIANT (:19): recall NEVER calls upsertNode/upsertEdge/tombstone/strengthen.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port typedReach (single-hop, vocab+kind filtered) + matchPredicate</name>
  <files>src/recall/typed-traversal.ts, tests/typed-traversal.test.ts</files>
  <read_first>
    - .planning/spikes/004-typed-predicate-edges/lib/traverse.ts:39-64 (typedReach — port the path-weight ranking + top-K + stable tiebreak verbatim) and the header note on control fairness/symmetry
    - .planning/spikes/004-typed-predicate-edges/05-precision.ts:113-122 (single-hop usage: predicate_path.length, typedReach(db, anchor, path, K))
    - src/db/semantic-store.ts (getOutEdgesWithRel from Wave 0 — the rel-carrying read) and src/model/typed-predicates.ts (PRED_SET)
    - .planning/phases/37-typed-predicate-edges-build/37-RESEARCH.md §3 (v1 = single-predicate-from-anchor; predicatePath.length === 1), §6 LANDMINE 2 (filter PRED_SET.has(e.rel) && kind==='relation' so links_to/extends are never followed) + Pitfall 1 + Pitfall 2
  </read_first>
  <behavior>
    - typedReach(store, anchor, ['uses'], K) returns only nodes reachable from anchor via a 'uses' edge, ranked by path-weight, capped at K.
    - An edge with rel='links_to' or rel='extends' (kind='relation' but NOT in vocab) is NOT followed (Pitfall 2).
    - An edge with kind!=='relation' (e.g. 'abstracts') is NOT followed even if rel happens to collide.
    - typedReach returns [] when the anchor has no matching predicate edge (→ caller falls back).
    - matchPredicate(cueVec, glossEmbeddings, threshold) returns the argmax predicate when maxCosine >= threshold, else null.
    - matchPredicate returns null when glossEmbeddings is null/empty (not yet embedded → fallback).
  </behavior>
  <action>
    Create `src/recall/typed-traversal.ts`. Port `typedReach` from spike `traverse.ts` but parameterize it over the live store: replace the spike's inline `SELECT dst, rel, w` with `store.getOutEdgesWithRel(node)`, and BEFORE the `e.rel !== pred` check add the LANDMINE-2 guard `if (!PRED_SET.has(e.rel) || e.kind !== 'relation') continue;`. Keep the path-weight accumulation, top-K slice, and stable id tiebreak verbatim. For v1, callers pass a single-element `predicatePath` (D-07; multi-hop deferred — do NOT add chaining). Add `matchPredicate(cueVec: Float32Array, glossEmbeddings: Record<Predicate, Float32Array> | null, threshold: number): Predicate | null` — compute cosine of `cueVec` against each of the (up to 12) gloss vectors via a plain Float32Array dot-product loop (no new dep; ~12×1536 FLOPs), return the argmax predicate iff its cosine >= threshold, else null; return null when glossEmbeddings is null/empty. Write `tests/typed-traversal.test.ts` covering the behavior block (use an in-memory store with seeded edges incl. a links_to/extends decoy, and mock gloss vectors for matchPredicate).
  </action>
  <verify>
    <automated>npm test -- --run typed-traversal</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run typed-traversal` passes including the links_to/extends decoy-exclusion case and the kind!=='relation' exclusion case.
    - Source assertion: `grep -c "getOutEdgesWithRel" src/recall/typed-traversal.ts` returns >= 1 and `grep -c "getOutEdges(" src/recall/typed-traversal.ts` (the rel-less primitive) returns 0.
    - Source assertion: `grep -c "PRED_SET.has\|kind !== 'relation'" src/recall/typed-traversal.ts` returns >= 1 (LANDMINE 2 filter present).
    - matchPredicate test asserts null below threshold and the right predicate above threshold.
  </acceptance_criteria>
  <done>typedReach (single-hop, vocab+kind filtered, path-weight ranked) and matchPredicate (LLM-free 12-way cosine) exist and are tested; rel-less getOutEdges never used.</done>
</task>

<task type="auto">
  <name>Task 2: Augment RecallEngine with the D-06 typed-path-OR-fallback branch</name>
  <files>src/recall/index.ts, tests/recall-engine.test.ts</files>
  <read_first>
    - src/recall/index.ts:19 (hard invariant — no upsertEdge/strengthen), :124-180 (recall flow: cueVec, bestMatch, schema resolution), :180-242 (neighborhood assembly — the D-06 fallback block, UNCHANGED)
    - src/recall/typed-traversal.ts (typedReach, matchPredicate — from Task 1) and src/consolidation/gloss-embeddings.ts (loadGlossEmbeddings — Wave 0)
    - .planning/phases/37-typed-predicate-edges-build/37-RESEARCH.md §3 (the augment-with-fallback pseudocode: argmax→threshold→typedReach OR existing assembly), §6 LANDMINE 3 (insert decision between bestMatch and neighborhood; reuse cueVec — no new embed) + LANDMINE 4 (D-08 guard) + Pitfall 4 (glosses loaded ONCE in constructor, never per-recall) + Pitfall 5
    - .planning/phases/37-typed-predicate-edges-build/37-CONTEXT.md D-06 (typed path OR neighborhood, never both) + D-07 (single-predicate-from-anchor) + D-08
  </read_first>
  <action>
    Load the gloss embeddings ONCE at `RecallEngine` construction via `loadGlossEmbeddings(store)` (Pitfall 4: never re-embed per recall; if null/not-yet-embedded, the engine simply always falls back). In `recall()`, between bestMatch resolution (~:134) and neighborhood assembly (~:180), call `matchPredicate(cueVec, this.glossEmbeddings, this.config.predicateGlossThreshold)` reusing the already-computed `cueVec` (LANDMINE 3 — no new online embed). If it returns a predicate, take the typed-path branch: `typedReach(this.store, bestMatch.id, [predicate], this.config.recallNeighborhoodBudget)`, resolve the frontier node values, and compose a RecallResult from the small labeled-triple payload (mirror the existing compose-and-append-inferred-episode shape — the typed payload replaces the neighborhood; D-06 = one mode OR the other, NEVER path+neighborhood). If typedReach returns empty, fall through to the existing neighborhood assembly (don't return NULL on empty typed path). If matchPredicate returns null, run the existing schema-neighborhood assembly UNCHANGED. The typed-path branch MUST NOT call upsertEdge/upsertNode/strengthen/tombstone (D-08 / LANDMINE 4 / hard invariant at :19) — the only write remains the `origin='inferred'` episode append, exactly as the neighborhood path does today. Add `tests/recall-engine.test.ts` cases with mock gloss embeddings forcing above- and below-threshold matches.
  </action>
  <verify>
    <automated>npm test -- --run recall-engine &amp;&amp; bash -c "test $(grep -rn 'upsertEdge\|upsertNode\|strengthen\|tombstone' src/recall/ | grep -v '//' | grep -vc 'NEVER\|never\|no upsert' ) -eq 0"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run recall-engine` passes including: (TYPED-02c) cosine >= threshold returns a typed-path payload smaller than the neighborhood; (TYPED-02d) cosine < threshold returns the unchanged schema-neighborhood result.
    - D-08 guard (TYPED-02e): `grep -rn 'upsertEdge\|strengthen' src/recall/` returns 0 functional hits (only the existing NEVER-call comments at :19). The verify command enforces 0.
    - Source assertion: `grep -c "matchPredicate" src/recall/index.ts` >= 1 and `grep -c "predicateGlossThreshold" src/recall/index.ts` >= 1.
    - Glosses loaded in the constructor, not in recall(): `grep -c "loadGlossEmbeddings" src/recall/index.ts` >= 1 and that call is in the constructor (verify by reading) — recall() must not call provider.embed for glosses.
    - `npm run build` succeeds; full suite `npm test -- --run` green.
  </acceptance_criteria>
  <done>recall() returns the small typed-path payload above threshold and the unchanged neighborhood below it (never both); glosses loaded once at construction; recall stays LLM-free and writes no graph edges; tests + D-08 grep green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query → predicate match | user query (already embedded) drives which predicate path is followed |
| recall branch → graph | the inference must never become a stored graph fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-09 | Elevation | typed-path recall minting/strengthening an edge (self-confirmation) | mitigate | recall branch never calls upsertEdge/upsertNode/strengthen/tombstone (D-08, hard invariant :19); enforced by grep guard in verify |
| T-37-10 | Tampering | following links_to/extends as if typed predicates | mitigate | typedReach filters PRED_SET.has(e.rel) && kind==='relation' (LANDMINE 2) |
| T-37-11 | Spoofing | predicate-match triggers path from the wrong anchor | accept | bestMatch is the existing cosine top-1; retrieval trust boundary unchanged this phase |
| T-37-12 | DoS | re-embedding 12 glosses on every recall (online LLM cost) | mitigate | glosses loaded ONCE at construction (Pitfall 4); matchPredicate is a pure dot-product loop, LLM-free |
| T-37-13 | Tampering | bloated payload (path + neighborhood) erasing the token win | mitigate | D-06 one-mode-or-other; typed branch returns before neighborhood assembly |
</threat_model>

<verification>
- `npm test -- --run typed-traversal` green
- `npm test -- --run recall-engine` green (above- and below-threshold cases)
- D-08 grep: `grep -rn 'upsertEdge|strengthen' src/recall/` → 0 functional hits
- `npm run build` + full suite green
</verification>

<success_criteria>
Recall returns a small typed-path payload when a query confidently maps to a predicate (cosine >= 0.35) and the unchanged K=20 schema-neighborhood otherwise — never both. The online path adds only a 12-way cosine (no new embed, no LLM) and writes no graph edges (D-08 intact).
</success_criteria>

<output>
Create `.planning/phases/37-typed-predicate-edges-build/37-03-SUMMARY.md` when done.
</output>
