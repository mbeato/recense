---
phase: 999.3-scope-aware-provenance-memory-importer
plan: 02
type: execute
wave: 2
depends_on: ["999.3-01"]
files_modified:
  - src/adapter/import-memory-cli.ts
  - src/adapter/recense.ts
  - tests/import-memory.test.ts
  - docs/import-memory.md
autonomous: false   # final migration + retirement is human-gated (D-S7)
requirements: [SCOPE-04]
preconditions:
  - "999.3-01 merged (node_scope + scope derivation live)"

must_haves:
  truths:
    - "recense import-memory ingests projects/*/memory recall-facts via the existing IngestionPipeline.recordEvent (D-S4: no new ingestion path)"
    - "policy bundles + MEMORY.md index files are SKIPPED (D-S5)"
    - "re-running the importer does NOT duplicate facts (idempotent via stable external_id)"
    - "imported episodes carry the source project's cwd so consolidation assigns the correct scope (D-S3)"
    - "no source file is deleted by the tool — retirement is a separate human-confirmed step (D-S7)"
  artifacts:
    - path: "src/adapter/import-memory-cli.ts"
      provides: "import-memory CLI: scan, skiplist, cwd-map, recordEvent, dry-run, idempotent external_id"
      contains: "memory-import"
    - path: "src/adapter/recense.ts"
      provides: "import-memory subcommand dispatch"
      contains: "import-memory"
  key_links:
    - from: "src/adapter/import-memory-cli.ts"
      to: "IngestionPipeline.recordEvent"
      via: "one episode per fact file, source='memory-import', cwd=mapped project path"
      pattern: "recordEvent"
---

<objective>
Provide a repeatable, idempotent importer that migrates the `~/.claude/projects/*/memory` recall-facts into recense via the existing ingestion pipeline (D-S4), skipping policy bundles (D-S5), so consolidation stamps the right scope (D-S3) — then run a VERIFIED migration and only retire source files after confirming retrievability (D-S7).

Purpose: collapse the redundant MEMORY.md fact stores into the one brain, safely.
Output: import-memory CLI + dispatch + docs + tests, then a human-gated migration + verification + retirement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.3-scope-aware-provenance-memory-importer/999.3-CONTEXT.md
@.planning/phases/999.3-scope-aware-provenance-memory-importer/999.3-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: import-memory CLI — scan, skiplist, cwd-map, idempotent ingest (D-S4, D-S5)</name>
  <files>src/adapter/import-memory-cli.ts, tests/import-memory.test.ts</files>
  <read_first>
    - src/ingest/pipeline.ts (recordEvent signature, the EpisodicEvent shape incl. cwd/source/external_id — confirm external_id dedup semantics)
    - src/adapter/seed-cli.ts (the existing one-shot memory reader — reuse its file-walk / claim-read approach where sensible, but this importer is RE-RUNNABLE, not one-shot)
    - src/lib/scope.ts (cwdToScope, to validate the mapping the importer relies on)
    - 999.3-CONTEXT.md (D-S4, D-S5, cwd→scope mapping)
  </read_first>
  <behavior>
    - Scans `~/.claude/projects/*/memory/*.md` across all projects.
    - SKIPS: `MEMORY.md` index files and the policy-bundle skiplist (voice_profile, feedback_no_inflated_metrics, feedback_drop_concentrations, outreach_framework, user_job_search_strategy, reference_linkedin_playbook, user_profile) — match by filename.
    - For each remaining fact file: derive the project from the `-Users-vtx-<slug>` folder → cwd `/Users/vtx/<slug>` (resume → cwd that maps to 'global'); call recordEvent with content=file body, source='memory-import', cwd=mapped, external_id=stable id (e.g. `memory-import:<project>:<filename>` or content hash).
    - `--dry-run` prints what WOULD be imported (file, project, scope, skip reason) and writes nothing.
    - Idempotent: a second real run with unchanged files adds zero new episodes (external_id dedup).
    - Never deletes or modifies source files.
  </behavior>
  <action>
    Implement src/adapter/import-memory-cli.ts: glob the memory dirs, apply the skiplist + MEMORY.md filter, map folder→cwd, and for each fact file call IngestionPipeline.recordEvent({content, source:'memory-import', cwd, external_id, ...}). Support `--dry-run` and `--project <slug>` (limit to one project). Print a summary (imported / skipped-policy / skipped-index counts). Reuse seed-cli's file-walk helpers if cleanly importable; do NOT make it one-shot (no `seeded` meta flag). Respect MAX_CONTENT_CHARS. Disable adapters during run is the operator's job (doc it).
  </action>
  <verify><automated>npx vitest run tests/import-memory.test.ts</automated></verify>
  <acceptance_criteria>
    - Test fixture dir with a policy bundle + MEMORY.md + 2 fact files → importer ingests exactly the 2 fact files, skips the other two, with correct cwd/scope.
    - Second run on the same fixture → 0 new episodes (idempotent).
    - --dry-run writes nothing.
    - Source fixture files unchanged after run.
  </acceptance_criteria>
  <done>Idempotent, skiplist-aware import-memory CLI ingesting via the existing pipeline, tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire import-memory into the recense dispatcher + docs</name>
  <files>src/adapter/recense.ts, docs/import-memory.md</files>
  <read_first>
    - src/adapter/recense.ts (subcommand dispatch table — how ingest/recall/serve are registered)
  </read_first>
  <behavior>
    - `recense import-memory [--dry-run] [--project <slug>]` runs the CLI.
    - docs/import-memory.md documents usage, the consolidate→verify→retire flow, the policy-bundle skiplist, and the "disable adapters + run sleep-pass manually" operational notes.
  </behavior>
  <action>
    Register `import-memory` in the dispatcher mirroring the `ingest` entry. Write docs/import-memory.md with the full runbook including embedding-cost note (~$1-2 for ~280 files) and the retirement checklist.
  </action>
  <verify><automated>grep -c "import-memory" src/adapter/recense.ts</automated></verify>
  <acceptance_criteria>
    - `recense import-memory --dry-run` dispatches and runs.
    - docs/import-memory.md exists with the consolidate→verify→retire runbook.
  </acceptance_criteria>
  <done>import-memory is a first-class subcommand with a runbook.</done>
</task>

<task type="manual">
  <name>Task 3: Verified migration + retirement (D-S7) — HUMAN-GATED</name>
  <files>(operational — produces 999.3-MIGRATION.md, no source changes)</files>
  <read_first>
    - docs/import-memory.md
  </read_first>
  <behavior>
    Run the real migration and verify before retiring anything.
  </behavior>
  <action>
    1. `recense import-memory --dry-run` — review the import/skip plan; confirm policy bundles are skipped and scopes look right.
    2. Disable adapters (RECENSE_ENABLED_SOURCES=) and run the real import, then trigger a manual sleep-pass (`recense sleep-pass`) so facts consolidate + get scope.
    3. VERIFY: for a sample of migrated facts per project, run `recense recall "<query>"` and confirm the fact returns with the correct scope. Spot-check ≥3 facts per project across ≥3 projects.
    4. Write `999.3-MIGRATION.md`: counts (imported/skipped), embedding cost actually incurred, verification samples (query → returned? → scope correct?), and any facts that failed to surface (investigate before retiring those).
    5. RETIRE only verified-retrievable facts' source files: move (not delete) the migrated `projects/*/memory/*.md` to an archive dir (e.g. `~/.claude/projects-memory-archive-<date>/`), preserving MEMORY.md indexes and policy bundles in place. Get explicit founder confirmation before this step.
  </action>
  <verify><manual>999.3-MIGRATION.md shows ≥90% of sampled facts retrievable with correct scope; founder confirms retirement</manual></verify>
  <acceptance_criteria>
    - Migration report exists with verification samples.
    - No source file deleted (archived/moved only), and only after verification + founder sign-off.
    - Policy bundles + MEMORY.md indexes remain in place.
  </acceptance_criteria>
  <done>projects/*/memory recall-facts live in recense with correct scope, verified, and sources archived — the redundant fact store retired.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-S4-01 | Data loss | retiring source files | mitigate | D-S7: verify retrievable first; archive (move) not delete; founder-gated (Task 3 is manual) |
| T-S4-02 | Duplication | re-running importer | mitigate | stable external_id dedup; idempotency test (Task 1) |
| T-S4-03 | Policy leak | importing load-bearing bundles | mitigate | filename skiplist (D-S5) + dry-run review before real run |
| T-S4-04 | Cost | embedding ~280 files | accept/manage | dry-run shows count; run with adapters disabled; ~$1-2 documented |
</threat_model>

<verification>
- tests/import-memory.test.ts green; `npx tsc --noEmit` clean.
- `recense import-memory --dry-run` runs and reports correct skip/import counts on the real dirs.
- 999.3-MIGRATION.md present with verification samples before any retirement.
</verification>

<success_criteria>
A repeatable `recense import-memory` migrates projects/*/memory recall-facts into recense via the existing pipeline (D-S4), skipping policy bundles (D-S5), idempotently; a verified, founder-gated migration confirms retrievability + correct scope, after which source files are archived (D-S7) — completing the consolidation of the MEMORY.md fact store into the one brain.
</success_criteria>

<output>
Create `999.3-02-SUMMARY.md` when done.
</output>
