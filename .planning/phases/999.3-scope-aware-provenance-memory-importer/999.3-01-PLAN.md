---
phase: 999.3-scope-aware-provenance-memory-importer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/types.ts
  - src/db/semantic-store.ts
  - src/lib/scope.ts
  - src/consolidation/run-sleep-pass.ts
  - src/adapter/ambient-recall.ts
  - tests/node-scope-schema.test.ts
  - tests/scope-derive.test.ts
  - tests/node-scope-store.test.ts
  - tests/consolidation-scope.test.ts
autonomous: true
requirements: [SCOPE-01, SCOPE-02, SCOPE-03]
preconditions:
  - "Phase 23 complete AND fk-consolidation-bug-handoff.md resolved (this plan edits the consolidation path)"
  - "Re-read src/db/schema.ts SCHEMA_VERSION before editing — was 9 on 2026-06-16; if a later phase bumped it, use current+1"

must_haves:
  truths:
    - "node_scope sidecar (D-S2: sparse table node_id PK FK→node.id, NOT a column on node) writes/reads a scope string per node"
    - "scope is derived from episode cwd (D-S3): a known project root → its slug; personal/home/empty/unknown cwd → 'global'; a node with contributing episodes from >1 distinct project → 'global'"
    - "consolidation behaviour is byte-for-byte unchanged for nodes when scope derivation is added (additive sidecar write only)"
    - "retrieval ranking is UNCHANGED — scope is surfaced in output only, never used as a filter or score input (D-S1/D-S6)"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "SCHEMA_VERSION bumped + node_scope table"
      contains: "node_scope"
    - path: "src/lib/scope.ts"
      provides: "cwdToScope(cwd) + resolveNodeScope(scopes[]) helpers"
      contains: "cwdToScope"
    - path: "src/db/semantic-store.ts"
      provides: "upsertNodeScope + getNodeScope"
      contains: "upsertNodeScope"
  key_links:
    - from: "src/consolidation/run-sleep-pass.ts"
      to: "node_scope table"
      via: "upsertNodeScope after node upsert, scope from contributing episode cwd"
      pattern: "upsertNodeScope"
    - from: "src/adapter/ambient-recall.ts buildHookOutput"
      to: "recalled fact lines"
      via: "getNodeScope prefix [scope]"
      pattern: "getNodeScope"
---

<objective>
Add a single-tenant **provenance** annotation to consolidated facts: a sparse `node_scope` sidecar (D-S2), a pure `cwd → scope` derivation helper (D-S3), the store writer/reader, the consolidation wiring that stamps scope from contributing episodes' cwd, and scope surfacing in recall output (D-S6). Retrieval ranking is untouched (D-S1). This is the engine layer; the importer + migration are plan 02.

Purpose: give every fact an attributable origin so the `projects/*/memory` stores can be migrated and retired, without breaking the global-recall thesis or the single-tenant boundary.
Output: schema vN+1 with node_scope, src/lib/scope.ts, store methods, consolidation write, recall surfacing, four test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.3-scope-aware-provenance-memory-importer/999.3-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: node_scope sidecar table + schema migration (D-S2)</name>
  <files>src/db/schema.ts, tests/node-scope-schema.test.ts</files>
  <read_first>
    - src/db/schema.ts (SCHEMA_VERSION at line 11 — CONFIRM current value, was 9; the node_temporal CREATE TABLE block ~line 121 as the exact sidecar precedent; the version-guard / migration approach)
    - 999.3-CONTEXT.md (D-S2)
  </read_first>
  <behavior>
    - Fresh in-memory DB → initSchema → node_scope exists: node_id (PK, FK→node.id), scope (NOT NULL), updated_at (NOT NULL).
    - SCHEMA_VERSION === current+1 (was 9 → 10).
    - Idempotent: initSchema twice no error; a DB lacking node_scope gains it via CREATE TABLE IF NOT EXISTS (additive, no ALTER, no BEGIN/COMMIT wrap — mirrors node_temporal).
  </behavior>
  <action>
    Bump SCHEMA_VERSION by 1. Add `CREATE TABLE IF NOT EXISTS node_scope (node_id TEXT PRIMARY KEY REFERENCES node(id), scope TEXT NOT NULL, updated_at INTEGER NOT NULL)` to the DDL after the node_temporal block. Optional index `CREATE INDEX IF NOT EXISTS idx_node_scope_scope ON node_scope(scope)` for future scope queries. node_scope is a DERIVED operational annotation mirroring node_temporal — node stays the pure belief record (faithfulness).
  </action>
  <verify><automated>npx vitest run tests/node-scope-schema.test.ts</automated></verify>
  <acceptance_criteria>
    - SCHEMA_VERSION literal incremented by exactly 1.
    - PRAGMA table_info(node_scope): node_id PK, scope NOT NULL, updated_at NOT NULL.
    - initSchema idempotent; creates node_scope on a DB that lacks it.
  </acceptance_criteria>
  <done>node_scope sidecar live at the new schema version, migration idempotent, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cwd → scope derivation helper (D-S3)</name>
  <files>src/lib/scope.ts, tests/scope-derive.test.ts</files>
  <read_first>
    - 999.3-CONTEXT.md (D-S3 + cwd→scope mapping section)
    - src/lib/config.ts or wherever known project roots / home are configured (check for an existing project-root list; if none, derive from cwd basename)
  </read_first>
  <behavior>
    - cwdToScope('/Users/vtx/VTX') === 'vtx'; cwdToScope('/Users/vtx/brain-memory') === 'brain-memory'.
    - cwdToScope('') === 'global'; cwdToScope('/Users/vtx') === 'global'; cwdToScope(undefined) === 'global'.
    - A cwd under the resume project / personal dirs → 'global'.
    - resolveNodeScope(['vtx']) === 'vtx'; resolveNodeScope(['vtx','vtx']) === 'vtx'; resolveNodeScope(['vtx','putyouon']) === 'global'; resolveNodeScope([]) === 'global'.
  </behavior>
  <action>
    Create src/lib/scope.ts exporting (1) `cwdToScope(cwd: string | undefined): string` — normalize, map a known project root to its slug (basename of the project dir), and return 'global' for home/empty/unknown/personal cwds; (2) `resolveNodeScope(scopes: string[]): string` — return the single distinct non-global scope if all contributing scopes agree, else 'global'. Pure functions, no DB. Keep the personal/global predicate small and documented (home dir, resume project, anything not a recognized project root).
  </action>
  <verify><automated>npx vitest run tests/scope-derive.test.ts</automated></verify>
  <acceptance_criteria>
    - cwdToScope + resolveNodeScope exported and cover all behavior cases above.
    - npx tsc --noEmit passes.
  </acceptance_criteria>
  <done>Pure cwd→scope + multi-source resolution helpers, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SemanticStore.upsertNodeScope + getNodeScope (D-S2)</name>
  <files>src/db/semantic-store.ts, src/lib/types.ts, tests/node-scope-store.test.ts</files>
  <read_first>
    - src/db/semantic-store.ts (upsertNode ~204 + the upsertNodeTemporal writer added in Phase 20 as the exact pattern to copy)
    - src/lib/types.ts
  </read_first>
  <behavior>
    - upsertNodeScope({node_id, scope, updated_at}) inserts for an existing node; second call REPLACES (one row, latest scope) — idempotent on re-consolidation.
    - getNodeScope(node_id) returns the stored scope, or undefined/null if none.
    - getNodeScopes(node_ids[]) batch read for recall surfacing (avoids N queries).
  </behavior>
  <action>
    Add `UpsertNodeScopeParams { node_id: string; scope: string; updated_at: number }` to types.ts. Add `upsertNodeScope` (INSERT OR REPLACE INTO node_scope ...) and `getNodeScope` / `getNodeScopes` to SemanticStore, following the upsertNodeTemporal prepared-statement convention.
  </action>
  <verify><automated>npx vitest run tests/node-scope-store.test.ts</automated></verify>
  <acceptance_criteria>
    - upsertNodeScope/getNodeScope/getNodeScopes exist; idempotent replace verified (COUNT=1 after two upserts).
    - Batch read returns a map for multiple ids.
  </acceptance_criteria>
  <done>Idempotent node_scope writer + single/batch readers, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Stamp scope during consolidation (D-S3)</name>
  <files>src/consolidation/run-sleep-pass.ts, tests/consolidation-scope.test.ts</files>
  <read_first>
    - src/consolidation/run-sleep-pass.ts (where episodes are turned into / merged into nodes; find where contributing episode(s) for a node are known, and where node upsert happens)
    - src/ingest/pipeline.ts:49,87 (episode.cwd is captured and stored — confirm the episode row exposes cwd to the consolidator)
    - src/lib/scope.ts (cwdToScope/resolveNodeScope)
  </read_first>
  <behavior>
    - After a node is upserted/updated by consolidation, node_scope is written: scope = resolveNodeScope(contributing episodes' cwd mapped via cwdToScope).
    - A node built from a single VTX-cwd episode → scope 'vtx'. From episodes spanning two projects → 'global'. From an empty-cwd episode → 'global'.
    - Pure additive: removing the scope write leaves node consolidation identical (no change to node values, dedup, tombstoning, judge behavior). Scope write failures must not abort consolidation (best-effort, logged).
  </behavior>
  <action>
    At the point the consolidator finalizes a node from its contributing episode(s), collect those episodes' cwd, map each via cwdToScope, resolve via resolveNodeScope, and call upsertNodeScope({node_id, scope, updated_at: now}). Wrap in try/catch so a scope-write error never breaks the belief write (scope is a derived annotation). Do NOT touch retrieval, ranking, dedup, or judge logic.
  </action>
  <verify><automated>npx vitest run tests/consolidation-scope.test.ts</automated></verify>
  <acceptance_criteria>
    - Test ingests episodes with distinct cwds, runs consolidation, asserts node_scope rows match expected slugs/global.
    - Test asserts a multi-project node resolves to 'global'.
    - Existing consolidation test suite still green (no behavioral regression).
  </acceptance_criteria>
  <done>Consolidation stamps node_scope from cwd, additively and best-effort, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Surface scope in recall output (D-S6) — NO ranking change (D-S1)</name>
  <files>src/adapter/ambient-recall.ts, tests/ (extend existing recall-output test or add one)</files>
  <read_first>
    - src/adapter/ambient-recall.ts (buildHookOutput ~148; where recalled facts are formatted into additionalContext)
    - src/db/semantic-store.ts getNodeScopes (from Task 3)
  </read_first>
  <behavior>
    - A recalled fact whose node has scope 'vtx' renders with a '[vtx]' marker; scope 'global' renders '[global]' (or is omitted for global — pick one and test it).
    - Retrieval order and selection are IDENTICAL to before — scope is read AFTER ranking, for display only. No scope value enters the score or filter.
  </behavior>
  <action>
    After the recall set is ranked and selected, batch-read scopes via getNodeScopes and prefix each surfaced fact line with its scope marker in buildHookOutput. Keep the change display-only; assert in a test that the selected node ids/order are unaffected by scope.
  </action>
  <verify><automated>npx vitest run tests/ -t scope</automated></verify>
  <acceptance_criteria>
    - Recalled facts display their scope marker.
    - A test asserts ranking/selection is byte-identical with and without the scope-surfacing code path (display-only guarantee).
  </acceptance_criteria>
  <done>Recall output shows fact provenance; ranking provably unchanged; tests green.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-S3-01 | Tampering | cwd string from hook input | mitigate | cwdToScope normalizes + defaults unknown/empty to 'global'; no path is trusted as a project unless it matches a known root |
| T-S3-02 | Faithfulness | scope as belief signal | mitigate | scope is a derived sidecar, never feeds retrieval ranking/score/filter (D-S1) — asserted by the display-only test in Task 5 |
| T-S3-SC | Tampering | package installs | mitigate | net-zero deps — TS + SQL DDL only |
</threat_model>

<verification>
- All four new test files green; existing consolidation + recall suites green.
- `npx tsc --noEmit` clean.
- `grep -c "node_scope" src/db/schema.ts` ≥ 1; SCHEMA_VERSION incremented by exactly 1 vs the pre-plan value.
- Manual: ranking-unchanged assertion present in Task 5 test.
</verification>

<success_criteria>
node_scope sidecar live (D-S2); scope derived from cwd at consolidation, multi-project→global (D-S3); recall surfaces provenance without altering ranking (D-S1/D-S6); single-tenant only, no namespace isolation; consolidation belief behavior unchanged.
</success_criteria>

<output>
Create `999.3-01-SUMMARY.md` when done.
</output>
