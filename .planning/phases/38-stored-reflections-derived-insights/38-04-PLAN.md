---
phase: 38-stored-reflections-derived-insights
plan: 04
type: execute
wave: 3
depends_on: ["38-02", "38-03"]
files_modified:
  - scripts/eval/replay-ku-harness.cjs
  - scripts/eval/results/38-insight-tokens.json
autonomous: false
requirements: [REFLECT-02]
must_haves:
  truths:
    - "The replay harness records per-case compose/payload token counts (not just answer correctness)"
    - "The harness runs the same cases insight-OFF vs insight-ON and reports the compose-token delta"
    - "The eval proves a compose-token reduction with no quality regression beyond the Phase-35 small-tolerance band"
    - "A founder checkpoint surfaces the measured win + a recommended insightSurfacingEnabled activation default + reflectConfidenceCeiling verification"
  artifacts:
    - path: "scripts/eval/replay-ku-harness.cjs"
      provides: "compose/payload token measurement + insight-on/off mode + delta in the results meta"
      contains: "composeTokens"
    - path: "scripts/eval/results/38-insight-tokens.json"
      provides: "the recorded on/off token + correctness numbers (the REFLECT-02 evidence artifact)"
      contains: "composeTokens"
  key_links:
    - from: "scripts/eval/replay-ku-harness.cjs"
      to: "config.insightSurfacingEnabled"
      via: "harness flag toggles the recall surfacing path on/off per run"
      pattern: "insightSurfacingEnabled"
---

<objective>
Prove the REFLECT-02 success bar: extend the existing KU replay harness to record
compose/payload tokens (not just answer correctness), run the same cases insight-OFF vs
insight-ON, and report the compose-token delta with the no-regression check against the
Phase-35 small-tolerance band. Then surface a founder checkpoint with the measured win, a
recommended `insightSurfacingEnabled` activation default (ship-on vs ship-dark), and the
`reflectConfidenceCeiling` verification — the two prove-before-activate knobs the founder
flagged.

Purpose: REFLECT-02 passes ONLY by demonstrating a decisive compose-token reduction with no
quality regression on the existing harness — the win is a MEASURED reduction, not an assumption.
Output: token-instrumented harness, the recorded on/off results JSON, and the founder decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-stored-reflections-derived-insights/38-CONTEXT.md
@.planning/phases/38-stored-reflections-derived-insights/38-PATTERNS.md
@.planning/phases/38-stored-reflections-derived-insights/38-02-SUMMARY.md
@.planning/phases/38-stored-reflections-derived-insights/38-03-SUMMARY.md

<interfaces>
<!-- The harness conventions to extend. Use directly. -->

From scripts/eval/replay-ku-harness.cjs (header L1-31):
  - consolidate once per case, then retrieve+answer+score; sweep mode writes per-variant result files
  - "Load-bearing output" convention: emits judge-engagement counters (tombstone/contradict/dup-mint)
  - Results written to --out with meta (embedder model, cache id, date, commit)
  - --dry-run = ZERO API/LLM calls; real run requires OPENAI_API_KEY (embed) + judge keys
  - the no-regression tolerance band is the same small band as Phase 35 D-07

From 38-02 / 38-03 (already shipped):
  - InsightReflector runs in Phase C (consolidate); insights are synthesized during the harness consolidate step
  - config.insightSurfacingEnabled toggles the recall surfacing path (dark by default)
  - config.reflectConfidenceCeiling (0.6, verify-with-founder)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument the harness with compose-token measurement + insight on/off mode</name>
  <files>scripts/eval/replay-ku-harness.cjs, scripts/eval/results/38-insight-tokens.json</files>
  <read_first>
    - scripts/eval/replay-ku-harness.cjs (header L1-31 conventions, the consolidate to retrieve to answer to score loop, the meta-output block, the sweep-mode result-file naming)
    - scripts/eval/longmemeval-harness.cjs (the LongMemEval sibling — mirror the same token field only if it shares the compose path)
    - docs/evals.md (the harness output convention + the no-regression bar wording)
    - .planning/phases/38-stored-reflections-derived-insights/38-PATTERNS.md (section on replay-ku-harness — the token-measurement directive)
    - .planning/phases/35-recency-strength-retrieval-ranking/35-CONTEXT.md (D-07 — the small-tolerance no-regression band to reuse)
  </read_first>
  <action>
    Extend the harness to record, per case, the compose/payload token count fed to the answer
    LLM — count the tokens of the assembled neighborhood-or-insight payload (the same string recall
    composes from), stored as a `composeTokens` field alongside the existing judge-engagement counters
    and the correctness score. Add an insight-mode toggle: run each case TWICE (or as a two-value sweep,
    mirroring the existing sweep-weights mode): once with `config.insightSurfacingEnabled=false`
    (baseline) and once with `=true` (insight-on), writing both into the result with per-case + aggregate
    `composeTokensOff`, `composeTokensOn`, and `composeTokenReductionPct`. Add a `quality` comparison:
    the insight-on correctness must stay within the Phase-35 small-tolerance band of insight-off
    (no regression); set a `regression` flag in the meta if it falls outside the band. Preserve the
    existing meta (embedder model, cache id, date, commit) and the --dry-run zero-API contract. Write
    the recorded numbers to `scripts/eval/results/38-insight-tokens.json` (the REFLECT-02 evidence
    artifact). Mirror the same `composeTokens` field into longmemeval-harness.cjs ONLY if it shares the
    compose-payload path through a clean seam — do not refactor the LongMemEval scorer otherwise.
  </action>
  <acceptance_criteria>
    - `grep -q "composeTokens" scripts/eval/replay-ku-harness.cjs` true; the harness records a per-case payload token count.
    - `grep -q "insightSurfacingEnabled" scripts/eval/replay-ku-harness.cjs` true; the harness runs both modes (off baseline + on) and records composeTokensOff / composeTokensOn (or sweep-file equivalents).
    - A no-regression check exists: `grep -qiE "regression|tolerance" scripts/eval/replay-ku-harness.cjs` true; insight-on correctness within the Phase-35 band produces no regression flag.
    - `node scripts/eval/replay-ku-harness.cjs --dry-run --out /tmp/replay-38-dry.json` exits 0 with ZERO API/LLM calls (the dry-run contract preserved) and the output JSON contains the new token fields.
    - Meta preserved: the result JSON still carries embedder model + cache id + date + commit.
    - `node -c scripts/eval/replay-ku-harness.cjs` (syntax check) passes.
  </acceptance_criteria>
  <verify>
    <automated>node -c scripts/eval/replay-ku-harness.cjs && node scripts/eval/replay-ku-harness.cjs --dry-run --out /tmp/replay-38-dry.json && grep -q composeTokens /tmp/replay-38-dry.json && echo TOKEN_FIELDS_OK</automated>
  </verify>
  <done>The harness records compose/payload tokens per case, runs insight-off vs insight-on, reports the reduction + a no-regression flag, preserves the dry-run zero-API contract and the result meta, and writes the evidence JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Run the eval (insight-off vs insight-on) and record the win</name>
  <files>scripts/eval/results/38-insight-tokens.json</files>
  <read_first>
    - scripts/eval/replay-ku-harness.cjs (the real-run invocation from the header, the env-var requirements)
    - docs/evals.md (the run procedure + where results land)
    - .planning/STATE.md (the API budget constraints — confirm before any paid run; embedding is the cost driver)
  </read_first>
  <action>
    Run the instrumented harness for real over the cached KU cases in both modes (insight-off
    baseline, insight-on). The replay path re-uses cached extractions (no re-extraction cost); the
    only real cost is embedding + the answer-LLM compose calls (subscription-billed judge). Record the
    aggregate compose-token reduction and the correctness delta into `scripts/eval/results/38-insight-tokens.json`.
    Because this is a paid/live run (embedding API + answer LLM), DO NOT execute it autonomously if the
    auto-mode classifier blocks coordinator-authorized paid mutations — in that case, prepare the exact
    one-line invocation and the recorded-from-a-prior-dry-run structure, and defer the live run to the
    founder checkpoint (Task 3) for explicit consent + budget confirmation. If consent is already in
    context, run it and capture the numbers.
  </action>
  <acceptance_criteria>
    - `scripts/eval/results/38-insight-tokens.json` exists and contains `composeTokensOff`, `composeTokensOn`, `composeTokenReductionPct`, the correctness scores for both modes, and the meta (embedder, cache id, date, commit).
    - The recorded `composeTokenReductionPct` is a real number (or the file documents that the live run is deferred to the Task 3 checkpoint for founder budget consent, with the exact invocation recorded).
    - If run: insight-on correctness is within the Phase-35 no-regression band (no regression flag set), OR the deviation is reported for the founder decision.
  </acceptance_criteria>
  <verify>
    <automated>test -f scripts/eval/results/38-insight-tokens.json && grep -q composeTokenReductionPct scripts/eval/results/38-insight-tokens.json && echo RESULTS_RECORDED</automated>
  </verify>
  <done>The on/off compose-token reduction + correctness deltas are recorded in the results JSON (or the live run is explicitly deferred to the Task 3 checkpoint with the exact invocation + budget note).</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Founder decision — activation default + confidence ceiling</name>
  <decision>Activate insight surfacing by default, and confirm reflectConfidenceCeiling</decision>
  <action>Present the recorded compose-token reduction %, the insight-on vs insight-off correctness delta, and the no-regression band check from scripts/eval/results/38-insight-tokens.json. Recommend an insightSurfacingEnabled activation default (on vs dark) based on whether the reduction is decisive AND correctness is within the band, and ask the founder to confirm or adjust reflectConfidenceCeiling (currently 0.6). If the live eval was deferred, request paid-run authorization + budget confirmation here before running it.</action>
  <context>
    REFLECT-02 passes only on a measured compose-token reduction with no quality regression.
    Two knobs were deliberately left prove-before-activate (Phase-35 dark-default posture):
    `insightSurfacingEnabled` ships DARK (false), and `reflectConfidenceCeiling` defaults to 0.6
    pending founder verification. This checkpoint surfaces the measured numbers (compose-token
    reduction %, correctness delta vs the no-regression band) and asks the founder to make the two
    calls the CONTEXT flagged. If the live eval was deferred for budget consent (Task 2), this is
    also where the founder authorizes the paid run (embedding + answer-LLM, ~subscription + embed
    cost — confirm against the ~$14-15 remaining budget).
  </context>
  <options>
    <option id="ship-on">
      <name>Flip insightSurfacingEnabled to true (ship on)</name>
      <pros>Captures the measured compose-token win immediately for customer-zero recall.</pros>
      <cons>Hot-path behavior change; only justified if the reduction is decisive AND correctness is within the band.</cons>
    </option>
    <option id="ship-dark">
      <name>Keep insightSurfacingEnabled=false (ship dark)</name>
      <pros>Zero-risk merge; the mechanism lands but recall behavior is unchanged until a later activation.</pros>
      <cons>The token win is not realized until a follow-up flips the flag.</cons>
    </option>
    <option id="tune-ceiling">
      <name>Adjust reflectConfidenceCeiling (from 0.6) and/or rerun before deciding</name>
      <pros>Pins the ceiling against the live brain's typical schema confidence before committing.</pros>
      <cons>One more eval cycle.</cons>
    </option>
  </options>
  <resume-signal>Select ship-on, ship-dark, or tune-ceiling (with a ceiling value); and authorize the paid eval run if it was deferred.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval run → live cost | A real harness run spends embedding API + answer-LLM tokens; must be founder-authorized against the remaining budget. |
| activation flag | Flipping insightSurfacingEnabled changes the hot recall path; only justified by a measured, no-regression win. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-10 | Information Disclosure (inflated metrics) | eval results | mitigate | the reduction is a measured number recorded with meta (embedder/cache/date/commit) + the no-regression band check — no rounded or assumed wins (CLAUDE.md no-inflated-metrics hard rule). |
| T-38-11 | Denial of Service (budget) | live eval run | mitigate | the paid run is gated behind the founder decision checkpoint (autonomous: false) with explicit budget confirmation; the replay path re-uses cached extractions so only embed + compose cost applies. |
| T-38-SC | Tampering | npm/pip/cargo installs | mitigate | net-zero new runtime deps — NO package installs in this plan (no package.json change); slopcheck N/A. |
</threat_model>

<verification>
- `node -c scripts/eval/replay-ku-harness.cjs` passes; `--dry-run` exits 0 with zero API calls and emits the new token fields.
- `scripts/eval/results/38-insight-tokens.json` records composeTokensOff/On + reduction + correctness for both modes (or documents the deferred live run + exact invocation).
- The founder decision (activation default + ceiling) is captured.
- No package.json / lockfile change.
</verification>

<success_criteria>
- A measured compose-token reduction with no quality regression on the existing harness (REFLECT-02 success bar) — recorded, not assumed.
- The founder pins insightSurfacingEnabled (on/dark) and reflectConfidenceCeiling against the live numbers.
</success_criteria>

<output>
Create `.planning/phases/38-stored-reflections-derived-insights/38-04-SUMMARY.md` when done.
</output>
