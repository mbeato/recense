---
phase: 61-corpus-chrome-index-column-project-browsing
plan: 11
type: execute
wave: 1
depends_on: []
gap_closure: true
gaps: ["GAP-7"]
requirements: ["GAP-7"]
files_modified:
  - src/viz/modules/corpus.js
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Exiting project focus (active-row toggle, Esc, canvas click) plays the same zoom/frame animation as focusing, in reverse — same duration/easing — instead of snapping."
  artifacts:
    - path: "src/viz/modules/corpus.js"
      provides: "focusCorpusProject(null) branch animates an inverse zoomToFit over the full visible set at CORPUS_FOCUS_TRANSITION_MS instead of the instant reassertPaint()+fitAndClamp() snap"
      contains: "CORPUS_FOCUS_TRANSITION_MS"
  key_links:
    - from: "focusCorpusProject(null) unfocus branch"
      to: "animated CorpusGraph.zoomToFit(CORPUS_FOCUS_TRANSITION_MS, 40, isNodeVisible)"
      via: "inverse-of-focus animated frame-out"
      pattern: "zoomToFit\\(\\s*CORPUS_FOCUS_TRANSITION_MS"
---

<objective>
Close GAP-7: focusing a project animates (good), but exiting focus SNAPS — the `focusCorpusProject(null)`
branch calls `reassertPaint()` + `fitAndClamp()`, both 0ms/instant. Founder direction: unfocus must play the
identical animation inverted — an animated zoom back out to the full visible set, same duration/easing as focus.

Purpose: focus and unfocus should feel like one reversible move. Focus already animates via
`CorpusGraph.zoomToFit(CORPUS_FOCUS_TRANSITION_MS, 40, <project filter>)`; the null (unfocus) branch must mirror
that with the full-visible-set filter so the camera glides back instead of jumping.
Output: active-row toggle, Esc, and empty-canvas click all animate the zoom-out over CORPUS_FOCUS_TRANSITION_MS.
Constraint: presentation-only, read-only viz; no new deps; reuse the existing zoomToFit + MAX_ZOOM clamp idiom.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-CONTEXT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-PATTERNS.md

<interfaces>
<!-- Live source, confirmed 2026-07-14 -->
corpus.js `ctx.focusCorpusProject = function focusCorpusProject(scope)` (~line 773-796):
  - null branch (~775-780): `focusedScope = null; reassertPaint(); fitAndClamp(); ctx.syncCorpusFocus(null); return;`
    — fitAndClamp() is the SNAP to fix (it does `zoomToFit(0, 40, isNodeVisible)` + immediate MAX_ZOOM clamp).
  - set branch (~782-796): guards `projectScopes.has(scope)`, sets `focusedScope = scope; reassertPaint();` then
    `CorpusGraph.zoomToFit(CORPUS_FOCUS_TRANSITION_MS, 40, (node) => projectScopeOf(node) === scope && isNodeVisible(node));`
    then `if (CorpusGraph.zoom() > MAX_ZOOM) CorpusGraph.zoom(MAX_ZOOM, 0);` then `ctx.syncCorpusFocus(focusedScope)`.
    THIS is the exact animation idiom the null branch must mirror (with the full-visible filter).
corpus.js `fitAndClamp()` (~566-578): `zoomToFit(0, 40, isNodeVisible)` + 0ms MAX_ZOOM clamp — the instant snap.
  Keep it as-is (still used by refitCorpus / onBeforeReveal / setCorpusProjectExpanded); only the unfocus branch stops calling it.
corpus.js `isNodeVisible(node)` — the visible-node predicate already used by fitAndClamp's filter.
corpus.js imports `CORPUS_FOCUS_TRANSITION_MS` (~line 34) and defines `MAX_ZOOM` (~line 118).
Callers of the null branch (unchanged): index.js active-row toggle → `ctx.focusCorpusProject(null)`;
  corpus.js `.onBackgroundClick` (~436) → `ctx.focusCorpusProject(null)`; the Esc listener (~814-819) → `ctx.focusCorpusProject(null)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animate the unfocus branch as the inverse of focus (corpus.js focusCorpusProject null branch)</name>
  <files>src/viz/modules/corpus.js</files>
  <read_first>
    - src/viz/modules/corpus.js (`ctx.focusCorpusProject` ~773-796 — BOTH branches, so the null branch mirrors the
      set branch's zoomToFit+clamp exactly; `fitAndClamp` ~566-578 to see the snap it replaces; `isNodeVisible` and
      the `CORPUS_FOCUS_TRANSITION_MS`/`MAX_ZOOM` symbols)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md (GAP-7 truth + founder_direction —
      "animated zoomToFit over the full visible set" is the named fix)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-PATTERNS.md (any focus/zoom animation notes)
  </read_first>
  <action>
    Rewrite ONLY the `scope === null` branch of `ctx.focusCorpusProject` so unfocus animates the inverse of focus:
    1. Keep `focusedScope = null;` first (so the subsequent repaint/frame reflects the cleared focus — chapters of
       the previously-focused project hide, dim clears).
    2. Keep `reassertPaint();` (re-asserts visibility/paint accessors after clearing focus — same order as the set branch).
    3. Replace the `fitAndClamp();` snap with an ANIMATED frame-out mirroring the set branch: call
       `CorpusGraph.zoomToFit(CORPUS_FOCUS_TRANSITION_MS, 40, (node) => isNodeVisible(node));` inside a try/catch
       (same guard style as the set branch), then the identical MAX_ZOOM clamp tail
       (`if (typeof CorpusGraph.zoom === 'function' && CorpusGraph.zoom() > MAX_ZOOM) CorpusGraph.zoom(MAX_ZOOM, 0);`).
       Use the same `40` padding and the same `CORPUS_FOCUS_TRANSITION_MS` duration as focus so the easing/duration match.
    4. Keep the `if (typeof ctx.syncCorpusFocus === 'function') ctx.syncCorpusFocus(null);` notify and the `return;`.
    5. Do NOT change the set branch, `fitAndClamp()`, `setCorpusProjectExpanded`, `refitCorpus`, or the Esc/onBackgroundClick
       wiring — they still call `focusCorpusProject(null)` and now get the animated exit for free.
    6. Update the `focusCorpusProject` doc comment: "null clears focus and ANIMATES a zoom-out to the full visible
       set (inverse of focus, same CORPUS_FOCUS_TRANSITION_MS), not an instant snap (GAP-7)."
    Note (do not use fitAndClamp here): fitAndClamp is intentionally 0ms to avoid tiny-graph overshoot on first paint;
    the unfocus set is the full visible corpus (not a tiny cluster), so animating it matches the set branch's own
    animated behavior — the MAX_ZOOM clamp tail still guards any overshoot.
  </action>
  <verify>
    <automated>cd /Users/vtx/brain-memory && npx tsc --noEmit 2>/dev/null; node -e "const s=require('fs').readFileSync('src/viz/modules/corpus.js','utf8'); const i=s.indexOf('function focusCorpusProject'); const nullBranch=s.slice(i, s.indexOf('if (!projectScopes.has(scope))', i)); if(!/scope === null/.test(nullBranch)){console.error('null branch not found');process.exit(1);} if(/fitAndClamp\(\)/.test(nullBranch)){console.error('unfocus branch still calls fitAndClamp (snap)');process.exit(1);} if(!/zoomToFit\(\s*CORPUS_FOCUS_TRANSITION_MS/.test(nullBranch)){console.error('unfocus branch does not animate zoomToFit at CORPUS_FOCUS_TRANSITION_MS');process.exit(1);} console.log('ok: unfocus animates inverse of focus');" && npx vitest run tests/viz-corpus-graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The `scope === null` branch no longer calls `fitAndClamp()` (source assertion via the node check).
    - The `scope === null` branch calls `CorpusGraph.zoomToFit(CORPUS_FOCUS_TRANSITION_MS, 40, (node) => isNodeVisible(node))`
      followed by the same MAX_ZOOM clamp tail as the set branch (source assertion).
    - `focusedScope = null` is still set before the repaint/frame; `ctx.syncCorpusFocus(null)` is still called.
    - The set branch, `fitAndClamp()`, `refitCorpus`, `setCorpusProjectExpanded`, and the Esc/onBackgroundClick
      handlers are unchanged (they route through `focusCorpusProject(null)`).
    - `npx tsc --noEmit` reports no new errors; `npx vitest run tests/viz-corpus-graph.test.ts` passes.
  </acceptance_criteria>
  <done>Unfocus (toggle/Esc/canvas-click) animates a zoom-out over the full visible set at CORPUS_FOCUS_TRANSITION_MS — the inverse of focus, not a snap; tsc clean; corpus-graph suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viz canvas camera | Animated zoom is a client-only presentation move; no server surface, no write, no LLM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-21 | Denial of Service | animated zoomToFit on unfocus | accept | Bounded single animation identical to the already-shipped focus animation; MAX_ZOOM clamp tail guards overshoot; try/catch swallows lib-version differences. |
</threat_model>

<verification>
- Automated: node source check confirms the null branch animates (no fitAndClamp, uses CORPUS_FOCUS_TRANSITION_MS);
  tsc clean; corpus-graph suite green.
- Behavioral (deferred to 61-14 founder checkpoint): focusing then exiting (toggle/Esc/canvas) glides the camera
  back out at the same speed/easing as focusing — no snap.
</verification>

<success_criteria>
- Unfocus animates as the inverse of focus (same duration/easing) over the full visible set.
- tsc clean; corpus-graph suite green.
</success_criteria>

<output>
Create `.planning/phases/61-corpus-chrome-index-column-project-browsing/61-11-SUMMARY.md` when done.
</output>
