---
phase: 61-corpus-chrome-index-column-project-browsing
plan: 04
type: execute
wave: 3
depends_on: ["61-01", "61-02", "61-03"]
files_modified:
  - src/viz/modules/constants.js
autonomous: false
requirements: [D1, D2, D3, D4]
user_setup: []
must_haves:
  truths:
    - "On the live install, the index reads as glass and as a navigable collapsible tree (D1 resolved)."
    - "Clicking a project focuses it (zoom + dim), clicking away/Esc exits, and browsing from project to docs and back is the primary path (D2 resolved)."
    - "The graph is decluttered: at-rest labels don't pile up, hover dims + amber-highlights the subtree, and chapters appear only when browsing their project (D3 resolved)."
    - "The reader round-trip (doc click -> reader -> back) still works exactly as before (D4 no regression)."
    - "The four provisional feel constants are ratcheted to their founder-approved live values in constants.js."
  artifacts:
    - path: "src/viz/modules/constants.js"
      provides: "CORPUS_FOCUS_DIM_OPACITY, CORPUS_HOVER_DIM_OPACITY, CORPUS_LABEL_ZOOM_THRESHOLD, CORPUS_FOCUS_TRANSITION_MS at founder-approved values"
      contains: "CORPUS_FOCUS_DIM_OPACITY"
  key_links:
    - from: "founder live-install feedback"
      to: "constants.js CORPUS_* tunables"
      via: "ratcheted values per the 54/56/57/58/59 closing-checkpoint pattern"
      pattern: "CORPUS_"
---

<objective>
Closing founder live-install sign-off (the 54/56/57/58/59 checkpoint pattern): with the corpus view
open on the live install, verify the four founder defects D1-D4 are resolved and ratchet the four
provisional feel constants to their founder-approved values.

Purpose: The dim opacities, label zoom threshold, and focus-transition timing were shipped as
provisional constants (Plan 02) explicitly to be tuned live — the founder's eye is the only valid
signal for these feel values. This plan closes the loop.
Output: Founder-confirmed resolution of D1-D4 and the final ratcheted values in constants.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-CONTEXT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UI-SPEC.md
@src/viz/modules/constants.js
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Founder live-install sign-off on D1-D4 + tune the four provisional constants</name>
  <files>src/viz/modules/constants.js</files>
  <action>
    Pause for the founder. Serve the viz on the live install, drive the D1-D4 verification steps below
    with the founder, and iteratively edit ONLY the four CORPUS_* tunables in src/viz/modules/constants.js
    (CORPUS_FOCUS_DIM_OPACITY, CORPUS_HOVER_DIM_OPACITY, CORPUS_LABEL_ZOOM_THRESHOLD,
    CORPUS_FOCUS_TRANSITION_MS) until each feel value is founder-approved. After any edit, re-run the
    invariants suite. Do not change any logic, color literal, or other constant. Record the final
    approved values in the SUMMARY.
  </action>
  <what-built>
    - D-01/D-02/D-08 (Plan 01 + 03): the index column is now a glass, collapsible Obsidian-style tree
      — collapsed projects with chevron + doc count; expand reveals docs in place; the #index-reopen
      handle is glassed.
    - D-03/D-04 (Plan 02 + 03): clicking a project name focuses its cluster (zoom + dim others) and
      expands its row; Esc / empty-canvas click / clicking another project exits or switches focus.
    - D-05/D-06/D-07 (Plan 02): graph-node hover highlights the subtree in amber AND dims the rest;
      labels are tiered (hubs always, subjects on zoom/focus, chapters on hover); the chapter-toggle
      button is gone and chapters appear only when browsing (focus/expand) their project.
    - The four provisional constants ship at CORPUS_FOCUS_DIM_OPACITY=0.18, CORPUS_HOVER_DIM_OPACITY=0.30,
      CORPUS_LABEL_ZOOM_THRESHOLD=1.2, CORPUS_FOCUS_TRANSITION_MS=500 (constants.js).
  </what-built>
  <how-to-verify>
    Start the viz on the live install and open the corpus view:
    1. Run `recense viz` (or the project's viz-serve command) and open the browser at the printed URL.
    2. Switch to the corpus view (the brain/corpus rail toggle).
    3. D1 (index): confirm the index reads as glass (blur/tint over the graph) and as a navigable tree
       — projects collapsed with chevron + count, expanding reveals docs, no wall of text.
    4. D2 (browsing): click a project NAME — the graph should zoom/frame that project's cluster and dim
       the rest; the project's row should expand. Click empty canvas (or press Esc) to exit focus; click
       a different project to switch focus. Confirm this is the primary browse path.
    5. D3 (graph declutter): at rest, confirm labels do NOT pile up (only project-hub labels show).
       Hover a node directly in the graph — confirm the subtree lights amber AND the rest dims (not
       flat). Confirm chapter docs appear only when their project is focused or its tree row is expanded.
    6. D4 (reader — MUST NOT regress): click a doc row (or a doc node) — the reader should open in place
       over the corpus; close it and confirm you return to the corpus with focus state intact.
    7. Feel-tuning: if the focus dim is too strong/weak, the hover dim too much/little, subject labels
       fade in at the wrong zoom, or the focus zoom feels too fast/slow — tell the executor the desired
       direction. The executor edits the four CORPUS_* constants and you re-check until it feels right.
  </how-to-verify>
  <verify>
    <automated>cd /Users/vtx/brain-memory && npx tsc --noEmit 2>/dev/null; npx vitest run tests/viz-activity-palette-invariants.test.ts</automated>
  </verify>
  <done>Founder confirms D1-D4 resolved (D4 no-regression) and the four CORPUS_* constants sit at their approved values; invariants suite green.</done>
  <resume-signal>Type "approved" (optionally with final constant values), or describe what still feels off (which defect, which direction to tune).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder feedback -> constants.js | Only the four named CORPUS_* tunables change; no color literal, no logic change. |
| viz server -> browser | Presentation-only; no server surface touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-10 | Tampering | constants.js tuning edits | mitigate | Only the four numeric CORPUS_* tunables change; run `npx vitest run tests/viz-activity-palette-invariants.test.ts` after any edit to confirm no invariant regressed. |
</threat_model>

<verification>
- Founder types "approved" for D1-D4 on the live install.
- After any constant edit: `npx tsc --noEmit` clean and `npx vitest run tests/viz-activity-palette-invariants.test.ts` green.
- The final approved values recorded in constants.js and the SUMMARY.
</verification>

<success_criteria>
- D1-D4 confirmed resolved by the founder on the live install (D4 explicitly no-regression).
- The four CORPUS_* constants ratcheted to founder-approved values.
- Invariants suite green; tsc clean.
</success_criteria>

<output>
Create `.planning/phases/61-corpus-chrome-index-column-project-browsing/61-04-SUMMARY.md` when done, recording the final approved constant values and the founder's D1-D4 sign-off.
</output>
