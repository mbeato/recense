---
phase: 61-corpus-chrome-index-column-project-browsing
plan: 10
type: execute
wave: 1
depends_on: []
gap_closure: true
gaps: ["GAP-5"]
requirements: ["GAP-5"]
files_modified:
  - src/viz/css/styles.css
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Project rows have no curved/rounded borders — row hover/active surfaces are square-edged (or full-bleed) within the rail."
  artifacts:
    - path: "src/viz/css/styles.css"
      provides: ".index-row and .index-entry with NO border-radius (square-edged row surfaces) + a durable design-rule comment banning rounded row corners"
      contains: ".index-row"
  key_links:
    - from: ".index-row / .index-entry hover+active background"
      to: "square-edged (0 radius) row surface"
      via: "removed border-radius declaration"
      pattern: "index-row"
---

<objective>
Close GAP-5: the curved (rounded) bottom border on project labels reads as AI slop — founder verdict
"NEVER DO IT AGAIN." Remove the `border-radius: 6px` from the index row surfaces (.index-row and
.index-entry) so hover/active backgrounds are square-edged within the rail, and record a durable design
rule (in-code comment + memory) that the index rail NEVER uses rounded row corners.

Purpose: the rounded row corners are the single defect flagged on GAP-2's re-test — the row structure and
hover/active treatment are otherwise approved. This is a surgical CSS removal plus a guard against recurrence.
Output: .index-row and .index-entry render square-edged; the hover/active background treatment is unchanged
otherwise; a durable comment rule is in styles.css and a memory fact is recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-CONTEXT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UI-SPEC.md

<interfaces>
<!-- Current row-surface CSS (live source, confirmed 2026-07-14) -->
styles.css `.index-entry` (~line 1412-1422): a `display:block` row with `padding: 5px 8px;` and
  `border-radius: 6px;` (~line 1419). Hover rule `.index-entry:hover` sets `background: var(--index-search-focus-bg)`.
styles.css `.index-row` (~line 1432-1442): the project (folder) row (flex chevron+name+count) with
  `border-radius: 6px;` (~line 1439). Hover `.index-row:hover` sets `background: var(--index-search-focus-bg)`.
styles.css `.index-row.active, .index-row.active:hover` (~line 1449-1454): active-row treatment —
  `background: var(--index-row-active-bg)` + `box-shadow: inset 2px 0 0 var(--text-mauve-rest)` (left accent bar).
  The accent bar is NOT a border-radius and stays.
NOT row surfaces (do NOT touch): `.index-collapse` radius (~1336, a 24px icon button) and
  `.index-search-input` radius (~1358, the filter text input) — neither is a project/doc row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove row-surface border-radius (.index-row + .index-entry) and record the durable no-rounded-rows rule</name>
  <files>src/viz/css/styles.css</files>
  <read_first>
    - src/viz/css/styles.css (~lines 1400-1460 — the `.index-entry`, `.index-entry:hover`, `.index-row`,
      `.index-row:hover`, and `.index-row.active` blocks — so you see the exact current declarations and the
      surrounding comment idiom before editing)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md (GAP-5 truth + founder_direction —
      the pass bar and the durable-rule requirement)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UI-SPEC.md (row treatment section — confirm
      no spec calls for rounded row corners; hover/active is background/luminance only)
  </read_first>
  <action>
    1. In `.index-entry` (~line 1419) delete the `border-radius: 6px;` declaration entirely. Leave every other
       declaration (display, font-size, line-height, color, padding: 5px 8px, cursor, transition) untouched.
    2. In `.index-row` (~line 1439) delete the `border-radius: 6px;` declaration entirely. Leave the flex layout,
       gap, padding-top/bottom, color, cursor, and transition untouched.
    3. Do NOT alter `.index-row:hover`, `.index-entry:hover`, or `.index-row.active` backgrounds — the
       hover/active background treatment stays exactly as-is (only the corner rounding is removed). Keep the
       active-row `box-shadow: inset 2px 0 0 var(--text-mauve-rest)` left accent bar.
    4. Do NOT touch `.index-collapse` (~1336) or `.index-search-input` (~1358) — those are a button and a text
       input, not row surfaces, and their radius is out of GAP-5 scope.
    5. Add a durable design-rule comment directly above the `.index-row` block (replace/extend the existing GAP-2
       comment) stating verbatim the rule: index-rail row surfaces are NEVER rounded — hover/active backgrounds are
       square-edged (full-bleed within the rail); `border-radius` on `.index-row`/`.index-entry` is banned (GAP-5,
       founder-locked "NEVER DO IT AGAIN").
    6. Record the same rule in cross-project memory: run `recense remember "recense viz index rail: NEVER use rounded (border-radius) row corners on .index-row/.index-entry — hover/active row surfaces stay square-edged/full-bleed. Founder-locked GAP-5." --scope recense`
       (best-effort; if the CLI is unavailable, note it in the SUMMARY — the in-code comment is the load-bearing durable rule).
  </action>
  <verify>
    <automated>cd /Users/vtx/brain-memory && node -e "const c=require('fs').readFileSync('src/viz/css/styles.css','utf8'); const block=(sel)=>{const i=c.indexOf(sel+' {'); if(i<0){console.error('missing '+sel);process.exit(1);} return c.slice(i, c.indexOf('}', i));}; for(const s of ['.index-row','.index-entry']){ if(/border-radius/.test(block(s))){ console.error(s+' still has border-radius'); process.exit(1);} } console.log('ok: no border-radius on .index-row/.index-entry');" && npx vitest run tests/viz-activity-palette-invariants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The `.index-row { ... }` block contains no `border-radius` declaration (source assertion via the node check above).
    - The `.index-entry { ... }` block contains no `border-radius` declaration (source assertion).
    - `.index-row:hover`, `.index-entry:hover`, and `.index-row.active` still set their existing backgrounds; the
      active-row `box-shadow: inset 2px 0 0 var(--text-mauve-rest)` accent bar is still present (grep confirms).
    - `.index-collapse` and `.index-search-input` still have their `border-radius: 6px` (untouched — grep confirms).
    - A comment above `.index-row` states the durable no-rounded-rows rule referencing GAP-5.
    - `npx vitest run tests/viz-activity-palette-invariants.test.ts` passes (no new amber/literal; token locks green).
  </acceptance_criteria>
  <done>.index-row and .index-entry are square-edged (no border-radius); hover/active backgrounds + active accent bar unchanged; a durable no-rounded-rows comment is in styles.css; invariants suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS token source → styles.css | Row-surface change is a declaration removal only; no new color literal, no amber, no token added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-20 | Tampering | styles.css row-surface edit | mitigate | Only `border-radius` removed from two selectors; run `npx vitest run tests/viz-activity-palette-invariants.test.ts` (D-14-A/B) after the edit; no literal/amber introduced. |
</threat_model>

<verification>
- Automated: the node source check reports no border-radius on `.index-row`/`.index-entry`; invariants suite green.
- Behavioral (deferred to 61-14 founder checkpoint): project/doc rows read square-edged; hover/active still legible.
</verification>

<success_criteria>
- No rounded corners on index row surfaces; hover/active treatment otherwise unchanged; durable rule recorded.
- Invariants suite green.
</success_criteria>

<output>
Create `.planning/phases/61-corpus-chrome-index-column-project-browsing/61-10-SUMMARY.md` when done, noting the durable rule was recorded (CSS comment + recense remember result).
</output>
