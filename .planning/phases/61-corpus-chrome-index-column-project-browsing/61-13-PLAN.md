---
phase: 61-corpus-chrome-index-column-project-browsing
plan: 13
type: execute
wave: 2
depends_on: ["61-11", "61-12"]
gap_closure: true
gaps: ["GAP-6"]
requirements: ["GAP-6"]
files_modified:
  - src/viz/modules/corpus.js
  - src/viz/modules/index.js
  - src/viz/index.html
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The corpus view opens with the index rail closed; the graph gets the full canvas by default and the rail opens only on explicit user action (left-edge handle), then docks/reflows as built."
  artifacts:
    - path: "src/viz/modules/corpus.js"
      provides: "goToCorpus no longer auto-opens the rail — it shows the left-edge reopen handle instead (rail starts closed, graph full canvas)"
      contains: "showIndexHandle"
    - path: "src/viz/modules/index.js"
      provides: "ctx.showIndexHandle() — reveals the #index-reopen handle without docking; the panel stays hidden until the user opens it explicitly"
      contains: "showIndexHandle"
  key_links:
    - from: "corpus.js goToCorpus (corpus view opens)"
      to: "rail stays closed + #index-reopen handle shown"
      via: "ctx.showIndexHandle() (replaces ctx.openIndexSidebar())"
      pattern: "showIndexHandle"
    - from: "user clicks #index-reopen handle"
      to: "openSidebar() docks + reflows (unchanged)"
      via: "existing reopenHandle click → openSidebar"
      pattern: "openSidebar"
---

<objective>
Close GAP-6: the docked rail still feels awkward "just sitting there" when it opens on its own. Founder direction:
keep the rail CLOSED by default — the corpus view opens with the graph on the full canvas, and the rail opens only
on an explicit left-edge handle click. Existing dock/reflow behavior once opened is unchanged.

Purpose: GAP-1 made the rail a proper docked split, but auto-opening it on every corpus entry is the residual
discomfort. Making it default-closed (open-on-demand) gives the graph the full canvas and makes the rail feel
summoned, not bolted on.
Output: entering the corpus shows the full-width graph + the slim reopen handle; clicking the handle docks/reflows
the rail exactly as built today. Returning to the brain hides both. Presentation-only, no new deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-CONTEXT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md

<interfaces>
<!-- Live source, confirmed 2026-07-14 -->
corpus.js goToCorpus (~651-659): currently `if (typeof ctx.openIndexSidebar === 'function') ctx.openIndexSidebar();`
  BEFORE `transition.toCorpus(...)` — this is the auto-open to replace. goToBrain (~660-667) already calls
  `ctx.closeIndexSidebar()` (hides panel + handle) — leave unchanged.
index.js:
  - `#index-panel` container defaults to CSS `display: none` (styles.css ~1291) — the rail is hidden until openSidebar sets display:flex.
  - `openSidebar()` (~382-397): sets isSidebarOpen=true, display:flex, adds `body.index-docked` (reflow), shows the
    panel, calls ctx.refitCorpus — the DOCK path (unchanged; still the handle's action).
  - `ensureReopenHandle()` (~102-112) creates `#index-reopen` (defaults CSS `display:none`; `.shown` → flex),
    whose click → `openSidebar`. `showReopenHandle(show)` (~377-380) calls ensureReopenHandle then toggles `.shown`.
  - `hidePanel(showHandleAfter)` (~399-416): undocks, hides panel; `collapseSidebar()`=hidePanel(true) (handle shown),
    `closeIndexSidebar()`=hidePanel(false) (handle hidden).
  - ctx hooks registered ~424-432: openIndexSidebar/closeIndexSidebar/openIndex/syncCorpusFocus.
  - prepareIndex() preloads /index content; already fired on a ~1200ms timer (~434).
  - module doc comment ~5-14 says "the sidebar opens by default when the corpus view opens" — now stale.
index.html ~154: comment "opens by default with the corpus view (corpus.js → ctx.openIndexSidebar)" — now stale.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Default-closed rail — show the reopen handle on corpus open instead of auto-docking (index.js + corpus.js + index.html)</name>
  <files>src/viz/modules/index.js, src/viz/modules/corpus.js, src/viz/index.html</files>
  <read_first>
    - src/viz/modules/index.js (openSidebar ~382-397, ensureReopenHandle/showReopenHandle ~102-112 + ~377-380,
      hidePanel/collapseSidebar/closeIndexSidebar ~399-421, ctx-hook registration ~424-432, module doc comment ~5-14)
    - src/viz/modules/corpus.js (goToCorpus ~651-659 and goToBrain ~660-667 — the auto-open call to replace and the
      close-on-brain path to leave intact)
    - src/viz/index.html (~line 154 — the stale "opens by default" comment)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md (GAP-6 truth + founder_direction)
  </read_first>
  <action>
    Make the rail default-closed with explicit open-on-demand:
    1. index.js — add a new hook `ctx.showIndexHandle = function showIndexHandle() { isSidebarOpen = false; showReopenHandle(true); prepareIndex(); }`
       registered next to the other ctx hooks (~424-432). It reveals the `#index-reopen` handle WITHOUT docking:
       it must NOT set `container.style.display`, must NOT add `body.index-docked`, and must NOT call refitCorpus
       (the graph stays full-width). `prepareIndex()` just preloads /index content so the first open is instant.
    2. corpus.js — in goToCorpus (~654-657), replace `if (typeof ctx.openIndexSidebar === 'function') ctx.openIndexSidebar();`
       with `if (typeof ctx.showIndexHandle === 'function') ctx.showIndexHandle();`. Update the adjacent comment to:
       "The rail starts CLOSED (GAP-6, founder direction) — corpus opens with the graph on the full canvas and only
       the slim left-edge reopen handle; the user opens the rail explicitly, then it docks/reflows as built."
    3. Leave the dock path untouched: openSidebar (handle click) still sets display:flex + body.index-docked +
       refitCorpus — opening still docks and reflows exactly as today. Leave goToBrain's `ctx.closeIndexSidebar()`
       (hides panel + handle on brain return) unchanged. Leave `ctx.openIndexSidebar` exported (now a latent hook,
       no longer auto-called — do not delete it; note it as no-longer-auto-invoked in the SUMMARY).
    4. index.js — update the module doc comment (~5-14): the rail no longer "opens by default"; it "starts closed;
       entering the corpus shows only the left-edge reopen handle (GAP-6); the user opens the rail explicitly, after
       which it docks/reflows over the corpus. Returning to the brain hides the rail and the handle."
    5. index.html — update the ~line 154 comment to match: the rail is default-closed; corpus shows the reopen
       handle (corpus.js → ctx.showIndexHandle); the user opens it explicitly.
  </action>
  <verify>
    <automated>cd /Users/vtx/brain-memory && npx tsc --noEmit 2>/dev/null; node -e "const c=require('fs').readFileSync('src/viz/modules/corpus.js','utf8'); const i=require('fs').readFileSync('src/viz/modules/index.js','utf8'); const g=c.slice(c.indexOf('function goToCorpus'), c.indexOf('function goToBrain')); if(/ctx.openIndexSidebar\(\)/.test(g)){console.error('goToCorpus still auto-opens the rail');process.exit(1);} if(!/ctx.showIndexHandle\(\)/.test(g)){console.error('goToCorpus does not show the handle');process.exit(1);} if(!/ctx.showIndexHandle\s*=\s*function/.test(i)){console.error('index.js missing showIndexHandle hook');process.exit(1);} console.log('ok: rail default-closed, handle shown on corpus open');" && npx vitest run tests/viz-frontend-static.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - corpus.js goToCorpus no longer calls `ctx.openIndexSidebar()`; it calls `ctx.showIndexHandle()` instead (source assertion via the node check).
    - index.js registers `ctx.showIndexHandle = function ...` that calls `showReopenHandle(true)` and does NOT set `container.style.display`, add `body.index-docked`, or call `refitCorpus` (source assertion — the panel stays closed, graph full-width).
    - The dock path (openSidebar via the reopen-handle click) is unchanged: still display:flex + body.index-docked + refitCorpus.
    - goToBrain still calls `ctx.closeIndexSidebar()`; the module doc comment (index.js) and the index.html ~line 154 comment describe the default-closed behavior.
    - `npx tsc --noEmit` clean; `npx vitest run tests/viz-frontend-static.test.ts` passes.
  </acceptance_criteria>
  <done>Entering the corpus shows the full-width graph + the slim reopen handle (rail closed); clicking the handle docks/reflows as before; returning to the brain hides both; tsc clean; frontend-static suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus.js ↔ index.js | Rail open/close crosses via ctx hooks only (guarded typeof checks); no direct import. |
| viz view state → DOM | Default-closed is a client-only presentation choice; no server surface, no write, no LLM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-24 | Tampering | goToCorpus open→handle swap | mitigate | Only the auto-open call is replaced by a handle-reveal; dock path untouched; node source check + tsc + frontend-static suite gate the change. |
| T-61-25 | Denial of Service | reopen handle never appears (user can't open the rail) | mitigate | showIndexHandle calls showReopenHandle(true) which ensures + shows #index-reopen; acceptance criteria assert the handle path; 61-14 founder checkpoint confirms the rail opens on the live install. |
</threat_model>

<verification>
- Automated: node source check confirms goToCorpus shows the handle (no auto-open) and index.js has showIndexHandle;
  tsc clean; frontend-static suite green.
- Behavioral (deferred to 61-14 founder checkpoint): corpus opens closed (full-width graph + handle); clicking the
  handle docks/reflows; brain return hides both.
</verification>

<success_criteria>
- Rail default-closed; graph full canvas by default; explicit-open via the left-edge handle docks/reflows as built.
- tsc clean; frontend-static suite green.
</success_criteria>

<output>
Create `.planning/phases/61-corpus-chrome-index-column-project-browsing/61-13-SUMMARY.md` when done, noting ctx.openIndexSidebar is now a latent (no-longer-auto-invoked) hook.
</output>
