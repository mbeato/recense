---
phase: 61-corpus-chrome-index-column-project-browsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/viz/css/styles.css
  - tests/viz-activity-palette-invariants.test.ts
autonomous: true
requirements: [D-02, D-08, D1]
must_haves:
  truths:
    - "The index panel reads as glass — backdrop blur + aubergine tint + hairline specular — matching the #detail/#reader focused-tier chrome, not a flat near-opaque slab."
    - "The #index-reopen handle reads as an ambient-tier glass affordance matching #hud-rail, not the old flat icon button."
    - "Tree folder rows have chevron + name + count-badge styling classes available (chevron rest/hover color, count-badge mono readout) so the D-01 tree render in Plan 03 has its CSS."
    - "The D-14 CSS invariants test stays green: zero raw color literals, amber-exclusivity intact, and #index-panel + #index-reopen are the ONLY new backdrop-filter selectors added to the allow-list."
  artifacts:
    - path: "src/viz/css/styles.css"
      provides: "#index-panel focused-tier glass; #index-reopen ambient-tier glass; new .index-row/.index-chevron/.index-count tree-row classes"
      contains: "backdrop-filter"
    - path: "tests/viz-activity-palette-invariants.test.ts"
      provides: "ALLOWED_SELECTORS extended with #index-panel and #index-reopen"
      contains: "#index-panel"
  key_links:
    - from: "src/viz/css/styles.css #index-panel"
      to: "ALLOWED_SELECTORS in viz-activity-palette-invariants.test.ts"
      via: "backdrop-filter allow-list membership (D-14-C gate)"
      pattern: "#index-panel"
    - from: "new styles.css color declarations"
      to: "HUD_CSS_TOKENS in constants.js"
      via: "var(--token) resolution (D-14-A literal ban)"
      pattern: "var\\(--"
---

<objective>
Reskin the corpus index chrome into the Phase-59 glass/token vocabulary (D-02, D-08) and add the
CSS classes the D-01 collapsible tree render (Plan 03) will consume — WITHOUT breaking the machine-
locked D-14 CSS invariants. This closes founder defect D1's "flat slab" half (the wall-of-text half
is Plan 03's tree restructure).

Purpose: The index column currently uses a near-opaque `--surface-index-panel` slab and a flat
`--surface-btn-icon` reopen button — off-vocabulary vs the Phase-59 rails/palette/detail glass. This
plan makes the index read as the same instrument.
Output: Two edited files — styles.css (glass reskin + new tree-row classes) and the invariants test
(two-line allow-list extension). Zero new raw color literals; zero new amber.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-CONTEXT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UI-SPEC.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-PATTERNS.md

<interfaces>
<!-- Existing tokens (already emitted to :root by css-tokens.js from HUD_CSS_TOKENS) the executor uses directly — no new tokens required. -->
Focused glass tier (apply to #index-panel, verbatim recipe from #detail styles.css:51-70):
  var(--glass-bg-focused)  = rgba(26,18,32,0.90)
  var(--glass-blur-md)     = 14px
  var(--glass-border-focused) = rgba(140,150,165,0.16)
  var(--glass-specular)    = inset 0 1px 0 rgba(255,255,255,0.06)
  var(--radius-lg)         = 12px
Ambient glass tier (apply to #index-reopen, verbatim recipe from #hud-rail styles.css:195-213):
  var(--glass-bg-ambient)  = rgba(26,18,32,0.66)
  var(--glass-blur-sm)     = 8px
  var(--glass-border)      = rgba(170,150,180,0.12)
  var(--radius-sm)         = 7px   (matches the existing #index-reopen border-radius:7px numerically)
Reuse-for-new-classes tokens (all exist in HUD_CSS_TOKENS — DO NOT add new keys):
  chevron rest color   → var(--text-recede)        (#9a8aa4)
  chevron hover color  → var(--text-bright-mauve)  (#c8bcd0)
  count-badge text     → var(--text-stat)          (#7fae93)   [numeric-readout voice]
  row hover background → var(--index-search-focus-bg)
  row hover text       → var(--text-bright)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Glass-reskin #index-panel + #index-reopen and extend the backdrop-filter allow-list</name>
  <files>src/viz/css/styles.css, tests/viz-activity-palette-invariants.test.ts</files>
  <read_first>
    - src/viz/css/styles.css (lines ~1246-1300 — the current #index-reopen and #index-panel blocks, plus #detail lines 51-70 and #hud-rail lines 195-213 as the verbatim recipe sources)
    - tests/viz-activity-palette-invariants.test.ts (lines ~455-490 — the ALLOWED_SELECTORS set and the three D-14-C backdrop-filter assertions)
    - src/viz/modules/constants.js (lines ~672-800 — HUD_CSS_TOKENS, to confirm every token referenced already exists)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UI-SPEC.md (Glass-tier rationale section — which tier each selector gets)
  </read_first>
  <action>
    In `#index-panel` (styles.css): replace `background: var(--surface-index-panel)` with
    `background: var(--glass-bg-focused)`; add `backdrop-filter: blur(var(--glass-blur-md))` and
    `-webkit-backdrop-filter: blur(var(--glass-blur-md))`; add `box-shadow: var(--glass-specular)`.
    Keep the existing `border-right: 1px solid var(--glass-border-focused)` edge-only treatment
    (the founder-locked docked-split look stays edge-only — do NOT add a full 4-side border).
    Keep the existing `opacity: 0` / `transition: opacity 320ms ease` / `.shown { opacity: 1 }`
    structure unchanged (opacity-only chrome transitions, D-11 lesson).

    In `#index-reopen` (styles.css): replace `background: var(--surface-btn-icon)` with
    `background: var(--glass-bg-ambient)`; add `backdrop-filter: blur(var(--glass-blur-sm))` and
    `-webkit-backdrop-filter: blur(var(--glass-blur-sm))`; add `box-shadow: var(--glass-specular)`.
    Change `border-radius: 7px` to `border-radius: var(--radius-sm)` (7px, token-routed per D-14).
    Keep its `border: 1px solid var(--index-hairline)` (already a var), color, opacity:0.75, and
    hover rules unchanged.

    In viz-activity-palette-invariants.test.ts: add the two string entries `'#index-panel'` and
    `'#index-reopen'` to the `ALLOWED_SELECTORS` Set (the D-12 glass-surface allow-list). Add ONLY
    these two — do not touch the `#tooltip has zero backdrop-filter` assertion or any other entry.

    Introduce ZERO raw hex/rgba literals in styles.css — every added declaration resolves through an
    existing `var(--token)`. Introduce ZERO new amber anywhere.
  </action>
  <verify>
    <automated>cd /Users/vtx/brain-memory && npx vitest run tests/viz-activity-palette-invariants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The `#index-panel` CSS block contains `backdrop-filter: blur(var(--glass-blur-md))` and `background: var(--glass-bg-focused)`; it no longer references `var(--surface-index-panel)`.
    - The `#index-reopen` CSS block contains `backdrop-filter: blur(var(--glass-blur-sm))` and `background: var(--glass-bg-ambient)`; it no longer references `var(--surface-btn-icon)`.
    - `grep -n "index-panel\|index-reopen" tests/viz-activity-palette-invariants.test.ts` shows both selectors present inside the `ALLOWED_SELECTORS` block.
    - The D-14-A test `zero raw hex/rgba literals remain in styles.css` passes (no new literal introduced).
    - The D-14-C test `every rule block using backdrop-filter is in the D-12 selector allow-list` passes AND `#tooltip rule block has zero backdrop-filter` still passes.
    - `npx vitest run tests/viz-activity-palette-invariants.test.ts` exits 0.
  </acceptance_criteria>
  <done>Both index selectors carry the correct glass tier via tokens only, and the invariants suite is green with the two new allow-list entries.</done>
</task>

<task type="auto">
  <name>Task 2: Add collapsible tree-row CSS classes (chevron + row + count badge)</name>
  <files>src/viz/css/styles.css</files>
  <read_first>
    - src/viz/css/styles.css (lines ~1395-1417 — the current `.index-entry` and `.index-entry:hover` blocks whose hover-background/color idiom must be reused; `.index-list`/`.index-list li` above it)
    - src/viz/modules/constants.js (lines ~722-800 — confirm var(--text-recede), var(--text-bright-mauve), var(--text-stat), var(--index-search-focus-bg), var(--text-bright) exist)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UI-SPEC.md (Typography table + Spacing scale — chevron 11-12px box, xs 4px chevron-to-label gap, count-badge JetBrains Mono HUD 11px)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-PATTERNS.md (styles.css section — "New tree-row wrapper ... should reuse this exact hover-background/color-transition idiom")
  </read_first>
  <action>
    Add three new CSS rule blocks near the existing `.index-entry` block, all colors via existing
    tokens (zero raw literals, zero amber):

    `.index-row` — a flex row wrapper (`display:flex; align-items:center; gap:4px`) that will hold
    the chevron + name anchor + count badge for a folder/project row. Give it the SAME hover
    treatment as `.index-entry:hover` (`color: var(--text-bright)`, `background: var(--index-search-focus-bg)`,
    `border-radius:6px`, `transition: background 0.12s ease, color 0.12s ease`).

    `.index-chevron` — the per-row expand/collapse toggle button: ~16-18px square hit target
    (content chevron glyph ~11-12px), `display:flex; align-items:center; justify-content:center`,
    `border:none; background:transparent; padding:0; cursor:pointer`, `color: var(--text-recede)`,
    hover `color: var(--text-bright-mauve)`. Add `.index-chevron svg { transition: transform var(--motion-fast) var(--ease-out-soft); }`
    and an `.index-chevron.expanded svg { transform: rotate(90deg); }` rule so ONE right-pointing
    glyph rotates between ▶ (collapsed) and ▼ (expanded) — no second SVG asset (net-zero deps).

    `.index-count` — the doc-count badge: `font-family: 'JetBrains Mono HUD', ui-monospace, monospace`,
    `font-size:11px`, `color: var(--text-stat)`, `margin-left: 8px` (auto-push to row end is fine via
    `margin-left:auto`). Terse numeric readout — no unit word.

    Keep the existing `.index-entry` block intact (leaf doc rows still use it). Do not restyle
    `.index-status`, `.index-heading`, `.index-search`, loading/empty overlays (D-08 surgical diff).
  </action>
  <verify>
    <automated>cd /Users/vtx/brain-memory && npx vitest run tests/viz-activity-palette-invariants.test.ts && grep -Eq 'index-chevron|index-row|index-count' src/viz/css/styles.css && echo CLASSES_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - styles.css contains `.index-row`, `.index-chevron`, `.index-count`, and `.index-chevron.expanded svg { transform: rotate(90deg)` rules.
    - Every color/background declaration in the three new blocks resolves through `var(--...)` — `grep -nE '#[0-9a-fA-F]{3,6}|rgba?\(' ` over the three new blocks returns nothing (the D-14-A literal test still passes).
    - `.index-count` uses `'JetBrains Mono HUD'` font-family and `var(--text-stat)` color.
    - The existing `.index-entry` block is unchanged (leaf rows unaffected).
    - `npx vitest run tests/viz-activity-palette-invariants.test.ts` exits 0 and the grep prints CLASSES_PRESENT.
  </acceptance_criteria>
  <done>Tree-row, chevron, and count-badge classes exist token-only, ready for the Plan-03 render, with the invariants suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored CSS/token source → runtime `:root` | New declarations must route through the token catalog; raw literals bypass the D-14 lock. |
| viz server → browser | Presentation-only; this plan adds no server surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | new styles.css color declarations | mitigate | Every color via `var(--token)`; D-14-A `findRawColorLiteralLines` test enforces zero raw literals (verify command runs it). |
| T-61-02 | Elevation | backdrop-filter allow-list | mitigate | Only `#index-panel`/`#index-reopen` added to `ALLOWED_SELECTORS`; D-14-C test blocks any unsanctioned glass surface and keeps `#tooltip` glass-free. |
| T-61-03 | Information disclosure | amber-exclusivity | accept | No amber added; D-14-B test guards. Amber stays confined to the 4 `AMBER_ALLOWED_KEYS`. |
</threat_model>

<verification>
- `npx vitest run tests/viz-activity-palette-invariants.test.ts` is green (D-14-A/B/C all pass).
- Full suite regression sanity: `npx vitest run tests/viz-*.test.ts` shows no newly-failing viz test.
</verification>

<success_criteria>
- #index-panel and #index-reopen carry the correct Phase-59 glass tier via tokens only (D-02, D-08).
- Tree-row/chevron/count-badge classes exist for Plan 03 to consume.
- Zero raw color literals, zero new amber, invariants suite green.
</success_criteria>

<output>
Create `.planning/phases/61-corpus-chrome-index-column-project-browsing/61-01-SUMMARY.md` when done.
</output>
