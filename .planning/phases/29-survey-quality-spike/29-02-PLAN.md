---
phase: 29-survey-quality-spike
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - scripts/spike/genuine-noise-judge.ts
autonomous: false
requirements: [INGEST-03]
must_haves:
  truths:
    - "Every fact stamped [usage] in the scratch DB is judged genuine | noise by a throwaway LLM judge"
    - "A per-area tally is reported showing genuine vs noise counts for each INGEST-03 area"
    - "Schemas induced from the [usage] facts are inspectable (count + labels) to prove the abstraction layer fired"
    - "The judge harness reads the scratch DB read-only and adds zero new runtime deps"
  artifacts:
    - path: "scripts/spike/genuine-noise-judge.ts"
      provides: "Per-fact genuine/noise judge + per-area tally + schema-induction inspection over the scratch DB"
      min_lines: 70
  key_links:
    - from: "scripts/spike/genuine-noise-judge.ts"
      to: "scratch DB node/node_scope tables"
      via: "read-only query for type IN ('fact','schema') AND scope='usage'"
      pattern: "node_scope"
    - from: "scripts/spike/genuine-noise-judge.ts"
      to: "createClaudeHeadlessClient"
      via: "per-fact judge call (subscription-billed)"
      pattern: "createClaudeHeadlessClient\\("
---

<objective>
Build the throwaway genuine/noise judge harness: query the scratch DB for facts stamped `[usage]`, score each `genuine | noise` via the existing headless judge, report a per-area tally, and inspect induced schemas.

Purpose: Prove Success Criterion 2 (≥5 facts per surveyed area judged genuine semantic knowledge, not raw-code noise — measured by the D-02 LLM-judge tally + founder spot-check) and Success Criterion 3 (≥1 schema induced from the `usage` facts — the abstraction layer fires). This is the measurement instrument for the go/no-go decision Plan 03 writes up.

Output: `scripts/spike/genuine-noise-judge.ts` (throwaway, read-only harness) + a printed per-area tally and schema list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-survey-quality-spike/29-CONTEXT.md
@.planning/phases/29-survey-quality-spike/29-PATTERNS.md
@.planning/phases/29-survey-quality-spike/29-01-SUMMARY.md

@scripts/eval/cost-benefit-harness.cjs
@src/model/claude-headless-client.ts
@src/consolidation/run-sleep-pass.ts
@src/lib/config.ts

<interfaces>
<!-- Contracts the executor needs. -->

Scratch DB query (facts + schemas stamped [usage]):
```sql
SELECT n.id, n.type, n.value, ns.scope
FROM node n
JOIN node_scope ns ON ns.node_id = n.id
WHERE n.type IN ('fact','schema') AND n.tombstoned = 0 AND ns.scope = 'usage'
ORDER BY n.created_at DESC;
```
Open read-only: `new Database(scratchDbPath, { readonly: true })`.

Headless judge:
```typescript
// const judgeConfig = { ...DEFAULT_CONFIG, dbPath, ...resolveProviderOverlay(process.env,'RECENSE_JUDGE_PROVIDER') };
// const { client, model } = createClaudeHeadlessClient(judgeConfig);
// const r = await client.messages.create({ model, max_tokens: 64, messages: [{ role:'user', content: prompt }] });
// const verdict = (r.content[0] as {text:string}).text.trim().toLowerCase();
// Guard empty verdict -> count as 'unknown' (timeout/failure fail-safe).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-fact genuine/noise judge + per-area tally</name>
  <read_first>
    - scripts/eval/cost-benefit-harness.cjs (read-only DB open, fact query, tally/report pattern)
    - src/model/claude-headless-client.ts lines 105-218 (createClaudeHeadlessClient, judge call shape, empty-on-failure guard, billing strip)
    - src/consolidation/run-sleep-pass.ts lines 191-247 (resolveProviderOverlay)
    - .planning/phases/29-survey-quality-spike/29-PATTERNS.md ("DB query pattern for facts in scope", "Headless judge call pattern", "Output tally pattern")
  </read_first>
  <action>
    Create `scripts/spike/genuine-noise-judge.ts`. Resolve `scratchDbPath` from `--db <path>` then `RECENSE_DB` then default `/tmp/recense-spike.db`; open it read-only (`{ readonly: true }`) — the harness NEVER writes. Query the scratch DB for `type='fact' AND tombstoned=0 AND scope='usage'` joined to `node_scope`, selecting `id, value, scope` and (where available) the originating `sessionId`/area so each fact maps back to its INGEST-03 area; if area is not directly available on the fact, derive it by joining through the contributing episodes' `session_id` (`project-survey:usage:<area>`) — fall back to bucketing by source if the join is empty.

    Build the judge via `createClaudeHeadlessClient({ ...DEFAULT_CONFIG, dbPath: scratchDbPath, ...resolveProviderOverlay(process.env,'RECENSE_JUDGE_PROVIDER') })` (D-02 — reuse the existing judge tier, net-zero new deps). Define `JUDGE_PROMPT` implementing the D-07 quality-gate definition: a fact is `genuine` if it is summarized why-level semantic knowledge (architecture rationale, conventions, design decisions, current state, gotchas); it is `noise` if it is a raw code line, structural trivia ("file X imports Y"), a dependency/import list, boilerplate, or a config dump. The prompt must instruct the judge to answer with exactly one word: `genuine` or `noise`. Call the judge per fact with `max_tokens: 64`; trim+lowercase the response; guard empty responses as `unknown` (timeout/failure).

    Accumulate a `tally: Record<area, {genuine, noise, unknown}>` and print: (a) the per-area tally, (b) overall totals, (c) for each area whether it meets the ≥5-genuine bar (Success Criterion 2 / D-02), and (d) every fact verdict line (id, area, verdict, value-prefix) so the founder can spot-check the judge's calls. This is a throwaway harness — do NOT add to package.json bin; do NOT write to the DB.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "genuine-noise-judge" | grep -v '^#' | grep -c error | grep -qx 0 && grep -q "readonly: true\|readonly:true" scripts/spike/genuine-noise-judge.ts && grep -q "node_scope" scripts/spike/genuine-noise-judge.ts && grep -q "createClaudeHeadlessClient" scripts/spike/genuine-noise-judge.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/spike/genuine-noise-judge.ts` exists, type-checks clean, opens the DB with `{ readonly: true }`.
    - Query selects facts where `type='fact' AND tombstoned=0 AND scope='usage'` joined to `node_scope`.
    - Judge built via `createClaudeHeadlessClient` + `resolveProviderOverlay(...,'RECENSE_JUDGE_PROVIDER')`; no new npm dep.
    - `JUDGE_PROMPT` encodes the D-07 gate (genuine = why-level semantic; noise = raw code / structural trivia / import list / config dump) and forces a one-word answer.
    - Empty verdicts are guarded as `unknown` (no throw).
    - Output includes per-area tally, overall totals, the ≥5-genuine pass/fail per area, and per-fact verdict lines for spot-check.
  </acceptance_criteria>
  <done>Running the harness against /tmp/recense-spike.db prints a per-area genuine/noise tally and flags which areas clear the ≥5-genuine bar, with per-fact verdicts the founder can audit.</done>
</task>

<task type="auto">
  <name>Task 2: Schema-induction inspection</name>
  <read_first>
    - scripts/eval/cost-benefit-harness.cjs (read-only DB query pattern)
    - .planning/phases/29-survey-quality-spike/29-PATTERNS.md ("Post-consolidation scope verification" SQL)
    - scripts/spike/genuine-noise-judge.ts (the file being extended)
  </read_first>
  <action>
    In the same `scripts/spike/genuine-noise-judge.ts`, add a `reportSchemas(db)` step that queries the scratch DB read-only for `type='schema' AND tombstoned=0 AND scope='usage'` (joined to `node_scope`), printing the count and each schema's label/value. Success Criterion 3 requires ≥1 schema induced from the surveyed `usage` facts — print an explicit `SCHEMAS INDUCED: <n> (need >= 1)` line with a PASS/FAIL marker. If schemas carry an `abstracts`/member relation, also print the number of facts each schema generalizes (best-effort via the existing schema-relation edges) to evidence that the abstraction fired over real survey facts rather than being spurious. Do not gate the script on the result — report it for the founder's go/no-go.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "genuine-noise-judge" | grep -v '^#' | grep -c error | grep -qx 0 && grep -q "SCHEMAS INDUCED\|schema" scripts/spike/genuine-noise-judge.ts && grep -q "type = 'schema'\|type='schema'\|'schema'" scripts/spike/genuine-noise-judge.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The harness queries and prints `type='schema' AND scope='usage'` nodes (count + labels) read-only.
    - An explicit `SCHEMAS INDUCED: <n> (need >= 1)` line with PASS/FAIL is printed (Success Criterion 3).
    - Where schema-relation edges exist, the count of facts each schema generalizes is reported (best-effort).
    - No DB writes; no new npm dep.
  </acceptance_criteria>
  <done>The harness reports the count and labels of schemas induced from the usage facts and whether the ≥1-schema bar is met, evidencing the abstraction layer fired.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Founder reviews the per-area tally + spot-checks judge verdicts</name>
  <action>Founder runs the judge harness, reads the per-area genuine tally + schema count, and spot-checks verdicts (D-02); see how-to-verify.</action>
  <what-built>A read-only throwaway harness that judges each [usage] fact genuine/noise via the headless judge, prints a per-area tally + the ≥5-genuine verdict per area, and reports induced schemas (≥1 bar).</what-built>
  <how-to-verify>
    1. Founder runs the harness (real subscription-billed judge calls — D-02 measurement is founder-owned):
       `RECENSE_LOCK_PATH=/tmp/recense-spike.lock RECENSE_JUDGE_PROVIDER=claude-headless npx ts-node scripts/spike/genuine-noise-judge.ts --db /tmp/recense-spike.db`
    2. Read the per-area tally: confirm whether each of architecture / conventions / decisions / current-state / gotchas clears ≥5 genuine facts (Success Criterion 2).
    3. Spot-check the per-fact verdict lines: does the judge's genuine/noise call agree with your own read? (D-02 founder spot-check — the judge is disposable; your judgment is the gate.) Note any miscalls.
    4. Confirm `SCHEMAS INDUCED: <n>` is ≥1 and the schema labels read as real generalizations over usage facts (Success Criterion 3).
  </how-to-verify>
  <resume-signal>Type "approved" with the per-area genuine counts and schema count recorded, or describe judge-prompt / re-survey adjustments needed before writing the calibration notes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scratch DB → judge harness | Reads fact values; opened read-only so the harness cannot corrupt the spike data |
| harness → `claude -p` subprocess | Spawns the judge; subscription-billed; child env must exclude API keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05 | Tampering | harness writes to / corrupts the scratch DB | mitigate | Open with `{ readonly: true }` — the harness only queries, never writes |
| T-29-06 | Information Disclosure | API key leaks via the judge subprocess | mitigate | Reuse `createClaudeHeadlessClient` (strips `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN`, subscription billing) — never spawn raw argv |
| T-29-07 | Repudiation | unverifiable go/no-go (judge miscalls trusted blindly) | mitigate | D-02 founder spot-check of per-fact verdicts; print every verdict line so the LLM tally is auditable, not authoritative |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — harness reuses existing deps only (net-zero new runtime deps). Stop and flag if any install is attempted. |
</threat_model>

<verification>
- `scripts/spike/genuine-noise-judge.ts` type-checks clean, adds no new dep, opens the DB read-only.
- After a founder run: a per-area genuine/noise tally is printed, the ≥5-genuine bar is evaluated per area (Success Criterion 2), and `SCHEMAS INDUCED: <n>` reports the schema count (Success Criterion 3).
- Founder has spot-checked the judge verdicts (D-02).
</verification>

<success_criteria>
- Each [usage] fact is judged genuine | noise with a per-area tally (D-02).
- The ≥5-genuine-per-area bar is explicitly evaluated (Success Criterion 2).
- ≥1 induced schema is reported with labels (Success Criterion 3).
- Harness is read-only, net-zero deps; founder has spot-checked verdicts.
</success_criteria>

<output>
Create `.planning/phases/29-survey-quality-spike/29-02-SUMMARY.md` when done. Record: the per-area genuine/noise counts, which areas cleared ≥5, the schema count + labels, any founder-noted judge miscalls, and the final JUDGE_PROMPT (quality-gate phrasing) for Plan 03.
</output>
