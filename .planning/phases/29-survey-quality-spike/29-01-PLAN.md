---
phase: 29-survey-quality-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/spike/survey-feeder.ts
autonomous: false
requirements: [INGEST-03]
must_haves:
  truths:
    - "An agent surveys /Users/vtx/usage and emits summarized observations (no raw code) per INGEST-03 area"
    - "Survey observations are appended as episodes through IngestionPipeline.recordEvent with origin='observed', source='project-survey', cwd='/Users/vtx/usage'"
    - "A consolidation pass runs on a scratch DB (never the live brain) producing facts stamped [usage]"
    - "The spike completes with zero new runtime dependencies (reuses claude -p headless transport)"
  artifacts:
    - path: "scripts/spike/survey-feeder.ts"
      provides: "Throwaway survey agent + episode feeder + consolidation trigger on scratch DB"
      min_lines: 80
  key_links:
    - from: "scripts/spike/survey-feeder.ts"
      to: "IngestionPipeline.recordEvent"
      via: "episode append with cwd=/Users/vtx/usage"
      pattern: "recordEvent\\("
    - from: "scripts/spike/survey-feeder.ts"
      to: "runConsolidation"
      via: "offline sleep pass under held lock"
      pattern: "runConsolidation\\("
    - from: "scripts/spike/survey-feeder.ts"
      to: "cwdToScope"
      via: "scope assertion before emit"
      pattern: "cwdToScope\\("
---

<objective>
Build the throwaway survey-feeder spike script: an agentic survey of `/Users/vtx/usage` whose summarized observations ride the existing offline pipeline into a scratch DB, then trigger consolidation to mint facts stamped `[usage]`.

Purpose: Prove Success Criterion 1 (agentic survey emits summarized observations as episodes through the existing pipeline, net-zero new deps) and set up the scratch DB that Plan 02 judges. This is the load-bearing spike mechanism — it validates the scope-tagging convention (D-04) and the summarization prompt shape (D-08) that Phase 30 will consume.

Output: `scripts/spike/survey-feeder.ts` (throwaway harness, NOT production wiring) + a populated scratch DB at `/tmp/recense-spike.db`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-survey-quality-spike/29-CONTEXT.md
@.planning/phases/29-survey-quality-spike/29-PATTERNS.md

@src/adapter/import-memory-cli.ts
@src/ingest/pipeline.ts
@src/lib/scope.ts
@src/source/source-adapter.ts
@src/model/claude-headless-client.ts
@src/adapter/generate-doc-cli.ts
@src/adapter/ingest-cli.ts
@src/consolidation/run-sleep-pass.ts
@src/adapter/lockfile.ts
@src/adapter/runtime-config.ts

<interfaces>
<!-- Contracts the executor needs. Extracted from live source — no exploration required. -->

From src/ingest/pipeline.ts (RecordEventParams):
```typescript
export interface RecordEventParams {
  content: string;
  role: EpisodeRole;          // use 'user'
  origin: Origin;             // use 'observed' (NEVER 'asserted_by_user')
  sessionId: string;
  source?: string;            // 'project-survey'
  externalId?: string | null; // contentExternalId(`usage/${area}`, content)
  cwd?: string;               // '/Users/vtx/usage' — load-bearing for scope='usage'
}
// new IngestionPipeline(new AllocationGate(config), episodes).recordEvent(params)
```

From src/lib/scope.ts:
```typescript
export function cwdToScope(cwd: string | undefined): string;
// cwdToScope('/Users/vtx/usage') === 'usage' (HOME_PROJECT_RE segment, not in PERSONAL_SLUGS)
```

From src/source/source-adapter.ts:
```typescript
export function contentExternalId(relPath: string, content: string): string;
// returns `<relPath>#<sha256[:16]>` — idempotent dedup key
```

From src/consolidation/run-sleep-pass.ts:
```typescript
export async function runConsolidation(
  db: Database.Database, dbPath: string, env: NodeJS.ProcessEnv,
  log: (msg: string) => void, opts?: {...}
): Promise<void>;
export function resolveProviderOverlay(env: NodeJS.ProcessEnv, key: string): Partial<Config>;
```

From src/model/claude-headless-client.ts:
```typescript
// createClaudeHeadlessClient(judgeConfig) -> { client, model }
// buildHeadlessArgs already includes --setting-sources project (drops global hooks,
//   prevents the self-ingestion loop) and strips ANTHROPIC_API_KEY/AUTH_TOKEN from child env.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Survey agent — summarized observations per INGEST-03 area</name>
  <read_first>
    - src/model/claude-headless-client.ts (transport, buildHeadlessArgs, billing-strip, --setting-sources project)
    - src/adapter/generate-doc-cli.ts (judge-tier provider construction + RECENSE_CLAUDE_HEADLESS_TIMEOUT_MS=600000 precedent)
    - src/consolidation/run-sleep-pass.ts lines 191-247 (resolveProviderOverlay)
    - .planning/phases/29-survey-quality-spike/29-PATTERNS.md ("Headless survey agent invocation", "Self-ingestion loop prevention", "Billing guard")
  </read_first>
  <action>
    Create `scripts/spike/survey-feeder.ts`. Implement a `surveyArea(area, judgeConfig)` function that invokes the survey agent once per INGEST-03 area in `SURVEY_AREAS = ['architecture','conventions','decisions','current-state','gotchas']` (D-06). Build the agent via `createClaudeHeadlessClient(judgeConfig)` where `judgeConfig = { ...DEFAULT_CONFIG, dbPath, ...resolveProviderOverlay(process.env, 'RECENSE_JUDGE_PROVIDER') }` (D-03 — reuse the existing transport, net-zero deps). Set `process.env['RECENSE_CLAUDE_HEADLESS_TIMEOUT_MS'] = '600000'` before client construction (agent runs are long, same as doc-gen). The agent must run with Read/Grep/Glob tooling over `cwd=/Users/vtx/usage` — pass the survey target directory so the agent reads the real repo (README.md, AGENTS.md, DESIGN.md, PLAN.md, app/, bin/, lib/, plugin/, scripts/).

    The per-area prompt (D-08, summarization prompt shape — this exact shape is a calibration deliverable Plan 03 records): ask for summarized observations about the named area, "why not what", as natural-language belief statements at roughly one belief per line, suited to the existing claim extractor. The prompt MUST explicitly instruct: emit NO raw code lines, NO import/dependency graphs, NO structural trivia ("file X imports Y"), NO config dumps — only why-level semantic knowledge (D-07 quality gate). Return the agent's raw text response; on empty response (timeout/failure fail-safe) log to LOG_PATH and return empty string so the caller can guard.

    Define `LOG_PATH = '/tmp/recense-survey-spike.log'` and a `fileLog` helper (import-memory-cli.ts pattern). Do NOT build claude argv manually — `buildHeadlessArgs` inside the client already adds `--setting-sources project` (drops the global UserPromptSubmit turn-capture hook → prevents the self-ingestion loop) and strips the API key from the child env (subscription billing). NO new npm dependency may be added.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "survey-feeder" | grep -v '^#' | grep -c error | grep -qx 0 && grep -q "SURVEY_AREAS" scripts/spike/survey-feeder.ts && grep -q "RECENSE_CLAUDE_HEADLESS_TIMEOUT_MS" scripts/spike/survey-feeder.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/spike/survey-feeder.ts` exists and type-checks clean (no tsc errors on the file).
    - `SURVEY_AREAS` contains exactly the five INGEST-03 areas (architecture, conventions, decisions, current-state, gotchas).
    - Survey agent built via `createClaudeHeadlessClient` with `resolveProviderOverlay(process.env,'RECENSE_JUDGE_PROVIDER')` — no manual claude argv, no new npm dep added to package.json.
    - The prompt text contains an explicit "no raw code / no import-dependency graphs / why not what" instruction (grep finds the no-raw-code clause).
    - Empty agent response is guarded (does not throw, logs to LOG_PATH).
  </acceptance_criteria>
  <done>The survey agent function emits summarized per-area observations from /Users/vtx/usage via the existing headless transport with zero new deps; empty responses are handled.</done>
</task>

<task type="auto">
  <name>Task 2: Episode feeder + scratch-DB consolidation under spike lock</name>
  <read_first>
    - src/adapter/import-memory-cli.ts (DB+pipeline construction, acquireLock-after-validation, recordEvent, require.main guard, finally releaseLock)
    - src/adapter/ingest-cli.ts lines 271-274 (runConsolidation after feed, same held lock)
    - src/adapter/lockfile.ts (RECENSE_LOCK_PATH override, getLockPath)
    - src/adapter/runtime-config.ts lines 52-66 (resolveDbPath)
    - src/source/source-adapter.ts line 57 (contentExternalId)
    - .planning/phases/29-survey-quality-spike/29-PATTERNS.md ("Core episode emission pattern", "Scope verification", "consolidation invocation")
  </read_first>
  <action>
    In the same `scripts/spike/survey-feeder.ts`, implement `main()`. Resolve `dbPath` from `--db <path>` then `RECENSE_DB` then default `/tmp/recense-spike.db` (D-05 — never the live brain; if a resolved path equals the live `~/.config/recense/recense.db`, abort with a stderr error and exit non-zero). BEFORE any DB open, assert `cwdToScope('/Users/vtx/usage') === 'usage'` and exit non-zero if it fails (scope-tagging is a spike deliverable). Validate args BEFORE `acquireLock()` (import-memory-cli ordering). The spike is run with env `RECENSE_LOCK_PATH=/tmp/recense-spike.lock` so `getLockPath()` returns the spike lock and the live hourly sleep pass is not blocked — no code change needed, but document this in a header comment.

    Open the scratch DB with `new Database(dbPath)`, `initSchema(db)`, build `config = { ...DEFAULT_CONFIG, dbPath }`, `episodes = new EpisodicStore(db, realClock, config)`, `pipeline = new IngestionPipeline(new AllocationGate(config), episodes)`. For each area, call `surveyArea(area, judgeConfig)`, split the returned text into episode-sized records (one belief-line per record, or small line-groups — chunking granularity is Claude's discretion per CONTEXT.md), and for each non-empty record call `pipeline.recordEvent({ content, role:'user', origin:'observed', sessionId:\`project-survey:usage:${area}\`, source:'project-survey', externalId: contentExternalId(\`usage/${area}\`, content), cwd:'/Users/vtx/usage' })`. After all areas are fed, trigger the offline pass under the SAME held lock: `await runConsolidation(db, dbPath, process.env, fileLog)` with `RECENSE_EXTRACTOR_PROVIDER=claude-headless` and `RECENSE_JUDGE_PROVIDER=claude-headless` (set via env at run time). Wrap DB+pipeline in try/catch/finally that calls `db?.close()` and `releaseLock()`. Add the `require.main === module` guard with a FATAL log + releaseLock.

    Print a final summary line: episodes fed per area and total. This script is throwaway harness in scripts/ — do NOT add it to package.json bin, do NOT create a `recense survey` CLI, do NOT implement SourceAdapter (Phase 30, deferred).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "survey-feeder" | grep -v '^#' | grep -c error | grep -qx 0 && grep -q "origin: 'observed'\|origin:'observed'" scripts/spike/survey-feeder.ts && grep -q "cwd: '/Users/vtx/usage'\|cwd:'/Users/vtx/usage'" scripts/spike/survey-feeder.ts && grep -q "runConsolidation" scripts/spike/survey-feeder.ts && grep -q "require.main === module" scripts/spike/survey-feeder.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `recordEvent` is called with `origin:'observed'`, `source:'project-survey'`, `cwd:'/Users/vtx/usage'`, and `externalId` from `contentExternalId` (grep confirms each literal).
    - DB path resolution defaults to `/tmp/recense-spike.db` and aborts if it resolves to the live `~/.config/recense/recense.db`.
    - `cwdToScope('/Users/vtx/usage') === 'usage'` assertion runs before any DB open.
    - Arg validation precedes `acquireLock()`; `finally` calls `db?.close()` and `releaseLock()`.
    - `runConsolidation` is invoked after the feed under the same held lock.
    - No entry added to package.json bin; no SourceAdapter implementation; no `src/` file modified.
  </acceptance_criteria>
  <done>Running the script with RECENSE_LOCK_PATH=/tmp/recense-spike.lock, --db /tmp/recense-spike.db, and the claude-headless providers feeds survey episodes (scope=usage) and runs consolidation on the scratch DB without touching the live brain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Founder runs the survey + spot-checks observation quality</name>
  <action>Founder executes the spike against the scratch DB and judges summarized-observation quality + prompt shape before Plan 02; see how-to-verify.</action>
  <what-built>A throwaway survey-feeder script that surveys /Users/vtx/usage via the headless claude -p agent, emits summarized per-area observations as episodes (origin=observed, cwd=/Users/vtx/usage), and runs consolidation on a scratch DB at /tmp/recense-spike.db.</what-built>
  <how-to-verify>
    1. Founder runs the spike (real subscription-billed `claude -p` calls — experiment design is human-owned per CLAUDE.md and CONTEXT.md D-02):
       `RECENSE_LOCK_PATH=/tmp/recense-spike.lock RECENSE_EXTRACTOR_PROVIDER=claude-headless RECENSE_JUDGE_PROVIDER=claude-headless npx ts-node scripts/spike/survey-feeder.ts --db /tmp/recense-spike.db`
    2. Confirm it completes without error, prints episodes-fed-per-area, and `/tmp/recense-spike.log` shows a "Sleep pass complete" line.
    3. Spot-check 5-10 emitted observations (grep the log or the episode rows): they must read as why-level summarized beliefs, NOT raw code / import lists / file-X-imports-Y trivia (D-07). Judge the summarization prompt shape (D-08) — is the level right for Phase 30?
    4. Confirm the live brain (~/.config/recense/recense.db) was NOT touched and the hourly sleep pass was not blocked.
  </how-to-verify>
  <resume-signal>Type "approved" (survey output quality is judgeable and the scratch DB is populated), or describe prompt-shape adjustments to apply before Plan 02.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /Users/vtx/usage repo content → survey agent prompt | Local repo file contents are read by Read/Grep/Glob and flow into the `claude -p` agent context (untrusted-ish content crossing into a prompt) |
| spike script → SQLite scratch DB | Writes episodes/facts; must target the scratch DB, never the live brain |
| spike script → `claude -p` subprocess | Spawns headless agent; subscription-billed; child env must exclude API keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | survey episodes pollute live brain | mitigate | Abort if resolved dbPath == live `~/.config/recense/recense.db`; default to `/tmp/recense-spike.db`; run under `RECENSE_LOCK_PATH=/tmp/recense-spike.lock` so the live hourly pass is never blocked (T-29-01 verified by the abort guard) |
| T-29-02 | Elevation of Privilege | survey agent reads repo content into prompt | accept | Survey target is the founder's own `~/usage` repo (trusted local code); the agent is read-only (Read/Grep/Glob, no write tools); output is summarized beliefs, not executed |
| T-29-03 | Information Disclosure | API key leaks via spawned `claude -p` | mitigate | Reuse `createClaudeHeadlessClient`, which strips `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN` from the child env (subscription billing) — never spawn raw argv |
| T-29-04 | Tampering | self-ingestion loop (agent calls captured as episodes) | mitigate | `buildHeadlessArgs` includes `--setting-sources project`, dropping the global UserPromptSubmit turn-capture hook — use the client, do not build argv manually |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed — spike reuses existing deps only (net-zero new runtime deps invariant). No legitimacy checkpoint needed; if any install is attempted, stop and flag. |
</threat_model>

<verification>
- `scripts/spike/survey-feeder.ts` type-checks clean and adds no new package.json dependency.
- After a founder run: scratch DB at `/tmp/recense-spike.db` contains episodes with `source='project-survey'`, `origin='observed'`, and facts stamped `[usage]`; the live brain DB is untouched.
- The headless transport reuse means net-zero new runtime deps (Success Criterion 1).
</verification>

<success_criteria>
- Survey agent emits summarized per-area observations (no raw code) from /Users/vtx/usage (D-06, D-08).
- Observations are appended as episodes through the real pipeline with the exact scope-tagging shape (D-04).
- Consolidation runs on the scratch DB producing facts stamped [usage].
- Spike completes with zero new runtime deps (Success Criterion 1).
- Founder has spot-checked observation quality and approved the prompt shape.
</success_criteria>

<output>
Create `.planning/phases/29-survey-quality-spike/29-01-SUMMARY.md` when done. Record: episodes fed per area, total facts produced, the exact final summarization prompt text used (Plan 03 needs it), and any founder-requested prompt adjustments.
</output>
