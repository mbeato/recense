---
phase: 29-survey-quality-spike
plan: 03
type: execute
wave: 3
depends_on: ["29-01", "29-02"]
files_modified:
  - .planning/phases/29-survey-quality-spike/29-CALIBRATION.md
autonomous: false
requirements: [INGEST-03]
must_haves:
  truths:
    - "A 29-CALIBRATION.md deliverable records the final summarization prompt shape, summarization level, quality-gate definition, and scope-tagging convention for Phase 30"
    - "A go/no-go decision is recorded, justified against all 4 ROADMAP success criteria with the measured numbers from Plans 01-02"
    - "Each carried-forward calibration input is concrete enough to drop into Phase 30's ingest-project build"
  artifacts:
    - path: ".planning/phases/29-survey-quality-spike/29-CALIBRATION.md"
      provides: "Calibration notes + go/no-go decision (Success Criterion 4)"
      contains: "go/no-go"
  key_links:
    - from: ".planning/phases/29-survey-quality-spike/29-CALIBRATION.md"
      to: "29-01-SUMMARY.md + 29-02-SUMMARY.md"
      via: "measured numbers + final prompt/gate text"
      pattern: "summarization prompt"
---

<objective>
Write the calibration notes deliverable and the go/no-go decision, synthesizing the measured results of Plans 01-02.

Purpose: Deliver Success Criterion 4 — written calibration notes (final summarization prompt shape D-08, summarization level, quality-gate definition D-07, scope-tagging convention D-04) that Phase 30's `ingest-project` command consumes — and record the founder-owned go/no-go (all four criteria met → go; shortfall → no-go with documented reasons). This is a pure documentation plan; no code.

Output: `.planning/phases/29-survey-quality-spike/29-CALIBRATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-survey-quality-spike/29-CONTEXT.md
@.planning/phases/29-survey-quality-spike/29-01-SUMMARY.md
@.planning/phases/29-survey-quality-spike/29-02-SUMMARY.md
@scripts/spike/survey-feeder.ts
@scripts/spike/genuine-noise-judge.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 29-CALIBRATION.md (calibration inputs + go/no-go draft)</name>
  <read_first>
    - .planning/phases/29-survey-quality-spike/29-01-SUMMARY.md (episodes fed, final survey prompt text, founder prompt adjustments)
    - .planning/phases/29-survey-quality-spike/29-02-SUMMARY.md (per-area genuine counts, schema count, final JUDGE_PROMPT)
    - .planning/phases/29-survey-quality-spike/29-CONTEXT.md (D-04/D-07/D-08 calibration deliverable definitions)
    - .planning/ROADMAP.md §"Phase 29" (the 4 success criteria + Phase 30 goal these inputs feed)
  </read_first>
  <action>
    Create `.planning/phases/29-survey-quality-spike/29-CALIBRATION.md`. Structure it as the four Phase-30 calibration inputs plus a decision section:

    1. **Final summarization prompt shape (D-08):** paste the exact final per-area survey prompt text used in 29-01 (from 29-01-SUMMARY.md, including any founder-requested adjustments). Note the "why not what / one belief per line / no raw code / no import-dependency graphs" framing and which phrasings produced the cleanest beliefs.
    2. **Summarization level:** describe the granularity that worked — chunking per belief-line vs grouped, how much abstraction per observation, episodes-per-area that produced the best genuine ratio (cite the 29-01 numbers).
    3. **Quality-gate definition (D-07):** paste the final JUDGE_PROMPT genuine/noise criteria from 29-02-SUMMARY.md as the canonical gate phrasing Phase 30 enforces — genuine = why-level semantic (architecture rationale, conventions, decisions, current state, gotchas); noise = raw code, structural trivia, import/dependency lists, boilerplate, config dumps. Note any miscall patterns the founder flagged and how to tighten the gate.
    4. **Scope-tagging convention (D-04):** document that `cwd=<project dir>` on each episode drives `cwdToScope` → `[scope]`; record that `/Users/vtx/usage` → `usage` was verified, and the convention Phase 30's adapter must follow (set cwd to the surveyed project root; source='project-survey'; origin='observed'; externalId=contentExternalId for idempotent re-ingest).

    Then a **Results vs Success Criteria** table mapping each of the 4 ROADMAP criteria to its measured outcome with the numbers from Plans 01-02 (SC1 net-zero-deps completion, SC2 per-area genuine counts vs the ≥5 bar, SC3 schema count vs the ≥1 bar, SC4 this document exists). End with a **Go/No-Go** section: a draft recommendation (go if all four met; no-go with documented shortfalls otherwise) explicitly marked as awaiting founder sign-off (the final call is founder-owned per CONTEXT.md D-02). This is a planning doc under .planning/ — use Write (note: .planning/ is gitignored, no git safety net; do not clobber existing content).
  </action>
  <verify>
    <automated>test -f .planning/phases/29-survey-quality-spike/29-CALIBRATION.md && grep -qi "summarization prompt" .planning/phases/29-survey-quality-spike/29-CALIBRATION.md && grep -qi "quality.gate" .planning/phases/29-survey-quality-spike/29-CALIBRATION.md && grep -qi "scope-tag" .planning/phases/29-survey-quality-spike/29-CALIBRATION.md && grep -qi "go/no-go\|go.no.go" .planning/phases/29-survey-quality-spike/29-CALIBRATION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `29-CALIBRATION.md` exists and contains all four calibration inputs: summarization prompt shape (with the exact final prompt text), summarization level, quality-gate definition (with the exact final JUDGE_PROMPT criteria), scope-tagging convention.
    - A Results-vs-Success-Criteria table cites the measured numbers from 29-01/29-02 (per-area genuine counts, schema count).
    - A Go/No-Go section gives a justified draft recommendation explicitly marked as awaiting founder sign-off.
    - Each calibration input is concrete enough to drop into Phase 30 (named env vars, the cwd→scope mechanic, source/origin/externalId tags).
  </acceptance_criteria>
  <done>29-CALIBRATION.md captures the four Phase-30 calibration inputs plus a justified, founder-pending go/no-go grounded in the measured spike results (Success Criterion 4).</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: Founder go/no-go decision</name>
  <action>Founder reviews 29-CALIBRATION.md and selects go / no-go-adjust / no-go-stop; rationale recorded in the Go/No-Go section.</action>
  <decision>Phase 29 go/no-go: does the agentic-survey approach produce genuine-enough fact/schema signal to build the full ingest-project command (Phases 30-32)?</decision>
  <context>
    The spike measured all four ROADMAP success criteria: SC1 net-zero-dep survey completion (29-01), SC2 ≥5 genuine facts per area (29-02 tally + founder spot-check), SC3 ≥1 induced schema (29-02), SC4 calibration notes (29-CALIBRATION.md, this plan). The genuine/noise judgment and the final go/no-go are founder-owned (CONTEXT.md D-02). Review 29-CALIBRATION.md before deciding.
  </context>
  <options>
    <option id="go">
      <name>GO — build Phase 30</name>
      <pros>All four criteria met; calibration inputs (prompt shape, gate, scope convention) are ready to carry into ingest-project.</pros>
      <cons>None if criteria genuinely cleared.</cons>
    </option>
    <option id="no-go-adjust">
      <name>NO-GO (adjust) — re-run spike with tuned prompt/gate</name>
      <pros>Cheap to iterate (throwaway scripts on scratch DB); fixes a shortfall before committing to the build.</pros>
      <cons>Another founder-supervised subscription-billed run.</cons>
    </option>
    <option id="no-go-stop">
      <name>NO-GO (stop) — agentic survey doesn't yield genuine signal</name>
      <pros>Avoids building Phases 30-32 on a flawed premise; documented reasons inform a different approach.</pros>
      <cons>v6.0 onboarding needs a rethink.</cons>
    </option>
  </options>
  <resume-signal>Select: go, no-go-adjust, or no-go-stop. The decision + rationale is recorded in 29-CALIBRATION.md's Go/No-Go section.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only plan — no new code, no new process, no DB writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08 | Tampering | clobbering an existing .planning doc (gitignored, no safety net) | mitigate | 29-CALIBRATION.md is a new file; confirm it does not already exist before Write; .planning/ has no git history so never blind-overwrite |
| T-29-09 | Repudiation | go/no-go recorded without founder sign-off | mitigate | The go/no-go is a blocking `checkpoint:decision` — the founder selects and the rationale is recorded (D-02 founder-owned) |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | No code, no installs — documentation-only. |
</threat_model>

<verification>
- `29-CALIBRATION.md` exists with all four calibration inputs and a Results-vs-Success-Criteria table citing measured numbers.
- A founder-selected go/no-go decision with rationale is recorded.
</verification>

<success_criteria>
- Written calibration notes capture the final summarization prompt shape, summarization level, quality-gate definition, and scope-tagging convention for Phase 30 (Success Criterion 4).
- The go/no-go decision is founder-owned, recorded, and justified against all four success criteria.
</success_criteria>

<output>
Create `.planning/phases/29-survey-quality-spike/29-03-SUMMARY.md` when done. Record: the final go/no-go decision, the four calibration inputs in brief, and what Phase 30 carries forward.
</output>
