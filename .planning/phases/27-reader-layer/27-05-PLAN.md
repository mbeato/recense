---
phase: 27-reader-layer
plan: 05
type: execute
wave: 5
depends_on: ["27-04"]
files_modified:
  - src/reader/doc-generator.ts
  - src/consolidation/doc-writer.ts
  - src/viz/server.ts
  - src/viz/modules/reader.js
  - src/viz/index.html
  - src/viz/css/styles.css
  - tests/doc-link-edges.test.ts
  - tests/viz-corpus-graph.test.ts
autonomous: false
requirements: [READER-04]
must_haves:
  decisions: ["D-07", "D-08", "D-09"]  # corpus graph = secondary, expanded-only swap button (never tray); reader opened from corpus doc-node entry point; centering corpus on a project subsumes the per-project graph view
  truths:
    - "Docs that reference other docs (recense://doc/<id>) get doc_link edges between the doc nodes"
    - "A doc→doc corpus graph is navigable in the viz behind an expanded-only swap button"
    - "Centering the corpus graph on a project surfaces its doc(s) alongside neighboring projects/entities — subsuming a per-project graph view"
    - "The corpus-graph swap button appears only in the expanded Brain Window, never in the tray/popover"
  artifacts:
    - path: "src/viz/server.ts"
      provides: "GET /graph?type=doc returning doc nodes + doc_link edges"
      contains: "doc_link"
    - path: "src/viz/modules/reader.js"
      provides: "expanded-only corpus-graph swap button (#btn-corpus) swapping graph data"
      contains: "btn-corpus"
    - path: "src/consolidation/doc-writer.ts"
      provides: "doc_link edge creation from recense://doc/<id> refs in the prose"
      contains: "doc_link"
  key_links:
    - from: "src/consolidation/doc-writer.ts"
      to: "doc_link edges"
      via: "recense://doc/<id> refs parsed from generated markdown"
      pattern: "doc_link"
    - from: "reader.js #btn-corpus"
      to: "/graph?type=doc"
      via: "Graph.graphData swap, expanded-only (.mode-window)"
      pattern: "type=doc"
---

<objective>
Deliver the doc→doc corpus graph (READER-04): create `doc_link` edges between doc nodes from the `recense://doc/<id>` refs the generator emits in prose; add a `GET /graph?type=doc` endpoint returning doc nodes + doc_link edges; add an expanded-only (`.mode-window`) swap button (#btn-corpus, never in the tray/popover per D-07) that swaps the viz graph data to the corpus graph; and make clicking a doc node in the corpus graph open its reader (D-08 browse-all-docs path). Centering the corpus graph on a project surfaces its docs alongside neighboring projects/entities — this IS the per-project view (D-09), subsuming a dedicated single-project graph.

Purpose: the corpus graph is the readable mid-altitude between the prose reader and the ~3,500-node atomic brain — Obsidian-style, but every doc node drops down into its cited atoms via the 27-03 toggle.
Output: doc_link edge creation, corpus-graph endpoint, expanded-only swap button + doc-node→reader entry, corpus tests, human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-reader-layer/27-PATTERNS.md
@.planning/phases/27-reader-layer/27-CONTEXT.md

<interfaces>
<!-- Live-source contracts. -->

src/viz/modules/reader.js DOC_LINK regex (line 14) — /\[([^\]]+)\]\(recense:\/\/doc\/([a-z0-9-]+)\)/g
src/consolidation/doc-writer.ts (27-02) — writeDoc; add doc_link edge creation alongside cites edges
src/reader/doc-generator.ts (27-02) — generateDoc returns markdown; parse recense://doc/<id> refs
src/viz/server.ts /graph (210–233) — JSON node/link pattern to extend with a type=doc filter
src/viz/modules/reader.js btn show/hide (27-03) — toggle pattern; add #btn-corpus second button
PATTERNS §styles.css — #btn-corpus { display:none } / .mode-window #btn-corpus { display:inline-flex } (expanded-only gate, D-07)
Viz density anchor (~3,500 nodes) + lod.js adaptive density — do NOT re-tune absolute node sizes
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: doc_link edge creation from recense://doc/<id> refs</name>
  <files>src/reader/doc-generator.ts, src/consolidation/doc-writer.ts, tests/doc-link-edges.test.ts</files>
  <read_first>
    - src/reader/doc-generator.ts (the file being modified — citedFactIds parse; add doc-ref parse)
    - src/consolidation/doc-writer.ts (the file being modified — cites-edge loop; add doc_link loop in the same transaction)
    - src/viz/modules/reader.js (DOC_LINK regex line 14 — reuse the exact ref shape)
    - .planning/phases/27-reader-layer/27-CONTEXT.md (Claude's-Discretion §"doc_link edge creation" — derived from recense://doc/<id> refs the generator emits)
  </read_first>
  <behavior>
    - generateDoc additionally returns linkedDocRefs: the unique target ids parsed from recense://doc/<id> refs in the generated markdown.
    - writeDoc creates one kind='doc_link' edge (src=this doc node, dst=target doc node) per unique linkedDocRef whose target doc node EXISTS — refs to a non-existent doc node are skipped (no dangling FK), same in-set guard the cites path uses.
    - doc_link edges are written inside the same single transaction as the node + cites edges (atomic single-writer).
  </behavior>
  <action>
    In doc-generator.ts, parse recense://doc/<id> refs from the generated markdown (reuse the DOC_LINK id shape [a-z0-9-]+) and return them as linkedDocRefs (deduped) alongside citedFactIds. In doc-writer.ts, after the cites-edge loop and inside the SAME db.transaction().immediate(), for each linkedDocRef resolve the target doc node id (the corpus may key doc nodes by slug — resolve the ref to the actual node id via node_doc.slug or a direct node id, whichever the ref scheme uses; skip if no live doc node exists) and upsertEdge({src:thisDocId, dst:targetDocId, rel:'doc_link', kind:'doc_link', w:1.0, last_access:now}). Skip refs whose target doc node does not exist (FK-safe, mirrors the cites in-set guard). No raw edge SQL — route through upsertEdge (single-writer).
  </action>
  <verify>
    <automated>npx vitest run tests/doc-link-edges.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n "doc_link" src/consolidation/doc-writer.ts` returns the edge creation; `grep -n "recense://doc/\|DOC" src/reader/doc-generator.ts` shows the doc-ref parse.
    - BEHAVIOR: tests/doc-link-edges.test.ts seeds two doc nodes; generates/writes a doc whose markdown links the other via recense://doc/<id> AND links a non-existent doc id; asserts exactly one doc_link edge is created (to the existing doc), the dangling ref is skipped, and PRAGMA foreign_key_check is empty.
    - TEST-COMMAND: `npx vitest run tests/doc-link-edges.test.ts` exits 0.
  </acceptance_criteria>
  <done>doc_link edges are minted from in-prose recense://doc refs to existing doc nodes only, inside the single writer transaction, FK-clean.</done>
</task>

<task type="auto">
  <name>Task 2: GET /graph?type=doc corpus endpoint + expanded-only swap button + doc-node→reader</name>
  <files>src/viz/server.ts, src/viz/modules/reader.js, src/viz/index.html, src/viz/css/styles.css, tests/viz-corpus-graph.test.ts</files>
  <read_first>
    - src/viz/server.ts (the file being modified — /graph 210–233; compile-once stmts)
    - src/viz/modules/reader.js (the file being modified — toggle/show/hide pattern from 27-03)
    - src/viz/index.html (the file being modified — add #btn-corpus button markup)
    - src/viz/css/styles.css (the file being modified — #btn-corpus expanded-only gate)
    - .planning/phases/27-reader-layer/27-PATTERNS.md (§"src/viz/server.ts" corpus graph filter; §"reader.js" corpus toggle; §"styles.css" #btn-corpus rule)
  </read_first>
  <action>
    In src/viz/server.ts, extend /graph to honor `?type=doc`: compile once a doc-nodes statement (SELECT id, type, value, s, c, origin, tombstoned FROM node WHERE type='doc' AND tombstoned=0) and a doc-links statement (SELECT src, dst, rel, w, kind FROM edge WHERE kind='doc_link'); when the query has type=doc, return {nodes:docNodes, links:docLinks} (mapping src/dst→source/target like the existing /graph). Server-side type filter (chosen over a client nodeIds filter for the corpus view — lower risk, per CONTEXT discretion). In reader.js, add a SECOND button #btn-corpus (distinct from #btn-reader) that swaps the live graph data to the corpus graph via ctx.Graph.graphData({nodes, links}) from a fetch of /graph?type=doc, and swaps back to the full brain on toggle-off. Clicking a doc node in the corpus graph opens its reader (D-08 — resolve the doc node's slug → open /?doc=<slug>&reader=1 path or call the reader show() with that slug). In index.html add the #btn-corpus button; in styles.css add `#btn-corpus { display:none }` and `.mode-window #btn-corpus { display:inline-flex }` so the button shows ONLY in the expanded Brain Window and NEVER in the tray/popover (.mode-popover) — D-07. Do NOT re-tune absolute node sizes / BRAIN_SCALE (density anchor invariant) — the corpus graph reuses the existing node rendering at the doc-node count.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-corpus-graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n "type='doc'\|type=doc" src/viz/server.ts` and `grep -n "kind='doc_link'\|kind = 'doc_link'" src/viz/server.ts` return matches (corpus endpoint).
    - SOURCE: `grep -n "btn-corpus" src/viz/modules/reader.js` and `grep -n "btn-corpus" src/viz/index.html` and `grep -n "mode-window #btn-corpus\|#btn-corpus" src/viz/css/styles.css` all return matches (expanded-only gate present).
    - BEHAVIOR: tests/viz-corpus-graph.test.ts (seeded DB with doc nodes + doc_link edges) asserts GET /graph?type=doc returns only doc nodes and only doc_link links (no fact/entity nodes, no cites/relation links); GET /graph (no filter) still returns the full graph.
    - TEST-COMMAND: `npx vitest run tests/viz-corpus-graph.test.ts` exits 0.
  </acceptance_criteria>
  <done>/graph?type=doc returns the doc-only corpus graph; an expanded-only swap button swaps to it and back; clicking a doc node opens its reader; density anchor untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the corpus graph navigation and per-project subsumption</name>
  <files>src/viz/modules/reader.js</files>
  <action>
    Pause for the founder. Claude has generated docs for >=3 real projects (some cross-linking) on a copy of the live DB and started the expanded viz. Surface the URL and wait for the corpus-graph verdict before proceeding.
  </action>
  <what-built>The doc→doc corpus graph. Claude has generated docs for ≥3 real projects (some cross-linking via recense://doc refs) on a copy of the live DB and started the expanded viz.</what-built>
  <how-to-verify>
    1. In the EXPANDED Brain Window, confirm the corpus-graph swap button (#btn-corpus) is visible. (Optional: confirm in the tray/popover viz it is NOT shown.)
    2. Click it → confirm the graph swaps to the doc→doc corpus (a small graph of doc nodes connected by doc_link edges), not the full ~3,500-node brain.
    3. Center on one project's doc → confirm its doc(s) appear alongside neighboring projects/related entities (this subsumes a per-project graph view, D-09).
    4. Click a doc node → confirm its reader opens (D-08 browse path).
    5. Swap back → confirm the full brain returns and the density/look is unchanged (no node-size regression).
  </how-to-verify>
  <resume-signal>Type "approved" if the corpus graph navigates and subsumes the per-project view, or describe the gap.</resume-signal>
<verify>
  <human-check>Founder has reviewed the running build and typed "approved" (or described the gap).</human-check>
</verify>
<done>Founder has confirmed the corpus graph navigates, subsumes the per-project view (D-09), opens readers on doc-node click (D-08), and leaves the density anchor unchanged (or described the gap).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc markdown doc-refs → doc_link edges | a generated ref could point at an arbitrary/non-existent id → dangling FK |
| corpus graph values → browser DOM | doc node values rendered in the graph → XSS surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-15 | Tampering (dangling FK) | doc_link edge creation | mitigate | doc_link edges created only to EXISTING doc nodes (in-set guard, same as cites); refs to non-existent docs skipped; test asserts foreign_key_check empty. |
| T-27-16 | Tampering (XSS) | corpus node values in viz | mitigate | Doc node values rendered through the existing viz node-label path (textContent / sanitized) — corpus reuses the same render, no new innerHTML; detail panel stays textContent-only (T-10-12). |
| T-27-17 | Information disclosure / clutter | corpus button in tray | mitigate | CSS gate shows #btn-corpus only in .mode-window — never .mode-popover (D-07 glance-only tray invariant); checkpoint verifies. |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | Net-zero new runtime deps; no install tasks. |
</threat_model>

<verification>
- `npx vitest run tests/doc-link-edges.test.ts tests/viz-corpus-graph.test.ts` green.
- `npx tsc --noEmit` green.
- Corpus-graph checkpoint approved.
</verification>

<success_criteria>
- doc_link edges minted from in-prose doc refs (FK-safe); /graph?type=doc serves the corpus; expanded-only swap button navigates it and back; doc-node click opens the reader; centering on a project subsumes the per-project view (READER-04).
</success_criteria>

<output>
Create `.planning/phases/27-reader-layer/27-05-SUMMARY.md` when done.
</output>
