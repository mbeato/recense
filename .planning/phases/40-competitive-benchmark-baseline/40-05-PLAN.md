---
phase: 40-competitive-benchmark-baseline
plan: 05
type: execute
wave: 4
depends_on: ["40-02", "40-03", "40-04"]
files_modified:
  - .planning/phases/40-competitive-benchmark-baseline/40-BASELINE.md
autonomous: false
requirements: [BENCH-02]
must_haves:
  truths:
    - "The D-01 cost probe runs ONE LoCoMo conversation, reads the real subscription usage/total_cost_usd envelope, extrapolates x10, and is confirmed against the /usage weekly meter BEFORE any full run"
    - "The official baseline run executes only against the v7.0-tagged SUT (post-39.1-05), capturing commit + the D-10 config snapshot in the results JSON"
    - "The live-brain p50/p95 retrieval latency is recorded on the ~9,062-node brain (retrieval-only)"
    - "40-BASELINE.md records headline J + R@5/R@10 + latency (live p50/p95 + synthetic curve) + per-write/per-recall token cost, with subscription-token vs direct-API-$ kept separate"
  artifacts:
    - path: ".planning/phases/40-competitive-benchmark-baseline/40-BASELINE.md"
      provides: "The written BENCH-02 baseline (accuracy + latency + token) with reproduction commands"
      contains: "p95"
  key_links:
    - from: ".planning/phases/40-competitive-benchmark-baseline/40-BASELINE.md"
      to: "scripts/eval/results/locomo-{commit}.json"
      via: "headline J + config snapshot reference"
      pattern: "locomo-"
    - from: "D-01 probe"
      to: "/usage weekly meter"
      via: "operator budget confirmation gate"
      pattern: "usage"
---

<objective>
The operator-gated terminal step: fire the D-01 cost probe (HARD GATE), confirm budget, then run the official baseline against the v7.0-tagged SUT and write the BENCH-02 baseline document. Every run task here is `autonomous: false` — an executor MUST NOT trigger a paid full run on its own. The harness, scorer, and latency curve are already built and dry-run-green (Plans 02-04); this plan is the human-triggered firing + write-up that happens pre-reset, post-v7.0-tag.

Purpose: BENCH-02 — the written baseline of accuracy, latency (p50/p95), and token cost that gates Phases 41-43. Reproducible from committed scripts; competitor numbers cited from Plan 04's note.
Output: `40-BASELINE.md` + the committed `locomo-{commit}.json` / `latency-curve-N.json` result files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-competitive-benchmark-baseline/40-CONTEXT.md
@.planning/phases/40-competitive-benchmark-baseline/40-RESEARCH.md
@.planning/phases/40-competitive-benchmark-baseline/40-02-SUMMARY.md
@.planning/phases/40-competitive-benchmark-baseline/40-03-SUMMARY.md
@.planning/phases/40-competitive-benchmark-baseline/40-04-SUMMARY.md
@.planning/phases/40-competitive-benchmark-baseline/40-COMPETITOR-TARGETS.md

<interfaces>
<!-- Reproduction commands the operator runs (built in Plans 02-04). -->

D-01 probe (RESEARCH Pitfall 3, VALIDATION Manual-Only):
  - `node scripts/eval/locomo-harness.cjs --probe --eval scripts/eval/locomo10.json --out <tmp>` -> runs ONE conversation (PROBE_LIMIT=1), prints subscription usage / total_cost_usd from the claude -p --output-format json envelope + the probe conversation's session count.
  - Pick the MEDIAN-length conversation for extrapolation (sort 10 conversations by session count, pick the 5th/6th); report min x10 .. max x10 range alongside median x10 (Pitfall 3).
  - Confirm projected % against Claude Code `/usage` weekly meter BEFORE running the full set.

Official full run (D-09, D-10):
  - Requires v7.0 tag present (post-39.1-05). Verify `git tag --list v7.0` is non-empty and the SUT is at the v7.0 commit before running.
  - `node scripts/eval/locomo-harness.cjs --run --eval scripts/eval/locomo10.json --out scripts/eval/results/locomo-hypotheses.jsonl` (full 10 conversations, subscription-billed Haiku/Sonnet).
  - `node scripts/eval/locomo-scorer.cjs --in scripts/eval/results/locomo-hypotheses.jsonl --out scripts/eval/results/locomo-<commit>.json` (gpt-4o-mini judge, direct-API $) -> headline J + by_category + rAtK + D-10 sut_config.

Live-brain latency (D-06a):
  - Time CandidateRetriever.topk retrieval-only against the live ~9,062-node brain; report p50/p95. Retrieval-only (Pitfall 4); the online path stays LLM-free.

Synthetic latency curve (D-06b, Plan 04):
  - `node scripts/eval/latency-curve.cjs --out scripts/eval/results/latency-curve-N.json` (full N list).

Cost separation (D-02, CLAUDE.md):
  - subscription tokens (Haiku extract + Sonnet judge via claude -p) reported separately from direct-API $ (gpt-4o-mini scorer + OpenAI embeddings). Reuse cost-benefit-probe.json per_model write-ledger shape.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: D-01 cost probe — HARD GATE before any full run</name>
  <read_first>
    - .planning/phases/40-competitive-benchmark-baseline/40-RESEARCH.md (Pitfall 3: median-conversation extrapolation, log session count, report range)
    - .planning/phases/40-competitive-benchmark-baseline/40-CONTEXT.md (D-01/D-02/D-03 cost gate)
    - .planning/phases/40-competitive-benchmark-baseline/40-VALIDATION.md (Manual-Only: probe -> /usage confirmation)
    - scripts/eval/results/cost-benefit-probe.json (the per_model write-ledger shape to mirror)
  </read_first>
  <what-built>
    The harness (Plan 02) supports `--probe` (PROBE_LIMIT=1): it runs ONE full LoCoMo conversation through ingest->consolidate->retrieve->answer and reports the subscription usage / total_cost_usd envelope plus that conversation's session count. The scorer + latency curve are dry-run-validated. Nothing paid has run yet.
  </what-built>
  <how-to-verify>
    1. Source the sleep env: `set -a; . ~/.config/recense/sleep.env; set +a` then `npm run build`.
    2. Identify the median-length conversation: count sessions per conversation in locomo10.json, sort, pick the 5th/6th of 10.
    3. Run the probe: `node scripts/eval/locomo-harness.cjs --probe --eval scripts/eval/locomo10.json --out /tmp/locomo-probe.jsonl` (probes one conversation — confirm it is representative; if not, note the session-count delta).
    4. Read the reported subscription usage + total_cost_usd and the probe conversation's session count.
    5. Extrapolate: median x10 plus the min x10 .. max x10 range (session counts vary 19-35 — Pitfall 3).
    6. Open Claude Code `/usage` and confirm the projected full-run % against the remaining weekly 20x general-bucket budget.
    7. Record the measured per-conversation cost, the x10 projection + range, and the gpt-4o-mini scorer + OpenAI embedding direct-$ estimate for the full ~1,491 scoreable questions.
    DECISION: approve proceeding to Task 2 only if the projected full-run subscription % is acceptable against /usage (D-03: schedule the heavy run pre-reset if it shows meaningful budget use). If too costly, STOP — defer the run, not the phase.
  </how-to-verify>
  <resume-signal>Type "approved — proceed to full run" with the measured cost + projected %, or "defer run" with the reason. Do not proceed to Task 2 without explicit approval.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Official baseline run on the v7.0-tagged SUT (operator-triggered, pre-reset)</name>
  <read_first>
    - .planning/phases/40-competitive-benchmark-baseline/40-CONTEXT.md (D-09 full LoCoMo-10; D-10 freeze SUT at v7.0 tag, capture commit + config snapshot)
    - .planning/STATE.md (v7.0 tag status — d41d5c8; 39.1-05 doc-verification deferred async, does not block the tag)
    - .planning/phases/40-competitive-benchmark-baseline/40-RESEARCH.md (Pitfall 6: official run waits for v7.0 tag; Pitfall 4: latency retrieval-only)
    - .planning/phases/40-competitive-benchmark-baseline/40-02-SUMMARY.md / 40-03-SUMMARY.md (harness + scorer reproduction commands)
  </read_first>
  <what-built>
    Task 1's probe confirmed the budget. The harness + scorer are built and dry-run-green. v7.0 is tagged (d41d5c8 per STATE; verify `git tag --list v7.0` non-empty). This task fires the full paid run + scoring + both latency measurements.
  </what-built>
  <how-to-verify>
    1. Verify the SUT: `git tag --list v7.0` is non-empty and HEAD is at (or checked out to) the v7.0-tagged commit — the baseline measures the frozen v7.0 system (D-10).
    2. Full harness run (subscription-billed): `node scripts/eval/locomo-harness.cjs --run --eval scripts/eval/locomo10.json --out scripts/eval/results/locomo-hypotheses.jsonl` (all 10 conversations).
    3. Score (direct-API $): `node scripts/eval/locomo-scorer.cjs --in scripts/eval/results/locomo-hypotheses.jsonl --out scripts/eval/results/locomo-<commit>.json` -> confirm meta.sut_commit + meta.judge_model=gpt-4o-mini + meta.sut_config (15 knobs) present, and questions_total ~ 1,491 (category-5 excluded).
    4. Live-brain latency (D-06a): time CandidateRetriever.topk retrieval-only against the live ~9,062-node brain; record p50/p95.
    5. Synthetic curve (D-06b): `node scripts/eval/latency-curve.cjs --out scripts/eval/results/latency-curve-N.json` (full N list).
    6. Confirm the result JSON files exist and carry the config snapshot + scores (headline J, by_category, rAtK).
  </how-to-verify>
  <resume-signal>Type "baseline run complete" once locomo-<commit>.json + latency-curve-N.json exist with the config snapshot and live p50/p95 recorded, or describe any failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 40-BASELINE.md — the BENCH-02 baseline document</name>
  <read_first>
    - scripts/eval/results/locomo-<commit>.json (Task 2 — headline J, by_category, rAtK, sut_config)
    - scripts/eval/results/latency-curve-N.json (Task 2 — synthetic curve)
    - .planning/phases/40-competitive-benchmark-baseline/40-COMPETITOR-TARGETS.md (Plan 04 — the cited comparators)
    - .planning/phases/40-competitive-benchmark-baseline/40-CONTEXT.md (D-02 cost separation; specifics: every recense number reproducible from a committed script)
    - scripts/eval/results/cost-benefit-probe.json (per_model token-ledger shape)
  </read_first>
  <action>
    Write `.planning/phases/40-competitive-benchmark-baseline/40-BASELINE.md` recording the BENCH-02 baseline on all three axes, every number sourced to a committed script + result file (no rounded/vibe figures — founder rule):
    ACCURACY — headline Overall J (from locomo-<commit>.json), R@5/R@10 (rAtK, labeled "retrieval diagnostic" per Pitfall 2 — NOT the headline), and the questions_total / adversarial-excluded counts; cite mem0's 66.88% as the primary comparator from 40-COMPETITOR-TARGETS.md.
    LATENCY — live-brain retrieval-only p50/p95 on ~9,062 nodes (headline, D-06a) + the synthetic latency-vs-N curve (D-06b), explicitly retrieval-only (Pitfall 4); note recense's retrieval-only definition differs from mem0's answer-gen-inclusive 91% claim.
    TOKEN — per-write (Haiku extract + Sonnet judge, subscription) and per-recall (inject) token cost on the LoCoMo corpus (D-07), in the cost-benefit-probe.json per_model shape; KEEP subscription tokens (Haiku/Sonnet) SEPARATE from direct-API $ (gpt-4o-mini scorer + OpenAI embeddings) per D-02.
    CONFIG — embed the D-10 sut_config snapshot + sut_commit so the baseline is reproducible against the frozen v7.0 system.
    REPRODUCTION — list the exact commands (harness --run, scorer, latency-curve) so any number can be re-derived. State the benchmark noise floor (differences < 5-7 pts not interpretable) from 40-COMPETITOR-TARGETS.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/40-competitive-benchmark-baseline/40-BASELINE.md && grep -q "p95" .planning/phases/40-competitive-benchmark-baseline/40-BASELINE.md && grep -qi "R@10\|R@5\|rAtK" .planning/phases/40-competitive-benchmark-baseline/40-BASELINE.md && grep -qi "subscription\|per_model\|token" .planning/phases/40-competitive-benchmark-baseline/40-BASELINE.md && grep -q "66.88\|mem0" .planning/phases/40-competitive-benchmark-baseline/40-BASELINE.md && grep -qi "reproduc\|node scripts/eval" .planning/phases/40-competitive-benchmark-baseline/40-BASELINE.md</automated>
  </verify>
  <done>40-BASELINE.md records accuracy (J headline + R@K diagnostic), latency (live p50/p95 + synthetic curve, retrieval-only), token cost (per-write/per-recall, subscription vs direct-$ separated), the D-10 config snapshot, mem0's 66.88% comparator, and reproduction commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> paid full run | the --run + probe gate; only a human approves spending subscription + API budget |
| harness -> live brain (read-only latency) | D-06a times topk against the live recense.db read-only; no writes to the live brain |
| results -> git | result JSONs carry model strings + config knobs, never secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-11 | Denial of Service (budget) | paid full run firing without budget confirmation | mitigate | Task 1 D-01 probe is a blocking-human HARD GATE; Task 2 is blocking-human and verifies the v7.0 tag before spending; autonomous: false on the plan; auto-advance ignored |
| T-40-12 | Tampering (metrics integrity) | baseline measured against a non-frozen SUT (not v7.0) | mitigate | Task 2 verifies `git tag --list v7.0` + the config snapshot in meta records the exact sut_commit — any drift is visible in the recorded snapshot |
| T-40-13 | Information Disclosure | live-brain content leaking into committed results | mitigate | D-06a records only p50/p95 latency numbers, not node values; accuracy/token measured on the public LoCoMo corpus (D-07), never the private brain |
| T-40-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH §Standard Stack: zero new deps) — nothing to slopcheck |
</threat_model>

<verification>
- D-01 probe run + /usage confirmation recorded (blocking-human Task 1).
- v7.0 tag verified; full run + scorer + latency produce locomo-<commit>.json + latency-curve-N.json with the config snapshot (blocking-human Task 2).
- 40-BASELINE.md contains p95, R@K, token/subscription separation, the mem0 66.88% comparator, and reproduction commands.
</verification>

<success_criteria>
- The D-01 cost probe gates the full run; the official baseline runs only on the v7.0-tagged SUT with the config snapshot captured.
- 40-BASELINE.md is the written BENCH-02 baseline on all three axes, every recense number reproducible from a committed script, competitor numbers cited from 40-COMPETITOR-TARGETS.md.
</success_criteria>

<output>
Create `.planning/phases/40-competitive-benchmark-baseline/40-05-SUMMARY.md` when done.
</output>
