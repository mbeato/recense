---
phase: 40-competitive-benchmark-baseline
plan: 04
type: execute
wave: 3
depends_on: ["40-02"]
files_modified:
  - scripts/eval/latency-curve.cjs
  - scripts/eval/fixtures/locomo-node-pool.json
  - .planning/phases/40-competitive-benchmark-baseline/40-COMPETITOR-TARGETS.md
  - tests/locomo-latency-curve.test.ts
autonomous: true
requirements: [BENCH-02, BENCH-03]
must_haves:
  truths:
    - "latency-curve.cjs builds fresh scratch DBs at controlled N (1K/2K/5K/9K/15K/20K), times CandidateRetriever.topk retrieval-only, and reports p50/p95 per N"
    - "The node pool is drawn from real LoCoMo-derived node values (public CC BY-NC corpus), not random strings or private brain content"
    - "The latency curve runs reproducibly from a committed script + a public node pool, exposing no private data"
    - "A competitor-targets methodology note documents every rival number with a one-line 'what it actually measures' caveat and explicitly DO-NOT-CITEs Zep's inflated 84%, using mem0's 66.88% as the primary comparator"
  artifacts:
    - path: "scripts/eval/latency-curve.cjs"
      provides: "D-06b synthetic latency-vs-N curve (retrieval-only p50/p95)"
      min_lines: 120
    - path: "scripts/eval/fixtures/locomo-node-pool.json"
      provides: "public LoCoMo-derived node-value pool for the latency curve"
    - path: ".planning/phases/40-competitive-benchmark-baseline/40-COMPETITOR-TARGETS.md"
      provides: "BENCH-03 sourced + methodology-noted competitor target table"
      contains: "DO NOT CITE"
  key_links:
    - from: "scripts/eval/latency-curve.cjs"
      to: "dist/src/retrieval/topk (CandidateRetriever.topk)"
      via: "timed retrieval-only call at each N"
      pattern: "topk\\("
    - from: ".planning/phases/40-competitive-benchmark-baseline/40-COMPETITOR-TARGETS.md"
      to: "mem0 66.88% / Zep 58.44% corrected"
      via: "primary comparator + DO-NOT-CITE note"
      pattern: "66.88"
---

<objective>
Two BENCH-axis deliverables that depend on the harness existing but not on a paid run:
(1) `latency-curve.cjs` (D-06b) — the reproducible, public-corpus latency-vs-N curve that shows recense's brute-force O(N) cosine cost at scale without exposing the private brain;
(2) `40-COMPETITOR-TARGETS.md` (D-08 / BENCH-03) — the sourced, methodology-noted competitor table that bakes in the no-inflated-metrics rule (DO-NOT-CITE Zep's inflated 84%; mem0's 66.88% as primary comparator).

Purpose: BENCH-02 (the methodology-reproducible latency curve complementing the live-brain p50/p95) + BENCH-03 (competitor numbers cited AND methodology-understood). Both are autonomous and cheap (the curve uses the embedder for ~20 queries; no Haiku/Sonnet, no judge).
Output: `latency-curve.cjs`, the public node pool, the competitor-targets note, a latency-curve unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-competitive-benchmark-baseline/40-CONTEXT.md
@.planning/phases/40-competitive-benchmark-baseline/40-RESEARCH.md
@.planning/phases/40-competitive-benchmark-baseline/40-PATTERNS.md
@.planning/phases/40-competitive-benchmark-baseline/40-02-SUMMARY.md
@scripts/eval/replay-ku-harness.cjs

<interfaces>
<!-- Analog targets (line refs verified in PATTERNS). -->

latency-curve.cjs analogs:
  - makeScratchDb() : replay-ku-harness.cjs 230-251 (or copy from locomo-harness.cjs Plan 02) — fresh tmpdir DB + initSchema + DEFAULT_CONFIG.
  - CandidateRetriever construction: `new CandidateRetriever(scratch.db)` (replay-ku-harness.cjs ~257).
  - Timing: `const t0=Date.now(); const r=retriever.topk(queryVec,K); const ms=Date.now()-t0;` (retrieval-only, Pitfall 4).
  - percentile(sortedArr, p): standard `Math.ceil(p/100*len)-1` index (PATTERNS 443-446) — no existing analog, implement inline.
  - OpenAIEmbedder: require DIST+'/model/embedder' (same import the harness uses) to embed the ~20 test queries.
  - Output envelope: { meta: {eval:'latency-curve', date, commit, engine_version, embed_model, k, queries_per_n}, curve: [{n_nodes, p50_ms, p95_ms, samples}] } → scripts/eval/results/latency-curve-N.json.

Node pool (D-06b, RESEARCH Item 5):
  - Exported from a completed locomo scratch DB: `SELECT value FROM node WHERE tombstoned=0 AND embedding IS NOT NULL`.
  - Store as a flat JSON array in scripts/eval/fixtures/locomo-node-pool.json. Cycle the pool when N > pool size.
  - Public (CC BY-NC LoCoMo-derived) — no private brain content. Live-brain headline N for context = 9,062 nodes (RESEARCH; that measurement belongs to the live run, not this script).

Competitor numbers (RESEARCH Item 4 — verbatim, with caveats):
  - mem0 LoCoMo Overall J = 66.88% ± 0.15 (gpt-4o-mini judge, adversarial excluded) — PRIMARY comparator.
  - Zep LoCoMo ~84% (marketing) = DO NOT CITE — inflated ~25 pts by adversarial-denominator bug; corrected ~58.44% ± 0.20 [getzep/zep-papers#5].
  - Zep DMR 94.8% (gpt-4-turbo), LongMemEval 71.2% (gpt-4o) — published/real; Zep did NOT evaluate LoCoMo.
  - MemPalace LongMemEval R@5 96.6% = RAW ChromaDB embedder (all-MiniLM-L6-v2), NOT the palace architecture; 84.2% compressed; LoCoMo R@10 88.9% = keyword heuristic, no LLM.
  - mem0 "91% lower latency" = p95 1.44s vs 17.1s full-context (answer-gen included, NOT retrieval-only — different from recense's D-06).
  - mem0 "90% fewer tokens" = 1,764 vs 26,031 context tokens/query (vs full-context).
  - claude-mem "~10x token savings" = UNVERIFIED, no primary benchmark, straw-man vs full-context dump; no accuracy bench.
  - Benchmark quality: 6.4% corrupted answer key + lenient judge (~62.81% accept rate for wrong-but-on-topic) → differences < 5-7 pts are noise.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export the public node pool from a completed LoCoMo scratch DB</name>
  <read_first>
    - .planning/phases/40-competitive-benchmark-baseline/40-RESEARCH.md (Item 5: node-pool export query + approach)
    - scripts/eval/locomo-harness.cjs (Plan 02 — supports --keep-dbs to retain a scratch DB; --dry-run produces no nodes, so a small real consolidation slice is needed)
    - scripts/eval/fixtures/locomo-mini.json (the small input to consolidate for pool extraction)
  </read_first>
  <action>
    Produce `scripts/eval/fixtures/locomo-node-pool.json` as a flat JSON array of node `value` strings drawn from LoCoMo-derived nodes. Generate it by running the Plan 02 harness with `--keep-dbs` over the small locomo-mini.json fixture (a real but tiny consolidation — this is the one place a few subscription-billed extract calls are acceptable; it is NOT the gated full run and produces only the pool, no scores), then `SELECT value FROM node WHERE tombstoned=0 AND embedding IS NOT NULL` from the kept scratch DB and write the values to the pool file. If subscription cost is a concern at execution time, the executor MAY instead extract node-shaped value strings directly from locomo10.json turn texts (sentence/entity-length fragments) to populate the pool — the pool only needs a realistic length/shape distribution, not real consolidation output. Either way the pool is public LoCoMo-derived content (CC BY-NC), never private brain data. The pool file MAY be committed (it is derived public text, node values only) — confirm it contains no private content before writing.
  </action>
  <verify>
    <automated>node -e "const p=require('./scripts/eval/fixtures/locomo-node-pool.json'); if(!Array.isArray(p)||p.length<50)process.exit(1); if(!p.every(v=>typeof v==='string'))process.exit(1); console.log('pool ok:',p.length,'values');"</automated>
  </verify>
  <done>locomo-node-pool.json is a flat array of ≥50 public LoCoMo-derived node-value strings, no private content.</done>
</task>

<task type="auto">
  <name>Task 2: latency-curve.cjs — retrieval-only p50/p95 across N</name>
  <read_first>
    - scripts/eval/replay-ku-harness.cjs (makeScratchDb 230-251, CandidateRetriever construction ~257)
    - scripts/eval/locomo-harness.cjs (Plan 02 — reuse makeScratchDb + embedder import + timing pattern)
    - .planning/phases/40-competitive-benchmark-baseline/40-PATTERNS.md (latency-curve.cjs section: scratch DB factory, topk timing, percentile fn, output envelope)
    - .planning/phases/40-competitive-benchmark-baseline/40-RESEARCH.md (Item 5: N = 1K/2K/5K/9K/15K/20K, 20 queries/N, retrieval-only timing; Pitfall 4)
    - scripts/eval/fixtures/locomo-node-pool.json (Task 1 output)
  </read_first>
  <action>
    Create `scripts/eval/latency-curve.cjs`. Implement `percentile(sortedArr, p)` inline (Math.ceil(p/100*len)-1 index). Default `--n-list 1000,2000,5000,9000,15000,20000` and `--queries-per-n 20` and `--k 10` (override-able). Load the node pool; embed ~20 held-out test queries (reuse locomo10.json QA questions or fixed strings) once via OpenAIEmbedder. For each N: makeScratchDb(), insert N nodes by cycling the pool (upsert node + setEmbedding via the same SemanticStore path the harness uses, or insert directly — match how the harness populates embeddings), construct `new CandidateRetriever(scratch.db)`, then for each test query time ONLY the topk call (`t0=Date.now(); retriever.topk(qVec, K); Date.now()-t0`), collect latencies, sort, compute p50/p95, push `{n_nodes:N, p50_ms, p95_ms, samples}` to the curve. Write `{ meta, curve }` to `scripts/eval/results/latency-curve-N.json`. Add a `--dry-run` (or `--quick`) mode that runs a single small N with 2 queries so CI can validate the script end-to-end cheaply (embedder may be mocked/stubbed in this mode to stay zero-API, mirroring the harness IS_DRY_RUN posture). Timing is retrieval-only — never include embed or answer time (Pitfall 4).
  </action>
  <verify>
    <automated>npm run build && node scripts/eval/latency-curve.cjs --quick --n-list 200 --queries-per-n 2 --out /tmp/latency-quick-$$.json && node -e "const r=require('/tmp/latency-quick-'+process.env.PPID+'.json')" 2>/dev/null; grep -q "function percentile\|const percentile" scripts/eval/latency-curve.cjs && grep -q "p95_ms\|p95" scripts/eval/latency-curve.cjs && grep -q "topk" scripts/eval/latency-curve.cjs</automated>
  </verify>
  <done>latency-curve.cjs runs in --quick mode (small N, zero/minimal API), times retrieval-only topk, and emits a {meta, curve:[{n_nodes,p50_ms,p95_ms,samples}]} JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Competitor-targets methodology note (BENCH-03) + un-skip latency-curve smoke test</name>
  <read_first>
    - .planning/phases/40-competitive-benchmark-baseline/40-RESEARCH.md (Item 4: full competitor verification table with every caveat; Pitfall 6: Zep DO-NOT-CITE; State of the Art: benchmark quality / noise floor)
    - .planning/phases/40-competitive-benchmark-baseline/40-CONTEXT.md (D-08: cite + methodology-note; specifics: founder no-inflated-metrics framing)
    - CLAUDE.md (project no-inflated-metrics hard rule — applies to reading competitors)
    - tests/eval-harness-smoke.test.ts (spawnSync .cjs pattern for the latency-curve smoke test)
  </read_first>
  <action>
    Write `.planning/phases/40-competitive-benchmark-baseline/40-COMPETITOR-TARGETS.md` as the BENCH-03 deliverable: a table with one row per competitor number (mem0 LoCoMo J 66.88%, mem0 latency 91%, mem0 tokens 90%, MemPalace R@5 96.6% raw / 84.2% compressed, MemPalace LoCoMo R@10 88.9%, Zep DMR 94.8%, Zep LongMemEval 71.2%, Zep LoCoMo ~84% DO-NOT-CITE / 58.44% corrected, claude-mem ~10x unverified) and for EACH: the published source, the exact metric/config that produced it, and a one-line "what it actually measures" caveat (verbatim from RESEARCH Item 4 — do not soften). Explicitly flag: Zep's 84% LoCoMo is DO NOT CITE (denominator inflation, [getzep/zep-papers#5]); mem0's 66.88% is the primary LoCoMo comparator; MemPalace 96.6% measures the embedder not the architecture; claude-mem has no accuracy benchmark. State the benchmark noise floor (6.4% corrupted key + lenient judge → differences < 5-7 pts are not interpretable) and the comparison-config mismatches (recense latency = retrieval-only vs mem0's answer-gen-inclusive; recense token = per-write+per-recall vs mem0's context-tokens-per-query). Then edit tests/locomo-latency-curve.test.ts (create if absent): a spawnSync smoke that runs latency-curve.cjs --quick and asserts exit 0 + a curve array with p50_ms/p95_ms keys (mirror eval-harness-smoke.test.ts).
  </action>
  <verify>
    <automated>test -f .planning/phases/40-competitive-benchmark-baseline/40-COMPETITOR-TARGETS.md && grep -q "DO NOT CITE" .planning/phases/40-competitive-benchmark-baseline/40-COMPETITOR-TARGETS.md && grep -q "66.88" .planning/phases/40-competitive-benchmark-baseline/40-COMPETITOR-TARGETS.md && grep -q "58.44" .planning/phases/40-competitive-benchmark-baseline/40-COMPETITOR-TARGETS.md && grep -q "raw\|embedder" .planning/phases/40-competitive-benchmark-baseline/40-COMPETITOR-TARGETS.md && npm run build && npx vitest run tests/locomo-latency-curve.test.ts</automated>
  </verify>
  <done>40-COMPETITOR-TARGETS.md documents every rival number with source + methodology caveat, DO-NOT-CITEs Zep's 84%, names mem0 66.88% as primary comparator, states the noise floor; latency-curve smoke test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| latency-curve → scratch DBs | synthetic scratch DBs at controlled N in os.tmpdir(); live recense.db never read |
| node pool → repo | only public LoCoMo-derived value strings may be written (no private brain content) |
| competitor note → public claims | BENCH-03 integrity boundary — every cited number must be sourced and caveated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-09 | Information Disclosure | private brain content leaking into the committed node pool | mitigate | Pool is LoCoMo-derived public text (CC BY-NC); Task 1 confirms no private content; live-brain values are never exported here |
| T-40-10 | Tampering (metrics integrity) | citing Zep's inflated 84% or MemPalace 96.6% as a real architecture target (no-inflated-metrics violation) | mitigate | 40-COMPETITOR-TARGETS.md hard-flags Zep 84% as DO NOT CITE + uses mem0 66.88% as primary; every number carries its config caveat; Task 3 grep-asserts the DO-NOT-CITE + corrected figures |
| T-40-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH §Standard Stack: zero new deps) — nothing to slopcheck |
</threat_model>

<verification>
- `node scripts/eval/latency-curve.cjs --quick` exits 0, emits p50/p95 curve.
- node-pool is a public LoCoMo-derived string array (≥50 values), no private content.
- 40-COMPETITOR-TARGETS.md contains DO-NOT-CITE, 66.88, 58.44, and embedder caveat.
- `npx vitest run tests/locomo-latency-curve.test.ts` green.
</verification>

<success_criteria>
- latency-curve.cjs reproduces the retrieval-only latency-vs-N curve from a public pool (D-06b), no private data.
- 40-COMPETITOR-TARGETS.md satisfies BENCH-03: every number sourced + methodology-noted, Zep 84% DO-NOT-CITE, mem0 66.88% primary.
</success_criteria>

<output>
Create `.planning/phases/40-competitive-benchmark-baseline/40-04-SUMMARY.md` when done.
</output>
