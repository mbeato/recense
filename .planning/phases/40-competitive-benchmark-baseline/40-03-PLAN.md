---
phase: 40-competitive-benchmark-baseline
plan: 03
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - scripts/eval/locomo-scorer.cjs
  - tests/locomo-scorer.test.ts
autonomous: true
requirements: [BENCH-02, BENCH-03]
must_haves:
  truths:
    - "locomo-scorer.cjs judges each hypothesis with the verbatim mem0 Appendix A prompt (gpt-4o-mini, temperature 0, max_tokens 10, JSON {\"label\":\"CORRECT\"|\"WRONG\"})"
    - "Category-5 rows are skipped — the Overall J denominator equals count(category != 5)"
    - "parseJudgeVerdict handles the {\"label\":\"CORRECT\"|\"WRONG\"} envelope"
    - "The output meta carries the full D-10 v7.0 config snapshot (sut_commit + judge_model + all frozen knobs) read from DEFAULT_CONFIG"
    - "A --mock mode scores without an OpenAI call so CI can verify the denominator + verdict parse with zero API"
  artifacts:
    - path: "scripts/eval/locomo-scorer.cjs"
      provides: "mem0 Appendix A LLM-judge scorer (gpt-4o-mini) + category-5 skip + D-10 config snapshot in meta"
      min_lines: 150
    - path: "tests/locomo-scorer.test.ts"
      provides: "un-skipped category-5 denominator + verdict-parse + config-snapshot assertions (--mock)"
  key_links:
    - from: "scripts/eval/locomo-scorer.cjs"
      to: "gpt-4o-mini (OpenAI chat.completions)"
      via: "verbatim mem0 Appendix A judge prompt"
      pattern: "gpt-4o-mini"
    - from: "scripts/eval/locomo-scorer.cjs"
      to: "dist/src/lib/config (DEFAULT_CONFIG)"
      via: "D-10 config snapshot into meta.sut_config"
      pattern: "DEFAULT_CONFIG"
---

<objective>
Build `scripts/eval/locomo-scorer.cjs` as a clone-and-adapt of `longmemeval-scorer.cjs`: judge each harness hypothesis with the verbatim mem0 Appendix A prompt (gpt-4o-mini, temp 0, max_tokens 10, `{"label":...}` JSON), skip category 5 from the denominator, and stamp the full D-10 v7.0 config snapshot into the result `meta`. This is the headline-accuracy half of BENCH-02 and the no-inflated-metrics scorer-protocol half of BENCH-03 (our J score is comparable to mem0's published 66.88% only if it uses mem0's exact judge).

Purpose: BENCH-02 (headline J score + config snapshot) + BENCH-03 (replicated mem0/Zep protocol, methodology-honest scorer). Independent of the harness (Plan 02) — different files — so it runs in the same wave.
Output: `locomo-scorer.cjs`, un-skipped scorer unit tests (run with `--mock`, zero API).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-competitive-benchmark-baseline/40-CONTEXT.md
@.planning/phases/40-competitive-benchmark-baseline/40-RESEARCH.md
@.planning/phases/40-competitive-benchmark-baseline/40-PATTERNS.md
@.planning/phases/40-competitive-benchmark-baseline/40-01-SUMMARY.md
@scripts/eval/longmemeval-scorer.cjs

<interfaces>
<!-- Verbatim-copy + adapt targets from longmemeval-scorer.cjs (line refs verified). -->

longmemeval-scorer.cjs (the analog — clone these):
  - Imports + arg parsing (DIST require of run-sleep-pass + claude-headless-client + lib/config; OpenAI; IS_MOCK): 30-55.
  - getCommit() / getEngineVersion(): 82-97 — copy verbatim (git rev-parse --short HEAD; package.json version).
  - parseJudgeVerdict(text): 121-148 — copy verbatim, ADD the `obj.label === 'CORRECT'/'WRONG'` branch (PATTERNS shows the exact addition).
  - Judge call shape: model 'gpt-4o-mini', max_tokens 10, temperature 0, single user message (no system prompt) — PATTERNS lines 310-315.
  - Output envelope { meta, scores, per_question } + fs.writeFileSync(OUT_FILE, JSON.stringify(envelope, null, 2)): 453-465, 469.

LoCoMo-specific (RESEARCH Item 1, PATTERNS):
  - buildLoCoMoJudgePrompt(question, goldAnswer, hypothesis): NEW — verbatim mem0 Appendix A (arXiv 2504.19413). Single generic template, "be generous — same topic = CORRECT", returns {"label":"CORRECT"|"WRONG"} JSON. Full text in PATTERNS lines 322-335 and RESEARCH Item 1.
  - Judge model = 'gpt-4o-mini' (paper version, NOT gpt-4o-2024-08-06 the LME default). Record exact string in meta.judge_model (Open Question: gpt-4o-mini vs gpt-4o — meta must state which was used).
  - Category filter: `if (qa.category === 5) continue;` — denominator = count(category != 5).
  - Scores: { headline: overallJ, by_category, rAtK: { r5, r10 } } (r5/r10 carried through from harness hit5/hit10).

D-10 config snapshot — exact DEFAULT_CONFIG keys (verified src/lib/config.ts; require DIST+'/lib/config').DEFAULT_CONFIG):
  openaiEmbedModel(737), embeddingDimensions(738), claudeHeadlessExtractModel(735),
  claudeHeadlessJudgeModel(734), consolSkipThreshold(741), consolSkipThresholdAssistant(742),
  rankStrengthWeight(749), rankedRetrievalK, rankedRetrievalFloor, candidateK(739),
  entityAnchorK(740), typedAnchorPoolK, injectionTokenBudget, insightSurfacingEnabled,
  predicateGlossThreshold(752). Plus sut_commit=getCommit(), engine_version=getEngineVersion(),
  judge_model='gpt-4o-mini'. (NOTE: config lives in src/lib/config.ts — src/model/config.ts does NOT exist.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone the scorer — mem0 Appendix A prompt, gpt-4o-mini judge, category-5 skip, --mock</name>
  <read_first>
    - scripts/eval/longmemeval-scorer.cjs (full read — copy imports/arg-parse 30-55, getCommit/getEngineVersion 82-97, parseJudgeVerdict 121-148, judge call 207-219, output envelope 453-469)
    - .planning/phases/40-competitive-benchmark-baseline/40-RESEARCH.md (Item 1: exact judge model, verbatim Appendix A prompt, temp 0 / max_tokens 10, denominator = count(category!=5), scorer adapter handoff)
    - .planning/phases/40-competitive-benchmark-baseline/40-PATTERNS.md (locomo-scorer.cjs section: parseJudgeVerdict obj.label addition; buildLoCoMoJudgePrompt full text; judge call shape; category filter)
    - .planning/phases/40-competitive-benchmark-baseline/40-01-SUMMARY.md (observed category counts — confirms category 5 is the row to skip)
  </read_first>
  <action>
    Create `scripts/eval/locomo-scorer.cjs`. Copy the imports/arg-parse block, getCommit(), getEngineVersion(), and parseJudgeVerdict() verbatim from longmemeval-scorer.cjs; extend parseJudgeVerdict with the two branches `if (obj.label === 'CORRECT') return {label:1,parseOk:true}; if (obj.label === 'WRONG') return {label:0,parseOk:true};` (the mem0 prompt returns {"label":...}). Add `buildLoCoMoJudgePrompt(question, goldAnswer, hypothesis)` returning the verbatim mem0 Appendix A template (RESEARCH Item 1 / PATTERNS 322-335): single user message, "be generous — same topic = CORRECT", requests `{"label":"CORRECT"|"WRONG"}`. Set the judge call to `model: 'gpt-4o-mini'`, `max_tokens: 10`, `temperature: 0`, one user message, no system prompt. In the scoring loop, read the harness hypotheses JSONL (default `--in scripts/eval/results/locomo-hypotheses-PENDING.jsonl`), and for each row `if (row.category === 5) continue;` so adversarial rows never reach the judge — the Overall J denominator = count of judged (non-5) rows. Add `--mock`: when set, skip the OpenAI call and derive the verdict deterministically (e.g. label CORRECT iff the gold answer substring appears in the hypothesis) so CI can exercise the denominator + parse path with zero API. Default `--out scripts/eval/results/locomo-{commit}.json`.
  </action>
  <verify>
    <automated>npm run build && grep -q "gpt-4o-mini" scripts/eval/locomo-scorer.cjs && grep -q "max_tokens: 10\|max_tokens:10" scripts/eval/locomo-scorer.cjs && grep -q "temperature: 0\|temperature:0" scripts/eval/locomo-scorer.cjs && grep -q "label === 'CORRECT'\|label===\"CORRECT\"" scripts/eval/locomo-scorer.cjs && grep -q "category === 5\|category===5" scripts/eval/locomo-scorer.cjs && grep -q "buildLoCoMoJudgePrompt" scripts/eval/locomo-scorer.cjs && grep -q "be generous\|same topic" scripts/eval/locomo-scorer.cjs</automated>
  </verify>
  <done>locomo-scorer.cjs exists; judge is gpt-4o-mini/temp-0/max_tokens-10 with the verbatim mem0 Appendix A prompt; category-5 rows are skipped; parseJudgeVerdict handles the {"label":...} envelope; --mock scores without an API call.</done>
</task>

<task type="auto">
  <name>Task 2: D-10 v7.0 config snapshot into result meta</name>
  <read_first>
    - scripts/eval/locomo-scorer.cjs (Task 1 output)
    - scripts/eval/longmemeval-scorer.cjs (meta block 453-461 — the analog to extend)
    - .planning/phases/40-competitive-benchmark-baseline/40-PATTERNS.md (Config Snapshot Extension table: exact DEFAULT_CONFIG key→value mapping verified at src/lib/config.ts)
    - .planning/phases/40-competitive-benchmark-baseline/40-RESEARCH.md (D-10 Config Snapshot — Fields to Capture)
    - src/lib/config.ts (DEFAULT_CONFIG, line 712+ — confirm the live knob names/values; do NOT read src/model/config.ts, it does not exist)
  </read_first>
  <action>
    Extend the scorer's output `meta` to carry the D-10 v7.0 SUT snapshot. Require `DEFAULT_CONFIG` from `DIST + '/lib/config'` and build `meta.sut_config` from these exact keys: openaiEmbedModel, embeddingDimensions, claudeHeadlessExtractModel, claudeHeadlessJudgeModel, consolSkipThreshold, consolSkipThresholdAssistant, rankStrengthWeight, rankedRetrievalK, rankedRetrievalFloor, candidateK, entityAnchorK, typedAnchorPoolK, injectionTokenBudget, insightSurfacingEnabled, predicateGlossThreshold. Set top-level `meta.sut_commit = getCommit()`, `meta.engine_version = getEngineVersion()`, `meta.judge_model = 'gpt-4o-mini'`, `meta.eval = 'locomo-10'`, `meta.questions_total` (judged count), `meta.questions_adversarial_excluded` (category-5 count). Emit the envelope `{ meta, scores: { headline, by_category, rAtK }, per_question }` via `fs.writeFileSync(OUT, JSON.stringify(envelope, null, 2))`. The snapshot reads live DEFAULT_CONFIG values at scorer time so the official run (Plan 05) records the actual frozen v7.0 knobs — no hardcoded numbers.
  </action>
  <verify>
    <automated>npm run build && grep -q "sut_config" scripts/eval/locomo-scorer.cjs && grep -q "sut_commit" scripts/eval/locomo-scorer.cjs && grep -q "DEFAULT_CONFIG.openaiEmbedModel\|DEFAULT_CONFIG\['openaiEmbedModel'\]" scripts/eval/locomo-scorer.cjs && grep -q "predicateGlossThreshold" scripts/eval/locomo-scorer.cjs && grep -q "insightSurfacingEnabled" scripts/eval/locomo-scorer.cjs && grep -q "questions_adversarial_excluded" scripts/eval/locomo-scorer.cjs</automated>
  </verify>
  <done>Result meta carries sut_commit, engine_version, judge_model, and a sut_config block of all 15 frozen knobs read from live DEFAULT_CONFIG, plus the adversarial-excluded count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Un-skip the scorer unit tests (--mock, zero API)</name>
  <read_first>
    - tests/locomo-scorer.test.ts (Plan 01 scaffold — the it.skip'd denominator test to un-skip)
    - tests/eval-harness-smoke.test.ts (spawnSync .cjs pattern)
    - scripts/eval/locomo-scorer.cjs (Tasks 1-2 output)
    - scripts/eval/fixtures/locomo-mini.json (provides a known category-5 count for the denominator assertion)
  </read_first>
  <behavior>
    - Un-skip the category-5 denominator test: feed the scorer (--mock) a small hypotheses file derived from locomo-mini.json (N rows, K of category 5); assert the output meta.questions_total === N - K and meta.questions_adversarial_excluded === K.
    - Verdict-parse test: parseJudgeVerdict('{"label":"CORRECT"}') → 1; parseJudgeVerdict('{"label":"WRONG"}') → 0 (call via a --mock run or by requiring the exported helper if exported; otherwise assert via scored output on crafted rows).
    - Config-snapshot test: a --mock run writes a result whose meta.sut_config contains all 15 knob keys and meta.judge_model === 'gpt-4o-mini'.
  </behavior>
  <action>
    Edit tests/locomo-scorer.test.ts: convert the it.skip'd denominator test to active, driving scripts/eval/locomo-scorer.cjs via spawnSync with `--mock` and an isolated tmp `--in`/`--out` (mirror eval-harness-smoke.test.ts's spawnSync structure, status 0). Build the tmp hypotheses input from locomo-mini.json's qa rows (so the category-5 count is known). Assert the denominator math, the verdict-parse branches, and the config-snapshot keys. All assertions run under --mock — zero OpenAI calls.
  </action>
  <verify>
    <automated>npm run build && npx vitest run tests/locomo-scorer.test.ts</automated>
  </verify>
  <done>tests/locomo-scorer.test.ts is fully green under --mock: denominator = N − category5count, {"label":...} verdict parse, and the 15-key config snapshot are all asserted with zero API calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scorer → OpenAI API | gpt-4o-mini judge call hits OpenAI directly (direct-API $, OPENAI_API_KEY); --mock bypasses it |
| OPENAI_API_KEY → repo | the key must stay in sleep.env (gitignored), never read from or written to committed files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-06 | Information Disclosure | OPENAI_API_KEY leaking into committed code/results | mitigate | Key read from process.env (sourced from gitignored sleep.env), never written into the result JSON; meta carries model strings only, not secrets |
| T-40-07 | Tampering (metrics integrity) | wrong judge model/prompt silently producing a non-comparable J score (BENCH-03 no-inflated-metrics) | mitigate | Verbatim mem0 Appendix A prompt + gpt-4o-mini recorded in meta.judge_model; Task 3 asserts the recorded model; methodology note (Plan 04) states exactly which judge produced the number |
| T-40-08 | Denial of Service (budget) | scorer auto-running ~1,491 gpt-4o-mini calls without intent | accept | Scorer runs on already-produced hypotheses and is operator-invoked post-run (Plan 05); --mock is the CI path; cost is bounded + cheap (gpt-4o-mini), not a full-run gate |
| T-40-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (openai 6.44.0 already in package.json) — nothing to slopcheck |
</threat_model>

<verification>
- Source greps confirm gpt-4o-mini / temp 0 / max_tokens 10 / verbatim "be generous" prompt / category-5 skip / {"label":...} parse / sut_config snapshot.
- `npx vitest run tests/locomo-scorer.test.ts` green under --mock (zero API).
</verification>

<success_criteria>
- locomo-scorer.cjs judges with the verbatim mem0 Appendix A protocol (gpt-4o-mini), skips category 5, and stamps the full D-10 config snapshot into meta.
- Scorer unit tests green under --mock.
</success_criteria>

<output>
Create `.planning/phases/40-competitive-benchmark-baseline/40-03-SUMMARY.md` when done.
</output>
