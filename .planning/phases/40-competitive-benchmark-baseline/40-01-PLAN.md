---
phase: 40-competitive-benchmark-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - scripts/eval/locomo10.json
  - scripts/eval/fixtures/locomo-mini.json
  - tests/locomo-scorer.test.ts
  - tests/locomo-harness.test.ts
autonomous: true
requirements: [BENCH-01]
must_haves:
  truths:
    - "locomo10.json is present on disk and parses as a 10-element JSON array of conversation objects"
    - "Category code 5 (adversarial) is confirmed present by counting actual qa.category values"
    - "A trimmed 1-conversation dry-run fixture exists with ~5 QA pairs spanning at least one category-5 row"
    - "Unit-test scaffolds exist (red/skipped) that pin the category-5 filter and R@K session-hit logic before the harness/scorer are built"
    - "locomo10.json is gitignored and never committed (CC BY-NC 4.0)"
  artifacts:
    - path: "scripts/eval/locomo10.json"
      provides: "LoCoMo-10 dataset (acquired, not committed)"
      contains: "qa"
    - path: "scripts/eval/fixtures/locomo-mini.json"
      provides: "1-conversation dry-run fixture with ~5 QA pairs"
    - path: "tests/locomo-scorer.test.ts"
      provides: "Category-5 filter unit-test scaffold"
    - path: "tests/locomo-harness.test.ts"
      provides: "R@K session-hit unit-test scaffold + dataset-schema validation"
    - path: ".gitignore"
      provides: "locomo10.json exclusion"
      contains: "locomo10.json"
  key_links:
    - from: "scripts/eval/fixtures/locomo-mini.json"
      to: "scripts/eval/locomo10.json"
      via: "hand-trimmed slice of conversation[0]"
      pattern: "category"
---

<objective>
Wave 0 foundation for the LoCoMo benchmark: acquire the LoCoMo-10 dataset, verify its category codes empirically (RESEARCH Assumption A2 mitigation — wrong codes silently inflate the J score), build the dry-run fixture every downstream `--dry-run` depends on, and stand up the unit-test scaffolds that pin the two pieces of genuinely-new logic (category-5 filter + R@K session-hit). Nothing here makes a paid API call.

Purpose: Everything in Waves 2–3 needs `locomo10.json`, the dry-run fixture, and the verified category mapping. Acquiring + verifying first removes the single biggest correctness risk (Pitfall 1) before any harness code is written.
Output: `scripts/eval/locomo10.json` (gitignored), `scripts/eval/fixtures/locomo-mini.json`, two red/skipped test files, `.gitignore` updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-competitive-benchmark-baseline/40-CONTEXT.md
@.planning/phases/40-competitive-benchmark-baseline/40-RESEARCH.md
@.planning/phases/40-competitive-benchmark-baseline/40-PATTERNS.md

<interfaces>
<!-- Existing analog the test scaffolds clone. Executor should mirror these, not explore. -->

tests/eval-harness-smoke.test.ts — the exact analog for both new test files:
  - makeHarness() builds in-memory better-sqlite3 + initSchema + FakeClock + DEFAULT_CONFIG + EpisodicStore/SemanticStore/CandidateRetriever (zero API).
  - spawnSync(process.execPath, [harness.cjs, '--dry-run', '--out', tmp, '--eval', FIXTURE_PATH]) is how a .test.ts drives a .cjs harness in CI.
  - vitest include glob (vitest.config.ts): tests/**/*.test.ts — new test files MUST live in tests/ and end in .test.ts.

LoCoMo conversation object schema (RESEARCH Item 2):
  { sample_id, speaker_a, speaker_b,
    session_1: [{name, dia_id, text}, ...], session_1_date_time: "...",
    session_2: [...], ...,
    observation: { session_1: "...", ... },
    qa: [{ question, answer, evidence: ["D1:9", ...], category: <int> }] }

Category codes (RESEARCH Item 2 — ASSUMED, MUST be verified by counting in this plan):
  1=multi-hop, 2=temporal, 3=open-domain, 4=single-hop, 5=adversarial(excluded)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acquire LoCoMo-10 dataset, gitignore it, and verify category codes by counting</name>
  <read_first>
    - .planning/phases/40-competitive-benchmark-baseline/40-RESEARCH.md (Item 2: dataset location, acquisition command, schema; Pitfall 1: category-code verification)
    - .gitignore (current contents — line 6 already excludes scripts/eval/longmemeval-s.jsonl; mirror that pattern)
  </read_first>
  <action>
    Clone the canonical dataset to a temp dir and copy locomo10.json into scripts/eval/:
    `git clone https://github.com/snap-research/locomo.git /tmp/locomo` then copy `/tmp/locomo/data/locomo10.json` to `scripts/eval/locomo10.json` (~2.68 MB, 10 conversations).
    Append `scripts/eval/locomo10.json` to .gitignore directly under the existing `scripts/eval/longmemeval-s.jsonl` line (license is CC BY-NC 4.0 — must never be committed).
    Then VERIFY the category mapping empirically (RESEARCH Assumption A2 / Pitfall 1) by counting actual qa.category values across all 10 conversations — run a node one-liner that loads locomo10.json, flatMaps every qa.category, and prints a sorted `uniq -c` style count. Record the observed counts and the inferred non-adversarial total in the SUMMARY. Confirm: (a) category code 5 exists, (b) total non-category-5 questions is in the ~1,491 range (RESEARCH: >1,600 scoreable means adversarial leaked in — a red flag). Do NOT trust the assumed 1=multi-hop/4=single-hop labels blindly; record the raw counts so the scorer's category-5 skip is grounded in observed data.
  </action>
  <verify>
    <automated>test -f scripts/eval/locomo10.json && node -e "const d=require('./scripts/eval/locomo10.json'); if(!Array.isArray(d)||d.length!==10)process.exit(1); const cats=d.flatMap(c=>c.qa.map(q=>q.category)); const c5=cats.filter(x=>x===5).length; const scoreable=cats.length-c5; console.log('total',cats.length,'cat5',c5,'scoreable',scoreable); if(c5===0)process.exit(1); if(scoreable>1600)process.exit(1);"</automated>
  </verify>
  <done>locomo10.json present (10-element array), gitignored, category 5 confirmed present by count, non-adversarial scoreable total recorded and within the expected ~1,491 band (not >1,600).</done>
</task>

<task type="auto">
  <name>Task 2: Build the 1-conversation dry-run fixture (locomo-mini.json)</name>
  <read_first>
    - scripts/eval/fixtures/longmemeval-mini.jsonl (the fixture-format analog)
    - scripts/eval/locomo10.json (source — take conversation[0])
    - .planning/phases/40-competitive-benchmark-baseline/40-PATTERNS.md (fixture section: single conversation object, 3-5 QA pairs)
  </read_first>
  <action>
    Create `scripts/eval/fixtures/locomo-mini.json` as a single-element JSON array containing conversation[0] from locomo10.json, hand-trimmed so it stays small but exercises the full parsing path: keep `sample_id`, `speaker_a`, `speaker_b`, the first 2 sessions (`session_1` + `session_1_date_time`, `session_2` + `session_2_date_time`) with their turn arrays, and trim the `qa` array to exactly 5 entries that MUST include at least one `category: 5` (adversarial) row plus at least two non-5 categories — this is what lets the dry-run and the category-filter unit test both fire on real shape. Use `.json` (not `.jsonl`) per PATTERNS to avoid the array-vs-line ambiguity; the harness `--dry-run` loader (Plan 02) will `JSON.parse` it. Keep turn objects in their native `{name, dia_id, text}` shape (do NOT rewrite to {role, content}).
  </action>
  <verify>
    <automated>node -e "const d=require('./scripts/eval/fixtures/locomo-mini.json'); if(!Array.isArray(d)||d.length!==1)process.exit(1); const c=d[0]; if(!c.sample_id||!Array.isArray(c.qa)||c.qa.length!==5)process.exit(1); if(!c.qa.some(q=>q.category===5))process.exit(1); if(new Set(c.qa.map(q=>q.category)).size<3)process.exit(1); if(!c.session_1||!c.session_1_date_time)process.exit(1); const t=c.session_1[0]; if(!('name' in t)||!('text' in t))process.exit(1); console.log('fixture ok: 5 qa, has cat5, multi-category, native turn shape');"</automated>
  </verify>
  <done>locomo-mini.json is a 1-element array with conversation[0] trimmed to 2 sessions and exactly 5 QA pairs, at least one category 5, at least 3 distinct categories, turns in native {name,dia_id,text} shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stand up red/skipped unit-test scaffolds for category-5 filter and R@K session-hit</name>
  <read_first>
    - tests/eval-harness-smoke.test.ts (the exact structural analog: makeHarness, spawnSync .cjs --dry-run, fixture-load + schema assertions)
    - .planning/phases/40-competitive-benchmark-baseline/40-RESEARCH.md (Item 1 scoring mechanics: denominator = count(category != 5); Item 3 Option A: session-level R@K hit via qa.evidence dialog-IDs → session index)
    - .planning/phases/40-competitive-benchmark-baseline/40-VALIDATION.md (Per-Task Verification Map; Wave 0 unit-test requirement)
    - vitest.config.ts (include glob: tests/**/*.test.ts)
  </read_first>
  <behavior>
    - tests/locomo-scorer.test.ts:
      - Test (skipped via it.skip until Plan 03 ships locomo-scorer.cjs): scorer skips every row where qa.category === 5 — given a fixture with N total and K category-5 rows, the J-score denominator equals N - K.
      - Test (active now, no scorer dependency): locomo-mini.json fixture loads, is a 1-element array, and contains at least one category-5 row (pins the fixture contract Plan 03 depends on).
    - tests/locomo-harness.test.ts:
      - Test (active now): locomo10.json + locomo-mini.json parse with the documented schema (qa[].{question,answer,evidence[],category}; session_N arrays of {name,dia_id,text}; session_N_date_time strings).
      - Test (active now, pure logic — no harness dependency): the R@K session-hit predicate. Implement the evidence→session-index parse inline as the test's own reference (parse "D1:9" → session index 0 via parseInt(e.split(':')[0].replace('D',''))-1), build a hitSessions Set, and assert hit=true when a retrieved-session set intersects hitSessions and hit=false otherwise. This locks the hit math before Plan 02 wires it to real retrieval.
      - Test (skipped via it.skip until Plan 02 ships locomo-harness.cjs): spawnSync(harness.cjs, ['--dry-run','--eval', fixture]) exits 0.
  </behavior>
  <action>
    Create `tests/locomo-scorer.test.ts` and `tests/locomo-harness.test.ts` in the tests/ dir (vitest include glob). Mirror eval-harness-smoke.test.ts imports and structure. Mark tests that depend on not-yet-built scripts with `it.skip(...)` and a TODO comment naming the plan that un-skips them (Plan 02 for harness spawn, Plan 03 for scorer denominator) — these are the Nyquist Wave 0 scaffolds, intentionally red-until-implemented, not failing. The active tests (fixture/schema validation + the inline R@K predicate math) MUST pass now. Do NOT implement the scorer or harness here — only the test contracts.
  </action>
  <verify>
    <automated>npm run build && npx vitest run tests/locomo-scorer.test.ts tests/locomo-harness.test.ts 2>&1 | grep -Eq "(passed|skipped)" && npx vitest run tests/locomo-harness.test.ts tests/locomo-scorer.test.ts</automated>
  </verify>
  <done>Both test files exist in tests/, run green (active tests pass, scaffold tests are it.skip), the R@K session-hit predicate math is asserted inline, and the category-5 denominator contract is pinned as a skipped test naming Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external repo → local disk | `git clone snap-research/locomo` pulls third-party data onto disk; offline, no execution of cloned code |
| dataset license → git history | CC BY-NC 4.0 data must not enter the committed repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Information Disclosure | locomo10.json (CC BY-NC) committed to git | mitigate | Add `scripts/eval/locomo10.json` to .gitignore in Task 1 before any commit; verify with `git check-ignore` |
| T-40-02 | Tampering | cloned repo contains executable code run inadvertently | accept | Only `data/locomo10.json` (a data file) is copied out; no scripts from the clone are executed; /tmp clone discarded |
| T-40-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan (RESEARCH §Standard Stack: zero new deps); nothing to slopcheck |
</threat_model>

<verification>
- `node -e` category count confirms category 5 present and scoreable total ≤ 1600 (Pitfall 1 / A2).
- `git check-ignore scripts/eval/locomo10.json` returns the path (gitignored).
- `npx vitest run tests/locomo-harness.test.ts tests/locomo-scorer.test.ts` is green (active pass, scaffolds skipped).
</verification>

<success_criteria>
- locomo10.json acquired, gitignored, category-5 confirmed present by empirical count, scoreable total recorded.
- locomo-mini.json fixture exists (1 conversation, 5 QA, ≥1 category-5, native turn shape).
- Two unit-test scaffolds exist in tests/ with the category-5 denominator contract and the R@K session-hit math pinned.
</success_criteria>

<output>
Create `.planning/phases/40-competitive-benchmark-baseline/40-01-SUMMARY.md` when done. Record the observed per-category counts and the non-adversarial scoreable total (grounds Plan 03's category-5 skip).
</output>
