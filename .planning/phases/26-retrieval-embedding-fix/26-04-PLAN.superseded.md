---
phase: 26-retrieval-embedding-fix
plan: 04
type: execute
wave: 3
depends_on: [26-02, 26-03]
files_modified:
  - scripts/eval/results/replay-ku-baseline-small.json
  - scripts/eval/results/replay-ku-after-large.json
  - .planning/phases/26-retrieval-embedding-fix/26-VALIDATION.md
autonomous: false
requirements: [RETR-02]
user_setup:
  - service: openai
    why: "Paid embedding (re-embed) + GPT-4o KU scorer + judge consolidation for the replay/correctness runs"
    env_vars:
      - name: OPENAI_API_KEY
        source: "~/.config/recense/sleep.env (founder's existing DIRECT OpenAI key — rotate-flagged in STATE.md)"
must_haves:
  truths:
    - "The founder explicitly approves the paid run after seeing the $ estimate before any spend (autonomous:false gate, D-10)"
    - "The live DB is backed up before the re-embed (Phase 25 precedent, D-07)"
    - "A before (small@1536, via the harness --embed-model override) and after (large@1536, config default) replay-KU run use the IDENTICAL harness in one paid window, differing only in the embedder — a clean controlled comparison"
    - "RETR-02 passes only if: KU retrieval improves AND the judge engages on >=1 KU contradiction case AND EVAL-02 belief-correction holds >=84.6% (the three D-09 criteria)"
  artifacts:
    - path: "scripts/eval/results/replay-ku-after-large.json"
      provides: "Post-swap KU score + tombstone/contradict counts"
      contains: "knowledge-update"
    - path: ".planning/phases/26-retrieval-embedding-fix/26-VALIDATION.md"
      provides: "Before/after table + the three-criteria RETR-02 verdict + actual $ spent"
      contains: "84.6"
  key_links:
    - from: ".planning/phases/26-retrieval-embedding-fix/26-VALIDATION.md"
      to: "scripts/eval/replay-ku-harness.cjs"
      via: "records before/after harness output"
      pattern: "replay-ku"
---

<objective>
RETR-02 paid validation (founder-gated): back up the live DB, run the live re-embed
(`recense reembed --no-dry-run`) onto `text-embedding-3-large@1536`, then run the
extraction-replay KU harness (Plan 02) before (small@1536, pinned via the harness
`--embed-model` override) and after (large@1536, config default) in one approved paid
window, plus the EVAL-02 correctness harness, and render the three-criteria RETR-02 verdict.

The before/after is a CONTROLLED comparison at the IDENTICAL harness — both runs use
`scripts/eval/replay-ku-harness.cjs`, differing only in the embedder model. By this wave
DEFAULT_CONFIG already reads `text-embedding-3-large` (Plan 03 swapped it in an earlier
wave), so the BEFORE baseline pins small@1536 via the `--embed-model text-embedding-3-small`
override flag that Plan 02 (wave 1) built into the harness. The AFTER run uses the harness
with no override (reads large@1536 from DEFAULT_CONFIG). This keeps the comparison
methodologically clean — no second harness, no checked-out config, no cross-tool baseline.

The paid run requires EXPLICIT founder approval (D-10): this plan is `autonomous: false`
and pauses at a blocking checkpoint after quoting the $ estimate. Budget ~$3–5; total
remaining ~$14–15. The re-embed itself is pennies; the replay + correctness eval is the bulk.

Purpose: RETR-02 — validate the fix via cached extraction replay (no re-extraction):
improved KU retrieval WITH the reconsolidation judge engaging, no EVAL-02 regression.
Output: before/after result JSONs + `26-VALIDATION.md` verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-retrieval-embedding-fix/26-CONTEXT.md
@.planning/phases/26-retrieval-embedding-fix/26-PATTERNS.md
@.planning/phases/26-retrieval-embedding-fix/26-DIAGNOSIS.md
@.planning/phases/26-retrieval-embedding-fix/26-02-SUMMARY.md
@.planning/phases/26-retrieval-embedding-fix/26-03-SUMMARY.md

<interfaces>
<!-- Built in prior plans; this plan only RUNS them. -->

scripts/eval/replay-ku-harness.cjs (Plan 02) —
  AFTER (large@1536, config default): `node scripts/eval/replay-ku-harness.cjs --out <path>`
  BEFORE (small@1536, pinned): `node scripts/eval/replay-ku-harness.cjs --embed-model text-embedding-3-small --out <path>`
  The default-off `--embed-model <model>` override (built in Plan 02, wave 1) constructs the
  embedder with the given model instead of reading openaiEmbedModel from DEFAULT_CONFIG; with
  no flag it reads DEFAULT_CONFIG (large@1536 by this wave). Optional `--embed-dims <n>` too.
  Outputs: { meta:{embedder_model,...}, scores:{knowledge-update}, tombstones, contradicts, per_case }

src/adapter/reembed-cli.ts (Plan 03) — `recense reembed` dry-run default; `recense reembed --no-dry-run` real run
  Dry-run reports total/tombstoned counts + current-vs-target embedding_model with no writes.

scripts/eval/correctness-harness.cjs (EVAL-02, existing) —
  `npm run build && OPENAI_API_KEY=... node scripts/eval/correctness-harness.cjs --out <path>`
  `--dry-run` runs ADD-only baseline (zero API). Belief-correction baseline to hold: >=84.6% (D-09).

Live DB: ~/.config/recense/recense.db (resolveDbPath default; tray app reads RECENSE_DB from sleep.env).
DB backup precedent (Phase 25): recense.db.bak-pre-dedup-260618 (D-07 — back up before bulk derived rewrite).
OPENAI_API_KEY: from ~/.config/recense/sleep.env (env only, never logged — T-02-KEY).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Founder approval for the paid validation run</name>
  <action>Pause for explicit founder approval before spending any API budget. Present the $ estimate (computed from the `recense reembed` dry-run node count) and confirm the rotate-flagged OPENAI_API_KEY in sleep.env is valid. Do not proceed to Task 1 until "approve" is received.</action>
  <decision>Approve the paid Phase 26 validation run (re-embed + replay KU before/after + EVAL-02)?</decision>
  <context>
    Diagnosis (Plan 01) returned GO/model-bound; the swap + reembed CLI (Plan 03) and the
    replay-KU harness (Plan 02) are built and dry-run-clean. The next step spends API budget.
    Quote the live estimate BEFORE this checkpoint (do the math from the dry-run counts):
    - Re-embed: ~3.5k live nodes + tombstoned, large@1536, OpenAI embeddings — pennies (quote actual node count from `recense reembed` dry-run).
    - Replay KU before+after: 2 × 20 KU cases × (haiku answer + GPT-4o scorer + judge consolidation).
    - EVAL-02 correctness harness: per the cases file.
    Total expected ~$3–5 of ~$14–15 remaining. NOTE STATE.md: OPENAI_API_KEY was exposed 2026-06-18 and is rotate-flagged — confirm the key in sleep.env is the rotated/valid one before spending.
  </context>
  <options>
    <option id="approve">
      <name>Approve — proceed with the paid run</name>
      <pros>Validates RETR-02; the fix either passes the three-criteria gate or fails cheaply within budget</pros>
      <cons>Spends ~$3–5 of the remaining ~$14–15</cons>
    </option>
    <option id="defer">
      <name>Defer — hold the paid run</name>
      <pros>Preserves budget</pros>
      <cons>RETR-02 stays unvalidated; the swap ships unproven</cons>
    </option>
  </options>
  <resume-signal>Reply "approve" (and confirm the key is rotated/valid) or "defer".</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Back up the live DB, run the live re-embed, run before/after replay KU + EVAL-02</name>
  <read_first>
    - src/adapter/reembed-cli.ts (dry-run vs --no-dry-run behavior, lock lifecycle)
    - scripts/eval/replay-ku-harness.cjs (CLI flags incl. the --embed-model override, --out, output shape)
    - scripts/eval/correctness-harness.cjs (EVAL-02 run command, --out, the 84.6% baseline meaning)
    - .planning/STATE.md (DB path canonical ~/.config/recense/recense.db; OPENAI_API_KEY rotate flag; Phase 25 backup precedent)
    - .planning/phases/26-retrieval-embedding-fix/26-CONTEXT.md (D-07 backup; D-09 gate; D-10 budget discipline)
  </read_first>
  <action>
    Only after "approve". Source the key: `set -a; . ~/.config/recense/sleep.env; set +a`.
    In order:
    (1) BACK UP the live DB (D-07): copy `~/.config/recense/recense.db` to
    `recense.db.bak-pre-reembed-260618` (mirror Phase 25's bak-pre-dedup naming). Verify
    the backup exists and is non-empty before any mutation.
    (2) BEFORE run (small@1536) — PRIMARY controlled-baseline path. By this wave Plan 03
    has already swapped DEFAULT_CONFIG to large@1536, so capture the true small@1536
    before-number by running the SAME replay harness with the embedder pinned via the
    Plan-02 override flag:
    `node scripts/eval/replay-ku-harness.cjs --embed-model text-embedding-3-small --out scripts/eval/results/replay-ku-baseline-small.json`.
    Confirm the output `meta.embedder_model` records `text-embedding-3-small` (the override
    took effect). This BEFORE run is the controlled baseline — it uses the identical harness
    as the AFTER run, differing only in the embedder, which is the crux of a clean RETR-02
    validation. (Run it BEFORE the live re-embed in step 3, but the override makes the order
    irrelevant for the baseline since the harness uses scratch DBs, not the live DB.)
    LAST-RESORT FALLBACK (only if the `--embed-model` override path errors and cannot be
    fixed quickly): fall back to the cached `n20-clean-score.json` 90.0% headline as the
    documented small@1536 reference, and EXPLICITLY flag in the SUMMARY + 26-VALIDATION.md
    that the before/after is cross-harness and therefore methodologically uncontrolled
    (weaker evidence). Do NOT take this path if the override works.
    (3) Run the live re-embed: `recense reembed` (dry-run) first — log total/tombstoned
    counts + current-vs-target model. Then `recense reembed --no-dry-run` — re-embeds all
    nodes (incl. tombstoned) to large@1536, stamps `embedding_model=text-embedding-3-large:1536`.
    (4) AFTER run (large@1536) — same harness, NO override (reads large@1536 from
    DEFAULT_CONFIG): `node scripts/eval/replay-ku-harness.cjs --out
    scripts/eval/results/replay-ku-after-large.json`. Confirm `meta.embedder_model` records
    `text-embedding-3-large`.
    (5) EVAL-02: `npm run build && node scripts/eval/correctness-harness.cjs --out
    scripts/eval/results/correctness-after-large.json` (belief-correction must hold >=84.6%).
    Surface burn live (quote running $). Keys env-only, never logged. Never delete a node.
  </action>
  <verify>
    <automated>test -s ~/.config/recense/recense.db.bak-pre-reembed-260618 && test -f scripts/eval/results/replay-ku-baseline-small.json && test -f scripts/eval/results/replay-ku-after-large.json</automated>
  </verify>
  <acceptance_criteria>
    - `recense.db.bak-pre-reembed-260618` exists and is non-empty BEFORE the re-embed (D-07).
    - `scripts/eval/results/replay-ku-baseline-small.json` exists with `meta.embedder_model` == `text-embedding-3-small` (the controlled BEFORE baseline via the override; the cross-harness 90.0% fallback was NOT used unless explicitly flagged in the SUMMARY as a last resort).
    - After `recense reembed --no-dry-run`, `recense reembed` dry-run reports current model == target (`text-embedding-3-large:1536`) — re-embed completed and stamped.
    - `scripts/eval/results/replay-ku-after-large.json` exists with `meta.embedder_model` == `text-embedding-3-large`, a `knowledge-update` score, and tombstone/contradict counts.
    - `scripts/eval/results/correctness-after-large.json` exists with a belief-correction score.
    - `PRAGMA foreign_key_check` on the live DB returns empty after the re-embed (graph untouched; no node deleted).
    - No OPENAI_API_KEY string in any results JSON or log committed.
  </acceptance_criteria>
  <done>DB backed up; controlled small@1536 BEFORE captured via the override at the identical harness; live re-embed to large@1536 done + stamped; large@1536 AFTER + EVAL-02 results captured within budget.</done>
</task>

<task type="auto">
  <name>Task 2: Render the three-criteria RETR-02 verdict</name>
  <read_first>
    - scripts/eval/results/replay-ku-baseline-small.json + replay-ku-after-large.json (from Task 1)
    - scripts/eval/results/correctness-after-large.json (EVAL-02)
    - .planning/phases/26-retrieval-embedding-fix/26-CONTEXT.md (D-09 — the three hard criteria; D-05 fallback to 3072 only if 1536-large misses)
    - ~/.recense-eval-cache/eval01-n20-2026-06-16/n20-clean-score.json (90.0% reference — only the fallback baseline, see below)
  </read_first>
  <action>
    Write `.planning/phases/26-retrieval-embedding-fix/26-VALIDATION.md` with: a before
    (small@1536) vs after (large@1536) table for KU score + tombstone count + contradict
    count, sourced from the two replay-ku result JSONs at the IDENTICAL harness (the
    controlled comparison); the EVAL-02 belief-correction number; the actual $ spent.
    State the comparison method explicitly: both runs used `replay-ku-harness.cjs`,
    before via `--embed-model text-embedding-3-small`, after via config default
    (large@1536). If — and only if — Task 1 had to use the last-resort cached 90.0%
    fallback, label the before-number as a cross-harness reference and note the comparison
    is uncontrolled (weaker evidence). Then the RETR-02 verdict against the THREE hard
    criteria (D-09): (a) KU retrieval improved; (b) the reconsolidation judge engaged on
    >=1 KU contradiction case (after-tombstones > 0 OR after-contradicts > 0 — a number
    bump WITHOUT judge engagement does NOT pass); (c) EVAL-02 belief-correction held
    >=84.6%. State PASS only if all three hold; else FAIL with which criterion missed. If
    FAIL specifically because the KU bump is insufficient at large@1536, note the
    documented fallback lever (native 3072 dims, D-05 / Claude's Discretion) as the NEXT
    step — do not auto-apply it. Quote actual $ spent vs the ~$3–5 budget. Never write any
    secret into the note.
  </action>
  <verify>
    <automated>test -f .planning/phases/26-retrieval-embedding-fix/26-VALIDATION.md && grep -q "84.6" .planning/phases/26-retrieval-embedding-fix/26-VALIDATION.md && grep -Eq "PASS|FAIL" .planning/phases/26-retrieval-embedding-fix/26-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `26-VALIDATION.md` has a before/after table with KU score + tombstone + contradict columns and the EVAL-02 number.
    - The note states the comparison method (identical harness; before via --embed-model override, after via config default) and, if the cached-90.0% fallback was used instead, explicitly flags the comparison as cross-harness/uncontrolled.
    - The verdict checks all three D-09 criteria explicitly and states PASS only if all three hold.
    - Judge-engagement is evaluated as a hard gate (tombstones/contradicts > 0), not inferred from the KU number alone.
    - Actual $ spent is recorded and is within the ~$3–5 budget (or the overage is flagged).
    - No secret string appears in the note.
  </acceptance_criteria>
  <done>RETR-02 verdict rendered against the three hard criteria with controlled before/after evidence and actual spend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paid run → OpenAI API | OPENAI_API_KEY (rotate-flagged) crosses to OpenAI; env-only, never logged/committed (T-02-KEY) |
| reembed --no-dry-run → live DB | bulk derived rewrite of the live recense.db; backed up first (D-07), gated behind founder approval + the CLI's --no-dry-run + lock |
| harness/correctness runs ↔ live DB | replay/correctness use scratch DBs (T-14-DB); only `recense reembed` touches the live DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-KEY | Information Disclosure | OPENAI_API_KEY (paid run) | mitigate | Sourced from sleep.env env-only; never logged or written to any results JSON/note; rotate-flag confirmed at the approval checkpoint before spend |
| T-26-09 | Tampering | live recense.db (live re-embed) | mitigate | DB backed up to bak-pre-reembed-260618 BEFORE mutation (D-07); graph values/edges untouched (only derived embeddings); foreign_key_check empty after; no node deleted |
| T-26-10 | Denial of Service | budget overspend | mitigate | autonomous:false approval gate with $ estimate before any spend; burn surfaced live; ~$3–5 of ~$14–15 cap |
| T-26-11 | Repudiation | unverified "it improved" claim | mitigate | Three-criteria verdict (incl. judge-engagement hard gate) recorded in 26-VALIDATION.md against a CONTROLLED before/after at the identical harness (before via --embed-model override, after via config default) — a number bump alone, and an uncontrolled cross-harness comparison, cannot pass cleanly |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs (runs existing CLIs/harnesses); slopcheck N/A — re-affirm at execute time if any install appears |
</threat_model>

<verification>
- Founder approval recorded before any spend (autonomous:false blocking checkpoint).
- Live DB backed up before the re-embed; foreign_key_check empty after; no node deleted.
- Controlled small@1536 BEFORE captured via the harness --embed-model override; re-embed completed + stamped large@1536; large@1536 AFTER + EVAL-02 results captured.
- 26-VALIDATION.md renders the three-criteria RETR-02 verdict against the controlled before/after with judge-engagement as a hard gate, plus actual $ spent.
- No secret committed.
</verification>

<success_criteria>
RETR-02 is validated (or cleanly failed) via the cached extraction-replay harness with a
CONTROLLED before/after at the identical harness (before small@1536 via the --embed-model
override, after large@1536 via config default): the verdict in 26-VALIDATION.md confirms
improved KU retrieval AND reconsolidation-judge engagement on >=1 KU contradiction case AND
EVAL-02 belief-correction >=84.6% — all within the ~$3–5 budget and behind an explicit
founder approval gate.
</success_criteria>

<output>
Create `.planning/phases/26-retrieval-embedding-fix/26-04-SUMMARY.md` when done.
</output>
</output>
