---
phase: 26-retrieval-embedding-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/eval/diagnose-cosine-ceiling.cjs
  - .planning/phases/26-retrieval-embedding-fix/26-DIAGNOSIS.md
autonomous: true
requirements: [RETR-01]
must_haves:
  truths:
    - "A $0 spot-check measures cosine on known KU query/cue pairs under large@1536 vs small@1536, with no instruction-prefix on the OpenAI path (D-01, D-03)"
    - "The diagnosis note records whether the weakness is model-bound (vs threshold/prefix bound) (D-01)"
    - "The decision to proceed with the model swap is grounded in measured numbers, not assumption (D-01)"
  artifacts:
    - path: "scripts/eval/diagnose-cosine-ceiling.cjs"
      provides: "Cheap diagnosis spot-check: large@1536 vs small@1536 cosine on KU pairs"
      min_lines: 40
    - path: ".planning/phases/26-retrieval-embedding-fix/26-DIAGNOSIS.md"
      provides: "Recorded root cause + go/no-go for the swap"
      contains: "model-bound"
  key_links:
    - from: "scripts/eval/diagnose-cosine-ceiling.cjs"
      to: "OpenAIEmbedder"
      via: "import from dist/src/model/embedder"
      pattern: "OpenAIEmbedder"
---

<objective>
RETR-01: confirm the sub-0.7 cosine retrieval weakness is **model-bound** (a
`text-embedding-3-small` ceiling) — not threshold-bound or prefix-bound — with a
cheap, near-$0 spot-check BEFORE committing to the re-embed + paid replay. Record
the confirmed root cause and a go/no-go decision in a short diagnosis note.

Purpose: RETR-01 demands a diagnosed root cause, not just a fix. This is the
gate that justifies (or rejects) the model swap before any budget is spent.
Output: `scripts/eval/diagnose-cosine-ceiling.cjs` (the spot-check) and
`26-DIAGNOSIS.md` (the recorded finding).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-retrieval-embedding-fix/26-CONTEXT.md
@.planning/phases/26-retrieval-embedding-fix/26-PATTERNS.md

<interfaces>
<!-- OpenAIEmbedder accepts (model, dimensions) and exposes embed(texts) -->
<!-- Confirmed live: src/lib/config.ts:623-624 — openaiEmbedModel:'text-embedding-3-small', embeddingDimensions:1536 -->

From src/model/embedder.ts:
  export class OpenAIEmbedder {
    constructor(model: string, dimensions: number);
    embed(texts: string[]): Promise<Float32Array[]>;
  }
  export const EMBEDDER_INPUT_MAX_CHARS = 24_000;
  export const EMBEDDER_MAX_BATCH = 2048;

Cosine helper: src/lib/* exports cosineSimF32(a: Float32Array, b: Float32Array): number
(used by src/retrieval/topk.ts — grep for the exact import path before use).

Cached KU material (no re-extraction): ~/.recense-eval-cache/eval01-n20-2026-06-16/
  - eval20-ku.jsonl — 20 KU cases (question + gold_answer)
  - n20-attribution.jsonl — per-case { question_id, claims:[{event_type,value,episode_id}], nodes:[{id,type,value,tombstoned,prev_value,s,c}], gold_answer }
  The contradicting count-claim pairs live in claims[]/nodes[] (prev_value vs value).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnosis spot-check script — large@1536 vs small@1536 cosine on KU pairs</name>
  <read_first>
    - src/model/embedder.ts (OpenAIEmbedder signature, EMBEDDER_MAX_BATCH, input char cap)
    - src/retrieval/topk.ts (cosineSimF32 import path + how the 0.7 floor / rankedRetrievalFloor is applied)
    - src/adapter/snapshot-cli.ts (how OPENAI_API_KEY-backed embed is constructed via DefaultModelProvider — env-only key, T-05-SNAP-K)
    - ~/.recense-eval-cache/eval01-n20-2026-06-16/n20-attribution.jsonl (claim/node shape — first line)
    - scripts/eval/calibrate-snapshot-threshold.cjs (existing cjs eval-script harness conventions: require ../../dist, arg parser)
  </read_first>
  <action>
    Create `scripts/eval/diagnose-cosine-ceiling.cjs` as a standalone Node cjs
    script (mirror the `require('../../dist/src/...')` + `arg(k,d)` convention from
    calibrate-snapshot-threshold.cjs; run after `npm run build`). It must:
    (1) Load the KU pairs from the cache: read `n20-attribution.jsonl`, and for each
    KU case extract the query text (the case `question`) and the contradicting
    count-claim CUE text (the node `value` and its `prev_value` — the pair the judge
    should cluster). Build a small set of (query, cue) pairs — pick the subset where
    the gold answer reflects a value-update (prev_value present), since those are the
    cases the judge must engage.
    (2) Embed every query and every cue text TWICE: once with
    `new OpenAIEmbedder('text-embedding-3-small', 1536)` and once with
    `new OpenAIEmbedder('text-embedding-3-large', 1536)`. Key is read from
    `process.env.OPENAI_API_KEY` (env only — never log it, never write it to the note).
    (3) Compute `cosineSimF32(queryVec, cueVec)` for each pair under each model.
    (4) Print a table per pair: small@1536 cosine, large@1536 cosine, and the
    `rankedRetrievalFloor` (0.3) and the 0.7 candidate band for reference. Print
    aggregate: how many pairs clear 0.7 under small vs under large.
    Add a `--dry-run` flag that parses the cache and prints the pair count with ZERO
    API calls (so structure is verifiable at $0). Cost of the real spot-check is a
    handful of short embeddings (well under $0.01) — this is the cheap decisive test
    per Claude's Discretion in CONTEXT (D-01: confirm root cause cheaply before any
    paid run). Do NOT add a query-instruction prefix to either embedder (D-03: OpenAI
    models are symmetric — prefix is Qwen3-local-only, out of scope). Do NOT touch any
    production config or DB.
  </action>
  <verify>
    <automated>npm run build && node scripts/eval/diagnose-cosine-ceiling.cjs --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/eval/diagnose-cosine-ceiling.cjs --dry-run` exits 0 and prints a pair count > 0 with zero API calls (D-01 cheap diagnosis).
    - The script reads OPENAI_API_KEY from env only; `grep -n "OPENAI_API_KEY" scripts/eval/diagnose-cosine-ceiling.cjs` shows it read from `process.env`, never written to a file or logged.
    - `grep -c "text-embedding-3-large" scripts/eval/diagnose-cosine-ceiling.cjs` ≥ 1 AND `grep -c "text-embedding-3-small" scripts/eval/diagnose-cosine-ceiling.cjs` ≥ 1 (both models compared).
    - No prefix string is prepended to query texts before embedding (no instruction-prefix logic) (D-03).
  </acceptance_criteria>
  <done>Spot-check script exists, dry-run passes at $0, compares both models, no prod side effects.</done>
</task>

<task type="auto">
  <name>Task 2: Run the spot-check and record the diagnosis + go/no-go</name>
  <read_first>
    - scripts/eval/diagnose-cosine-ceiling.cjs (from Task 1)
    - .planning/phases/26-retrieval-embedding-fix/26-CONTEXT.md (D-01 root-cause expectation: model-bound; D-04 single lever first)
    - Memory note grounding: the prior finding "Q-cues never clear 0.7 with text-embedding-3-small"
  </read_first>
  <action>
    Run the real spot-check (`node scripts/eval/diagnose-cosine-ceiling.cjs`, with
    OPENAI_API_KEY exported from `~/.config/recense/sleep.env`). Capture the cosine
    numbers. Write `.planning/phases/26-retrieval-embedding-fix/26-DIAGNOSIS.md`
    recording: the per-pair small@1536 vs large@1536 cosines, how many pairs clear
    the 0.7 candidate band under each model, and the explicit classification — state
    `model-bound` if large@1536 lifts pairs over 0.7 that small@1536 left below
    (D-01 expects this); state `threshold-bound` or `prefix-bound` otherwise. End with
    a one-line GO/NO-GO: GO to the model swap (D-02/D-04) only if model-bound. If the
    finding is NOT model-bound, the note must say so and STOP — do not proceed to the
    swap (that would invalidate D-04's single-lever-first discipline). Quote the
    actual $ spent (expected < $0.01). Never write the API key or any secret into the note.
  </action>
  <verify>
    <automated>test -f .planning/phases/26-retrieval-embedding-fix/26-DIAGNOSIS.md && grep -Eq "model-bound|threshold-bound|prefix-bound" .planning/phases/26-retrieval-embedding-fix/26-DIAGNOSIS.md && grep -Eq "GO|NO-GO" .planning/phases/26-retrieval-embedding-fix/26-DIAGNOSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - `26-DIAGNOSIS.md` exists and contains a per-pair cosine table with both small@1536 and large@1536 columns.
    - The note contains exactly one classification token (`model-bound` / `threshold-bound` / `prefix-bound`) and an explicit GO or NO-GO line (D-01).
    - `grep -i "sk-\|OPENAI_API_KEY=" 26-DIAGNOSIS.md` returns nothing (no secret leaked into the note).
    - The recorded $ spent is < $0.01 (this is the $0 diagnosis step, not the paid run).
  </acceptance_criteria>
  <done>Root cause recorded with measured numbers; GO/NO-GO decision is grounded, not assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diagnosis script → OpenAI API | OPENAI_API_KEY crosses to a third party; must come from env, never be logged or written to the note |
| cached eval JSONL → script | local trusted file; parsed as data only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | OPENAI_API_KEY in diagnose-cosine-ceiling.cjs | mitigate | Key read from `process.env.OPENAI_API_KEY` only (T-02-KEY survives); never logged, never written to 26-DIAGNOSIS.md; grep gate in acceptance criteria |
| T-26-02 | Tampering | production config / live DB | accept | Script is read-only re: prod — touches no config and no recense.db; pure local compute on cached JSONL |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (uses existing better-sqlite3 + OpenAIEmbedder); slopcheck N/A — re-affirm at execute time if any install appears |
</threat_model>

<verification>
- `npm run build` clean.
- Dry-run of the spot-check exits 0 with a positive pair count and zero API calls.
- 26-DIAGNOSIS.md exists with a measured cosine table, a single classification token, and a GO/NO-GO line.
- No secret string appears in the diagnosis note.
</verification>

<success_criteria>
RETR-01 root cause is diagnosed with measured large@1536 vs small@1536 cosine on
KU query/cue pairs; the note classifies the weakness and gates the model swap on a
GO decision — all at < $0.01.
</success_criteria>

<output>
Create `.planning/phases/26-retrieval-embedding-fix/26-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
