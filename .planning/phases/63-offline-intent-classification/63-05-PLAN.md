---
phase: 63-offline-intent-classification
plan: 05
type: execute
wave: 3
depends_on: [63-01, 63-02, 63-04]
files_modified:
  - tests/intent-conservation.test.ts
autonomous: true
requirements: [CLASSIFY-01, CLASSIFY-02, CLASSIFY-04]

must_haves:
  truths:
    - "The status vocabulary is identical at every stage: prompt text, JSON schema enum, parser coercer, and ClaimDecision type"
    - "Adding a fifth status to the code vocabulary without updating the prompt fails the suite"
    - "A consolidation pass over a gmail episode WITH classification produces a byte-identical database to the same pass WITHOUT classification"
    - "No database table, column, or index anywhere mentions the classification fields"
  artifacts:
    - path: "tests/intent-conservation.test.ts"
      provides: "cross-stage vocabulary parity + zero-DB-delta inertness conservation"
      contains: "INTENT_STATUSES"
  key_links:
    - from: "src/model/claim-extractor.ts:INTENT_STATUSES"
      to: "src/source/extraction-prompts.ts:promptForSource('gmail')"
      via: "every vocabulary member must appear verbatim in the gmail prompt"
      pattern: "INTENT_STATUSES"
    - from: "src/model/claim-extractor.ts:CLAIM_ARRAY_SCHEMA"
      to: "src/model/claim-extractor.ts:INTENT_STATUSES"
      via: "schema enum array is derived from the same constant"
      pattern: "intent_status"
---

<objective>
Lock the two cross-stage properties that per-stage tests structurally cannot catch: (1) the four-state vocabulary is the SAME at every hop from prompt to typed field, and (2) classification is genuinely inert — a full consolidation pass carrying classification leaves a database identical to one without it.

Purpose: Phase 63 is a multi-stage pipeline (prompt → model output → JSON schema → parser → `ClaimDecision`). Each stage is unit-tested by plans 01, 02, and 04, and each will keep passing while the stages silently disagree. This plan tests the seams between them.
Output: one new test file. No source changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-offline-intent-classification/63-CONTEXT.md
@.planning/phases/63-offline-intent-classification/63-01-SUMMARY.md
@.planning/phases/63-offline-intent-classification/63-02-SUMMARY.md
@.planning/phases/63-offline-intent-classification/63-04-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Stages the conservation test spans, as landed by plans 01/02/04. -->

Stage 1 — prompt: `promptForSource('gmail')` from `src/source/extraction-prompts.ts` (contains `GMAIL_INTENT_CLASSIFICATION_BLOCK`).
Stage 2 — constrained-decode schema: `CLAIM_ARRAY_SCHEMA.items.properties.intent_status.enum` from `src/model/claim-extractor.ts`.
Stage 3 — parser: `parseClaimsFromArray` + `toIntentStatus` / `toIntentConfidence`.
Stage 4 — in-memory decision: `ClaimDecision.claimIntentStatus` / `claimIntentEntity` / `claimIntentConfidence` in `src/consolidation/consolidator.ts` (not exported; observe via the test-local spy approach chosen in plan 63-04).
Sink — the database: `initSchema(db)` in `src/db/schema.ts`. Classification must produce ZERO delta here (D-08).

Shared vocabulary constants: `INTENT_STATUSES` (`applied|interviewing|rejected|offer`) and `INTENT_CONFIDENCES` (`high|medium|low`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Cross-stage vocabulary parity</name>
  <files>tests/intent-conservation.test.ts</files>
  <read_first>
    - src/model/claim-extractor.ts — `INTENT_STATUSES`, `INTENT_CONFIDENCES`, `CLAIM_ARRAY_SCHEMA`, `toIntentStatus`, `toIntentConfidence`
    - src/source/extraction-prompts.ts — the authored `GMAIL_INTENT_CLASSIFICATION_BLOCK` wording
    - .planning/phases/63-offline-intent-classification/63-02-SUMMARY.md — the confidence rubric as authored
    - tests/src-import-boundary.test.ts — the non-vacuousness discipline to apply here too
  </read_first>
  <action>
Create `tests/intent-conservation.test.ts` with a `describe('vocabulary parity across the classification pipeline')` block.

Drive every assertion off the exported constants — never off a literal array retyped in the test. That is the whole point: a test that hardcodes `['applied','interviewing','rejected','offer']` would pass while the code vocabulary drifts.

Assertions:
- **Prompt covers the schema:** for every member of `INTENT_STATUSES`, `promptForSource('gmail')` contains that literal. Same for every member of `INTENT_CONFIDENCES`. Run this under both `RECENSE_ENABLE_EPISODIC_EMAIL` unset and `'on'`.
- **Schema equals the constant:** `CLAIM_ARRAY_SCHEMA.items.properties.intent_status.enum` sorted deep-equals `[...INTENT_STATUSES]` sorted; same for `intent_confidence` vs `INTENT_CONFIDENCES`.
- **Parser accepts exactly the constant:** every member of `INTENT_STATUSES` round-trips through `toIntentStatus`; a generated set of near-miss values (wrong case, trailing space, plural, `'other'`, `'unknown'`, `'pending'`) all return `undefined`.
- **Vocabulary is closed at four / three:** assert `INTENT_STATUSES.size === 4` and `INTENT_CONFIDENCES.size === 3` with a comment naming CLASSIFY-04 — this is the tripwire that forces a future vocabulary expansion to be a deliberate, reviewed change rather than a silent one.
- **Non-vacuousness:** assert that a synthetic status value NOT in the constant (e.g. `'ghosted'`) is absent from the gmail prompt, proving the "prompt contains every member" assertion is discriminating rather than trivially satisfied by a prompt that happens to contain lots of words.
  </action>
  <verify>
    <automated>npx vitest run tests/intent-conservation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every parity assertion iterates `INTENT_STATUSES` / `INTENT_CONFIDENCES` imported from `src/model/claim-extractor` — `grep -c "INTENT_STATUSES" tests/intent-conservation.test.ts` is at least 4 and no four-element status literal array appears in the file
    - A test asserts `INTENT_STATUSES.size === 4` and `INTENT_CONFIDENCES.size === 3`
    - A test asserts the schema enum deep-equals the constant for both fields
    - A test asserts `'ghosted'` is absent from `promptForSource('gmail')`
    - `npx vitest run tests/intent-conservation.test.ts` passes
  </acceptance_criteria>
  <done>Adding a status to the code vocabulary without updating the prompt (or vice versa) fails the suite.</done>
</task>

<task type="auto">
  <name>Task 2: Zero-DB-delta inertness conservation (D-08)</name>
  <files>tests/intent-conservation.test.ts</files>
  <read_first>
    - tests/consolidation-intent.test.ts — the harness and scripted-provider setup landed by plan 63-04; reuse its shape
    - tests/consolidation-temporal.test.ts — for contrast, the temporal path DOES write `node_temporal`; this test asserts the intent path writes nothing analogous
    - src/db/schema.ts — the table set `initSchema` creates, so the snapshot helper can enumerate it
    - .planning/phases/63-offline-intent-classification/63-CONTEXT.md (D-08)
  </read_first>
  <action>
Add a `describe('classification is inert — zero database delta (D-08)')` block to the same file.

Write a helper `snapshotDb(db)` that enumerates every user table via `SELECT name FROM sqlite_master WHERE type='table' AND name NOT LIKE 'sqlite_%'` and, for each, records `COUNT(*)` plus the sorted list of a stable identifying column where one exists (`value` for `node`, `content` for `episode`; fall back to count-only for tables without an obvious stable column). Return a plain object suitable for `toEqual`.

Run two full `Consolidator.consolidate()` passes over two independently constructed in-memory databases seeded identically (same `FakeClock`, same seeded nodes, same gmail episode content, same synthetic embed fn, same scripted judge verdicts). The ONLY difference: run A's scripted extraction JSON carries `intent_status` / `intent_entity` / `intent_confidence` on its claims; run B's does not. Assert `snapshotDb(dbA)` deep-equals `snapshotDb(dbB)`.

Add a second, narrower assertion that does not depend on snapshot fidelity: after run A, assert that no table in `sqlite_master` has a column whose name contains `intent` — enumerate columns via `PRAGMA table_info(<table>)` for every table, plus assert `sqlite_master` SQL text contains no `intent` substring (covers indexes and views too).

Add a comment at the top of the block stating what this test will need to become in Phase 66: when `action_proposal` lands, this test must be updated deliberately (the delta becomes expected and bounded to that one table), and a silent update is a review-blocking change.
  </action>
  <verify>
    <automated>npx vitest run tests/intent-conservation.test.ts tests/consolidation-intent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts `snapshotDb(dbA)` deep-equals `snapshotDb(dbB)` across two full consolidation passes differing only in classification presence
    - A test enumerates `PRAGMA table_info` for every table in `sqlite_master` and asserts zero column names contain `intent`
    - A test asserts the concatenated `sql` column of `sqlite_master` contains no `intent` substring
    - The snapshot helper enumerates tables dynamically from `sqlite_master` (not a hardcoded table list), so a future migration is automatically covered
    - `npx vitest run tests/intent-conservation.test.ts` passes
  </acceptance_criteria>
  <done>Classification is proven inert by observation of the database, not by inspection of the code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stage N → stage N+1 (prompt → schema → parser → decision) | each hop can silently drop or diverge on a vocabulary member while every per-stage test still passes |
| `ClaimDecision` → database | the boundary D-08 forbids classification from crossing this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-05-A | Tampering | vocabulary drift between prompt and code | mitigate | Task 1 drives every assertion off the exported `INTENT_STATUSES` / `INTENT_CONFIDENCES` constants so a one-sided change fails the suite; a size assertion makes vocabulary expansion a deliberate act (CLASSIFY-04). |
| T-63-05-B | Elevation of Privilege | classification silently gaining a DB write path | mitigate | Task 2 asserts a byte-equivalent database across classified and unclassified consolidation passes, plus a `PRAGMA table_info` / `sqlite_master` scan for any `intent` column, index, or view (D-08). |
| T-63-05-C | Repudiation | future phase silently relaxing this guard | mitigate | Task 2's header comment names Phase 66's `action_proposal` table as the ONE expected future delta and states that updating this test is a review-blocking change rather than routine maintenance. |
| T-63-05-SC | Tampering | package-manager installs | accept | No installs in this plan; `git diff --exit-code package.json package-lock.json` must be clean at plan close. |
</threat_model>

<verification>
- `npx vitest run tests/intent-conservation.test.ts` passes
- `git diff --stat src/` shows zero source changes from this plan
- Full suite green: `npm test`
</verification>

<success_criteria>
- The four-state vocabulary is provably identical at prompt, schema, parser, and typed-field stages
- A classified consolidation pass and an unclassified one leave indistinguishable databases
- No table, column, index, or view anywhere mentions the classification fields
</success_criteria>

<output>
Create `.planning/phases/63-offline-intent-classification/63-05-SUMMARY.md` when done
</output>
