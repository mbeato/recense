---
phase: 63-offline-intent-classification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/model/claim-extractor.ts
  - tests/claim-extractor-intent.test.ts
autonomous: true
requirements: [CLASSIFY-01, CLASSIFY-04]

must_haves:
  truths:
    - "A model response carrying all three intent fields on a claim yields an ExtractedClaim with intent_status / intent_entity / intent_confidence populated (D-04)"
    - "A model response with an out-of-enum status (e.g. 'ghosted') yields a claim with ALL THREE intent fields undefined — not a coerced fallback (D-05)"
    - "A model response with a numeric confidence (e.g. 0.87) yields a claim with ALL THREE intent fields undefined (D-06)"
    - "A model response with no intent fields yields a claim identical to today's parse output (backward-compat, D-05)"
    - "The four-state status vocabulary exists in exactly one place in code and is reused by the JSON schema (D-04)"
  artifacts:
    - path: "src/model/claim-extractor.ts"
      provides: "IntentStatus / IntentConfidence closed enums, drop-on-mismatch coercers, 3 optional ExtractedClaim fields, CLAIM_ARRAY_SCHEMA properties"
      contains: "INTENT_STATUSES"
    - path: "tests/claim-extractor-intent.test.ts"
      provides: "parse/coercion guard coverage for the all-or-nothing gate"
  key_links:
    - from: "src/model/claim-extractor.ts:CLAIM_ARRAY_SCHEMA"
      to: "src/model/claim-extractor.ts:INTENT_STATUSES"
      via: "schema enum built from the shared constant, not a duplicated literal array"
      pattern: "INTENT_STATUSES"
    - from: "src/model/claim-extractor.ts:parseClaimsFromArray"
      to: "src/model/claim-extractor.ts:toIntentStatus"
      via: "all-or-nothing gate calls the drop-on-mismatch coercer"
      pattern: "toIntentStatus"
---

<objective>
Add the three optional intent-classification fields to `ExtractedClaim`, their closed vocabularies, their drop-on-mismatch coercers, and the all-or-nothing parse gate in `parseClaimsFromArray` — mirroring the TEMP-02 `due_at`/`action_type` optional-annotation pattern but with strictly stricter semantics (absence over guess).

Purpose: this is the data contract every other plan in Phase 63 depends on. The prompt (63-02) emits into it, the consolidator (63-04) threads it, the invariant guards (63-05) assert against it.
Output: extended `src/model/claim-extractor.ts` + a new unit test file. No consumer, no DB write, no behavior change for any existing source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-offline-intent-classification/63-CONTEXT.md
@.planning/phases/63-offline-intent-classification/63-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Current shape of the file being extended. Executor should use these directly. -->

`src/model/claim-extractor.ts` today (verified live 2026-08-02):

- `export type ActionType = 'deadline' | 'flight' | 'appointment' | 'receipt' | 'payment' | 'meeting' | 'other'` (line 32)
- `export const ACTION_TYPES: ReadonlySet<ActionType>` (line 42)
- `export function toActionType(raw: unknown): ActionType` — coerces out-of-enum to `'other'`, NEVER drops (line 51)
- `export type ExtractedClaim = { type; value; links?; origin?; due_at?; action_type? }` (line 71)
- `export const CLAIM_ARRAY_SCHEMA: object` — array-root schema, `required: ['type','value']`, optional `due_at` / `action_type` properties (line 203)
- `export function parseClaimsFromArray(raw: unknown[]): ExtractedClaim[]` — the load-bearing gate; temporal fields computed at lines 290-298 and spread into the returned claim at line 298
- `export function parseClaims(text: string): ExtractedClaim[]` — fence/object-wrap tolerant wrapper that delegates to `parseClaimsFromArray`

Production call sites (`ProviderClaimExtractor.extract`, `extractClaimsWithChunking`) already route every source through `parseClaims`/`parseClaimsFromArray` and already pass `CLAIM_ARRAY_SCHEMA` — no call-site change is needed once the schema and parser are extended.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the intent vocabularies, coercers, ExtractedClaim fields, and JSON schema properties</name>
  <files>src/model/claim-extractor.ts</files>
  <read_first>
    - src/model/claim-extractor.ts (lines 1-110 for the ActionType/ExtractedClaim block, lines 195-230 for CLAIM_ARRAY_SCHEMA) — the file being modified; mirror its JSDoc and export conventions exactly
    - .planning/phases/63-offline-intent-classification/63-PATTERNS.md (section "src/model/claim-extractor.ts") — the analog block and the explicit "key deviation" note
    - .planning/phases/63-offline-intent-classification/63-CONTEXT.md (D-04, D-05, D-06)
  </read_first>
  <action>
In `src/model/claim-extractor.ts`, directly beneath the existing `ActionType` / `ACTION_TYPES` / `toActionType` block, add the Phase 63 intent vocabulary:

- `export type IntentStatus = 'applied' | 'interviewing' | 'rejected' | 'offer'` — exactly these four members (CLASSIFY-04 scoped vocabulary), no `'other'`, no `'unknown'`, no `'ghosted'`.
- `export const INTENT_STATUSES: ReadonlySet<IntentStatus>` built from the same four literals.
- `export type IntentConfidence = 'high' | 'medium' | 'low'` and `export const INTENT_CONFIDENCES: ReadonlySet<IntentConfidence>`.
- `export function toIntentStatus(raw: unknown): IntentStatus | undefined` — returns the value when it is a string member of `INTENT_STATUSES`, otherwise `undefined`. This is the deliberate inversion of `toActionType`'s coerce-to-`'other'` contract (D-05: absence over guess — a wrong status is worse than none). Its JSDoc must state that inversion and cite D-05 so a future reader does not "fix" it into symmetry with `toActionType`.
- `export function toIntentConfidence(raw: unknown): IntentConfidence | undefined` — same drop-on-mismatch shape. Numeric input (e.g. `0.87`) returns `undefined`; D-06 forbids any raw numeric confidence anywhere in the schema or output.

Add three optional fields to `ExtractedClaim`, immediately after `action_type`, using the same one-line-JSDoc "present only when X" convention already used for `due_at`/`action_type`:
- `intent_status?: IntentStatus`
- `intent_entity?: string` — free-text company/role descriptor as the email states it. JSDoc must say resolution to a graph node is Phase 64's job and that this field is NEVER matched against the graph in Phase 63.
- `intent_confidence?: IntentConfidence`
The JSDoc on the trio must state D-05 all-or-nothing presence: the three appear together or not at all.

Extend `CLAIM_ARRAY_SCHEMA.items.properties` with `intent_status` (`{ type: 'string', enum: [...INTENT_STATUSES] }`), `intent_entity` (`{ type: 'string' }`), and `intent_confidence` (`{ type: 'string', enum: [...INTENT_CONFIDENCES] }`). Build both `enum` arrays by spreading the exported `ReadonlySet` constants — do NOT re-type the literals inline; the shared constant is what makes the prompt/schema/parser parity test in 63-05 meaningful. Leave `required: ['type','value']` unchanged (backward-compat: baseline prompts omit these fields).

Do NOT add any consumer, any DB write, any migration, or any `RECENSE_*` env flag (D-08, D-10).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export type IntentStatus" src/model/claim-extractor.ts` returns 1
    - `node -e "const m=require('./dist/src/model/claim-extractor.js'); console.log([...m.INTENT_STATUSES].join(','))"` (after `npm run build`) prints exactly `applied,interviewing,rejected,offer`
    - `node -e "const m=require('./dist/src/model/claim-extractor.js'); console.log(m.toIntentStatus('ghosted'), m.toIntentStatus('offer'), m.toIntentConfidence(0.87))"` prints `undefined offer undefined`
    - `JSON.stringify(CLAIM_ARRAY_SCHEMA).includes('"intent_status"')` is true and `CLAIM_ARRAY_SCHEMA.items.required` still equals `['type','value']`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>The intent vocabulary, coercers, claim fields, and JSON schema properties exist; typecheck is clean; no consumer or migration was added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: All-or-nothing intent parse gate in parseClaimsFromArray</name>
  <files>src/model/claim-extractor.ts, tests/claim-extractor-intent.test.ts</files>
  <read_first>
    - src/model/claim-extractor.ts (lines 263-300) — `parseClaimsFromArray` and the TEMP-02 temporal gate it must sit beside
    - tests/claim-extractor-temporal.test.ts — the exact test-file analog (structure, imports, naming, assertion style) to mirror
    - .planning/phases/63-offline-intent-classification/63-PATTERNS.md (the "Parse/coercion guard pattern" block and the D-05 deviation note)
  </read_first>
  <behavior>
    - All three present and valid → all three survive onto the returned claim, trimmed for `intent_entity`
    - `intent_status` out of enum (`'ghosted'`, `'Applied'` wrong case, `''`, `null`, `42`) → ALL THREE undefined
    - `intent_confidence` out of enum (`'certain'`, `0.9`, missing) → ALL THREE undefined
    - `intent_entity` missing, empty, whitespace-only, or non-string → ALL THREE undefined
    - Only `intent_status` present (no entity, no confidence) → ALL THREE undefined (partial classification is never accepted)
    - No intent fields at all → claim parses exactly as today; `due_at`/`action_type` behavior is untouched
    - A claim carrying BOTH temporal fields and intent fields keeps both sets independently (the two gates do not interfere)
    - An item whose intent fields are dropped still yields the claim itself (type/value/links survive — a bad classification never discards the claim)
  </behavior>
  <action>
Inside `parseClaimsFromArray`, directly after the existing TEMP-02 `due_at`/`action_type` gate, add the Phase 63 intent gate as NEW logic (do not copy the per-field-independent temporal shape — D-05 requires cross-field coupling):

Compute `intent_status` via `toIntentStatus(obj['intent_status'])`, `intent_confidence` via `toIntentConfidence(obj['intent_confidence'])`, and `intent_entity` as the trimmed string when `obj['intent_entity']` is a non-empty string, else `undefined`. Then apply a single all-or-nothing collapse: if ANY of the three is `undefined`, ALL THREE are emitted as `undefined`. Add the three fields to the returned claim object literal at the end of the function.

Write the collapse as one explicit guard with a comment citing D-05 and naming the failure mode it prevents (a partially-classified claim reaching Phase 64/65 as if it were a complete signal). Do not silently widen the claim-level filter: an item with invalid intent fields still returns its claim, just without classification.

Create `tests/claim-extractor-intent.test.ts` mirroring `tests/claim-extractor-temporal.test.ts` (same imports, same `describe`/`it` phrasing style), covering every case in the behavior block above plus direct unit coverage of `toIntentStatus` / `toIntentConfidence` (valid member, wrong case, empty string, null, number, object). Route at least two cases through `parseClaims` (the fence-tolerant wrapper) rather than only `parseClaimsFromArray`, so the production entry path is covered.
  </action>
  <verify>
    <automated>npx vitest run tests/claim-extractor-intent.test.ts tests/claim-extractor-temporal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/claim-extractor-intent.test.ts` passes with at least 10 assertions covering every bullet in `<behavior>`
    - `npx vitest run tests/claim-extractor-temporal.test.ts` still passes unmodified (backward-compat regression lock)
    - A test asserts `parseClaimsFromArray([{type:'fact',value:'x',intent_status:'offer'}])[0]` has `intent_status === undefined` (partial → dropped)
    - A test asserts a claim with `intent_status:'ghosted'` still returns the claim itself (`value === 'x'`) with all three intent fields undefined
    - `grep -c "D-05" src/model/claim-extractor.ts` is at least 1 (the collapse guard cites the decision)
  </acceptance_criteria>
  <done>The all-or-nothing gate is implemented and proven by test; the temporal test file passes unedited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output → typed struct | `provider.generate()` returns attacker-influenceable text (email content shaped the response); `parseClaimsFromArray` is the coercion boundary that must never trust it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01-A | Tampering | `parseClaimsFromArray` intent gate | mitigate | Closed-enum membership test via `INTENT_STATUSES`/`INTENT_CONFIDENCES`; any out-of-vocabulary value drops all three fields (D-05). No value from model output ever reaches a typed field without set-membership validation. |
| T-63-01-B | Tampering | `intent_entity` free text | accept | Free text by design (D-04) — resolution is Phase 64's job. Inert this phase (never written to DB, never used as a query key, never interpolated into SQL). Re-evaluate when Phase 64 consumes it. |
| T-63-01-C | Elevation of Privilege | new optional fields | mitigate | Fields are pure passengers — no consumer, no DB write, no migration added in this plan. Verified behaviorally by 63-05's zero-DB-delta conservation test. |
| T-63-01-SC | Tampering | package-manager installs | accept | No `npm install` in this plan; net-zero-new-runtime-dependencies invariant holds. `git diff --exit-code package.json package-lock.json` must be clean at plan close. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npx vitest run tests/claim-extractor-intent.test.ts tests/claim-extractor-temporal.test.ts` passes
- `git diff --exit-code package.json package-lock.json` is clean (no new dependency)
- `grep -rn "intent_status" src/db/` returns zero matches (no schema/migration touched — D-08)
</verification>

<success_criteria>
- `ExtractedClaim` carries `intent_status` / `intent_entity` / `intent_confidence` as optional fields with the exact four-state and three-level vocabularies
- Out-of-enum or partial classification from the model produces absence, never a coerced guess
- Every existing extraction path (all sources) parses byte-identically to before when the model emits no intent fields
</success_criteria>

<output>
Create `.planning/phases/63-offline-intent-classification/63-01-SUMMARY.md` when done
</output>
