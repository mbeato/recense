---
phase: 63-offline-intent-classification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/online-llm-free-sentinel.test.ts
autonomous: true
requirements: [CLASSIFY-03]

must_haves:
  truths:
    - "The SessionStart inject path completes with a provider whose generate/judge/judgeBatch all throw"
    - "GET /v1/surface returns 200 with that same fail-if-called provider wired into the server"
    - "The retrieval ranking path reaches no model-generation call at all"
    - "The guard is non-vacuous: planting a generation call in the scanned online-path set makes the structural half fail"
  artifacts:
    - path: "tests/online-llm-free-sentinel.test.ts"
      provides: "CLASSIFY-03 dynamic fail-if-called regression across all three live online entrypoints, plus a structural scan"
      contains: "should not be called"
  key_links:
    - from: "tests/online-llm-free-sentinel.test.ts"
      to: "src/adapter/ambient-recall.ts:ambientRecall"
      via: "direct in-process call with a fail-if-called ModelProvider"
      pattern: "ambientRecall\\("
    - from: "tests/online-llm-free-sentinel.test.ts"
      to: "src/adapter/serve-cli.ts:createBrainHttpServer"
      via: "server factory with provider injection, GET /v1/surface over node:http"
      pattern: "createBrainHttpServer"
---

<objective>
Ship the CLASSIFY-03 regression lock: an automated test that fails if any online path calls the model provider for generation or judging. This is the guard that keeps Phase 63's offline-only classification from creeping onto a hot path — now, and in Phase 66 when `/v1/proposals` extends the same test.

Purpose: online paths block the user. An LLM call on SessionStart inject or `/v1/surface` is felt every session; a shipped test is the only thing that keeps this true across future phases.
Output: one new test file. No source changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-offline-intent-classification/63-CONTEXT.md
@.planning/phases/63-offline-intent-classification/63-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Live signatures of the three online entrypoints (verified 2026-08-02). -->

SessionStart inject:
`export async function ambientRecall(db, promptText, provider: ModelProvider, config, clock): Promise<string>` — `src/adapter/ambient-recall.ts:94`. Calls `provider.embed([...])` (line 121), then `engine.retrieveRanked(vec, AMBIENT_K, AMBIENT_FLOOR, undefined, { vizFloor })` (line 132). No generate, no judge.

HTTP surface:
`export async function createBrainHttpServer(opts: BrainHttpServerOptions): Promise<BrainHttpServer>` — `src/adapter/serve-cli.ts:252`. `opts.provider?: ModelProvider` (line 80) is forwarded into `wireMemoryEngine` (line 259). `GET /v1/surface` handler at lines 401-431 calls `ops.surface(...)`.

Retrieval:
`src/retrieval/*.ts` currently contains ZERO imports of `ModelProvider` (verified by grep) — retrieval takes a pre-computed vector. This is the property the structural half of the test locks.

Fail-if-called stub analog (`tests/recall.test.ts:237-252`):
a `ModelProvider` object literal where `embed` does real work and `generate` / `judge` / `judgeBatch` each `throw new Error('<name> should not be called')`.

HTTP test harness analog: `tests/surface-sentinel.test.ts` (temp file DB, `getFreePort`, `node:http.request`, hermetic per-test `RECENSE_LOCK_PATH`, `createBrainHttpServer` with an injected provider).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Dynamic fail-if-called sentinel across SessionStart inject, retrieval, and GET /v1/surface</name>
  <files>tests/online-llm-free-sentinel.test.ts</files>
  <read_first>
    - tests/recall.test.ts (lines 230-255) — the `emptyProvider` fail-if-called shape to copy
    - tests/ambient-recall.test.ts — the in-process `ambientRecall(db, PROMPT, provider, config(), clock)` wiring, seeded-node setup, and `FIXED_VEC` cosine trick
    - tests/surface-sentinel.test.ts (lines 1-115) — the HTTP harness: temp DB, free port, `node:http.request`, provider injection into `createBrainHttpServer`
    - src/adapter/ambient-recall.ts — confirm which provider method the inject path legitimately uses (`embed`) before writing the stub
    - .planning/phases/63-offline-intent-classification/63-CONTEXT.md (D-13)
  </read_first>
  <action>
Create `tests/online-llm-free-sentinel.test.ts`.

Define one shared helper `makeLlmFreeProvider(embedFn)` returning a `ModelProvider` object literal where `embed` delegates to `embedFn` and `generate`, `judge`, and `judgeBatch` each throw `new Error('<method> must not be called on an online path (CLASSIFY-03)')`. `embed` is deliberately allowed — embedding is not an LLM generation call and the inject path legitimately makes exactly one; the assertion under test is zero generate/judge/judgeBatch. Also count `embed` calls in the helper so each test can assert the embed count it expects (inject path: 1; `/v1/surface`: 0).

Three describe blocks:

1. **SessionStart inject** — seed a node with a known embedding (mirror `tests/ambient-recall.test.ts`'s `FIXED_VEC` setup so results actually clear `AMBIENT_FLOOR` and the code path is genuinely exercised rather than short-circuiting on an empty result). Call `ambientRecall(db, promptText, llmFreeProvider, config, clock)` and assert it resolves, that the returned text is non-empty (proof the path ran to completion, not that it bailed early), and that the embed counter is exactly 1.

2. **Retrieval** — construct the retrieval engine the same way `ambientRecall` does and call `retrieveRanked` directly with a pre-computed vector against the seeded graph; assert it returns results and that no provider object is required to do so. Assert the embed counter did not change across this block.

3. **GET /v1/surface** — mirror `tests/surface-sentinel.test.ts`'s harness exactly (temp file DB, `initSchema`, seeded surfaceable node, free port, hermetic `RECENSE_LOCK_PATH`), but pass `makeLlmFreeProvider(...)` as `opts.provider` instead of `MockModelProvider`. Issue `GET /v1/surface` via `node:http.request`, assert `statusCode === 200` and a parseable `items` array, and assert the embed counter is 0. Close the server and remove the temp DB in `afterEach`.

Add a file-header docblock stating that Phase 66 must extend this file with `/v1/proposals` when that route lands (D-13), and that the `recense recall` inference path is deliberately NOT covered here because it is a user-initiated LLM call, not an ambient online path.
  </action>
  <verify>
    <automated>npx vitest run tests/online-llm-free-sentinel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/online-llm-free-sentinel.test.ts` passes with at least three tests (inject, retrieval, `/v1/surface`)
    - The inject test asserts a non-empty returned string AND `embedCount === 1` (proves the path completed rather than short-circuiting)
    - The `/v1/surface` test asserts `statusCode === 200` and `embedCount === 0`
    - `grep -c "must not be called on an online path" tests/online-llm-free-sentinel.test.ts` is at least 3
    - The file header names `/v1/proposals` as the Phase 66 extension point
  </acceptance_criteria>
  <done>All three live online entrypoints are proven LLM-free by a shipped dynamic test.</done>
</task>

<task type="auto">
  <name>Task 2: Non-vacuous structural scan of the online-path source set</name>
  <files>tests/online-llm-free-sentinel.test.ts</files>
  <read_first>
    - tests/src-import-boundary.test.ts (full file) — the exported-predicate + real-walk + planted-offender three-part shape
    - src/adapter/ambient-recall.ts, src/retrieval/topk.ts, src/retrieval/engine.ts — the files the scan will cover; confirm none currently contains a generation call before writing the predicate
  </read_first>
  <action>
Append a fourth describe block to `tests/online-llm-free-sentinel.test.ts` implementing the structural half of D-13 (planner-discretion variant, taken because the dynamic half only proves the paths exercised by these specific fixtures — a new branch added later could evade it).

Export a pure predicate `findOnlineGenerationOffenders(files: Array<{path: string, text: string}>): string[]` that flags any file whose text contains `.generate(`, `.judge(`, or `.judgeBatch(` outside of comment lines. Strip `//` line comments and `/* */` block comments before matching so a doc comment mentioning `generate()` cannot create a false positive AND cannot mask a real one.

Scan set: `src/adapter/ambient-recall.ts` plus every `.ts` file under `src/retrieval/`. Assert the predicate returns `[]`. Then assert non-vacuousness by running the SAME exported predicate over a synthetic in-memory file containing `await provider.generate('x')` and expecting a non-empty offender list, and over a synthetic file where that call appears only inside a `//` comment and expecting `[]`.

Do not extend the scan set to `src/adapter/serve-cli.ts` — that file legitimately hosts non-online routes; the `/v1/surface` route is covered dynamically by Task 1.
  </action>
  <verify>
    <automated>npx vitest run tests/online-llm-free-sentinel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The real-scan test asserts `expect(offenders).toEqual([])` over `src/adapter/ambient-recall.ts` + all of `src/retrieval/*.ts`
    - The non-vacuousness test uses the exported `findOnlineGenerationOffenders` (not a re-implemented regex) and asserts a non-empty result on a planted offender
    - A third test proves comment-only occurrences are not flagged
    - `npx vitest run tests/online-llm-free-sentinel.test.ts` passes
  </acceptance_criteria>
  <done>The structural guard exists, covers the online-path source set, and is proven to fire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated HTTP client → `/v1/surface` | remote caller reaches a read route; an LLM call there would be a remotely-triggerable cost/latency amplifier |
| Claude Code SessionStart hook → inject path | runs on every session start; blocks the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-03-A | Denial of Service | `GET /v1/surface`, SessionStart inject | mitigate | Fail-if-called `ModelProvider` stub exercised through both entrypoints; any generation call on an online path throws and fails CI. Structural scan catches branches the fixtures do not exercise. |
| T-63-03-B | Denial of Service | future online routes (`/v1/proposals`, Phase 66) | transfer | File header records the extension obligation; Phase 66 owns adding that route to this test (D-13). Not mitigable from this phase — the route does not exist yet. |
| T-63-03-C | Repudiation | vacuous guard (test that can never fail) | mitigate | Task 2's planted-offender test proves the predicate fires; Task 1 asserts positive completion signals (non-empty output, exact embed counts) so a short-circuiting path cannot pass as "LLM-free". |
| T-63-03-SC | Tampering | package-manager installs | accept | No installs in this plan; `git diff --exit-code package.json package-lock.json` must be clean at plan close. |
</threat_model>

<verification>
- `npx vitest run tests/online-llm-free-sentinel.test.ts` passes
- `git diff --stat src/` shows zero source changes from this plan
- `git diff --exit-code package.json package-lock.json` is clean
</verification>

<success_criteria>
- SessionStart inject, retrieval, and `GET /v1/surface` each complete with a provider that throws on generate/judge/judgeBatch
- The guard is demonstrably non-vacuous (planted offender fails; comment-only mention does not)
- The Phase 66 extension point for `/v1/proposals` is recorded in the file itself, not only in planning docs
</success_criteria>

<output>
Create `.planning/phases/63-offline-intent-classification/63-03-SUMMARY.md` when done
</output>
