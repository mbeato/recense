---
phase: 63-offline-intent-classification
plan: 04
type: execute
wave: 2
depends_on: [63-01]
files_modified:
  - src/consolidation/consolidator.ts
  - tests/consolidation-intent.test.ts
autonomous: true
requirements: [CLASSIFY-01, CLASSIFY-02, CLASSIFY-04]

must_haves:
  truths:
    - "A gmail episode whose extraction emits intent fields produces ClaimDecision objects carrying those fields, on every decision route (fast-path confirm, auto-unrelated, and judge-escalated) (D-07)"
    - "A hitl-source episode is never classified — it is skipped before any claim is extracted (D-09)"
    - "An inferred-origin episode and an echo episode are likewise never classified (D-09)"
    - "Exactly one provider.generate call is made per gmail episode through the consolidator (D-12)"
    - "No classification field reaches a DB write in this phase (D-08)"
  artifacts:
    - path: "src/consolidation/consolidator.ts"
      provides: "claimIntentStatus / claimIntentEntity / claimIntentConfidence threaded on ClaimDecision and PendingJudge, filled at every decision site"
      contains: "claimIntentStatus"
    - path: "tests/consolidation-intent.test.ts"
      provides: "three-route threading coverage, hitl/inferred/echo sentinel, one-generate-call sentinel"
  key_links:
    - from: "src/consolidation/consolidator.ts hard-stop (origin/echo/hitl continue)"
      to: "the classification threading sites"
      via: "threading is textually AFTER the continue, inside the same per-episode loop iteration"
      pattern: "episode.source === 'hitl'"
    - from: "src/model/claim-extractor.ts:ExtractedClaim.intent_status"
      to: "src/consolidation/consolidator.ts:ClaimDecision.claimIntentStatus"
      via: "per-claim field copy at all four decision-fill sites"
      pattern: "claimIntentStatus: claim.intent_status"
---

<objective>
Thread the three intent fields from `ExtractedClaim` onto the in-memory `ClaimDecision`, at every decision route, as inert passengers — and prove structurally that the existing `hitl` / `inferred` / `echo` hard-stop is inherited rather than re-implemented.

Purpose: CLASSIFY-02 is the highest-risk item in this phase. PITFALLS.md Pitfall 6 names this exact defect class as one the codebase has shipped twice before (a pre-launch critical finding, then v9.0's live C-2). The mitigation is placement, not vigilance: the branch lives inside the loop the guard already protects.
Output: extended `src/consolidation/consolidator.ts` + a new integration test file. Zero DB writes, zero schema changes, zero new consumer methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-offline-intent-classification/63-CONTEXT.md
@.planning/phases/63-offline-intent-classification/63-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/63-offline-intent-classification/63-01-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Live map of every site this plan touches (verified 2026-08-02; re-grep before editing, line numbers shift). -->

`src/consolidation/consolidator.ts`:
- `isEligibleForExtraction(episode, config)` line 91 — prefetch-pool guard; returns false for sub-threshold, `origin === 'inferred'`, and `source === 'hitl'`. Its header comment states it deliberately duplicates the in-loop guard so both sites cannot drift.
- `interface ClaimDecision` line 151 — `claimDueAt?: string` at 174, `claimActionType?: ActionType` at 176, `gcalSourceEventId?` at 181.
- `interface PendingJudge` line 197 — carries `claimDueAt?` / `claimActionType?` at 203-204 so judge-escalated claims keep their annotations across the await.
- Per-episode loop begins `for (let episode of chunk) {` at line 596.
- **Hard stop at line 643:** `if (episode.origin === 'inferred' || echoSourceId !== null || episode.source === 'hitl') { this.markSkipped(episode.id); continue; }`
- Four TEMP-02 fill sites, ALL after that `continue`: fast-path confirm `decisionSlots[claimIdx] = {...}` at 733-748 (`claimDueAt` 744); auto-unrelated `decisionSlots[claimIdx] = {...}` at 827-843 (`claimDueAt` 838); `pendingJudges.push({...})` at 859-866 (`claimDueAt` 864); post-judge `decisionSlots[slotIdx] = {...}` at 910-926 (`claimDueAt` 921).
- `maybeWriteNodeTemporal` line ~1073 — TEMP-02's DOWNSTREAM CONSUMER (writes `node_temporal`). **Explicitly NOT to be mirrored** (D-08): no analogous intent writer, and `applyDecision` must not learn about the intent fields.

`src/model/claim-extractor.ts` (from plan 63-01): `intent_status?: IntentStatus`, `intent_entity?: string`, `intent_confidence?: IntentConfidence`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Thread the three intent fields through ClaimDecision and PendingJudge at every decision site</name>
  <files>src/consolidation/consolidator.ts</files>
  <read_first>
    - src/consolidation/consolidator.ts (lines 85-210 for the guards and both interfaces; lines 596-930 for the loop and all four fill sites; lines 1060-1130 for `maybeWriteNodeTemporal` and `applyDecision` so you can see precisely what NOT to add)
    - .planning/phases/63-offline-intent-classification/63-PATTERNS.md (section "src/consolidation/consolidator.ts" and the "No Analog Found" table row on `maybeWriteNodeTemporal`)
    - .planning/phases/63-offline-intent-classification/63-CONTEXT.md (D-07, D-08, D-09)
    - .planning/research/PITFALLS.md (Pitfall 6, lines 136-162)
    - src/model/claim-extractor.ts — the exact field names and types landed by plan 63-01
  </read_first>
  <action>
Before editing, run `grep -n "claimDueAt" src/consolidation/consolidator.ts` and enumerate every hit. That grep is the authoritative site list — a missed site silently drops classification for one decision route, which is exactly the convention-enforced-invariant drift class this phase exists to avoid.

Add to `interface ClaimDecision`, immediately after `claimActionType`, three optional fields with the same one-line-JSDoc convention:
- `claimIntentStatus?: IntentStatus`
- `claimIntentEntity?: string`
- `claimIntentConfidence?: IntentConfidence`
Import `IntentStatus` and `IntentConfidence` as types from `../model/claim-extractor` alongside the existing `ActionType` type import. The JSDoc must state: all three present together or all absent (D-05); INERT this phase — nothing consumes them, no DB row is written (D-08); Phase 64 resolves `claimIntentEntity`, Phase 65 maps `claimIntentConfidence` onto PE magnitude, Phase 66 persists.

Add the same three optional fields to `interface PendingJudge`, mirroring how `claimDueAt`/`claimActionType` are carried across the judge await.

Fill the fields at all four sites, copying from `claim.intent_status` / `claim.intent_entity` / `claim.intent_confidence` (and from `pendingJudges[i]!.claimIntent*` at the post-judge site, exactly as `claimDueAt` does):
1. fast-path confirm `decisionSlots[claimIdx]`
2. auto-unrelated `decisionSlots[claimIdx]`
3. `pendingJudges.push({...})`
4. post-judge `decisionSlots[slotIdx]`
Tag each added line with a `// CLASSIFY-02` trailing comment matching the existing `// TEMP-02` convention.

Add a block comment immediately above the fast-path fill site recording the D-09 structural claim: these fields are derived inside the existing per-episode loop, textually after the `origin === 'inferred' || echoSourceId !== null || episode.source === 'hitl'` hard stop, so the guard is inherited by construction; classification must never be re-implemented as an independent scan over episodes (Pitfall 6).

Do NOT: add a `maybeWriteIntentClassification` method, reference the new fields anywhere inside `applyDecision` or any `db.transaction` body, add a migration, add an env flag, emit the fields through `sink.emit()`'s `payload`, or modify `isEligibleForExtraction`.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/consolidation-temporal.test.ts tests/consolidation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Code-line count (comments excluded) is exactly 6: `grep -vE '^\s*(//|\*|/\*)' src/consolidation/consolidator.ts | grep -c claimIntentStatus` returns 6 — two interface declarations plus four fill sites
    - Every producer-side `claimDueAt` code line has a matching `claimIntentStatus` code line: the comment-stripped counts of `claimDueAt` and `claimIntentStatus` are equal after excluding the `maybeWriteNodeTemporal` consumer block (lines ~1068-1082)
    - Every fill site is below the hard stop: `grep -vE '^\s*(//|\*|/\*)' src/consolidation/consolidator.ts | grep -n claimIntent` cross-referenced against the real file shows only the two interface declarations above the `episode.source === 'hitl'` continue; all four fill sites are below it
    - `grep -c maybeWriteIntent src/consolidation/consolidator.ts` returns 0 (no consumer method added)
    - `grep -vE '^\s*(//|\*|/\*)' src/consolidation/consolidator.ts | grep -c 'claimIntent'` returns 18 (3 fields x 6 sites)
    - `npm run typecheck` exits 0 and `npx vitest run tests/consolidation-temporal.test.ts tests/consolidation.test.ts` passes unmodified
  </acceptance_criteria>
  <done>All four decision routes carry the intent fields; no consumer, writer, or schema change was introduced; the existing consolidation suites pass unedited.</done>
</task>

<task type="auto">
  <name>Task 2: Threading coverage, hitl/inferred/echo sentinel, and one-generate-call sentinel</name>
  <files>tests/consolidation-intent.test.ts</files>
  <read_first>
    - tests/consolidation-temporal.test.ts (lines 1-120 for the harness, then the temporal write-path tests) — the analog for scripting claims through a real `Consolidator` pass with `MockModelProvider` + in-memory SQLite
    - tests/hitl-audit-provenance.test.ts (lines 185-330, describe block "(b) consolidator exclusion") — the analog for asserting a hitl episode is marked consolidated with zero graph effects after a real pass
    - tests/consolidation.test.ts (lines 1090-1140) — the counting-provider pattern (`embedCallCount` wrapper around a raw `ModelProvider` object literal)
    - src/consolidation/consolidator.ts — the file under test, after Task 1
    - .planning/phases/63-offline-intent-classification/63-CONTEXT.md (D-09, D-12a)
  </read_first>
  <action>
Create `tests/consolidation-intent.test.ts` using `tests/consolidation-temporal.test.ts`'s harness verbatim (in-memory SQLite, `FakeClock`, synthetic embed fn, `MockModelProvider` or a raw `ModelProvider` literal scripting the extraction JSON).

**Observation seam:** the intent fields never reach the DB (D-08), so they cannot be observed by querying tables. Observe them by exercising the real `Consolidator.consolidate()` with a scripted provider and asserting on the DB-visible consequences plus provider-call behavior, and additionally by a narrow accessor: subclass `Consolidator` in the test file to capture the `ClaimDecision[]` array, OR export nothing new from `src/` and instead assert threading via the decision-derived behavior. Prefer the test-local subclass/spy approach — do NOT add a production export, a sink `payload` write, or any other observation channel to `src/` for the benefit of this test (PATTERNS flags the `sink.emit()` payload route as a D-08 violation risk; reject it explicitly and record that choice in the SUMMARY).

Tests to write:

1. **Three-route threading.** Three cases, each driving one decision route with a gmail episode whose scripted extraction emits a claim carrying all three intent fields, asserting the captured `ClaimDecision` carries `claimIntentStatus === 'rejected'` (or the scripted value), `claimIntentEntity`, and `claimIntentConfidence`:
   - fast-path confirm: pre-seed a node whose normalized value exactly matches the claim value
   - auto-unrelated: use the zero-vector embed fn so cosine falls below `unrelatedSimilarityThreshold` with no anchors and no BM25 hits
   - judge-escalated: seed a near-but-not-exact node so the claim escalates, with a scripted judge verdict
2. **Absence passthrough.** A gmail episode whose extraction emits no intent fields yields decisions with all three fields `undefined` (backward-compat).
3. **hitl sentinel (CLASSIFY-02, the load-bearing test).** Append an episode with `source: 'hitl'` whose content would unambiguously classify (a rejection email body) and a scripted provider whose `generate` throws `new Error('generate must not be called for a hitl episode')`. Run a real `consolidate()` pass. Assert: the pass completes without throwing, the episode is marked `consolidated = 1`, node and edge counts are unchanged, and zero `ClaimDecision` objects were produced for it. Name the test so the guard is greppable, e.g. `"hitl-source episode is never classified (D-43 / CLASSIFY-02)"`.
4. **inferred + echo sentinels.** Same shape for an `origin: 'inferred'` episode and for an episode backfilled with a non-null `source_inference_id` echo — both must reach zero classification, proving the branch inherits all three arms of the hard stop, not just the hitl arm.
5. **One-generate-call sentinel (D-12a).** Wrap a `generateCallCount` counter around a provider whose `generate` returns a scripted claim array with intent fields (mirror the `embedCallCount` pattern at `tests/consolidation.test.ts:1096-1136`). Append exactly one gmail episode above the skip threshold, run `consolidate()`, and assert `generateCallCount === 1`. Add a second case with two gmail episodes asserting `generateCallCount === 2`, so the assertion is per-episode rather than an accidental constant.
  </action>
  <verify>
    <automated>npx vitest run tests/consolidation-intent.test.ts tests/hitl-audit-provenance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/consolidation-intent.test.ts` passes with at least 8 tests covering all five groups above
    - The hitl test's provider throws on `generate` and the pass still completes — proving no extraction was attempted, not merely that no decision was recorded
    - The hitl test asserts `consolidated = 1` for the episode AND unchanged node/edge counts
    - The one-call sentinel asserts `generateCallCount === 1` for one episode and `=== 2` for two episodes
    - `grep -rn "sink.emit" tests/consolidation-intent.test.ts` returns nothing (the rejected observation seam was not used)
    - `npx vitest run tests/hitl-audit-provenance.test.ts` passes unmodified
  </acceptance_criteria>
  <done>Threading is proven on all three decision routes; hitl, inferred, and echo episodes are proven never to be classified; the one-call-per-episode cost claim is proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hitl audit episode → consolidation input | `source='hitl'` episodes embed the system's own retrieved output; treating them as evidence is the D-43 self-confirmation hole |
| inferred / echo episode → consolidation input | inference output re-entering as evidence (v9.0 finding C-2) |
| in-memory `ClaimDecision` → DB write | the boundary D-08 forbids classification from crossing this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-04-A | Elevation of Privilege | classification of `source='hitl'` / inferred / echo episodes | mitigate | Structural placement: the threading sites sit textually after the `origin === 'inferred' \|\| echoSourceId !== null \|\| episode.source === 'hitl'` hard stop, inside the same per-episode loop iteration — the guard is inherited, never re-implemented (D-09, Pitfall 6). Enforced by the Task 1 acceptance criterion that every fill site sits below the hard-stop line (only the two interface declarations appear above it) AND by the three sentinel tests in Task 2. |
| T-63-04-B | Elevation of Privilege | classification fields influencing `node.s` / `node.c` | mitigate | No consumer method, no `applyDecision` reference, no `db.transaction` reference. The existing "UPDATE node SET s/c only inside consolidate()" invariant is untouched. Verified behaviorally by 63-05's zero-DB-delta conservation test. |
| T-63-04-C | Tampering | drift between the two guard sites (`isEligibleForExtraction` and the in-loop stop) | accept | Pre-existing duplication, documented in the source with a comment explaining why. This plan adds no new guard surface and does not modify `isEligibleForExtraction`, so it introduces no new drift risk. Re-evaluate if a future phase adds a third site. |
| T-63-04-D | Denial of Service (cost) | second `generate` call per episode | mitigate | Task 2's counting-provider sentinel asserts exactly one call per gmail episode (D-12a, Pitfall 15). |
| T-63-04-SC | Tampering | package-manager installs | accept | No installs in this plan; `git diff --exit-code package.json package-lock.json` must be clean at plan close. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npx vitest run tests/consolidation-intent.test.ts tests/consolidation-temporal.test.ts tests/consolidation.test.ts tests/hitl-audit-provenance.test.ts` passes
- `grep -rn "intent" src/db/schema.ts` returns zero matches (no migration)
- Comment-stripped `claimIntent` code-line count is 18 (3 fields x [2 interfaces + 4 fill sites]): `grep -vE '^\s*(//|\*|/\*)' src/consolidation/consolidator.ts | grep -c claimIntent`
- `git diff --exit-code package.json package-lock.json` is clean
</verification>

<success_criteria>
- Every decision route carries the classification through to `ClaimDecision`
- hitl, inferred, and echo episodes are never classified — proven by tests whose provider throws if extraction is attempted
- Exactly one `provider.generate` call per gmail episode
- Not one byte of classification reaches the database
</success_criteria>

<output>
Create `.planning/phases/63-offline-intent-classification/63-04-SUMMARY.md` when done. Record the observation-seam choice (test-local spy vs. the rejected `sink.emit()` payload route) and the greppable site count.
</output>
