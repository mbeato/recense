---
phase: 59-hud-integration-visible-but-belong
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - src/viz/modules/constants.js
  - src/viz/css/styles.css
  - tests/viz-activity-palette-invariants.test.ts
autonomous: true
requirements: [D-12, D-14]

must_haves:
  truths:
    - "Every color literal in styles.css is a var(--token) reference; the only authored home of the values is constants.js"
    - "The 3 elevation box-shadows on #reader/#settings-panel/#index-panel are gone, replaced by hairline-border definition only"
    - "detail/settings/reader carry the glass token language; tooltip is token-migrated but keeps zero backdrop-filter (Pitfall 1 exception)"
  artifacts:
    - path: "src/viz/css/styles.css"
      provides: "fully token-migrated stylesheet; glass reskin of existing panels"
      contains: "var(--glass"
    - path: "src/viz/modules/constants.js"
      provides: "content-color tokens for every remaining deduped literal"
      contains: "HUD_CSS_TOKENS"
    - path: "tests/viz-activity-palette-invariants.test.ts"
      provides: "D-14 CSS literal scan + amber-exclusivity + backdrop-filter allow-list locks"
      contains: "D-14"
  key_links:
    - from: "src/viz/css/styles.css"
      to: "src/viz/modules/constants.js (via emitted :root vars)"
      via: "var(--token) references resolve to runtime-injected values"
      pattern: "var\\(--"
---

<objective>
Perform the D-14 exhaustive CSS token migration — every one of the ~89 deduped color
literals in styles.css becomes a var(--token) reference sourced from constants.js — and
apply the D-12 glass reskin to the existing panels (detail, settings, reader, tooltip),
removing the 3 elevation drop-shadows. Lock all of it with the D-14 invariants scan.

Purpose: D-14 makes amber-exclusivity and the no-foreign-palette-ramp rule machine-checked
over the WHOLE stylesheet, and D-12 gives Phase 60 a complete panel vocabulary. This is the
mechanical heart of "token discipline extended to the HUD CSS."

Output: token-migrated styles.css, content-color token additions to constants.js, and the
D-14 CSS-scan invariants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-hud-integration-visible-but-belong/59-CONTEXT.md
@.planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md
@.planning/phases/59-hud-integration-visible-but-belong/59-RESEARCH.md
@.planning/phases/59-hud-integration-visible-but-belong/59-PATTERNS.md
@.planning/phases/59-hud-integration-visible-but-belong/59-01-SUMMARY.md

<interfaces>
Token vocabulary + emission established by Plan 01: HUD_CSS_TOKENS in constants.js,
emitHudTokens() in css-tokens.js (data-driven — new keys added to HUD_CSS_TOKENS are
auto-emitted, no css-tokens.js edit needed). Reference tokens as var(--KEY).

Migration scope facts (verified this planning pass):
- 89 unique deduped color literals in styles.css (grep -oE hex/rgba | sort -u).
- 3 box-shadows to REMOVE (D-12 / anti-slop item 9): styles.css:571 (#reader),
  styles.css:869 (#settings-panel), styles.css:1203 (#index-panel) — all
  "2px 0 24-28px rgba(0,0,0,0.35)". Replace with border 1px solid var(--glass-border-focused).
  Do NOT substitute a softer drop-shadow.
- Sanctioned amber-family exceptions to PRESERVE + allow-list (RESEARCH Pitfall 6):
  rgba(217, 160, 92, *) and #d9a05c at ~15 hover/focus/active border-tint + fact-ref
  link sites (e.g. styles.css:140,233,374,500,531,555,620,667,671). These stay; they get
  their OWN token (accent-amber-tint) but must NOT be flagged as a new violation.
- Tooltip exception (Pitfall 1): #tooltip (styles.css:63-76) token-migrate literals
  (background token at existing 0.93 alpha, border/radius) but add ZERO backdrop-filter
  and ZERO --glass-specular; UPDATE (do not delete) its explanatory comment.
- Existing #panel/#detail glass idiom (styles.css:97-121) is the formalization target.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog + tokenize every remaining color literal in constants.js</name>
  <files>src/viz/modules/constants.js</files>
  <read_first>
    - src/viz/modules/constants.js (HUD_CSS_TOKENS from Plan 01 — extend this object)
    - src/viz/css/styles.css (full — the source of the literals; produce the deduped catalog first)
    - .planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md (Color section +
      Glass Construction Recipe — which families are locked; naming discipline)
  </read_first>
  <action>
    Run the dedup grep (hex + rgba literals, sort -u) to produce the exact ~89-value catalog.
    For every literal NOT already covered by a Plan-01 recipe token, add a semantic-named key to
    HUD_CSS_TOKENS in constants.js. Naming scheme: group by role — glass/border/radius/motion
    already exist; add content-color tokens with intent-revealing names (e.g. sse-dot-live,
    sse-dot-idle, text-muted, divider, and accent-amber-tint for the sanctioned amber
    rgba(217,160,92,*)/#d9a05c family). A token per distinct VALUE is the floor; a token per ROLE
    is preferred where a value repeats — 30-40 semantic tokens covering the deduped set is fine, do
    not invent 89 one-offs. Every new key stays inside the LOCKED aubergine/hairline/slate families
    (no new saturated violet — anti-slop item 2). Confine the amber family to a single named
    accent-amber-tint token (or a small named set) so the D-14 test can allow-list it precisely.
    Values remain plain strings; no runtime behaviour added to constants.js.
  </action>
  <verify>
    <automated>node -e "import('./src/viz/modules/constants.js').then(m=>{const t=m.HUD_CSS_TOKENS;const keys=Object.keys(t);if(keys.length<25){console.error('too few tokens',keys.length);process.exit(1)}if(!Object.values(t).some(v=>/217,\s*160,\s*92|d9a05c/i.test(v))){console.error('amber tint token missing');process.exit(1)}console.log('ok',keys.length)})"</automated>
  </verify>
  <acceptance_criteria>
    - HUD_CSS_TOKENS covers every distinct color value found by the dedup grep of styles.css
      (no literal left without a token) — proven in Task 2's migration verify
    - A token exists whose value is the sanctioned amber family (rgba(217, 160, 92, *) or #d9a05c)
    - No new token value falls outside the locked aubergine/hairline/slate/amber families (no new
      saturated indigo/violet)
    - constants.js has no function added (still pure export const)
  </acceptance_criteria>
  <done>Every deduped styles.css literal has a semantic token in constants.js; amber isolated to a named token.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate styles.css literals to var(); remove box-shadows; glass reskin panels</name>
  <files>src/viz/css/styles.css</files>
  <read_first>
    - src/viz/css/styles.css (full — this is a whole-file rewrite of color literals)
    - src/viz/modules/constants.js (the complete token set from Task 1 — the target var names)
    - .planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md (Glass Construction
      Recipe, Radius Tokens, Motion Tokens, Tooltip exception, anti-slop item 9)
    - .planning/phases/59-hud-integration-visible-but-belong/59-RESEARCH.md (Pitfall 1 tooltip
      resolution; box-shadow line numbers 571/869/1203)
  </read_first>
  <action>
    Replace every color literal in styles.css with its var(--token) reference from the Task-1 set.
    Remove the 3 elevation box-shadows at #reader/#settings-panel/#index-panel and replace each with
    border 1px solid var(--glass-border-focused) (no softer drop-shadow). Apply the glass token
    language to #detail, #settings-panel, #reader: background var(--glass-bg-focused), border
    var(--glass-border-focused), backdrop-filter blur(var(--glass-blur-md)), the var(--glass-specular)
    inset top-highlight, border-radius var(--radius-lg); swap their hardcoded transition timings to
    var(--motion-slow)/var(--ease-out-soft). For #tooltip: token-migrate its literals (background at
    its existing 0.93-alpha aubergine value via a token, border-radius var(--radius-sm), border
    var(--glass-border)) but add NO backdrop-filter and NO --glass-specular; UPDATE its existing
    explanatory comment to state this is the deliberate phase-59 contrast-first exception (do not
    delete the comment). Do NOT touch internal layout/content/padding of these panels — restyle only
    (D-12). Leave --index-width as-is. Radii resolve to the xs/sm/md/lg token set only.
  </action>
  <verify>
    <automated>bash -c 'set -e; RAW=$(grep -nE "(background|color|border|box-shadow|fill|stroke)[^:]*:[^;]*(#[0-9a-fA-F]{3,8}|rgba?\()" src/viz/css/styles.css | grep -v "var(--" | grep -v "index-width" || true); if [ -n "$RAW" ]; then echo "UNMIGRATED:"; echo "$RAW"; exit 1; fi; if grep -qE "box-shadow: 2px 0 (24|28)px" src/viz/css/styles.css; then echo "elevation shadow not removed"; exit 1; fi; echo ok'</automated>
  </verify>
  <acceptance_criteria>
    - No color-bearing property (background/color/border/box-shadow/fill/stroke) in styles.css uses
      a raw hex/rgba literal — all resolve through var(--token) (grep gate above passes)
    - The 3 elevation box-shadows (2px 0 24-28px) at #reader/#settings-panel/#index-panel are absent
    - #detail, #settings-panel, #reader rule blocks contain backdrop-filter: blur(var(--glass-blur-md))
      and box-shadow: var(--glass-specular)
    - #tooltip rule block contains NO backdrop-filter and NO var(--glass-specular); its explanatory
      comment is present and mentions the phase-59 exception
    - `npm test` full suite still green
  </acceptance_criteria>
  <done>styles.css fully token-migrated; elevation shadows gone; panels reskinned; tooltip exception preserved.</done>
</task>

<task type="auto">
  <name>Task 3: D-14 CSS-scan invariants (literal ban, amber allow-list, backdrop-filter allow-list)</name>
  <files>tests/viz-activity-palette-invariants.test.ts</files>
  <read_first>
    - tests/viz-activity-palette-invariants.test.ts (the source-parse harness + Plan-01 readStylesCss())
    - src/viz/css/styles.css (the migrated result to lock)
    - .planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md (anti-slop table items
      1/2/9, Color section — the sanctioned amber exception; the D-12 backdrop-filter selector list)
    - .planning/phases/59-hud-integration-visible-but-belong/59-RESEARCH.md (Pitfall 6 — the test
      MUST allow-list existing amber uses or it fails against current code)
  </read_first>
  <behavior>
    - D-14-A literal ban: no color-bearing property in styles.css uses a raw hex/rgba literal
      (excluding --index-width) — all colors flow through var(--token); test fails if any reappears
    - D-14-B amber-exclusivity: the ONLY amber-family (rgba(217,160,92,*) / #d9a05c) value present is
      the single named accent-amber-tint token in constants.js; styles.css references it only via var();
      no NEW amber fill/background/text literal anywhere
    - D-14-C backdrop-filter allow-list: backdrop-filter appears ONLY in rule blocks for the D-12
      selector set (chip/rail/topics-rail/#palette/#detail/#settings-panel/#reader/.toast) and NEVER
      on #tooltip or any other selector (anti-slop item 1)
  </behavior>
  <action>
    Add describe('D-14 CSS token discipline', ...) to the invariants file using the existing
    source-parse harness (readStylesCss + stripComments). Implement D-14-A as a regex scan of
    color-bearing declarations for raw literals (allow-list only --index-width). Implement D-14-B by
    asserting the amber family appears in constants.js exactly once (as accent-amber-tint) and that
    styles.css contains zero raw amber literals (only var references). Implement D-14-C by parsing
    styles.css rule blocks and asserting every backdrop-filter declaration belongs to a selector in
    the D-12 allow-list and that the #tooltip block has none. Follow RESEARCH Pitfall 6: the amber
    allow-list must make the test PASS against the migrated stylesheet — do not write a blanket
    amber-ban regex. Reuse the existing describe/it/expect + regex convention.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-activity-palette-invariants.test.ts -t "D-14"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/viz-activity-palette-invariants.test.ts` exits 0 (D-14 + D-11 + D-10 +
      all prior Phase-57/58 locks green)
    - The file contains describe('D-14 and asserts the literal ban, the single-amber-token rule, and
      the backdrop-filter selector allow-list (including #tooltip has none)
    - Introducing a raw hex color into a styles.css background as a temporary check makes the D-14-A
      test RED (confirm the gate has teeth, then revert)
  </acceptance_criteria>
  <done>D-14 CSS-scan invariants green over the fully-migrated stylesheet; amber + backdrop-filter allow-lists have teeth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viz server → browser | Loopback, read-only. This plan is pure CSS + test — no new runtime data path, no boundary added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-02 | Tampering | styles.css color values | accept | All values authored in-repo (constants.js); no user/server-sourced color input. No injection vector. |
| T-59-SC | Tampering | package installs | N/A | Zero new packages this phase. |

Spoofing / Repudiation / Info-Disclosure / DoS / Elevation: N/A — presentation-layer CSS only.
</threat_model>

<verification>
- `npx vitest run tests/viz-activity-palette-invariants.test.ts` green (D-14/D-11/D-10 + priors)
- `npm test` full suite green (shared constants.js touched)
- Manual: load `recense viz`, confirm detail/settings/reader render with glass borders and no drop
  shadows; tooltip stays a crisp opaque card (no blur)
</verification>

<success_criteria>
- Every styles.css color literal migrated to var(--token) from constants.js
- 3 elevation box-shadows removed; detail/settings/reader glass-reskinned; tooltip exception preserved
- D-14 amber-exclusivity + no-foreign-ramp + backdrop-filter allow-list machine-locked and green
</success_criteria>

<output>
Create `.planning/phases/59-hud-integration-visible-but-belong/59-02-SUMMARY.md` when done
</output>
