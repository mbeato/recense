---
phase: 59-hud-integration-visible-but-belong
plan: 04
type: execute
wave: 4
depends_on: ["59-01", "59-02", "59-03"]
files_modified:
  - src/viz/index.html
  - src/viz/modules/hud.js
  - src/viz/modules/topics.js
  - src/viz/css/styles.css
autonomous: true
requirements: [D-01, D-02, D-03, D-13]

must_haves:
  truths:
    - "A status chip (SSE dot + node count, legend folds in on hover) sits top-left; scene center stays clear"
    - "One vertical icon rail docked mid-right holds reader/settings/corpus/recenter/search magnifier — the two floating corner buttons are gone"
    - "A slim topics rail on the left edge expands on hover and highlights regions via ctx.selectNode"
    - "The old always-open search field, results list, event-log button and tombstone button are deleted from the DOM"
  artifacts:
    - path: "src/viz/index.html"
      provides: "#hud-chip, #hud-rail (5 inline-SVG icons), #topics-rail; old #panel internals + floating buttons removed"
      contains: "id=\"hud-rail\""
    - path: "src/viz/css/styles.css"
      provides: "chip/rail/topics-rail glass styling + dock positions + compact-mode gating"
      contains: "hud-rail"
  key_links:
    - from: "src/viz/index.html #hud-rail magnifier"
      to: "ctx.openPalette"
      via: "search icon click opens the ⌘K palette (D-03 single search surface)"
      pattern: "openPalette"
    - from: "src/viz/modules/hud.js"
      to: "#hud-chip"
      via: "SSE dot + node count re-homed from #panel status row"
      pattern: "hud-chip|sse-dot"
---

<objective>
Restructure the overlay DOM into the D-01 footprint — a top-left status chip, one mid-right
vertical icon rail consolidating all action icons (absorbing both floating corner buttons), and
a slim left-edge topics rail — and delete the crammed #panel internals, the always-open search
UI, and the event-log/tombstone buttons (now palette commands). Re-home the SSE status/count to
the chip and wire the rail icons to their existing actions.

Purpose: D-01/D-02/D-03 — chrome consolidation that clears the scene center and gives every
surface the glass language, with the diegetic split (D-13: only troika labels live in-scene;
chip/rails are flat screen-space glass).

Output: index.html DOM restructure, hud.js chip re-home + button-binding removal, topics.js rail
render, and the chip/rail/topics-rail glass CSS with compact-mode gating.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-hud-integration-visible-but-belong/59-CONTEXT.md
@.planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md
@.planning/phases/59-hud-integration-visible-but-belong/59-RESEARCH.md
@.planning/phases/59-hud-integration-visible-but-belong/59-PATTERNS.md

<interfaces>
Available after Plans 01-03:
- ctx.openPalette (palette.js) — rail magnifier calls this (D-03 single search surface).
- ctx.toggleTombstones / ctx.toggleLog (hud.js) — now palette commands; their old button bindings
  are removed here.
- ctx.listTopics (topics.js) — the topics rail renders from this; selection via ctx.selectNode.
- ctx.searchNodes (search.js) — the always-open search DOM is retired; fetch lives in the palette.

DOM to REMOVE (index.html, per RESEARCH State-of-the-Art + PATTERNS deleted-block precedent):
- #panel status/actions/search-wrap/topic-wrap internals (keep or replace the container per Pitfall 7)
- #search-wrap (lines ~51-62), #search-results, #topic-wrap (lines ~63-67)
- #btn-tombstones, #btn-log (lines ~45,48), old legend block
- #btn-corpus (lines ~82-88), #btn-recenter (lines ~98-106) floating corner buttons — absorbed into rail
- #hull-credit STAYS (CC BY-SA attribution required) but drops to hairline opacity (D-02)

DOM to ADD (index.html — copy the existing block conventions, PATTERNS lines 236-256):
- #hud-chip (top-left): #sse-dot + node-count readout (mono font) + legend that folds in on hover
- #hud-rail (mid-right, docked): 5 inline-SVG outline icons — reader, settings, corpus, recenter,
  search magnifier — each with aria-label + title from UI-SPEC Copywriting Contract ("Open reader"/
  "Open settings"/"Open corpus view"/"Recenter view"/"Search (⌘K)"). Icon style copied from existing
  #btn-corpus/#btn-recenter SVG (viewBox 0 0 24 24, fill none, stroke currentColor, stroke-width 2,
  round caps/joins), rendered 14-16px, currentColor (never amber). Rail holds 4-5 icons max (D-02).
- #topics-rail (left edge): slim collapsed vertical rail, expands on hover (hover-expand chosen —
  Claude's Discretion per D / RESEARCH), renders ctx.listTopics rows, selection via ctx.selectNode.

Wiring: rail magnifier → ctx.openPalette; reader → reader show(); settings → settings show();
corpus → corpus open; recenter → graph/camera recenter (reuse the exact handlers the deleted
#btn-corpus/#btn-recenter used). hud.js: move SSE-dot + node-count textContent writes to #hud-chip
elements; DELETE the #btn-log/#btn-tombstones click bindings (logic stays as ctx callables from
Plan 03). Preserve textContent-only discipline.

Compact gating (Pitfall 7): the new #hud-chip/#hud-rail/#topics-rail MUST be reached by the existing
@media (max-width:500px),(max-height:500px) hide rule — add their IDs to the hide-list following the
existing "#btn-recenter stays / most hidden" pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html DOM restructure — add chip/rail/topics-rail, delete old chrome</name>
  <files>src/viz/index.html</files>
  <read_first>
    - src/viz/index.html (full — the #panel/#detail/#settings-panel/#reader block conventions, the
      existing #btn-corpus/#btn-recenter inline-SVG icons to copy, and every element being deleted)
    - src/viz/modules/hud.js (which element IDs it queries for SSE dot + node count — so the chip
      exposes matching IDs or the re-home in Task 2 stays consistent)
    - .planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md (Icon System, Copywriting
      Contract labels, Color — icons currentColor never amber)
    - .planning/phases/59-hud-integration-visible-but-belong/59-PATTERNS.md (index.html analog lines
      236-258 — new-block + deleted-block precedent)
  </read_first>
  <action>
    Add #hud-chip (top-left): an #sse-dot span, a node-count readout span (mono class), and a legend
    block that is visually folded and expands on hover (opacity/max-content on :hover, no layout push
    of the scene). Add #hud-rail docked mid-right: 5 icon buttons (reader, settings, corpus, recenter,
    search magnifier) each an inline outline SVG copied from the existing #btn-corpus/#btn-recenter
    style, each with aria-label + title from the UI-SPEC Copywriting Contract. Add #topics-rail on the
    left edge (slim, empty container populated by topics.js in Task 2). Delete: #search-wrap,
    #search-results, #topic-wrap, #btn-tombstones, #btn-log, the old legend block, #btn-corpus,
    #btn-recenter, and the now-empty #panel status/actions internals (keep a container only if needed
    for compact gating). Keep #hull-credit (attribution) — it drops to hairline opacity via CSS in
    Task 3. Every new element uses a comment above stating the D-01/D-02/D-03 decision it implements.
    Do NOT add amber anywhere.
  </action>
  <verify>
    <automated>bash -c 'set -e; for id in hud-chip hud-rail topics-rail sse-dot; do grep -q "id=\"$id\"" src/viz/index.html || { echo "missing #$id"; exit 1; }; done; for gone in search-wrap search-results topic-wrap btn-tombstones btn-log btn-corpus btn-recenter; do grep -q "id=\"$gone\"" src/viz/index.html && { echo "$gone not deleted"; exit 1; }; done; grep -q "hull-credit" src/viz/index.html; C=$(grep -c "aria-label" src/viz/index.html); [ "$C" -ge 5 ] || { echo "rail icons missing aria-labels"; exit 1; }; echo ok'</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains id="hud-chip", id="hud-rail", id="topics-rail", id="sse-dot"
    - index.html no longer contains id="search-wrap", "search-results", "topic-wrap",
      "btn-tombstones", "btn-log", "btn-corpus", "btn-recenter"
    - #hull-credit still present (attribution preserved)
    - The rail's 5 icons are inline <svg> with viewBox="0 0 24 24" stroke="currentColor" and each
      carries an aria-label + title (>=5 aria-label attributes present)
    - No emoji/pictographic characters in the new DOM
  </acceptance_criteria>
  <done>DOM footprint = chip + mid-right icon rail + left topics rail; old panel/search/dev-button DOM removed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-home hud.js status to chip; wire rail icons; render topics rail</name>
  <files>src/viz/modules/hud.js, src/viz/modules/topics.js</files>
  <read_first>
    - src/viz/modules/hud.js (full — SSE-dot/node-count textContent writes, #btn-log/#btn-tombstones
      click bindings to remove, showToast, the header security note)
    - src/viz/modules/topics.js (ctx.listTopics from Plan 03; the ctx.selectNode region-highlight call)
    - src/viz/index.html (the new #hud-chip / #hud-rail / #topics-rail element IDs from Task 1)
    - src/viz/modules/corpus.js, src/viz/modules/graph.js, src/viz/modules/reader.js,
      src/viz/modules/settings.js (the exact handlers the deleted #btn-corpus/#btn-recenter used and
      the show()/open()/recenter entry points the rail icons rebind to)
    - .planning/phases/59-hud-integration-visible-but-belong/59-RESEARCH.md (Pitfall 4 — do NOT add a
      second mousemove listener; Pitfall 3 — no central keyboard dispatcher)
  </read_first>
  <action>
    In hud.js: repoint the SSE-dot status and node-count textContent writes from the old #panel status
    row to the new #hud-chip child elements (#sse-dot + the count readout), guarded with `if (!el) return`;
    DELETE the #btn-log and #btn-tombstones click-handler bindings (their logic stays as ctx.toggleLog/
    ctx.toggleTombstones callables from Plan 03 — do not delete the logic). Preserve the textContent-only
    security discipline and the EventSource('/events') wiring. Wire the #hud-rail icons: magnifier →
    ctx.openPalette; reader → the existing reader show() entry; settings → settings show(); corpus →
    the exact handler #btn-corpus used (corpus.js open/toggle); recenter → the exact handler
    #btn-recenter used (graph/camera recenter). Reuse existing handlers verbatim — do not reimplement
    camera/corpus logic. In topics.js: render ctx.listTopics rows into #topics-rail (textContent-only,
    createElement), each row selecting via ctx.selectNode(schemaNode) + ctx.markActive(); hover-expands
    the rail (CSS in Task 3). Do NOT add a new mousemove listener anywhere (Pitfall 4).
  </action>
  <verify>
    <automated>bash -c 'set -e; grep -qE "hud-chip|getElementById\\(.sse-dot" src/viz/modules/hud.js; grep -q "openPalette" src/viz/modules/hud.js; grep -q "topics-rail" src/viz/modules/topics.js; grep -q "selectNode" src/viz/modules/topics.js; grep -q "btn-log" src/viz/modules/hud.js && { echo "btn-log binding not removed"; exit 1; }; grep -q "btn-tombstones" src/viz/modules/hud.js && { echo "btn-tombstones binding not removed"; exit 1; }; echo ok'</automated>
  </verify>
  <acceptance_criteria>
    - hud.js writes SSE status + node count into #hud-chip elements (references #sse-dot / the chip
      count node), guarded against null
    - hud.js no longer references #btn-log or #btn-tombstones (bindings removed; ctx callables intact)
    - The #hud-rail magnifier click calls ctx.openPalette; reader/settings/corpus/recenter icons call
      the existing show()/open()/recenter entry points
    - topics.js renders rows into #topics-rail via textContent/createElement and selects via ctx.selectNode
    - Exactly one document mousemove listener remains in the codebase (grep app.js — no new one added)
    - `npm test` full suite green; `recense viz` boots with a live SSE dot in the chip
  </acceptance_criteria>
  <done>Chip shows live SSE status/count; rail icons drive existing actions; topics rail populated; dev-button bindings gone.</done>
</task>

<task type="auto">
  <name>Task 3: Chip / rail / topics-rail glass CSS + dock positions + compact gating</name>
  <files>src/viz/css/styles.css</files>
  <read_first>
    - src/viz/css/styles.css (token-migrated stylesheet; the existing #panel glass idiom lines ~97-121;
      the compact @media block ~lines 420-435; the z-index map; #hull-credit rules)
    - src/viz/modules/constants.js (glass/radius/motion tokens the new selectors reference)
    - .planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md (Glass Construction ambient
      tier for chip/rails, Spacing Scale, Typography mono chip readout, Color icons currentColor)
    - .planning/phases/59-hud-integration-visible-but-belong/59-RESEARCH.md (Pitfall 7 compact gating;
      z-index map — chip/rail at the existing z:10/z:70 tier)
  </read_first>
  <action>
    Style #hud-chip, #hud-rail, #topics-rail with the AMBIENT glass tier: background var(--glass-bg-ambient),
    border var(--glass-border), backdrop-filter blur(var(--glass-blur-sm)), box-shadow var(--glass-specular),
    border-radius var(--radius-md); transitions opacity var(--motion-base) var(--ease-out-soft). Dock
    positions: chip top-left, rail mid-right (vertically centered, docked to the right edge with md=16px
    offset), topics-rail left edge. The chip legend is folded by default and reveals on #hud-chip:hover
    (opacity/max-height, no scene layout push). The topics-rail expands on :hover (width/opacity — width
    change is on the rail itself over empty gutter, not the scene; keep the recede opacity behavior
    opacity-only in Plan 05). Rail icons: currentColor at the secondary/30% tier, never amber; chip
    count readout uses font-family 'JetBrains Mono HUD'. Drop #hull-credit to hairline opacity (D-02).
    Add #hud-chip, #hud-rail, #topics-rail to the existing compact @media hide-list (Pitfall 7),
    following the "#btn-recenter stays visible / rest hidden" exception pattern. No drop-shadow, no glow.
  </action>
  <verify>
    <automated>bash -c 'set -e; for sel in hud-chip hud-rail topics-rail; do grep -q "#$sel" src/viz/css/styles.css || { echo "missing rule #$sel"; exit 1; }; done; grep -q "JetBrains Mono HUD" src/viz/css/styles.css; grep -A40 "max-width: 500px" src/viz/css/styles.css | grep -qE "hud-chip|hud-rail|topics-rail" || { echo "new chrome not in compact hide-list"; exit 1; }; if grep -qE "transform-style:\s*preserve-3d|perspective\(|preserve-3d" src/viz/css/styles.css; then echo "D-13 violation: CSS3D/preserve-3d on screen-space chrome — only troika labels are diegetic"; exit 1; fi; echo ok'</automated>
  </verify>
  <acceptance_criteria>
    - #hud-chip/#hud-rail/#topics-rail rules use var(--glass-bg-ambient)/var(--glass-border)/
      blur(var(--glass-blur-sm))/var(--glass-specular)/var(--radius-md)
    - Chip docked top-left, rail mid-right (right-edge, vertically centered), topics-rail left edge
    - The compact @media block hides #hud-chip, #hud-rail, #topics-rail
    - No box-shadow with an outward blurred profile and no filter/text-shadow glow on any of the three
    - D-13 diegetic split (grep-verified): styles.css contains no transform-style:preserve-3d / perspective() / CSS3D on any HUD selector — only the troika WebGL labels live in-scene; chip/rails/palette/tooltip are flat screen-space glass
    - `npx vitest run tests/viz-activity-palette-invariants.test.ts` green (D-14 backdrop-filter
      allow-list now includes chip/rail/topics-rail)
  </acceptance_criteria>
  <done>Chip/rails render as ambient-tier glass in the D-01 dock positions; compact mode hides them; invariants green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viz server → browser | Topics rail + chip render server-sourced schema names + node count — untrusted strings into DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-05 | Tampering | topics rail + chip rendering | mitigate | textContent/createElement-only for schema names + count (inherit hud.js/topics.js discipline); no innerHTML with server data. |
| T-59-SC | Tampering | package installs | N/A | Zero new packages. |

Spoofing / Repudiation / Info-Disclosure / DoS / Elevation: N/A — loopback presentation layer.
</threat_model>

<verification>
- `npx vitest run tests/viz-activity-palette-invariants.test.ts` green (D-14 includes chip/rail)
- `npm test` full suite green
- Manual: scene center clear; chip top-left with live SSE dot + count + hover legend; mid-right rail
  with 5 working icons (magnifier opens palette); left topics rail expands on hover and highlights
  regions; no floating corner buttons; nothing renders in the tray popover
</verification>

<success_criteria>
- D-01 footprint live: status chip + mid-right icon rail (both floating buttons absorbed) + left topics rail
- Search demoted to one magnifier→palette surface; event-log/tombstone buttons deleted (now commands)
- Chip/rails are ambient-tier glass, currentColor icons, compact-mode gated; scene center clear
</success_criteria>

<output>
Create `.planning/phases/59-hud-integration-visible-but-belong/59-04-SUMMARY.md` when done
</output>
