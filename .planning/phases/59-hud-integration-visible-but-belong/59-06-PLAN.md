---
phase: 59-hud-integration-visible-but-belong
plan: 06
type: execute
wave: 6
depends_on: ["59-01", "59-02", "59-03", "59-04", "59-05"]
files_modified:
  - .planning/phases/59-hud-integration-visible-but-belong/59-EVIDENCE.md
autonomous: false
requirements: [D-15]

must_haves:
  truths:
    - "The full glass look + rails + palette + recede are judged together on the live install by the founder"
    - "The evidence record (screenshots + short screen recording of recede/palette motion + anti-slop checklist) is captured"
    - "The full vitest suite is green before the qualitative gate"
  artifacts:
    - path: ".planning/phases/59-hud-integration-visible-but-belong/59-EVIDENCE.md"
      provides: "D-15 evidence record — screenshot/recording links + anti-slop pass/fail checklist"
      contains: "Evidence"
  key_links:
    - from: "founder judgment"
      to: "provisional tunables (HUD_IDLE_TIMEOUT_MS, RECEDE_GHOST_OPACITY, glass/blur values)"
      via: "ratchet-at-checkpoint — values tuned live and locked"
      pattern: "HUD_IDLE_TIMEOUT_MS"
---

<objective>
Run the single closing founder checkpoint (D-15) on the live `recense viz` install: the glass
panel language, the consolidated chip/rails, the ⌘K palette, and the auto-recede feel are judged
together — no mid-phase gate. Capture the evidence record (screenshots + a short screen recording
of recede/palette motion + the anti-slop pass/fail checklist) and ratchet the provisional tunables.

Purpose: D-15 — the phase's one qualitative gate, extending the 57/58 evidence pattern. This is
where the founder tunes idle-timeout, ghost-opacity, blur, and timing values live and locks them.

Output: 59-EVIDENCE.md with the evidence links + anti-slop checklist, and any tuned constant values
committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-hud-integration-visible-but-belong/59-CONTEXT.md
@.planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md

<interfaces>
D-15 Evidence Checklist (from UI-SPEC — the anti-slop pass/fail lines the record MUST include):
- [ ] Glass confined to the D-12 element list — no backdrop-filter on any other surface
- [ ] Zero emoji/pictographic characters anywhere in DOM chrome

Provisional tunables to ratchet live (constants.js): HUD_IDLE_TIMEOUT_MS (4000), RECEDE_GHOST_OPACITY
(0.12, within 0.10-0.15), glass blur radii (8/14px), motion timings, topics-rail expand behavior.

Precedent: Phase 57/58 closed with a founder checkpoint + evidence record; this extends that pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automated gate — full suite green + anti-slop machine checks</name>
  <files>.planning/phases/59-hud-integration-visible-but-belong/59-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md (D-15 Evidence Checklist;
      Anti-Slop Contract enforcement column)
    - tests/viz-activity-palette-invariants.test.ts (the D-14/D-11/D-10 locks)
    - tests/viz-hud-palette.test.ts (the matcher unit test)
  </read_first>
  <action>
    Run the full vitest suite and the two phase-specific test files; confirm all green. Run the
    anti-slop machine checks: (a) grep styles.css confirming backdrop-filter appears ONLY on the D-12
    selector list (chip/rail/topics-rail/#palette/#detail/#settings-panel/#reader/.toast) — the D-14
    test already enforces this, re-run it; (b) grep index.html + JS-built UI strings for
    emoji/pictographic characters and confirm none. Create 59-EVIDENCE.md and record the automated
    results (suite pass count, the two anti-slop machine-check outcomes) as the objective floor before
    the qualitative gate. Leave placeholders for the founder screenshots/recording links + the
    checklist to be filled at Task 2.
  </action>
  <verify>
    <automated>bash -c 'set -e; npm test >/dev/null 2>&1 && echo "suite green"; npx vitest run tests/viz-activity-palette-invariants.test.ts tests/viz-hud-palette.test.ts; EMOJI=$(grep -nP "[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}]" src/viz/index.html || true); if [ -n "$EMOJI" ]; then echo "emoji in chrome DOM:"; echo "$EMOJI"; exit 1; fi; echo ok'</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (full suite green)
    - `npx vitest run tests/viz-activity-palette-invariants.test.ts tests/viz-hud-palette.test.ts` exits 0
    - No emoji/pictographic characters in src/viz/index.html
    - 59-EVIDENCE.md exists with the automated-gate results recorded and placeholders for founder evidence
  </acceptance_criteria>
  <done>Full suite + anti-slop machine checks green; evidence file scaffolded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Founder D-15 checkpoint — live install judgment + evidence capture</name>
  <what-built>
    The complete HUD integration on the live `recense viz` install: Liquid-Glass panel language
    across chip/rails/palette/detail/settings/reader/tooltip (aubergine, hairline specular, no drop
    shadows); the D-01 footprint (top-left status chip, mid-right icon rail absorbing both floating
    corner buttons, left topics rail); the vanilla ⌘K palette (Nodes/Topics/Commands, select-and-go);
    and auto-recede (idle + camera-flight + focus → opacity ghost, instant return on mouse move).
  </what-built>
  <how-to-verify>
    1. Launch the viz: run the `recense viz` command / open the app.
    2. Glass language: confirm chip/rails/palette/detail/settings/reader read as one aubergine glass
       family; no drop shadows on dark; tooltip stays a crisp opaque card (no blur).
    3. Chrome footprint: scene center is clear; chip top-left shows a live SSE dot + node count and a
       legend that folds in on hover; mid-right rail has 5 working icons (magnifier opens the palette,
       reader/settings/corpus/recenter act); left topics rail expands on hover and highlights regions.
    4. Palette: press ⌘K (or Ctrl-K); type a query; confirm Nodes/Topics/Commands sections filter;
       selecting a node closes the palette and flies the camera to it; "Toggle tombstones" and
       "Show event log" commands work; the old always-open search field is gone.
    5. Recede: stop moving the mouse → chrome fades to ghost after ~4s; move → instant return;
       trigger a camera fly-to → chrome recedes during flight; focus a node / open detail → chrome
       drops to ghost immediately; SSE traces firing do NOT cause recede; nothing shifts layout.
    6. Tune live: adjust HUD_IDLE_TIMEOUT_MS, RECEDE_GHOST_OPACITY (0.10-0.15), blur radii, timings,
       and topics-rail expand feel until right; these values are then locked.
    7. Capture evidence: take screenshots (glass look + rails + palette) and a short screen recording
       of recede + palette motion. Record both D-15 anti-slop checklist lines (glass confined to the
       D-12 list; zero emoji in chrome). Add the links + checklist + any tuned constant values to
       59-EVIDENCE.md.
  </how-to-verify>
  <read_first>
    - .planning/phases/59-hud-integration-visible-but-belong/59-EVIDENCE.md (the scaffold from Task 1)
    - src/viz/modules/constants.js (the provisional tunables to ratchet)
    - .planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md (D-15 Evidence Checklist)
  </read_first>
  <acceptance_criteria>
    - 59-EVIDENCE.md contains screenshot reference(s) + a screen-recording reference of recede/palette motion
    - Both D-15 anti-slop checklist lines are recorded as PASS (glass confined to D-12 list; zero emoji)
    - Any tuned tunable values are written back to constants.js and committed (ratchet-at-checkpoint)
    - Founder has approved the combined glass + rails + palette + recede feel
  </acceptance_criteria>
  <resume-signal>Type "approved" once the founder has judged the live install and the evidence record is captured, or describe issues to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Verification + evidence-capture plan; no code path or boundary introduced beyond the tunable ratchet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-07 | Tampering | constant ratchet | accept | Tuned values are founder-authored numeric constants in constants.js; the D-11/D-14 invariants re-run and must stay green after any ratchet. |
| T-59-SC | Tampering | package installs | N/A | Zero new packages. |

Spoofing / Repudiation / Info-Disclosure / DoS / Elevation: N/A.
</threat_model>

<verification>
- `npm test` full suite green before the gate
- Founder approval on the live install; 59-EVIDENCE.md complete with screenshots + recording + checklist
- Any ratcheted constants keep the invariants suite green
</verification>

<success_criteria>
- Combined glass + rails + palette + recede judged and approved by the founder on the live install
- Evidence record captured (screenshots + recording + both anti-slop checklist lines PASS)
- Provisional tunables ratcheted and locked; full suite green
</success_criteria>

<output>
Create `.planning/phases/59-hud-integration-visible-but-belong/59-06-SUMMARY.md` when done
</output>
