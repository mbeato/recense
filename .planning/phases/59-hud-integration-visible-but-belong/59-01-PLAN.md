---
phase: 59-hud-integration-visible-but-belong
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/viz/modules/constants.js
  - src/viz/modules/css-tokens.js
  - src/viz/modules/app.js
  - src/viz/css/styles.css
  - tests/viz-activity-palette-invariants.test.ts
autonomous: true
requirements: [D-11, D-14]

must_haves:
  truths:
    - "Every HUD design token (glass recipe, radii, motion timings) has a single authored home in constants.js"
    - "At viz boot a <style id=\"hud-tokens\"> :root block is injected from constants.js values — styles.css can reference var(--glass-*), var(--radius-*), var(--motion-*)"
    - "The vendored JetBrains Mono font is available to CSS as font-family 'JetBrains Mono HUD'"
  artifacts:
    - path: "src/viz/modules/constants.js"
      provides: "HUD_CSS_TOKENS object + RECEDE/PALETTE/HUD_IDLE scalar exports"
      contains: "HUD_CSS_TOKENS"
    - path: "src/viz/modules/css-tokens.js"
      provides: "emitHudTokens() — builds :root var block from HUD_CSS_TOKENS and injects it"
      exports: ["emitHudTokens"]
    - path: "src/viz/css/styles.css"
      provides: "@font-face for JetBrains Mono HUD"
      contains: "@font-face"
  key_links:
    - from: "src/viz/modules/css-tokens.js"
      to: "src/viz/modules/constants.js"
      via: "ESM import of HUD_CSS_TOKENS (single source of truth)"
      pattern: "import.*HUD_CSS_TOKENS.*constants"
    - from: "src/viz/modules/app.js"
      to: "src/viz/modules/css-tokens.js"
      via: "emitHudTokens() called early in boot, before other init"
      pattern: "emitHudTokens"
---

<objective>
Establish the HUD token vocabulary and the runtime CSS-custom-property emission
mechanism that every downstream Phase-59 CSS plan consumes, plus wire the vendored
JetBrains Mono as a CSS web font.

Purpose: D-14's exhaustive token migration and D-11's machine-checkable motion tokens
both require a single authored home for HUD design values (constants.js) and a
zero-dependency mechanism to surface them as CSS custom properties. This plan proves
that pipeline end-to-end on the glass/radius/motion recipe subset before Plan 02 does
the exhaustive 89-literal migration against it.

Output: constants.js token exports, a new css-tokens.js emission module, an @font-face
rule, app.js boot wiring, and the D-11/D-10 invariants that lock the single-source rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-hud-integration-visible-but-belong/59-CONTEXT.md
@.planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md
@.planning/phases/59-hud-integration-visible-but-belong/59-RESEARCH.md
@.planning/phases/59-hud-integration-visible-but-belong/59-PATTERNS.md

<interfaces>
Token values are pinned in UI-SPEC.md (Glass Construction Recipe, Radius Tokens,
Motion Tokens, Palette Tunables tables). Reproduce those exact values.

Existing single-source-of-truth precedent to mirror (do not reinvent):
- src/viz/server.ts parseSchedulerScalars(modulesRoot) — readFileSync + regex-parse of
  constants.js at boot. This plan adds a JS→CSS consumer of the same authored source,
  via native ESM import (no regex needed — values are already plain JS strings/numbers).
- src/viz/css/styles.css:1190 — the ONLY existing custom property: `:root { --index-width: 270px; }`
  New tokens extend this exact `:root { --name: value; }` shape (injected at runtime).
- constants.js header (lines 1-8) BANS runtime behaviour in that file — the emission
  FUNCTION must live in the new css-tokens.js, importing FROM constants.js (RESEARCH Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define HUD design tokens + scalars in constants.js</name>
  <files>src/viz/modules/constants.js</files>
  <read_first>
    - src/viz/modules/constants.js (full — see the `/** one-line JSDoc */ export const NAME = value;`
      convention, the header ban on runtime behaviour, and the existing KIND_COLOR/scalar layout)
    - .planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md (Glass Construction
      Recipe, Radius Tokens, Motion Tokens, Palette Tunables tables — the exact pinned values)
  </read_first>
  <action>
    Add a new section to constants.js exporting `HUD_CSS_TOKENS` — a plain object mapping
    CSS-var suffix (without the leading `--`) to string value, seeded with the design-system
    recipe from UI-SPEC: glass-bg-ambient=rgba(26,18,32,0.66), glass-bg-focused=rgba(26,18,32,0.90),
    glass-blur-sm=8px, glass-blur-md=14px, glass-border=rgba(170,150,180,0.12),
    glass-border-focused=rgba(140,150,165,0.16), glass-specular=inset 0 1px 0 rgba(255,255,255,0.06),
    radius-xs=4px, radius-sm=7px, radius-md=11px, radius-lg=12px,
    ease-out-soft=cubic-bezier(0.22, 1, 0.36, 1), motion-fast=120ms, motion-base=200ms, motion-slow=220ms,
    recede-ghost-opacity=0.12. Also export individual JS scalars for the modules that import them
    directly: HUD_IDLE_TIMEOUT_MS=4000, RECEDE_GHOST_OPACITY=0.12, PALETTE_DEBOUNCE_MS=200,
    PALETTE_CAP_NODES=8, PALETTE_CAP_TOPICS=6, PALETTE_CAP_COMMANDS=8, PALETTE_Z_INDEX=50,
    PALETTE_BACKDROP_Z_INDEX=49. Each export carries a one-line JSDoc matching the file convention.
    Do NOT add any function or runtime behaviour to constants.js (per its header ban). Keep
    RECEDE_GHOST_OPACITY consistent between the scalar and the HUD_CSS_TOKENS.recede-ghost-opacity
    entry (same 0.12 literal). These are provisional per D-08/D-09 — ratcheted at the D-15 checkpoint.
  </action>
  <verify>
    <automated>node -e "import('./src/viz/modules/constants.js').then(m=>{const t=m.HUD_CSS_TOKENS; if(!t['glass-bg-ambient']||!t['ease-out-soft']||m.HUD_IDLE_TIMEOUT_MS!==4000||m.PALETTE_Z_INDEX!==50){process.exit(1)} console.log('ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - constants.js contains `export const HUD_CSS_TOKENS = {` with keys glass-bg-ambient,
      glass-bg-focused, glass-blur-sm, glass-blur-md, glass-border, glass-border-focused,
      glass-specular, radius-xs, radius-sm, radius-md, radius-lg, ease-out-soft, motion-fast,
      motion-base, motion-slow, recede-ghost-opacity
    - `HUD_CSS_TOKENS['ease-out-soft']` equals the string `cubic-bezier(0.22, 1, 0.36, 1)`
    - constants.js contains `export const HUD_IDLE_TIMEOUT_MS = 4000` and `export const PALETTE_Z_INDEX = 50`
    - No function declaration was added to constants.js (grep for `function ` inside the new
      section returns nothing; only `export const`)
  </acceptance_criteria>
  <done>All HUD tokens + scalars exported from constants.js; node import smoke check exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: css-tokens.js emission module + @font-face + app.js boot wiring</name>
  <files>src/viz/modules/css-tokens.js, src/viz/css/styles.css, src/viz/modules/app.js</files>
  <read_first>
    - src/viz/modules/app.js (init wiring block ~lines 232-246 and the import block ~lines 23-37 —
      where emitHudTokens must be imported and called BEFORE the initX chain)
    - src/viz/modules/constants.js (the HUD_CSS_TOKENS export added in Task 1)
    - src/viz/css/styles.css (top of file + line 1190 `--index-width` — the one existing custom
      property, and confirm the vendor font path relative to css/)
    - src/viz/modules/labels.js (~line 46 — confirms the vendored font lives at
      src/viz/vendor/fonts/JetBrainsMono-Regular.ttf; @font-face src is `../vendor/fonts/...`
      relative to css/styles.css)
    - .planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md (Typography section —
      the exact @font-face block and font-family name 'JetBrains Mono HUD')
  </read_first>
  <action>
    Create src/viz/modules/css-tokens.js exporting `emitHudTokens()`: import HUD_CSS_TOKENS from
    constants.js, build a `:root { --KEY: VALUE; ... }` string by iterating the object (prefix each
    key with `--`), create a `<style id="hud-tokens">` element, set its textContent to that string,
    and append it to document.head. Guard against double-injection (if `document.getElementById('hud-tokens')`
    exists, return). Give the module a header JSDoc matching the repo convention, noting it is the
    JS→CSS consumer of constants.js's single authored source (mirrors server.ts parseSchedulerScalars,
    but via native import — no regex). Then add the JetBrains Mono @font-face rule to the TOP of
    styles.css exactly as pinned in UI-SPEC (font-family: 'JetBrains Mono HUD'; src url
    ../vendor/fonts/JetBrainsMono-Regular.ttf format truetype; font-weight 400; font-display swap).
    Finally wire app.js: import emitHudTokens and call it at the very start of the boot sequence
    (before initStats), so the :root vars exist before any styled DOM renders.
  </action>
  <verify>
    <automated>grep -q "id=\"hud-tokens\"" src/viz/modules/css-tokens.js && grep -q "emitHudTokens" src/viz/modules/app.js && grep -q "@font-face" src/viz/css/styles.css && grep -q "JetBrains Mono HUD" src/viz/css/styles.css && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - css-tokens.js contains `export function emitHudTokens` and `import { HUD_CSS_TOKENS }` (or
      equivalent named import) from `./constants.js`
    - css-tokens.js builds a string containing `:root {` and `--glass-bg-ambient:` and appends a
      `<style id="hud-tokens">` to document.head, guarded against double-injection
    - styles.css contains an `@font-face` block with `font-family: 'JetBrains Mono HUD'` and
      `src: url('../vendor/fonts/JetBrainsMono-Regular.ttf')`
    - app.js imports emitHudTokens and calls `emitHudTokens()` before `initStats(ctx)`
    - `npm test` still passes (no existing suite broken)
  </acceptance_criteria>
  <done>Boot injects the :root token block from constants.js; @font-face wired; app.js calls emitHudTokens first.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-11 motion-token + D-10 single-source invariants</name>
  <files>tests/viz-activity-palette-invariants.test.ts</files>
  <read_first>
    - tests/viz-activity-palette-invariants.test.ts (full — copy the readConstantsJs/stripComments
      source-parse harness and the describe/it/expect + regex assertion style of the existing
      D-10/D-02 blocks; this file is EXTENDED, not replaced)
    - src/viz/modules/constants.js (HUD_CSS_TOKENS + scalars from Task 1)
    - src/viz/modules/css-tokens.js (emission — the JS→CSS single source of truth)
    - .planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md (Motion Tokens table —
      the values the D-11 lock asserts)
  </read_first>
  <behavior>
    - Test D-11-A: HUD_CSS_TOKENS in constants.js contains ease-out-soft, motion-fast, motion-base,
      motion-slow keys, and ease-out-soft === 'cubic-bezier(0.22, 1, 0.36, 1)' (no bounce/overshoot)
    - Test D-11-B: css-tokens.js imports HUD_CSS_TOKENS from constants.js (single source) and
      emits a `--` prefixed :root block — it does NOT re-declare any token value as its own literal
    - Test D-10-C: styles.css does NOT contain a hardcoded `:root { ... }` block redefining any
      glass/radius/motion token value (values flow only through the runtime-injected hud-tokens
      style) — the sole permitted literal custom property remains --index-width
  </behavior>
  <action>
    Add a `describe('D-11 shared motion tokens', ...)` and `describe('D-10 HUD token single source', ...)`
    to the existing invariants file, following the exact source-parse harness already present
    (readFileSync + regex over source text). Add a readCssTokensJs() and readStylesCss() sibling
    reader alongside the existing readConstantsJs(). Assert the D-11-A/B and D-10-C behaviors above.
    For D-10-C, scan styles.css for `--glass-`, `--radius-`, `--motion-`, `--ease-` custom-property
    DECLARATIONS (a `--name:` at declaration position inside a `:root`/selector block) and assert
    none exist as authored literals — they are runtime-injected only; permit `--index-width` as the
    one grandfathered exception. Do NOT write the D-14 CSS color-literal scan here (that lands in
    Plan 02 with the migration). Reuse the stripComments() helper.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-activity-palette-invariants.test.ts -t "D-11"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/viz-activity-palette-invariants.test.ts` exits 0
    - The file contains `describe('D-11` and `describe('D-10 HUD token single source`
    - The D-11 block asserts ease-out-soft equals `cubic-bezier(0.22, 1, 0.36, 1)`
    - The D-10 block fails if a `--glass-`/`--radius-`/`--motion-` value is hardcoded in styles.css
      (verify by temporarily confirming the assertion targets styles.css text, not constants.js)
    - The full existing suite in this file (D-10 scheduler, D-02, D-06, D-15) still passes
  </acceptance_criteria>
  <done>D-11 motion-token + D-10 single-source invariants green; existing locks unbroken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viz server → browser | Loopback-bound, read-only static asset + JSON server; no user accounts, no writes. This plan adds no new boundary — pure client-side constants + a runtime <style> injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Tampering | css-tokens.js <style> injection | accept | Token values are hardcoded constants authored in-repo (constants.js), never user/server-sourced strings — no injection vector. textContent (not innerHTML) is used to set the style block. |
| T-59-SC | Tampering | package installs | N/A | Zero new packages this phase (RESEARCH Package Legitimacy Audit) — no supply-chain surface. |

Spoofing / Repudiation / Info-Disclosure / DoS / Elevation: N/A — local loopback presentation-layer, no auth, no persistence, no external calls introduced.
</threat_model>

<verification>
- `npx vitest run tests/viz-activity-palette-invariants.test.ts` green (D-11 + D-10 + all prior locks)
- `npm test` full suite green (this plan touches shared constants.js)
- Manual: load `recense viz`, confirm a `<style id="hud-tokens">` appears in <head> with --glass-* vars
</verification>

<success_criteria>
- HUD_CSS_TOKENS + scalars exported from constants.js (single authored source)
- emitHudTokens() injects the :root var block at boot; @font-face wired for JetBrains Mono HUD
- D-11 motion-token and D-10 single-source invariants machine-locked and green
</success_criteria>

<output>
Create `.planning/phases/59-hud-integration-visible-but-belong/59-01-SUMMARY.md` when done
</output>
