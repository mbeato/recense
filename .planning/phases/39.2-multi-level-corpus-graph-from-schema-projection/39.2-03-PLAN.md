---
phase: 39.2-multi-level-corpus-graph-from-schema-projection
plan: 03
type: execute
wave: 3
depends_on: ["39.2-01", "39.2-02", "39.2-04"]
files_modified:
  - src/viz/server.ts
  - src/viz/modules/corpus.js
autonomous: false
requirements: [DOC-04]
must_haves:
  truths:
    - "The /graph?type=doc payload includes node_scope.scope per doc node (one additive LEFT JOIN, near-free)"
    - "All docs render on Corpus open; the ~200 UUID schema-chapter docs are visible but dimmed (faded/desaturated), not hidden (D-13)"
    - "Containment, reference, and cross-project reference edges are visually distinct (arrowed/heavy vs thin/dashed vs cross-project accent) (D-14)"
    - "Layout is force-directed with project hubs as gravity anchors so each project's subjects pool around their hub (D-15)"
    - "Nodes are colored by project scope via generative hue rotation bounded to the muted rose/slate/mauve band; amber stays reserved for activation/hover only (D-16/D-17)"
    - "Cross-project reference edges visibly span two color regions"
    - "The reader related/backlinks panel surfaces the richer projection edges (D-18, near-free via existing stmtDocBacklinks)"
  artifacts:
    - path: "src/viz/server.ts"
      provides: "stmtDocNodes enriched with ns.scope"
      contains: "node_scope ns"
    - path: "src/viz/modules/corpus.js"
      provides: "scope hue rotation, dimmed UUID chapters, cross-project edge treatment, hub-anchored force"
      contains: "scopeColor"
  key_links:
    - from: "src/viz/server.ts"
      to: "node_scope.scope"
      via: "LEFT JOIN node_scope in stmtDocNodes"
      pattern: "LEFT JOIN node_scope"
    - from: "src/viz/modules/corpus.js"
      to: "node.scope"
      via: "scopeColor(node.scope) in nodeCanvasObject"
      pattern: "scopeColor"
---

<objective>
Foreground the new doc->doc graph in the 2D corpus reader: enrich `/graph?type=doc` with per-doc `scope`, then drive scope-grouped node coloring (D-16/D-17), dimmed UUID chapter docs (D-13), three visually-distinct edge types (D-14), and hub-anchored force layout (D-15) in `corpus.js`. The richer projection edges (Plan 01) flow into the reader backlinks panel for free (D-18).

Purpose: The deriver (Plan 01) produces a real multi-level graph, but today the corpus viz paints every node one color and cannot show the structure or cross-project bridges. This plan makes the structure legible.

Output: one additive SQL change in `server.ts` + presentation logic in `corpus.js`. No engine change.

This plan is `autonomous: false` — it ends with a human-verify checkpoint (visual rendering of color/dimming/edge-distinction/anchoring cannot be auto-asserted).

Wave/dependency note: this plan is Wave 3, depending on 39.2-01 (deriver), 39.2-02 (deriver wired into the sleep pass + promoter retired = the live edge writer), AND 39.2-04 (the `derive-doc-graph` backfill CLI). The human-verify checkpoint (Task 3) requires real `doc_reference`/`doc_containment` edges to inspect; those only exist after the Plan 04 CLI (`dist/adapter/derive-doc-graph-cli.js`) runs against the live DB (or a full sleep pass executes via Plan 02's wiring). Tasks 1 and 2 (server.ts + corpus.js, source-only) do not strictly need the edges populated, but the checkpoint does — hence the serialized Wave 3 placement so the binary exists and the edges are real when a human evaluates the viz.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.2-multi-level-corpus-graph-from-schema-projection/39.2-CONTEXT.md
@.planning/phases/39.2-multi-level-corpus-graph-from-schema-projection/39.2-PATTERNS.md

<interfaces>
<!-- server.ts: stmtDocNodes (L155-162) selects per-doc columns; rows flow straight to the
     /graph?type=doc payload as NodeRecord[] (L381) — adding ns.scope to the SELECT surfaces
     `scope` on each node object automatically (no mapping change). stmtDocLinks (L166-171)
     already returns doc_link + doc_containment + doc_reference; links carry { source, target,
     rel, w, kind } (L389-395). stmtDocBacklinks (L245-255) already reads the doc edges. -->

<!-- corpus.js existing constants (L37-44):
     REST_NODE '#9c7080' (uniform doc color, to be replaced by scope tint)
     HOVER_NODE '#ffb866' (AMBER — activation/hover ONLY, founder-locked; auto-tints must NOT collide)
     LINK_REST 'rgba(130,105,140,0.35)' (doc_link + doc_reference base)
     CONTAINMENT_COLOR 'rgba(110,90,130,0.70)' (containment spine)
   existing accessors: nodeCanvasObject (L144-168), linkColor (L177-191),
     linkLineDash (L192: doc_reference -> [2,2]); d3Force retune block (L203-254). -->

<!-- Plan 04 backfill CLI (dist/adapter/derive-doc-graph-cli.js):
     `node dist/adapter/derive-doc-graph-cli.js --db <live-db-path>` runs the deriver once
     against the live DB and writes real doc_reference + doc_containment edges (JSON counts to
     stdout). `--dry-run` computes counts without writing. This is what populates the edges the
     Task 3 checkpoint inspects. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich stmtDocNodes with node_scope.scope (server.ts)</name>
  <read_first>
    - src/viz/server.ts (the file being modified) — stmtDocNodes L155-162, the qType==='doc' node-load path L379-396 (confirm rows pass straight through to the payload)
    - src/db/schema.ts — node_scope sidecar DDL (node_id, scope) to confirm the join column
    - PATTERNS.md "viz/server.ts (MODIFY)" section (the exact stmtDocNodes modification)
  </read_first>
  <action>
    In `src/viz/server.ts`, modify `stmtDocNodes` (L155-162) to add `LEFT JOIN node_scope ns ON ns.node_id = n.id` and add `ns.scope` to the SELECT column list (after the `label` column). LEFT JOIN (not inner) so a doc without a node_scope row still renders (scope becomes null -> client falls back to the default tint). Make NO other change: `stmtDocLinks` and `stmtDocBacklinks` already return the full doc-edge set and pick up the deriver's richer rows automatically (D-18 is free). Do not touch the `NodeRecord`/payload mapping — the extra column flows through `stmtDocNodes.all()` into the JSON payload unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const s=require('fs').readFileSync('src/viz/server.ts','utf8'); process.exit(/LEFT JOIN node_scope ns/.test(s) && /ns\.scope/.test(s) ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "LEFT JOIN node_scope ns" src/viz/server.ts` returns the new join inside stmtDocNodes.
    - `grep -n "ns.scope" src/viz/server.ts` shows `scope` added to the stmtDocNodes SELECT.
    - `stmtDocLinks` and `stmtDocBacklinks` are unchanged.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The /graph?type=doc payload carries per-doc `scope`; stmtDocLinks/stmtDocBacklinks untouched; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Scope coloring + dimmed UUID chapters + cross-project edges + hub-anchored force (corpus.js)</name>
  <read_first>
    - src/viz/modules/corpus.js (the file being modified) — color constants L37-44, nodeCanvasObject L144-168, linkColor L177-191, linkLineDash L192, the d3Force retune block L203-254
    - .planning/phases/39.2-multi-level-corpus-graph-from-schema-projection/39.2-CONTEXT.md decisions D-13 through D-17 + the Phase-15 founder-locked muted-palette + amber-only-activation constraint
    - PATTERNS.md "viz/modules/corpus.js (MODIFY)" section (scopeColor rotation, dimmed-chapter alpha, cross-project linkColor/linkLineDash, CROSS_PROJECT_REF constant)
  </read_first>
  <action>
    In `src/viz/modules/corpus.js`:
    (1) D-16/D-17 scope coloring: add a `scopeColor(scope)` helper backed by a module-level `scopeColorCache` Map. Assign each new scope a hue via generative rotation: `hue = (SCOPE_HUE_START + idx * SCOPE_HUE_STEP) % 360` with named constants `SCOPE_HUE_START` (start in the mauve/rose region, ~300), `SCOPE_HUE_STEP` (~53, prime-ish to avoid early repeats), `SCOPE_SAT` (~28% — muted), `SCOPE_LIGHT` (~52% — mid). GUARDRAIL: choose the constants so generated `hsl()` tints stay in the founder-locked muted rose/slate/mauve band and NEVER land on warm amber (amber `#ffb866` ~ hue 33 is reserved for activation/hover); verify no rotation step produces a hue in the warm 20-50 range for the first ~8 scopes, or explicitly skip that band. Null/empty scope -> return the existing `REST_NODE` fallback.
    (2) D-13 dimmed chapters: in `nodeCanvasObject` (L144-168), detect UUID schema-chapter docs via a `UUID_RE` regex against `node.slug`; set `canvasCtx.globalAlpha = isChapterDoc ? 0.35 : 1.0` around the fill, and restore `globalAlpha = 1.0` after. Replace the uniform `REST_NODE` fill with `isHover ? HOVER_NODE : scopeColor(node.scope)` — hover/selected stays amber (founder-locked), rest takes the scope tint. Chapters remain VISIBLE (dimmed), not hidden.
    (3) D-14 cross-project edges: extend `linkColor` (L177-191) — for `kind==='doc_reference'`, read `link.source.scope` / `link.target.scope` (force-graph mutates source/target to node objects after layout init; guard for the pre-layout string form) and if both exist and differ, return a new `CROSS_PROJECT_REF` constant (muted cool blue, e.g. `'rgba(140,170,200,0.55)'`) instead of `LINK_REST`. Keep containment -> `CONTAINMENT_COLOR` and the existing amber-highlight-on-containment behavior. Extend `linkLineDash` (L192) so cross-project `doc_reference` uses a longer dash (`[4,3]`) vs same-project reference (`[2,2]`); containment/doc_link stay solid (null).
    (4) D-15 hub anchoring: in the d3Force retune block (L203-254), add gravity toward hub nodes. Identify hub nodes (slug present, no ':' , not a UUID). Add or tune a positioning force so each project's subjects pool around their hub — either a per-scope `forceX`/`forceY` cluster pull keyed on hub position, or strengthen the link force for hub->subject containment edges so hubs act as anchors. Keep the existing collision + center forces; do not regress the loose-charge fix (charge ~-35). Document the chosen anchoring approach in a comment.
    Keep all changes presentation-only — no data writes, no engine calls.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/viz/modules/corpus.js','utf8'); const ok=/scopeColor/.test(s)&&/CROSS_PROJECT_REF/.test(s)&&/UUID_RE/.test(s)&&/globalAlpha/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "scopeColor\|SCOPE_HUE_START\|scopeColorCache" src/viz/modules/corpus.js` shows the hue-rotation helper + cache.
    - `grep -n "CROSS_PROJECT_REF" src/viz/modules/corpus.js` shows the cross-project edge constant, referenced in both linkColor and linkLineDash.
    - `grep -n "UUID_RE\|globalAlpha" src/viz/modules/corpus.js` shows the dimmed-chapter logic in nodeCanvasObject.
    - `HOVER_NODE` (amber) is still used ONLY for hover/selected/highlight — no auto-generated scope tint references amber.
    - No new npm dependency added (net-zero deps); `git diff --stat package.json` shows no change.
  </acceptance_criteria>
  <done>Scope-tinted nodes, dimmed-but-visible UUID chapters, three distinct edge treatments, hub-anchored force; amber reserved for activation; net-zero deps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    server.ts now serves per-doc `scope` on /graph?type=doc; corpus.js paints nodes by project-scope hue (muted band), dims (but shows) the ~200 UUID schema-chapter docs, renders containment vs reference vs cross-project-reference edges distinctly, and anchors the force layout around project hubs. The reader backlinks panel automatically shows the richer projection edges.
  </what-built>
  <how-to-verify>
    1. BACKFILL FIRST (required — the live DB must have real edges before the viz can show structure). Plan 04 is a dependency of this plan, so `dist/adapter/derive-doc-graph-cli.js` exists. From the repo root, run `npm run build` (ensure dist is current), then `node dist/adapter/derive-doc-graph-cli.js --db <live-db-path>` and confirm the stdout JSON reports nonzero `containment` and `reference` counts. (Optionally run with `--dry-run` first to preview counts without writing.) If counts come back zero, STOP and report — the deriver/wiring (Plan 02) or the backfill is not producing edges, and the viz check below would be meaningless.
    2. Start the viz server and open the Corpus view in the browser (the 2D #corpus-graph).
    3. Confirm: (a) each project's nodes share one muted tint and different projects have visibly different tints; (b) UUID schema-chapter docs are visible but faded/dimmed; (c) containment edges read as a directed/heavier hierarchy, plain reference edges are thin dashed, and cross-project reference edges have a distinct (cool-blue, longer-dash) treatment that visibly spans two color regions; (d) project hubs anchor their subject pools; (e) amber appears ONLY on hover/selection, never as a node's resting tint.
    4. Open a doc in the reader and confirm the related/backlinks panel lists more related docs than before (D-18).
  </how-to-verify>
  <resume-signal>Type "approved" or describe rendering issues (wrong tint band, amber bleed, indistinguishable edges, no hub anchoring, chapters hidden instead of dimmed), or report zero backfill counts.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local DB -> viz HTTP payload | server.ts reads local SQLite and serves JSON to the local-only viz client. Additive read-only column (scope); no new write path, no new input crosses inward. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.2-09 | Information Disclosure | /graph?type=doc payload | accept | Adds node_scope.scope (a project-name string) to an already local-only, single-tenant viz endpoint. No PII/secret; same trust level as existing slug/label fields. |
| T-39.2-10 | Tampering | corpus.js color/edge logic | accept | Presentation-only client logic; no DB write, no engine call. Cannot corrupt graph-as-truth. |
| T-39.2-11 | Spoofing (brand drift) | auto-generated scope tints | mitigate | Hue-rotation constants bounded to the muted rose/slate/mauve band; amber explicitly reserved for activation — acceptance criteria assert no auto-tint collides with amber (founder-locked palette, Phase 15). |

No package installs (net-zero deps) — no supply-chain checkpoint required.
</threat_model>

<verification>
- `npx tsc --noEmit` — server.ts compiles.
- grep gates: LEFT JOIN node_scope + ns.scope (server.ts); scopeColor + CROSS_PROJECT_REF + UUID_RE + globalAlpha (corpus.js).
- `git diff --stat package.json` empty (net-zero deps).
- Backfill via the Plan 04 CLI produces nonzero edge counts before the visual check.
- Human-verify checkpoint confirms the visual: scope tints, dimmed chapters, distinct edges, hub anchoring, amber discipline, richer backlinks.
</verification>

<success_criteria>
- /graph?type=doc carries per-doc scope (additive LEFT JOIN).
- Nodes scope-colored in the muted band; UUID chapters dimmed-but-visible; containment/reference/cross-project edges distinct; hubs anchor the layout.
- Reader backlinks surface the richer projection edges (free via stmtDocBacklinks).
- Amber reserved for activation; net-zero deps; human verification passes against real backfilled edges.
</success_criteria>

<output>
Create `.planning/phases/39.2-multi-level-corpus-graph-from-schema-projection/39.2-03-SUMMARY.md` when done.
</output>
</content>
