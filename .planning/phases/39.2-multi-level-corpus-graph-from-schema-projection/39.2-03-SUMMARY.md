---
phase: 39.2-multi-level-corpus-graph-from-schema-projection
plan: "03"
subsystem: viz
tags: [viz, corpus-reader, scope-coloring, edge-styling, force-layout, declutter]
dependency_graph:
  requires: [DocGraphDeriver, doc_reference edges, doc_containment edges]
  provides: [scope-grouped node coloring, 3 distinct edge types, hub-anchored layout, chapter-hide toggle]
  affects: [src/viz/server.ts, src/viz/modules/corpus.js]
tech_stack:
  added: []
  patterns: [d3-force retune, generative hue rotation within muted band, nodeVisibility/linkVisibility filtering]
key_files:
  created: []
  modified:
    - src/viz/server.ts
    - src/viz/modules/corpus.js
decisions:
  - "D-16/D-17 scope coloring: only RECOGNIZED project scopes (derived from subject docs) get a hue tint; chapter/isolated/malformed-scope docs fall back to muted rose REST_NODE (founder override during human-verify — no random per-scope colors)"
  - "D-13 OVERRIDDEN by founder: chapter docs HIDDEN by default (the all-shown-dimmed default was unreadable — ~160 hazed chapters drowned the skeleton). A 'Show chapter docs' toggle (off by default) reveals them; hidden via nodeVisibility/linkVisibility so positions stay pinned"
  - "Chapter toggle docked left of the top-right button column (right:54px) so it does not render beneath it"
  - "Three edge types kept visually distinct (D-14): containment arrowed/heavy spine, same-project reference thin [2,2] mauve, cross-project reference [4,3] cool blue"
metrics:
  completed: "2026-06-23"
  tasks_completed: 3
  files_created: 0
---

# Phase 39.2 Plan 03: Viz Foregrounding Summary

Foregrounds the doc→doc graph in the 2D corpus reader: per-doc `scope` in the `/graph` payload, scope-grouped coloring, three distinct edge types, hub-anchored force layout, and (per founder human-verify feedback) a chapter-hide toggle. The richer projection edges also flow into the reader backlinks panel for free (D-18).

## What Was Built

- **`src/viz/server.ts`** — added `ns.scope` to `stmtDocNodes` (`/graph?type=doc` now serves per-node `scope`); the staleness/backlinks routes already read the new doc edges (D-18).
- **`src/viz/modules/corpus.js`** —
  - `scopeColor()` generative hue rotation within the founder-locked muted rose/slate/mauve band (amber reserved for hover/activation only, D-17). Project-scope-keyed tints (D-16), with the **muted-rose fallback** for non-project docs.
  - Three visually-distinct edge types (D-14) + hub-anchored force layout (D-15).
  - **Chapter-hide-by-default toggle** (founder override of D-13) with `nodeVisibility`/`linkVisibility` filtering and visible-node-filtered zoom-to-fit.

## Human-Verify Checkpoint (Task 3)

Approved by the founder against the live brain after the content rebuild (Plan 05) populated real `subject-schema-ids`. Live result: 3 hubs → 18 subjects → 40 parented chapters, cross-project bridges, three project tints. Iterative founder feedback during the checkpoint produced the chapter toggle, toggle repositioning, and the project-only coloring rule (all captured above).

## Commits
- `cc14da9` server.ts scope enrichment
- `c0a317e` corpus.js coloring + edges + layout
- `2451716` hide chapter docs by default + toggle
- `f… ` toggle reposition; project-only scope coloring

## Self-Check: PASSED
