---
phase: 39.2-multi-level-corpus-graph-from-schema-projection
plan: "04"
subsystem: adapter
tags: [cli, backfill, doc-graph, deriver]
dependency_graph:
  requires: [DocGraphDeriver]
  provides: [recense derive-doc-graph CLI]
  affects: [src/adapter/recense.ts]
tech_stack:
  added: []
  patterns: [lock discipline (validate db path before acquireLock), require.main guard, JSON stdout]
key_files:
  created:
    - src/adapter/derive-doc-graph-cli.ts
  modified:
    - src/adapter/recense.ts
decisions:
  - "Structural copy of promote-corpus-cli.ts (WR-02 lock discipline: validate DB path before acquireLock; release in finally)"
  - "--dry-run reports {containment, reference} counts without mutating (deriver dryRun path)"
  - "Registered as 'derive-doc-graph' subcommand adjacent to promote-corpus"
metrics:
  duration: "~10m"
  completed: "2026-06-23"
  tasks_completed: 2
  files_created: 1
---

# Phase 39.2 Plan 04: Backfill CLI Summary

`recense derive-doc-graph [--db <path>] [--dry-run]` — manual one-shot entry point (D-19) that runs the `DocGraphDeriver` (Plan 01) against the live brain so the projection can be backfilled and inspected immediately, without waiting for an organic sleep pass.

## What Was Built

- **`src/adapter/derive-doc-graph-cli.ts`** (NEW) — validates the DB path before `acquireLock` (WR-02), constructs `DocGraphDeriver(db, store, config, realClock)`, calls `deriveDocGraph({ dryRun })`, emits `{containment, reference, dryRun}` JSON to stdout, releases the lock in `finally`. `require.main === module` guard for test isolation.
- **`src/adapter/recense.ts`** — `case 'derive-doc-graph': spawnScript('derive-doc-graph-cli.js', …)` + help-string entry.

## Verification (live)

Run against the live brain (`~/.config/recense/recense.db`) during the human-verify session produced `{"containment":59,"reference":3}` once subjects carried real schema-ids — proving the full Plan 01→04 pipeline on real data. `--dry-run` reports counts without writing.

## Commits
- `d1e9206` feat(39.2-04): add derive-doc-graph-cli.ts (lock + dry-run + JSON output)
- `49fc75b` feat(39.2-04): register derive-doc-graph subcommand in recense.ts

## Self-Check: PASSED

(SUMMARY reconstructed post-hoc — the worktree copy did not survive the wave merge; both feature commits are in history.)
