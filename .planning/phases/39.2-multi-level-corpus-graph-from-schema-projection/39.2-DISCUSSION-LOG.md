# Phase 39.2: Multi-Level Corpus Graph from Schema Projection - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-23
**Phase:** 39.2-multi-level-corpus-graph-from-schema-projection
**Areas discussed:** Reference signal & density, Multi-level containment, Junk-doc handling, Viz foregrounding (+ node coloring)

---

## Reference signal & density

| Option | Description | Selected |
|--------|-------------|----------|
| Discrete shared-members | Expand subject-schema-ids → schema members; weight by shared gated facts/entities | ✓ |
| Reuse centroid-cosine | Extend corpus-promoter's 0.55 cosine ladder over subject centroids | |
| Both: discrete primary + cosine backfill | Discrete primary, cosine adds edges where members don't overlap | |

**User's choice:** Discrete shared-members.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — additive second signal | schema_rel ≥0.72 / shared super-schema also creates/boosts a reference edge | ✓ |
| No — shared-members only | Reserve the abstracts ladder purely for containment | |

**User's choice:** Yes — abstraction-ladder adjacency as an additive second reference signal.

| Option | Description | Selected |
|--------|-------------|----------|
| IDF down-weight + top-K per doc | Inverse-frequency weight kills ubiquitous-entity hairball; keep top-K per doc | ✓ |
| Min-overlap threshold only | Single global threshold, no per-doc cap | |
| IDF + threshold + top-K | All three (belt-and-suspenders) | |

**User's choice:** IDF down-weight + top-K per doc.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — same rule, IDF handles it | Cross-project edges via identical mechanism; IDF suppresses generic exhaust | ✓ |
| Yes, but higher bar cross-project | Stricter min-score across project boundaries | |
| No — within-project only | Defer cross-project linking | |

**User's choice:** Yes — same rule, IDF handles it.

**Notes:** Reference symmetry (union vs mutual top-K) accepted as a research-flagged default = union.

---

## Multi-level containment

| Option | Description | Selected |
|--------|-------------|----------|
| Abstraction-ladder position | Doc whose schemas sit higher in the abstracts ladder = parent; mass only as fallback | ✓ |
| Mass-gap (reuse existing) | corpus-promoter's larger-mass = parent, gap ≥2 | |
| Both: ladder primary, mass fallback | Ladder where it exists, mass for the rest | |

**User's choice:** Abstraction-ladder position.

| Option | Description | Selected |
|--------|-------------|----------|
| True multi-parent (DAG) | Multiple doc_containment edges; corpus is a DAG | ✓ |
| Single primary home + reference | One containment parent; extra memberships as references | |

**User's choice:** True multi-parent (DAG) — matches roadmap "non-strict/many-to-many."

| Option | Description | Selected |
|--------|-------------|----------|
| Hub indexes every subject | hub→subject for ALL subjects + subject→subject depth on top | ✓ |
| Hub indexes only top-level | Hub contains only top-level subjects; nested reached via parent | |

**User's choice:** Hub indexes every subject (complete flat index).

**Notes:** Subject→subject ancestry rule (ALL vs majority vs ANY) accepted as a research-flagged default = strict ALL.

---

## Junk-doc handling

| Option | Description | Selected |
|--------|-------------|----------|
| New-taxonomy only | Project over hubs + scope:name subjects; exclude UUID chapters | |
| All docs, viz filters | Derive edges over all live docs; viz does the filtering | ✓ |
| Exclude UUID chapters AND empty-body | Strictest — only filled new-taxonomy docs | |

**User's choice:** All docs, viz filters.

| Option | Description | Selected |
|--------|-------------|----------|
| New deriver owns all | DocGraphDeriver wipe-rebuilds ALL doc_reference/doc_containment; retire corpus-promoter's old path | ✓ |
| Coexist with corpus-promoter | New deriver only adds subject↔subject edges; old path stays | |

**User's choice:** New deriver owns all (doc_link untouched).

---

## Viz foregrounding

| Option | Description | Selected |
|--------|-------------|----------|
| Hubs+subjects, chapters hidden | Chapters behind a toggle, off by default | |
| Everything shown, chapters dimmed | All docs render; UUID chapters de-emphasized, no hidden state | ✓ |

**User's choice:** Everything shown, chapters dimmed.

| Option | Description | Selected |
|--------|-------------|----------|
| All three distinct | Containment / reference / cross-project each styled distinctly | ✓ |
| Containment vs reference only | Cross-project looks like within-project reference | |
| Weight/opacity only | One style, varying by weight | |

**User's choice:** All three edge types distinct.

| Option | Description | Selected |
|--------|-------------|----------|
| Force-directed, hubs anchored | Hubs as gravity anchors; projects pool, bridges stretch | ✓ |
| Force-directed, free | No special anchoring | |
| Keep current layout | Reuse unchanged | |

**User's choice:** Force-directed, hubs anchored.

### Node coloring (user-raised redirect)

| Option | Description | Selected |
|--------|-------------|----------|
| Project scope | Each hub + its subjects share one hue from node_scope; stable | ✓ |
| Graph communities (topic) | Community detection colors clusters; can shift between passes | |
| Containment subtree | Color tracks hierarchy; ambiguous under multi-parent DAG | |

**User's choice:** Project scope.

| Option | Description | Selected |
|--------|-------------|----------|
| Curated muted ramp | Hand-picked muted tints; best for 2-3 scopes | |
| Generative hue rotation | Programmatic hue rotation; scales to many projects | ✓ |

**User's choice:** Generative hue rotation (guardrail: constrained muted band, amber reserved).

**Notes:** User redirected from the proposed algorithm-detail gray areas to node coloring — "all doc nodes are the same color right now; I like different neighborhoods having a different color." Aligns with the standing brain note to match node colors to the in-app palette.

---

## Claude's Discretion

- Deriver insertion point (Phase C, after corpusPromoter.promote()), atomic wipe-and-rebuild discipline.
- IDF document-frequency scope, exact top-K value, adjacency-vs-shared-member weight balance, edge-weight normalization.
- Containment-vs-reference de-dup between the same pair.
- Whether to store shared-member count / IDF score as edge weight for viz opacity.

## Deferred Ideas

- All corpus-CONTENT quality work (empty bodies, exhaust naming, exhaust-judge gate, killing ~200 UUID chapter docs) — later corpus-content phase.
- Non-default alternatives: reference symmetry = mutual-only; ancestry rule = majority/ANY (researcher may switch).
- Graph-community / topic coloring (vs project-scope) — deferred, color-stability concern.
- Markdown export, clustering at 100+ docs, tonos ingestion — pre-existing backlog.
