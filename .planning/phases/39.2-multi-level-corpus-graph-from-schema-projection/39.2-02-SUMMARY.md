---
phase: 39.2-multi-level-corpus-graph-from-schema-projection
plan: "02"
subsystem: consolidation/corpus-promotion
tags: [doc-graph, single-ownership, D-11, D-20, refactor, wiring]
dependency_graph:
  requires: ["39.2-01"]
  provides: ["39.2-03", "39.2-04"]
  affects: [corpus-promoter, consolidator, run-sleep-pass]
tech_stack:
  added: []
  patterns: [D-11 single-ownership, D-20 Phase C ordering, Noop-last DI param]
key_files:
  modified:
    - src/consolidation/corpus-promoter.ts
    - src/consolidation/consolidator.ts
    - src/consolidation/run-sleep-pass.ts
decisions:
  - "Retired all three legacy doc-edge writers in corpus-promoter.ts (D-11 single-ownership)"
  - "DocGraphDeriver wired as last defaulted Noop constructor param in Consolidator"
  - "Phase C order: promote() -> deriveDocGraph() -> reflect() (D-20)"
metrics:
  duration: "~8 minutes"
  completed: "2026-06-23"
  tasks_completed: 3
  files_modified: 3
---

# Phase 39.2 Plan 02: Retire Legacy Doc-Edge Writers + Wire DocGraphDeriver Summary

**One-liner:** Retired three legacy doc_containment/doc_reference edge writers from corpus-promoter.ts (D-11 single-ownership) and wired DocGraphDeriver into Consolidator Phase C between promote() and reflect() (D-20).

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Retire centroid-cosine doc-edge derivation + SubjectPromoter hub→subject containment | 9e6f309 | src/consolidation/corpus-promoter.ts |
| 2 | Wire DocGraphDeriver into Consolidator Phase C (DI param + invocation, D-20) | 28dea13 | src/consolidation/consolidator.ts |
| 3 | Construct + pass DocGraphDeriver in run-sleep-pass.ts | 6fd2ff9 | src/consolidation/run-sleep-pass.ts |

## What Was Built

### Task 1 — corpus-promoter.ts
Retired all three doc-edge writers from corpus-promoter.ts:

1. **promote() centroid-cosine ladder (Block 1):** Removed `stmtWipeDocContainment` + `stmtWipeDocReference` prepared statements and their constructor initializations. Removed entire cosine-ladder computation (`schemasForLadder`, `bestParent`, `referencePairs`, `containmentCandidates`). Removed Phase B wipe+edge loops (both `doc_containment` and `doc_reference` rebuilds). Removed `cosineSimF32` import and `stmtGetClusterableNodes` field/init (only used for centroid computation).

2. **promoteScope() hub→chapter containment (Block 2):** Removed the landing→chapter `doc_containment` edge-writing loop (step 3 of the transaction). Kept all stub creation (chapter-doc stubs + landing-doc stub) because the deriver depends on stubs existing before it runs.

3. **SubjectPromoter.promoteSubjects() hub→subject containment (Block 3):** Removed step 4 edge loop and the now-orphaned `existingSubjectDocs` pre-population (only fed the removed loop). Kept hub stub creation, all subject stub creation, and the `subject-schema-ids:<slug>` meta write (Plan 03 + deriver contract).

Updated file-level doc comment to note DocGraphDeriver is now the sole edge owner (Phase 39.2 D-11). Updated `PromoteResult.containment` and `.reference` returns to `0` consistently.

### Task 2 — consolidator.ts
- Imported `{ NoopDocGraphDeriver }` (value) + `type { DocGraphDeriver }` from `./doc-graph-deriver`
- Added private field `docGraphDeriver: DocGraphDeriver | NoopDocGraphDeriver`
- Added last constructor parameter `docGraphDeriver = new NoopDocGraphDeriver()` (existing tests unaffected by arity)
- Assigned `this.docGraphDeriver = docGraphDeriver` in constructor body
- Inserted `await this.docGraphDeriver.deriveDocGraph()` in Phase C at line 927, between `corpusPromoter.promote()` (line 923) and `insightReflector.reflect()` (line 930)

### Task 3 — run-sleep-pass.ts
- Imported `DocGraphDeriver` from `../consolidation/doc-graph-deriver`
- Constructed `const docGraphDeriver = new DocGraphDeriver(db, store, config, realClock)` after `CorpusPromoter` construction
- Passed `docGraphDeriver` as the final positional argument to `new Consolidator(...)`
- `consumePendingCorpusMarkers(db, store, corpusPromoter, log)` at L572 left untouched

## Acceptance Criteria Verification

- `grep -n 'doc_containment' corpus-promoter.ts | grep upsertEdge` → **0 lines** (PASS)
- `grep -c "stmtWipeDocContainment|stmtWipeDocReference" corpus-promoter.ts` → **0** (PASS)
- `grep -n "subject-schema-ids" corpus-promoter.ts` → **meta write at L992 preserved** (PASS)
- `grep -n "docGraphDeriver.deriveDocGraph" consolidator.ts` → **line 927, after promote() L923, before reflect() L930** (PASS)
- New constructor param is last with `= new NoopDocGraphDeriver()` default (PASS)
- `grep -n "new DocGraphDeriver" run-sleep-pass.ts` → **L495** (PASS)
- `npx tsc --noEmit` → **clean** (PASS)
- `npm run build` → **clean** (PASS)
- `npx vitest run tests/consolidator.test.ts` → **8/8 passed** (PASS)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Cleanup] Removed stmtGetClusterableNodes from CorpusPromoter**
- **Found during:** Task 1
- **Issue:** After removing the centroid computation, `stmtGetClusterableNodes` became orphaned dead code (the only user was the centroid computation removed in the same task).
- **Fix:** Removed the private field declaration and constructor initialization of `stmtGetClusterableNodes` from CorpusPromoter. Updated the schema-members query comment that referenced it.
- **Files modified:** src/consolidation/corpus-promoter.ts
- **Commit:** 9e6f309

**2. [Rule 2 - Cleanup] Removed orphaned existingSubjectDocs variable in SubjectPromoter**
- **Found during:** Task 1
- **Issue:** After removing the hub→subject edge loop, the `existingSubjectDocs` Map and its pre-population block (reading existing subject docs before the transaction) became dead code — the only consumer was the removed loop.
- **Fix:** Removed the `existingSubjectDocs` Map declaration and pre-population block. Removed the `.set(subject.subjectSlug, docId)` call inside the transaction (which fed the removed loop). `hubDocId` was kept as it's still used in the return statement.
- **Files modified:** src/consolidation/corpus-promoter.ts
- **Commit:** 9e6f309

## Known Stubs

None — this plan is a pure refactor (retire legacy edge-writing + wire the new deriver). No stub values or placeholder data introduced.

## Threat Flags

None — this plan modifies only the offline consolidation pass. No new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Self-Check: PASSED

- src/consolidation/corpus-promoter.ts: modified (9e6f309)
- src/consolidation/consolidator.ts: modified (28dea13)
- src/consolidation/run-sleep-pass.ts: modified (6fd2ff9)
- All commits verified in git log
- `npx tsc --noEmit` clean
- `npm run build` clean
- `tests/consolidator.test.ts` 8/8 green
