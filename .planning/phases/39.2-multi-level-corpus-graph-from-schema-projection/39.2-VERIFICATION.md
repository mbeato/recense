---
status: passed
phase: 39.2-multi-level-corpus-graph-from-schema-projection
verified: 2026-06-23
method: goal-backward (structural greps + tsc + test battery + live-brain inspection)
plans_verified: [01, 02, 03, 04, 05]
---

# Phase 39.2 Verification — Multi-Level Corpus Graph from Schema Projection

**Goal:** Turn the corpus doc layer from a shallow 2-level hub→subject star into a multi-level,
interconnected doc→doc graph, derived purely as an LLM-free projection of the existing schema/fact
graph (~$0, no re-ingest/re-embed). Structure-only; the 2D corpus reader foregrounds the graph.

**Verdict: PASSED.** The goal is genuinely achieved — verified against live source and the live brain,
not just task completion.

## Must-haves verified

| # | Must-have | Evidence | ✓ |
|---|-----------|----------|---|
| 1 | LLM-free `DocGraphDeriver` (IDF shared-members + ladder ancestry) | `src/consolidation/doc-graph-deriver.ts` exists; D-37 gated query verbatim (`origin != 'inferred'` present); zero new deps | ✓ |
| 2 | D-11 single ownership | `grep doc_containment\|doc_reference … \| grep upsertEdge` in corpus-promoter.ts → **0 lines** (all legacy writers retired) | ✓ |
| 3 | D-12 doc_link untouched | doc-writer.ts still owns 6 `doc_link` refs; deriver never touches doc_link | ✓ |
| 4 | Wired into Phase C (D-20) | consolidator.ts L927 `await this.docGraphDeriver.deriveDocGraph()` after promote(); Noop default ctor param (L254) | ✓ |
| 5 | Backfill CLI (D-19) | `recense derive-doc-graph` registered; `derive-doc-graph-cli.ts` exists w/ lock discipline + `--dry-run` | ✓ |
| 6 | Viz foregrounding (D-13..D-18) | server `stmtDocNodes` adds `ns.scope`; corpus.js scope coloring + 3 edge types + hub-anchored layout + chapter toggle | ✓ |
| 7 | Acyclicity (D-09) | deriver skip-if-reverse-exists guard + acyclicity test asserts no A→B/B→A pair | ✓ |
| 8 | Subject→chapter containment (Plan 05) | deriver `slugToDocId` block parents chapters under subjects; verified live (40 chapters parented) | ✓ |
| 9 | Build + tests | `tsc --noEmit` clean; 71/71 across doc-graph-deriver, corpus-subject-promoter, consolidator, anthropic-client | ✓ |

## Live-brain proof

After the in-phase content rebuild populated real `subject-schema-ids`, the deriver produced a genuine
multi-level graph on `~/.config/recense/recense.db`:
- **hub→subject: 18** (brain-memory 8 + vtx 6 + tonos 4), **subject→chapter: 41** (40 chapters parented),
  **cross-project reference: 2** (vtx↔brain-memory). Totals: 59 containment + 3 reference.
- **0 malformed docs**, **0 dangling edges** to tombstoned nodes.

## Scope deviations (honest record — improvements, not goal failures)

1. **D-13 OVERRIDDEN by founder** during the human-verify checkpoint: chapter docs are now **hidden by
   default with a toggle**, not all-shown-dimmed — the original "honest presentation" default was
   unreadable (~160 hazed chapters drowned the skeleton). Founder direction supersedes the decision.
2. **In-phase content rebuild (Plan 05, added by founder):** the live corpus had empty `subject-schema-ids`
   (the deferred content bug) so the projection initially yielded 0 edges. Founder opted to pull the fix in:
   `promoteSubjects` index-based ID mapping (root cause: the prompt showed labels but asked for IDs),
   existing-subject scope correction, subject→chapter containment, and a `backfill-subjects` CLL.
3. **Pre-existing reader/provider bugs fixed** (surfaced during live verification, outside original scope):
   doc-gen cloud-SDK 60s timeout (raised to 600s), silent regen failures (now surfaced), regen
   edge-orphaning (regen now re-derives), and the `/doc/*` slug sanitizer stripping `:` (created malformed
   subject-doc duplicates — now allows `:`; 2 existing dupes tombstoned).

## Outstanding (non-blocking, noted for a future corpus-content phase)
- ~160 chapter docs remain unparented (subject-proposal LLM sees only top-20 schemas/project — tunable).
- Chapter docs carry `node_scope = schemaId`, so when toggled visible they don't share a project tint
  (now rendered muted-rose by default rather than random colors).
