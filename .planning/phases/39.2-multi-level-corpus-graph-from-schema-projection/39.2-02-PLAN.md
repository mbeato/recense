---
phase: 39.2-multi-level-corpus-graph-from-schema-projection
plan: 02
type: execute
wave: 2
depends_on: ["39.2-01"]
files_modified:
  - src/consolidation/corpus-promoter.ts
  - src/consolidation/consolidator.ts
  - src/consolidation/run-sleep-pass.ts
autonomous: true
requirements: [DOC-03]
must_haves:
  truths:
    - "Corpus-promoter no longer creates centroid-cosine doc_reference or doc_containment edges — the DocGraphDeriver is the sole owner (D-11)"
    - "SubjectPromoter.promoteSubjects() no longer writes hub→subject doc_containment edges (the deriver owns hub→subject containment via D-08); its stub creation + subject-schema-ids meta write remain intact"
    - "The DocGraphDeriver runs in consolidate() Phase C immediately after corpusPromoter.promote() and before insightReflector.reflect() (D-20)"
    - "promoteScope's hub/landing/chapter stub creation still runs (stubs must exist for the deriver) but its doc_containment edge-writing is retired (D-11)"
    - "Adding the deriver does not break existing Consolidator constructions — it is the last defaulted (Noop) constructor parameter"
    - "doc_link (prose citations) is untouched (D-12)"
  artifacts:
    - path: "src/consolidation/consolidator.ts"
      provides: "DocGraphDeriver DI param + Phase C invocation after corpusPromoter.promote()"
      contains: "docGraphDeriver"
    - path: "src/consolidation/run-sleep-pass.ts"
      provides: "DocGraphDeriver construction + wiring into the Consolidator"
      contains: "DocGraphDeriver"
    - path: "src/consolidation/corpus-promoter.ts"
      provides: "centroid-cosine doc-edge derivation + SubjectPromoter hub→subject containment retired; stub creation + subject-schema-ids meta retained"
  key_links:
    - from: "src/consolidation/consolidator.ts"
      to: "docGraphDeriver.deriveDocGraph()"
      via: "Phase C call after corpusPromoter.promote()"
      pattern: "docGraphDeriver\\.deriveDocGraph"
    - from: "src/consolidation/run-sleep-pass.ts"
      to: "new DocGraphDeriver(...)"
      via: "constructed and passed as last Consolidator param"
      pattern: "new DocGraphDeriver"
---

<objective>
Retire the legacy centroid-cosine doc-edge derivation from `corpus-promoter.ts` and wire the new `DocGraphDeriver` (Plan 01) into the sleep pass so it becomes the single authority for `doc_reference` + `doc_containment` edges (D-11), running in Phase C right after `corpusPromoter.promote()` (D-20).

Purpose: Two derivation paths writing the same edge kinds would double-derive and conflict. This plan establishes single ownership: the deriver wipes-and-rebuilds both edge kinds; the promoter keeps only its stub-creation + `subject-schema-ids` meta responsibilities. This includes retiring the `SubjectPromoter` hub→subject `doc_containment` upsert (the deriver owns hub→subject containment via D-08).

Output: surgical edits to three files. No new behavior beyond moving edge ownership; `doc_link` stays untouched (D-12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.2-multi-level-corpus-graph-from-schema-projection/39.2-CONTEXT.md
@.planning/phases/39.2-multi-level-corpus-graph-from-schema-projection/39.2-PATTERNS.md
@.planning/phases/39.2-multi-level-corpus-graph-from-schema-projection/39.2-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output (src/consolidation/doc-graph-deriver.ts) — LOCKED signature: -->
export class DocGraphDeriver {
  constructor(db: Database.Database, store: SemanticStore, config: EngineConfig, clock: Clock)
  async deriveDocGraph(opts?: { dryRun?: boolean }): Promise<{ containment: number; reference: number }>
}
export class NoopDocGraphDeriver {
  async deriveDocGraph(opts?: { dryRun?: boolean }): Promise<{ containment: number; reference: number }>
  // returns { containment: 0, reference: 0 }
}

<!-- Consolidator constructor (consolidator.ts L234-259) — existing defaulted-Noop tail: -->
  corpusPromoter: CorpusPromoter | NoopCorpusPromoter = new NoopCorpusPromoter(),
  insightReflector: InsightReflector | NoopInsightReflector = new NoopInsightReflector(),
<!-- Phase C order (consolidator.ts L916-919): -->
  await this.corpusPromoter.promote();   // ← insert deriver AFTER this
  ... await this.insightReflector.reflect();

<!-- run-sleep-pass.ts: corpusPromoter constructed ~L483; Consolidator constructed ~L520+;
     consumePendingCorpusMarkers(db, store, corpusPromoter, log) at L565 calls promoteScope. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire centroid-cosine doc-edge derivation + SubjectPromoter hub→subject containment in corpus-promoter.ts (keep stubs + meta)</name>
  <read_first>
    - src/consolidation/corpus-promoter.ts (the file being modified) — focus L135-136 + L190-194 (wipe stmts), L246-418 (centroid + cosine-ladder computation producing containmentCandidates/referencePairs), L480-523 (the wipe + two edge loops to remove), L687-706 (promoteScope landing->chapter edge loop), L771-774 + L1178-1183 (SubjectPromoter creates hub+subject stubs and writes subject-schema-ids meta — KEEP), L1186-1200 (SubjectPromoter hub→subject doc_containment edge loop — REMOVE)
    - src/consolidation/schema-relations.ts L155-159 (confirm the D-37 gated query the deriver now owns — promoter need no longer derive edges from it)
    - .planning/phases/39.2-multi-level-corpus-graph-from-schema-projection/39.2-CONTEXT.md D-08, D-10, D-11, D-12 + PATTERNS.md "corpus-promoter.ts (MODIFY)" section (Blocks 1-3, the KEEP/REMOVE map)
  </read_first>
  <action>
    In `src/consolidation/corpus-promoter.ts`, perform the D-11 retirement surgically:
    (1) Inside `promote()`'s transaction, REMOVE the wipe + both edge loops — the block at L480-523: `this.stmtWipeDocContainment.run(); this.stmtWipeDocReference.run();`, the `doc_containment` rebuild loop (L486-502), and the `doc_reference` rebuild loop (L504-523). KEEP everything above it in the transaction: step 0 (tombstone demoted stubs) and step 1 (create eager doc stubs via `upsertNode`/`upsertNodeDoc`/`upsertNodeScope`).
    (2) REMOVE the now-dead cosine-ladder computation in Phase A that produced `containmentCandidates`/`referencePairs`/`bestParent`/`schemasForLadder` (the L348-418 region) AND the per-schema centroid computation (L246-308 region) IF AND ONLY IF the centroid is used solely by that ladder — verify via `grep -n "centroid" src/consolidation/corpus-promoter.ts`; if any KEPT code path still reads `centroid`, leave the centroid computation in place and only remove the ladder loops. Remove the `cosineSimF32` import (L39) only if no remaining code references it.
    (3) REMOVE the two prepared statements `stmtWipeDocContainment` (L135/L190-192) and `stmtWipeDocReference` (L136/L193-194) — the DocGraphDeriver owns wiping now.
    (4) In `promoteScope()`, REMOVE the landing->chapter `doc_containment` edge-writing loop (L687-706) but KEEP the chapter-doc stub creation and the landing-doc stub creation (L606-684) — the deriver writes hub->subject containment, but the stubs must still exist. Update `promoteScope`'s return `containment` count to 0 (or to the count of stubs touched) since it no longer writes edges; keep the method callable so `consumePendingCorpusMarkers` (run-sleep-pass L565) still drains markers and creates stubs.
    (5) In `SubjectPromoter.promoteSubjects()`, REMOVE the hub→subject `doc_containment` edge-writing block at L1186-1200 (the `// 4. Write hub→subject doc_containment edges for ALL live subject stubs.` comment through the closing of the `for (const [subjectSlug, subjectDocId] of existingSubjectDocs)` loop, including its `this.store.upsertEdge({ ... rel: 'doc_containment', kind: 'doc_containment', w: 1.0, ... })` call). DocGraphDeriver now owns hub→subject containment (D-08) — a competing writer here violates D-11 single-ownership and leaves dead code (the deriver's wipe overrides it each pass anyway). CRITICAL: KEEP the hub+subject STUB creation (L771-774) and the `subject-schema-ids:<slug>` meta write (step 3, L1178-1183) fully intact — the deriver depends on the stubs existing and on the meta key being readable. After removing the edge loop, drop any now-orphaned local bindings (e.g. `hubDocId`, `existingSubjectDocs`, `now`) ONLY if no remaining code in the method references them; if stub creation or meta writing still uses them, leave them in place.
    (6) DO NOT TOUCH: the `subject-schema-ids:<slug>` meta write (L1178-1183), `SubjectPromoter.promoteSubjects()` hub+subject stub creation (L771-774), and anything related to `doc_link` (D-12). Update the file-level doc comment (L11-12) so it no longer claims promoter "wipes+rebuilds doc_containment + doc_reference edges" — replace with a note that DocGraphDeriver (Phase 39.2) now owns those edges.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/consolidation/corpus-promoter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'doc_containment' src/consolidation/corpus-promoter.ts | grep upsertEdge` returns ZERO lines (no doc_containment edge upsert remains anywhere in the file — promote(), promoteScope(), and SubjectPromoter all retired).
    - `grep -n "doc_reference\|doc_containment" src/consolidation/corpus-promoter.ts | grep -v "^[0-9]*: *\*\|//"` returns NO `upsertEdge` rel/kind lines for these kinds (only doc-comment mentions remain).
    - `grep -c "stmtWipeDocContainment\|stmtWipeDocReference" src/consolidation/corpus-promoter.ts` returns 0.
    - `grep -n "subject-schema-ids" src/consolidation/corpus-promoter.ts` STILL returns the meta-write line (KEEP preserved).
    - SubjectPromoter still creates hub + subject stubs (L771-774 region intact) — verifiable by the stub-creation test in corpus-promoter.test.ts staying green.
    - `npx tsc --noEmit` passes (no dangling `cosineSimF32` / `containmentCandidates` / orphaned `hubDocId` references).
    - corpus-promoter unit tests pass (stub creation + meta write still verified; edge-derivation tests, if any, updated or removed to reflect the retirement).
  </acceptance_criteria>
  <done>Promoter (organic promote, promoteScope, AND SubjectPromoter) no longer writes doc_reference/doc_containment edges; stub creation + subject-schema-ids meta intact; build + promoter tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DocGraphDeriver into Consolidator Phase C (DI param + invocation, D-20)</name>
  <read_first>
    - src/consolidation/consolidator.ts (the file being modified) — L50-56 (Noop import pattern for corpusPromoter/insightReflector), L215-217 (private fields), L234-259 (constructor params + assignments), L904-920 (Phase C order; L916 corpusPromoter.promote(), L919 insightReflector.reflect())
    - src/consolidation/doc-graph-deriver.ts (Plan 01 — DocGraphDeriver + NoopDocGraphDeriver interface; the LOCKED method signature is `deriveDocGraph(opts?: { dryRun?: boolean }): Promise<{ containment: number; reference: number }>`)
    - PATTERNS.md "consolidator.ts (MODIFY)" section (import / field / DI / insertion patterns)
  </read_first>
  <action>
    In `src/consolidation/consolidator.ts`: (1) add imports mirroring the corpusPromoter/insightReflector pattern — `import { NoopDocGraphDeriver } from './doc-graph-deriver';` and `import type { DocGraphDeriver } from './doc-graph-deriver';`. (2) Add the private field `private readonly docGraphDeriver: DocGraphDeriver | NoopDocGraphDeriver;` next to `insightReflector`. (3) Add the constructor parameter as the LAST positional param with a default Noop (minimizes test churn per PATTERNS Key-Decision #3): `docGraphDeriver: DocGraphDeriver | NoopDocGraphDeriver = new NoopDocGraphDeriver(),` and assign `this.docGraphDeriver = docGraphDeriver;`. (4) In Phase C, insert the invocation IMMEDIATELY after `await this.corpusPromoter.promote();` (L916) and BEFORE `await this.insightReflector.reflect();` (D-20): `await this.docGraphDeriver.deriveDocGraph();`. The method returns `{ containment, reference }` counts — you may ignore the return at the call site (the CLI in Plan 04 is what surfaces them) or log it; do not destructure into a typed mismatch. Add a one-line comment tying it to D-11/D-20.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/consolidation/consolidator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "docGraphDeriver.deriveDocGraph" src/consolidation/consolidator.ts` shows the call located AFTER the `corpusPromoter.promote()` line and BEFORE the `insightReflector.reflect()` line (verify by line numbers).
    - The new constructor param is the LAST param and has `= new NoopDocGraphDeriver()` default.
    - Existing consolidator tests pass unchanged (no constructor-arity breakage).
  </acceptance_criteria>
  <done>DocGraphDeriver is a defaulted Noop DI param and runs in Phase C after promote(), before reflect(); consolidator tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Construct + pass DocGraphDeriver in run-sleep-pass.ts</name>
  <read_first>
    - src/consolidation/run-sleep-pass.ts (the file being modified) — the SchemaRelationDeriver + CorpusPromoter construction region (~L474-524), the `new Consolidator(...)` call site, and the `consumePendingCorpusMarkers(db, store, corpusPromoter, log)` call at L565 (confirm promoteScope still drains markers + creates stubs after Plan-02 Task 1)
    - src/consolidation/doc-graph-deriver.ts (Plan 01 — constructor signature (db, store, config, clock))
    - PATTERNS.md "run-sleep-pass.ts (MODIFY)" section
  </read_first>
  <action>
    In `src/consolidation/run-sleep-pass.ts`: (1) import `DocGraphDeriver` from `../consolidation/doc-graph-deriver`. (2) Construct it alongside the existing `SchemaRelationDeriver`/`CorpusPromoter` constructions (~L483-486): `const docGraphDeriver = new DocGraphDeriver(db, store, config, realClock);` with a comment noting it is LLM-free, wipe-and-rebuild, sole owner of doc_reference + doc_containment (D-11), running in Phase C after corpusPromoter.promote(). (3) Pass `docGraphDeriver` as the LAST positional argument to the `new Consolidator(...)` call (after `insightReflector`). DO NOT alter the `consumePendingCorpusMarkers(...)` call at L565 — promoteScope still creates stubs (its edge-writing was retired in Task 1, but the marker-drain + stub-creation path is intact and still required so subject/chapter stubs exist before the deriver runs).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "new DocGraphDeriver" src/consolidation/run-sleep-pass.ts` returns the construction line.
    - The `new Consolidator(...)` call passes `docGraphDeriver` as the final argument (verify it lines up with the new last constructor param from Task 2).
    - `consumePendingCorpusMarkers(db, store, corpusPromoter, log)` at ~L565 is unchanged.
    - `npm run build` succeeds (dist compiles — the sleep pass entry point is buildable).
  </acceptance_criteria>
  <done>run-sleep-pass constructs DocGraphDeriver and passes it to Consolidator as the final arg; marker-drain/stub path untouched; full build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sleep-pass orchestration -> derived doc-edges | Wiring change only; same offline boundary as the deriver. No new untrusted input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.2-05 | Tampering (double-derivation) | doc_reference / doc_containment ownership | mitigate | D-11 single-owner: retire promoter's edge-writing entirely (organic promote, promoteScope, AND SubjectPromoter hub→subject) so only DocGraphDeriver wipes+rebuilds these kinds — eliminates conflicting writers (Task 1). |
| T-39.2-06 | Repudiation (lost stub creation) | promoteScope / SubjectPromoter stub path | accept-with-guard | Retire ONLY edge-writing in promoteScope + SubjectPromoter; keep stub creation + subject-schema-ids meta. Acceptance criteria assert the meta write + stub creation survive — deriver depends on stubs existing. |
| T-39.2-07 | Tampering (ordering) | Phase C insertion point | mitigate | D-20: deriver runs AFTER promote() (stubs + meta fresh) and BEFORE reflect()/eviction. Asserted by line-order acceptance check (Task 2). |
| T-39.2-08 | Denial of Service (test breakage) | Consolidator constructor arity | mitigate | New param is LAST + defaulted Noop — existing constructions unaffected (PATTERNS Key-Decision #3). Asserted by unchanged consolidator tests. |

No package installs (net-zero deps) — no supply-chain checkpoint required.
</threat_model>

<verification>
- `npx tsc --noEmit` — no dangling references after retirement.
- `npm run build` — sleep-pass dist compiles.
- `npx vitest run src/consolidation/corpus-promoter.test.ts src/consolidation/consolidator.test.ts` — green.
- Promoter (incl. SubjectPromoter) writes no doc_reference/doc_containment edges; deriver invoked in Phase C between promote() and reflect(); subject-schema-ids meta + stub creation intact; doc_link untouched.
</verification>

<success_criteria>
- corpus-promoter no longer owns doc_reference/doc_containment edges anywhere — organic promote, promoteScope, and SubjectPromoter hub→subject all retired (single ownership, D-11).
- DocGraphDeriver runs in Phase C after promote(), before reflect() (D-20).
- promoteScope + SubjectPromoter stub creation + subject-schema-ids meta retained; doc_link untouched (D-12).
- DocGraphDeriver constructed in run-sleep-pass and passed as the last Consolidator arg.
- Build + affected unit tests green.
</success_criteria>

<output>
Create `.planning/phases/39.2-multi-level-corpus-graph-from-schema-projection/39.2-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
