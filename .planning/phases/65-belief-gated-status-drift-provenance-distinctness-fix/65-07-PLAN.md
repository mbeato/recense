---
phase: 65-belief-gated-status-drift-provenance-distinctness-fix
plan: 07
type: execute
wave: 4
depends_on: ["65-01", "65-03", "65-06"]
files_modified:
  - src/adapter/ingest-cli.ts
  - tests/ingest-cli.test.ts
  - tests/update-decision.test.ts
  - tests/pe-machinery-lock.test.ts
  - src/lib/types.ts
  - src/consolidation/update-decision.ts
  - src/db/schema.ts
  - src/ingest/pipeline.ts
  - src/db/episode-store.ts
  - src/consolidation/consolidator.ts
autonomous: true
requirements: [DRIFT-01, DRIFT-03]

must_haves:
  truths:
    - "The provenance-distinctness key selected by the 65-01 audit is wired end to end: an ingested Gmail episode's counted provenance value is the derived key when enabled, and the collapsed literal when disabled"
    - "Three genuinely independent status emails produce countDistinctProvenance === 3; three copies of one forwarded/quoted thread produce 1 — asserted directly on countDistinctProvenance's inputs"
    - "countDistinctProvenance still excludes origin='inferred' entries (D-19) and still counts by Set cardinality"
    - "Non-gmail sources' session ids are byte-identical to pre-Phase-65 values"
    - "routeContradiction, isOscillation, and the PE band config are unchanged from their pre-Phase-65 source, and the change is caught if anyone edits them"
    - "No bi-temporal validity or supersedes-chain column was added to any table"
  artifacts:
    - path: "src/adapter/ingest-cli.ts"
      provides: "Provenance-key-aware sessionId mint (PRIMARY shape) or unchanged mint (FALLBACK shape)"
      contains: "provenance_key"
    - path: "tests/pe-machinery-lock.test.ts"
      provides: "DRIFT-01 structural lock: the PE machinery's source and the schema's column set are pinned"
      min_lines: 90
    - path: "tests/update-decision.test.ts"
      provides: "The D-07 locked pair asserted directly on countDistinctProvenance, plus the preserved D-19 exclusion"
  key_links:
    - from: "src/adapter/ingest-cli.ts"
      to: "src/source/source-adapter.ts"
      via: "NormalizedRecord.provenance_key consumed at the recordEvent mint"
      pattern: "provenance_key"
    - from: "tests/pe-machinery-lock.test.ts"
      to: "src/consolidation/update-decision.ts"
      via: "source-text pin over routeContradiction and isOscillation"
      pattern: "routeContradiction|isOscillation"
---

<objective>
Wire the provenance-distinctness key into the mechanism it feeds, using the shape the Plan 65-01 audit LOCKED, and pin DRIFT-01's "machinery unmodified" claim with a structural test.

Purpose: everything before this plan computes and carries the key without it counting for anything. This is the plan where `countDistinctProvenance` can finally exceed 1 on Gmail-only evidence — the hard prerequisite research Pitfall 3 says the differentiator cannot ship without. It is also where the D-07 locked test pair lands on the real mechanism rather than on a helper.
Output: the mint (or fallback threading) wired, the locked pair asserted on `countDistinctProvenance`, and a machinery/schema lock test.

**Not in this plan:** no drift-layer wiring, no `ClaimDecision` drift fields, no per-source `contradictionN` lookup, no sink changes (all 65-08).

**Branching:** this plan implements exactly ONE of two shapes, selected by the `## VERDICT` section of `65-SESSION-ID-AUDIT.md`. Read that file FIRST. Both shapes are fully specified in Task 1; implement the selected one and leave the other's files untouched. Do not invent a third shape — CONTEXT D-02 locks the choice to these two.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-CONTEXT.md
@.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-PATTERNS.md
@.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-SESSION-ID-AUDIT.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Live-verified 2026-08-02, plus the upstream Phase 65 plans. -->

The mint site, `src/adapter/ingest-cli.ts:181-196` (inside `runPullPhase`'s `db.transaction`):
```
const appendBatch = db.transaction((batch: NormalizedRecord[]) => {
  for (const r of batch) {
    pipeline.recordEvent({
      content: r.content,
      role: r.role,
      origin: r.origin,
      sessionId: `ingest:${r.source}`,
      source: r.source,
      externalId: r.external_id,
      eventTs: r.event_ts ?? null,
    });
    appended++;
  }
});
```

The mechanism, `src/consolidation/update-decision.ts:79-98` (unchanged under the PRIMARY shape):
```
export function countDistinctProvenance(entries: PendingContradiction[]): number {
  const sessions = new Set<string>();
  for (const entry of entries) {
    if (entry.origin !== 'inferred') {
      sessions.add(entry.session_id);
    }
  }
  return sessions.size;
}
```

`src/lib/types.ts:9-18`:
```
export interface PendingContradiction {
  episode_id: string;
  session_id: string;
  origin: Origin;
}
```

From Plan 65-06: `NormalizedRecord.provenance_key?: string` — set by the Gmail adapter to either the derived `ingest:gmail:<domain>:<threadId>` (when `provenanceDistinctnessEnabled`) or the collapsed `'ingest:gmail'`; omitted entirely by every other adapter.
From Plan 65-04: `COLLAPSED_GMAIL_PROVENANCE_KEY = 'ingest:gmail'`.
From Plan 65-03: `provenanceDistinctnessEnabled` (default `false`).

`src/ingest/pipeline.ts:27-57,75-97` — `RecordEventParams` and `IngestionPipeline.recordEvent`, which maps params onto `EpisodicStore.append`.
`src/db/episode-store.ts:110-225` — `append` and its named-parameter INSERT.
`src/db/schema.ts:24-41` — the `episode` DDL; migrations are guarded `ALTER TABLE` blocks keyed on a column-presence check (see the v16 `event_ts` migration at lines 271-278 for the exact pattern).
`src/consolidation/consolidator.ts:1437-1441` and `:1588-1592` — the two `recordContradiction` call sites that construct a `PendingContradiction`.

Pre-existing assertions that must keep passing unchanged:
- `tests/episode-event-ts.test.ts:302` asserts `row.session_id === 'ingest:granola'`
- `tests/update-decision.test.ts` `describe('countDistinctProvenance')` — the existing cases including the `origin: 'inferred'` exclusion
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the LOCKED shape from the 65-01 audit verdict</name>
  <files>src/adapter/ingest-cli.ts, src/lib/types.ts, src/consolidation/update-decision.ts, src/db/schema.ts, src/ingest/pipeline.ts, src/db/episode-store.ts, src/consolidation/consolidator.ts</files>
  <read_first>
    - .planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-SESSION-ID-AUDIT.md (the `## VERDICT` section — read this FIRST; it selects which half of this task applies)
    - src/adapter/ingest-cli.ts (lines 145-205 — `runPullPhase` and the mint transaction)
    - src/source/source-adapter.ts (the `NormalizedRecord.provenance_key` field and the 7th invariant, added in Plan 65-06)
    - src/consolidation/update-decision.ts (full file, 99 lines)
    - src/lib/types.ts (lines 1-30)
    - .planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
Read `65-SESSION-ID-AUDIT.md`'s `## VERDICT` line and implement **exactly one** of the following. Record which one, verbatim, at the top of this plan's SUMMARY.

---

**IF the verdict is `PRIMARY`** (richer `sessionId` minted at ingest):

Change only `src/adapter/ingest-cli.ts`. In the `appendBatch` transaction loop, replace the `sessionId` expression with the record-supplied key when present:

  `sessionId: r.provenance_key ?? \`ingest:${r.source}\`,`

That single expression is the whole change. Add a comment above it stating:
  - The nullish fallback preserves byte-identical behavior for every adapter that does not set the field — today that is every adapter except Gmail (DRIFT-03, D-02 primary shape).
  - When `provenanceDistinctnessEnabled` is `false`, the Gmail adapter sets the field to the collapsed `'ingest:gmail'` literal, which is exactly what the fallback expression would have produced, so the dark switch is a pure config flip with no code-path difference.
  - **D-03 forward-only:** episodes already ingested keep their historical collapsed session id. There is no retroactive migration and none is planned; pending contradictions accumulated under the old collapsed id remain one bucket forever.
  - `PendingContradiction`, `countDistinctProvenance`, `routeContradiction`, and the `episode` schema are all byte-unchanged — the mechanism is fed differently, not modified. That is the DRIFT-01 "machinery unmodified" spirit extended to DRIFT-03.

Leave `src/lib/types.ts`, `src/consolidation/update-decision.ts`, `src/db/schema.ts`, `src/ingest/pipeline.ts`, `src/db/episode-store.ts`, and `src/consolidation/consolidator.ts` untouched.

---

**IF the verdict is `FALLBACK`** (optional `provenance_key` on `PendingContradiction`):

Leave `src/adapter/ingest-cli.ts:188` byte-unchanged and thread the key on a parallel path:

  1. `src/db/schema.ts` — add a nullable `provenance_key TEXT` column to the `episode` DDL and a guarded migration mirroring the v16 `event_ts` pattern exactly (column-presence check, then `ALTER TABLE episode ADD COLUMN provenance_key TEXT`, no default, no data rewrite, additive and reversible), and bump `SCHEMA_VERSION` following the file's existing convention. The column is required because ingest and consolidation run in different process invocations — the key cannot survive in memory. Comment that this is a nullable provenance sidecar column, explicitly NOT a bi-temporal validity or supersedes-chain column (the v9.0 DEFER stands).
  2. `src/db/episode-store.ts` — add `provenance_key` to `EpisodeRow`, to the INSERT column/value lists, and to the `append` parameter mapping, mirroring how `event_ts` is handled at lines 58, 122-126, 206, and 221.
  3. `src/ingest/pipeline.ts` — add `provenanceKey?: string | null` to `RecordEventParams` with a JSDoc mirroring `eventTs`'s, and pass `provenance_key: e.provenanceKey ?? null` through to `store.append`.
  4. `src/adapter/ingest-cli.ts` — pass `provenanceKey: r.provenance_key ?? null` in the existing `recordEvent` call. The `sessionId` line stays byte-identical.
  5. `src/lib/types.ts` — add `provenance_key?: string;` to `PendingContradiction` with a JSDoc stating: optional richer distinctness key (D-02 fallback, DRIFT-03); when present, `countDistinctProvenance` counts this instead of `session_id`; written at record time only for sources that compute one (Gmail today); absent elsewhere.
  6. `src/consolidation/consolidator.ts` — add `episodeProvenanceKey: string | null` to `ClaimDecision` (mirroring `episodeSessionId`'s JSDoc density), fill it from `episode.provenance_key` at all three existing `episodeSessionId: episode.session_id` fill sites (approximately lines 815, 912, 1001 — locate them by grepping `episodeSessionId:`), and include `provenance_key: decision.episodeProvenanceKey ?? undefined` in both `PendingContradiction` object literals (approximately lines 1437-1441 and 1588-1592). Both sites must be updated; a one-site change leaves the secondary-contradiction path silently on the old key.
  7. `src/consolidation/update-decision.ts` — change EXACTLY one line inside `countDistinctProvenance`: `sessions.add(entry.provenance_key ?? entry.session_id);`. The `origin !== 'inferred'` D-19 exclusion, the `Set` structure, the return value, and the function's JSDoc semantics stay otherwise byte-identical. Extend the JSDoc with one sentence naming the new counted field and why the coalesce preserves pre-Phase-65 behavior for every entry that lacks it.

Under EITHER shape, do NOT modify `routeContradiction`, `isOscillation`, the PE band config values, or any node/edge column.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/update-decision.test.ts tests/ingest-cli.test.ts tests/ingest.test.ts tests/episode-event-ts.test.ts tests/schema.test.ts tests/store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the verdict verbatim as read from `65-SESSION-ID-AUDIT.md`
    - Under PRIMARY: `git diff --stat` lists `src/adapter/ingest-cli.ts` as the ONLY changed source file, and `grep -c "provenance_key" src/adapter/ingest-cli.ts` is at least 1
    - Under FALLBACK: `git diff src/adapter/ingest-cli.ts` shows the `sessionId:` line unchanged; `grep -c "provenance_key" src/lib/types.ts` is at least 1; `grep -c "entry.provenance_key ?? entry.session_id" src/consolidation/update-decision.ts` returns 1; `grep -c "provenance_key" src/consolidation/consolidator.ts` is at least 3 (field + both record sites)
    - Under EITHER: `git diff src/consolidation/update-decision.ts` shows zero changes to `routeContradiction` and `isOscillation` (only `countDistinctProvenance` may differ, and only under FALLBACK)
    - Under EITHER: `grep -c "origin !== 'inferred'" src/consolidation/update-decision.ts` still returns 1 — the D-19 exclusion survives
    - `npx vitest run tests/episode-event-ts.test.ts` passes unmodified — non-gmail session ids are byte-identical
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Exactly one of the two LOCKED shapes is wired, the other's files are untouched, the D-19 exclusion survives, and non-gmail behavior is byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land the D-07 locked pair on countDistinctProvenance and prove the mint end to end</name>
  <files>tests/update-decision.test.ts, tests/ingest-cli.test.ts</files>
  <read_first>
    - tests/update-decision.test.ts (full file, 190 lines — the existing `describe('countDistinctProvenance')` block is where the locked pair lands)
    - tests/ingest-cli.test.ts (full file — the existing `runPullPhase` harness, fake adapter, and DB assertions to reuse)
    - src/consolidation/update-decision.ts (as it stands after Task 1)
    - src/adapter/ingest-cli.ts (as it stands after Task 1)
    - src/source/provenance-key.ts and tests/provenance-key.test.ts (Plan 65-04 — reuse its fixtures so the two suites agree on what independence looks like)
    - .planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-CONTEXT.md (D-07 — "unit test directly on countDistinctProvenance inputs, per Pitfall 3's prescription")
  </read_first>
  <behavior>
    - **LOCKED PAIR direction A, on the mechanism:** three `PendingContradiction` entries whose counted values are three distinct derived keys (three sender domains, three thread ids) → `countDistinctProvenance` returns `3`
    - **LOCKED PAIR direction B, on the mechanism:** three entries whose counted values are all the collapsed `'ingest:gmail'` (three forwards of one thread, three different forwarding senders) → returns `1`
    - **Pre-phase regression:** three entries all carrying the collapsed `'ingest:gmail'` because the dark switch is off → returns `1`, which is exactly today's behavior
    - **D-19 preserved:** adding a fourth entry with `origin: 'inferred'` and a fresh distinct key to direction A still returns `3`
    - **Mixed sources:** entries from Gmail derived keys and from a Claude Code UUID session id count together by set cardinality, with no source-aware special case
    - **End-to-end mint, enabled:** driving the real `runPullPhase` with a fake adapter emitting three independent Gmail records and `provenanceDistinctnessEnabled: true`, the three appended episodes have three distinct counted provenance values in the DB
    - **End-to-end mint, disabled:** the same three records with the stock `false` produce one counted provenance value, byte-equal to `'ingest:gmail'`
    - **End-to-end mint, non-gmail:** a fake adapter with `source: 'granola'` and no `provenance_key` still appends `session_id === 'ingest:granola'`
  </behavior>
  <action>
**In `tests/update-decision.test.ts`:** add the D-07 locked pair to the existing `describe('countDistinctProvenance', ...)` block — do not create a parallel file. Build the entries by calling the real `deriveGmailProvenanceKey` from `src/source/provenance-key.ts` on the same fixtures `tests/provenance-key.test.ts` uses, rather than hand-writing key strings. Hand-written strings would let this suite keep passing after a derivation regression, which is precisely the failure mode research Pitfall 3's warning-signs list describes.

Under the PRIMARY shape put the derived values in `session_id`; under the FALLBACK shape put them in `provenance_key` and leave `session_id` as the collapsed literal on every entry — the FALLBACK arrangement is the stronger test because direction A only returns `3` if the new field is genuinely the counted one.

Give the pair its own comment block stating: these two assertions ARE ROADMAP success criterion 3 for DRIFT-03, both directions are first-class, and this is the mechanism-level counterpart to the key-level pair in `tests/provenance-key.test.ts`. Mark it review-blocking.

**In `tests/ingest-cli.test.ts`:** add an end-to-end block driving the real `runPullPhase` against an in-memory DB with a fake `SourceAdapter`. Assert the three end-to-end cases from the behavior block by reading the appended episode rows back out of SQLite and computing the distinct count over the field the selected shape actually uses. Reuse the file's existing harness rather than building a new one.

Add one assertion that the enabled and disabled runs produce **identical `episode.content`** for the same input record, so the dark switch is proven to affect provenance only. This complements Plan 65-06's adapter-level content-invariance case at the ingest layer.
  </action>
  <verify>
    <automated>npx vitest run tests/update-decision.test.ts tests/ingest-cli.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/update-decision.test.ts` passes, and the `countDistinctProvenance` block now contains assertions expecting `3` and `1` on the locked-pair fixtures
    - The locked-pair entries are built by calling the real `deriveGmailProvenanceKey`, not by hand-written key strings (`grep -c "deriveGmailProvenanceKey" tests/update-decision.test.ts` is at least 1)
    - The D-19 case adds an `origin: 'inferred'` entry with a distinct key and still expects `3`
    - `npx vitest run tests/ingest-cli.test.ts` passes with the three end-to-end mint cases and the content-identity assertion
    - The non-gmail case asserts the exact string `'ingest:granola'`
    - Mutation check #1, recorded in the SUMMARY: temporarily revert Task 1's wiring (PRIMARY: restore the plain `` `ingest:${r.source}` `` mint; FALLBACK: restore `sessions.add(entry.session_id)`), confirm the end-to-end enabled case FAILS; revert the mutation
    - Mutation check #2, recorded in the SUMMARY: temporarily remove the `origin !== 'inferred'` guard from `countDistinctProvenance`, confirm the D-19 case FAILS; revert
  </acceptance_criteria>
  <done>The DRIFT-03 locked pair is asserted on the real mechanism with derived (not hand-written) keys, the D-19 exclusion is mutation-checked, and the mint is proven end to end in both switch positions plus the non-gmail control.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin DRIFT-01 — the PE machinery and the schema column set</name>
  <files>tests/pe-machinery-lock.test.ts</files>
  <read_first>
    - src/consolidation/update-decision.ts (full file — the two functions being pinned)
    - src/db/schema.ts (lines 22-110 for the `episode` and `node` DDL; lines 260-300 for the migration-guard pattern)
    - tests/no-ats-domain-table.test.ts and tests/src-import-boundary.test.ts (the two structural-guard precedents)
    - tests/schema.test.ts (the existing schema assertions — do not duplicate what it already covers)
    - .planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-CONTEXT.md (D-08 — "routeContradiction, isOscillation, and the band thresholds are NOT modified")
    - .planning/REQUIREMENTS.md (DRIFT-01's exact wording — "no new data model, no bi-temporal or supersedes-chain columns")
  </read_first>
  <behavior>
    - The `routeContradiction` function body, extracted from the live source with comments and whitespace normalized, matches a pinned expected form containing exactly the three band comparisons and the `peAppendNewMinResistance` guard, in that order
    - The `isOscillation` function body, similarly normalized, matches its pinned expected form
    - The live `node` table's column set (read from `PRAGMA table_info(node)` against a freshly initialized in-memory DB) equals the pinned expected set — no `valid_from`, `valid_to`, `supersedes`, `superseded_by`, `asserted_at`, or `effective_at` column exists
    - The live `edge` table's column set likewise contains no bi-temporal or supersedes column
    - `DEFAULT_CONFIG.peReconcileBandLow`, `peReconcileBandHigh`, `peAppendNewMinResistance`, and `contradictionN` equal their pre-Phase-65 values
    - The pin is non-vacuous: a synthetic modified function body and a synthetic column list with `valid_from` both fail through the SAME predicates the real check uses
  </behavior>
  <action>
Create `tests/pe-machinery-lock.test.ts` — the shipped enforcement of DRIFT-01's "no new data model, no bi-temporal or supersedes-chain columns, machinery unmodified."

**Source pin.** Export a pure helper `normalizedFunctionBody(source: string, fnName: string): string` that locates `export function <fnName>(` in the given source, extracts the balanced-brace body, strips `//` and `/* */` comments, collapses all whitespace runs to a single space, and trims. Run it over the live `src/consolidation/update-decision.ts` read from disk for `routeContradiction` and `isOscillation`, and compare each against a pinned expected string held as a constant in the test file. Comment that the pin is deliberately over the NORMALIZED body, not a hash of the file: comment and formatting changes stay free, semantic edits do not.

When this test fails, the failure message must tell the next engineer what to do. Write that guidance into the expectation's failure comment: DRIFT-01 forbids modifying these two functions in this phase; if a later milestone genuinely needs to change them, update the pinned constant in the same commit as the change and record the rationale — do not delete the test.

**Schema pin.** Open `new Database(':memory:')`, call `initSchema`, and read `PRAGMA table_info(node)` and `PRAGMA table_info(edge)`. Assert the column-name sets deep-equal pinned expected arrays. Separately assert that the set intersection with a `BITEMPORAL_TOKENS` list (`valid_from`, `valid_to`, `supersedes`, `superseded_by`, `asserted_at`, `effective_at`, `tx_time`, `valid_time`) is empty for `node`, `edge`, and `episode`.

Note explicitly in a comment that the `episode` table IS permitted to gain a nullable `provenance_key` column under the Plan 65-01 FALLBACK verdict, and that such a column is a provenance sidecar, not bi-temporal validity — so pin `episode` against the token list only, not against an exact column set, and state that reasoning rather than leaving it as an unexplained asymmetry.

**Config pin.** Assert the four PE-related `DEFAULT_CONFIG` values equal their pre-Phase-65 numbers. Read the current values from `src/lib/config.ts` and hardcode them in the test with an inline comment naming each one's origin.

**Non-vacuousness.** Feed `normalizedFunctionBody` a synthetic source whose `routeContradiction` body has one comparison flipped and assert the result does NOT equal the pinned string. Feed the bi-temporal-token predicate a synthetic column list containing `valid_from` and assert it reports a violation. Both must go through the same exported helpers the real checks use.

File header must state: this test is the executable form of DRIFT-01 and of CONTEXT D-08; it is expected to fail loudly on any future attempt to modify the PE gate or add bi-temporal columns, and that failure is the intended signal, not a flaky test.
  </action>
  <verify>
    <automated>npx vitest run tests/pe-machinery-lock.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/pe-machinery-lock.test.ts` passes with at least 8 cases
    - The source pin reads `src/consolidation/update-decision.ts` from disk at test time (not an import of the compiled function) — `grep -c "readFileSync" tests/pe-machinery-lock.test.ts` is at least 1
    - Both non-vacuousness cases go through the exported helpers, not through inline duplicates
    - The bi-temporal token check covers `node`, `edge`, AND `episode`
    - The `episode` asymmetry (token-list-only, exact-set exempt) is explained in a comment naming the FALLBACK `provenance_key` column
    - Mutation check, recorded in the SUMMARY: temporarily change `peReconcileBandLow`'s comparison in `routeContradiction` from `<` to `<=`, confirm the source pin FAILS; revert and confirm green
  </acceptance_criteria>
  <done>DRIFT-01 is enforced by a shipped test: the two PE functions are pinned by normalized body, the node/edge column sets are pinned exactly, no table carries a bi-temporal or supersedes column, and both pins are proven non-vacuous.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter-supplied `provenance_key` → the counted distinctness value | The key derives from sender-controlled headers (Plan 65-04's threat surface) and now directly determines `countDistinctProvenance`'s output, which gates force-destabilization of a belief. |
| ingest process → consolidation process | Under the FALLBACK shape the key crosses a process boundary through a persisted `episode` column; under PRIMARY it crosses through the existing `session_id` column. Either way it is written by ingest and read by the sleep pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-07-FARM | Tampering | Forwarded/quoted farming reaching `countDistinctProvenance` | mitigate | The D-07 locked pair is asserted on the real mechanism with keys produced by the real derivation, so a derivation regression fails here too. Direction B uses three distinct senders and three distinct thread ids and still expects `1`. |
| T-65-07-D19 | Elevation of Privilege | Inferred output destabilizing a fact | mitigate | The `origin !== 'inferred'` exclusion is preserved byte-identically under both shapes, asserted by a dedicated case, and mutation-checked. Under FALLBACK the coalesce is inserted INSIDE the existing guard, not around it. |
| T-65-07-HALFWIRE | Tampering | FALLBACK wired at the primary `recordContradiction` site but not the secondary | mitigate | Task 1 names both sites with line references and Task 1's acceptance criteria require at least three `provenance_key` occurrences in `consolidator.ts`. A one-site change would leave the secondary-contradiction path silently counting the collapsed key. |
| T-65-07-BITEMP | Tampering | Bi-temporal or supersedes columns smuggled in to "properly" fix ordering | mitigate | Task 3 pins the `node` and `edge` column sets exactly and scans all three tables for eight bi-temporal token names, with a planted-offender non-vacuousness check. CONTEXT D-12 and the v9.0 DEFER both forbid this; the test makes the prohibition executable. |
| T-65-07-MACHINERY | Tampering | Silent edits to `routeContradiction` / `isOscillation` / the PE bands | mitigate | Normalized-body source pin plus a four-value config pin, both non-vacuous and mutation-checked. Comment and formatting changes remain free so the pin does not become a nuisance that gets deleted. |
| T-65-07-MIGRATE | Denial of Service | FALLBACK schema migration failing on an existing DB | mitigate | The migration mirrors the shipped v16 `event_ts` pattern exactly: a column-presence guard, a single additive nullable `ALTER TABLE`, no default, no data rewrite, and therefore reversible. `tests/schema.test.ts` and `tests/schema-v12-migration.test.ts` run as part of this plan's verification. |
| T-65-SC | Tampering | npm/pip/cargo installs | N/A | Zero package installs. Net-zero new runtime dependencies invariant holds. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npx vitest run tests/update-decision.test.ts tests/ingest-cli.test.ts tests/pe-machinery-lock.test.ts` — all pass
- `npx vitest run tests/ingest.test.ts tests/ingest-cli-multiaccount.test.ts tests/episode-event-ts.test.ts tests/schema.test.ts tests/schema-v12-migration.test.ts tests/store.test.ts tests/consolidation.test.ts tests/consolidator.test.ts` — no collateral regression
- `git diff src/consolidation/update-decision.ts` shows no change to `routeContradiction` or `isOscillation` under either shape
- `npx vitest run tests/session-id-provenance-consumers.test.ts` still passes — the wiring introduced no session-id content branching
</verification>

<success_criteria>
The LOCKED shape from the 65-01 audit is wired end to end so `countDistinctProvenance` can exceed 1 on Gmail-only evidence when enabled and is byte-identical to pre-Phase-65 when disabled; the DRIFT-03 locked pair is asserted on the real mechanism with derived keys in both directions; the D-19 inferred exclusion survives and is mutation-checked; and DRIFT-01's machinery-unmodified and no-new-data-model claims are enforced by a shipped, non-vacuous structural test.
</success_criteria>

<output>
Create `.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-07-SUMMARY.md` when done. Record the verdict implemented verbatim, the exact diff scope under that shape, the locked-pair results, and all four mutation-check results.
</output>
