---
phase: 65-belief-gated-status-drift-provenance-distinctness-fix
plan: 10
type: execute
wave: 6
depends_on: ["65-08"]
files_modified:
  - scripts/eval/drift-05-dry-run.cjs
  - scripts/eval/cases/drift-05-cases.json
  - package.json
  - tests/drift-05-harness-smoke.test.ts
autonomous: false
requirements: [DRIFT-05]

must_haves:
  truths:
    - "The harness reports, over real multi-email status threads, how many distinct provenance keys the redesigned derivation produces versus the one bucket today's collapsed key produces"
    - "The harness reports belief-correction accuracy against a labeled case set with the methodology stated inline in its own output"
    - "The harness runs in --dry-run mode with zero API calls and zero network, over synthetic cases, so CI can exercise it"
    - "The harness reports the residual-length distribution so provenanceMinResidualChars can be tuned against real data rather than guessed"
    - "The output states explicitly that no external accuracy bar exists for this feature class and cites none"
    - "Enabling provenanceDistinctnessEnabled is a recorded human decision made after reviewing this harness's output — the phase does not flip it silently"
  artifacts:
    - path: "scripts/eval/drift-05-dry-run.cjs"
      provides: "DRIFT-05 belief-correction accuracy + provenance-key distinctness dry-run harness"
      min_lines: 280
    - path: "scripts/eval/cases/drift-05-cases.json"
      provides: "Labeled synthetic status-thread cases covering hold, release, forwarding, and out-of-order"
      min_lines: 60
    - path: "tests/drift-05-harness-smoke.test.ts"
      provides: "Zero-network smoke test of the case schema and the harness's dry-run path"
      min_lines: 120
  key_links:
    - from: "scripts/eval/drift-05-dry-run.cjs"
      to: "dist/src/source/provenance-key.js"
      via: "compiled-dist require of the real derivation, mirroring correctness-harness.cjs"
      pattern: "dist/src/source/provenance-key"
    - from: "scripts/eval/drift-05-dry-run.cjs"
      to: "package.json"
      via: "npm run eval:drift05 / eval:drift05:dry scripts"
      pattern: "eval:drift05"
---

<objective>
Close DRIFT-05: measure belief-correction accuracy and provenance-key behavior against recense's own harness, honestly, and gate the enablement of `provenanceDistinctnessEnabled` on a human review of that measurement.

Purpose: research SUMMARY's Phase Ordering Rationale makes this the gate on Phase 66 — it is cheaper to fix a threshold or the key before any consumer depends on the emission stream than after. D-14 requires a dry-run against real multi-email status threads before the key goes live; D-15 requires the number to come from recense's own harness with the methodology stated, and forbids citing an external bar because none exists for this feature class.
Output: a `--dry-run`-capable harness following `correctness-harness.cjs`'s conventions, a labeled synthetic case set, two npm scripts, a zero-network smoke test, and a recorded enablement decision.

**Not in this plan:** no engine changes. No `ActionProposalSink` and no consumer wiring — that is Phase 66, which this plan gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-CONTEXT.md
@.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md

<interfaces>
<!-- Live-verified 2026-08-02. -->

`scripts/eval/correctness-harness.cjs` (404 lines) — the harness this one extends. Conventions to copy verbatim:
- Header block naming the requirement, the exact run commands, and an honest note about what the scoring is and is not (see its lines 20-25, the "corrected scoring... an approximation" admission)
- `'use strict';` then `fs`/`os`/`path`/`child_process` requires
- `const arg = (k, d) => { const i = process.argv.indexOf(k); return i !== -1 ? process.argv[i + 1] : d; };`
- `const DRY_RUN = process.argv.includes('--dry-run');`
- `const CASES_PATH = arg('--cases', 'scripts/eval/cases/<name>.json');`
- `const OUT = arg('--out', 'scripts/eval/results/<name>-PENDING.json');`
- Compiled-dist requires: `require('../../dist/src/db/schema')`, `require('../../dist/src/lib/config')`, `require('../../dist/src/lib/clock')`, `require('../../dist/src/consolidation/run-sleep-pass')` — the harness runs against `dist/`, so every run command is prefixed with `npm run build`
- API-key guard for non-dry runs: collect missing `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` into an array and fail with a clear message
- Fresh scratch DB per case under `os.tmpdir()`

`package.json` script conventions (lines 47-52):
`"eval:correctness": "npm run build && node scripts/eval/correctness-harness.cjs"`
`"eval:correctness:dry": "npm run build && node scripts/eval/correctness-harness.cjs --dry-run"`

`scripts/eval/cases/correctness-cases.json` — the labeled-case-set shape; `tests/correctness-harness-smoke.test.ts` validates its schema with a `REQUIRED_FIELDS` array and asserts a case count range. Copy both patterns.

`tests/eval-harness-smoke.test.ts` — the second smoke precedent, including the `spawnSync` invocation of the `.cjs` in `--dry-run` mode and its "Pattern note" explaining that `Consolidator` + `MockModelProvider` is the correct CI wiring while `runConsolidation()` is for full runs only.

Phase 65 modules the harness measures (all available under `dist/` after `npm run build`):
- `dist/src/source/provenance-key.js` — `deriveGmailProvenanceKey`, `normalizeSenderDomain`, `COLLAPSED_GMAIL_PROVENANCE_KEY`
- `dist/src/source/strip-quoted.js` — `stripQuotedForwarded`, `isNearEmptyResidual`
- `dist/src/consolidation/status-drift.js` — `StatusDrift`, `dampByConfidence`, `isEmissionEligible`
- `dist/src/consolidation/update-decision.js` — `countDistinctProvenance`

Phase 64's observability counters, which this harness reads as its measurement substrate (`consolidator.ts:636-646`): `resolutionAttempts`, `resolutionHits`, `resolutionAbstains`, plus Phase 65's `driftEvaluations`, `driftDamped`, `driftStaleDropped`, `driftEventTsUnknown` from Plan 65-08. Phase 64's own block comment names this DRIFT-05 pass as their consumer.

Config knobs the harness sweeps: `provenanceDistinctnessEnabled`, `provenanceMinResidualChars`, `contradictionNBySource`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the DRIFT-05 harness and its labeled case set</name>
  <files>scripts/eval/drift-05-dry-run.cjs, scripts/eval/cases/drift-05-cases.json, package.json</files>
  <read_first>
    - scripts/eval/correctness-harness.cjs (full file, 404 lines — the CLI conventions, the scratch-DB-per-case discipline, the compiled-dist requires, the API-key guard, and the honest-methodology header style)
    - scripts/eval/cases/correctness-cases.json (the labeled-case-set shape)
    - scripts/eval/README.md (the conventions the eval directory documents)
    - package.json (lines 40-60 — the eval script naming convention)
    - src/source/provenance-key.ts, src/source/strip-quoted.ts, src/consolidation/status-drift.ts (the modules being measured; read their exported signatures)
    - src/consolidation/consolidator.ts (lines 628-650 — the counter block whose values the harness reports)
    - .planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-CONTEXT.md (D-14, D-15, D-16)
    - .planning/research/SUMMARY.md (the Phase Ordering Rationale — why this gates Phase 66)
  </read_first>
  <action>
**A. Case set.** Create `scripts/eval/cases/drift-05-cases.json` holding at least 12 labeled synthetic cases. Each case is one status thread scenario with these required fields (mirror `correctness-cases.json`'s flat-object shape and document them in the harness header):

  - `case_id` — stable string
  - `scenario` — one of `hold-single-ambiguous`, `release-three-independent`, `forward-farm`, `quote-farm`, `out-of-order`, `chronological-control`, `undated`
  - `initial_status_fact` — the belief text established first
  - `messages` — an array of `{ from, thread_id, date, body, intent_confidence }` objects in ingestion order (deliberately NOT send order for the out-of-order cases)
  - `expected_outcome` — one of `hold`, `corrected`, `unchanged`
  - `expected_distinct_provenance` — the integer `countDistinctProvenance` should report
  - `rationale` — one sentence naming which requirement or pitfall the case exercises

Cover, at minimum: the DRIFT-02 hold case, the three-independent release case, both farming variants, the out-of-order pair with its chronological control, and an undated (`date: ''`) case. Use obviously-synthetic domains (`.test`, `.example`) so no real correspondent appears in a committed file.

**B. Harness.** Create `scripts/eval/drift-05-dry-run.cjs` following `correctness-harness.cjs`'s conventions exactly: `'use strict'`, the same `arg` helper, `--dry-run` / `--cases` / `--out` flags, compiled-`dist/` requires, a fresh scratch DB per case under `os.tmpdir()`, and the non-dry API-key guard.

Add two Phase-65-specific flags:
  - `--inbox <path>` — a JSONL file of REAL exported Gmail messages (`{ from, thread_id, date, body }` per line) supplied by the operator and **never committed**. When absent, the harness runs on the synthetic case set only and says so in its output.
  - `--sweep-residual <a,b,c>` — comma-separated `provenanceMinResidualChars` values to evaluate the key derivation under. Default `10,20,30`.

The harness produces **three report sections**, all written to the `--out` JSON and echoed as a human-readable summary:

  1. **Provenance-key distinctness report.** For each input thread group, compute today's collapsed key and the derived key via the real `deriveGmailProvenanceKey`, and report: total messages; distinct keys under the collapsed scheme (which is always `1` for gmail — state it as the measured baseline, not as an assumption); distinct keys under the derived scheme; the count that fell back to the collapsed key and the reason distribution (`no-domain` / `no-thread-id` / `near-empty-residual`); and the residual-length distribution (min, p25, median, p75, max of non-whitespace counts after `stripQuotedForwarded`). Repeat the distinct-key count for each `--sweep-residual` value so the threshold's effect is visible as a curve rather than a single point.
  2. **Belief-correction accuracy report.** For each case, run a real consolidation over a scratch DB and compare the observed outcome against `expected_outcome` and `expected_distinct_provenance`. Report per-scenario counts and an overall accuracy, and include the four Phase 65 drift counters plus Phase 64's three resolution counters for the run.
  3. **Methodology block**, embedded in the output JSON itself under a `methodology` key, stating in plain sentences: what was measured; the case count and whether real inbox data was included; the exact config used (all six Phase 65 knobs, verbatim); what the number does NOT cover; and, literally, that **no external accuracy bar exists for this feature class — no competitor publishes a methodology-disclosed number for email-to-status classification — so no comparison is offered and none should be inferred.** Copy the honesty register from `correctness-harness.cjs`'s own header, which already admits where its scoring is an approximation.

**Dry-run semantics:** `--dry-run` must complete with zero network calls and zero API keys. Sections 1 and 3 run fully in dry mode (the derivation and the stripper are pure and LLM-free). Section 2 in dry mode uses a scripted mock provider rather than `runConsolidation`, following the `tests/eval-harness-smoke.test.ts` pattern note — say so in the header, and have the output mark section 2's dry results as `provider: 'mock'` so a mock-derived number can never be mistaken for a live one.

**Never enable the knob from inside the harness.** The harness reads config, reports, and exits; flipping `provenanceDistinctnessEnabled` is Task 3's human decision. State this in the header.

**C. npm scripts.** Add to `package.json`, adjacent to the existing eval entries:
  - `"eval:drift05": "npm run build && node scripts/eval/drift-05-dry-run.cjs"`
  - `"eval:drift05:dry": "npm run build && node scripts/eval/drift-05-dry-run.cjs --dry-run"`
  </action>
  <verify>
    <automated>npm run build && node scripts/eval/drift-05-dry-run.cjs --dry-run --out /tmp/drift05-verify.json && node -e "const r=require('/tmp/drift05-verify.json'); if(!r.methodology) throw new Error('missing methodology'); if(!r.provenance_distinctness) throw new Error('missing section 1'); if(!r.belief_correction) throw new Error('missing section 2'); console.log('ok');"  </verify>
  <acceptance_criteria>
    - `npm run eval:drift05:dry` completes with exit code 0, makes zero network calls, and requires no API key
    - `scripts/eval/cases/drift-05-cases.json` holds at least 12 cases, every case has all eight required fields, and every one of the seven `scenario` values appears at least once
    - No case in the committed JSON contains a real email domain — every `from` value uses `.test` or `.example`
    - The output JSON has top-level `provenance_distinctness`, `belief_correction`, and `methodology` keys
    - The `methodology` block contains the literal phrase `no external accuracy bar exists for this feature class` and names all six Phase 65 config values used
    - Section 2's dry-mode results are tagged `provider: 'mock'`
    - The residual sweep reports a distinct-key count for each of the three default threshold values
    - `grep -c "provenanceDistinctnessEnabled = true\|provenanceDistinctnessEnabled: true" scripts/eval/drift-05-dry-run.cjs` returns `0` — the harness never enables the knob
    - `grep -c "dist/src/source/provenance-key" scripts/eval/drift-05-dry-run.cjs` is at least 1 — the real derivation is measured, not a reimplementation of it
    - `package.json` gains exactly two scripts and no dependency change (`git diff package.json` shows script additions only)
  </acceptance_criteria>
  <done>A convention-following harness reports key distinctness with a residual-threshold sweep, belief-correction accuracy against a labeled case set, and an embedded methodology block that cites no external bar — runnable with zero network in dry mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Zero-network smoke test for the case schema and the dry-run path</name>
  <files>tests/drift-05-harness-smoke.test.ts</files>
  <read_first>
    - tests/correctness-harness-smoke.test.ts (full file — the `REQUIRED_FIELDS` schema validation and the in-memory `Consolidator` + `MockModelProvider` wiring)
    - tests/eval-harness-smoke.test.ts (lines 1-60 — the `spawnSync` dry-run invocation pattern and the "Pattern note" about `runConsolidation` versus `Consolidator` in CI)
    - scripts/eval/drift-05-dry-run.cjs and scripts/eval/cases/drift-05-cases.json (written in Task 1)
    - src/source/provenance-key.ts (the derivation the harness must be shown to actually call)
  </read_first>
  <behavior>
    - The committed case file parses, holds at least 12 cases, every case carries all eight required fields with the right types, `case_id` values are unique, and every `scenario` value is one of the seven allowed strings
    - Every `from` value in the case file matches a synthetic-domain pattern (`.test` or `.example`) — no real correspondent is committed
    - Every case's `expected_distinct_provenance` is consistent with running the real `deriveGmailProvenanceKey` over that case's `messages` at the default threshold — the case file cannot drift away from the shipped derivation
    - `spawnSync`-ing the harness with `--dry-run` and a temp `--out` exits `0` and writes a JSON file with the three top-level sections
    - The written output's `methodology` block contains the no-external-bar sentence and the six config values
    - The harness exits non-zero with a clear message when `--cases` points at a nonexistent file
    - The harness makes no network call in dry mode — assert indirectly by running with `ANTHROPIC_API_KEY` and `OPENAI_API_KEY` unset and asserting success
  </behavior>
  <action>
Create `tests/drift-05-harness-smoke.test.ts` following `tests/correctness-harness-smoke.test.ts`'s structure: a `REQUIRED_FIELDS` array, a `readFileSync` + `JSON.parse` of the committed case file, and per-field type assertions.

Add the **case-file-versus-derivation consistency** block, which is the case that keeps this suite from being a schema rubber stamp: for every case, run the real `deriveGmailProvenanceKey` (imported from `src/source/provenance-key`, not from `dist/`) over that case's `messages` at `DEFAULT_CONFIG.provenanceMinResidualChars`, count the distinct keys, and assert it equals the case's `expected_distinct_provenance`. If a case's expectation is wrong, fix the case, not the assertion — say so in a comment.

Add the **dry-run invocation** block using `spawnSync` exactly as `tests/eval-harness-smoke.test.ts` does. Build first if the suite's conventions require it; if `dist/` staleness makes `spawnSync` flaky in CI, guard that block behind the same mechanism the existing eval smoke tests use and record which mechanism you used in the SUMMARY. Run with the two API-key env vars explicitly deleted from the child environment so the zero-network claim is enforced rather than assumed.

File header must state that this suite validates the harness's SHAPE and its dry path only — the actual DRIFT-05 numbers come from a human-run pass recorded in the phase SUMMARY, and a green smoke test is not evidence that the measurement was performed.
  </action>
  <verify>
    <automated>npx vitest run tests/drift-05-harness-smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/drift-05-harness-smoke.test.ts` passes with at least 8 cases
    - The consistency block calls the real `deriveGmailProvenanceKey` from `src/` for every case in the file
    - The synthetic-domain assertion covers every `from` value in every case
    - The `spawnSync` child environment has `ANTHROPIC_API_KEY` and `OPENAI_API_KEY` deleted
    - The missing-`--cases`-file case asserts a non-zero exit and a non-empty stderr message
    - Mutation check, recorded in the SUMMARY: temporarily change one case's `expected_distinct_provenance` to a wrong value, confirm the consistency block FAILS; revert
  </acceptance_criteria>
  <done>Eight-plus-case smoke suite green, the committed case file is pinned to the real derivation's behavior, and the dry path is proven to run with no API keys present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review the dry-run against real inbox threads and record the enablement decision</name>
  <what-built>
A DRIFT-05 harness (`npm run eval:drift05`) that reports, for a supplied set of real multi-email status threads:
  1. how many distinct provenance keys the redesigned derivation produces versus the single bucket today's collapsed key produces, with a sweep across `provenanceMinResidualChars` values 10 / 20 / 30;
  2. the fallback-reason distribution (`no-domain` / `no-thread-id` / `near-empty-residual`) and the residual-length distribution;
  3. belief-correction accuracy over the labeled case set, with the Phase 64 and Phase 65 observability counters, and an embedded methodology block that cites no external bar because none exists for this feature class.

Everything upstream is shipped and green, but `provenanceDistinctnessEnabled` is still `false` — the phase deliberately does not flip it. D-14 makes enablement a deliberate, recorded human decision, and research SUMMARY's Phase Ordering Rationale makes this review the gate on Phase 66.
  </what-built>
  <how-to-verify>
    1. Export a sample of real multi-email status threads from your own inboxes — rejections, recruiter chains, ATS auto-notices — as JSONL with one `{ from, thread_id, date, body }` object per line. Do NOT commit this file; write it outside the repo (for example `~/drift05-inbox.jsonl`).
    2. Run: `npm run eval:drift05 -- --inbox ~/drift05-inbox.jsonl --out scripts/eval/results/drift-05-<date>.json`
    3. Read the printed summary and answer four questions:
       - **Does independence look right?** Threads you consider genuinely independent should land on distinct keys. Spot-check five derived keys against the messages they came from.
       - **Does farming stay blocked?** Any forwarded or quoted duplicate in your sample should appear in the `near-empty-residual` fallback bucket, not as a new distinct key.
       - **Is the residual threshold placed sensibly?** Look at the sweep. If `20` collapses genuine short status emails you care about, or if `10` lets filler forwards through, the default is wrong and should change before enablement.
       - **Is the accuracy number honest?** Confirm the methodology block states the case count, the config, and what the number does not cover, and that it cites no external comparison.
    4. Decide and state one of:
       - `ENABLE` — flip `provenanceDistinctnessEnabled` to `true` in `DEFAULT_CONFIG`, optionally adjusting `provenanceMinResidualChars` and `contradictionNBySource.gmail` based on what the sweep showed.
       - `HOLD` — keep it `false` and name what would have to change.
    5. Whichever you choose, the committed result JSON (with real bodies redacted, or the file kept out of the repo — your call) and the decision go into the phase SUMMARY.
  </how-to-verify>
  <acceptance_criteria>
    - The founder confirms the reported distinct-key counts were produced from real inbox threads, not from the synthetic case set alone
    - The founder states `ENABLE` or `HOLD` explicitly, and the SUMMARY records that word verbatim
    - Under `ENABLE`, the only source diff is knob values in `src/lib/config.ts`, and `npx vitest run tests/runtime-config.test.ts` is updated in the same commit so the stock-default block matches the new shipped values
    - Under `HOLD`, the SUMMARY names the specific blocking observation and the config stays untouched
    - No number appears in the SUMMARY that the harness did not produce, and no external accuracy bar is cited
  </acceptance_criteria>
  <files>scripts/eval/results/drift-05-*.json, src/lib/config.ts, tests/runtime-config.test.ts, .planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-10-SUMMARY.md</files>
  <action>Run the harness against the founder-supplied `--inbox` export, present the three report sections and the residual sweep, and pause. This checkpoint is NOT auto-approvable: CONTEXT D-14 puts enabling the distinctness key in the same caution class as a `GMAIL_EXTRACTION_PROMPT` change, so `workflow.auto_advance` does not apply. Do not flip `provenanceDistinctnessEnabled` before the founder says `ENABLE`, and do not adjust any reported number in response to the checkpoint — if a figure looks wrong, re-run the harness rather than editing the text. Under `ENABLE`, change only the knob values named by the founder plus the matching assertions in `tests/runtime-config.test.ts`.</action>
  <verify>
    <human-check>Founder has reviewed the harness output against real inbox threads and stated ENABLE or HOLD</human-check>
  </verify>
  <done>The founder's ENABLE-or-HOLD decision is recorded verbatim in the SUMMARY alongside the reviewed numbers, and under ENABLE the knob values and their config-test assertions are updated together.</done>
  <resume-signal>Reply `ENABLE` (optionally with revised knob values) or `HOLD` (with the blocking reason)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real inbox export → harness input | The `--inbox` JSONL contains real correspondents' addresses and real message bodies — PII that must never enter the repository. |
| harness output → the enablement decision | The reported numbers are what a human uses to decide whether a load-bearing correctness mechanism goes live. A number that looks better than reality directly causes a bad enablement. |
| harness output → external claims | D-15 forbids claiming anything the measurement does not show, and forbids citing an external bar because none exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-10-PII | Information Disclosure | Real inbox content committed to the repo | mitigate | Real data enters only through an operator-supplied `--inbox` path that defaults to absent; the committed case file is synthetic and the smoke test asserts every `from` value uses a `.test` or `.example` domain. The checkpoint instructions tell the operator to write the export outside the repo and to redact or withhold the result JSON. |
| T-65-10-MOCKNUM | Repudiation | A mock-provider dry number presented as a live measurement | mitigate | Section 2's dry results are tagged `provider: 'mock'` in the output, and the smoke test's file header states that a green smoke run is not evidence the measurement was performed. |
| T-65-10-OVERCLAIM | Repudiation | Claiming accuracy the measurement does not support, or implying a competitive comparison | mitigate | The methodology block is embedded in the output JSON (not just in prose a reader might skip), must name the case count, the exact config, and the coverage gaps, and must contain the literal no-external-bar sentence — asserted by the smoke test. D-15. |
| T-65-10-SILENT | Elevation of Privilege | The key going live without human review | mitigate | The harness never writes config; a grep in Task 1's acceptance criteria enforces it. Enablement is a blocking human checkpoint (`autonomous: false`) that is explicitly not auto-approvable. |
| T-65-10-DRIFTCASE | Tampering | The committed case file drifting away from the shipped derivation | mitigate | The smoke test recomputes every case's expected distinct-provenance count with the real `deriveGmailProvenanceKey` and fails on mismatch, with a mutation check confirming it bites. |
| T-65-10-KEYLEAK | Information Disclosure | API keys reaching logs or output during a live run | mitigate | The harness copies `correctness-harness.cjs`'s guard shape: keys are checked for presence and never printed, and the dry path is asserted to succeed with both variables deleted from the child environment. |
| T-65-SC | Tampering | npm/pip/cargo installs | N/A | Zero package installs. The harness uses only Node builtins plus already-shipped `better-sqlite3` and compiled `dist/` modules; `package.json` gains two scripts and no dependency. Net-zero new runtime dependencies invariant holds. |
</threat_model>

<verification>
- `npm run build && npm run eval:drift05:dry` exits 0 with no API keys set
- `npx vitest run tests/drift-05-harness-smoke.test.ts` — all cases pass
- `npx vitest run tests/correctness-harness-smoke.test.ts tests/eval-harness-smoke.test.ts tests/episodic-dryrun-gate.test.ts` — no collateral regression in the eval suites
- `git diff package.json` shows two added scripts and no dependency change
- `git diff --stat -- src/` is empty unless the Task 3 decision is `ENABLE`, in which case the only source change is `src/lib/config.ts` knob values
- The phase SUMMARY records the Task 3 decision and the reviewed numbers
</verification>

<success_criteria>
A convention-following, zero-network-in-dry-mode harness measures provenance-key distinctness (with a residual-threshold sweep and a fallback-reason breakdown) and belief-correction accuracy against a labeled case set pinned to the real derivation, emits a methodology block that states its config and coverage and cites no external bar, never enables the knob itself, and has been run against real multi-inbox status threads with the resulting ENABLE-or-HOLD decision recorded — the gate research SUMMARY places in front of Phase 66.
</success_criteria>

<output>
Create `.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-10-SUMMARY.md` when done. Record: the measured distinct-key counts under the collapsed and derived schemes, the residual-threshold sweep results, the fallback-reason distribution, the belief-correction accuracy with its methodology restated, the Task 3 decision (`ENABLE` or `HOLD`) with any revised knob values, and the Task 2 mutation-check result. State explicitly that no external accuracy bar was cited because none exists for this feature class.
</output>
