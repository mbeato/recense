---
phase: 65-belief-gated-status-drift-provenance-distinctness-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-SESSION-ID-AUDIT.md
  - tests/session-id-provenance-consumers.test.ts
autonomous: true
requirements: [DRIFT-03]

must_haves:
  truths:
    - "Every `session_id` / `sessionId` consumer under src/ is enumerated with an explicit per-consumer verdict on whether per-thread granularity breaks it"
    - "A single LOCKED verdict is recorded selecting either the D-02 primary shape (richer sessionId mint) or the D-02 locked fallback (`provenance_key` on PendingContradiction) — no third invention"
    - "The audit's central claim (no consumer parses or branches on session_id string CONTENT) is locked by a shipped structural test, not by prose"
    - "The structural test is non-vacuous: a planted synthetic offender is caught by the same predicate the real walk uses"
    - "D-03 (existing gmail episodes keep their historical collapsed session_id; forward-only, no retroactive migration) is recorded as a named accepted limitation"
  artifacts:
    - path: ".planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-SESSION-ID-AUDIT.md"
      provides: "Per-consumer blast-radius table + LOCKED D-02 mechanism verdict + named accepted limitations"
      contains: "VERDICT"
      min_lines: 60
    - path: "tests/session-id-provenance-consumers.test.ts"
      provides: "Structural lock: no src/ file branches on session_id string content; planted-offender non-vacuousness check"
      min_lines: 90
  key_links:
    - from: ".planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-SESSION-ID-AUDIT.md"
      to: "src/adapter/ingest-cli.ts"
      via: "verdict names the exact edit site (or states it is unchanged under the fallback)"
      pattern: "ingest-cli"
    - from: "tests/session-id-provenance-consumers.test.ts"
      to: "src/"
      via: "recursive .ts walk through an exported pure predicate"
      pattern: "collectTsFiles|readdirSync"
---

<objective>
Discharge the D-02 **research gate** before any placement code is written: audit every `session_id` / `sessionId` consumer under `src/` for semantic breakage under per-thread granularity, and record a single LOCKED verdict selecting the D-02 primary shape (richer per-email `sessionId` minted at ingest) or the D-02 locked fallback (optional `provenance_key` on `PendingContradiction`, counted by `countDistinctProvenance`).

Purpose: `session_id` is currently `ingest:gmail` for every Gmail episode ever ingested, which makes `countDistinctProvenance` mathematically unable to exceed 1 on email-only evidence (research Pitfall 3). Changing what populates that column is a change to a load-bearing correctness mechanism's INPUT. CONTEXT D-02 makes the audit a hard gate: "If any consumer breaks, fallback is locked." Plan 65-07 wires whichever shape this plan selects.
Output: `65-SESSION-ID-AUDIT.md` with a per-consumer table and a LOCKED verdict, plus a shipped structural test that keeps the audit's central claim true as the codebase evolves.

**Not in this plan:** no `src/` changes of any kind. No key derivation (65-04), no adapter capture (65-06), no mint-site edit (65-07). This plan produces a decision and a lock, nothing else.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-CONTEXT.md
@.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Live-verified 2026-08-02. The complete consumer surface — use this list, do not re-derive it blind. -->

`grep -rn "session_id\|sessionId" src --include="*.ts"` returns 48 hits across 17 files. The full file list:

  src/adapter/import-memory-cli.ts     src/adapter/ingest-cli.ts        src/adapter/ingest-project-cli.ts
  src/adapter/memory-ops.ts            src/adapter/serve-cli.ts         src/adapter/stop-cli.ts
  src/adapter/turn-capture-cli.ts      src/consolidation/consolidator.ts src/consolidation/update-decision.ts
  src/db/episode-store.ts              src/db/schema.ts                 src/db/semantic-store.ts
  src/eval/snapshot.ts                 src/ingest/pipeline.ts           src/lib/types.ts
  src/recall/index.ts                  src/responder/index.ts

Known shapes at the two ends of the flow:
- `src/adapter/ingest-cli.ts:188` — `sessionId: \`ingest:${r.source}\`` inside `appendBatch`'s `db.transaction` loop (the D-02 primary edit site).
- `src/consolidation/update-decision.ts:90-98` — `countDistinctProvenance(entries)`: `new Set<string>()`, `if (entry.origin !== 'inferred') sessions.add(entry.session_id)`, returns `sessions.size`. The ONLY semantic consumer of session_id VALUES.
- `src/consolidation/consolidator.ts:1439` and `:1590` — `session_id: decision.episodeSessionId` written into `PendingContradiction` at the two `recordContradiction` sites.
- `src/lib/types.ts:9-18` — `PendingContradiction { episode_id: string; session_id: string; origin: Origin }` (the D-02 fallback edit site).
- `src/db/schema.ts:34` — `session_id TEXT NOT NULL` on `episode`. No index on it.
- `src/ingest/pipeline.ts:31,88` — `RecordEventParams.sessionId` → `store.append({ session_id: e.sessionId })`. Pure pass-through.
- `src/db/episode-store.ts:32,121-125,217` — column read/write only; no parsing, no prefix matching.

`tests/src-import-boundary.test.ts` and `tests/no-ats-domain-table.test.ts` are the two shipped structural-guard precedents in this repo. `no-ats-domain-table.test.ts` is the closer analog: exported pure predicate + real `src/` walk asserting `[]` + a planted-offender non-vacuousness check run through the SAME predicate.

Known pre-existing test assertion on a session literal: `tests/episode-event-ts.test.ts:302` asserts `row.session_id === 'ingest:granola'` (a NON-gmail source — must remain true under every shape considered here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit every session_id consumer and record the LOCKED D-02 verdict</name>
  <files>.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-SESSION-ID-AUDIT.md</files>
  <read_first>
    - src/adapter/ingest-cli.ts (lines 160-205 — the `runPullPhase` append transaction and the mint site at :188)
    - src/consolidation/update-decision.ts (full file, 99 lines — `countDistinctProvenance` is the only value-semantic consumer)
    - src/consolidation/consolidator.ts (lines 1427-1520 and 1582-1625 — both `recordContradiction` sites that write `session_id`)
    - src/ingest/pipeline.ts (lines 26-98 — `RecordEventParams.sessionId` pass-through)
    - src/db/episode-store.ts (lines 25-70 and 110-225 — the `EpisodeRow.session_id` column and `append`)
    - src/recall/index.ts (lines 130-150, 235-250, 395-410, 565-580 — the four sessionId uses)
    - src/responder/index.ts (lines 130-250)
    - src/adapter/memory-ops.ts (lines 160-175, 295-310, 390-400, 420-430)
    - src/eval/snapshot.ts (lines 40-105)
    - src/db/semantic-store.ts (lines 430-455 — `recordContradiction` and its doc comment)
    - src/lib/types.ts (lines 1-30 — `PendingContradiction`)
    - .planning/research/PITFALLS.md (Pitfall 3 in full — the farmable-external_id trap and the sender+thread-lineage prescription)
    - .planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
Run `grep -rn "session_id\|sessionId" src --include="*.ts"` yourself first and reconcile the result against the 17-file list in `<interfaces>`; if the live grep disagrees, the live grep wins and the extra/missing files must appear in the table with their own verdict.

Create `.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-SESSION-ID-AUDIT.md` containing, in this order:

**1. Scope statement.** One paragraph: what per-thread granularity means concretely — today every Gmail episode carries the literal `ingest:gmail`; under the D-02 primary shape a Gmail episode would carry `ingest:gmail:<normalized-sender-domain>:<gmail-thread-id>`, so the column's cardinality for `source='gmail'` goes from exactly 1 to roughly one-per-(sender-domain, thread). Non-gmail sources are unaffected in every shape considered.

**2. Per-consumer table** with one row per file from the live grep, columns: `File` | `Use of session_id` | `Semantics assumed` | `Breaks under per-thread granularity?` | `Evidence (line refs)`. Classify each use into exactly one of four kinds and say which in the "Use" column:
  - `mint` — constructs a session id value (ingest-cli, import-memory-cli, ingest-project-cli, turn-capture-cli, stop-cli, memory-ops)
  - `passthrough` — carries the value without inspecting it (pipeline, episode-store, schema)
  - `value-semantic` — the value's identity/cardinality changes behavior (expected: `update-decision.ts` `countDistinctProvenance` only)
  - `unrelated` — a different `sessionId` concept entirely, not the episode column (expected: recall, responder, memory-ops MCP session, serve-cli MCP transport, eval/snapshot `created_session`)

  For every row you classify `unrelated`, state in the Evidence column WHY it is a different concept (e.g. "recall mints its own per-recall UUID for the inferred episode it writes; never reads an ingest-minted value").

**3. Cardinality / cost check.** State explicitly: (a) `episode.session_id` has no index (`src/db/schema.ts` — confirm by grep, no `idx_*session*` exists), so higher cardinality adds no index-maintenance cost; (b) `node.pending_contradictions` is an unbounded JSON array appended by `SemanticStore.recordContradiction` with no cap — higher key cardinality means the `Set` in `countDistinctProvenance` grows, which is the INTENDED effect, and note the array itself is bounded in practice only by force-destabilization tombstoning the node.

**4. VERDICT section** headed literally `## VERDICT` containing:
  - `**Shape selected:** PRIMARY (richer sessionId minted at ingest)` **or** `**Shape selected:** FALLBACK (provenance_key on PendingContradiction)` — exactly one, stated in that literal form so Plan 65-07 can grep it.
  - The decision rule applied verbatim from CONTEXT D-02: select PRIMARY only if ZERO consumers classified `value-semantic` other than `countDistinctProvenance` AND zero consumers classified `passthrough` are shown to parse, prefix-match, split, or equality-compare an ingest-minted session id. Otherwise select FALLBACK. No third shape may be invented.
  - Two-to-four sentences of justification citing the specific rows that drove the call.
  - Under PRIMARY: name the exact edit site `src/adapter/ingest-cli.ts:188` and state that `src/lib/types.ts`, `src/consolidation/update-decision.ts`, and `src/db/schema.ts` stay byte-unchanged.
  - Under FALLBACK: name the exact edit set — `src/lib/types.ts` (`provenance_key?: string` on `PendingContradiction`), `src/consolidation/update-decision.ts` (`sessions.add(entry.provenance_key ?? entry.session_id)` — that ONE line, D-19 `origin !== 'inferred'` exclusion and Set structure preserved), `src/db/schema.ts` (nullable `episode.provenance_key` column + migration, since consolidation runs in a later process than ingest and the key must survive on the row), `src/ingest/pipeline.ts` (`RecordEventParams.provenanceKey`), `src/db/episode-store.ts` (column write/read), and `src/consolidation/consolidator.ts` (a `episodeProvenanceKey` field on `ClaimDecision` plus both `recordContradiction` sites) — and state that `src/adapter/ingest-cli.ts:188` stays byte-unchanged.

**5. Named accepted limitations** (these ship regardless of shape; write them as a numbered list, each with a one-line "why this is acceptable"):
  - **D-03 forward-only:** existing Gmail episodes keep their historical `ingest:gmail` session id. No retroactive migration. Pending contradictions already accumulated under the collapsed id count as one bucket forever; the redesign only affects episodes ingested after enablement.
  - **Sender-domain rotation:** an actor controlling three distinct sending domains can still reach `contradictionN` legitimately-shaped. This is the same bar as controlling three Claude Code sessions and is not made worse by this change; it is bounded by the residual gate (Plan 65-02) and by `contradictionNBySource` (Plan 65-03).
  - **D-12 cross-run/cross-account interleaving:** out-of-order evidence spanning separate sleep-pass runs or separate account backfills is NOT closed by this milestone (honest-null convention, research Pitfall 5 third bullet). Bi-temporal validity columns stay deferred.

Do NOT edit any file under `src/` in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-SESSION-ID-AUDIT.md && grep -c "^## VERDICT" .planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-SESSION-ID-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `65-SESSION-ID-AUDIT.md` exists and contains a line matching `^## VERDICT`
    - `grep -E "\*\*Shape selected:\*\* (PRIMARY|FALLBACK)" 65-SESSION-ID-AUDIT.md` returns exactly 1 match
    - The per-consumer table has at least one row per file returned by `grep -rln "session_id\|sessionId" src --include="*.ts"` — verify the row count matches the file count, and record both numbers in the SUMMARY
    - Every table row's "Breaks under per-thread granularity?" cell is one of `no` / `yes` (never blank, never "maybe")
    - Exactly one row is classified `value-semantic`, and it is `src/consolidation/update-decision.ts`; if a second one appears, the verdict MUST be FALLBACK
    - The document contains the literal string `ingest-cli.ts:188`
    - Section 5 contains three numbered limitations including one whose text contains `D-03` and one whose text contains `D-12`
    - `git diff --stat -- src/` shows zero changes (this task touches no source file)
  </acceptance_criteria>
  <done>A per-consumer blast-radius table covering every live grep hit, a single grep-able LOCKED verdict, and three named accepted limitations are recorded. No source file changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship the structural lock that keeps the audit's central claim true</name>
  <files>tests/session-id-provenance-consumers.test.ts</files>
  <read_first>
    - tests/no-ats-domain-table.test.ts (full file — the exported-predicate + real-walk + planted-offender shape to mirror exactly)
    - tests/src-import-boundary.test.ts (the second structural-guard precedent; skim for the walk helper style)
    - .planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-SESSION-ID-AUDIT.md (written in Task 1 — the claim this test locks)
    - src/adapter/ingest-cli.ts (line 188 — the one legitimate template-literal mint, which must NOT be flagged)
    - src/adapter/import-memory-cli.ts (line 221), src/adapter/ingest-project-cli.ts (lines 349, 553) — the other legitimate mints
  </read_first>
  <behavior>
    - `findSessionIdContentBranches(source, relPath)` returns `[]` for a file that only assigns or passes through a session id (e.g. the `pipeline.ts` body)
    - It returns `[]` for a template-literal MINT such as `` sessionId: `ingest:${r.source}` `` — constructing a value is allowed; inspecting one is not
    - It returns a non-empty array for `if (session_id.startsWith('ingest:'))`
    - It returns a non-empty array for `sessionId.split(':')[1]`
    - It returns a non-empty array for `episode.session_id === 'ingest:gmail'`
    - It returns a non-empty array for `sessionId.includes('ingest:')` and for a regex test against a session id
    - It ignores matches inside `//` line comments and `/* */` block comments (the audit doc and module doc comments name these patterns in prose)
    - The real walk over `src/**/*.ts` returns `[]`
  </behavior>
  <action>
Create `tests/session-id-provenance-consumers.test.ts`, structurally mirroring `tests/no-ats-domain-table.test.ts`:

Export a pure predicate `findSessionIdContentBranches(source: string, relPath: string): string[]` returning one human-readable offense string per violation (`${relPath}:${lineNo} — ${trimmedLine}`). It must:
  - strip `//` line comments and `/* */` block comments BEFORE matching — mandatory, because this test file's own header, the audit doc, and the Phase 65 module doc comments all name these forbidden patterns in prose, so an unstripped scan would self-invalidate
  - flag any expression that INSPECTS a session id's string content: a `.startsWith(`, `.endsWith(`, `.includes(`, `.split(`, `.slice(`, `.substring(`, `.match(`, `.indexOf(`, or a `===`/`!==`/`==`/`!=` comparison against a string literal, when the receiver/left operand is an identifier or member expression whose final segment is `session_id` or `sessionId`
  - NOT flag assignment or object-property construction (`sessionId: <anything>`, `session_id: <anything>`, `session_id = <anything>`) — minting is legitimate and happens at five sites today
  - NOT flag `src/lib/types.ts` interface declarations or `src/db/schema.ts` DDL strings

Use a regex-based scan (no TypeScript AST dependency — net-zero new runtime dependencies applies to devDependencies here too; the two precedent guards are both regex-based). Compile every pattern once at module scope.

Add three `describe` blocks:
  1. **real walk** — recursively collect `src/**/*.ts` with a local `collectTsFiles` helper (copy the shape from `no-ats-domain-table.test.ts`), run the predicate over every file, and `expect(offenses).toEqual([])`. On failure the message must list the offending file:line, so build the array of strings rather than a boolean.
  2. **non-vacuousness** — plant each of the five synthetic offenders from the behavior block through the SAME exported predicate and assert each returns a non-empty array. Two independently-maintained pattern lists is the exact duplication defect this guard exists to prevent — the planted offenders must go through the real predicate, never a copy of it.
  3. **allowed-mint regression** — feed the four real mint lines verbatim (`` sessionId: `ingest:${r.source}` ``, `` sessionId: `${IMPORT_SOURCE}:${item.project}` ``, `` sessionId: `project-survey:${scope}:${area}` ``, `` sessionId: `project-doc:${scope}:${relPath}` ``) and assert `[]`, so the guard cannot be satisfied by banning construction.

File header must state: what claim this locks (audit section 2 — no consumer branches on session id CONTENT, which is what makes per-thread granularity safe), why it is review-blocking (weakening it silently reopens the D-02 gate the audit closed), and that the comment-stripping is load-bearing.
  </action>
  <verify>
    <automated>npx vitest run tests/session-id-provenance-consumers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/session-id-provenance-consumers.test.ts` passes
    - The real-walk case asserts `toEqual([])` over every `src/**/*.ts` file (record the file count scanned in the SUMMARY; it must be > 100)
    - All five planted offenders are caught, each through the exported `findSessionIdContentBranches` (not through an inline copy of the regexes)
    - All four real mint lines return `[]`
    - `grep -c "findSessionIdContentBranches" tests/session-id-provenance-consumers.test.ts` is at least 8 (one export + real walk + five planted + four allowed, with reuse)
    - Mutation check, recorded in the SUMMARY: temporarily add `if (episode.session_id.startsWith('ingest:')) { /* planted */ }` to `src/ingest/pipeline.ts`, confirm the real-walk case FAILS and names `pipeline.ts`, then revert and confirm green again
  </acceptance_criteria>
  <done>A non-vacuous structural guard ships that fails if any future src/ code starts inspecting session-id string content, with the mutation check recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed in this plan | This plan writes one planning document and one test file. No runtime code path, no network, no data flow, no package install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01-A | Repudiation | The D-02 mechanism decision | mitigate | The verdict is written to a versioned, committed artifact with the decision rule stated verbatim and the driving evidence rows cited, so the choice is auditable rather than implicit in a diff. |
| T-65-01-B | Tampering | Silent erosion of the audit's premise by later code | mitigate | Task 2 ships a shipped structural test over the whole `src/` tree with a planted-offender non-vacuousness check and a recorded mutation check — the premise is enforced by CI, not by review memory. |
| T-65-SC | Tampering | npm/pip/cargo installs | N/A | Zero package installs in this plan. The guard is regex-based specifically to avoid adding an AST parser (net-zero new runtime dependencies invariant). No install task exists in any Phase 65 plan. |
</threat_model>

<verification>
- `65-SESSION-ID-AUDIT.md` exists, contains `## VERDICT`, and exactly one `**Shape selected:** PRIMARY|FALLBACK` line
- `npx vitest run tests/session-id-provenance-consumers.test.ts` passes
- `npm run typecheck` exits 0 (the new test file is type-checked by `tests/tsconfig.json`)
- `git diff --stat -- src/` shows zero source changes from this plan
</verification>

<success_criteria>
The D-02 research gate is discharged: every live `session_id` consumer is enumerated with an explicit verdict, one of the two locked shapes is selected in a grep-able form that Plan 65-07 consumes, three limitations (including D-03 forward-only and D-12 interleaving) are named rather than silently shipped, and the audit's central claim is locked by a non-vacuous structural test.
</success_criteria>

<output>
Create `.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-01-SUMMARY.md` when done. Record: the selected shape verbatim, the consumer-file count vs table-row count, the number of `src/` files the guard scanned, and the Task 2 mutation-check result.
</output>
