---
phase: 65-belief-gated-status-drift-provenance-distinctness-fix
plan: 08
type: execute
wave: 5
depends_on: ["65-05", "65-07"]
files_modified:
  - src/consolidation/consolidator.ts
  - tests/status-drift-wiring.test.ts
autonomous: true
requirements: [DRIFT-01, DRIFT-02, DRIFT-04]

must_haves:
  truths:
    - "Every ClaimDecision carries its episode's event_ts and source, filled at all three existing fill sites"
    - "The drift layer is consulted before BOTH routeContradiction call sites — the primary contradict branch and the secondary-contradiction mirror"
    - "A drop decision produces no graph effect at all: no routing, no recordContradiction, no sink emit"
    - "routeContradiction receives the damped magnitude and is itself unmodified"
    - "The force-destabilization threshold is looked up per-source with a fallback to the global contradictionN, at both distinct-count sites"
    - "A non-gmail decision takes a byte-identical path to pre-Phase-65: no drift query runs and the magnitude is unchanged"
    - "Drift outcomes are counted and logged at pass end, mirroring the Phase 46 and Phase 64 observability precedents"
  artifacts:
    - path: "src/consolidation/consolidator.ts"
      provides: "episodeEventTs/episodeSource on ClaimDecision, StatusDrift consulted at both contradict branches, per-source contradictionN, drift counters"
      contains: "statusDrift"
    - path: "tests/status-drift-wiring.test.ts"
      provides: "Wiring-level proof that the drift layer is consulted at both branches and is a no-op off the gmail path"
      min_lines: 180
  key_links:
    - from: "src/consolidation/consolidator.ts"
      to: "src/consolidation/status-drift.ts"
      via: "StatusDrift instantiated in the constructor and evaluated before both routeContradiction calls"
      pattern: "StatusDrift|statusDrift"
    - from: "src/consolidation/consolidator.ts"
      to: "src/lib/config.ts"
      via: "contradictionNBySource[source] ?? contradictionN at both distinct-count sites"
      pattern: "contradictionNBySource"
---

<objective>
Wire the drift layer into the consolidator's two contradict branches, thread the two episode fields it needs onto `ClaimDecision`, and land D-16's per-source force-destabilization threshold.

Purpose: Plans 65-05 and 65-07 built a decision module and a working distinctness key that nothing consults. This plan connects them — placing the drift evaluation structurally BEFORE `routeContradiction` (never inside it), inheriting the existing hard stop by call-site position rather than re-checking it, and keeping every non-Gmail decision on a byte-identical path.
Output: two new `ClaimDecision` fields, drift evaluation at both branches, per-source `contradictionN`, four observability counters, and a wiring test.

**Not in this plan:** no end-to-end DRIFT-02 / DRIFT-04 proofs and no D-13 emission sentinel — those are 65-09, which extends this plan's wiring with full-pass fixtures. No sink, no `action_proposal` table, no `ActionProposalSink` (Phase 66).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-CONTEXT.md
@.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Live-verified 2026-08-02, plus the upstream Phase 65 plans. -->

From Plan 65-05 (`src/consolidation/status-drift.ts`):
- `new StatusDrift(db: Database.Database, config: EngineConfig)` — no `ModelProvider`, no `SemanticStore`
- `evaluate(input: { magnitude: number; confidence?: IntentConfidence; claimEventTs: number | null; candidateNodeId: string }): DriftDecision`
- `DriftDecision = { action: 'proceed'; magnitude: number; damped: boolean; staleness: 'ok' | 'unknown-claim-ts' | 'unknown-prior-ts' | 'guard-off' | 'not-applicable' } | { action: 'drop'; reason: 'stale-event'; priorEventTs: number; claimEventTs: number }`
- `isEmissionEligible(eventType)`, `EMISSION_ELIGIBLE_EVENT_TYPES` — exported for Phase 66; NOT consumed by this plan

`src/consolidation/consolidator.ts` — the exact edit sites:
- `ClaimDecision` interface at lines 152-234. Existing episode-derived fields to mirror: `episodeSessionId: string` (:158), `episodeSourceInferenceId: string | null` (:162), `episodeRole: EpisodeRole` (:164).
- **Three `ClaimDecision` fill sites**, each containing the line `episodeSessionId: episode.session_id,`:
  - ~line 815 — D-17 fast-path confirm
  - ~line 912 — auto-unrelated
  - ~line 1001 — post-judge fill
  Locate all three by grepping `episodeSessionId: episode.session_id`. The grep must return exactly 3; if it returns a different count, reconcile before editing.
- `EntityResolver` instantiation precedent, constructor line 329: `this.entityResolver = new EntityResolver(db, store, config);` with the field declared at line 274 as `private readonly entityResolver: EntityResolver;`
- **Primary contradict branch**, `applyDecision`, lines 1324-1517. Key landmarks: the secondary loop at :1331-1335; `if (!decision.bestCandidateId) break;` at :1337; `const node = this.store.getNode(...)` at :1341; `const resistance = effectiveS * node.c;` at :1347; `const action = routeContradiction(decision.magnitude, resistance, this.config);` at :1350; the hold branch at :1427-1515 containing `recordContradiction`, `countDistinctProvenance` (:1448), and `if (distinctCount >= this.config.contradictionN)` (:1452).
- **Secondary mirror**, `applySecondaryContradiction`, lines 1543-1625. Same shape: `routeContradiction` at :1555, `countDistinctProvenance` at :1597, `if (distinctCount >= this.config.contradictionN)` at :1599.
- **Observability counter precedent**, declared at `consolidate()` method scope so they accumulate across chunks: Phase 46's at lines 628-634 (`cosineCandidateTotal`, `judgeFiredContradiction`, ...) and Phase 64's at lines 636-646 (`resolutionAttempts`, `resolutionHits`, `resolutionAbstains`, ...). Phase 64's block comment explicitly names Phase 65's DRIFT-05 measurement as the consumer of that data.
- **Hard stop**, line 706: `if (episode.origin === 'inferred' || echoSourceId !== null || episode.source === 'hitl') { this.markSkipped(episode.id); continue; }` — preceded by the `WR-01 / CR-01 / ACT-03` comment block at :691-705.
- **Hoisted source gate**, line 716: `const gmailSourced = episode.source === 'gmail';` with the comment stating it must never be hoisted above the hard stop.

`src/lib/types.ts` — `EpisodeRow` carries `event_ts?: number | null` and `source: string`.

`src/lib/config.ts` (Plan 65-03): `contradictionNBySource: Record<string, number>` (default `{}`), `statusDriftEnabled`, `statusDriftConfidenceDamping`, `statusDriftEventTsGuard`.

`tests/consolidation-intent.test.ts` (563 lines) — the consolidator-integration harness: `Database(':memory:')` + `initSchema`, `FakeClock`, `makeSyntheticEmbedFn` / `makeZeroEmbedFn` / `makeAlwaysSameEmbedFn`, and an **instance-level spy** that monkey-patches `applyDecision` on the single `Consolidator` under test (a real subclass would collide because `applyDecision` is a TS `private`). Reuse this harness; do not build a new one.
`tests/backfill-chronological-order.test.ts` — the content-keyed `DynamicReconcileProvider` pattern for driving a real `consolidate()` pass where the contradiction target is minted at runtime. Its header explains why a call-order-indexed provider script would produce a green test that proves nothing; that reasoning applies to this plan's fixtures too.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread episodeEventTs and episodeSource onto ClaimDecision</name>
  <files>src/consolidation/consolidator.ts</files>
  <read_first>
    - src/consolidation/consolidator.ts (lines 150-235 for the `ClaimDecision` interface; lines 800-830, 900-930, and 990-1020 for the three fill sites)
    - src/lib/types.ts (the `EpisodeRow` shape — `event_ts` and `source`)
    - .planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-CONTEXT.md (D-11b, D-16)
  </read_first>
  <action>
Add two fields to `ClaimDecision`, positioned immediately after `episodeSessionId` (line ~158), each with a JSDoc matching the density of its neighbours:

  - `episodeEventTs: number | null;` — the episode's source-asserted event time (EMAIL-04), or `null` when the source asserts none. JSDoc must state: consumed by the drift layer's `event_ts` staleness guard (DRIFT-04 / D-11b); `null` makes the guard abstain into pre-Phase-65 behavior rather than guess; this is NOT `episode.ts` (ingest time), and the two must never be conflated — `ts` is when recense LEARNED the episode, `event_ts` is when the source says it happened.
  - `episodeSource: string;` — the episode's source adapter name. JSDoc must state: consumed by the per-source force-destabilization threshold lookup (`contradictionNBySource`, D-16). Note that a `gmailSourced` local already exists at line 716 for the intent-field gate and that this field is deliberately the raw source string rather than a boolean, because `contradictionNBySource` is keyed by arbitrary source names.

Fill both at all three fill sites, immediately after the existing `episodeSessionId: episode.session_id,` line:

  `episodeEventTs: episode.event_ts ?? null,`
  `episodeSource: episode.source,`

Both are non-optional, so TypeScript will fail the build if any fill site is missed — that is deliberate and is why they are declared required rather than optional. State that in the interface JSDoc so a future reader does not "helpfully" make them optional.

Grep `episodeSessionId: episode.session_id` first and confirm it returns exactly 3 matches before editing; if the count differs, stop and reconcile rather than editing the three you happen to find.

Make no other change in this task — no drift call, no counters, no threshold lookup.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/consolidation.test.ts tests/consolidator.test.ts tests/consolidation-intent.test.ts tests/consolidation-resolution.test.ts tests/consolidation-temporal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "episodeEventTs" src/consolidation/consolidator.ts` returns exactly 4 (interface field + three fill sites)
    - `grep -c "episodeSource:" src/consolidation/consolidator.ts` returns exactly 4
    - `grep -c "episodeSessionId: episode.session_id" src/consolidation/consolidator.ts` still returns exactly 3
    - Both fields are declared required (no `?`) on `ClaimDecision`
    - `npm run typecheck` exits 0
    - All five listed consolidation suites pass unchanged — this task is pure field threading with no behavior change
  </acceptance_criteria>
  <done>Both episode-derived fields are on `ClaimDecision`, filled at all three sites, required so a missed site is a compile error, with no behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: Consult the drift layer at both contradict branches and land per-source contradictionN</name>
  <files>src/consolidation/consolidator.ts</files>
  <read_first>
    - src/consolidation/consolidator.ts (lines 265-340 for the field declarations and constructor; lines 620-720 for the counter block and the hard stop; lines 1324-1520 for the primary contradict branch; lines 1521-1630 for `applySecondaryContradiction`)
    - src/consolidation/status-drift.ts (Plan 65-05 — the constructor, `evaluate`, and the `DriftDecision` union in full)
    - src/consolidation/entity-resolution.ts (the Phase 64 module this one's instantiation mirrors)
    - src/consolidation/update-decision.ts (full file — the function being fronted, which must not be modified)
    - .planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-CONTEXT.md (D-08, D-09, D-11, D-13, D-16)
    - .planning/research/PITFALLS.md (Pitfall 5's routing-decision half; Pitfall 6's "a separate scan does not inherit the guard")
  </read_first>
  <action>
**A. Instantiate.** Declare `private readonly statusDrift: StatusDrift;` beside `entityResolver` (line ~274) and assign `this.statusDrift = new StatusDrift(db, config);` beside the `EntityResolver` assignment (line ~329). Import `StatusDrift` and the `DriftDecision` type from `./status-drift`.

**B. Counters.** Add four `let` counters at `consolidate()` method scope, in the same block as Phase 64's `resolutionAttempts` group (lines ~636-646), with a block comment in the same style stating they are observability only and never gate behavior, and naming Plan 65-10's DRIFT-05 measurement as the consumer:
  - `driftEvaluations` — incremented once per `evaluate` call
  - `driftDamped` — incremented when a `proceed` decision reports `damped: true`
  - `driftStaleDropped` — incremented on every `drop`
  - `driftEventTsUnknown` — incremented when a `proceed` decision's `staleness` is `'unknown-claim-ts'` or `'unknown-prior-ts'`

Because `applyDecision` and `applySecondaryContradiction` are separate methods, these method-scoped counters cannot be incremented from inside them directly. Choose ONE of these two shapes and state which in the SUMMARY, with the reason:
  (a) promote the four counters to private instance fields reset at the top of `consolidate()`, or
  (b) have the two branch methods return or accumulate into a small per-pass object passed down.
Prefer (a) — it matches how the existing per-pass state is already reset and keeps both call sites uniform. Whichever you choose, the reset must happen at the START of `consolidate()` so counts are per-pass, not cumulative across the process lifetime.

Log them at pass end wherever the existing Phase 46 / Phase 64 counters are logged; find that site by grepping `resolutionAbstains` and follow the same format.

**C. Primary branch** (`applyDecision`, `case 'contradict'`). Insert the drift evaluation so that it runs **before the secondary loop at line 1331**, because a stale claim must have no graph effect on secondaries either:

  1. At the top of the `case 'contradict':` block, before the `for (const secId of decision.contradictedIds)` loop, evaluate against the PRIMARY candidate when there is one. Guard on `decision.bestCandidateId !== null`; when it is null there is no node to compare timestamps against, so skip evaluation and keep today's behavior (the branch already `break`s at :1337).
  2. `const drift = this.statusDrift.evaluate({ magnitude: decision.magnitude, confidence: decision.claimIntentConfidence, claimEventTs: decision.episodeEventTs, candidateNodeId: decision.bestCandidateId });`
  3. `if (drift.action === 'drop') { <counter>++; break; }` — `break` out of the whole `case`, so no secondary routing, no primary routing, no `recordContradiction`, and no sink emit occurs. Comment that this deliberately mirrors the existing "not provenance-eligible: drop silently" behavior at line 1514, and that dropping rather than holding is the DRIFT-04 crux: a hold would record a `pending_contradictions` entry and three stale backfilled rejections would then force-destabilize a newer offer.
  4. Replace `routeContradiction(decision.magnitude, resistance, this.config)` at line 1350 with `routeContradiction(drift.magnitude, resistance, this.config)`, where `drift.magnitude` is the `proceed` arm's possibly-damped value. **Do not modify `routeContradiction` itself and do not add a branch inside it** — the guard sits structurally before it (D-11), which is what keeps DRIFT-01's "machinery unmodified" true.
  5. Keep the `decision.magnitude` value in every `sink.emit` payload UNCHANGED — the sink records the judge's own emitted severity, and rewriting it with the damped value would make the audit trail lie about what the judge said. Add a comment at the first emit site saying so; this is a real trap because the two values are now different and "consistency" is the tempting wrong call.

  Position the whole block textually AFTER the `WR-01 / CR-01 / ACT-03` hard stop (which lives in the per-episode loop and has already `continue`d out inferred/echo/hitl episodes before `applyDecision` is reached) and add a comment saying so, mirroring the Phase 64 branch comment at lines 1020-1028. **Do not re-check origin, echo, or hitl here** — a second check with different logic is exactly the drift-prone duplication Pitfall 6 warns about.

**D. Secondary branch** (`applySecondaryContradiction`). Apply the same three steps against the SECONDARY node id: evaluate with `candidateNodeId: nodeId`, `return` early on `drop` (this method returns `void`, so `return` is the equivalent of the primary's `break`), and feed the damped magnitude to `routeContradiction` at line 1555. Staleness is evaluated per node because "most recent supporting evidence" is a property of the node, not of the claim — comment this. Confidence damping resolves to the same factor at both branches because it is a claim-level property; that is expected, not duplicated logic.

**E. Per-source contradictionN (D-16).** At BOTH `if (distinctCount >= this.config.contradictionN)` sites (lines ~1452 and ~1599), replace the threshold with:

  `const threshold = this.config.contradictionNBySource[decision.episodeSource] ?? this.config.contradictionN;`

then compare against `threshold`. Mirror the existing per-source lookup idiom already used for `consolSkipThresholdBySource` at lines 668-672. Comment that the empty default map means every source falls back to the global `3`, so this is behavior-neutral until an entry is added, and that Plan 65-10's dry-run is what supplies evidence for any entry.

Make no change to `src/consolidation/update-decision.ts`, `src/consolidation/sink.ts`, or `src/db/schema.ts` in this task.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/consolidation.test.ts tests/consolidator.test.ts tests/consolidation-intent.test.ts tests/consolidation-resolution.test.ts tests/consolidation-temporal.test.ts tests/consolidation-source.test.ts tests/update-decision.test.ts tests/pe-machinery-lock.test.ts tests/backfill-chronological-order.test.ts tests/sink.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "statusDrift" src/consolidation/consolidator.ts` is at least 4 (field, constructor assignment, primary call, secondary call)
    - `grep -c "routeContradiction(" src/consolidation/consolidator.ts` still returns exactly 2 — no third call site was introduced
    - Neither `routeContradiction(` call passes `decision.magnitude` any more: `grep -c "routeContradiction(decision.magnitude" src/consolidation/consolidator.ts` returns `0`
    - `grep -c "contradictionNBySource" src/consolidation/consolidator.ts` returns exactly 2 (both distinct-count sites)
    - `grep -c "this.config.contradictionN)" src/consolidation/consolidator.ts` returns `0` — neither site compares against the global directly any more
    - Every `sink.emit` payload still carries `magnitude: decision.magnitude` — `grep -c "magnitude: drift" src/consolidation/consolidator.ts` returns `0`
    - The four counters exist and are reset per pass (verify by reading the reset site)
    - `git diff --stat -- src/consolidation/update-decision.ts src/consolidation/sink.ts src/db/schema.ts` shows zero changes
    - `npx vitest run tests/pe-machinery-lock.test.ts` passes — the DRIFT-01 pin is intact
    - `npm run typecheck` exits 0
    - Every listed suite passes. If `tests/consolidation-intent.test.ts` or `tests/backfill-chronological-order.test.ts` fails, do NOT relax the assertion — investigate whether the drift layer changed a path it should have left alone, and record the finding in the SUMMARY
  </acceptance_criteria>
  <done>The drift layer is consulted before both routing calls, a drop short-circuits every graph effect including secondaries, the damped magnitude reaches `routeContradiction` while the sink still records the judge's own value, and the force-destabilization threshold is per-source with a behavior-neutral empty default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wiring test — both branches consulted, non-gmail untouched, counters accurate</name>
  <files>tests/status-drift-wiring.test.ts</files>
  <read_first>
    - src/consolidation/consolidator.ts (as modified in Tasks 1-2)
    - tests/consolidation-intent.test.ts (full file, 563 lines — the harness, the embed-fn helpers, and the instance-level `applyDecision` spy technique; reuse all of it)
    - tests/consolidation-resolution.test.ts (Phase 64's wiring test — the closest structural analog for "a standalone module is consulted from a consolidator branch")
    - tests/backfill-chronological-order.test.ts (the content-keyed dynamic provider pattern and its rationale for why call-order-indexed scripts produce vacuous green tests)
    - src/consolidation/status-drift.ts (the `evaluate` signature being spied on)
  </read_first>
  <behavior>
    - **Primary branch consulted:** a gmail episode producing a `contradict` verdict against a primary candidate causes exactly one `StatusDrift.evaluate` call carrying that candidate's node id, the claim's `claimIntentConfidence`, and the episode's `event_ts`
    - **Secondary branch consulted:** a verdict listing a second contradicted id causes an additional `evaluate` call carrying the SECONDARY node id
    - **Damped magnitude reaches routing:** with `statusDriftConfidenceDamping.medium` set to `0.6` and a judge magnitude of `0.5`, the value passed to `routeContradiction` is `0.3` — assert via a spy on the drift result plus the observed routing outcome, or by asserting the routing outcome flips between two configs that differ only in the damping factor
    - **Drop short-circuits everything:** an episode whose `event_ts` predates the candidate's most recent dated supporting evidence produces zero `consolidation_event` rows for that node, zero new `pending_contradictions` entries, and no node mutation — assert all three
    - **Drop short-circuits secondaries too:** the same fixture with a secondary contradicted id also leaves the secondary node untouched
    - **Non-gmail no-op:** a `claude-code` episode producing a `contradict` verdict causes `evaluate` to return `not-applicable` without a staleness query, the magnitude reaching `routeContradiction` equals the judge's value, and the resulting graph state is identical to a run with `statusDriftEnabled: false`
    - **Master switch:** with `statusDriftEnabled: false`, a gmail low-confidence contradiction routes exactly as it did pre-Phase-65
    - **Per-source threshold:** with `contradictionNBySource: { gmail: 2 }`, two distinct gmail provenances force-destabilize; with the stock `{}`, the same two do not
    - **Counters:** after a pass containing one damped decision, one dropped decision, and one undated decision, the logged counters report `1`, `1`, and `1` respectively
    - **Sink honesty:** for a damped decision that still routes, the emitted `consolidation_event.magnitude` equals the JUDGE's magnitude, not the damped one
  </behavior>
  <action>
Create `tests/status-drift-wiring.test.ts` covering every case in the behavior block. Build on `tests/consolidation-intent.test.ts`'s harness verbatim — copy its `Harness` interface, its embed-fn helpers, and its instance-level monkey-patch spy technique rather than subclassing (`applyDecision` is a TS `private`, so a subclass override collides at the type level).

Spy on `StatusDrift.evaluate` by monkey-patching the method on the `consolidator`'s own `statusDrift` instance, recording the argument object and delegating to the real implementation. Delegating rather than stubbing is required: a stub would make the drop and damping cases assert against the test's own fiction instead of the shipped decision logic.

Use a **content-keyed dynamic provider** for extraction and judging, following `tests/backfill-chronological-order.test.ts`'s pattern — branch on a marker substring in the prompt and throw on an unrecognized prompt. A call-order-indexed response queue would return the right verdict regardless of whether the drift layer was consulted at all, producing a green test that proves nothing; that file's header explains the trap and this suite is subject to the same one.

Seed prior supporting evidence for the drop cases by running a first `consolidate()` pass that mints the node from a dated gmail episode, then running a second pass with an older-dated episode. Deriving the "prior" from a real pass rather than a hand-inserted `consolidation_event` row is what makes the case exercise the real join.

For the **counters** case, capture the pass-end log output the same way the existing suites capture it (find the pattern by grepping `resolutionAbstains` in `tests/`); if no suite currently asserts on those counters, read them by whatever mechanism the consolidator exposes and state in a comment which mechanism you used and why.

For the **non-gmail no-op** case, run the identical fixture twice — once with stock config and once with `statusDriftEnabled: false` — and assert the resulting whole-DB snapshots are equal, reusing `snapshotDb` from `tests/intent-conservation.test.ts`. Equality of outcomes is a stronger and more durable claim than asserting on an internal call count.

File header must state: this suite proves the drift layer is genuinely CONSULTED at both branches (the wiring), while `tests/status-drift.test.ts` proves the decisions themselves are correct (the logic) and Plan 65-09 proves the end-to-end DRIFT-02/DRIFT-04 outcomes. Name the division so a future reader does not duplicate coverage across the three.
  </action>
  <verify>
    <automated>npx vitest run tests/status-drift-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/status-drift-wiring.test.ts` passes with at least 12 cases
    - The spy DELEGATES to the real `StatusDrift.evaluate` rather than stubbing a return value (verify by reading)
    - The extraction/judge provider is content-keyed and throws on an unrecognized prompt
    - The drop case asserts all three of: zero new `consolidation_event` rows for the node, zero new `pending_contradictions` entries, and no node mutation
    - The non-gmail case asserts whole-DB snapshot equality between stock config and `statusDriftEnabled: false`
    - The sink-honesty case asserts the persisted `magnitude` equals the judge's value on a damped-but-routed decision
    - The per-source-threshold case runs the same fixture under `{}` and `{ gmail: 2 }` and asserts different outcomes
    - Mutation check #1, recorded in the SUMMARY: temporarily move the drift evaluation to AFTER the secondary loop, confirm the drop-short-circuits-secondaries case FAILS; revert
    - Mutation check #2, recorded in the SUMMARY: temporarily pass `decision.magnitude` instead of `drift.magnitude` to `routeContradiction`, confirm the damped-magnitude case FAILS; revert
  </acceptance_criteria>
  <done>Twelve-plus-case wiring suite green with a delegating spy and a content-keyed provider; both branches proven consulted; drop proven to leave primaries and secondaries untouched; non-gmail proven byte-identical by whole-DB snapshot; two mutation checks recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classified email evidence → belief mutation | This plan is where classifier-derived confidence and sender-asserted timestamps first affect whether the graph is written. Everything upstream of it was inert. |
| drift decision → sink audit record | The `consolidation_event` stream is the audit trail Phase 66 will read. If it records a damped magnitude as though the judge emitted it, the audit lies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-08-GUARD | Elevation of Privilege | Drift wiring implemented as an independent scan that fails to inherit the `source==='hitl'` / inferred / echo hard stop | mitigate | The evaluation is placed inside `applyDecision` and `applySecondaryContradiction`, both reached only from the per-episode loop AFTER the line 706 hard stop, and the plan explicitly forbids re-checking origin/echo/hitl. This is research Pitfall 6's named defect class, which has shipped twice in this codebase; Phase 64 closed it the same way and its branch comment is the template. |
| T-65-08-STALEHOLD | Tampering | Stale evidence accumulating toward force-destabilization | mitigate | `drop` `break`s/`return`s out before the secondary loop, before routing, and before `recordContradiction`. Asserted three ways in Task 3 (no event rows, no pending entries, no node mutation) and mutation-checked by relocating the evaluation after the secondary loop. |
| T-65-08-AUDIT | Repudiation | Sink recording the damped magnitude as the judge's verdict | mitigate | Every `sink.emit` keeps `magnitude: decision.magnitude`. Enforced by a grep in Task 2's acceptance criteria (`magnitude: drift` must return 0) and asserted persistently in Task 3's sink-honesty case. |
| T-65-08-COLLATERAL | Tampering | Drift layer silently changing non-Gmail belief behavior | mitigate | The `confidence === undefined` early return in `StatusDrift.evaluate` (Plan 65-05) is the structural no-op, and Task 3 proves it by whole-DB snapshot equality between stock config and `statusDriftEnabled: false` on a `claude-code` fixture. |
| T-65-08-THRESH | Tampering | Per-source threshold lowering the force-destabilization bar unnoticed | mitigate | `contradictionNBySource` ships `{}`, so every source falls back to the global `3` and the change is behavior-neutral until an entry is added. Both sites are converted (grep-asserted) so a partial conversion cannot leave the secondary path on a different bar. |
| T-65-08-MACHINERY | Tampering | The guard implemented by editing `routeContradiction` | mitigate | The guard sits structurally before the call; `tests/pe-machinery-lock.test.ts` (Plan 65-07) runs as part of this plan's verification and fails on any edit to the pinned function bodies. |
| T-65-SC | Tampering | npm/pip/cargo installs | N/A | Zero package installs. Net-zero new runtime dependencies invariant holds. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npx vitest run tests/status-drift-wiring.test.ts tests/status-drift.test.ts` — all pass
- `npx vitest run tests/consolidation.test.ts tests/consolidator.test.ts tests/consolidation-intent.test.ts tests/consolidation-resolution.test.ts tests/consolidation-temporal.test.ts tests/consolidation-source.test.ts tests/consolidation-scope.test.ts tests/backfill-chronological-order.test.ts tests/intent-conservation.test.ts tests/intent-source-gate.test.ts tests/sink.test.ts tests/update-decision.test.ts tests/pe-machinery-lock.test.ts` — no collateral regression
- `npx vitest run tests/online-llm-free-sentinel.test.ts` — online paths remain LLM-free
- `git diff --stat -- src/consolidation/update-decision.ts src/consolidation/sink.ts src/db/schema.ts src/consolidation/status-drift.ts` shows zero changes
- `grep -c "routeContradiction(" src/consolidation/consolidator.ts` returns exactly 2
</verification>

<success_criteria>
The drift layer is consulted before both `routeContradiction` call sites with the episode's event time and source threaded onto every `ClaimDecision`; a stale-evidence drop produces no graph effect on primaries or secondaries; the damped magnitude drives routing while the sink still records the judge's own magnitude; the force-destabilization threshold is per-source with a behavior-neutral empty default; and a non-Gmail decision's whole-DB outcome is identical to running with the drift layer disabled.
</success_criteria>

<output>
Create `.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-08-SUMMARY.md` when done. Record the counter-plumbing shape chosen (instance fields vs passed accumulator) and why, the exact insertion points used in both branches, any pre-existing suite that needed investigation, and both mutation-check results.
</output>
