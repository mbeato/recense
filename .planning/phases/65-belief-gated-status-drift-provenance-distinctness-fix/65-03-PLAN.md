---
phase: 65-belief-gated-status-drift-provenance-distinctness-fix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/config.ts
  - tests/runtime-config.test.ts
autonomous: true
requirements: [DRIFT-02, DRIFT-03, DRIFT-04]

must_haves:
  truths:
    - "The provenance-distinctness key redesign ships dark: with stock config, Gmail episodes keep the pre-Phase-65 collapsed session id and countDistinctProvenance behaves exactly as before (D-14)"
    - "Every Phase 65 knob has a documented value that restores pre-Phase-65 behavior exactly, and the doc comment states it"
    - "The confidence→magnitude damping map cannot amplify: no configured value can produce a magnitude greater than the input magnitude (D-09 lower-only lock, D-43 doctrine)"
    - "contradictionNBySource defaults to an empty map, so contradictionN stays the global 3 for every source (D-16 dark knob)"
    - "A single test block asserts every Phase 65 knob's stock default reproduces pre-phase behavior"
  artifacts:
    - path: "src/lib/config.ts"
      provides: "Six Phase 65 dark knobs on EngineConfig + DEFAULT_CONFIG, each with a documented restore-old-behavior value"
      contains: "provenanceDistinctnessEnabled"
    - path: "tests/runtime-config.test.ts"
      provides: "Phase 65 stock-default block asserting pre-phase behavior and the damping clamp domain"
  key_links:
    - from: "src/lib/config.ts"
      to: "src/lib/config.ts"
      via: "contradictionNBySource mirrors the shipped consolSkipThresholdBySource per-source-override shape"
      pattern: "contradictionNBySource"
---

<objective>
Land every Phase 65 configuration knob in one place, each defaulting to pre-Phase-65 behavior, each with a documented reversal value. This is D-14's dark-launch discipline and D-16's dark-knob adoption, plus the tunables the drift layer (65-05) and the dry-run harness (65-10) both read.

Purpose: the provenance-distinctness key is a change to a load-bearing correctness mechanism's input. Research Pitfall 3 puts it in the same caution class as `GMAIL_EXTRACTION_PROMPT` changes: compute and log first, enable only after a reviewed dry-run against real multi-email status threads. Putting all six knobs in one plan also means `src/lib/config.ts` appears in exactly one Phase 65 plan, so every later plan can run without contending for it.
Output: six fields on `EngineConfig` + `DEFAULT_CONFIG`, and a stock-default assertion block in the existing runtime-config suite.

**Not in this plan:** no consumer reads any of these knobs yet. Key derivation is 65-04, the drift layer is 65-05, the mint site is 65-07, the per-source threshold lookup is 65-08. This plan ships inert configuration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-CONTEXT.md
@.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Live-verified 2026-08-02. -->

`src/lib/config.ts` — the two conventions to mirror:

1. **Global threshold this phase adds a per-source override for** (interface lines 84-88, default line 808):
```
  /**
   * Number of distinct provenance contradictions before force-destabilization
   * (Chen-2020 threshold). N=3 balances noise tolerance vs. responsiveness.
   */
  contradictionN: number;
  ...
  contradictionN: 3,
```
`contradictionN` lives directly on `EngineConfig` (NOT inside `SalienceConfig`). `DEFAULT_CONFIG` is `export const DEFAULT_CONFIG: Omit<EngineConfig, 'dbPath'>` at line 804; `contradictionN: 3` is at line 808 between `beta: 0.05` and `evictionSThreshold: 0.05`.

2. **Per-source-override shape to copy** — `SalienceConfig.consolSkipThresholdBySource` (interface lines 42-51, default lines 780-795):
```
  /**
   * Per-source consolidation skip threshold (D-60, mirrors consolSkipThresholdAssistant).
   * Sources not listed fall back to consolSkipThreshold (global 0.2 default).
   * 'gmail': 0.4 -> higher bar; most email is lower-signal than conversation turns.
   * D-13 calibration placeholders — tune against real consolidation cost vs. recall.
   * Reversibility: remove an entry to restore the global consolSkipThreshold for that source.
   */
  consolSkipThresholdBySource: Record<string, number>;
```
Note its consumption site for reference (`src/consolidation/consolidator.ts:668-672`):
`this.config.salience.consolSkipThresholdBySource[episode.source] ?? (episode.role === 'assistant' ? ... : ...)`

`src/model/claim-extractor.ts:77-83` — the confidence enum this phase's damping map is keyed on:
`export type IntentConfidence = 'high' | 'medium' | 'low';` and `export const INTENT_CONFIDENCES: ReadonlySet<IntentConfidence>`.

`src/consolidation/update-decision.ts:45-61` — `routeContradiction(peMagnitude, resistance, config)`. `ratio = peMagnitude / Math.max(resistance, EPS)`; `ratio < config.peReconcileBandLow` returns `'hold'`. **A magnitude of exactly 0 therefore always routes to `'hold'` through this unmodified function** — that is the mechanism the `low: 0` damping factor uses, so no engine change is needed to satisfy D-09's "a low-confidence classification must not produce a decisive flip on its own."

`tests/runtime-config.test.ts` — the existing config suite this plan extends. Read it before editing; match its `describe`/`it` naming and its assertion style rather than inventing a new one.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the six Phase 65 dark knobs to EngineConfig and DEFAULT_CONFIG</name>
  <files>src/lib/config.ts</files>
  <read_first>
    - src/lib/config.ts (lines 36-60 for the `SalienceConfig.consolSkipThresholdBySource` interface + JSDoc convention; lines 76-108 for the `contradictionN` neighbourhood on `EngineConfig`; lines 240-270 for the `bm25CandidateK` / `entityResolutionFloor` dark-knob JSDoc pattern; lines 770-830 for the `DEFAULT_CONFIG` block and the `consolSkipThresholdBySource` default)
    - src/model/claim-extractor.ts (lines 59-100 — `IntentStatus`, `IntentConfidence`, `INTENT_CONFIDENCES`, and the drop-on-mismatch coercion helpers)
    - src/consolidation/update-decision.ts (lines 25-61 — `routeContradiction`'s bands, to confirm that magnitude 0 routes to `'hold'`)
    - .planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-CONTEXT.md (D-07, D-09, D-11, D-14, D-16)
    - .planning/research/PITFALLS.md (Pitfall 3's `contradictionNBySource` suggestion; Pitfall 5's out-of-order compromise)
  </read_first>
  <action>
Add six fields. Each JSDoc block follows the established three-part dark-knob convention already used by `bm25CandidateK` and `entityResolutionFloor`: **what the knob controls** / **calibration rationale for the default** / **the exact value that reproduces pre-Phase-65 behavior**. Every default below is chosen so stock config behaves exactly as the engine did before this phase.

Add these four directly on `EngineConfig`, positioned immediately after `contradictionN` (interface ~line 88), and their defaults immediately after `contradictionN: 3` in `DEFAULT_CONFIG` (~line 808), each with a one-line inline `// Phase 65 D-NN:` comment in the existing style:

  - **`contradictionNBySource: Record<string, number>`** — default `{}`. Per-source override for `contradictionN` (D-16, research Pitfall 3). Sources not listed fall back to the global `contradictionN` (3). Rationale to state: email's realistic corroboration cadence is days between genuinely distinct signals, while Claude Code's is minutes between turns, so one global N is a compromise for both. Ships EMPTY deliberately — the dry-run in Plan 65-10 is what supplies evidence for any entry. **Reversibility: remove an entry to restore the global `contradictionN` for that source; an empty map is pre-Phase-65 behavior exactly.** Mirror `consolSkipThresholdBySource`'s doc density and closing reversibility line verbatim in spirit.

  - **`provenanceDistinctnessEnabled: boolean`** — default **`false`**. Master dark-launch switch for the DRIFT-03 redesigned provenance-distinctness key (D-14). When `false`, the Gmail adapter still COMPUTES and reports the derived key for observability but every ingested Gmail episode keeps the pre-Phase-65 collapsed `ingest:gmail` session id, so `countDistinctProvenance` behaves byte-identically to today. When `true`, the derived `(normalized-sender-domain, gmail-thread-id)` key takes effect. Rationale to state: research Pitfall 3 puts this change in the same caution class as `GMAIL_EXTRACTION_PROMPT` edits — compute-and-log first, enable only after the Plan 65-10 dry-run against real multi-email status threads is reviewed. **Reversibility: `false` is pre-Phase-65 behavior exactly.** Also state D-03 in this block: enabling is forward-only — episodes already ingested keep their historical collapsed session id and are never migrated.

  - **`provenanceMinResidualChars: number`** — default `20`. Minimum count of NON-WHITESPACE characters that must survive `stripQuotedForwarded` for a message to contribute an independent provenance (D-07). Below this the message is treated as pure quotation/forward and collapses into the shared non-independent bucket, which is what makes three forwards of one thread count as one. Rationale to state: 20 non-whitespace characters clears habitual filler forwards ("FYI", "Thoughts?", "see below") while a genuine short status line ("Unfortunately, we are moving forward with other candidates") clears it comfortably. Calibration placeholder — Plan 65-10's dry-run reports the observed residual-length distribution over real threads, and this is the knob it tunes. **Setting it to `0` disables the residual gate entirely and is NOT recommended: name the failure it reopens (forwarded/quoted distinctness farming, research Pitfall 3's inverse risk).**

  - **`statusDriftEnabled: boolean`** — default **`true`**. Master switch for the belief-gated status-drift layer (confidence damping D-09 plus the `event_ts` staleness guard D-11b). Defaults ON, unlike the provenance knob, and the JSDoc must justify why: the drift layer is strictly non-amplifying — it can only hold, dampen, or drop a contradiction, never strengthen or accelerate one — and it is a structural no-op for any decision that carries no intent-classification confidence, which is every non-Gmail decision in the engine. It closes DRIFT-02 and DRIFT-04, which are phase success criteria, so dark-launching it would ship the phase without its own guarantees. **Reversibility: `false` restores pre-Phase-65 routing exactly — `routeContradiction` is called with the undamped magnitude and no staleness check runs.**

Add these two also directly on `EngineConfig`, immediately after `statusDriftEnabled`:

  - **`statusDriftConfidenceDamping: Record<IntentConfidence, number>`** — default `{ high: 1, medium: 0.6, low: 0 }`. Multiplier applied to a contradicting claim's PE magnitude before it reaches the UNMODIFIED `routeContradiction`, keyed on Phase 63's coarse intent confidence (D-09). Import the `IntentConfidence` type from `../model/claim-extractor` for the key type — do not restate the three literals as a fresh union. The JSDoc must state all four of the following:
      - **Lower-only lock (D-09, extends the D-43 non-strengthening doctrine and backlog B-04's lower-only constraint):** every factor is clamped to `[0, 1]` at the point of consumption, so no configured value — not even `5` — can ever produce a magnitude greater than the one the judge emitted. Consumption-side clamping rather than validation-side rejection is deliberate: a malformed config degrades to safe behavior instead of throwing during a sleep pass.
      - **Why `high: 1` and not something greater:** identity, never amplification. A high-confidence classification earns the judge's own magnitude unchanged, nothing more.
      - **Why `low: 0` specifically:** magnitude 0 divided by any resistance is 0, which is below `peReconcileBandLow`, so `routeContradiction` returns `'hold'` — deterministically, through the unmodified function. This is how "a low-confidence classification must not produce a decisive flip on its own" is guaranteed structurally rather than by a threshold that a weak node's tiny resistance could still let through. A held claim still records a pending contradiction, so three independent low-confidence emails can still cross `contradictionN` and force-destabilize — that is "email earns confidence through consolidation volume," and it is the pre-existing mechanism, unmodified.
      - **Reversibility:** `{ high: 1, medium: 1, low: 1 }` restores pre-Phase-65 magnitudes exactly (equivalent to `statusDriftEnabled: false` for the damping half).

  - **`statusDriftEventTsGuard: boolean`** — default `true`. When `true`, a contradicting status claim whose episode `event_ts` is older than the most recent dated supporting evidence for the candidate node is DROPPED — no routing, no pending contradiction recorded, no sink event (D-11b, research Pitfall 5). The JSDoc must state: dropping rather than holding is load-bearing, because a held contradiction accumulates toward `contradictionN` and three stale backfilled rejections would then force-destabilize a newer offer — the exact DRIFT-04 failure the guard exists to prevent. Where either timestamp is null (undated evidence), the guard abstains and pre-Phase-65 behavior applies; that abstention is counted and logged, never silent. **Reversibility: `false` restores pre-Phase-65 behavior exactly.** Also name D-12 here: cross-run and cross-account interleaving beyond this guard plus the Plan 65-06 backfill sort remains a named, accepted, documented risk this milestone — bi-temporal validity columns stay deferred.

Do NOT add a knob for the provenance key's composition (domain vs address, hashing, separators) — that is fixed in code in Plan 65-04, following the Phase 55-01 D-01/D-02 precedent of keeping calibration literals module-private with a test guard rather than promoting them to config.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/runtime-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Each of the six names appears at least twice in `src/lib/config.ts` (interface field + `DEFAULT_CONFIG` entry): verify `grep -c` ≥ 2 for `contradictionNBySource`, `provenanceDistinctnessEnabled`, `provenanceMinResidualChars`, `statusDriftEnabled`, `statusDriftConfidenceDamping`, `statusDriftEventTsGuard`
    - `grep -n "provenanceDistinctnessEnabled: false" src/lib/config.ts` matches inside `DEFAULT_CONFIG`
    - `grep -n "provenanceMinResidualChars: 20" src/lib/config.ts` matches inside `DEFAULT_CONFIG`
    - `grep -n "statusDriftEnabled: true" src/lib/config.ts` matches inside `DEFAULT_CONFIG`
    - `grep -n "statusDriftEventTsGuard: true" src/lib/config.ts` matches inside `DEFAULT_CONFIG`
    - `grep -n "contradictionNBySource: {}" src/lib/config.ts` matches inside `DEFAULT_CONFIG`
    - The damping default is exactly `{ high: 1, medium: 0.6, low: 0 }` (any formatting, same three key/value pairs)
    - `grep -c "IntentConfidence" src/lib/config.ts` is at least 1 and the import is from `../model/claim-extractor` — the three confidence literals are NOT restated as a fresh union type in this file
    - Every one of the six JSDoc blocks contains the word `Reversibility` or an explicit "restores pre-Phase-65 behavior" sentence naming the value
    - `npm run typecheck` exits 0
    - `git diff src/lib/config.ts` shows only additions — no existing field's value changed (in particular `contradictionN` is still `3`)
  </acceptance_criteria>
  <done>Six knobs shipped, all defaulting to pre-Phase-65 behavior, each documenting its reversal value, with the lower-only lock and the `low: 0` mechanism explained in the source.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the Phase 65 stock defaults reproduce pre-phase behavior</name>
  <files>tests/runtime-config.test.ts</files>
  <read_first>
    - tests/runtime-config.test.ts (full file — match its existing describe/it naming and assertion style; do not introduce a new harness)
    - src/lib/config.ts (the six blocks written in Task 1)
    - src/consolidation/update-decision.ts (lines 45-61 — `routeContradiction`, used to prove the `low: 0` claim empirically rather than by comment)
    - src/model/claim-extractor.ts (lines 77-83 — `INTENT_CONFIDENCES`, to assert key-set parity)
  </read_first>
  <action>
Add one `describe('Phase 65 dark knobs — stock defaults reproduce pre-phase behavior', ...)` block to the existing `tests/runtime-config.test.ts`. Do not create a new file.

Cases:
  1. `DEFAULT_CONFIG.provenanceDistinctnessEnabled` is `false`, `DEFAULT_CONFIG.contradictionNBySource` deep-equals `{}`, and `DEFAULT_CONFIG.contradictionN` is still `3` — the three assertions that together mean stock config counts distinct provenance exactly as it did before this phase.
  2. `DEFAULT_CONFIG.statusDriftEnabled` is `true` and `DEFAULT_CONFIG.statusDriftEventTsGuard` is `true`.
  3. **Damping key-set parity:** `Object.keys(DEFAULT_CONFIG.statusDriftConfidenceDamping).sort()` deep-equals `[...INTENT_CONFIDENCES].sort()`, importing `INTENT_CONFIDENCES` from `src/model/claim-extractor`. This fails loudly if Phase 63's confidence enum ever grows a fourth value without the damping map growing with it — a silently-missing key would otherwise read as `undefined` and be coerced somewhere downstream.
  4. **Non-amplification domain:** every value in `DEFAULT_CONFIG.statusDriftConfidenceDamping` is `>= 0` and `<= 1`.
  5. **The `low: 0` mechanism, proven not asserted:** call the real `routeContradiction(0 * anyMagnitude, resistance, DEFAULT_CONFIG)` across a spread of resistances that includes a fresh-node value (`0.1 * 0.5 = 0.05`), a strong-node value (`0.9 * 0.9 = 0.81`), and `0`, and assert it returns `'hold'` every time. Import `routeContradiction` from `src/consolidation/update-decision`. Add a comment stating this is the empirical basis for the `low: 0` gate-to-hold design and that if `peReconcileBandLow` is ever lowered to 0 this test is the tripwire.
  6. `DEFAULT_CONFIG.provenanceMinResidualChars` is `20` and is a positive integer.

Do NOT assert anything about behavior that does not exist yet (no adapter, no drift layer, no mint site) — this block is strictly about default VALUES plus the one `routeContradiction` property they depend on.
  </action>
  <verify>
    <automated>npx vitest run tests/runtime-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/runtime-config.test.ts` passes, including at least 6 new cases in the Phase 65 block
    - The key-set parity case imports `INTENT_CONFIDENCES` from `src/model/claim-extractor` rather than hardcoding `['high','medium','low']`
    - The `low: 0` case calls the real `routeContradiction` (imported from `src/consolidation/update-decision`) with at least three distinct resistance values including `0`, and asserts `'hold'` for all of them
    - Mutation check, recorded in the SUMMARY: temporarily set `statusDriftConfidenceDamping.low` to `0.5` in `DEFAULT_CONFIG`, confirm no case in this block fails (proving case 5 tests the ROUTING property, not the config value), then temporarily set `provenanceDistinctnessEnabled` to `true` and confirm case 1 DOES fail; revert both
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Six-case stock-default block green in the existing config suite, with confidence-enum parity guarded and the gate-to-hold mechanism proven against the real routing function.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-supplied config → engine behavior | `EngineConfig` values reach correctness-critical routing. A hand-edited or drifted config file is the trust boundary; the operator is trusted but fallible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-03-AMP | Elevation of Privilege | `statusDriftConfidenceDamping` used to amplify a contradiction | mitigate | Factors are clamped to `[0, 1]` at the point of consumption (implemented in Plan 65-05, asserted there against a configured value of `5`). Consumption-side clamping means a malformed config degrades to safe behavior instead of throwing mid-sleep-pass. Task 2 asserts the shipped defaults sit inside the domain. |
| T-65-03-DARK | Tampering | Provenance key enabled before the dry-run reviews it | mitigate | `provenanceDistinctnessEnabled` defaults `false`; enabling is a deliberate recorded decision gated on Plan 65-10's dry-run (D-14). Task 2 asserts the `false` default and the mutation check confirms the assertion is not vacuous. |
| T-65-03-ENUM | Tampering | Confidence enum grows without the damping map | mitigate | Task 2's key-set parity case compares against the live `INTENT_CONFIDENCES` set rather than a hardcoded literal list, so a fourth confidence value fails the suite instead of silently reading `undefined`. |
| T-65-03-GATE | Tampering | Residual gate disabled (`provenanceMinResidualChars: 0`), reopening forward farming | accept | Documented in the JSDoc as not-recommended with the reopened failure named. A single-tenant local engine whose operator is the founder; no remote config surface exists and no untrusted party can set this value. |
| T-65-SC | Tampering | npm/pip/cargo installs | N/A | Zero package installs. Configuration fields only. Net-zero new runtime dependencies invariant holds. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npx vitest run tests/runtime-config.test.ts` — all cases pass including the six new ones
- `npx vitest run tests/config-cli.test.ts tests/settings-loader.test.ts tests/update-decision.test.ts tests/consolidation.test.ts` — no collateral regression from the config additions
- `git diff src/lib/config.ts` shows additions only; `contradictionN` is still `3`
- `grep -rn "provenanceDistinctnessEnabled\|statusDriftEnabled" src/ | grep -v "src/lib/config.ts"` returns nothing — no consumer reads these yet, by design
</verification>

<success_criteria>
All six Phase 65 knobs exist on `EngineConfig` and `DEFAULT_CONFIG` with three-part JSDoc and an explicit reversal value; stock config is byte-for-byte pre-Phase-65 in behavior; the damping map's key set is enum-parity-guarded; and the `low: 0` gate-to-hold mechanism is proven against the real, unmodified `routeContradiction`.
</success_criteria>

<output>
Create `.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-03-SUMMARY.md` when done. Record the six shipped defaults verbatim, and both Task 2 mutation-check results.
</output>
