---
phase: 53-brain-layout-at-scale-declutter-deliberate-clustering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/viz/modules/graph.js
  - src/viz/modules/constants.js
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-07, D-08]

must_haves:
  truths:
    - "No visible lattice/gridlines: node seeding samples continuous in-hull points, not snapped voxel centres (D-04, ROADMAP SC1)"
    - "Related nodes start clustered: members seed near their schema hub centroid (D-01 hybrid, D-02 schema-membership signal)"
    - "Layout is stable: the same corpus reproduces the same positions every load via deterministic hash seeding, no persistent cache (D-08)"
    - "Settle is wall-clock bounded then pinned: cooldownTicks(12) replaced by a SETTLE_BUDGET_MS timeout that never hangs at 15k (D-03)"
    - "Reveal UX unchanged: canvas stays opacity 0 during settle, then fades in the finished layout — revealSettled() body untouched (D-07, Phase 52 no-regress)"
  artifacts:
    - path: "src/viz/modules/graph.js"
      provides: "seedNodePositions() rewritten for continuous in-hull + cluster-centroid + deterministic seeding; settle/reveal block uses wall-clock budget; seedNodePositions exported for test"
      contains: "_hashIndex"
    - path: "src/viz/modules/constants.js"
      provides: "SETTLE_BUDGET_MS named tunable"
      contains: "SETTLE_BUDGET_MS"
  key_links:
    - from: "src/viz/modules/graph.js seedNodePositions"
      to: "node.__schemaId / __cat (set by initLod)"
      via: "schema-hub centroid lookup for member bias"
      pattern: "__schemaId"
    - from: "src/viz/modules/graph.js settle block"
      to: "SETTLE_BUDGET_MS (constants.js)"
      via: "setTimeout(revealSettled, SETTLE_BUDGET_MS)"
      pattern: "SETTLE_BUDGET_MS"
---

<objective>
Kill the gridlines and make the brain start clustered, stably, and bounded — all in `graph.js` (plus one tunable in `constants.js`).

Rewrite `seedNodePositions()` so nodes are placed at continuous points inside the brain hull (no voxel-centre snap → no lattice), biased toward their schema-cluster centroid (related nodes start grouped), using the existing deterministic `_hashIndex` so the same corpus reproduces the same layout on every load with no persistent cache. Replace the fixed `Graph.cooldownTicks(12)` settle with a wall-clock `SETTLE_BUDGET_MS` budget that pins-then-fades regardless of node count, while keeping the locked `revealSettled()` body verbatim.

Purpose: closes founder symptoms (1) visible gridlines and (3) no longer clustered/deliberate, and makes the settle bounded at 15k.
Output: rewritten `seedNodePositions` (exported for unit test), wall-clock settle block, `SETTLE_BUDGET_MS` constant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-CONTEXT.md
@.planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-PATTERNS.md

<interfaces>
<!-- Locked anchors — must NOT change (constants.js:184, graph.js:67-71, graph.js:603-611): -->
BRAIN_SCALE = 460                       // never use a different scale in multiplyScalar
nodeRadius()                            // schema/member/haze sizing — locked
revealSettled() body (graph.js:603-611) // pin fx/fy/fz + opacity 0→1 fade — locked (Phase 52)
brainContainment via Graph.onEngineTick // NOT Graph.d3Force() — documented crash

<!-- Reusable, do NOT rewrite: -->
function brainOccupied(brainVol, qx, qy, qz) -> boolean   // graph.js:155-164, hull occupancy test
function _hashIndex(idx, mod) -> int in [0,mod)           // graph.js:231-235, Knuth hash (stable reloads)

<!-- Cluster signal set by initLod(ctx) BEFORE seedNodePositions runs (confirm exact field names in lod.js): -->
node.__cat       // 'schema' | 'haze' | (member) — classification
node.__schemaId  // member's owning schema id (hub node id), when present
ctx.memberSchema // Map<memberId, schemaId>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SETTLE_BUDGET_MS tunable to constants.js</name>
  <files>src/viz/modules/constants.js</files>
  <read_first>
    - src/viz/modules/constants.js (lines 184-226 — BRAIN_SCALE/CONTAIN_STRENGTH/density block; copy the JSDoc + `export const` pattern)
    - .planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-PATTERNS.md (section "constants.js:194-226" — exact new-constant shape)
  </read_first>
  <behavior>
    - constants.js exports `SETTLE_BUDGET_MS` with initial value 250 (number, ms).
    - JSDoc states it replaces cooldownTicks(12), is bounded regardless of node count, and is tuned at the founder visual checkpoint.
    - BRAIN_SCALE stays exactly 460; no existing constant is modified.
  </behavior>
  <action>
    Append a new `export const SETTLE_BUDGET_MS = 250;` to `src/viz/modules/constants.js` near the density band (after HAZE_DENSE_SCALE, around line 226), following the existing JSDoc-comment + `export const` pattern. Initial value 250 (ms) — a named tunable, NOT a magic number; document in JSDoc that it replaces `Graph.cooldownTicks(12)`, bounds the force-settle by wall-clock regardless of node count (D-03), and is tuned at the founder visual checkpoint. Do NOT touch BRAIN_SCALE (460), nodeRadius, or any density constant. Do NOT add OVERVIEW_NODE_CAP here — that belongs to plan 53-02.
  </action>
  <verify>
    <automated>grep -v '^\s*\*' src/viz/modules/constants.js | grep -v '^\s*//' | grep -Eq "export const SETTLE_BUDGET_MS = 250" && grep -q "export const BRAIN_SCALE = 460" src/viz/modules/constants.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `SETTLE_BUDGET_MS` is exported from constants.js with value 250 (asserted outside comments).
    - `BRAIN_SCALE = 460` still present and unchanged.
    - No other constant's value changed (git diff shows only the additive const + its JSDoc).
  </acceptance_criteria>
  <done>SETTLE_BUDGET_MS exported; BRAIN_SCALE untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite seedNodePositions() — continuous in-hull + schema-centroid + deterministic</name>
  <files>src/viz/modules/graph.js, tests/viz-seed-determinism.test.ts</files>
  <read_first>
    - src/viz/modules/graph.js (lines 155-235 — brainOccupied, seedNodePositions, _hashIndex; lines 316-347 — haze deterministic scatter analog)
    - src/viz/modules/lod.js (initLod — confirm exact field names: node.__cat, node.__schemaId, ctx.memberSchema, and that schema hub nodes carry __cat==='schema')
    - .planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-PATTERNS.md (section "graph.js:171-221 seedNodePositions" — exact current code, haze analog, occupied[] reuse)
    - tests/viz-haze-activation.test.ts (lines 1-60 — the vi.mock('three') headless pattern a determinism test reuses)
  </read_first>
  <behavior>
    - Determinism: calling seedNodePositions twice on identical inputs yields byte-identical n.x/n.y/n.z for every node (no Math.random in the brainVol path).
    - No lattice: per-node jitter spans the full inter-voxel spacing continuously (not the fixed ±4 / ±2u), so co-voxel nodes do not snap to a shared centre.
    - Clustering: a member node (has __schemaId) lands within a bounded cluster radius of its schema hub node's seeded position; a node with no schema falls back to a deterministic continuous in-hull point.
    - All seeded points pass brainOccupied (validated; bounded retries then fallback).
  </behavior>
  <action>
    Rewrite the `brainVol`-present branch of `seedNodePositions(allNodes, brainVol)` (graph.js:211-221) — keep the function signature and the null-brainVol fallback branch (172-183) unchanged. Build a local `id → node` Map from `allNodes` (no signature change). Enumerate nodes with an integer index `i` for deterministic hashing.

    Pass 1 — place schema hub nodes (`n.__cat === 'schema'`): pick a deterministic occupied voxel via `_hashIndex(i, occupied.length)`, then add a CONTINUOUS offset spanning the full inter-voxel cell (derive a fractional in [0,1) as `_hashIndex(k, 1<<20)/(1<<20)`, map to ±(world voxel half-extent) where the world cell size ≈ `(2/R)*BRAIN_SCALE` after the rotMat+`multiplyScalar(BRAIN_SCALE)` transform). This continuous full-cell jitter (not the old ±4) is what kills the lattice (D-04). Validate the final point with `brainOccupied`; on miss, re-hash with a perturbed key up to ~8 tries, else keep the voxel-derived point.

    Pass 2 — place member nodes (have `n.__schemaId`): look up the schema hub node via the local id-map; bias the sample toward the hub's already-seeded position by sampling a deterministic continuous offset within a cluster radius (initial ≈ 0.12 * BRAIN_SCALE — centroid-bias strength is tunable, derive offsets from `_hashIndex`), then validate with `brainOccupied`; on persistent miss, fall back to the Pass-1 continuous in-hull placement. This realizes D-01 (seed-then-settle hybrid) + D-02 (schema-membership grouping).

    Pass 3 — nodes with neither schema nor hub: same continuous in-hull placement as Pass 1.

    Use ONLY `_hashIndex`-derived randomness (D-08) — no `Math.random()` anywhere in the brainVol branch. Reuse the existing `occupied[]` list, `rotMat`, and `multiplyScalar(BRAIN_SCALE)` transform (BRAIN_SCALE LOCKED at 460). Add `export` to `function seedNodePositions` so it is unit-testable headlessly (additive; no behavior change). Do NOT touch `_hashIndex` (231-235) or `brainOccupied` (155-164).
  </action>
  <verify>
    <automated>npm test -- tests/viz-seed-determinism.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A new `tests/viz-seed-determinism.test.ts` (vi.mock('three') headless, mirroring viz-haze-activation.test.ts) imports the now-exported `seedNodePositions`, runs it twice over a fixed fake brainVol + node set, and asserts identical x/y/z per node (determinism, D-08).
    - The test asserts member nodes land within the cluster radius of their schema hub (clustering, D-02) and all points pass the fake brainOccupied.
    - `grep -n "Math.random" src/viz/modules/graph.js` shows NO occurrence inside the brainVol branch of seedNodePositions (the null-fallback branch may retain it).
    - `seedNodePositions` is exported; signature `(allNodes, brainVol)` unchanged; BRAIN_SCALE usage still `multiplyScalar(BRAIN_SCALE)`.
  </acceptance_criteria>
  <done>seedNodePositions is continuous, clustered, deterministic, exported; determinism test green; no Math.random in the brainVol path.</done>
</task>

<task type="auto">
  <name>Task 3: Replace cooldownTicks(12) with wall-clock SETTLE_BUDGET_MS settle</name>
  <files>src/viz/modules/graph.js</files>
  <read_first>
    - src/viz/modules/graph.js (lines 594-614 — settle/reveal block; lines 582-592 — brainContainment via onEngineTick)
    - src/viz/modules/graph.js (the import block at lines 16-35 — add SETTLE_BUDGET_MS to the constants import)
    - .planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-PATTERNS.md (section "graph.js:594-614 settle/reveal block" — exact change: stop-condition only; section "Hidden-then-fade reveal (LOCKED)")
    - .planning/phases/52-brain-viz-honest-traces/52-CONTEXT.md (the honest-traces flashes the reveal path must not regress)
  </read_first>
  <action>
    In the settle/reveal block (graph.js:594-614): import `SETTLE_BUDGET_MS` from `./constants.js` (add to the existing constants import). Remove `Graph.cooldownTicks(12)` (line 598) and replace with `Graph.cooldownTicks(0)` (or a high ceiling) so the sim runs freely during the budget rather than self-stopping after 12 ticks (D-03). Change the primary reveal trigger `setTimeout(revealSettled, 200)` (line 614) to `setTimeout(revealSettled, SETTLE_BUDGET_MS)`. Keep `Graph.onEngineStop(revealSettled)` as the fallback. The `revealSettled()` function body (lines 603-611 — the `n.fx/fy/fz` pin + `opacity 0 → transition 0.35s ease → opacity 1` fade) is LOCKED (D-07, Phase 52 regression risk) — do NOT modify it. Do NOT touch `brainContainment` or its `Graph.onEngineTick` wiring (582-592).
  </action>
  <verify>
    <automated>grep -q "import" src/viz/modules/graph.js && ! grep -q "cooldownTicks(12)" src/viz/modules/graph.js && grep -q "setTimeout(revealSettled, SETTLE_BUDGET_MS)" src/viz/modules/graph.js && grep -q "graphEl.style.transition = 'opacity 0.35s ease'" src/viz/modules/graph.js && grep -q "Graph.onEngineStop(revealSettled)" src/viz/modules/graph.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `Graph.cooldownTicks(12)` no longer appears in graph.js.
    - Primary reveal is `setTimeout(revealSettled, SETTLE_BUDGET_MS)`; `SETTLE_BUDGET_MS` is imported from constants.js.
    - `revealSettled()` body byte-unchanged: the `opacity 0.35s ease` fade and `fx/fy/fz` pin are intact (Phase 52 no-regress, D-07).
    - `Graph.onEngineStop(revealSettled)` fallback retained; brainContainment/onEngineTick untouched.
  </acceptance_criteria>
  <done>Settle is wall-clock bounded by SETTLE_BUDGET_MS; reveal path locked; containment untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph payload → client viz | Existing, unchanged. Node positions are computed client-side from the already-delivered payload; no new data crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | seedNodePositions / settle block | accept | Pure presentation-layer geometry. No new input parsed; node positions are decorative (CLAUDE.md: viz is chrome, positions NOT semantic). Deterministic seed is a pure function of the existing payload — introduces no new attack surface. |
| T-53-02 | Denial of Service | wall-clock settle | mitigate | SETTLE_BUDGET_MS hard-bounds the settle by wall-clock regardless of node count (D-03) — removes the unbounded-tick risk at scale rather than adding one. |

No high-severity threats: zero new data, network, auth, or input-handling surface.
</threat_model>

<verification>
- `npm test -- tests/viz-seed-determinism.test.ts` green (determinism + clustering).
- Source greps: no `cooldownTicks(12)`, no `Math.random` in the brainVol seed path, `SETTLE_BUDGET_MS` imported + used, `revealSettled()` body unchanged, `BRAIN_SCALE = 460` intact.
- Full Phase 52 viz suite stays green (verified in plan 53-03): `npm test -- tests/viz-haze-activation.test.ts tests/remember-viz-bridge.test.ts tests/sleep-pass-viz-lighting.test.ts`.
</verification>

<success_criteria>
- Gridlines eliminated at the seed layer (continuous full-cell sampling; D-04 / ROADMAP SC1).
- Nodes start clustered by schema membership (D-01/D-02 / ROADMAP SC3).
- Layout deterministic across reloads, no persistent cache (D-08).
- Settle bounded by SETTLE_BUDGET_MS, reveal path locked (D-03/D-07 / Phase 52 no-regress).
</success_criteria>

<output>
Create `.planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-01-SUMMARY.md` when done.
</output>
