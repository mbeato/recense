---
phase: 68-telegram-hitl-belief-kind-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - clients/telegram/types.ts
  - clients/telegram/index.ts
  - clients/telegram/belief-proposal-client.ts
  - clients/telegram/belief-bridge.ts
  - clients/telegram/tests/approval-handler.test.ts
  - clients/telegram/tests/edit-path.test.ts
  - clients/telegram/tests/typed-confirm.test.ts
  - clients/telegram/tests/proposal-store.test.ts
  - clients/telegram/tests/belief-union-store.test.ts
  - clients/telegram/tests/belief-proposal-client.test.ts
autonomous: true
requirements: [APPROVE-02]

must_haves:
  truths:
    - "A StoredProposal with kind:'belief' round-trips through the UNMODIFIED proposal-store — putProposal then getProposal returns it byte-equal, and isExpired(row, t) is false before and true after the server's expires_at (D-01, D-03)"
    - "proposal-engine.ts and proposal-store.ts have zero diff against the phase base commit 561f9c8 (D-01, APPROVE-02 locked SC)"
    - "The belief HTTP bridge builds no request at all for a proposal id that is not 64 lowercase hex characters (D-04)"
    - "A belief-kind row can never reach the MCP tool-execution path: every tool-path load site refuses on kind !== 'tool' (D-02)"
    - "Tests live in clients/telegram/tests (scanned by the hardened import-boundary guard, zero engine imports), the sha256 hash-lock + zero-diff assertion on proposal-store.ts/proposal-engine.ts is part of plan verification, and the full coverage list of D-10 (union round-trip, bypass sentinel, edit short-circuit, refusal handling, batching, no-numeric-confidence structural grep, zero-diff lock) is distributed across the three plans (D-10)"
  artifacts:
    - path: "clients/telegram/types.ts"
      provides: "StoredProposal as a discriminated union of StoredToolProposal (kind:'tool') and StoredBeliefProposal (kind:'belief')"
      contains: "kind: 'belief'"
    - path: "clients/telegram/belief-proposal-client.ts"
      provides: "list/approve/reject against /v1/proposals with Bearer auth, 64-hex id gate, ProposalHttpError"
      exports: ["createBeliefProposalClient", "ProposalHttpError", "PROPOSAL_SCHEMA_VERSION", "isInspectableProposalRecord"]
    - path: "clients/telegram/belief-bridge.ts"
      provides: "toStoredBeliefProposal record→row mapping and beliefLocalId derivation"
      exports: ["toStoredBeliefProposal", "beliefLocalId", "isBeliefProposal"]
    - path: "clients/telegram/tests/belief-union-store.test.ts"
      provides: "store round-trip + expiry + cap proof through the untouched store, and the zero-diff hash lock"
  key_links:
    - from: "clients/telegram/belief-bridge.ts"
      to: "clients/telegram/proposal-store.ts isExpired"
      via: "dueAt / createdAt / maxTtlMs synthesized from the server record's expires_at"
      pattern: "maxTtlMs"
    - from: "clients/telegram/belief-proposal-client.ts"
      to: "/v1/proposals"
      via: "fetch with Authorization: Bearer"
      pattern: "/v1/proposals"
---

<objective>
Make `kind:'belief'` a first-class member of the existing `StoredProposal` union and stand up the HTTP bridge to the frozen Phase 66 `/v1/proposals` contract — while `proposal-engine.ts` and `proposal-store.ts` stay byte-identical (APPROVE-02, locked verbatim in the ROADMAP SC).

Purpose: this is the load-bearing wave. The whole zero-fork claim rests on the belief member carrying `dueAt` / `createdAt` / `maxTtlMs` in shapes `isExpired()` already understands, so the store's expiry and daily-cap machinery works on belief rows with no code change.
Output: extended union, narrowing guards at every tool-path load site, a belief HTTP client, the record→row mapper, and the tests that lock all of it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/68-telegram-hitl-belief-kind-extension/68-CONTEXT.md
@.planning/phases/68-telegram-hitl-belief-kind-extension/68-PATTERNS.md
@clients/telegram/types.ts
@clients/telegram/proposal-store.ts
@clients/proposal-reference/proposal-client.ts

<interfaces>
<!-- Contracts the executor needs. Extracted from live source — do not go exploring. -->

The store function the belief member MUST satisfy, verbatim from clients/telegram/proposal-store.ts:115-117
(this signature does NOT change — it is why the belief member needs dueAt/createdAt/maxTtlMs):
```typescript
export function isExpired(p: StoredProposal, now: number): boolean {
  return now > Date.parse(p.dueAt) || now > Date.parse(p.createdAt) + p.maxTtlMs;
}
```

The store's load filter, verbatim from clients/telegram/proposal-store.ts:64-68 — it validates only `id`,
so a belief row persists and reloads with no store change:
```typescript
const proposals = Array.isArray(obj['proposals'])
  ? (obj['proposals'] as unknown[]).filter((p): p is StoredProposal =>
      typeof p === 'object' && p !== null && typeof (p as Record<string, unknown>)['id'] === 'string',
    )
  : [];
```

The wire record to mirror locally (never import it), from clients/proposal-reference/proposal-client.ts:40-57:
```typescript
export interface ActionProposalRecord {
  id: string; kind: 'belief'; entity_node_id: string; entity_descriptor: string;
  belief_node_id: string; change_field: string; change_from: string | null; change_to: string;
  evidence_episode: string; evidence_quote: string; confidence: 'high' | 'medium' | 'low';
  schema_version: number; status: 'pending' | 'approved' | 'rejected' | 'superseded' | 'expired';
  created_at: number; updated_at: number; expires_at: number;
}
```
`PROPOSAL_SCHEMA_VERSION` is 17 (verified against src/db/schema.ts:12 — SCHEMA_VERSION = 17).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn StoredProposal into a discriminated union and guard every tool-path load site</name>
  <files>clients/telegram/types.ts, clients/telegram/index.ts, clients/telegram/tests/approval-handler.test.ts, clients/telegram/tests/edit-path.test.ts, clients/telegram/tests/typed-confirm.test.ts, clients/telegram/tests/proposal-store.test.ts</files>
  <read_first>
    - clients/telegram/types.ts (full — the flat StoredProposal at :103-141 and ProposalAction at :147)
    - clients/telegram/index.ts lines 576-602 (renderProposalCard / proposalKeyboard), 730-760 (literal at :737), 960-1010 (literal at :976), 1036-1148 (handleProposalAction)
    - clients/telegram/index.ts lines 778-908 (executeStoredProposal — field accesses at :801,804,810,816-817,832-833,849-851,864-865)
    - .planning/phases/68-telegram-hitl-belief-kind-extension/68-PATTERNS.md section "clients/telegram/types.ts — extend StoredProposal into a discriminated union (D-01)"
  </read_first>
  <action>
Rename the existing flat interface to `StoredToolProposal`, add a required discriminant `kind: 'tool'` as its first field, keep every other field unchanged. Add `StoredBeliefProposal` with `kind: 'belief'` and exactly these fields: `id` (LOCAL store key, 32 lowercase hex — see Task 3, NOT the server id), `serverProposalId` (the server's 64-hex sha256 id, the only value ever interpolated into a request path), `entityDescriptor`, `changeField`, `changeFrom: string | null`, `changeTo`, `evidenceQuote`, `confidence: 'high' | 'medium' | 'low'`, `serverCreatedAtMs: number` (the record's own `created_at`, carried so Plan 02 can headline the genuinely latest transition in a batch instead of inferring recency from expiry order), `localStatus: 'pending' | 'terminal'`, plus the three store-compatibility fields `dueAt`, `createdAt`, `maxTtlMs`. Export `export type StoredProposal = StoredToolProposal | StoredBeliefProposal;` so every existing importer keeps compiling against the same name. Also export `ProposalStatus` as the locally-declared server status vocabulary ('pending' | 'approved' | 'rejected' | 'superseded' | 'expired') — declared by hand, never imported from src/ (CLIENT-01).

Document on `StoredBeliefProposal` why `dueAt`/`createdAt`/`maxTtlMs` exist: they are the field contract `isExpired()` reads, and carrying them is what makes APPROVE-02's zero-change-to-proposal-store constraint true. Document that `confidence` is categorical-only and is never a gate (D-06). Do NOT add `numericConfidence`, `score`, or any numeric field.

Add `kind: 'tool'` to the two production literal sites: index.ts:737 (`const proposal: StoredProposal = {`) and index.ts:976 (`const freshProposal: StoredProposal = {`). Retype both annotations to `StoredToolProposal`, and retype `tryGenerateProposal`'s return to `Promise<StoredToolProposal | null>` and `renderProposalCard`'s parameter to `StoredToolProposal` — this keeps the tool card renderer structurally incapable of receiving a belief row.

Add the kind guard at every site where index.ts loads an opaque row from the store and then reads tool-only fields. There are five: `executeStoredProposal` (right after its `loadExecutable` call, before the `mcpConfigs.find` at :801), `handleEditPatch` (right after `getProposal` at :930, before `original.args` is spread), and three inside `handleProposalAction` — after `getProposal` in the reject branch (:1052), after `loadExecutable` in the edit branch (:1077), and after `loadExecutable` in the approve branch (:1106). Each guard: if the loaded row's `kind === 'belief'`, log a one-line refusal, reply to the user with a fixed literal ("that decision is handled by the belief approval flow"), write no MCP call and no hitlEpisode tool fields, and return. This is not defensive decoration — it is the T-68-02 mitigation: `2|{beliefLocalId}|a` is a forgeable callback_data string and without the guard it would reach `callServerTool` with an undefined tool name. Import `isBeliefProposal` from ./belief-bridge (Task 3) for the check rather than inlining `kind === 'belief'` string comparisons in five places.

Update the four test helper factories so their literals still typecheck as tool proposals: approval-handler.test.ts:83-85, edit-path.test.ts:163-165, typed-confirm.test.ts:73-74 and :91-92, proposal-store.test.ts:54. Add `kind: 'tool'` to each literal and change the `Partial<StoredProposal>` override parameters and return annotations to `StoredToolProposal`. Do not change any assertion in those files — the existing behavior is unchanged and must stay proven.
  </action>
  <acceptance_criteria>
    - `clients/telegram/types.ts` exports `StoredToolProposal`, `StoredBeliefProposal`, and `StoredProposal` as their union; `grep -c "kind: 'belief'" clients/telegram/types.ts` is at least 1
    - `npx tsc --noEmit -p clients/telegram/tsconfig.json` exits 0
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (the engine tree is untouched and must stay clean)
    - `npx vitest run clients/telegram/tests` passes with no reduction in test count versus the pre-change baseline
    - `git diff --stat 561f9c8 -- clients/telegram/proposal-store.ts clients/telegram/proposal-engine.ts` prints nothing
    - `grep -vn '^\s*\*' clients/telegram/index.ts | grep -c "isBeliefProposal("` is at least 5 (one guard per tool-path load site)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p clients/telegram/tsconfig.json &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npx vitest run clients/telegram/tests &amp;&amp; test -z "$(git diff --stat 561f9c8 -- clients/telegram/proposal-store.ts clients/telegram/proposal-engine.ts)"</automated>
  </verify>
  <done>The union compiles, every pre-existing telegram test still passes, the two frozen files are byte-identical to the phase base commit, and a belief row cannot be read as a tool row anywhere in index.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Belief HTTP bridge client against the frozen /v1/proposals contract</name>
  <files>clients/telegram/belief-proposal-client.ts, clients/telegram/tests/belief-proposal-client.test.ts</files>
  <read_first>
    - clients/proposal-reference/proposal-client.ts (full 172 lines — the proven consumer of this exact contract; mirror its structure)
    - clients/telegram/memory-client.ts lines 94-130 (this client's own Bearer/timeout/throw-on-non-2xx factory conventions)
    - clients/proposal-reference/index.ts lines 85-95 (REQUIRED_RECORD_KEYS / isInspectableRecord wire-shape gate)
    - src/adapter/serve-cli.ts lines 517-605 (the exact statuses the client will surface: 200, 400, 404, 409, 413, 500, 503)
    - clients/proposal-reference/tests/sync-loop.test.ts lines 1-150 (http.createServer stub harness + free-port pattern)
  </read_first>
  <behavior>
    - listProposals() on a stub returning `{items:[record]}` resolves to a one-element array
    - listProposals() on a body with no `items` key resolves to `[]` (never throws on shape)
    - listProposals() on HTTP 500 rejects with ProposalHttpError carrying `status === 500`
    - approve('not-hex') rejects with a plain Error and the stub server records ZERO requests
    - approve('<64 hex>') issues exactly one POST to `/v1/proposals/<id>/approve` carrying `Authorization: Bearer <token>`
    - reject() on HTTP 409 rejects with ProposalHttpError carrying `status === 409`
    - isInspectableProposalRecord() returns false for a null, for a string, and for an object missing `change_to`
    - the serve token appears in no thrown Error message and in no value the module returns
  </behavior>
  <action>
Create `clients/telegram/belief-proposal-client.ts` with zero imports — Node global `fetch` only, matching proposal-client.ts's zero-import posture and satisfying CLIENT-01 (the hardened import-boundary guard scans this file automatically).

Transcribe the wire vocabulary locally: `ProposalStatus`, `ActionProposalRecord` (all 16 fields), and `export const PROPOSAL_SCHEMA_VERSION = 17` with a comment recording that it mirrors src/db/schema.ts SCHEMA_VERSION at authoring time and that a mismatch is a stop condition for the caller, never a coercion.

Export `isInspectableProposalRecord(v: unknown): boolean` built on a `REQUIRED_RECORD_KEYS` array covering all 16 keys — the same wire-shape gate 67 uses before any per-item work.

Copy `PROPOSAL_ID_RE = /^[0-9a-f]{64}$/` and the `ProposalHttpError` class shape verbatim from proposal-client.ts:68 and :79-86. Copy the WR-04 rationale comment: fetch's WHATWG URL normalization collapses dot segments, so an id containing `/`, `..`, `?`, or `#` would steer the authenticated POST onto a route this client never intended to call — a non-conforming id must build no request at all.

Export `createBeliefProposalClient(serveUrl: string, serveToken: string)` returning `{ listProposals, approve, reject }`. Build the `Bearer` header value exactly once at factory scope and never log it, never include it in an Error message, never return it. `listProposals` GETs `/v1/proposals` with a 10s `AbortSignal.timeout` and returns `body.items` when it is an array, `[]` otherwise. `approve`/`reject` POST `/v1/proposals/{id}/approve|reject` with an empty JSON object body (serve drains but never parses it) and throw `ProposalHttpError(res.status)` on non-2xx. Do not interpret or branch on the status here and do not parse the `detail` string — status interpretation belongs to Plan 03's handler, exactly as 67 split it.

Write `clients/telegram/tests/belief-proposal-client.test.ts` as a stub-server behavioral proof following the sync-loop.test.ts harness: `http.createServer` on a free port, one `it()` per behavior above, and a request-recording array so the malformed-id case can assert zero requests reached the server.
  </action>
  <acceptance_criteria>
    - `npx vitest run clients/telegram/tests/belief-proposal-client.test.ts` passes with at least 8 assertions matching the `<behavior>` list
    - `grep -c "from '" clients/telegram/belief-proposal-client.ts` returns 0 (zero-import posture preserved)
    - `grep -c "\^\[0-9a-f\]{64}\$" clients/telegram/belief-proposal-client.ts` is at least 1
    - the malformed-id test asserts the stub server's recorded-request array has length 0
    - `npx vitest run clients/telegram/tests/import-boundary.test.ts` passes with the new file in the scan
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run clients/telegram/tests/belief-proposal-client.test.ts clients/telegram/tests/import-boundary.test.ts</automated>
  </verify>
  <done>The bridge lists, approves, and rejects against a stub serve; a non-64-hex id produces no HTTP request; non-2xx surfaces as a typed status the caller can branch on; the token never leaves the factory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record→row mapping, deterministic local id, and the zero-diff lock</name>
  <files>clients/telegram/belief-bridge.ts, clients/telegram/tests/belief-union-store.test.ts</files>
  <read_first>
    - clients/telegram/proposal-store.ts (full — isExpired :115, putProposal :128, getProposal :146, loadExecutable :185, tryReserveProposalSlot :238, toLocalDate :216)
    - clients/telegram/tests/proposal-store.test.ts lines 1-80 (tmpdir store-path fixture pattern to reuse)
    - .planning/phases/68-telegram-hitl-belief-kind-extension/68-PATTERNS.md section "Zero-diff constraint note"
  </read_first>
  <behavior>
    - toStoredBeliefProposal(record, nowMs) produces a row where `Date.parse(dueAt) === record.expires_at`
    - the produced row satisfies `Date.parse(createdAt) + maxTtlMs === record.expires_at` — both isExpired conditions fall on the same instant
    - isExpired(row, record.expires_at - 1) is false; isExpired(row, record.expires_at + 1) is true
    - a record whose expires_at is already in the past yields maxTtlMs === 0 (never negative)
    - beliefLocalId(serverId) returns the first 32 characters of the server id and is stable across calls
    - toStoredBeliefProposal copies serverCreatedAtMs from record.created_at unchanged
    - putProposal(row) then getProposal(row.id) returns a deep-equal row through the UNMODIFIED store
    - a belief row and a tool row coexist in one store file; getProposal returns each by its own id
    - tryReserveProposalSlot decrements the same shared daily budget regardless of which kind consumed it
    - the store file written with a belief row inside reloads correctly after a simulated process restart (fresh read)
  </behavior>
  <action>
Create `clients/telegram/belief-bridge.ts` (zero src/ imports; it may import types from ./types and functions from ./proposal-store).

Export `beliefLocalId(serverProposalId: string): string` returning `serverProposalId.slice(0, 32)`. Document the two reasons this truncation exists rather than a `randomUUID()`: (1) it makes re-listing idempotent for free — a re-listed proposal maps to the same store key, so `getProposal(beliefLocalId(id))` IS the dedup check and no store-listing API has to be added to the frozen store; (2) `2|` and `3|` callback_data payloads are capped at Telegram's 64-byte limit, and 32 hex chars keeps a `3|{id}|{code}` payload at 37 bytes. 128 bits of a sha256 prefix is not a collision concern at this scale.

Export `toStoredBeliefProposal(record: ActionProposalRecord, nowMs: number): StoredBeliefProposal`. The field mapping is the load-bearing part of APPROVE-02 — get it exactly right:
`id` = beliefLocalId(record.id); `serverProposalId` = record.id; `entityDescriptor`/`changeField`/`changeFrom`/`changeTo`/`evidenceQuote`/`confidence` copied verbatim from the record with no normalization, no trimming, no paraphrase (D-05 — display-time sanitization happens in Plan 02's renderer, never here); `serverCreatedAtMs` = record.created_at; `localStatus` = 'pending'; `dueAt` = `new Date(record.expires_at).toISOString()`; `createdAt` = `new Date(nowMs).toISOString()`; `maxTtlMs` = `Math.max(0, record.expires_at - nowMs)`.

Write a comment on the maxTtlMs line stating explicitly that `maxTtlMs: 0` is WRONG here: `isExpired` evaluates `now > Date.parse(createdAt) + maxTtlMs`, so a zero TTL expires the row on the very next tick. The two expiry conditions must be made to coincide at the server's `expires_at` so the client's local aging and the server's own expiry agree and no row is surfaced past its server-side window.

Export `isBeliefProposal(p: StoredProposal): p is StoredBeliefProposal` — the single type predicate Task 1's five index.ts guards import.

Write `clients/telegram/tests/belief-union-store.test.ts` covering every `<behavior>` bullet against the real `proposal-store.ts` (no mocks of the store — the point is proving the untouched store handles the new kind), using a per-test tmpdir store path.

Add a second describe block in the same file: the zero-diff hash lock (D-10, "convention-enforced invariants fail"). Compute the sha256 of `clients/telegram/proposal-store.ts` and `clients/telegram/proposal-engine.ts` at test time with node:crypto and assert each equals a pinned constant recorded from the current, untouched files. Head the block with a comment stating that these two files are frozen for the lifetime of APPROVE-02, that a failure here means the zero-fork success criterion was breached, and that updating a constant is a deliberate decision requiring the APPROVE-02 SC to be revisited — not a routine test fix.
  </action>
  <acceptance_criteria>
    - `npx vitest run clients/telegram/tests/belief-union-store.test.ts` passes with at least 9 test cases
    - the test file imports `isExpired`, `putProposal`, `getProposal`, and `tryReserveProposalSlot` from `../proposal-store` (real store, not a stub)
    - `grep -c "maxTtlMs" clients/telegram/belief-bridge.ts` is at least 2 (mapping line plus the wrong-value warning comment)
    - the hash-lock block fails when either frozen file is modified — prove it by editing a comment in proposal-store.ts, observing the failure, then reverting with `git checkout -- clients/telegram/proposal-store.ts`
    - `git diff --stat 561f9c8 -- clients/telegram/proposal-store.ts clients/telegram/proposal-engine.ts` prints nothing after the deliberate-failure check is reverted
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run clients/telegram/tests/belief-union-store.test.ts &amp;&amp; test -z "$(git diff --stat 561f9c8 -- clients/telegram/proposal-store.ts clients/telegram/proposal-engine.ts)"</automated>
  </verify>
  <done>A belief row lives in the real store, ages on the server's clock, and shares the daily cap — with both frozen files provably unmodified and a test that will scream if that ever changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recense serve → telegram client | `/v1/proposals` records carry model-extracted, email-derived free text (entity_descriptor, change_from, change_to, evidence_quote) that originated with an untrusted third party |
| Telegram getUpdates → telegram client | `callback_data` is attacker-influenceable: any chat member can craft a value, and only the sender allowlist stands in front of it |
| telegram client → recense serve | An authenticated Bearer POST whose path contains a server-supplied id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Elevation of Privilege | `handleProposalAction` / `executeStoredProposal` / `handleEditPatch` | mitigate | Forged `2|{beliefLocalId}|a` would reach `callServerTool` with an undefined tool name. Task 1 adds an `isBeliefProposal` refusal at all five tool-path load sites, before any field read |
| T-68-02 | Tampering | `belief-proposal-client.approve/reject` | mitigate | `^[0-9a-f]{64}$` on `serverProposalId` before path construction; a non-conforming id builds no request (67 WR-04 lesson, verified by a zero-requests-recorded test) |
| T-68-03 | Information Disclosure | `createBeliefProposalClient` | mitigate | Bearer header built once at factory scope; never logged, never in an Error message, never returned. `ProposalHttpError` carries only the numeric status |
| T-68-04 | Tampering | `toStoredBeliefProposal` | mitigate | Verbatim copy only — no `eval`, no template expansion, no interpretation of record text at map time. Display-time sanitization is Plan 02's renderer, keeping the stored value honest (D-05) |
| T-68-05 | Spoofing | wire-shape / schema gate | mitigate | `isInspectableProposalRecord` plus `PROPOSAL_SCHEMA_VERSION === 17` give Plan 02 a fail-closed stop before any per-item work — an unrecognized response shape applies nothing |
| T-68-06 | Denial of Service | store growth via belief rows | accept | Rows are bounded by the server's `PROPOSAL_LIST_LIMIT` and the existing daily cap, and expire on the server's clock via the synthesized `dueAt`/`maxTtlMs`; the store already cleans expired rows on read |
| T-68-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs in this phase — the net-zero-runtime-deps invariant holds. Verification asserts `git diff --stat 561f9c8 -- package.json package-lock.json` is empty, so no legitimacy gate is bypassed by an unnoticed dependency |
</threat_model>

<verification>
- `npx tsc --noEmit -p clients/telegram/tsconfig.json` and `npx tsc --noEmit -p tsconfig.json` both exit 0
- `npx vitest run clients/telegram/tests` — all green, count not lower than baseline
- `git diff --stat 561f9c8 -- clients/telegram/proposal-store.ts clients/telegram/proposal-engine.ts` prints nothing (APPROVE-02 zero-diff lock)
- `git diff --stat 561f9c8 -- package.json package-lock.json` prints nothing (net-zero deps)
- KNOWN FLAKE: `tests/strip-hidden.test.ts` has 2 machine-load-sensitive perf cases. If the only failures are there, re-run that file in isolation before treating it as a regression.
</verification>

<success_criteria>
- `StoredProposal` is a discriminated union with a `kind:'belief'` member, and every tool-path load site refuses belief rows
- A belief row persists, reloads, ages, and consumes daily-cap budget through `proposal-store.ts` with that file byte-identical to commit 561f9c8
- `belief-proposal-client.ts` lists/approves/rejects against a stub serve, with the 64-hex id gate proven to build no request on malformed input
- No new runtime dependency, no `src/` import anywhere under `clients/telegram/`
</success_criteria>

<output>
Create `.planning/phases/68-telegram-hitl-belief-kind-extension/68-01-SUMMARY.md` when done
</output>
