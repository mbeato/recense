---
phase: 38.1-consolidation-intra-pass-dedup-fix-embed-on-mint
verified: 2026-06-21T15:14:30Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
---

# Phase 38.1: Consolidation Intra-Pass Dedup Fix (Embed-on-Mint) Verification Report

**Phase Goal:** Claims arriving in the SAME consolidation pass can dedup/contradict each other — a mid-pass-minted node is visible to later episodes' `topk` in that pass, instead of minting permanent duplicate islands. Fixes the bulk-ingest onboarding path (`ingest-project` into a small/empty graph) and unblocks a fair Phase 35 RANK-02 re-test.
**Verified:** 2026-06-21T15:14:30Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Two claims that contradict each other, arriving in the SAME consolidation pass, reconcile (contra >= 1 + tombstone) instead of minting two unrelated duplicate nodes | VERIFIED | DEDUP-01 test at tests/consolidator.test.ts:450 passes: tombstoned.cnt >= 1 AND provider.judgeCalls >= 1 confirmed by `npm test -- --run tests/consolidator.test.ts` (8/8 passed) |
| 2 | A node minted mid-pass by episode N is visible to episode N+1's topk in the same pass (its embedding is non-NULL immediately after mint) | VERIFIED | `setEmbedding(mintedId, decision.claimVec)` called at all 8 mint sites in applyDecision (consolidator.ts lines 945, 973, 997, 1052, 1081, 1103, 1151, 1174). `grep -c "decision.claimVec" src/consolidation/consolidator.ts` = 8 |
| 3 | Incremental multi-pass behavior is unchanged: the existing consolidator tests stay green | VERIFIED | `npm test -- --run tests/consolidator.test.ts` exits 0 with 8 tests passed (7 pre-existing + 1 new DEDUP-01 test). Full suite: 2031 passed, 3 skipped (137 files, 1 skipped) |
| 4 | setEmbedding remains the single writer of node.embedding (T-01-DIRTY) | VERIFIED | `grep -rn "SET embedding" src/` returns exactly one result: `src/db/semantic-store.ts:132` — no new writer added |
| 5 | No await is introduced inside the Phase B db.transaction (T-02-ASYNC) | VERIFIED | Phase B transaction block confirmed at consolidator.ts:813-829. `grep -n "await" src/consolidation/consolidator.ts` shows no await between those lines. All await tokens are at lines 303, 312, 317, 325, 344, 368, 409, 460, 481, 508, 560, 567, 568, 575, 584, 727, 738, 741, 841, 844, 848, 852, 855 — all outside the transaction block |
| 6 | RANK-01 dark default (rankStrengthWeight: 0) is untouched | VERIFIED | `grep -n "rankStrengthWeight" src/lib/config.ts` shows `rankStrengthWeight: 0,  // D-04: dark default — ships w=0; no behavior change at merge` at line 727 |

**Score:** 6/6 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/consolidation/consolidator.ts` | ClaimDecision.claimVec field + embed-on-mint at value==claim mint sites | VERIFIED | `claimVec?: Float32Array` field at line ~157 (inside ClaimDecision interface); populated at auto-unrelated fill (line 700: `claimVec: queryVec`) and judge-verdict fill (line 778: `claimVec: claimVecs[pendingJudges[i]!.slotIdx] ?? undefined`); 8 guarded setEmbedding calls at mint sites |
| `tests/consolidator.test.ts` | DEDUP-01 intra-pass dedup regression test (same pair in ONE pass -> contra=1) | VERIFIED | `describe('DEDUP-01: embed-on-mint intra-pass visibility', ...)` at line 450; uses CandidateCapturingProvider subclass (lines 409-447) that echoes candidates[0].id as best_candidate_id; asserts tombstoned.cnt >= 1 AND judgeCalls >= 1 |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| decisionSlots fill (auto-unrelated ~line 700) | ClaimDecision.claimVec | `claimVec: queryVec` (`queryVec = claimVecs[claimIdx]` in scope at line 609) | WIRED | Line 700: `claimVec: queryVec,  // DEDUP-01: embed-on-mint` confirmed |
| decisionSlots fill (judge-verdict ~line 778) | ClaimDecision.claimVec | `claimVec: claimVecs[pendingJudges[i]!.slotIdx] ?? undefined` | WIRED | Line 778: `claimVec: claimVecs[pendingJudges[i]!.slotIdx] ?? undefined, // DEDUP-01: embed-on-mint` confirmed |
| applyDecision value==claim upsertNode mint sites (8 sites) | store.setEmbedding(mintedId, decision.claimVec) | synchronous setEmbedding call inside Phase B transaction, guarded by `if (decision.claimVec)` | WIRED | All 8 sites confirmed: newId_ (945), standaloneId (973), unrelatedId (997), oscId (1052), reconciledId (1081), appendNewId (1103), fdOscId (1151), fdId (1174) |

---

### Data-Flow Trace (Level 4)

Not applicable — this phase modifies engine internals (synchronous DB write path), not a rendering component. No user-visible data rendering to trace.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Build exits clean | `npm run build` | exit 0 | PASS |
| DEDUP-01 test + all consolidator tests pass | `npm test -- --run tests/consolidator.test.ts` | 8 passed (0 failed) | PASS |
| Full test suite green | `npm test` | 2031 passed, 3 skipped (137 files, 1 skipped) | PASS |
| Single writer: only setEmbedding in semantic-store.ts writes node.embedding | `grep -rn "SET embedding" src/` | `src/db/semantic-store.ts:132` (one result) | PASS |
| RANK-01 dark default unchanged | `grep -n "rankStrengthWeight" src/lib/config.ts` | `rankStrengthWeight: 0` at line 727 | PASS |
| `decision.claimVec` count matches 8 qualifying mint sites | `grep -c "decision.claimVec" src/consolidation/consolidator.ts` | 8 | PASS |
| No await inside Phase B transaction (lines 813-829) | Inspection + grep | No await tokens in that range | PASS |

---

### Probe Execution

No probe scripts declared or applicable for this phase.

---

### Requirements Coverage

| Requirement | Source | Description | Status | Evidence |
|-------------|--------|-------------|--------|----------|
| DEDUP-01 | ROADMAP.md:492 + 38.1-01-PLAN.md | Embed-on-mint: minted node's embedding set from the already-computed claim vector so it's `topk`-visible immediately (Option D) | SATISFIED | `claimVec` field added to ClaimDecision; populated at 2 fill sites; `setEmbedding(mintedId, decision.claimVec)` called at all 8 mint sites in applyDecision; DEDUP-01 regression test passes |
| DEDUP-02 | ROADMAP.md:492 + 38.1-01-PLAN.md | No regression: incremental multi-pass path + RANK-01 dark default unchanged; setEmbedding stays single writer, T-01-DIRTY intact | SATISFIED | Full suite 2031 passed; single-writer grep clean; rankStrengthWeight: 0 unchanged; no await in Phase B |

**Note on REQUIREMENTS.md coverage:** DEDUP-01 and DEDUP-02 as used in Phase 38.1 are inline roadmap-level requirement IDs defined within ROADMAP.md:492 for this inserted correctness phase. They are distinct from the Phase 25 entity-dedup DEDUP-01/02/03 IDs. REQUIREMENTS.md tracks v6.0 milestone scope (INGEST-*, DOCING-*, REINGEST-*, RECALL-*, VIZ-POLISH-*); Phase 38.1 is a correctness fix inserted between v6.0 milestones and its requirements are fully accounted for in ROADMAP.md:492,497.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | — |

No TBD, FIXME, XXX, or stub patterns found in the modified files. The two modified files (`src/consolidation/consolidator.ts`, `tests/consolidator.test.ts`) contain clean, fully-implemented changes with doc comments referencing DEDUP-01.

---

### Human Verification Required

None. All must-haves are verified programmatically via build, test suite, and grep checks.

---

### Gaps Summary

No gaps. All 6 must-haves verified. Phase goal achieved.

---

_Verified: 2026-06-21T15:14:30Z_
_Verifier: Claude (gsd-verifier)_
