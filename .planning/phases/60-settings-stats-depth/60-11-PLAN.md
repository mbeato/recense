---
phase: 60-settings-stats-depth
plan: 11
type: execute
wave: 2
depends_on: [60-10]
gap_closure: true
files_modified:
  - src/viz/modules/stats-dashboard.js
  - src/viz/css/styles.css
  - tests/viz-frontend-static.test.ts
autonomous: true
requirements: [GAP-4a, GAP-4b, GAP-4c, GAP-4d, GAP-4e, GAP-4f]
must_haves:
  truths:
    - "The Usage tab leads with exactly ONE hero figure (today's tokens, ~40-48px JetBrains Mono) — the other four stats are a quiet hairline-separated supporting row, not five equal boxes"
    - "The 'vs your typical' prose block is gone; framing is expressed as signed deltas on the figures (plain +18% / −12% text, green when usage is down, mauve when up) — no ▲▼→ glyphs anywhere"
    - "Retail-$ shows 2 decimals ($X.XX) with the subscription-billed line as a muted footnote, not a tile label"
    - "Levers and breakdown are quiet borderless tables: 10px uppercase muted column headers, hairline row separators, %-saved the one emphasized (mono, green) value per lever row"
    - "Both tabs are de-boxed — no .chart-card container; structure comes from 10px uppercase letter-spaced section headers + hairline dividers on the flat #stats-view surface"
    - "Single-series charts (burn, node growth, reconsolidations, tombstones) carry NO legend; true multi-series charts (kind mix, judge, episodes) keep theirs"
    - "Brain Health six metric groups are consistent-height sections (small-multiples feel); last-sleep-pass is a one-line muted readout (label + mono value + honest status), not a card"
    - "Charts still re-render responsively at measured width on resize (GAP-1) and marker hover / nearest-point hover (CR-01/CR-04) still work on the de-boxed charts"
  artifacts:
    - path: "src/viz/modules/stats-dashboard.js"
      provides: "De-boxed Usage + Brain Health render with hero/supporting/deltas/quiet tables/section idiom"
      contains: "makeSection"
    - path: "src/viz/css/styles.css"
      provides: "Section-header idiom + hero/supporting/delta/quiet-table CSS; retired .chart-card stats rules"
      contains: "stats-section-head"
    - path: "tests/viz-frontend-static.test.ts"
      provides: "Machine-checkable guards: chart-card retired on stats, no trend glyphs, hero present"
  key_links:
    - from: "renderUsageTab hero"
      to: ".stats-hero-value (~44px mono)"
      via: "createElement + class"
      pattern: "stats-hero"
    - from: "stats-dashboard section render"
      to: ".stats-section-head (10px uppercase muted)"
      via: "makeSection helper"
      pattern: "makeSection"
    - from: "measureChartWidth"
      to: "de-boxed container inner width"
      via: "container.clientWidth (chart-card padding subtraction removed)"
      pattern: "clientWidth"
---

<objective>
Close GAP-4: re-skin the Usage and Brain Health tabs from "uniform bordered
boxes" (AI-slop diagnosis) into the Phase-59 recessive-chrome idiom — one hero
figure per tab, quiet supporting rows, deltas integrated into figures, quiet
borderless tables, section-header structure instead of cards, and the GAP-4(d)
mark-spec call-site rules (bar cap ≤24px, legends only on true multi-series).
Consumes the 60-10 primitives.

Purpose: The founder verdict (2026-07-14) is that the current tabs "read as AI
slop" — everything an identical box, five competing 28px numbers, text trend
glyphs, pointless single-series legends. This plan delivers the locked six-point
design contract (a)–(f) exactly, presentation-only (no /stats/* changes).

Output: Redesigned stats-dashboard.js render paths + de-boxed CSS + honest test
guards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-settings-stats-depth/60-HUMAN-UAT.md
@.planning/phases/60-settings-stats-depth/60-UI-SPEC.md
@.planning/phases/60-settings-stats-depth/60-REVIEW.md
@.planning/phases/60-settings-stats-depth/60-10-PLAN.md

@src/viz/modules/stats-dashboard.js
@src/viz/modules/charts.js

<interfaces>
<!-- charts.js (as updated by 60-10) — mark-spec builders this plan calls: -->
line(points, { color, strokeWidth, dashed, areaFill, baselineY })   // 2px round; areaFill → 10% wash
bar(bars, { color, fillAlpha })                                     // strokeless rounded-top path
axis(opts)                                                          // recessive hairline grid — call on EVERY chart
legend(series, opts)                                               // call ONLY for true multi-series charts
directLabel(point, text, { dx, dy, anchor, color })               // selective endpoint/extreme label
attachHover(svg, points, opts)                                     // CR-01/CR-04 — keep wired on line charts

<!-- Design tokens (var(--<key>); styles.css is under the D-14-A raw-literal
     lock — every new color rule MUST use a token, add one to constants.js
     HUD_CSS_TOKENS if a needed shade has no token yet): -->
--text-bright: #e7dfec        --text-recede: #9a8aa4       --text-body-mauve: #a99db3
--text-stat: #7fae93 (down-usage POSITIVE green)           --text-mauve-rest: #8b7090 (up = neutral)
--divider-faint: rgba(140,150,165,0.11)                    --section-divider-faint: rgba(140,150,165,0.08)
--error-text: #f08080 (stale/none sleep-pass)              JetBrains Mono HUD (numeric readouts)

<!-- Server payload shape consumed (UNCHANGED this round — presentation only):
     data.summary { today_tokens, week_tokens, month_tokens, avg_tokens_per_day,
       retail_usd_30d, today_vs_typical_pct, week_vs_typical_pct, trend_direction,
       trend_pct, heaviest_day{date,tokens} }
     data.buckets[], data.bucket_granularity, data.cost_event_deltas[],
     data.lever_deltas[]{label,date,before_avg,after_avg,pct_saved},
     data.by_feature[], data.by_model[]
     brain-health: node_growth{points,approximate}, kind_mix{}, reconsolidations_per_day[],
       tombstones_per_day[], judge_activity{fires,escalation_rate}, episodes{pending,consolidated},
       last_sleep_pass{status,ts,duration_ms} -->

<!-- RETAIL_LABEL is D-09 LOCKED verbatim — keep the exact string, only relocate
     it from a tile label to a muted footnote line:
     'API-retail equivalent (subscription-billed: $0 marginal)' -->

<!-- Preserve intact (do NOT regress): GAP-1 measureChartWidth + resize cache
     (lastUsageData/lastHealthData + handleResize); CR-01 attachHover viewBox
     conversion; CR-04 marker tooltip precedence (.chart-marker skip + hit rect);
     WR-03 buildMarkers out-of-window skip; the textContent-only discipline. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-box the Usage tab — hero + supporting row + integrated deltas + quiet tables (GAP-4 a,b,c,e)</name>
  <files>src/viz/modules/stats-dashboard.js, src/viz/css/styles.css, src/viz/modules/constants.js</files>
  <read_first>
    - src/viz/modules/stats-dashboard.js lines 257-670 (renderUsageTab, renderStatTileRow, renderFraming, TREND_ARROW, renderLeversCard, renderBurnChart, renderUsageBreakdown, makeCard)
    - src/viz/modules/stats-dashboard.js lines 56-79 (chart geometry consts + measureChartWidth — CHART_CARD_PADDING/BORDER subtraction becomes wrong once de-boxed)
    - src/viz/css/styles.css lines 1580-1722 (#stats-body, .chart-card*, .stats-headline*, .stats-tile-row, .stats-framing, .stats-levers*, .stats-breakdown*)
    - src/viz/modules/charts.js (line/bar/axis/legend/directLabel signatures per 60-10)
  </read_first>
  <action>
    Introduce a `makeSection(titleText)` helper replacing `makeCard()`: returns
    a `div.stats-section` whose first child is a `div.stats-section-head`
    (10px uppercase letter-spaced muted section header, textContent-set) — NO
    border, NO background, NO card chrome. Every Usage element now hangs off
    section headers + hairline dividers on the flat #stats-view surface (GAP-4a).
    Retire `.chart-card` usage in this file entirely (grep must reach 0).

    HERO + SUPPORTING (GAP-4b): replace `renderStatTileRow`'s five equal 28px
    tiles with (1) one hero figure — `div.stats-hero` holding `.stats-hero-value`
    (today's tokens, ~40-48px JetBrains Mono, --text-bright) over a 10px
    uppercase `.stats-hero-label` "today"; (2) a quiet `.stats-supporting-row`
    of four items (this week / last 30 days / avg tokens/day / retail-$), each
    a `.stats-supporting-item` = 10px uppercase `.stats-supporting-label` over a
    ~16-18px mono `.stats-supporting-value`, hairline-separated, NO boxes.
    Retail-$ value uses `.toFixed(2)` → `$X.XX` (supersedes UI-SPEC 4-decimal;
    note the supersession in a code comment). Emit the D-09 verbatim RETAIL_LABEL
    as a muted `.stats-footnote` line under the supporting row (per GAP-4b:
    footnote, not a tile label). Keep fmtTokens for the token values.

    INTEGRATE FRAMING INTO DELTAS (GAP-4c): DELETE the `TREND_ARROW` map and the
    `renderFraming` prose block. Instead attach signed deltas to the figures:
    render `summary.trend_pct` as plain signed text on/next to the hero (e.g.
    "+18%" / "−12%" using a real minus glyph, via textContent) inside a
    `.stats-delta` span; color = direction: `trend_direction === 'down'` →
    var(--text-stat) (down-usage is the POSITIVE/green signal), else
    var(--text-mauve-rest) (up/flat = neutral mauve). Keep the "vs prior 7d"
    qualifier as adjacent muted text. Keep `today_vs_typical_pct` /
    `week_vs_typical_pct` as compact signed/percent context on the relevant
    supporting items (skip a line when its source is null — honesty). Keep the
    heaviest-day as ONE muted `.stats-footnote`-style line under the supporting
    row (GAP-4c). Do NOT reintroduce any ▲▼→ glyph.

    QUIET TABLES (GAP-4e): restyle levers + breakdown as borderless quiet
    tables — 10px uppercase muted column headers, hairline row separators, no
    outer border/box. In `renderLeversCard` (rename to a section), make
    `pct_saved` the one emphasized cell per row: mono, green (var(--text-stat))
    when positive. Keep the honest ">= 0 → '% saved' / < 0 → '% more' / null →
    '—'" labeling and the server-computed pct (no client math). `renderUsageBreakdown`
    stays a quiet swatch table under a section header (no card).

    BURN CHART (GAP-4d call sites): keep `renderBurnChart` but (1) drop its
    single-series `legend([...])` call (the section header "Daily/Weekly token
    burn" names the series); (2) pass `areaFill` (NEUTRAL_SERIES_RAMP[0], 10%
    wash) + `baselineY` to `line()`; (3) add ONE `directLabel` at the final
    (endpoint) data point showing its fmtTokens value — selective, never
    per-point. Keep axis() on both axes. Keep cost-event markers + appendMarkers
    hover + attachHover (CR-01/CR-04 intact). Keep bar caps ≤24px anywhere bars
    are used on this tab (breakdown is a table, so N/A here, but if any bar
    remains cap barWidth at Math.min(24, ...)).

    UPDATE measureChartWidth (GAP-1 preserved, geometry corrected): the charts
    now sit directly under section divs on the flat surface, not inside a padded
    bordered card — remove the `CHART_CARD_PADDING*2 + CHART_CARD_BORDER*2`
    subtraction and measure the real inner width of the de-boxed container
    (container.clientWidth minus any actual section horizontal padding you set
    in CSS, else 0). Keep the CHART_W_MIN floor and the resize-cache re-render
    path unchanged.

    CSS: in styles.css, replace the `.chart-card*` + old `.stats-headline*` /
    `.stats-tile-row` / `.stats-framing` / `.stats-levers*` / `.stats-breakdown*`
    rules with the de-boxed set: `.stats-section` (margin rhythm, no border),
    `.stats-section-head` (10px, 600, uppercase, letter-spacing 0.06em,
    var(--text-recede), a hairline `border-bottom: 1px solid var(--divider-faint)`
    + bottom padding), `.stats-hero` / `.stats-hero-value` (JetBrains Mono ~44px,
    var(--text-bright)) / `.stats-hero-label`, `.stats-supporting-row` (flex,
    hairline separators between items), `.stats-supporting-item/-label/-value`,
    `.stats-delta` (mono, inline), `.stats-footnote` (10-11px, var(--text-recede)),
    and quiet-table rules (10px uppercase headers, `border-bottom: 1px solid
    var(--divider-faint)` row separators, no outer border; `.stats-levers-pct`
    emphasis mono/green). Every color via var() token — add any missing shade to
    constants.js HUD_CSS_TOKENS (D-14-A lock). No backdrop-filter on any
    #stats-view descendant (D-14-C). No amber.
  </action>
  <acceptance_criteria>
    - `grep -c "chart-card" src/viz/modules/stats-dashboard.js` == 0
    - `grep -c "TREND_ARROW" src/viz/modules/stats-dashboard.js` == 0
    - `grep -Ec "▲|▼|→" src/viz/modules/stats-dashboard.js` == 0
    - `grep -c "makeSection" src/viz/modules/stats-dashboard.js` >= 1 and `grep -c "makeCard" src/viz/modules/stats-dashboard.js` == 0
    - `grep -c "stats-hero-value" src/viz/modules/stats-dashboard.js` >= 1
    - `grep -c "toFixed(2)" src/viz/modules/stats-dashboard.js` >= 1 (retail-$ 2-decimal) and the D-09 RETAIL_LABEL string is still present verbatim
    - Burn chart render fn contains zero `legend(` calls (`awk '/function renderBurnChart/,/^  }/' | grep -c 'legend('` == 0) but retains `axis(` and `directLabel(`
    - `grep -c "stats-section-head" src/viz/css/styles.css` >= 1; `grep -c "\.chart-card" src/viz/css/styles.css` == 0 (or a documented single plot-wrapper class — pick zero)
    - `grep -Ei "ffb866|accent-amber" src/viz/modules/stats-dashboard.js` returns nothing; `grep -c "innerHTML" src/viz/modules/stats-dashboard.js` == 0
    - No new raw color literal in styles.css (D-14-A lock passes)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/viz-activity-palette-invariants.test.ts tests/viz-charts-geometry.test.ts</automated>
  </verify>
  <done>Usage tab is de-boxed with one hero + quiet supporting row, signed-text deltas (no glyphs), 2-decimal retail-$ footnote, quiet borderless tables, section-idiom structure, legend dropped on the burn chart, responsive re-render + hover intact.</done>
</task>

<task type="auto">
  <name>Task 2: De-box the Brain Health tab — section idiom, small-multiples, one-line sleep readout (GAP-4 a,d,f)</name>
  <files>src/viz/modules/stats-dashboard.js, src/viz/css/styles.css</files>
  <read_first>
    - src/viz/modules/stats-dashboard.js lines 672-1055 (all renderHealth* functions, buildLineChartSvg, buildBarChartSvg, renderLastSleepPassTile, renderHealthTab)
    - src/viz/modules/stats-dashboard.js single-series legend call sites: lines 762 (node growth), 831 (recon), 867 (tombstone) — to be removed
    - src/viz/modules/stats-dashboard.js multi-series legend sites: lines 801 (kind mix), 916 (judge), 950 (episodes) — to be KEPT
    - src/viz/css/styles.css (new de-box classes from Task 1 — reuse makeSection/section-head)
  </read_first>
  <action>
    Convert every Brain Health render function from `makeCard` to the shared
    `makeSection` helper (GAP-4a, GAP-4f) so the six metric groups become
    sections under the 10px uppercase header idiom with hairline dividers, no
    boxes. Give the six charts a CONSISTENT height so they read as small
    multiples (GAP-4f): pick one shared height constant for the health line/bar
    charts (e.g. reuse BAR_H for all six, or a single new HEALTH_CHART_H) instead
    of node-growth using the taller BURN_H — remove the `.chart-card-primary`
    special-casing (it was inert anyway, IN-02). 

    LEGENDS (GAP-4d): remove the single-series `legend(...)` calls in
    renderNodeGrowthChart, renderReconChart, renderTombstoneChart (the section
    header names the series). KEEP the multi-series `legend(...)` calls in
    renderKindMixChart, renderJudgeActivityChart, renderEpisodesChart. Keep
    `axis(` on every chart. Keep the D-13 node-growth approximation caption and
    the EVENT_COUNT_CAPTION recon/tombstone captions (honesty — do not drop).

    MARK SPECS (GAP-4d): line charts already flow through the 60-10 `line()`
    (2px round). For the bar charts (kind mix, judge, episodes) cap bar
    thickness at ≤24px — in `buildBarChartSvg` change `barWidth = Math.min(56,
    slot * 0.5)` to `Math.min(24, slot * 0.5)`, and ensure adjacent bars keep a
    ≥2px surface gap (guaranteed since barWidth < slot; verify visually). Bars
    are strokeless rounded-top via the 60-10 `bar()` builder — no per-call
    change needed beyond the width cap. Keep attachHover on the line charts
    (recon/tombstone/node-growth) for D-07 nearest-point hover.

    LAST SLEEP-PASS (GAP-4f): rewrite `renderLastSleepPassTile` from a
    `chart-card stats-headline-tile` into a one-line muted readout — a single
    `.stats-sleep-readout` row: a 10px uppercase label ("last sleep pass") + a
    mono value (relative time + duration, or "no sleep pass has run yet") +
    honest status color (var(--text-stat) fresh / var(--error-text) stale|none).
    Keep relativeTimeFromMs/formatDurationMs and the SLEEP_PASS_STALE_MS
    freshness logic and the never-fabricate-success rule (status 'none'/'unknown'
    only from server). No card, no 28px number.

    CSS: add `.stats-sleep-readout` (flex, one line, 10px uppercase label +
    mono value) and any small-multiples spacing rule to styles.css using var()
    tokens only. Reuse the Task 1 section/hero classes. No amber, no
    backdrop-filter under #stats-view.
  </action>
  <acceptance_criteria>
    - Node-growth / recon / tombstone render fns contain zero `legend(` calls; kind-mix / judge / episodes each retain exactly one `legend(` call (`grep -c "legend(" src/viz/modules/stats-dashboard.js` drops from 9 to 5)
    - `grep -c "axis(" src/viz/modules/stats-dashboard.js` stays >= 12 (axis on every chart — call count unchanged)
    - `grep -c "chart-card" src/viz/modules/stats-dashboard.js` == 0 (carried from Task 1, still 0)
    - `grep -c "makeCard" src/viz/modules/stats-dashboard.js` == 0
    - `grep -c "Math.min(24" src/viz/modules/stats-dashboard.js` >= 1 (bar cap ≤24px)
    - `grep -c "stats-sleep-readout" src/viz/modules/stats-dashboard.js` >= 1 and `grep -c "stats-sleep-readout" src/viz/css/styles.css` >= 1
    - D-13 caption + EVENT_COUNT_CAPTION still present (`grep -c "NODE_GROWTH_CAPTION\|EVENT_COUNT_CAPTION" src/viz/modules/stats-dashboard.js` >= 2)
    - `grep -Ei "ffb866|accent-amber" src/viz/modules/stats-dashboard.js` returns nothing
    - `npx tsc --noEmit` clean; D-14-A/-C locks green
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/viz-activity-palette-invariants.test.ts tests/viz-charts-geometry.test.ts</automated>
  </verify>
  <done>Brain Health is de-boxed into consistent-height section-idiom small multiples; single-series legends dropped, multi-series kept; bars capped ≤24px strokeless rounded-top; sleep-pass is a one-line honest readout; captions and honesty rules preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the redesign with honest static guards + document invalidated legend counts</name>
  <files>tests/viz-frontend-static.test.ts</files>
  <read_first>
    - tests/viz-frontend-static.test.ts lines 40-75 (module source reads), 155-300 (css/source grep guards)
    - src/viz/modules/stats-dashboard.js (final redesigned source)
    - src/viz/css/styles.css (final de-boxed rules)
  </read_first>
  <action>
    Add a `describe('Phase 60 GAP-4 de-box redesign')` block to
    tests/viz-frontend-static.test.ts asserting the machine-checkable invariants
    of the redesign against the read stats-dashboard.js + styles.css sources:
    (1) stats-dashboard.js contains no `chart-card` and no `makeCard`;
    (2) stats-dashboard.js contains no `TREND_ARROW` and none of the glyphs
    ▲ ▼ → (the single-series-legend removal + glyph removal are the deliberately
    invalidated earlier acceptance criteria — add an inline comment explaining
    single-series charts drop the legend per the dataviz rule "a single series
    needs no legend box", so the earlier `legend( >= 9` expectation is
    intentionally superseded to `== 5`);
    (3) stats-dashboard.js source `legend(` count == 5 — that is 3 real
    multi-series legend calls (kind mix, judge, episodes) plus the 2 dead
    "legend()" mentions in the two axis/legend-discipline code comments; the 4
    single-series calls (burn, node growth, reconsolidations, tombstones) are
    dropped. Assert the exact post-redesign count with a comment naming the 3
    charts that keep legends;
    (4) styles.css contains `stats-section-head` and `stats-hero-value` and
    `stats-sleep-readout`, and zero `.chart-card` rule;
    (5) styles.css global scrollbar guard: exactly one `scrollbar-width: thin`
    and the `*::-webkit-scrollbar` set present (cross-checks 60-10 GAP-3).
    Do NOT weaken existing guards; only add. If any pre-existing assertion in
    this file references the old boxed layout or per-container scrollbar, update
    it to the new reality with a comment stating why.
  </action>
  <acceptance_criteria>
    - New describe block present; `npx vitest run tests/viz-frontend-static.test.ts` green
    - The `legend( == 5` assertion carries an inline comment naming the four dropped single-series charts and the rule ("a single series needs no legend box")
    - Guards for chart-card==0, no trend glyphs, stats-hero-value/stats-section-head/stats-sleep-readout present, single global scrollbar all green
    - Full suite `npx vitest run` green (no regressions in viz-charts-geometry, viz-activity-palette-invariants, viz-settings-panel, viz-stats-routes)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Static guards lock the de-box redesign; the superseded legend-count expectation is updated to == 5 with a documented rationale; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /stats/* JSON → DOM | every token count, date, cost, %-saved reaches the DOM via textContent (incl. SVG `<text>`) — the redesign adds new render paths that must keep this discipline |
| design-invariant locks | D-14-A raw-color-literal ban, D-08 amber ban, D-14-C backdrop-filter allow-list, GAP-1 responsive re-render, honesty rules (no fabricated sleep status, WR-03/WR-04 marker/bucket honesty) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-11-01 | Tampering | new hero/supporting/delta/table/section render nodes | mitigate | textContent-only for every server value; no innerHTML; static guard asserts zero innerHTML in stats-dashboard.js |
| T-60-11-02 | Tampering | new de-box CSS rules | mitigate | color via var() tokens only; D-14-A lock re-run in each task verify; no backdrop-filter under #stats-view (D-14-C) |
| T-60-11-03 | Spoofing/Repudiation | last-sleep-pass readout | mitigate | render only server-sent 'none'/'unknown' status; never synthesize a healthy word (honesty rule preserved) |
| T-60-11-04 | Information Disclosure | cost-event marker deltas | mitigate | WR-03 out-of-window skip + WR-04 zero-fill honesty preserved (no data-layer change; buildMarkers guard untouched) |
| T-60-11-05 | Tampering | design regression (amber/glyph creep) | mitigate | grep guards: no amber literal, no ▲▼→, no TREND_ARROW |
| T-60-11-SC | Tampering | npm/pip/cargo installs | mitigate | zero new dependencies — no install tasks |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx vitest run` full suite green (viz-frontend-static, viz-charts-geometry, viz-activity-palette-invariants, viz-settings-panel, viz-stats-routes)
- All Task 1/2/3 grep gates pass
- Manual (founder re-walk, out of band): Usage tab leads with one hero + quiet supporting row; deltas are signed text not glyphs; both tabs read as flat section-structured surfaces; charts resize correctly; marker + nearest-point hover work
</verification>

<success_criteria>
- Locked six-point contract (a)–(f) each delivered: de-box (a), hero+supporting (b), integrated deltas (c), mark specs (d), quiet tables (e), Brain Health sections + one-line sleep readout (f)
- No .chart-card / makeCard / TREND_ARROW / trend glyphs remain in stats-dashboard.js
- Single-series legends dropped (count 9→5), multi-series kept; axis on every chart
- GAP-1 responsive re-render + CR-01/CR-04 hover behavior intact; all honesty rules preserved
- No new raw color literal, no amber, no backdrop-filter under #stats-view
</success_criteria>

<output>
Create `.planning/phases/60-settings-stats-depth/60-11-SUMMARY.md` when done
</output>
