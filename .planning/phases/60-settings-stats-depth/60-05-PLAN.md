---
phase: 60-settings-stats-depth
plan: 05
type: execute
wave: 3
depends_on: ["60-03"]
files_modified:
  - src/viz/modules/settings.js
  - src/viz/modules/palette.js
autonomous: true
requirements: [D-04]
must_haves:
  truths:
    - "The settings panel's 30d/all-time usage readout block is replaced by a single 'View usage stats →' link that opens the stats takeover on the Usage tab"
    - "The per-toggle inline usage lines (appendUsageLines) remain — only the standalone readout block is removed"
    - "A ⌘K palette command 'Open stats' opens the stats takeover"
  artifacts:
    - path: "src/viz/modules/settings.js"
      provides: "D-04 link replacement + ctx threaded into render()"
      contains: "View usage stats"
    - path: "src/viz/modules/palette.js"
      provides: "open-stats command entry"
      contains: "open-stats"
  key_links:
    - from: "src/viz/modules/settings.js link"
      to: "ctx.openStatsDashboard"
      via: "click handler"
      pattern: "openStatsDashboard"
    - from: "src/viz/modules/palette.js open-stats command"
      to: "ctx.openStatsDashboard"
      via: "run(c)"
      pattern: "openStatsDashboard"
---

<objective>
Wire the two entry points into the stats takeover (D-04 settings link + the ⌘K
palette command), establishing a single source of truth for usage numbers: the
settings panel stops duplicating the 30d/all-time readout and instead links into
the dashboard. Small, isolated, and parallel with the Brain-Health plan.

Purpose: Make the dashboard reachable and remove the duplicate number block.
Output: settings.js link edit, palette.js command append.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-settings-stats-depth/60-CONTEXT.md
@.planning/phases/60-settings-stats-depth/60-PATTERNS.md
@.planning/phases/60-settings-stats-depth/60-UI-SPEC.md

<interfaces>
<!-- Extracted from the live codebase. -->
settings.js: initSettings(_ctx) takes an underscore-prefixed unused ctx (line 34);
  render() calls appendFullUsageReadout(bodyEl, usageData) at ~line 255-256;
  appendFullUsageReadout body is lines 393-473 (the 30d/all-time readout to remove);
  appendUsageLines (lines 364-384, per-toggle inline usage) STAYS (D-04 keeps it);
  fmtTokens (478-482) STAYS if still referenced.
palette.js: commands array (lines 91-104); comment at line 90 anticipates the
  Phase-60 append; entries are { id, label, run(ctx), visibleIn? }; existing
  open-corpus uses visibleIn:['brain','reader']; currentView() (111-115) returns
  'corpus'|'reader'|'brain'.
ctx.openStatsDashboard(tab) exists after 60-03 (app.js initStatsDashboard(ctx)).
UI-SPEC copy: settings CTA verbatim `View usage stats →`; palette label `Open stats`;
  palette visibleIn:['brain','reader','corpus'], opens on Usage tab.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: settings.js D-04 link replacement</name>
  <files>src/viz/modules/settings.js</files>
  <read_first>
    - src/viz/modules/settings.js (34 initSettings signature; 250-260 render() call site; 360-482 appendUsageLines/appendFullUsageReadout/fmtTokens) — see exactly which block to remove vs keep
    - .planning/phases/60-settings-stats-depth/60-PATTERNS.md (settings.js D-04 edit section) — the precise remove-vs-keep guidance
    - .planning/phases/60-settings-stats-depth/60-UI-SPEC.md (Copywriting Contract) — verbatim `View usage stats →`
  </read_first>
  <action>
    Rename `initSettings(_ctx)` to `initSettings(ctx)` and thread `ctx` so `render()`
    can reach `ctx.openStatsDashboard`. At the render call site (~255-256) replace
    the `appendFullUsageReadout(bodyEl, usageData)` call with a single link row: an
    `<a>` element, class `settings-usage-link` (styled per Phase-59 tokens), text set
    via `.textContent = 'View usage stats →'` (verbatim, T-44-19), whose click handler
    calls `ev.preventDefault()` then `ctx.openStatsDashboard()` guarded by a
    typeof-function check. Delete the now-dead `appendFullUsageReadout` function body
    (lines 393-473). KEEP `appendUsageLines` (per-toggle inline usage, D-04 explicitly
    retains it) and keep `fmtTokens` if still referenced by appendUsageLines. Do not
    remove any cost-control toggle.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-settings-panel.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "View usage stats" src/viz/modules/settings.js` matches verbatim (with the → arrow)
    - `grep -n "appendFullUsageReadout" src/viz/modules/settings.js` returns no match (block removed)
    - `grep -n "appendUsageLines" src/viz/modules/settings.js` still matches (per-toggle lines kept)
    - `grep -n "openStatsDashboard" src/viz/modules/settings.js` matches; initSettings signature is `initSettings(ctx)` not `_ctx`
    - viz-settings-panel test passes; tsc clean
  </acceptance_criteria>
  <done>Settings panel links into the dashboard; the duplicate 30d/all-time readout is gone; per-toggle lines and toggles remain.</done>
</task>

<task type="auto">
  <name>Task 2: palette.js Open stats command</name>
  <files>src/viz/modules/palette.js</files>
  <read_first>
    - src/viz/modules/palette.js (80-130: commands array + the line-90 anticipation comment + currentView) — append at the anticipated spot
    - .planning/phases/60-settings-stats-depth/60-UI-SPEC.md (Copywriting: `Open stats`, visibleIn all three views)
  </read_first>
  <action>
    Append a command to the `commands` array (at the spot the line-90 comment
    anticipates): `{ id:'open-stats', label:'Open stats', visibleIn:['brain','reader','corpus'],
    run: c => { if (c.openStatsDashboard) c.openStatsDashboard(); } }`. Optionally add
    a `'stats'` branch to currentView() gated on ctx.isStatsDashboardOpen so the
    command self-hides while the dashboard is open (Claude's Discretion — only if it
    reads cleanly alongside the existing corpus/reader/brain branches). Label text
    verbatim `Open stats`.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-hud-palette.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "open-stats" src/viz/modules/palette.js` matches
    - `grep -n "Open stats" src/viz/modules/palette.js` matches verbatim
    - `grep -n "openStatsDashboard" src/viz/modules/palette.js` matches (command runs the ctx method)
    - viz-hud-palette test passes; tsc clean
  </acceptance_criteria>
  <done>The ⌘K palette exposes an 'Open stats' command that opens the dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings link / palette command → ctx | pure client navigation, no server input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04 | Tampering (XSS) | link/command labels | mitigate | static string labels via .textContent; no server data rendered here |
| T-60-09 | Integrity | single source of truth | mitigate | duplicate 30d/all-time readout removed so numbers can't drift between panel and dashboard (D-04) |
| T-60-SC | Tampering | package installs | accept | zero new deps — no install task |
</threat_model>

<verification>
- `npx vitest run tests/viz-settings-panel.test.ts tests/viz-hud-palette.test.ts` green
- `npx tsc --noEmit` clean
- `grep -c "appendFullUsageReadout" src/viz/modules/settings.js` == 0
</verification>

<success_criteria>
Both entry points open the stats takeover, and the settings panel no longer
duplicates the usage numbers (one source of truth, D-04).
</success_criteria>

<output>
Create `.planning/phases/60-settings-stats-depth/60-05-SUMMARY.md` when done
</output>
