---
phase: 60-settings-stats-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/viz/server.ts
  - tests/viz-stats-routes.test.ts
autonomous: true
requirements: [D-09, D-10, D-11, D-12, D-13, D-14]
must_haves:
  truths:
    - "GET /stats/usage returns daily-bucketed token burn, per-feature split, per-model split, retail-$ sum, and per-cost-event before/after avg-daily-burn deltas for the requested window"
    - "GET /stats/brain-health returns node-growth series, kind mix, reconsolidations/day, tombstones/day, judge fires + Sonnet-escalation rate, episodes pending vs consolidated, and a derived last-sleep-pass timestamp/duration/status"
    - "Both routes return zeroed/empty aggregates (not an error) on an empty DB"
    - "Both routes reject non-loopback Host headers with 403 and never leak SQL/stack text on error"
    - "window param accepts only 7d/30d/90d/all; all switches bucketing to weekly"
    - "cost-event markers are source-parsed from constants.js server-side (single-source, D-11) — never re-declared as a server literal"
  artifacts:
    - path: "src/viz/server.ts"
      provides: "GET /stats/usage and GET /stats/brain-health read-only routes + their startup-compiled prepared statements + startup source-parse of COST_EVENTS"
      contains: "/stats/usage"
    - path: "tests/viz-stats-routes.test.ts"
      provides: "seeded-DB response-shape + guard + empty-DB tests for both routes"
      contains: "stats/brain-health"
  key_links:
    - from: "src/viz/server.ts /stats/* handlers"
      to: "read-only db handle prepared statements"
      via: "db.prepare(...) compiled once at startup"
      pattern: "db\\.prepare"
    - from: "src/viz/server.ts /stats/usage handler"
      to: "src/viz/modules/constants.js COST_EVENTS"
      via: "startup source-parse (parseSchedulerScalars-style), not a re-declared literal"
      pattern: "COST_EVENTS"
---

<objective>
Add two read-only HTTP routes to the viz server that aggregate the existing
`token_usage_ledger`, `consolidation_event`, `node`, and `episode` tables into
the JSON contracts the Phase-60 dashboards render — with zero schema change, no
new write path, and no LLM call. This is the entire backend of the phase; every
frontend plan fetches from these two routes.

Purpose: One source of truth for every dashboard number, computed live from the
ledger/event log instead of hand-queried (D-09/D-10/D-13/D-14).
Output: `GET /stats/usage?window=` and `GET /stats/brain-health` + a test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-settings-stats-depth/60-CONTEXT.md
@.planning/phases/60-settings-stats-depth/60-PATTERNS.md

<interfaces>
<!-- Existing /usage precedent the new routes mirror. Extracted from src/viz/server.ts. -->
Prepared-statement block (server.ts:415-441): stmtUsage30d / stmtUsageAllTime —
  SELECT feature_tag, SUM(input_tokens) AS input_tokens, SUM(output_tokens),
  SUM(cache_write_tokens), SUM(cache_read_tokens), SUM(total_cost_usd)
  FROM token_usage_ledger [WHERE ts > ?] GROUP BY feature_tag

Handler precedent (server.ts:1267-1311): `if (url === '/usage') { method-guard→405;
  try { rows = stmt.all(cutoff); res 200 JSON } catch { res 500 'internal error' } }`
  Empty ledger → zeroed aggregate object, never an error.

Query-param idiom (server.ts:809/887/933/1004):
  new URLSearchParams(req.url?.split('?')[1] ?? '')

DNS-rebinding guard (server.ts:656-664): top of http.createServer callback,
  rejects any Host ≠ 127.0.0.1:${port} / localhost:${port} with 403 — already
  covers every route, no per-route duplication.

Source-parse-from-constants.js precedent (server.ts:55-98): parseSchedulerScalars()
  reads constants.js's source text once at startVizServer init and regexes named
  constants out of it (fail-fast if absent — its comment cites the real SPONT_HOP_TOPN
  6-vs-3 drift bug). The COST_EVENTS marker load MUST mirror this exact mechanism so
  the marker list has ONE source of truth (D-11) and cannot drift from the frontend.

Schema columns:
  token_usage_ledger(ts INTEGER ms, feature_tag TEXT, model TEXT,
    input_tokens, output_tokens, cache_write_tokens, cache_read_tokens, total_cost_usd)
  consolidation_event(ts INTEGER ms, event_type TEXT, node_id, episode_id, ...)
    node-birth event_types: schema_emitted (+ node creations logged via node_id on
    confirm/extend/contradict_append_new). reconsolidation event_types:
    contradict_reconcile / contradict_force_destabilize / contradict_oscillation.
    tombstone: nodes with tombstoned=1 OR falsification events (schema_falsified).
  node(type CHECK IN 'entity','fact','schema','doc','insight'; tombstoned) — NO created_at.
  episode(consolidated INTEGER 0/1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /stats/usage route + prepared statements</name>
  <files>src/viz/server.ts, tests/viz-stats-routes.test.ts</files>
  <read_first>
    - src/viz/server.ts (lines 1-25 route-list header; 55-98 parseSchedulerScalars source-parse precedent — the exact fail-fast constants.js source-parse the COST_EVENTS load mirrors; 405-445 usage prepared stmts; 656-664 Host guard; 800-1010 query-param idiom sites; 1260-1315 /usage handler) — replicate the exact read-only/compile-once/method-guard/500-'internal error' shape and the parse-from-constants single-source mechanism
    - src/viz/modules/constants.js — the COST_EVENTS array literal ({date,label} objects, D-11) that the server source-parses; confirm its exact declaration shape for the regex
    - src/db/schema.ts (178-192 token_usage_ledger) — column names and that ts is Date.now() ms
    - tests/viz-settings-routes.test.ts — the startVizServer + http.request + tmp-DB seeding harness this new test file copies verbatim
    - .planning/phases/60-settings-stats-depth/60-CONTEXT.md (D-09/D-10/D-11/D-12) and 60-UI-SPEC.md (Chart Series / cost-event markers) for the response fields the frontend needs
  </read_first>
  <behavior>
    - window=30d (default): response has daily buckets (one per calendar day in window), each bucket carrying total tokens (input+output) and summed total_cost_usd
    - Response includes per-feature totals keyed by feature_tag (extract/judge/corpus_gen/schema_abstract) and per-model totals keyed by the ledger `model` column
    - Response includes an all-time-independent retail-$ figure = SUM(total_cost_usd) over the window (D-09)
    - Response includes, for each COST_EVENTS marker (source-parsed from constants.js server-side at startup, D-11), avg daily burn before vs after that marker's date computed live from the buckets (D-10); each delta carries the marker's `label` from the same parsed source
    - window=all → weekly buckets instead of daily (D-12); window in {7d,30d,90d} → daily
    - window param not in the allow-list {7d,30d,90d,all} → treated as 30d default (no error, no injection)
    - empty ledger → buckets:[], zeroed per-feature/per-model/retail totals, 200 not 500
    - non-loopback Host → 403 (inherited guard); DELETE/POST → 405
  </behavior>
  <action>
    Compile new prepared statements in the same startup block as stmtUsage30d
    (server.ts ~415-441) on the same read-only `db` handle: (a) a daily-bucket burn
    statement grouping by `strftime('%Y-%m-%d', ts/1000, 'unixepoch')` (or integer
    day division on ts ms) with SUM over input+output tokens and SUM(total_cost_usd)
    WHERE ts > ? ; (b) a weekly-bucket variant grouping by `strftime('%Y-%W', ...)`
    for window=all ; (c) a per-model statement GROUP BY model WHERE ts > ? ;
    (d) reuse the per-feature GROUP BY feature_tag shape from stmtUsage30d.
    Source-parse the COST_EVENTS marker list ({date,label} objects) out of
    constants.js's source text at startVizServer init — mirror parseSchedulerScalars()
    (server.ts:55-98) EXACTLY: read the constants.js file once at startup, regex the
    COST_EVENTS array literal into {date,label} pairs, fail-fast if the array is absent.
    Do NOT re-declare COST_EVENTS as a server-side literal (that would fork the source
    and re-introduce the client/server mirror-drift class the precedent exists to kill —
    D-11 single-source intent). This gives the handler both the marker dates AND their
    labels from one authoritative source.
    Add the route handler `if (url === '/stats/usage')` immediately after the
    existing `/usage` block (server.ts ~1311, before POST /doc/generate). Parse
    `window` via new URLSearchParams(req.url?.split('?')[1] ?? ''); map
    7d/30d/90d to Date.now()-N*86_400_000 cutoffs and pick daily-vs-weekly bucket
    statement; validate window against the literal set {'7d','30d','90d','all'} and
    fall back to '30d' on anything else (never interpolate the raw param into SQL).
    Compute per-marker before/after avg-daily-burn deltas in JS over the returned
    buckets, tagging each delta with its parsed `label`. Response shape:
    { window, bucket_granularity, buckets:[{date,tokens,cost_usd}],
    by_feature:[...], by_model:[...], retail_usd, cost_event_deltas:[{date,label,before_avg,after_avg}] }.
    Method guard → 405 'method not allowed'; try/catch → 500 'internal error'
    (T-44-17, never leak SQL/stack). Add the one-line `GET /stats/usage?window= → {…}`
    summary to the route-list header comment (server.ts:4-21) with a `(Phase 60)` tag.
    Create tests/viz-stats-routes.test.ts by copying the startVizServer + http.request
    + tmp-DB harness from tests/viz-settings-routes.test.ts; seed token_usage_ledger
    rows across several days/features/models and assert bucket count, per-feature,
    per-model, retail_usd, and one before/after delta (with its label); add empty-DB
    (zeroed), 405 (POST), and 403 (non-loopback Host) cases.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-stats-routes.test.ts -t "stats/usage"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/viz-stats-routes.test.ts -t "stats/usage"` passes
    - `grep -n "'/stats/usage'" src/viz/server.ts` returns the handler match
    - COST_EVENTS is source-parsed from constants.js: `grep -n "constants.js" src/viz/server.ts` shows the COST_EVENTS read AND `grep -nE "COST_EVENTS\s*=\s*\[" src/viz/server.ts` returns NO re-declared array literal (single-source per D-11)
    - `cost_event_deltas` entries carry a `label` sourced from the parse (a seeded test asserts a non-empty label on at least one delta)
    - Seeded-DB test asserts a daily bucket for window=30d and a weekly bucket for window=all
    - Empty-DB test asserts 200 with zeroed retail_usd and empty buckets, not 500
    - POST /stats/usage returns 405; non-loopback Host returns 403
    - No raw `window` value appears inside any db.prepare string (grep shows only literal SQL); `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>GET /stats/usage returns the windowed burn/feature/model/retail/cost-delta contract with labels from a single-source constants.js parse, guards inherited, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /stats/brain-health route + derived last-sleep-pass</name>
  <files>src/viz/server.ts, tests/viz-stats-routes.test.ts</files>
  <read_first>
    - src/viz/server.ts (the /stats/usage block just added — mirror its structure)
    - src/db/schema.ts (23-38 episode.consolidated; 39-65 node type CHECK + tombstoned; 90-102 consolidation_event) — confirm no node.created_at exists, so growth derives from consolidation_event ts+node_id (D-13)
    - .planning/phases/60-settings-stats-depth/60-CONTEXT.md (D-13/D-14) and 60-PATTERNS.md "No Analog Found" row for last-sleep-pass — no persisted last-pass record exists; derive from MAX(ts) over consolidation_event / sleep-pass ledger rows and label status honestly as approximated/unknown (no fabricated success flag)
  </read_first>
  <behavior>
    - Response carries six metric groups (D-14): node_growth (cumulative count over time derived from consolidation_event node-birth rows), kind_mix (count per node.type over non-tombstoned nodes: entity/fact/schema/doc/insight), reconsolidations_per_day, tombstones_per_day, judge_activity {fires, escalation_rate} where escalation_rate = share of feature_tag='judge' ledger rows whose model is the Sonnet model, episodes {pending, consolidated}
    - last_sleep_pass: { ts, duration_ms, status } where ts = MAX(ts) over the latest sleep-pass batch (consolidation_event / sleep-pass ledger rows), duration_ms = span from that batch's first to last ts, status = 'unknown' honestly (no success signal is derivable) — never a fabricated 'ok'; when no rows exist, ts=null / status='none'
    - node_growth carries an `approximate: true` flag so the frontend can render the D-13 pruning-approximation disclosure
    - empty DB → every group zeroed/empty, last_sleep_pass.status='none', 200 not 500
    - method guard → 405; non-loopback Host → 403 (inherited)
  </behavior>
  <action>
    Compile the brain-health prepared statements in the same startup block:
    node-growth as a per-day COUNT of node-birth consolidation_event rows
    (event_type in the node-creation set with a non-null node_id) ordered by day,
    the frontend/handler accumulating a running total; kind_mix as
    SELECT type, COUNT(*) FROM node WHERE tombstoned=0 GROUP BY type;
    reconsolidations_per_day and tombstones_per_day as per-day COUNTs over the
    reconsolidation event_types (contradict_reconcile / contradict_force_destabilize /
    contradict_oscillation) and tombstone signals respectively; judge fires as a
    COUNT over token_usage_ledger WHERE feature_tag='judge' plus a COUNT where model
    = the Sonnet model id for the escalation share; episodes pending/consolidated as
    two COUNTs over episode GROUP BY consolidated. Derive last_sleep_pass from
    MAX(ts) and the surrounding batch span across consolidation_event; set status to
    the literal 'unknown' (or 'none' when empty) — do NOT invent a success/failure
    value (honest-labeling / no-inflated-metrics posture). Read the Sonnet model id
    from the same config source the ledger writer uses rather than hard-coding a
    string. Add `if (url === '/stats/brain-health')` after the /stats/usage block,
    same method-guard/try-catch/500-'internal error' shape, response
    { node_growth:{points:[...],approximate:true}, kind_mix, reconsolidations_per_day,
    tombstones_per_day, judge_activity, episodes, last_sleep_pass }. Add the route to
    the header comment list with a `(Phase 60)` tag. Extend tests/viz-stats-routes.test.ts
    with a seeded consolidation_event + node + episode + judge-ledger fixture asserting
    each group and the last_sleep_pass derivation, plus an empty-DB and a 403 case.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-stats-routes.test.ts -t "brain-health"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/viz-stats-routes.test.ts` passes (both routes)
    - `grep -n "'/stats/brain-health'" src/viz/server.ts` matches
    - Seeded test asserts node_growth.approximate===true, kind_mix per type, an escalation_rate between 0 and 1, and episodes.pending/consolidated
    - last_sleep_pass.status is the literal 'unknown' when rows exist and 'none' on empty DB — grep confirms no 'ok'/'success' literal is emitted for status
    - Empty-DB brain-health test asserts 200 zeroed, not 500; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>GET /stats/brain-health returns all six honest metric groups + derived last-sleep-pass; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→viz server | HTTP query params + Host header cross here (untrusted) |
| server→SQLite | read-only handle; aggregate queries only |
| constants.js source → server | marker list parsed from disk text at startup (trusted, own-repo source) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Tampering | `window` query param → SQL | mitigate | validate against literal set {7d,30d,90d,all}, fall back to 30d; only prepared statements with `?` binds — raw param never string-concatenated into SQL |
| T-60-02 | Information disclosure | error path | mitigate | catch → 500 'internal error' plain text; no SQL/stack in body (T-44-17) |
| T-60-03 | Spoofing | Host header | mitigate | inherited DNS-rebinding 403 guard (server.ts:656-664) covers new routes; test asserts 403 |
| T-60-04 | Repudiation/Integrity | metrics honesty | mitigate | last-sleep-pass status='unknown' not fabricated; node_growth flagged approximate (no-inflated-metrics) |
| T-60-09 | Integrity (mirror-drift) | COST_EVENTS marker list | mitigate | source-parsed from constants.js at startup (parseSchedulerScalars pattern), never re-declared server-side — one source of truth (D-11) |
| T-60-05 | Denial of service | unbounded aggregate | accept | read-only, window-bounded, prepared once at startup; single-tenant loopback-only surface |
| T-60-SC | Tampering | package installs | accept | net-zero new runtime deps this phase — no install task exists |
</threat_model>

<verification>
- `npx vitest run tests/viz-stats-routes.test.ts` green (both routes, guards, empty-DB)
- `npx tsc --noEmit` clean
- `grep -c "db.prepare" src/viz/server.ts` increased (new statements compiled once at startup, not per-request)
</verification>

<success_criteria>
Both routes return their documented JSON contract on seeded data, zeroed on empty
data, 403 on non-loopback Host, 405 on wrong method, never leaking SQL — all
LLM-free on the read-only handle, with cost-event markers source-parsed from the
single constants.js source (D-11).
</success_criteria>

<output>
Create `.planning/phases/60-settings-stats-depth/60-01-SUMMARY.md` when done
</output>
</content>
</invoke>
