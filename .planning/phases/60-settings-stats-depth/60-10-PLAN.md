---
phase: 60-settings-stats-depth
plan: 10
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/viz/css/styles.css
  - src/viz/modules/charts.js
  - tests/viz-charts-geometry.test.ts
autonomous: true
requirements: [GAP-3, GAP-4d]
must_haves:
  truths:
    - "Every scrollable surface (detail, reader, settings, index, stats, any future) shows the muted-mauve styled scrollbar from ONE global rule set — no per-container duplication"
    - "Line series render as 2px round-join strokes; an optional ~10% area wash can sit under a line"
    - "Bar series render with a 4px rounded data-end + square baseline and NO stroke around the mark"
    - "Grid/axis lines stay solid recessive hairlines one step off the surface"
    - "charts.js still passes the amber-ban and innerHTML source guards, and exposes an endpoint direct-label primitive"
  artifacts:
    - path: "src/viz/css/styles.css"
      provides: "One global scrollbar rule set replacing the four per-container blocks"
      contains: "scrollbar-width: thin"
    - path: "src/viz/modules/charts.js"
      provides: "Updated line/bar mark-spec builders + endpoint direct-label"
      exports: ["line", "bar", "axis", "legend", "attachHover", "directLabel"]
    - path: "tests/viz-charts-geometry.test.ts"
      provides: "Machine-checkable mark-spec assertions locking the new geometry"
  key_links:
    - from: "src/viz/css/styles.css global scrollbar"
      to: "var(--scrollbar-thumb) / var(--scrollbar-thumb-hover)"
      via: "token reference (no raw literal — D-14-A)"
      pattern: "scrollbar-color: var\\(--scrollbar-thumb\\)"
    - from: "src/viz/modules/charts.js bar()"
      to: "rounded-top strokeless path"
      via: "createSvgNode('path', ...)"
      pattern: "createSvgNode\\('path'"
---

<objective>
Close GAP-3 (global styled scrollbar) and lay the GAP-4(d) chart mark-spec
foundation in the shared SVG primitives. This plan owns the two "contract"
surfaces that the de-box redesign (60-11) consumes: the global scrollbar rule
and the line/bar/axis/legend/endpoint-label geometry in charts.js.

Purpose: Ship the presentation primitives first so 60-11 renders against a
stable mark-spec contract instead of re-deriving it. Both changes are
independently shippable and do not break the current dashboards (they only
restyle marks / consolidate scrollbar CSS).

Output: One global scrollbar rule set, updated charts.js builders, and
mark-spec test assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-settings-stats-depth/60-HUMAN-UAT.md
@.planning/phases/60-settings-stats-depth/60-UI-SPEC.md
@.planning/phases/60-settings-stats-depth/60-REVIEW.md

<interfaces>
<!-- Design tokens (defined in src/viz/modules/constants.js HUD_CSS_TOKENS,
     injected onto the document root by app.js — reference as var(--<key>),
     never as a raw literal: styles.css is under a zero-raw-color-literal lock,
     tests/viz-activity-palette-invariants.test.ts D-14-A). -->
--scrollbar-thumb:        rgba(170, 150, 180, 0.28)
--scrollbar-thumb-hover:  rgba(170, 150, 180, 0.45)
--section-divider-faint:  rgba(140, 150, 165, 0.08)   /* recessive hairline grid/axis */
--divider-faint:          rgba(140, 150, 165, 0.11)

<!-- Existing per-container scrollbar blocks in styles.css to be REMOVED and
     replaced by one global set (identical thumb/track recipe): -->
  .detail-page #detail        (lines ~621-631)
  #reader                     (lines ~726-738)
  #settings-panel             (lines ~1020-1037)
  .index-content              (lines ~1392-1404)

<!-- charts.js current builder signatures (src/viz/modules/charts.js): -->
export function line(points, { color, strokeWidth=1.5, dashed })      // polyline, round join/cap
export function bar(bars, { color, fillAlpha=0.75 })                  // <rect> + 1px stroke  ← REMOVE stroke, ROUND top
export function axis(opts)                                            // gridColor default rgba(140,150,165,0.08) — keep
export function legend(series, opts)                                  // unchanged this plan
export function attachHover(svg, points, opts)                        // CR-01 viewBox conversion — DO NOT REGRESS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate the styled scrollbar into one global rule set (GAP-3)</name>
  <files>src/viz/css/styles.css</files>
  <read_first>
    - src/viz/css/styles.css lines ~600-640 (`.detail-page #detail` scrollbar block)
    - src/viz/css/styles.css lines ~705-738 (`#reader` scrollbar block)
    - src/viz/css/styles.css lines ~1015-1040 (`#settings-panel` scrollbar block)
    - src/viz/css/styles.css lines ~1385-1405 (`.index-content` scrollbar block)
    - src/viz/modules/constants.js lines 779-780 (--scrollbar-thumb / --scrollbar-thumb-hover token values)
    - tests/viz-activity-palette-invariants.test.ts lines 371-405 (D-14-A raw-literal lock — the new rule MUST use var() tokens only)
  </read_first>
  <action>
    Delete all FOUR per-container scrollbar blocks (`.detail-page #detail`,
    `#reader`, `#settings-panel`, `.index-content` — both the `scrollbar-width`/
    `scrollbar-color` shorthand lines and their four `::-webkit-scrollbar*`
    rules each). Replace them with ONE global rule set placed near the top of
    styles.css (right after the reset/layout skeleton block, so it applies
    document-wide including `#stats-view`, per GAP-3 founder decision
    2026-07-14). Use the `*` universal selector so every current and future
    scroll surface inherits it:
      `* { scrollbar-width: thin; scrollbar-color: var(--scrollbar-thumb) transparent; }`
      `*::-webkit-scrollbar { width: 8px; }`
      `*::-webkit-scrollbar-track { background: transparent; }`
      `*::-webkit-scrollbar-thumb { background: var(--scrollbar-thumb); border-radius: 4px; border: 2px solid transparent; background-clip: content-box; }`
      `*::-webkit-scrollbar-thumb:hover { background: var(--scrollbar-thumb-hover); background-clip: content-box; }`
    Every color value MUST be `var(--scrollbar-thumb)` / `var(--scrollbar-thumb-hover)`
    (no raw rgba — D-14-A lock). Amber stays activation-only; the thumb is mauve.
    Add a one-line comment: global muted-mauve scrollbar (GAP-3, replaces the
    four per-container blocks). Do NOT touch #topics-rail's own hover-expand
    overflow rules — that is layout, not the scrollbar skin.
  </action>
  <acceptance_criteria>
    - `grep -c "::-webkit-scrollbar" src/viz/css/styles.css` == 4 (only the single global set: scrollbar, -track, -thumb, -thumb:hover)
    - `grep -c "scrollbar-width: thin" src/viz/css/styles.css` == 1
    - `grep -c "scrollbar-color: var(--scrollbar-thumb) transparent" src/viz/css/styles.css` == 1
    - `grep -c "\.detail-page #detail::-webkit-scrollbar" src/viz/css/styles.css` == 0
    - `grep -c "#reader::-webkit-scrollbar" src/viz/css/styles.css` == 0
    - `grep -c "#settings-panel::-webkit-scrollbar" src/viz/css/styles.css` == 0
    - `grep -c ".index-content::-webkit-scrollbar" src/viz/css/styles.css` == 0
    - `grep -Ei "webkit-scrollbar-thumb\b.*(#|rgba\()" src/viz/css/styles.css` returns nothing (no raw color literal — all via var())
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/viz-activity-palette-invariants.test.ts tests/viz-frontend-static.test.ts</automated>
  </verify>
  <done>The four per-container scrollbar blocks are gone; one global `*`-scoped set remains using only var() tokens; the D-14-A raw-literal lock still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update line/bar mark specs + add endpoint direct-label + area wash (GAP-4d)</name>
  <files>src/viz/modules/charts.js, tests/viz-charts-geometry.test.ts</files>
  <read_first>
    - src/viz/modules/charts.js lines 165-291 (line/bar/axis/legend builders)
    - src/viz/modules/charts.js lines 292-367 (attachHover — CR-01 viewBox conversion must NOT regress)
    - tests/viz-charts-geometry.test.ts lines 1-110 (existing geometry tests + charts.js source guards)
    - src/viz/modules/stats-dashboard.js lines 447-600 (call sites that will consume these builders in 60-11 — do not edit here, only confirm signatures stay compatible)
  </read_first>
  <behavior>
    - line(points) renders a polyline with stroke-width "2" and stroke-linejoin/linecap "round"
    - line(points, { areaFill: '<color>' }) additionally renders a filled area polygon down to the chart baseline at ~0.1 fill-opacity (the ~10% wash), BEHIND the stroke; omitting areaFill renders no fill (back-compat)
    - bar(bars) renders each mark as a rounded-top path (4px radius on the two data-end/top corners, square baseline corners) with NO stroke attribute and fill at the series color
    - bar clamps the corner radius to min(4, width/2, height) so a short/thin bar never inverts
    - directLabel(point, text, opts) returns an SVG <text> node positioned at the point (endpoint/extreme), textContent-set only, recessive fill — used for selective direct labels, never per-point
    - axis() keeps the solid recessive hairline gridColor (rgba(140,150,165,0.08)) — unchanged
    - No innerHTML, no amber-family literal anywhere in charts.js
  </behavior>
  <action>
    In `line()`: change the default strokeWidth to 2 (round join/cap already
    set). Add an optional `areaFill` opt — when a color string is supplied,
    build and RETURN a `<g>` containing (a) a `<polygon>`/`<polyline>`-derived
    filled area from the points down to the baseline y (caller passes
    `baselineY` in opts) with `fill: areaFill`, `fill-opacity: 0.1`, no stroke,
    then (b) the existing 2px stroke polyline on top; when `areaFill` is absent,
    keep returning the bare polyline exactly as today (back-compat for callers
    that pass no areaFill). Keep `dashed` support.
    In `bar()`: REMOVE the `stroke`/`stroke-width` attributes entirely (no
    stroke around marks — GAP-4d). Replace the `<rect>` per bar with a
    `createSvgNode('path', ...)` whose `d` draws a rounded-TOP rectangle:
    start at bottom-left `(x, y+height)`, up to `(x, y+r)`, arc to `(x+r, y)`,
    line to `(x+w-r, y)`, arc to `(x+w, y+r)`, down to `(x+w, y+height)`,
    close. `r = Math.min(4, b.width/2, b.height)`. Keep `fill: color` +
    `fill-opacity` (default 0.75). Keep the wrapping `<g class="chart-bar-series">`.
    Add and export `directLabel(point, text, opts = {})` — returns a
    `createSvgNode('text', ...)` at `point.x`/`point.y` (offset via opts.dx/dy,
    default dy -6), `text-anchor` from opts (default 'middle'),
    `font-size: 10`, `fill` = opts.color or the recessive '#9a90a4', with
    `label.textContent = text` (T-44-19 — textContent only, even numeric).
    Do NOT change `attachHover` (CR-01 viewBox conversion + CR-04 marker skip
    stay intact). Do NOT change `axis()` gridColor. Do NOT introduce any amber
    literal or innerHTML.
  </action>
  <acceptance_criteria>
    - `grep -c "createSvgNode('path'" src/viz/modules/charts.js` >= 1 (bar builds a rounded-top path)
    - `grep -c "export function directLabel" src/viz/modules/charts.js` == 1
    - In `bar()`, `grep -A25 "export function bar" src/viz/modules/charts.js | grep -c "stroke"` == 0 (no stroke around bar marks)
    - `grep -c "fill-opacity" src/viz/modules/charts.js` >= 2 (bar fill + area wash)
    - `grep -Ei "ffb866|accent-amber" src/viz/modules/charts.js` returns nothing
    - `grep -c "innerHTML" src/viz/modules/charts.js` == 0
    - `npx tsc --noEmit` clean
    - tests/viz-charts-geometry.test.ts updated with assertions: line default stroke-width == "2"; bar mark is a `path` with no `stroke` attr; directLabel sets textContent and returns a `text` node; area wash uses fill-opacity 0.1 when areaFill passed. All green.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/viz-charts-geometry.test.ts</automated>
  </verify>
  <done>line() is 2px with optional 10% area wash; bar() is a strokeless rounded-top path; directLabel() exists; amber/innerHTML guards still pass; new mark-spec assertions are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server JSON → SVG/DOM | /stats/* values reach the DOM through these builders; any `<text>` content must be `textContent`, never markup |
| design-invariant locks | styles.css raw-literal ban (D-14-A), amber ban (D-08), backdrop-filter allow-list (D-14-C) are machine-enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-10-01 | Tampering | charts.js directLabel/text nodes | mitigate | textContent-only assignment (T-44-19); no innerHTML introduced; source-grep guard asserts zero innerHTML |
| T-60-10-02 | Tampering | styles.css global scrollbar rule | mitigate | color via var(--scrollbar-thumb) only; D-14-A raw-literal lock re-run in Task 1 verify |
| T-60-10-03 | Information Disclosure | none (presentation only, no new data path) | accept | this plan reads no new server data and adds no route |
| T-60-10-SC | Tampering | npm/pip/cargo installs | mitigate | zero new dependencies — no install tasks in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx vitest run tests/viz-charts-geometry.test.ts tests/viz-activity-palette-invariants.test.ts tests/viz-frontend-static.test.ts` green
- Scrollbar grep gates in Task 1 acceptance all pass
- charts.js amber/innerHTML guards still green
</verification>

<success_criteria>
- One global scrollbar rule set; four per-container blocks removed
- line() 2px + optional 10% area wash; bar() strokeless rounded-top path; directLabel() exported
- No raw color literal in styles.css; no amber/innerHTML in charts.js
- CR-01 viewBox hover conversion and CR-04 marker-skip untouched
</success_criteria>

<output>
Create `.planning/phases/60-settings-stats-depth/60-10-SUMMARY.md` when done
</output>
