---
phase: 60-settings-stats-depth
plan: 06
type: execute
wave: 4
depends_on: ["60-04", "60-05"]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-06, D-15]
must_haves:
  truths:
    - "On the live install, opening the stats takeover from both the settings link and the ⌘K 'Open stats' command replaces the brain full-window and renders both tabs against real data"
    - "The founder confirms the surface reads as belonging to the Phase-59 HUD language, charts are legible, no amber appears, and the honest labels (retail-$, node-growth approximation, last-sleep-pass status) are present"
  artifacts:
    - path: ".planning/phases/60-settings-stats-depth/60-06-SUMMARY.md"
      provides: "founder sign-off record + any tuning follow-ups"
      contains: "checkpoint"
  key_links: []
---

<objective>
Closing founder verification of the whole Phase-60 surface on the live install —
the same closing-checkpoint pattern Phase 59 used (59-06). The UI-SPEC was
auto-chained without a live user, so the two dashboards get one real look-and-feel
pass before the phase is called done. No new implementation; a verification and
sign-off plan.

Purpose: Confirm the dashboards render correctly and belong visually before close.
Output: a sign-off SUMMARY capturing approval + any tuning follow-ups.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-settings-stats-depth/60-CONTEXT.md
@.planning/phases/60-settings-stats-depth/60-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automated gate</name>
  <files></files>
  <read_first>
    - .planning/phases/60-settings-stats-depth/60-UI-SPEC.md (Checker Sign-Off + contracts) — what the founder is verifying against
  </read_first>
  <action>
    Before pausing for the founder, run the full viz test + typecheck gate so the
    checkpoint is verifying a green build, not debugging one. Run the viz test suite
    (the viz-*.test.ts files, including viz-stats-routes, viz-charts-geometry,
    viz-frontend-static, viz-activity-palette-invariants, viz-settings-panel,
    viz-hud-palette) and `npx tsc --noEmit`. Confirm the recense viz server can start
    against the live DB. Summarize pass/fail counts for the founder.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-stats-routes.test.ts tests/viz-charts-geometry.test.ts tests/viz-frontend-static.test.ts tests/viz-activity-palette-invariants.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All listed viz test files pass and tsc is clean before the checkpoint opens
    - No amber literal present in any Phase-60 source (grep viz/modules/stats-dashboard.js, charts.js, constants.js additions)
  </acceptance_criteria>
  <done>Green build confirmed; ready for the founder look-and-feel pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Founder live-install sign-off</name>
  <what-built>
    Two dashboards in a full-window takeover reachable from the settings
    "View usage stats →" link and the ⌘K "Open stats" command: a Usage tab (daily
    burn + cost-event markers with before/after deltas, per-feature + per-model
    splits, retail-$ headline) and a Brain Health tab (node growth, kind mix,
    reconsolidations/tombstones per day, judge activity + escalation, episodes
    pending/consolidated, last-sleep-pass tile) — hand-rolled SVG, Phase-59 HUD
    language, Phase-57 identity hues, no amber.
  </what-built>
  <how-to-verify>
    1. Start the live viz: `recense viz` and open the served localhost URL.
    2. Open settings, click `View usage stats →` — the brain should be replaced
       full-window by the stats surface on the Usage tab.
    3. Confirm the Usage tab: burn chart is the focal anchor, cost-event dashed
       markers appear at the known dates and hovering one shows a before/after
       delta; per-feature and per-model splits render; the retail-$ headline reads
       `API-retail equivalent (subscription-billed: $0 marginal)`.
    4. Switch range pills (7d/30d/90d/all-time) — charts rescope; all-time shows
       weekly buckets. Click refresh — the `as of {HH:MM:SS}` stamp updates.
    5. Switch to Brain Health — confirm all six metric groups render in identity
       hues (reconsolidation magenta, tombstone coral, judge cyan, node-growth
       sage), the node-growth approximation caption is present, and the
       last-sleep-pass tile shows an honest status (not a fabricated success).
    6. Confirm no amber anywhere on either tab, the surface is flat (not glass),
       and Escape / × closes back to the brain.
    7. Also open via ⌘K → `Open stats`.
    8. Confirm nothing leaks into the compact/tray popover LOD.
  </how-to-verify>
  <acceptance_criteria>
    - Founder confirms both tabs render against real data, read as Phase-59-native, are amber-free, and carry the honest labels; or files specific tuning notes for a follow-up
  </acceptance_criteria>
  <resume-signal>Type "approved" or list the visual/behavioral issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | verification-only plan; no code or input boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-10 | Integrity | pre-checkpoint build state | mitigate | full viz test + tsc gate runs before the founder pass so sign-off is against a green build |
| T-60-SC | Tampering | package installs | accept | verification-only — no installs |
</threat_model>

<verification>
- Automated gate green (Task 1)
- Founder approval recorded in the SUMMARY (Task 2)
</verification>

<success_criteria>
The two dashboards are confirmed correct and visually native on the live install,
with founder sign-off (or an explicit tuning follow-up list) recorded.
</success_criteria>

<output>
Create `.planning/phases/60-settings-stats-depth/60-06-SUMMARY.md` when done
</output>
