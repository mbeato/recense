---
phase: 60-settings-stats-depth
plan: 09
type: execute
wave: 2
gap_closure: true
depends_on: ["60-07", "60-08"]
files_modified:
  - src/viz/modules/stats-dashboard.js
  - src/viz/css/styles.css
autonomous: true
requirements: [GAP-2a, GAP-2b, GAP-2c, GAP-2d, D-08, D-09, D-14]
must_haves:
  truths:
    - "The Usage tab LEADS with a stat-tile row on top: today / this-week / 30d tokens, avg tokens/day, and retail-$ — reusing the .stats-headline-tile Display-28px treatment; the burn chart is demoted below the tiles"
    - "Subscription-limit framing is visible: today-vs-typical-day share, week-vs-typical-week share, a trend arrow vs the prior period, and heaviest day this week — labeled as 'vs your typical', no fabricated quota number, no amber"
    - "A visible levers card lists one row per COST_EVENT — label, date, before-avg/day, after-avg/day, %-saved — replacing hover-only delta discovery; the burn-chart dashed markers still render"
    - "The per-feature and per-model splits are collapsed into ONE compact presentation (single stacked bar or simple table), not two separate full chart cards"
    - "Design language holds: no amber, flat surface (no backdrop-filter on #stats-view), textContent-only rendering, Phase-59 tokens"
  artifacts:
    - path: "src/viz/modules/stats-dashboard.js"
      provides: "Redesigned renderUsageTab: stat-tile row + framing + levers card + collapsed split, charts demoted"
      contains: "lever_deltas"
    - path: "src/viz/css/styles.css"
      provides: "Styles for the stat-tile row, framing, levers card, and collapsed split — flat, no backdrop-filter"
      contains: "stats-tile-row"
  key_links:
    - from: "renderUsageTab"
      to: "data.summary"
      via: "stat-tile row + framing render from the summary object"
      pattern: "summary"
    - from: "levers card"
      to: "data.lever_deltas"
      via: "one row per COST_EVENT with before/after/%-saved"
      pattern: "lever_deltas"
---

<objective>
GAP-2 (a)(c)(d) client redesign: rebuild the Usage tab so it leads with glanceable numbers
and answers decisions, not chart-first noise.

(a) Stat-tile row on top — today / this-week / 30d tokens, avg tokens/day, retail-$ — reusing
    the `.stats-headline-tile` Display-28px treatment; burn chart demoted below.
(b) Subscription-limit framing — today-vs-typical, week-vs-typical, trend arrow vs prior
    period, heaviest day this week; "vs your typical" honest labeling, no fabricated quota.
(c) Visible levers card — one row per COST_EVENT (label, date, before-avg/day, after-avg/day,
    %-saved), replacing hover-only delta discovery; burn-chart markers stay.
(d) Collapse the per-feature + per-model bar charts into ONE compact presentation (single
    stacked bar or simple table) — the founder called two full chart cards low-value noise.

Purpose: founder UAT verdict (2026-07-11) — the Usage tab "is not really useful or
informative." These are locked founder decisions.

Output: client-only redesign of the Usage tab, consuming the new `summary` and `lever_deltas`
fields shipped by 60-08 and building on the responsive rendering shipped by 60-07. The Brain
Health tab is untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-settings-stats-depth/60-HUMAN-UAT.md
@.planning/phases/60-settings-stats-depth/60-UI-SPEC.md
@.planning/phases/60-settings-stats-depth/60-CONTEXT.md

<interfaces>
<!-- The redesign consumes the 60-08 server fields and reuses existing helpers/tokens. -->

/stats/usage response after 60-08 (fields this plan consumes):
  summary: {
    today_tokens, week_tokens, month_tokens, avg_tokens_per_day, retail_usd_30d,
    today_vs_typical_pct (number|null), week_vs_typical_pct (number|null),
    trend_pct (number|null), trend_direction ('up'|'down'|'flat'),
    heaviest_day ({ date, tokens } | null)
  }
  lever_deltas: [{ date, label, before_avg, after_avg, pct_saved (number|null) }]
  buckets, by_feature, by_model, bucket_granularity, cost_event_deltas   // still present

stats-dashboard.js current renderUsageTab order (to REPLACE):
  renderHeadline (single retail-$ tile) → renderBurnChart → renderFeatureSplit → renderModelSplit

Reuse verbatim:
  RETAIL_LABEL = 'API-retail equivalent (subscription-billed: $0 marginal)'   // D-09 label, keep on the retail tile
  fmtTokens(n) from charts.js;  safeDateLabel/fmtDate for dates
  FEATURE_ORDER = ['extract','judge','corpus_gen','schema_abstract'];  NEUTRAL_SERIES_RAMP
  .stats-headline-tile / .stats-headline-value (28px mono) / .stats-headline-label (10px) — styles.css:1618-1631
  .chart-card (flat, --chart-card bg, --divider-faint border, no backdrop-filter) — styles.css:1599

Design tokens (existing, non-amber): --text-bright, --text-recede, --text-body-mauve,
  --text-stat (green), --divider-faint, --chart-card, --radius-md, --accent-toggle-mauve
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stat-tile row + subscription-limit framing on top</name>
  <read_first>
    - src/viz/modules/stats-dashboard.js (renderUsageTab, renderHeadline — the tile being replaced)
    - src/viz/css/styles.css:1595-1642 (.stats-headline-tile / -value / -label to reuse)
    - .planning/phases/60-settings-stats-depth/60-UI-SPEC.md (Typography Display-28px; Color — no amber; Copywriting)
    - .planning/phases/60-settings-stats-depth/60-HUMAN-UAT.md (GAP-2a/2b)
  </read_first>
  <action>
    Replace `renderHeadline` with a `renderStatTileRow(container, summary)` that renders a
    flex row of five `.stats-headline-tile` tiles reusing the Display-28px `.stats-headline-value`
    for the number and `.stats-headline-label` for the caption:
      - Today: fmtTokens(summary.today_tokens) / label "today"
      - This week: fmtTokens(summary.week_tokens) / label "this week"
      - 30d: fmtTokens(summary.month_tokens) / label "last 30 days"
      - Avg/day: fmtTokens(summary.avg_tokens_per_day) / label "avg tokens/day"
      - Retail-$: '$' + summary.retail_usd_30d.toFixed(4) / label RETAIL_LABEL (verbatim, D-09)
    Add a `renderFraming(container, summary)` block beneath the tiles rendering, as concise
    "vs your typical" copy (Body/Label typography):
      - today-vs-typical: "today: {pct}% of your typical day" (skip line when pct null)
      - week-vs-typical:  "this week: {pct}% of a typical week" (skip when null)
      - trend: an arrow glyph by trend_direction ('up'→'▲', 'down'→'▼', 'flat'→'→') plus
        "{trend_pct}% vs prior 7d" (or "no prior baseline" when trend_pct null)
      - heaviest day: "heaviest day: {MMM D} ({tokens})" (skip when heaviest_day null)
    All values textContent only (T-44-19). NO amber — trend arrow uses existing neutral/green
    tokens only (e.g. down→--text-stat, up/flat→--text-body-mauve); pick from existing tokens,
    never introduce a warm hue. Do NOT print a "quota remaining" number — framing is baseline-
    relative only. Wire both into `renderUsageTab` as the FIRST things rendered (above the
    chart), reading `data.summary` (guard for a missing summary → render nothing / graceful).
  </action>
  <verify>
    <automated>grep -nE "renderStatTileRow|renderFraming|typical" src/viz/modules/stats-dashboard.js && grep -q "RETAIL_LABEL" src/viz/modules/stats-dashboard.js && npx tsc --noEmit && npx vitest run tests/viz-frontend-static.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Five tiles rendered from summary: `grep -cE "today_tokens|week_tokens|month_tokens|avg_tokens_per_day|retail_usd_30d" src/viz/modules/stats-dashboard.js` ≥ 5
    - Framing present: `grep -cE "trend_direction|today_vs_typical_pct|heaviest_day|typical" src/viz/modules/stats-dashboard.js` ≥ 3
    - RETAIL_LABEL kept verbatim on the retail tile: `grep -c "API-retail equivalent (subscription-billed: \$0 marginal)" src/viz/modules/stats-dashboard.js` ≥ 1
    - No amber literal in the module: `grep -niE "ffb866|accent-amber" src/viz/modules/stats-dashboard.js` returns no match
    - No fabricated quota copy: `grep -niE "quota remaining|remaining quota|quota_remaining" src/viz/modules/stats-dashboard.js` returns no match
    - T-44-19 preserved: `grep -vE '^\s*(//|\*)' src/viz/modules/stats-dashboard.js | grep -c "innerHTML"` == 0
    - `npx tsc --noEmit` exits 0; `npx vitest run tests/viz-frontend-static.test.ts` green
  </acceptance_criteria>
  <done>Usage tab opens with a five-tile Display-28px stat row (today/week/30d/avg-day/retail-$) plus "vs your typical" framing (share, trend arrow, heaviest day); no amber, no quota fabrication; charts demoted below.</done>
</task>

<task type="auto">
  <name>Task 2: Visible levers card (one row per COST_EVENT)</name>
  <read_first>
    - src/viz/modules/stats-dashboard.js (renderBurnChart / appendMarkers — markers must STAY;
      the card is the new primary surface)
    - .planning/phases/60-settings-stats-depth/60-HUMAN-UAT.md (GAP-2c — before/after/%-saved fields)
    - .planning/phases/60-settings-stats-depth/60-UI-SPEC.md (levers/table styling — flat, no backdrop-filter)
  </read_first>
  <action>
    Add `renderLeversCard(container, leverDeltas)` rendering a `.chart-card` titled e.g.
    "Cost levers" containing a simple table/list — one row per entry in `data.lever_deltas`:
    columns label, date (safeDateLabel), before-avg/day (fmtTokens(before_avg)+'/day'),
    after-avg/day (fmtTokens(after_avg)+'/day'), and %-saved. Render %-saved from
    `pct_saved` (server-provided): show e.g. "{pct}% saved" when pct_saved is a positive
    number, "{|pct|}% more" when negative, "—" when null. All values textContent only
    (T-44-19). Empty `lever_deltas` → a "no cost levers recorded yet" empty row (reuse
    `.stats-empty-state` copy register). Wire into `renderUsageTab` ABOVE the burn chart
    (levers are a primary decision surface per GAP-2c). The burn-chart dashed markers +
    their hover tooltip (renderBurnChart/appendMarkers, using the WINDOWED cost_event_deltas)
    remain unchanged — the card supplements, does not replace, the markers.
  </action>
  <verify>
    <automated>grep -nE "renderLeversCard|lever_deltas|pct_saved" src/viz/modules/stats-dashboard.js && grep -q "appendMarkers" src/viz/modules/stats-dashboard.js && npx tsc --noEmit && npx vitest run tests/viz-frontend-static.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Levers card reads the new field: `grep -c "lever_deltas" src/viz/modules/stats-dashboard.js` ≥ 1 and renders before/after/%-saved: `grep -cE "before_avg|after_avg|pct_saved" src/viz/modules/stats-dashboard.js` ≥ 3
    - Burn-chart markers retained: `grep -c "appendMarkers" src/viz/modules/stats-dashboard.js` ≥ 1 (markers not removed)
    - Empty-state handled: `grep -c "stats-empty-state" src/viz/modules/stats-dashboard.js` ≥ 1
    - No amber: `grep -niE "ffb866|accent-amber" src/viz/modules/stats-dashboard.js` returns no match
    - `npx tsc --noEmit` exits 0; `npx vitest run tests/viz-frontend-static.test.ts` green
  </acceptance_criteria>
  <done>A visible "Cost levers" card lists each COST_EVENT with label, date, before/after avg-per-day and %-saved; burn-chart markers still render; empty state handled.</done>
</task>

<task type="auto">
  <name>Task 3: Collapse per-feature + per-model into one compact presentation + CSS</name>
  <read_first>
    - src/viz/modules/stats-dashboard.js (renderFeatureSplit + renderModelSplit — the two cards to collapse; renderSplitBarChart)
    - src/viz/css/styles.css:1595-1642 (chart-card/headline-tile styles to extend)
    - .planning/phases/60-settings-stats-depth/60-UI-SPEC.md (Color — no amber; Glass Discipline — NO backdrop-filter on any #stats-view sub-surface, D-14)
    - .planning/phases/60-settings-stats-depth/60-HUMAN-UAT.md (GAP-2d)
  </read_first>
  <action>
    Replace the two separate `renderFeatureSplit` + `renderModelSplit` chart cards with ONE
    compact `renderUsageBreakdown(container, byFeature, byModel)` — a single card presenting
    both splits compactly. Prefer a simple table (feature/model rows with token totals +
    NEUTRAL_SERIES_RAMP swatches), or a single stacked bar; either MUST be responsive
    (a table is trivially responsive; a stacked bar must use the 60-07 measured-width path).
    Keep FEATURE_ORDER + NEUTRAL_SERIES_RAMP coloring (D-08, non-amber). Then set the final
    `renderUsageTab` order to: stat-tile row (Task 1) → framing (Task 1) → levers card
    (Task 2) → burn chart (demoted) → collapsed breakdown. Add CSS to styles.css for the new
    surfaces — `.stats-tile-row` (flex row of tiles, gap 16px, wrap), the framing block, the
    levers card table rows, and the collapsed breakdown — all reusing existing tokens
    (--chart-card, --divider-faint, --text-*), FLAT with NO `backdrop-filter` (D-14 invariant
    — the styles.css allow-list test must stay green). No amber anywhere.
  </action>
  <verify>
    <automated>grep -nE "renderUsageBreakdown|stats-tile-row" src/viz/modules/stats-dashboard.js src/viz/css/styles.css && ! grep -qE "renderModelSplit" src/viz/modules/stats-dashboard.js && npx tsc --noEmit && npx vitest run tests/viz-frontend-static.test.ts tests/viz-activity-palette-invariants.test.ts tests/viz-settings-panel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Single collapsed presentation exists: `grep -c "renderUsageBreakdown" src/viz/modules/stats-dashboard.js` ≥ 1
    - The two-card split is gone: `grep -E "renderModelSplit|renderFeatureSplit" src/viz/modules/stats-dashboard.js` returns no match (folded into the breakdown)
    - New CSS present: `grep -c "stats-tile-row" src/viz/css/styles.css` ≥ 1
    - D-14 invariant intact — no backdrop-filter added on any #stats-view sub-surface: the activity-palette-invariants test (backdrop-filter allow-list) stays green
    - renderUsageTab order is tiles → framing → levers → burn → breakdown (inspect the function body)
    - No amber: `grep -niE "ffb866|accent-amber" src/viz/modules/stats-dashboard.js src/viz/css/styles.css` returns no NEW match beyond pre-existing tokens unrelated to #stats-view
    - `npx tsc --noEmit` exits 0; the three named test files green
  </acceptance_criteria>
  <done>Per-feature and per-model splits render as one compact card; Usage tab order leads with tiles/framing/levers and demotes the burn chart + breakdown; new flat CSS with no backdrop-filter; D-14 invariant test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM ↔ server data | summary/lever_deltas values are server-sourced; must render textContent-only |
| CSS ↔ D-14 invariant | new #stats-view sub-surfaces must not add backdrop-filter (machine-locked allow-list) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-09-01 | Tampering | stat tiles / framing / levers / breakdown text | mitigate | Every server-sourced value via `.textContent` (T-44-19); enforced by acceptance-criteria grep + viz-frontend-static suite |
| T-60-09-02 | Elevation (design-invariant bypass) | new styles.css selectors | mitigate | No backdrop-filter on #stats-view sub-surfaces; D-14 allow-list test (viz-activity-palette-invariants) must stay green |
| T-60-09-03 | Information Disclosure | framing copy | accept | Baseline-relative "vs your typical" only; no fabricated quota; no new data beyond the already-exposed summary |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx vitest run tests/viz-frontend-static.test.ts tests/viz-charts-geometry.test.ts tests/viz-activity-palette-invariants.test.ts tests/viz-settings-panel.test.ts` green
- Full suite unbroken: `npx vitest run` (no regressions)
</verification>

<success_criteria>
The Usage tab leads with a five-tile Display-28px stat row + "vs your typical" framing, shows
a visible levers card (row per COST_EVENT with before/after/%-saved), collapses per-feature +
per-model into one compact card, and demotes the burn chart below — all amber-free, flat
(no backdrop-filter), textContent-only, Phase-59-native; typecheck + suites green.
</success_criteria>

<output>
Create `.planning/phases/60-settings-stats-depth/60-09-SUMMARY.md` when done
</output>
