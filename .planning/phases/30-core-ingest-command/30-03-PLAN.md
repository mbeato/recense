---
phase: 30-core-ingest-command
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - .planning/phases/30-core-ingest-command/30-03-VALIDATION.md
autonomous: false
requirements: [INGEST-01, INGEST-02, INGEST-04]
must_haves:
  truths:
    - "Running `recense ingest-project <real-repo>` on the REAL committed transport produces ≥5 genuine facts per surveyed area (SC2 re-validated, NOT the unsound 82% spike number)"
    - "After a sleep pass, the ingested facts are retrievable via `recense recall` with the correct [scope] prefix"
    - "At least 1 schema is induced from the surveyed project's facts (SC3)"
    - "Raw code lines and low-value structural facts are absent (the calibrated quality gate holds on the real path)"
  artifacts:
    - path: ".planning/phases/30-core-ingest-command/30-03-VALIDATION.md"
      provides: "Written evidence: per-area genuine counts on the committed transport, [scope] recall samples, schema count, founder sign-off"
      min_lines: 30
  key_links:
    - from: "recense ingest-project (committed)"
      to: "facts with [scope]"
      via: "survey -> episodes -> sleep pass -> recall"
      pattern: "ingest-project"
---

<objective>
Re-validate SC2 (≥5 genuine facts/surveyed area), SC3 (≥1 schema), and INGEST-02 (`[scope]` recall) on the REAL committed Phase-30 transport. This is mandatory and cannot be skipped: RESEARCH Pitfall 2 established that the Phase-29 82%-genuine number was measured on a code path that does NOT exist in committed code (the committed transport had `--tools none` → `NO_TOOLS`). The Plan-01 survey transport is the first committed path that can actually read a repo, so SC2 has never been measured on shippable code.

The run writes survey-derived episodes into the live brain (D-04 default) using a subscription-billed headless agent with read access over an operator-supplied repo, then consolidates. That makes it a founder-supervised live mutation — modeled as a blocking `checkpoint:human-verify` (`autonomous: false`), mirroring the Phase-29 live-run checkpoint and the Phase-24 human-gated migration pattern.

Purpose: prove the shipped command produces genuine project knowledge end-to-end before declaring Phase 30 done.
Output: `30-03-VALIDATION.md` with per-area genuine counts on the committed path, `[scope]` recall samples, the schema count, and the founder GO/NO-GO.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-core-ingest-command/30-CONTEXT.md
@.planning/phases/29-survey-quality-spike/29-CALIBRATION.md
@.planning/phases/30-core-ingest-command/30-01-SUMMARY.md
@.planning/phases/30-core-ingest-command/30-02-SUMMARY.md

# Load-bearing operational env (the calibration's "these silently bite" notes):
@.planning/phases/29-survey-quality-spike/29-CALIBRATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the live-run command + pre-flight checks (no live write yet)</name>
  <files>.planning/phases/30-core-ingest-command/30-03-VALIDATION.md</files>
  <read_first>
    - .planning/phases/29-survey-quality-spike/29-CALIBRATION.md (Input 4 operational notes: OPENAI_API_KEY from sleep.env, pinned node bin, RECENSE_*_PROVIDER=claude-headless; the schema-has-no-scope-row caveat for the SC3 count query)
    - .planning/phases/30-core-ingest-command/30-02-SUMMARY.md (the shipped flag surface + default behaviors)
    - .planning/phases/30-core-ingest-command/30-CONTEXT.md (D-05 --dry-run as the pre-write sanity check; D-09 print-resolved-scope)
  </read_first>
  <action>
    Assemble (do NOT execute the live write yet) the exact operator commands for the founder, as single-line copy-paste-safe invocations. (1) A `--dry-run` first: `recense ingest-project <real-repo-dir> --dry-run` to confirm the resolved scope, per-area would-be counts, and that the transport actually reads the repo (areas returning in <20s or generic facts = the Pitfall-1 NO_TOOLS regression — abort and fix). Choose a real repo that is NOT brain-memory itself (so the target-repo-hooks question is moot) — e.g. a small external clone or another of the founder's projects. (2) The real run: decide between the default deferred path (then a manual or scheduled sleep pass) and `--consolidate` inline. For a tight verification loop recommend `--consolidate` so facts are recallable immediately, OR the deferred path + a manual `recense sleep-pass`. Source `~/.config/recense/sleep.env` for OPENAI_API_KEY + provider envs + the pinned node bin (the calibration's masked-failure trio). Write the pre-flight checklist and both command forms into the top of `30-03-VALIDATION.md`. Prepare the verification queries: per-area genuine tally (reuse the Phase-29 judge gate prompt from 29-CALIBRATION Input 3 against the freshly-minted facts joined to `session_id` `project-survey:<scope>:<area>`), the `recense recall` calls to spot-check `[scope]` attribution, and the SC3 schema count `SELECT count(*) FROM node WHERE type='schema' AND tombstoned=0` minus a pre-run baseline (NO scope join — the schema-has-no-scope-row caveat, else it falsely reports 0).
  </action>
  <verify>
    <human-check>The founder reviews the assembled --dry-run + real-run commands and the verification queries in 30-03-VALIDATION.md before any live write.</human-check>
  </verify>
  <acceptance_criteria>
    - `30-03-VALIDATION.md` exists with: the pre-flight env checklist (OPENAI_API_KEY, pinned node bin, RECENSE_*_PROVIDER), the `--dry-run` command, the real-run command (both forms documented), and the four verification queries (per-area genuine tally via the D-07 judge gate, `[scope]` recall spot-checks, the no-scope-join schema count, the pre-run schema baseline).
    - The chosen target repo is documented and is NOT brain-memory itself (Pitfall 4 avoidance).
    - All commands are single-line, copy-paste-safe (no `\` continuations, no multi-line heredocs).
  </acceptance_criteria>
  <done>The exact live-run + verification recipe is assembled and ready for founder review; no live write has occurred.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Founder-supervised live SC2/SC3/INGEST-02 re-validation on the committed transport</name>
  <files>.planning/phases/30-core-ingest-command/30-03-VALIDATION.md</files>
  <action>
    BLOCKING human checkpoint — this is the founder-supervised live run that re-measures SC2/SC3/INGEST-02 on the committed transport (the 82% Phase-29 number is unsound per RESEARCH Pitfall 2). The executor does NOT run the live write autonomously; it presents the prepared commands, the founder executes them, and the executor records the measured numbers into 30-03-VALIDATION.md and waits for the resume signal. See <what-built>/<how-to-verify>/<resume-signal>.
  </action>
  <what-built>
    `recense ingest-project <dir>` (Plans 01-02): the opt-in tool-enabled survey transport + the command + scope tagging + retry/skip + deferred/inline consolidation. This checkpoint measures SC2/SC3/INGEST-02 on the REAL committed path — the 82% Phase-29 number was measured on a non-committed path (RESEARCH Pitfall 2) and is NOT a sound baseline.
  </what-built>
  <how-to-verify>
    1. Source the env: `set -a; . ~/.config/recense/sleep.env; set +a` (gives OPENAI_API_KEY, RECENSE_*_PROVIDER, pinned node bin — the calibration's masked-failure trio).
    2. Dry-run first: run the `--dry-run` command from 30-03-VALIDATION.md. CONFIRM: the resolved `[scope]` printed matches the target project; each area takes ~90-100s with real tool turns (NOT <20s — sub-20s/generic facts = the NO_TOOLS regression, abort); per-area would-be counts look sane (~10-25/area, gotchas not in the hundreds).
    3. Real run: run the real-run command (deferred + manual `recense sleep-pass`, OR `--consolidate` inline). This writes to the live brain (D-04) and bills the Max subscription.
    4. After the sleep pass, run the per-area genuine tally (D-07 judge gate over the new facts) and confirm ≥5 genuine/area for the surveyed areas (SC2). Run `recense recall` spot-checks and confirm the new facts carry the correct `[scope]` prefix (INGEST-02). Run the no-scope-join schema count vs the baseline and confirm ≥1 new schema (SC3). Eyeball a sample for raw-code/structural-trivia absence (SC4/quality gate).
    5. Record all numbers (per-area genuine counts, recall samples, schema delta) into 30-03-VALIDATION.md.
  </how-to-verify>
  <verify>
    <human-check>Founder runs the dry-run + real run + verification queries and confirms ≥5 genuine/surveyed-area, correct [scope] recall, and ≥1 new schema on the committed transport.</human-check>
  </verify>
  <resume-signal>Type "approved" with the per-area genuine counts + schema delta recorded, or describe the gap (e.g. an area <5 genuine, NO_TOOLS regression, wrong scope) for gap-closure planning.</resume-signal>
  <done>The committed-transport run is measured; 30-03-VALIDATION.md records ≥5 genuine/surveyed-area, correct [scope] recall, and ≥1 new schema; the founder has typed "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder-supervised live run → customer-zero brain | A real subscription-billed survey writes episodes into the live DB and consolidates |
| operator-supplied target repo → survey agent | Read access over an arbitrary repo (transport mitigations from Plan 01 apply) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-10 | Tampering | live-brain pollution from a bad run | mitigate | Mandatory `--dry-run` first (per-area would-be counts + resolved scope printed) before the real write; the run is a blocking human checkpoint — no autonomous live mutation. |
| T-30-11 | Tampering | NO_TOOLS regression silently ingesting hallucinated facts | mitigate | Verification step explicitly checks per-area duration (~90-100s) and repo-specificity; sub-20s/generic = abort (RESEARCH Pitfall 1 warning sign). |
| T-30-12 | Spoofing / billing | unexpected API billing | mitigate | The committed transport strips ANTHROPIC_API_KEY (Plan 01) so the run bills the Max subscription; founder supervises the run. |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No installs; validation-only plan. |
</threat_model>

<verification>
- `30-03-VALIDATION.md` contains measured per-area genuine counts on the committed transport (≥5/area for surveyed areas), `[scope]` recall samples, and the schema delta (≥1).
- The founder has typed "approved" at the blocking checkpoint.
</verification>

<success_criteria>
- SC2 re-validated on the REAL committed transport (≥5 genuine facts/surveyed area) — the unsound 82% spike number is replaced with a measured-on-shippable-code result.
- INGEST-02: ingested facts are recallable with the correct `[scope]`.
- SC3: ≥1 schema induced (counted WITHOUT the scope join).
- SC4/quality gate: raw code + structural trivia absent on the real path.
</success_criteria>

<output>
Create `.planning/phases/30-core-ingest-command/30-03-SUMMARY.md` when done (referencing 30-03-VALIDATION.md for the measured evidence).
</output>
