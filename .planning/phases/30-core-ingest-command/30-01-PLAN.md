---
phase: 30-core-ingest-command
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/model/claude-headless-client.ts
  - src/adapter/survey-observations.ts
  - tests/claude-headless-client.test.ts
  - tests/survey-observations.test.ts
autonomous: true
requirements: [INGEST-01]
must_haves:
  truths:
    - "A tool-enabled survey transport can read an arbitrary <dir> via claude -p with read-only tools, zero permission denials"
    - "The exact 3-flag union (cwd:tmpdir + --add-dir + --permission-mode bypassPermissions + --tools Read Grep Glob) is behaviorally proven to read a fixture file in Wave 1, not just shape-asserted"
    - "The existing judge/extractor transport path is byte-for-byte unchanged (--tools none, cwd: os.tmpdir())"
    - "Both load-bearing guards (--setting-sources project, ANTHROPIC_API_KEY strip) are preserved on the survey path"
    - "A refusal / tool-access-failure agent response is detectable as a pure function before any ingest"
    - "splitObservations chunks an agent response into one-belief-per-line records, capped at 25/area"
  artifacts:
    - path: "src/model/claude-headless-client.ts"
      provides: "Opt-in survey path: createClaudeHeadlessSurveyClient(config, surveyDir)"
      contains: "createClaudeHeadlessSurveyClient"
    - path: "src/adapter/survey-observations.ts"
      provides: "Carried pure helpers: splitObservations, isRefusalOrToolFailure, SURVEY_AREAS, buildSurveyPrompt"
      exports: ["splitObservations", "isRefusalOrToolFailure", "SURVEY_AREAS", "buildSurveyPrompt"]
  key_links:
    - from: "createClaudeHeadlessSurveyClient"
      to: "claude -p argv"
      via: "buildSurveyHeadlessArgs"
      pattern: "--add-dir"
    - from: "splitObservations"
      to: "one episode per belief-line"
      via: "ingest-project-cli (Plan 02)"
      pattern: "splitObservations"
---

<objective>
Build the two load-bearing primitives Plan 02 depends on:

1. A NEW opt-in tool-enabled survey transport on the headless client. The committed `buildHeadlessArgs` passes `--tools none` + `cwd: os.tmpdir()` — a live `claude -p` probe (RESEARCH Seam 1) confirmed this returns `NO_TOOLS`, so the survey agent cannot read any repo through the committed path. The Phase-29 82%-genuine result was measured on a code path that does NOT exist in committed code. This is genuinely NEW critical-path work for INGEST-01, not a carry-forward.

2. The carried Phase-29 pure helpers (`splitObservations`, refusal/tool-failure detection per D-07, `SURVEY_AREAS`, `buildSurveyPrompt`) extracted from the throwaway spike into a real importable module with unit tests.

Purpose: isolate the new transport seam + testable primitives so Plan 02 wires them into the command without re-deriving them, and so the existing judge/extractor transport stays untouched (its tests are the regression guard).
Output: a survey-mode factory on `claude-headless-client.ts`, a `survey-observations.ts` helper module, and unit tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-core-ingest-command/30-CONTEXT.md
@.planning/phases/30-core-ingest-command/30-RESEARCH.md
@.planning/phases/29-survey-quality-spike/29-CALIBRATION.md

<interfaces>
<!-- The committed headless client. The survey path is ADDITIVE; the existing path stays. -->

From src/model/claude-headless-client.ts (existing — DO NOT alter the existing exports):
```typescript
export const NEUTRAL_SYSTEM: string; // "...no tool use..." — must NOT be used on the survey path
export function buildHeadlessArgs(model: string, systemPrompt: string): string[];
// returns ['-p','--output-format','json','--model',model,'--system-prompt',systemPrompt,
//          '--setting-sources','project','--tools','none','--strict-mcp-config',
//          '--exclude-dynamic-system-prompt-sections']
export function createClaudeHeadlessClient(config: EngineConfig): { client: AnthropicLike; model: string };
// spawns with cwd: os.tmpdir(); deletes ANTHROPIC_API_KEY / ANTHROPIC_AUTH_TOKEN from childEnv
export interface HeadlessUsage { model: string; usage?: Record<string,number>; total_cost_usd?: number; duration_ms?: number; }
export function setHeadlessUsageSink(fn: ((u: HeadlessUsage) => void) | null): void;
```

From scripts/spike/survey-feeder.ts (THROWAWAY spike — carry these verbatim, do NOT re-derive):
```typescript
export const SURVEY_AREAS = ['architecture','conventions','decisions','current-state','gotchas'] as const;
export function buildSurveyPrompt(area: SurveyArea): string; // D-08 calibrated prompt — see below
export function splitObservations(text: string): string[];   // strips markers, drops short/code-like, caps at 25
const MAX_OBS_PER_AREA = 25;
```
</interfaces>

# Source-of-truth files the executor MUST read (do not trust this summary):
@src/model/claude-headless-client.ts
@scripts/spike/survey-feeder.ts
@tests/claude-headless-client.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry the survey pure helpers into src/adapter/survey-observations.ts + refusal detector</name>
  <read_first>
    - scripts/spike/survey-feeder.ts (the verbatim source of SURVEY_AREAS, buildSurveyPrompt, splitObservations, MAX_OBS_PER_AREA)
    - .planning/phases/29-survey-quality-spike/29-CALIBRATION.md (Input 1 = the locked D-08 prompt shape; Input 3 = D-07 refusal/access-failure patterns; the gotchas tightening clause)
    - .planning/phases/30-core-ingest-command/30-CONTEXT.md (D-07 refusal/retry/skip; D-08 per-area override + gotchas tightening)
  </read_first>
  <behavior>
    - splitObservations("- one belief here that is long enough\n* x\nshort\nfoo();") returns exactly the markers-stripped long belief lines (drops <20 chars, single-token, lines ending in ;{} , lines with => or require( ).
    - splitObservations on a 40-line compliant response returns at most 25 records (MAX_OBS_PER_AREA cap).
    - isRefusalOrToolFailure("Read, Grep, and Glob tools cannot access /x") === true.
    - isRefusalOrToolFailure("I'm sorry, I cannot access that directory") === true.
    - isRefusalOrToolFailure("No genuine observations available") === true.
    - isRefusalOrToolFailure("The architecture uses a thin API route layer because ...") === false.
    - isRefusalOrToolFailure("") === true (empty = treat as failure to skip, never ingest).
    - buildSurveyPrompt('architecture', { repoDir, repoDesc }) for the 4 healthy areas (architecture/conventions/decisions/current-state) is byte-identical to the D-08 calibrated base text with repoDir/repoDesc/area interpolated (D-08 verbatim-vs-tuned split).
    - buildSurveyPrompt('gotchas', {...}) contains the extra why-level steering clause ("a senior dev's hard-won warnings, NOT a list of what files do") AND keeps the rest of the base prompt (D-08 gotchas tightening).
  </behavior>
  <action>
    Create `src/adapter/survey-observations.ts` exporting: `SURVEY_AREAS` (+ `SurveyArea` type) and `MAX_OBS_PER_AREA=25` carried verbatim from the spike; `splitObservations(text)` carried verbatim (same strip/filter/slice logic); `buildSurveyPrompt(area, { repoDir, repoDesc })` — generalize the spike's hardcoded `SURVEY_CWD` / contextscope description into params (repoDir replaces `/Users/vtx/usage`, repoDesc replaces the parenthetical package description), keep the 4 healthy areas' text byte-identical to 29-CALIBRATION Input 1 (D-08), and for `area === 'gotchas'` append the why-level steering clause per D-08; and `isRefusalOrToolFailure(text)` — case-insensitive substring/regex match over the D-07 patterns ("cannot access", "i'm sorry", "i am sorry", "no genuine observations", tool-access-failure phrasings) plus empty/whitespace → true. Pure functions only — no DB, no I/O, no claude calls. This is the importable replacement for the spike's inline helpers; the spike file stays untouched (it is throwaway).
  </action>
  <verify>
    <automated>cd /Users/vtx/brain-memory && npx vitest run tests/survey-observations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/adapter/survey-observations.ts` exists and exports `SURVEY_AREAS`, `SurveyArea`, `splitObservations`, `buildSurveyPrompt`, `isRefusalOrToolFailure`.
    - `tests/survey-observations.test.ts` covers every case in the `<behavior>` block and passes.
    - `grep -c "import.*better-sqlite3\|import Database\|recordEvent\|claude" src/adapter/survey-observations.ts` returns 0 (pure module, no DB / no transport).
    - `npx tsc --noEmit` is clean.
    - The 4-healthy-area prompt text matches 29-CALIBRATION Input 1 verbatim (assert the load-bearing line `Write WHY, NOT WHAT` and the `MUST NOT contain` list are present unchanged).
  </acceptance_criteria>
  <done>splitObservations + isRefusalOrToolFailure + buildSurveyPrompt are importable, unit-tested pure functions; the spike's inline copies are superseded but the spike file is left as-is.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the opt-in tool-enabled survey transport to claude-headless-client.ts</name>
  <read_first>
    - src/model/claude-headless-client.ts (the FILE being modified — read buildHeadlessArgs lines 105-116, the spawn body lines 144-213, the API-key strip 145-148, the --setting-sources rationale 91-99)
    - tests/claude-headless-client.test.ts (the existing regression suite — line 79-80 asserts the default path carries `--tools none`; that assertion MUST stay green)
    - .planning/phases/30-core-ingest-command/30-RESEARCH.md (Seam 1 "Exact change required" — the precise survey arg set + the tmpdir-cwd + --add-dir defensive choice; Pitfall 1 + Pitfall 4. NOTE: Seam 1 probed --add-dir / --permission-mode / --tools as SEPARATE elements; the exact UNION used here was not probed as one set — hence the manual smoke-check below)
  </read_first>
  <behavior>
    - buildSurveyHeadlessArgs(model, systemPrompt, surveyDir) returns an argv containing '-p','--output-format','json','--model',model,'--system-prompt',systemPrompt,'--setting-sources','project','--strict-mcp-config','--exclude-dynamic-system-prompt-sections','--tools','Read','Grep','Glob','--add-dir',surveyDir,'--permission-mode','bypassPermissions' and does NOT contain '--tools','none'.
    - createClaudeHeadlessSurveyClient(cfg, '/some/dir') spawns with cwd: os.tmpdir() (NOT surveyDir — Pitfall 4 defensive choice) and passes '--add-dir','/some/dir'.
    - The survey client's spawn env has ANTHROPIC_API_KEY and ANTHROPIC_AUTH_TOKEN deleted (billing guard preserved).
    - The survey client's argv contains '--setting-sources','project' (self-ingestion guard preserved).
    - The survey client's argv carries a non-empty system prompt that is NOT NEUTRAL_SYSTEM (NEUTRAL_SYSTEM forbids tool use — Seam 1 caveat).
    - The EXISTING default path is unchanged: createClaudeHeadlessClient(cfg) still produces argv with '--tools','none' and spawns cwd: os.tmpdir() (the existing test assertions still pass).
  </behavior>
  <action>
    Add to `src/model/claude-headless-client.ts` (additive only — do NOT mutate `buildHeadlessArgs` or `createClaudeHeadlessClient`): (1) `export const SURVEY_SYSTEM` — a minimal system prompt that PERMITS read-only tool use (the survey agent must use Read/Grep/Glob; NEUTRAL_SYSTEM forbids tools so it cannot be reused). (2) `export function buildSurveyHeadlessArgs(model, systemPrompt, surveyDir)` mirroring `buildHeadlessArgs` but replacing the tool/dir/permission flags per RESEARCH Seam 1 "Exact change required": `'--tools','Read','Grep','Glob'`, `'--add-dir', surveyDir`, `'--permission-mode','bypassPermissions'`; KEEP `'--setting-sources','project'`, `'--strict-mcp-config'`, `'--exclude-dynamic-system-prompt-sections'`. (3) `export function createClaudeHeadlessSurveyClient(config, surveyDir)` — copy the existing spawn body but call `buildSurveyHeadlessArgs(useModel, SURVEY_SYSTEM, surveyDir)` and keep `cwd: os.tmpdir()` (the defensive tmpdir + --add-dir posture, Pitfall 4 — does NOT load the target repo's project hooks). Preserve verbatim: the `delete childEnv['ANTHROPIC_API_KEY']` / `ANTHROPIC_AUTH_TOKEN` strip, the timeout/SIGKILL handling, the JSON-envelope `.result` parse, and the fail-safe-empty contract. The shared timeout var `RECENSE_CLAUDE_HEADLESS_TIMEOUT_MS` is already honored. Factor the common spawn body if it reduces duplication, but the existing path's externally-observable behavior must not change.

    EARLY TRANSPORT SMOKE-CHECK (run ONCE, manual — do NOT add to the CI/vitest suite). RESEARCH Seam 1 probed `--add-dir`, `--permission-mode`, and `--tools Read Grep Glob` as SEPARATE elements; the exact 3-flag UNION used by `buildSurveyHeadlessArgs` (`cwd: os.tmpdir()` + `--add-dir <dir>` + `--permission-mode bypassPermissions` + `--tools Read Grep Glob`) was never run as one probe, and the argv-shape unit tests above prove the flags are PASSED, not that tools actually READ files. This union is the single load-bearing assumption of the whole phase, so prove it behaviorally NOW in Wave 1 (not at the Wave-3 human checkpoint, which is too late to catch a transport regression cheaply). Steps: (a) `mktemp -d` a fixture dir and write a single `README.md` into it containing a unique sentinel string (e.g. `SMOKE-FIXTURE-MARKER-<random>`); (b) invoke the survey transport ONCE against that fixture using the EXACT survey argv — a one-line `claude -p --output-format json --tools Read Grep Glob --add-dir <fixture> --permission-mode bypassPermissions --setting-sources project --system-prompt "<SURVEY_SYSTEM>"` run from a neutral tmpdir cwd, prompting the agent to read the fixture's README and report the sentinel. ASSERT from the JSON envelope: `.result` is NOT the string `NO_TOOLS`; `permission_denials` is `[]`; the run takes meaningfully longer than a tool-less reply (real tool turns occurred); and `.result` echoes the fixture's actual sentinel content (proving a real read, not a hallucination). This call is subscription-billed + non-deterministic — that is exactly why it stays a one-time manual gate and never enters vitest. If it returns `NO_TOOLS` or any denial, STOP: the survey argv is wrong and the phase is blocked — do NOT proceed to Plan 02. Record the smoke-check command + the observed result (sentinel echoed, 0 denials, NOT NO_TOOLS) in the plan SUMMARY. This moves transport-regression detection to Wave 1; the Wave-3 checkpoint remains the full SC2 genuine-ratio validation, not a transport smoke test.
  </action>
  <verify>
    <automated>cd /Users/vtx/brain-memory && npx vitest run tests/claude-headless-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `claude-headless-client.ts` exports `createClaudeHeadlessSurveyClient`, `buildSurveyHeadlessArgs`, `SURVEY_SYSTEM`.
    - New tests in `tests/claude-headless-client.test.ts` assert the survey argv passes `'--tools','Read','Grep','Glob'`, `'--add-dir', surveyDir`, `'--permission-mode','bypassPermissions'`, AND does NOT contain `'--tools','none'`.
    - New test asserts the survey spawn has `cwd === os.tmpdir()` and `env.ANTHROPIC_API_KEY === undefined` and `--setting-sources project`.
    - The PRE-EXISTING test asserting the default path carries `--tools none` (tests/claude-headless-client.test.ts ~line 79-80) still passes UNCHANGED — assert this by running the full file green.
    - `grep -nE "buildHeadlessArgs\b" src/model/claude-headless-client.ts` shows the original `buildHeadlessArgs` body still returns `'--tools','none'` (existing extractor/judge path untouched).
    - MANUAL SMOKE-CHECK (one-time, not in CI): the documented one-line `claude -p` survey-argv command against the tmp fixture returned `.result` NOT equal to `NO_TOOLS`, `permission_denials: []`, and echoed the fixture sentinel — and this command + result are recorded in 30-01-SUMMARY.md. (If `NO_TOOLS`/denials, the phase is blocked.)
    - `npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <done>A second, opt-in survey transport exists that grants read-only Read/Grep/Glob over an arbitrary `<dir>` via `--add-dir` from a neutral tmpdir cwd, with both load-bearing guards preserved; the exact 3-flag union is behaviorally smoke-proven against a fixture in Wave 1 (NOT NO_TOOLS, 0 denials, sentinel echoed); the existing judge/extractor `--tools none` + tmpdir path is byte-for-byte unchanged and its regression tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-supplied `<dir>` → survey agent | The agent gets read tool access to an arbitrary directory the operator names |
| survey agent (claude -p) → headless transport | Untrusted target-repo files/hooks could attempt to influence the spawned agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Elevation of Privilege | survey transport tool set | mitigate | Survey path passes ONLY `--tools Read Grep Glob` (read-only); no Bash/Write/Edit, so a malicious target repo cannot trigger writes/exec. Asserted in the unit test. |
| T-30-02 | Tampering | target-repo project hooks/CLAUDE.md | mitigate | Spawn with `cwd: os.tmpdir()` + `--add-dir <dir>` (RESEARCH Pitfall 4 / Test B) so the target repo's project hooks are NOT loaded; `--setting-sources project` from a neutral cwd loads no project settings. Documented + asserted (cwd === os.tmpdir()). |
| T-30-03 | Spoofing / billing | claude -p auth | mitigate | Preserve the `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN` strip verbatim on the survey path so calls bill the Max subscription, not the direct API. Asserted in the unit test. |
| T-30-04 | Repudiation / self-ingestion | global UserPromptSubmit capture hook | mitigate | Preserve `--setting-sources project` verbatim so the survey's own `claude -p` calls are not re-captured as episodes (self-ingestion loop). Asserted in the unit test. |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No new packages added this phase (net-zero deps engine invariant); `package.json` unchanged. No install tasks → no Package Legitimacy Gate needed. |
</threat_model>

<verification>
- `npx vitest run tests/survey-observations.test.ts tests/claude-headless-client.test.ts` — all green.
- `npx tsc --noEmit` clean.
- Manual transport smoke-check passed (NOT NO_TOOLS, 0 denials, fixture sentinel echoed) and is recorded in 30-01-SUMMARY.md.
- `git diff --stat` shows only the two source files + two test files changed; `scripts/spike/survey-feeder.ts` untouched; `package.json` unchanged (net-zero deps).
</verification>

<success_criteria>
- The opt-in survey transport reads an arbitrary `<dir>` (read-only tools, neutral tmpdir cwd, both guards preserved) — behaviorally proven against a fixture in Wave 1.
- The existing judge/extractor `--tools none` path is unchanged and its regression tests pass.
- `splitObservations` + `isRefusalOrToolFailure` + `buildSurveyPrompt` are importable, unit-tested pure functions ready for Plan 02.
</success_criteria>

<output>
Create `.planning/phases/30-core-ingest-command/30-01-SUMMARY.md` when done.
</output>
