---
phase: 62-multi-inbox-email-ingest-hardening
plan: 28
type: execute
wave: 21
depends_on: ["62-26"]
gap_closure: true
files_modified:
  - tests/support/html-render-oracle.ts
  - tests/support/differential-helpers.ts
  - tests/html-wrapper-differential.test.ts
  - tests/css-liveness-differential.test.ts
autonomous: true
requirements: [EMAIL-03]
user_setup: []

must_haves:
  truths:
    - "An HTML-layer divergence between production and a conformant browser is detectable by a shipped test — the coverage band that let CR-01, CR-03 and WR-01 ship under a green 3287-test suite"
    - "The ground truth for that band comes from a parser production does not use (parse5), so the judge cannot inherit the judged's parsing opinion"
    - "The differential is proven non-vacuous: run against the unfixed module at HEAD it independently rediscovers the CR-01 and CR-03 families, and that output is recorded verbatim"
    - "A divergence matching no named mechanism fails the suite"
  artifacts:
    - path: "tests/support/html-render-oracle.ts"
      provides: "parse5-derived rendered-text ground truth for a fixed hiding rule"
      min_lines: 80
    - path: "tests/html-wrapper-differential.test.ts"
      provides: "HTML-wrapper generator crossed against a fixed hiding rule, hard-gated"
      min_lines: 200
    - path: "tests/support/differential-helpers.ts"
      provides: "The ONE definition site for the marker scheme and the failure gate, shared by both differentials"
  key_links:
    - from: "tests/support/html-render-oracle.ts"
      to: "parse5"
      via: "parseFragment tree walk, never htmlparser2 and never src/"
      pattern: "from 'parse5'"
    - from: "tests/html-wrapper-differential.test.ts"
      to: "failures -> throw"
      via: "any divergence matching no named HF-* mechanism"
      pattern: "failures.push"
---

<objective>
Build the missing differential: vary the HTML wrapper, fix the CSS, and adjudicate with a parser
production does not share.

Purpose: this is the structural gap (`62-VERIFICATION.md` WR-03, status `partial`), and it outranks
the three point fixes. `tests/css-liveness-differential.test.ts:510-515` holds the wrapper fixed at
`'<style>' + css + '</style>' + ...` and varies only the CSS. Plans 62-21, 62-22 and 62-24 rewrote the
ENTIRE HTML layer. `tests/html-parser-conformance.test.ts` exercises htmlparser2 in isolation and must
never import the stripper. Net result, measured this pass: 198 files, 3287 passed / 4 expected-fail —
green — while three injection carriers leak. Six rounds of point fixes have each been followed by a
new leak one band over; without this differential the seventh round produces an eighth.

The verifier's stated fix direction is precise: "An HTML-wrapper generator crossed against a fixed
hiding rule, with ground truth from a parser production does not share (parse5, already evaluated in
62-21)."

This plan lands BEFORE any of this round's production fixes, on purpose. Its non-vacuousness is not
argued — it is demonstrated by running it against the unfixed module at HEAD and recording the
failures it finds on its own.

Output: a parse5-derived rendered-text oracle, an HTML-wrapper differential that hard-fails on
unnamed divergence, and the verbatim record of it independently rediscovering CR-01 and CR-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

@.planning/phases/62-multi-inbox-email-ingest-hardening/62-VERIFICATION.md
@.planning/phases/62-multi-inbox-email-ingest-hardening/62-REVIEW.md
@.planning/phases/62-multi-inbox-email-ingest-hardening/62-21-SUMMARY.md
@.planning/phases/62-multi-inbox-email-ingest-hardening/62-25-PLAN.md
@.planning/phases/62-multi-inbox-email-ingest-hardening/62-25-SUMMARY.md
@.planning/phases/62-multi-inbox-email-ingest-hardening/62-26-SUMMARY.md
@tests/css-liveness-differential.test.ts
@tests/support/css-liveness-oracle.ts
@tests/html-parser-conformance.test.ts

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

Production entry point under test (the ONLY production import this plan's test file makes):

  // src/source/strip-hidden.ts
  export function stripHiddenContent(text: string): string;

Existing marker convention to reuse verbatim (tests/css-liveness-differential.test.ts:470-478) —
delimiter-bounded so no probe name's marker is a substring of another's:

  function classMarker(name: string): string { return `PL[${name}]`; }
  function idMarker(name: string): string   { return `PLID[${name}]`; }

Existing hard-gate shape to reuse (tests/css-liveness-differential.test.ts:551-558):

  function throwIfFailures(failures: string[], contextLabel: string): void;

parse5@7.3.0 surface this plan uses (installed by 62-26, dual CJS/ESM, bundled types):

  import { parseFragment } from 'parse5';
  // parseFragment(html) -> DocumentFragment; nodes carry `nodeName`, `tagName`,
  // `attrs: Array<{name,value}>` (entity-DECODED), `childNodes`, and for text nodes
  // `nodeName === '#text'` with `value`. `<template>` content lives on `node.content`.
  // Default `scriptingEnabled: true` — <noscript> content is raw text, not elements.
</interfaces>

<file_ownership_rationale>
Wave 21, sole plan in its wave. It creates two new support files and the new differential, and it
additionally writes `tests/css-liveness-differential.test.ts` for ONE purpose: migrating the marker
helpers and `throwIfFailures` out of it into `tests/support/differential-helpers.ts` so there is
exactly one definition site. That file is owned by 62-27 in wave 20 — an EARLIER wave — so the write
is ordered, not concurrent, and no wave contains two writers of it. This mirrors the same pattern
62-29 uses in wave 22 against this plan's own differential file.
</file_ownership_rationale>

<oracle_independence_rules>
Non-negotiable, and each is asserted by grep in the acceptance criteria:

1. `tests/support/html-render-oracle.ts` imports NOTHING from `src/`. It cannot see production.
2. It imports NOTHING from `htmlparser2`. That is the parser production uses; a judge sharing the
   judged's parser is the WR-09 root cause this phase has already reproduced twice (once in the
   oracle's declaration layer, closed by 62-25; once here, one layer up).
3. It does not re-implement any production algorithm. It answers ONE question — "what text would a
   browser show a human?" — from parse5's tree plus the fixed hiding rule. Every rule it encodes must
   be traceable to an HTML/CSS spec statement written in its doc comment, not to a line of
   `strip-hidden.ts`.
4. The differential test file may import `stripHiddenContent` — it is the thing under test. The
   ORACLE file may not.
</oracle_independence_rules>

<why_the_css_is_fixed_and_the_html_varies>
The existing differential already covers the CSS axis exhaustively and is now hard-gated (62-25).
Crossing both axes would multiply the case count without adding adjudication power, and would make
every divergence ambiguous between the two layers. Fixing the CSS to ONE trivially-understood rule
(`.legal{display:none}` / `#legal{display:none}`) collapses the oracle's CSS work to "is this
stylesheet applied at all", which needs no CSS engine — so every remaining degree of freedom, and
therefore every divergence this file reports, is HTML-layer. That is exactly the uncovered band.
</why_the_css_is_fixed_and_the_html_varies>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parse5-derived rendered-text oracle for a fixed hiding rule</name>
  <files>tests/support/html-render-oracle.ts</files>
  <read_first>
    - `tests/support/css-liveness-oracle.ts` in full — the house shape for an oracle module in this
      phase: exported verdict type, doc block stating what it shares and what it does not, parse
      errors swallowed browser-style
    - `62-21-SUMMARY.md` lines 80-87 (the C2 source-location and attribute-decode findings for
      parse5) and lines 56-60 (the C3 totality failure and the two htmlparser2 RAWTEXT defects) —
      what parse5 gets right is exactly what this oracle needs
    - `tests/html-parser-conformance.test.ts:283` — the §13.2.5 assertion that `<style/>` is a
      spec-correct no-op and the element opens normally; the oracle must agree with it
    - `62-26-SUMMARY.md` — the recorded probe output showing parse5 opening `<style/>` as a real
      style element
  </read_first>
  <behavior>
    `renderedText(html)` returns `{ text, oracleUnavailable }` where `text` is the prose a browser
    would show a human, given the FIXED rule `.legal{display:none}` (and its `#legal` twin).

    Rules, each with its spec citation in the doc comment:
      - Text inside `script`, `style`, `head`, `title`, `template`, `noscript`, `svg`, `iframe`,
        `noembed`, `noframes` contributes NOTHING. Fallback content of `iframe`/`noembed`/`noframes`
        is never rendered by any modern browser; `template` content is inert; with parse5's default
        `scriptingEnabled: true`, `noscript` content is raw text.
      - A `<style>` element's text is a stylesheet, never prose. The stylesheet is APPLIED iff parse5
        recognized it as a real `style` ELEMENT (not as raw text inside a never-applied ancestor —
        parse5 resolves that itself, since `iframe`/`noembed`/`noframes`/`noscript` content is
        tokenized as raw text, so a `<style>` written inside them is not an element at all).
      - An element is HIDDEN iff some applied stylesheet in the document contains the fixed rule AND
        the element's decoded `class` list contains `legal` (or its decoded `id` equals `legal`).
        A hidden element contributes no text, and neither does its subtree.
      - Every other text node contributes its `value` verbatim.

    Asserted behaviors (write these as tests in the differential file or a colocated block; each is a
    fact about the ORACLE, not about production):
      - `renderedText('<style>.legal{display:none}</style>ok<span class="legal">PL[legal]</span>')`
        -> text contains `ok`, does NOT contain `PL[legal]`.
      - `renderedText('<style/>.legal{display:none}</style>ok<span class="legal">PL[legal]</span>')`
        -> IDENTICAL verdict to the line above. This is the §13.2.5 rule and the whole point.
      - `renderedText('<iframe>INJECT</iframe>ok')` -> text is `ok`; `noembed` and `noframes` the same.
      - `renderedText('<xmp>KEEP</xmp>ok')` and `renderedText('<textarea>KEEP</textarea>ok')` ->
        text CONTAINS `KEEP` (both are rendered; they are the over-strip controls).
      - `renderedText('<iframe><style>.legal{display:none}</style></iframe>ok<span class="legal">PL[legal]</span>')`
        -> text contains BOTH `ok` and `PL[legal]`: the stylesheet is inside raw text and is never
        applied (this is WR-01's ground truth).
      - `renderedText('<span class="leg&#97;l">PL[legal]</span>')` with an applied rule -> hidden
        (parse5 decodes the attribute).
      - A `parse5` throw sets `oracleUnavailable: true` and returns empty text; it is never swallowed
        into a normal verdict.
  </behavior>
  <action>
    Create `tests/support/html-render-oracle.ts`. Export the verdict type and `renderedText`. Walk
    parse5's `parseFragment` tree in two passes: first collect applied stylesheet text (so a rule
    declared after the element it hides still applies, as in a browser), then walk again accumulating
    text with the never-rendered and hidden-element skips.

    Write the doc block in the house style established by `tests/support/css-liveness-oracle.ts`:
    state plainly what this oracle shares with production (nothing but the JavaScript runtime), what
    it deliberately does not model (cascade, specificity, inheritance, `@media`, external
    stylesheets, `visibility`/`opacity`/geometry hiding — the fixed rule needs none of them), and
    what remains a blind spot. An oracle with unstated blind spots is this phase's root cause; do not
    replace one overclaim with another.

    Do not implement CSS parsing. The rule is fixed and its presence in a stylesheet is a
    whitespace-insensitive literal check — write it that way and say so.
  </action>
  <verify>
    <automated>npm run typecheck && test "$(grep -c "from '\.\./\.\./src\|from '\.\./src\|htmlparser2" tests/support/html-render-oracle.ts)" = "0" && echo "oracle imports neither src/ nor htmlparser2" && grep -q "from 'parse5'" tests/support/html-render-oracle.ts && echo "oracle uses parse5"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "src/" tests/support/html-render-oracle.ts` is 0 and `grep -c "htmlparser2" tests/support/html-render-oracle.ts` is 0.
    - `grep -c "from 'parse5'" tests/support/html-render-oracle.ts` is 1.
    - All eight behaviors in `<behavior>` are asserted and pass.
    - The doc block names each never-rendered element with its reason, and lists the oracle's blind
      spots explicitly.
    - `npm run typecheck` exits 0 (both projects — 62-27 wired this into CI).
  </acceptance_criteria>
  <done>
    A ground truth for "what a human sees" exists, computed by a parser production does not use.
  </done>
</task>

<task type="auto">
  <name>Task 2: HTML-wrapper generator — run it against the unfixed module and record what it finds</name>
  <files>tests/support/differential-helpers.ts, tests/html-wrapper-differential.test.ts, tests/css-liveness-differential.test.ts</files>
  <read_first>
    - `tests/css-liveness-differential.test.ts:466-558` — `truthSummary`, the marker helpers,
      `runDifferential`, `throwIfFailures`: the structure and failure-message discipline to mirror
      (generating input, oracle verdict, observed output, failing probe, all verbatim)
    - `62-VERIFICATION.md` "Independent Reproductions" table (lines 113-124) — the exact payloads and
      outputs this generator must be capable of producing on its own
    - `62-REVIEW.md` CR-01 (lines 72-129), CR-03 (lines 194-231), WR-01 (lines 235-256), WR-10
      (lines 393-411) — the four HTML-layer shapes with their repro rows
    - Task 1's oracle module
    - `62-27-SUMMARY.md` — 62-27 already factored the CSS differential's wrapper into a single helper
      and asserted it by grep count; this task applies the same rule to the marker scheme and the gate
  </read_first>
  <behavior>
    One fixed CSS string, `.legal{display:none}` for the class probe and `#legal{display:none}` for
    the id probe. One fixed visible sentence, `VISIBLE_SENTENCE`. One fixed payload marker per probe,
    reusing the existing `PL[name]` / `PLID[name]` convention.

    A wrapper alphabet of at least these 22 shapes, crossed against {class, id} probes and against
    {payload inside the wrapper, payload after the wrapper}:

      1.  `<style>CSS</style>`                       (control — must be correct today)
      2.  `<style/>CSS</style>`                      (CR-01 a)
      3.  `<style />CSS</style>`                     (CR-01 b)
      4.  `<STYLE/>CSS</STYLE>`                      (CR-01 c)
      5.  `<style type=a/>CSS</style>`               (CR-01 d)
      6.  `<style>CSS</style foo>`                   (D-62-21-01 close-tag residual)
      7.  `<style>CSS</style/>`                      (RAWTEXT close-slash defect)
      8.  `<style>CSS`                               (unterminated to EOF)
      9.  `<!--<style>--><style>CSS</style>`         (CR-10 regression lock)
      10. `<iframe>PAYLOAD</iframe>`                 (CR-03)
      11. `<noembed>PAYLOAD</noembed>`               (CR-03)
      12. `<noframes>PAYLOAD</noframes>`             (CR-03)
      13. `<xmp>PAYLOAD</xmp>`                       (rendered — over-strip control)
      14. `<textarea>PAYLOAD</textarea>`             (rendered — over-strip control)
      15. `<template>PAYLOAD</template>`             (inert)
      16. `<noscript><style>CSS</style></noscript>`  (WR-01)
      17. `<iframe><style>CSS</style></iframe>`      (WR-01)
      18. `<svg><style>CSS</style></svg>`
      19. entity-encoded selector value: `class="leg&#97;l"` / `id="leg&#97;l"`
      20. implied-close siblings: `<p>VISIBLE_SENTENCE<p>PAYLOAD`
      21. `<div style="display:none">PAYLOAD</div foo="a>b">tail`   (WR-10)
      22. unquoted attribute ending in `/`: `<span class=legal/>PAYLOAD</span>`

    For every generated case:
      - `truth = renderedText(html)`; if `truth.oracleUnavailable`, count it and skip (exact count
        asserted; a nonzero count on an alphabet with no lone surrogates is itself a finding).
      - If the oracle renders `VISIBLE_SENTENCE` and production's output does not -> divergence.
      - For each probe marker: oracle-hidden + present in output -> UNDER-STRIP divergence (a leak).
        Oracle-rendered + absent from output -> OVER-STRIP divergence.
      - Every divergence carries the shape id, the full generating HTML, the oracle's rendered text
        and production's output, all verbatim, so the case is reproducible from the log alone.

    This task's deliverable is the generator plus the RECORDED OUTPUT of running it against the
    module as it exists at HEAD — unfixed. It is expected to report divergences. That is the point.
  </behavior>
  <action>
    **First, extract — do not copy.** Move `classMarker`, `idMarker` and `throwIfFailures` out of
    `tests/css-liveness-differential.test.ts` into a new `tests/support/differential-helpers.ts`, and
    import them from BOTH differentials. Copying them into the new file is prohibited: a second
    hand-maintained copy of a shared scheme is the exact failure mode this round exists to close, and
    sibling plan 62-27 already applied the stricter rule to the wrapper string in the same file. There
    must be exactly one definition site for each of the three, repo-wide.

    Then create `tests/html-wrapper-differential.test.ts`. Build the shape alphabet as a named,
    documented table (shape id, template, one-line reason it is in the alphabet) so a future author
    adds rows rather than editing a string concatenation.

    Run the generator. Capture the complete divergence report verbatim. Do not fix production, do not
    weaken an assertion, do not delete a shape because it fails. Task 3 dispositions the output;
    this task produces it.

    The recorded report is the plan's central evidence: it must independently exhibit at least the
    CR-01 family (shapes 2-5 leaking where shape 1 does not) and the CR-03 family (shapes 10-12
    emitting their fallback text), discovered by the generator rather than transcribed from the
    verification report.
  </action>
  <verify>
    <automated>npx vitest run tests/html-wrapper-differential.test.ts 2>&1 | tee /tmp/62-28-red.txt; npx vitest run tests/css-liveness-differential.test.ts && npm run typecheck && test "$(grep -rn 'function classMarker\|function idMarker\|function throwIfFailures' tests/ | wc -l | tr -d ' ')" = "3" && echo "exactly one definition site each"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "function classMarker\|function idMarker\|function throwIfFailures" tests/ | wc -l` is
      exactly 3 — one definition each, all three in `tests/support/differential-helpers.ts`, and both
      differentials import them. No copy exists in either test file.
    - `tests/css-liveness-differential.test.ts` still passes unchanged in behavior after the
      extraction: its per-mechanism exact counts are identical to the values 62-27 recorded, quoted in
      the SUMMARY as before/after.
    - The alphabet contains at least the 22 shapes enumerated in `<behavior>`, each with an id and a
      documented reason.
    - The captured report is quoted verbatim in the SUMMARY and contains, at minimum: an UNDER-STRIP
      divergence for each of shapes 2, 3, 4, 5 with shape 1 diverging on NONE of them; and an
      UNDER-STRIP divergence for each of shapes 10, 11, 12.
    - The SUMMARY states, explicitly, that these were produced by running the generator against the
      unfixed module — with the command and the `git rev-parse HEAD` it ran at.
    - No production file is modified by this task: `git status --porcelain src/` is empty.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>
    The differential exists and has independently rediscovered the round's blockers, on the record.
  </done>
</task>

<task type="auto">
  <name>Task 3: Hard-gate the differential, name the open families, and prove the gate blocks</name>
  <files>tests/html-wrapper-differential.test.ts</files>
  <read_first>
    - Task 2's captured report
    - `tests/css-liveness-differential.test.ts:408-439` and its `it.fails` locks — the exact house
      pattern for "named mechanism + exact count + minimal reproduction"
    - `62-25-PLAN.md` Task 3 and `62-25-SUMMARY.md` — the injection-proof pattern this phase accepts
      as evidence that a gate blocks
    - `62-VERIFICATION.md` "Gaps Summary" (lines 239-247) — why a bounded counter is not a gate
  </read_first>
  <behavior>
    Any divergence that matches NO named mechanism reaches `failures` and throws. Named mechanisms
    for the families Task 2 found, each with a structural predicate keyed to the SHAPE ID (not to a
    substring of the input), an exact count, and a minimal `it.fails` reproduction:

      - `HF-01` — self-closing start-tag syntax on a RAWTEXT element excludes it from the harvest
        while stage 4/5 deletes it anyway (shapes 2-5). Owner: plan 62-29 (CR-01).
      - `HF-02` — `iframe` / `noembed` / `noframes` fallback content emitted verbatim (shapes 10-12).
        Owner: plan 62-29 (CR-03).
      - `HF-03` — a stylesheet inside a never-applied context is harvested and deletes visible prose
        (shapes 16-17, OVER-STRIP direction). Owner: plan 62-29 (WR-01).
      - one named mechanism per additional family Task 2 surfaced, if any.

    Per WR-04's lesson, the `it.fails` bodies assert production behavior ONLY; the oracle
    precondition for each lock lives in its own passing `it`.

    The named predicates are TEMPORARY and say so in their doc comments: plan 62-29 deletes each one
    together with its lock and replaces it with a passing assertion. A predicate that outlives its
    fix is a tolerance, which is the CR-11 defect wearing a different hat.
  </behavior>
  <action>
    Add the gate and the named mechanisms per `<behavior>`. Assert exact counts per mechanism and an
    exact count for the oracle-unavailable bucket. Do not add any magnitude bound: `grep -c
    toBeLessThan` on this file must be 0.

    **Injection proof.** Demonstrate the gate genuinely blocks rather than passing because every
    divergence found a name: wrap `stripHiddenContent` in this test with a thin injector that, for
    ONE specific generated shape, returns output still containing the probe marker — then run the
    file and capture the failure output verbatim. Remove the injector, re-run green, and show `git
    status --porcelain` clean. Record the command, the failure text and the restoration. Do not
    commit the injector.

    **Header doc comment.** State what this file is for (the WR-03 coverage gap), why the CSS is
    fixed and the HTML varies, that the ground truth is parse5 and production is htmlparser2, and
    that every HF-* predicate is scheduled for deletion by its owning plan. Name the shipped counts.
  </action>
  <verify>
    <automated>npx vitest run tests/html-wrapper-differential.test.ts && npx vitest run && npm run typecheck && npm run build && test "$(grep -c 'toBeLessThan' tests/html-wrapper-differential.test.ts)" = "0" && echo "no magnitude bounds" && test -z "$(git status --porcelain src/)" && echo "production untouched"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "toBeLessThan" tests/html-wrapper-differential.test.ts` is 0.
    - Every HF-* predicate keys on a shape id, has an exact count assertion, has a minimal `it.fails`
      reproduction, has a named owning plan in its doc comment, and has its oracle precondition in a
      separate passing `it`.
    - The injection proof's command, verbatim failure output, and restoration are recorded in the
      SUMMARY; the file is green afterwards and the injector is not committed.
    - Full suite `npx vitest run` green; `npm run typecheck` exits 0; `npm run build` exits 0.
    - `git status --porcelain src/` is empty — this plan changes no production code.
    - The SUMMARY makes no claim that the HTML layer is now covered; it states which shapes are in
      the alphabet and which mechanisms are open, and nothing wider.
  </acceptance_criteria>
  <done>
    An HTML-layer divergence nobody has named fails the suite, and the gate has been observed failing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated email HTML -> `stripHiddenContent` | The sender-controlled markup boundary; every carrier in this round crosses it |
| ground-truth oracle -> every assertion built on it | A judge that shares the judged's parser cannot adjudicate a parsing disagreement — the WR-09 root cause, displaced one layer up |
| named HF-* predicate -> suite verdict | A predicate that outlives its fix becomes a permanent amnesty for a live leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-28-01 | Information disclosure | HTML-layer injection carriers undetectable by any shipped test (WR-03) | mitigate | Wrapper generator over 22 named shapes crossed with class/id probes and payload placement, adjudicated by parse5 |
| T-62-28-02 | Spoofing | oracle inheriting production's parsing opinion | mitigate | Oracle imports neither `src/` nor `htmlparser2`, asserted by grep; parse5 is the only parser it uses |
| T-62-28-03 | Repudiation | a gate that has never been observed failing | mitigate | Task 3's injection proof records verbatim failure output and the restoration |
| T-62-28-06 | Tampering | a second hand-maintained copy of the marker/gate scheme | mitigate | `classMarker`, `idMarker` and `throwIfFailures` are extracted to `tests/support/differential-helpers.ts` and imported by both differentials; asserted by a repo-wide grep count of exactly 3 definition sites |
| T-62-28-04 | Tampering | a named predicate widening into a tolerance | mitigate | Each HF-* keys on a shape id, carries an exact count and a minimal lock, names its owning plan, and is deleted by that plan |
| T-62-28-05 | Denial of service | parse5's known non-totality (62-21 C3) reaching a decision path | mitigate | Alphabet contains no lone surrogates; any throw sets `oracleUnavailable` and is counted exactly, never swallowed into a verdict |
</threat_model>

<verification>
- `npx vitest run` full suite green; `npm run typecheck` exits 0; `npm run build` exits 0.
- The oracle imports neither `src/` nor `htmlparser2`; it imports parse5.
- Exactly three definition sites repo-wide for `classMarker` / `idMarker` / `throwIfFailures`, all in `tests/support/differential-helpers.ts`; the CSS differential's counts are unchanged by the extraction.
- No `toBeLessThan` in the new differential; every HF-* has an exact count and a minimal lock.
- The pre-fix divergence report is recorded verbatim and independently exhibits the CR-01 and CR-03 families.
- The injection proof is recorded and the injector is not committed.
- `git status --porcelain src/` empty.
- Frozen surface untouched: `git log --oneline 4354c81..HEAD -- src/adapter/gmail-auth-cli.ts src/adapter/runtime-config.ts src/adapter/recense-doctor.ts src/lib/config.ts src/consolidation/episode-order.ts` returns 0 lines.
</verification>

<success_criteria>
- The HTML band that hid CR-01, CR-03 and WR-01 under a green suite is now differentially tested.
- Ground truth comes from parse5; production uses htmlparser2; the two are adjudicated against each other.
- The differential independently rediscovered this round's blockers before any fix landed, on the record.
- A divergence with no name fails the suite, demonstrated by injection.
</success_criteria>

<output>
Create `.planning/phases/62-multi-inbox-email-ingest-hardening/62-28-SUMMARY.md` when done.

Include: the full shape alphabet table; the verbatim pre-fix divergence report with the command and
commit it ran at; the per-mechanism exact counts; the injection proof (command, verbatim failure,
restoration); and the oracle's stated blind spots. State plainly that this plan closes WR-03's
detection gap only — it fixes no leak — and name HF-01/HF-02/HF-03 as open with 62-29 as owner.
</output>
