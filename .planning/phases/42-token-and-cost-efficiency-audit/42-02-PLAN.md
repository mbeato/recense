---
phase: 42-token-and-cost-efficiency-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/eval/42-progressive-disclosure-harness.cjs
  - scripts/eval/results/42-progressive-disclosure-PENDING.json
autonomous: true
requirements: [COST-04]
must_haves:
  truths:
    - "Progressive disclosure (fact-index → fact-detail, the literal claude-mem search→get_observations mechanism) is benchmarked head-to-head against recense's current one-shot bounded inject (D-07, D-08, COST-04)"
    - "The challenger is bracketed by an oracle policy (best case) and a fixed-top-K policy (realistic case) so the win is not overclaimed (D-09)"
    - "The harness is LLM-free / $0 — no engine change, no MCP call, no new LLM/embedding/judge import (D-09)"
    - "A documented decline-with-numbers (incumbent wins) is recorded as a valid, expected verdict, not a failure (D-10)"
  artifacts:
    - path: "scripts/eval/42-progressive-disclosure-harness.cjs"
      provides: "A/B harness: incumbent=session-start-cli spawn, challenger=simulated two-step fact-index/detail with oracle + fixed-top-K brackets; reduction % + verdict"
      min_lines: 150
    - path: "scripts/eval/results/42-progressive-disclosure-PENDING.json"
      provides: "Incumbent vs challenger token blocks, oracle/top-K reduction %, verdict, caveats"
  key_links:
    - from: "scripts/eval/42-progressive-disclosure-harness.cjs"
      to: "dist/src/adapter/session-start-cli.js"
      via: "spawnSync incumbent arm (one-shot bounded inject)"
      pattern: "session-start-cli"
    - from: "scripts/eval/42-progressive-disclosure-harness.cjs"
      to: "live recense.db"
      via: "readonly node-row query for thin-index + detail simulation"
      pattern: "readonly: true"
---

<objective>
Build and RUN now (LLM-free, $0) the progressive-disclosure A/B harness: claude-mem's fact-index→fact-detail mechanism (challenger) vs recense's current one-shot bounded inject (incumbent), bracketed by an oracle and a fixed-top-K expansion policy. This is COST-04's measured evaluation; per D-09 it is harness-only with NO engine change, and per D-10 a documented decline-with-numbers is a valid outcome.

Purpose: decide, on measured tokens, whether progressive disclosure beats recense's schema-prior compression before any MCP tool is built.
Output: `scripts/eval/42-progressive-disclosure-harness.cjs` + a result JSON with oracle and fixed-top-K reduction % reported separately and an explicit verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-token-and-cost-efficiency-audit/42-CONTEXT.md
@.planning/phases/42-token-and-cost-efficiency-audit/42-PATTERNS.md

Mechanism (D-07): step 1 = compact fact hits (id + one-line gloss only); step 2 = full value + provenance + 1-hop neighborhood on demand. Surface (D-08): MCP interactive pull is the challenger; SessionStart one-shot inject is the incumbent. Brackets (D-09): oracle = expand only the facts the gold answer needs (best case); fixed-top-K = expand all TOP_K hits (realistic). The real `memory_search`→`memory_expand` MCP tool is built only if this A/B shows a win — out of scope here (D-09, deferred).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the progressive-disclosure A/B harness (incumbent spawn + challenger two-step simulation + verdict)</name>
  <files>scripts/eval/42-progressive-disclosure-harness.cjs</files>
  <read_first>
    - scripts/eval/injection-efficiency-harness.cjs (PRIMARY analog: header lines 1-24, arg parser + readonly DB open + graceful degrade lines 33-97, session-start-cli spawn lines 120-150, result envelope lines 244-280; `--db`/`--cwd`/`--out` flags lines 33-38)
    - scripts/eval/replay-ku-harness.cjs (SECONDARY analog: insight-mode OFF/ON A/B structure + aggregate reduction % lines 867-1014, reduction-% formula lines 993-996; charsToTokens line 105)
    - .planning/phases/42-token-and-cost-efficiency-audit/42-PATTERNS.md (§"42-progressive-disclosure-harness.cjs" — file header lines 248-252, arg+DB lines 256-282, charsToTokens lines 286-293, incumbent spawn lines 297-327, challenger simulation lines 331-360, A/B split + verdict lines 364-389, result envelope lines 393-444; shared no-secret guard T-26-03)
    - src/adapter/session-start-cli.ts (the incumbent surface being spawned — read its stdin/stdout JSON contract: hookEventName SessionStart → hookSpecificOutput.additionalContext)
  </read_first>
  <action>
    Create `scripts/eval/42-progressive-disclosure-harness.cjs`. Copy the injection-efficiency-harness header invariants: `$0, LLM-free`; "No LLM/embedding/judge module is imported or invoked"; two policies bracketed (oracle best-case, fixed top-K realistic); D-10 decline-with-numbers is a valid outcome. Copy verbatim: the `arg` parser, the readonly DB open + no-data graceful exit, `charsToTokens(chars) => Math.round(chars/4)`, the commit/engine_version meta block, the result-write pattern, the FATAL catch, and the T-26-03 no-secret-keys guard.
    Args: `--db` (live recense.db), `--cwd`, `--top-k` (default 5), `--out` (default scripts/eval/results/42-progressive-disclosure-PENDING.json).
    Incumbent arm (recense one-shot bounded inject = "schema-prior compression"): spawnSync `dist/src/adapter/session-start-cli.js --db <DB_PATH>` with stdin `{hookEventName:'SessionStart', cwd}`, parse `hookSpecificOutput.additionalContext`, `incumbent_tokens = charsToTokens(text.length)`.
    Challenger arm (simulated two-step, from live DB rows, NO MCP call, NO engine spawn): step 1 thin index = `[id] gloss` per node where `gloss(value)` = first sentence capped ~60 chars (~15 tokens); select TOP_K candidate nodes by recency/rank over `node WHERE tombstoned=0`; `thin_tokens = charsToTokens(thinIndex.join('\n').length)`. Step 2 detail = full node value (+ a 1-hop neighborhood size proxy via an in-edge/out-edge count query). Compute two expansion policies: oracle = expand only the nodes a gold answer needs (best case — for the harness, the minimal hit set); fixed-top-K = expand all TOP_K hits (realistic). `oracle_tokens = thin_tokens + oracle_expansion_tokens`; `topk_tokens = thin_tokens + topk_expansion_tokens`.
    Reduction % (positive = challenger fewer tokens = challenger wins) per the replay-ku formula for both oracle and top-K, computed against `incumbent_tokens`. Verdict: `challenger-wins-oracle` / `challenger-wins-top-k` / `incumbent-wins-decline-documented` (D-10).
    Result envelope: `meta` with eval='progressive-disclosure', mechanism='fact-index-to-fact-detail' (D-07), surface='mcp-pull-challenger-vs-session-start-push-incumbent' (D-08), prototype_depth='harness-only-ab' (D-09); `incumbent` block; `challenger.oracle` + `challenger.top_k` blocks (thin_index_tokens, expansion_tokens, total_tokens, reduction_pct each); `verdict`; `caveats` (chars/4 proxy not a real tokenizer; incumbent from real spawn; challenger simulated from DB rows; oracle=best case; decline is valid per D-10; subscription marginal ≈ $0).
    Do NOT change any engine source, MCP tool, or session-start-cli — this is harness-only (D-09).
  </action>
  <verify>
    <automated>npm run build && node scripts/eval/42-progressive-disclosure-harness.cjs --db /tmp/nonexistent-recense.db --out /tmp/42-pd-dry.json; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/eval/42-progressive-disclosure-harness.cjs --db /tmp/nonexistent-recense.db` exits 0 with a "no data" message (graceful degrade), spawning nothing destructive.
    - The harness spawns `session-start-cli` for the incumbent arm (`grep -c "session-start-cli" scripts/eval/42-progressive-disclosure-harness.cjs` ≥ 1).
    - The harness imports NO LLM/embedding/judge module (`grep -Ec "claude-headless|openai|embed|judge|run-sleep-pass" scripts/eval/42-progressive-disclosure-harness.cjs` filtered of comment lines = 0 for invocation; LLM-free invariant holds).
    - The result envelope emits `challenger.oracle.reduction_pct` and `challenger.top_k.reduction_pct` as SEPARATE fields plus a `verdict`.
    - `verdict` can take the value `incumbent-wins-decline-documented` (the decline path exists in source, D-10).
    - No credential keys appear in the result JSON or console.
    - No engine/MCP/session-start-cli source file is modified (`git status --porcelain src/ | grep -q . && exit 1 || true`).
  </acceptance_criteria>
  <done>The A/B harness builds, dry-runs to exit 0, measures incumbent via real session-start spawn and challenger via LLM-free DB simulation, and emits oracle + top-K reduction % with an explicit verdict including the decline path.</done>
</task>

<task type="auto">
  <name>Task 2: Run the A/B now on the live brain ($0) and record the verdict</name>
  <files>scripts/eval/results/42-progressive-disclosure-PENDING.json</files>
  <read_first>
    - scripts/eval/42-progressive-disclosure-harness.cjs (the harness from Task 1)
    - scripts/eval/injection-efficiency-harness.cjs (confirms the live-db read-only $0 run pattern)
    - .planning/phases/42-token-and-cost-efficiency-audit/42-CONTEXT.md (D-09/D-10 — harness-only, decline is valid)
  </read_first>
  <action>
    Run the harness now against the live recense.db (read-only; LLM-free; $0) at `--top-k 5` (and optionally also `--top-k 10` for a sensitivity row), writing `scripts/eval/results/42-progressive-disclosure-PENDING.json`. Record the verdict and both reduction % figures. If the live db is unavailable in the execution environment, run against the latest available scratch/snapshot db and note the source; do NOT fabricate numbers.
    In the SUMMARY, state the verdict plainly with the numbers (D-10): if the incumbent (recense's bounded inject) wins, that is the expected, valid outcome and the real `memory_search`→`memory_expand` MCP two-step tool is NOT built (D-09 — remains deferred). If the challenger wins on oracle but not fixed-top-K, say so explicitly (the realistic case governs the adopt/decline call).
    Make NO engine or MCP change in this plan regardless of verdict — building the real tool, if warranted, is a separate follow-on (deferred per D-09).
  </action>
  <verify>
    <automated>test -f scripts/eval/results/42-progressive-disclosure-PENDING.json && node -e "const f=require('./scripts/eval/results/42-progressive-disclosure-PENDING.json'); if(typeof f.verdict!=='string'||!f.challenger||!('reduction_pct' in (f.challenger.oracle||{}))){process.exit(1)}"; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The result JSON exists and contains a string `verdict` and both `challenger.oracle.reduction_pct` and `challenger.top_k.reduction_pct`.
    - The harness was run LLM-free (`subscription marginal ≈ $0` recorded in caveats); no paid/API/headless call was made.
    - The SUMMARY states the COST-04 adopt/decline call with the actual numbers; a decline is documented as valid (D-10), not a failure.
    - No engine/MCP source file changed (`git status --porcelain src/` is empty).
  </acceptance_criteria>
  <done>The A/B has been run on the live brain at $0, the verdict + separate oracle/top-K reduction % are recorded, and the adopt-only-on-measured-win / decline-with-numbers call (COST-04, D-10) is documented with no engine change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → live recense.db | Reads the founder's live brain to simulate the challenger; must be read-only (never mutate customer-zero memory). |
| harness → session-start-cli subprocess | Spawns the real inject CLI for the incumbent arm; must pass only `--db` + the SessionStart stdin envelope, no write flags. |
| harness → result JSON / stdout | Output may be committed; must not leak secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | Tampering | 42-progressive-disclosure-harness.cjs DB access | mitigate | Open live db with `{ readonly: true, fileMustExist: true }`; challenger simulation is SELECT-only; incumbent spawn passes no mutating flags. |
| T-42-05 | Information Disclosure | result JSON + console output | mitigate | T-26-03 no-secret guard; only token counts + reduction % written, no node PII dump beyond glosses already bounded, no API keys. |
| T-42-06 | Elevation of Privilege | accidental engine/MCP change | mitigate | Harness-only (D-09); acceptance gate asserts `git status --porcelain src/` empty — no engine, MCP, or session-start-cli modification. |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (net-zero-dep invariant); reuses better-sqlite3 + dist adapter only. |
</threat_model>

<verification>
- `npm run build` succeeds; harness dry-runs to exit 0 on a missing db.
- Result JSON has incumbent + challenger(oracle, top_k) token blocks, separate reduction %, and a verdict including the decline path.
- LLM-free / $0; no engine or MCP change made.
</verification>

<success_criteria>
- COST-04: progressive disclosure is benchmarked vs schema-prior compression on the token axis; the adopt/decline call is made on measured numbers; declining-with-numbers is documented as valid (D-07/D-08/D-09/D-10).
</success_criteria>

<output>
Create `.planning/phases/42-token-and-cost-efficiency-audit/42-02-SUMMARY.md` when done — state the verdict + oracle/top-K reduction % and the adopt/decline decision for COST-04.
</output>
