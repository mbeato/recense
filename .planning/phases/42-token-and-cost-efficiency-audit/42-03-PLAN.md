---
phase: 42-token-and-cost-efficiency-audit
plan: 03
type: execute
wave: 2
depends_on: ["42-01", "42-02"]
files_modified:
  - scripts/eval/results/42-injection-efficiency-PENDING.json
  - .planning/phases/42-token-and-cost-efficiency-audit/42-COST-SAVINGS-REPORT.md
autonomous: true
requirements: [COST-03]
must_haves:
  truths:
    - "recense's own reproduced recall-side % savings (bounded inject vs flat full-context dump) is the headline number, measured from a committed script (D-11, D-12, COST-03)"
    - "Write-side (pay-at-sleep) cost is reported SEPARATELY as an amortized cost + breakeven session count — never netted with recall savings into one flattering number (D-12)"
    - "Each competitor figure (mem0 ~90%, claude-mem ~10x) is cited verbatim with a one-line 'what it actually measures' note (COST-03, Phase-40 D-08)"
    - "The progressive-disclosure A/B verdict (from 42-02) is folded into the report so the comparison vs claude-mem's ~10x mechanism is honest"
  artifacts:
    - path: ".planning/phases/42-token-and-cost-efficiency-audit/42-COST-SAVINGS-REPORT.md"
      provides: "The defensible competitor-savings write-up: recense reproduced number + cited competitor figures with methodology notes, recall headline + write-side separate"
      min_lines: 60
    - path: "scripts/eval/results/42-injection-efficiency-PENDING.json"
      provides: "The reproduced recall-side self-baseline (flat-dump vs bounded inject scaling curve)"
  key_links:
    - from: ".planning/phases/42-token-and-cost-efficiency-audit/42-COST-SAVINGS-REPORT.md"
      to: "scripts/eval/results/42-injection-efficiency-PENDING.json"
      via: "headline recall-side % savings cited from the committed harness output"
      pattern: "injection-efficiency"
    - from: ".planning/phases/42-token-and-cost-efficiency-audit/42-COST-SAVINGS-REPORT.md"
      to: "scripts/eval/results/cost-benefit-probe.json"
      via: "write-side amortized cost + breakeven session count, reported separately"
      pattern: "breakeven"
---

<objective>
Produce the COST-03 defensible competitor-savings claim: recense's OWN reproduced recall-side % savings (bounded inject vs a flat full-context dump, D-11) as the headline (D-12), the write-side pay-at-sleep cost reported SEPARATELY as amortized cost + breakeven session count, and each competitor figure (mem0 ~90%, claude-mem ~10x) cited verbatim with a one-line "what it actually measures" note. No netting, no inflation.

Purpose: turns "we're token-efficient vs competitors" into a falsifiable, reproducible statement with sources.
Output: a reproduced recall-side self-baseline JSON (reusing the existing injection-efficiency harness, $0) + `42-COST-SAVINGS-REPORT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-token-and-cost-efficiency-audit/42-CONTEXT.md
@.planning/phases/42-token-and-cost-efficiency-audit/42-PATTERNS.md
@.planning/phases/42-token-and-cost-efficiency-audit/42-02-SUMMARY.md

Competitor figures to cite verbatim (from `.planning/ROADMAP.md` §Phase 40 + `.planning/phases/40-competitive-benchmark-baseline/40-COMPETITOR-TARGETS.md`): mem0 "~90% fewer tokens vs OpenAI full-context memory"; claude-mem "~10x retrieval-token savings via progressive disclosure". Each needs a one-line "what it measures" note (recall-side only; vendor-published; methodology caveat). Reuse Phase-40 D-08 notes — do not re-derive.

Existing reproduced inputs (no new expensive run needed): `scripts/eval/injection-efficiency-harness.cjs` (LLM-free, $0, computes the flat-file-of-everything-vs-bounded scaling curve = the D-11 self-baseline + belief-correction count; the recall-side reduction figure is emitted at `point_estimate.token_reduction_pct`, with structural blocks `meta` / `point_estimate` / `scaling_projection` / `belief_correction`) and `scripts/eval/results/cost-benefit-probe.json` (measured ~1,470 Haiku tokens/episode write-side) + the cost-benefit-harness breakeven combiner.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce the recall-side self-baseline + capture the write-side breakeven</name>
  <files>scripts/eval/results/42-injection-efficiency-PENDING.json</files>
  <read_first>
    - scripts/eval/injection-efficiency-harness.cjs (the $0 LLM-free harness to run; `--db`/`--cwd`/`--flat`/`--out` flags lines 33-38; flat-dump-vs-bounded scaling curve + belief-correction count it computes; result envelope `{ meta, point_estimate, scaling_projection, belief_correction }` lines 244-280, recall reduction at `point_estimate.token_reduction_pct` line 262)
    - scripts/eval/cost-benefit-harness.cjs (the breakeven combiner — PART B net(N) table; `--read-savings` flag line ~52 to feed the recall savings JSON in)
    - scripts/eval/results/cost-benefit-probe.json (the measured write-side ~1,470 Haiku tok/episode write-ledger to amortize)
    - .planning/phases/42-token-and-cost-efficiency-audit/42-CONTEXT.md (D-11 self-baseline, D-12 headline axis + write-side separate)
  </read_first>
  <action>
    Run `scripts/eval/injection-efficiency-harness.cjs` against the live recense.db (read-only, $0, LLM-free) writing `scripts/eval/results/42-injection-efficiency-PENDING.json`. Pass `--flat <path>` pointing at the flat full-context dump (the old MEMORY.md-of-everything) so the comparison is computed (when the flat file is absent the harness records `point_estimate.flat_missing=true` and a null reduction — provide the flat path so the headline figure is real). This produces the D-11 self-baseline: recense's bounded inject vs the flat dump scaling curve = recense's reproduced recall-side % savings. Capture the headline % (`point_estimate.token_reduction_pct`) at the live-brain node count.
    Then capture the write-side breakeven: feed the recall-savings JSON into the cost-benefit-harness breakeven combiner (`--read-savings <path>`) OR read the existing `cost-benefit-probe.json` write-ledger + the harness's net(N) table — produce the amortized write cost per session and the breakeven session count where recall savings overtake write cost. Do NOT trigger the deferred write-side sleep pass; use the already-measured probe numbers (D-06 — no expensive run this phase).
    Record both numbers (recall-side headline %, write-side amortized cost + breakeven N) for Task 2. If the live db is unavailable, run against the latest snapshot and note the source; never fabricate.
  </action>
  <verify>
    <automated>npm run build && node scripts/eval/injection-efficiency-harness.cjs --db /tmp/nonexistent-recense.db --out /tmp/42-inj-dry.json; echo "smoke_exit=$?"; test -f scripts/eval/results/42-injection-efficiency-PENDING.json && node -e "const f=require('./scripts/eval/results/42-injection-efficiency-PENDING.json'); if(!f.meta||!f.point_estimate||!f.scaling_projection||!('token_reduction_pct' in f.point_estimate)||typeof f.point_estimate.injected_tokens!=='number'){process.exit(1)}"; echo "json_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/eval/injection-efficiency-harness.cjs --db /tmp/nonexistent-recense.db` exits 0 (graceful degrade) — `smoke_exit=0`.
    - After the live/snapshot run, `scripts/eval/results/42-injection-efficiency-PENDING.json` exists and has the correct structure: `meta`, `point_estimate` (with the `token_reduction_pct` key and a numeric `injected_tokens`), and `scaling_projection` blocks (`json_exit=0`). This validates the live run wrote a well-formed envelope, not just that the degrade path exits 0.
    - The reproduced recall-side % savings is captured from `point_estimate.token_reduction_pct` in the committed harness output (not hand-typed); if `flat_missing` is true the run is re-done with `--flat` so the headline figure is real.
    - The write-side amortized cost + breakeven session count is sourced from `cost-benefit-probe.json` / the breakeven combiner — no new sleep-pass run is triggered.
    - No credential keys appear in the result JSON.
  </acceptance_criteria>
  <done>The recall-side self-baseline is reproduced from a committed $0 script (the PENDING.json is written with a well-formed `meta`/`point_estimate`/`scaling_projection` envelope and a real `token_reduction_pct`) and the write-side breakeven is captured from existing measured probe data — both ready to cite.</done>
</task>

<task type="auto">
  <name>Task 2: Write the COST-03 competitor-savings report (recall headline + write-side separate + cited competitors)</name>
  <files>.planning/phases/42-token-and-cost-efficiency-audit/42-COST-SAVINGS-REPORT.md</files>
  <read_first>
    - scripts/eval/results/42-injection-efficiency-PENDING.json (the reproduced recall-side number from Task 1)
    - scripts/eval/results/cost-benefit-probe.json (the write-side amortized cost + breakeven)
    - .planning/phases/40-competitive-benchmark-baseline/40-COMPETITOR-TARGETS.md (competitor figures + Phase-40 D-08 methodology notes to reuse verbatim)
    - .planning/phases/42-token-and-cost-efficiency-audit/42-02-SUMMARY.md (the progressive-disclosure A/B verdict to fold in)
    - .planning/phases/42-token-and-cost-efficiency-audit/42-CONTEXT.md (D-11/D-12 framing; no-inflated-metrics hard rule)
  </read_first>
  <action>
    Write `.planning/phases/42-token-and-cost-efficiency-audit/42-COST-SAVINGS-REPORT.md`. Structure:
    (1) Headline — recense's reproduced RECALL-SIDE % savings (D-12 headline axis), the exact figure from the Task-1 injection-efficiency output, with the node count + commit it was measured at and the one-line "what it measures" note (bounded inject vs flat full-context dump, D-11).
    (2) Write-side, reported SEPARATELY (D-12) — the amortized pay-at-sleep cost per session + the breakeven session count from `cost-benefit-probe.json`/breakeven combiner; an explicit sentence that write and recall are NEVER netted into one number (no-inflated-metrics).
    (3) Competitor comparison table — mem0 "~90% fewer tokens vs OpenAI full-context memory" and claude-mem "~10x retrieval-token via progressive disclosure", each cited verbatim with its source and a one-line "what it actually measures" note (recall-side, vendor-published, methodology caveat from Phase-40 D-08). State that the comparison axis is recall-side for both (honest apples-to-apples).
    (4) Progressive-disclosure note — fold in the 42-02 A/B verdict: whether recense's schema-prior compression already matches/beats claude-mem's progressive-disclosure mechanism on the token axis, with the measured numbers.
    (5) Reproducibility footer — every recense figure traced to its committed script + result JSON path; subscription marginal cost ≈ $0 stated alongside the retail-$ estimate.
    Hard constraints (CLAUDE.md): no rounded-up or vibe figures; every recense number reproducible from a committed script; recall and write costs never combined; subscription-tokens vs direct-API-$ kept explicit.
  </action>
  <verify>
    <automated>test -f .planning/phases/42-token-and-cost-efficiency-audit/42-COST-SAVINGS-REPORT.md && grep -qi "recall" .planning/phases/42-token-and-cost-efficiency-audit/42-COST-SAVINGS-REPORT.md && grep -qi "breakeven\|write-side" .planning/phases/42-token-and-cost-efficiency-audit/42-COST-SAVINGS-REPORT.md && grep -qi "mem0" .planning/phases/42-token-and-cost-efficiency-audit/42-COST-SAVINGS-REPORT.md && grep -qi "claude-mem" .planning/phases/42-token-and-cost-efficiency-audit/42-COST-SAVINGS-REPORT.md; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The report states recense's reproduced RECALL-SIDE % savings as the headline, with the source script + result JSON path cited.
    - The write-side cost is reported in a SEPARATE section with an amortized cost + breakeven N, and the report contains an explicit "never netted" statement.
    - Both mem0 (~90%) and claude-mem (~10x) are cited verbatim, each with a source and a one-line "what it actually measures" note.
    - The 42-02 progressive-disclosure verdict is folded in with its measured numbers.
    - A reproducibility footer traces every recense figure to a committed script; subscription≈$0 stated alongside retail-$.
    - No figure is rounded-up or unsourced (no-inflated-metrics hard rule).
  </acceptance_criteria>
  <done>The COST-03 report makes the defensible savings claim — recall-side headline + write-side separate + competitors cited with methodology notes — every recense number reproducible, nothing netted or inflated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → live recense.db | The injection-efficiency run reads the live brain; read-only, never mutated. |
| report → committed result JSONs | The claim must trace to committed scripts; no hand-typed/inflated figures. |
| report / result JSON → disk | Output may be committed; must not leak secrets or PII beyond bounded glosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-07 | Repudiation | 42-COST-SAVINGS-REPORT.md figures | mitigate | Every recense number traced to a committed script + result JSON path (reproducibility footer); no vibe/rounded figures (no-inflated-metrics). |
| T-42-08 | Tampering | injection-efficiency-harness DB access | mitigate | Live db opened read-only; $0 LLM-free run; no write-side sleep pass triggered (deferred per D-06). |
| T-42-09 | Information Disclosure | result JSON + report output | mitigate | T-26-03 no-secret guard; subscription-token vs direct-API-$ kept explicit and never conflated; no API keys written. |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (net-zero-dep invariant); reuses existing harnesses + result JSONs only. |
</threat_model>

<verification>
- The injection-efficiency self-baseline is reproduced from a committed $0 script (PENDING.json written with a well-formed envelope and a real `token_reduction_pct`); the write-side breakeven is captured from existing measured probe data.
- The report cites recall headline + write-side separate + both competitors with methodology notes; nothing netted or inflated.
</verification>

<success_criteria>
- COST-03: the savings claim is stated with recense's reproduced number AND the cited competitor figures, recall-side headline, write-side separate, no inflated comparison (D-11/D-12).
</success_criteria>

<output>
Create `.planning/phases/42-token-and-cost-efficiency-audit/42-03-SUMMARY.md` when done — include the headline recall-side % savings, the write-side breakeven N, and the cited competitor figures.
</output>
</output>
