---
phase: 42-token-and-cost-efficiency-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/eval/42-lever-sweep-harness.cjs
  - scripts/eval/replay-ku-harness.cjs
  - scripts/eval/results/42-sweep-PENDING.json
autonomous: true
requirements: [COST-01, COST-02]
must_haves:
  truths:
    - "Each in-bounds lever can be swept one-at-a-time against the frozen v7.0 baseline, producing a clean per-lever token attribution (D-01, D-03, COST-01)"
    - "A whole feature can be dropped as a lever (e.g. RECENSE_CORPUS_GEN=0) and reported with its own token delta (D-02)"
    - "The KU-replay cached-extraction gate is BUILT and --dry-run-validated as the cheap inner-loop no-regression check: the lever value provably propagates into the spawned KU process via --config-override-key/--config-override-value (verified by the meta.config_override echo in the dry-run), but the REAL KU accuracy run is DEFERRED to 42-DEFERRED-RUN-RUNBOOK.md — do NOT run replay-ku-harness without --dry-run in this phase (D-04 KU arm, D-06 run-at-reset)"
    - "The lever-acceptance rule (a candidate passes only when accuracy stays within the noise band, D-05) is ENCODED in the gate and dry-run-validated; the real within-band accuracy verdict per candidate is produced by the deferred KU run in 42-04's runbook, not inline this phase"
    - "The expensive write-side sleep-pass measurement is BUILT but its heavy run is NOT executed in this phase (D-06 build-now/run-at-reset); when headless providers are not active the write ledger is measured:false, never fabricated"
  artifacts:
    - path: "scripts/eval/42-lever-sweep-harness.cjs"
      provides: "Greedy one-at-a-time lever-sweep harness wrapping the cost + KU accuracy harnesses; in-memory config override per run; per-lever result JSON"
      min_lines: 200
    - path: "scripts/eval/results/42-sweep-PENDING.json"
      provides: "Aggregate sweep result envelope (meta.lever_under_test, write_ledger.per_model shape, ku_gate, skip_split)"
  key_links:
    - from: "scripts/eval/42-lever-sweep-harness.cjs"
      to: "dist/src/lib/config (DEFAULT_CONFIG)"
      via: "spread override { ...DEFAULT_CONFIG, dbPath: scratchPath, [leverKey]: leverValue }"
      pattern: "\\.\\.\\.DEFAULT_CONFIG"
    - from: "scripts/eval/42-lever-sweep-harness.cjs"
      to: "scripts/eval/replay-ku-harness.cjs"
      via: "spawnSync KU inner-loop gate dispatch per lever candidate (exercised in --dry-run this phase), passing --config-override-key/--config-override-value so the lever propagates into the spawned KU process"
      pattern: "config-override-key"
    - from: "scripts/eval/42-lever-sweep-harness.cjs"
      to: "live recense.db"
      via: "VACUUM INTO scratch (live opened read-only; never mutated)"
      pattern: "VACUUM INTO"
---

<objective>
Build the greedy one-at-a-time lever-sweep harness that produces the per-lever token attribution COST-01 requires and the tuned-best candidate COST-02 needs, gated by the cheap KU-replay no-regression check (D-04). Honor the D-06 build-now / run-at-reset split, extended per the founder's 2026-06-24 build-only steer (weekly subscription limit nearly exhausted): the LLM-free skip-split accounting and the recall-side $0 token attribution run NOW; the KU inner-loop gate is BUILT and exercised in `--dry-run` ONLY this phase (validates wiring + the `meta.config_override` echo, costs $0, makes zero API/embedding calls); the REAL KU accuracy validation of lever candidates AND the write-side sleep-pass token breakdown (Haiku extract + Sonnet judge) are BOTH DEFERRED to plan 42-04's cost-probe-gated reset window. Do NOT run `replay-ku-harness.cjs` without `--dry-run` anywhere in this phase.

Purpose: gives the "broken down by lever" attribution (COST-01) and the combined-best candidate config (COST-02) without spending subscription tokens in this phase.
Output: `scripts/eval/42-lever-sweep-harness.cjs` + an aggregate sweep result JSON, plus a printed greedy comparison/argmax table and a combined-best candidate config handed off to 42-04 for its deferred KU-accuracy validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-token-and-cost-efficiency-audit/42-CONTEXT.md
@.planning/phases/42-token-and-cost-efficiency-audit/42-PATTERNS.md

Frozen v7.0 baseline: commit `d41d5c8`, tag `v7.0` (from `.planning/phases/40-competitive-benchmark-baseline/40-BASELINE.md`). This is the `phase_40_baseline_commit` value for the result `meta`.

In-bounds lever keys (read-only from `dist/src/lib/config` DEFAULT_CONFIG; confirmed line numbers in src/lib/config.ts):
- `consolSkipThreshold` (0.2), `consolSkipThresholdAssistant` (0.5), `consolSkipThresholdBySource` (per-source map), `injectionTokenBudget` (500), `recallNeighborhoodBudget` (20), `candidateK` (5), `recallSidewaysHopBudget` (3).
- `RECENSE_CORPUS_GEN=0` is an env-var feature-drop lever, NOT a config key — gate via `process.env['RECENSE_CORPUS_GEN'] === '0'` and report it as a lever with its own delta (D-02).

Output-flag convention (pinned): the harness uses a single `--out` flag for the aggregate sweep result file (matching every analog harness — cost-benefit, injection-efficiency, replay-ku all use `--out` for one result file). Per-lever and per-KU-gate auxiliary JSONs are written into the directory derived from `path.dirname(--out)` using the PATTERNS.md naming convention. There is no `--out-dir` flag.

Build-only constraint (founder 2026-06-24): the founder has nearly exhausted the weekly subscription limit, so NO eval-style API/subscription spend may happen during execution. The KU gate is wiring-validated in `--dry-run` only; a real KU run needs OpenAI embeddings + a judge (subscription tokens if headless) and is reserved for the 42-04 reset-window runbook.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the lever-sweep harness core (config override + skip-split + write-ledger instrumentation + result envelope)</name>
  <files>scripts/eval/42-lever-sweep-harness.cjs</files>
  <read_first>
    - scripts/eval/cost-benefit-harness.cjs (PRIMARY analog: header style, imports lines 40-59, `--out` single-result flag line 51, VACUUM INTO scratch lines 243-253, effectiveThreshold helper lines 115-123, skip-split lines 295-309, usage-sink + runConsolidation lines 366-411, headless-not-active fallback lines 351-363, PRICES + retailUsd, result write lines 590-592, FATAL catch lines 597-600)
    - scripts/eval/results/cost-benefit-probe.json (exact write_ledger.per_model + totals + retail_usd + subscription_marginal_usd shape to mirror)
    - scripts/eval/results/cost-std-baseline.json (usage.per_model reporting shape)
    - .planning/phases/42-token-and-cost-efficiency-audit/42-PATTERNS.md (§"42-lever-sweep-harness.cjs" — config override generalization line 60-68, result envelope lines 155-183, shared patterns: arg parser, header, meta block, no-data exit, measured:false guard, subscription-vs-retail split, no-secret guard T-26-03)
    - src/lib/config.ts lines 739-775 (read-only lever defaults — do NOT modify this file)
  </read_first>
  <action>
    Create `scripts/eval/42-lever-sweep-harness.cjs`. Copy the cost-benefit-harness header style (currency framing = subscription tokens with retail-$ API-list estimate; no-inflated-metrics guard; Phase-40 D-01 cost-probe reference; measured:false fallback guarantee). Copy verbatim from cost-benefit-harness.cjs: the `arg`/`flag` parser, `header()`, commit + engine_version meta block, no-data graceful exit, the `effectiveThreshold` helper, the `PRICES` table + `retailUsd()` helper, and the T-26-03 no-secret-keys guard (no ANTHROPIC_API_KEY / OPENAI_API_KEY ever written to result JSON or console).
    Args: `--db` (default live recense.db), `--out` (single aggregate result file, default `scripts/eval/results/42-sweep-PENDING.json` — same single-result convention as every analog harness; NO `--out-dir`), `--lever <key>`, `--values <csv>` (split on `,` per replay-ku-harness `--sweep-weights` convention), `--sample <n>`, `--dry-run`. Derive the auxiliary output directory once as `const OUT_DIR = path.dirname(OUT);` — per-lever and per-KU-gate files are written there (Task 2 pins their names).
    Core override operation: build the per-run config as `{ ...DEFAULT_CONFIG, dbPath: scratchPath, [leverKey]: leverValue }` — override exactly ONE key per run. For `RECENSE_CORPUS_GEN` the override is the env var, not a config key.
    DB isolation: open live db read-only, then `VACUUM INTO` a scratch path under `os.tmpdir()` (`recense-eval42-scratch-<ts>.db`); NEVER write to the live db.
    LLM-free skip-split (runs now, $0): for each sampled episode compute `effectiveThreshold(ep, config)` and count `n_below_threshold` vs `n_extracted`; record the active thresholds (global/assistant/per_source) in `skip_split`.
    Write-ledger instrumentation (BUILT now, RUN deferred per D-06): wire `setHeadlessUsageSink` + `runConsolidation` from the dist modules exactly as cost-benefit-harness lines 366-411, accumulating per-model usage. Guard with the headless-active check (judge+extractor both `claude-headless`): when NOT active, set `write_ledger = { measured: false, reason, stack_used }` — never fabricate. Always clear the sink in a finally.
    Result envelope: extend the cost-benefit-probe.json shape with `meta.eval='lever-sweep'`, `meta.lever_under_test`, `meta.lever_value`, `meta.baseline_value` (= DEFAULT_CONFIG[leverKey]), `meta.phase_40_baseline_commit='d41d5c8'`; a root `lever` block (key, candidate_value, baseline_value, delta_pct); `skip_split`; `write_ledger` (with per_model, totals, retail_usd, `subscription_marginal_usd: 0`, prices_dated); and a `caveats` array stating chars/4-proxy-free (this harness measures real usage tokens), subscription≈$0, and the D-06 deferral note. Write the aggregate envelope to `OUT` via the mkdir-p + writeFileSync pattern; wrap the main in the FATAL catch.
    Do NOT modify src/lib/config.ts, the engine, or any existing harness — this harness reads DEFAULT_CONFIG and overrides in-memory only.
  </action>
  <verify>
    <automated>npm run build && node scripts/eval/42-lever-sweep-harness.cjs --lever consolSkipThreshold --values 0.2,0.35 --dry-run --db /tmp/nonexistent-recense.db; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/eval/42-lever-sweep-harness.cjs --lever consolSkipThreshold --values 0.2,0.35 --dry-run --db /tmp/nonexistent-recense.db` exits 0 with a "no data" message (graceful-degrade path) OR writes the aggregate result JSON when a real db is present.
    - The harness defines a single `--out` flag (default `scripts/eval/results/42-sweep-PENDING.json`) and derives `OUT_DIR = path.dirname(OUT)`; no `--out-dir` flag exists (`grep -c "out-dir" scripts/eval/42-lever-sweep-harness.cjs` == 0).
    - The harness source contains the literal override `...DEFAULT_CONFIG` and `[leverKey]` and a `VACUUM INTO` call.
    - When headless providers are unset, the emitted `write_ledger.measured` is `false` with a `reason` string — no numeric token counts fabricated (`grep -v '^#' scripts/eval/42-lever-sweep-harness.cjs | grep -c "measured: false"` ≥ 1).
    - The result envelope (when written) contains `meta.lever_under_test`, `meta.phase_40_baseline_commit`, `write_ledger.per_model`, `write_ledger.subscription_marginal_usd`, and `skip_split`.
    - No `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` literal appears in any code path that writes the result JSON or console output.
    - src/lib/config.ts is unchanged (`git diff --quiet src/lib/config.ts`).
  </acceptance_criteria>
  <done>The harness builds, dry-runs to exit 0, overrides one lever in-memory over a VACUUM INTO scratch db, emits the per-lever envelope to `--out`, and marks write_ledger measured:false when headless is inactive.</done>
</task>

<task type="auto">
  <name>Task 2: Greedy sweep driver + KU inner-loop gate dispatch (with pinned lever propagation, --dry-run-validated this phase) + argmax table; run the cheap LLM-free sweep now</name>
  <files>scripts/eval/42-lever-sweep-harness.cjs, scripts/eval/replay-ku-harness.cjs, scripts/eval/results/42-sweep-PENDING.json</files>
  <read_first>
    - scripts/eval/35-strength-sweep.cjs (sweep orchestration lines 152-176 spawnSync child-dispatch; comparison/argmax table lines 120-133; result naming lines 104-106)
    - scripts/eval/replay-ku-harness.cjs (the KU inner-loop gate to dispatch AND to extend with the override flag pair; `arg()` helper lines ~52-58, `--dry-run`/`--out`/`--sweep-weights`/`--cache` flags lines 52-93; the place it builds its retrieval/consolidation config from DEFAULT_CONFIG; runSweep internal loop lines 554-705; reduction-% pattern lines 993-996; result `meta` block where `config_override` is echoed)
    - scripts/eval/42-lever-sweep-harness.cjs (the Task 1 core this extends)
    - .planning/phases/42-token-and-cost-efficiency-audit/42-PATTERNS.md (§"KU inner-loop gate dispatch" lines 193-209 — note the `// future:` config-override placeholder; this task replaces it with real flags; §"Sweep orchestration" lines 187-189; §"Result file naming" lines 224-228; result `ku_gate` block lines 506-513)
  </read_first>
  <action>
    Extend the harness with the greedy one-at-a-time sweep driver (D-01): iterate the in-bounds lever set; for each lever sweep its candidate `--values` against the frozen v7.0 baseline value, record each candidate's token attribution (skip-split + recall-side, $0). Rank candidates per lever by the MEASURED $0 token delta and emit a combined-best candidate config object (the handoff that 42-04's deferred KU run will accuracy-validate and possibly demote) — do NOT run a combined sleep pass here and do NOT lock candidates on a real KU accuracy verdict this phase (that lock is the runbook's first deferred step). Write the aggregate sweep envelope to `--out` (default `scripts/eval/results/42-sweep-PENDING.json`); write each per-lever file into `OUT_DIR` (= `path.dirname(OUT)`) as `42-sweep-<leverKey>-<value with '.'→'_'>.json` per PATTERNS.md.
    PIN the KU lever-override mechanism (fixes the D-04 no-op gap — BUILD this now): add a `--config-override-key` / `--config-override-value` flag pair to `scripts/eval/replay-ku-harness.cjs` — this is the ONE source edit this plan makes to that harness. Parse both with the existing `arg()` helper; coerce the value (numeric levers → Number(...), the `RECENSE_CORPUS_GEN` lever stays an env var, not this flag); apply the override via the SAME `{ ...DEFAULT_CONFIG, [key]: value }` spread the sweep harness uses, at the point where replay-ku-harness builds its retrieval/consolidation config; then echo the applied override into the KU result envelope as `meta.config_override = { key, value }` so propagation is verifiable. The override path must also be exercisable under the harness's existing `--dry-run` (echo the config_override into the result without running extraction/judge/embeddings). Replace the `// future:` placeholder in the PATTERNS.md dispatch snippet (lines 199-203) with these real flags.
    KU inner-loop gate dispatch (D-04 — BUILT now, real run DEFERRED): build the dispatch path so that per lever candidate the harness dispatches `scripts/eval/replay-ku-harness.cjs` via spawnSync with `--out <OUT_DIR>/42-ku-gate-<leverKey>-<value>.json` PLUS `--config-override-key <leverKey> --config-override-value <leverValue>`, env `{ ...process.env, RECENSE_MODEL_PROVIDER: 'claude-headless' }`, cwd repo root. Read back `ku_score`; compute regression vs the baseline KU score; record `ku_gate { ku_score, regression, tolerance_band_pts, source }`. ENCODE the acceptance rule (D-05): a candidate passes only if `ku_score` stays within the noise band (≤1pt / within CI) of baseline; record the band used. BUILD-ONLY THIS PHASE: the dispatch is exercised by passing `--dry-run` through to the KU child ($0, zero API/embedding calls, no re-extraction) — the dry-run validates the wiring + that the lever value landed (the KU result's `meta.config_override` reflects it). In dry-run the `ku_gate.ku_score`/`regression` are null/placeholder and `ku_gate.deferred=true`. The REAL KU accuracy run that produces a true per-candidate ku_score verdict is DEFERRED to 42-DEFERRED-RUN-RUNBOOK.md (plan 42-04, cost-probe-gated). Do NOT invoke replay-ku-harness.cjs without `--dry-run` anywhere in this phase — a real run needs OpenAI embeddings + a judge (subscription tokens) and is reserved for the reset window.
    CRITICAL: without this flag pair the KU gate would be a NO-OP for the recall-side levers (`injectionTokenBudget`, `recallNeighborhoodBudget`, `candidateK`, `recallSidewaysHopBudget`) — the spawned process would silently run default config and COST-02's no-regression gate would pass trivially for exactly the levers most likely to win. Even in the dry-run validation this phase, the dispatch must verify the override landed (the KU result `meta.config_override` reflects the lever value) before the deferred run is trusted.
    Feature-drop levers (D-02): include `RECENSE_CORPUS_GEN=0` as a sweep entry (env-var lever) and report its own token delta; decide tune-only vs droppable per-lever from the MEASURED delta, never pre-committed.
    Print a comparison table (one row per lever value: `lever_value | skip_rate | write_tokens_per_turn | ku_score | within_band`); in this phase the `ku_score`/`within_band` columns show "deferred" for real-run-only rows. Print the argmax per lever ranked by the $0 token delta (35-strength-sweep pattern).
    RUN the cheap LLM-free parts now: execute the skip-split sweep + the recall-side $0 token attribution over the live brain copy (read-only → VACUUM INTO scratch). Exercise the KU gate dispatch in `--dry-run` only (wiring + config_override echo check, $0). If the live db / KU cache is unavailable, degrade to `--dry-run` and document that the cheap LLM-free run is pending the cache. Do NOT run the write-side sleep pass and do NOT run a real KU accuracy pass — both are deferred to 42-04.
  </action>
  <verify>
    <automated>npm run build && node scripts/eval/42-lever-sweep-harness.cjs --lever consolSkipThreshold --values 0.2,0.35,0.5 --dry-run --out scripts/eval/results/42-sweep-PENDING.json; echo "exit=$?"; node -e "const f=require('./scripts/eval/results/42-sweep-PENDING.json'); if(!f.meta||!f.meta.lever_under_test){process.exit(1)}" 2>/dev/null; echo "json_keys_exit=$?"; node scripts/eval/replay-ku-harness.cjs --dry-run --config-override-key candidateK --config-override-value 7 --out /tmp/42-ku-override-smoke.json 2>/dev/null; node -e "const f=require('/tmp/42-ku-override-smoke.json'); if(!f.meta||!f.meta.config_override||f.meta.config_override.key!=='candidateK'){process.exit(1)}" 2>/dev/null; echo "ku_override_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The sweep prints a per-value comparison table with at minimum `lever_value`, the $0 token columns, and a within-noise-band/`ku_score` column (showing "deferred" for the real-run-only KU values this phase).
    - The harness dispatches `replay-ku-harness.cjs` as the inner-loop gate (`grep -c "replay-ku-harness" scripts/eval/42-lever-sweep-harness.cjs` ≥ 1), passes `--dry-run` through this phase, and does NOT trigger re-extraction.
    - `replay-ku-harness.cjs` accepts `--config-override-key`/`--config-override-value`, applies them via the `{ ...DEFAULT_CONFIG, [key]: value }` spread, and echoes `meta.config_override = { key, value }` into its result even under `--dry-run` (`ku_override_exit=0` in the verify above). This proves the lever propagates into the spawned KU process — the gate is NOT a no-op for recall-side levers.
    - The sweep dispatch passes `--config-override-key/--config-override-value` to the KU child (`grep -c "config-override-key" scripts/eval/42-lever-sweep-harness.cjs` ≥ 1), and the per-candidate `ku_gate.source` JSON (from the dry-run) carries the matching `meta.config_override`.
    - `RECENSE_CORPUS_GEN` appears as a sweep lever entry (`grep -c "RECENSE_CORPUS_GEN" scripts/eval/42-lever-sweep-harness.cjs` ≥ 1).
    - The aggregate result JSON contains a `ku_gate` block with the `ku_score`, `regression`, and `tolerance_band_pts` fields present (their values null/placeholder with `deferred:true` this phase), and the run emits a combined-best candidate config object ranked by the $0 token delta.
    - The greedy driver records a per-lever candidate ranking by the MEASURED $0 token delta (D-01) — verifiable from the printed argmax rows; the real accuracy-based lock is explicitly deferred to 42-04's runbook KU step.
    - No real KU run and no write-side sleep pass is executed in this phase: the KU child is always invoked with `--dry-run`, and when run without `--dry-run` with headless inactive the `write_ledger.measured` is `false`.
  </acceptance_criteria>
  <done>The greedy sweep produces per-lever $0 token attribution now and emits a combined-best candidate config for 42-04; the KU no-regression gate is BUILT and --dry-run-validated (the lever value provably propagates into each spawned KU process, so the recall-side gate is real, not a no-op), with the real KU accuracy verdict and the write-side sleep pass both deferred to 42-04's cost-probe-gated runbook. No replay-ku-harness run without --dry-run happens this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → live recense.db | The sweep reads the founder's live brain; it must never mutate it (no destructive sweep of customer-zero memory). |
| harness → result JSON / stdout | Output is written to disk and may be committed; must not leak subscription-secret/API-key contents. |
| harness → src/lib/config.ts | Lever overrides must stay in-memory; no accidental destructive config write to the live engine. |
| sweep harness → replay-ku-harness.cjs | The lever value crosses a process boundary via CLI flags; the override must be applied in-memory in the child (DEFAULT_CONFIG spread) and never written back to source/config. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | 42-lever-sweep-harness.cjs DB access | mitigate | Open live db `readonly`; `VACUUM INTO` a scratch copy under os.tmpdir before any consolidation; all overrides target the scratch dbPath. |
| T-42-02 | Information Disclosure | result JSON + console output | mitigate | T-26-03 no-secret guard — no ANTHROPIC_API_KEY/OPENAI_API_KEY/subscription token contents written to results or logs; only token counts + retail-$ estimates. |
| T-42-03 | Tampering | config override path | mitigate | Override via in-memory `{...DEFAULT_CONFIG, [k]:v}` only in BOTH the sweep harness and the KU harness's new `--config-override-key/value` path; src/lib/config.ts opened read-only / never written (`git diff --quiet` gate). |
| T-42-01-DoS | Denial of Service | accidental expensive run | mitigate | Build-only this phase: the KU child is always dispatched with `--dry-run` (zero API/embedding calls), and the write-side sleep pass is gated behind `measured:false` when headless inactive; both the real KU run and the write-side run are deferred to 42-04's cost-probe gate. No heavy run path is reachable without explicit headless env + dropping `--dry-run` + the 42-04 gate. |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (net-zero-dep invariant); harness reuses existing better-sqlite3 + dist engine modules only. No legitimacy gate required. |
</threat_model>

<verification>
- `npm run build` succeeds; the harness dry-runs to exit 0 on a missing db.
- The aggregate sweep result JSON carries `meta.lever_under_test`, `meta.phase_40_baseline_commit`, `write_ledger.per_model`/`subscription_marginal_usd`, `skip_split`, and `ku_gate` (with `deferred:true` this phase).
- The KU inner-loop gate is dispatched per candidate in `--dry-run` AND the lever value provably propagates (KU result `meta.config_override` matches); no real KU run and no write-side sleep pass runs in this phase.
- No credential keys appear in any committed result JSON.
</verification>

<success_criteria>
- COST-01: token cost is attributable per lever (skip-split + recall-side $0 now; write-ledger instrumented for the deferred run) against the frozen v7.0 baseline.
- COST-02: a combined-best candidate config is produced (ranked by the $0 token deltas), with the KU no-regression gate BUILT and --dry-run-validated (lever value provably applied via `meta.config_override`, not a default-config no-op); the real within-noise-band accuracy verdict (D-05) per candidate is produced by the deferred KU run in 42-04's runbook.
- D-01/D-02/D-03/D-04(KU, built+dry-run-validated)/D-05(encoded)/D-06(build half) all traceable to the harness.
</success_criteria>

<output>
Create `.planning/phases/42-token-and-cost-efficiency-audit/42-01-SUMMARY.md` when done — include the combined-best candidate config object (lever → best value, ranked by the $0 token delta) for plan 42-04's deferred KU-accuracy validation, and note that the real KU run was deferred (KU gate dry-run-validated only).
</output>
