---
phase: 42-token-and-cost-efficiency-audit
plan: 04
type: execute
wave: 3
depends_on: ["42-01", "42-03"]
files_modified:
  - .planning/phases/42-token-and-cost-efficiency-audit/42-DEFERRED-RUN-RUNBOOK.md
autonomous: false
requirements: [COST-01, COST-02]
must_haves:
  truths:
    - "A documented, runnable runbook exists for the deferred expensive run: cost-probe gate FIRST, then the cheap KU-replay inner-loop accuracy validation (the cheap filter that gates lever candidates on the D-05 noise band), then the full write-side token breakdown at the combined-best lever config, then the LOCOMO/LongMemEval-S no-regression confirm (D-06, D-04)"
    - "The cost-probe (1-conversation) is a HARD GATE that must pass before ANY deferred run is scheduled (including the cheap KU run), and the heavy run is scheduled near a weekly reset (Phase-40 D-01/D-02/D-03 carried forward)"
    - "The cheap KU-replay inner-loop validation is documented as near-$0-but-not-zero (needs OPENAI_API_KEY for embeddings + a judge / headless) and runs as the cheap filter before the expensive LOCOMO/LongMemEval confirm — it produces the real per-candidate accuracy verdict that 42-01 deferred"
    - "The expensive run is NOT executed during this phase — the gate is a blocking founder checkpoint, and every deferred run (KU + write-side + LOCOMO/LongMemEval) is left as a scheduled, documented procedure (D-06 defer-the-run)"
    - "The no-regression accept/reject uses the noise-band tolerance from D-05 in BOTH the cheap KU filter and the final LOCOMO/LongMemEval confirm; an accuracy drop outside the band rejects the token win"
  artifacts:
    - path: ".planning/phases/42-token-and-cost-efficiency-audit/42-DEFERRED-RUN-RUNBOOK.md"
      provides: "Step-by-step deferred-run procedure: cost-probe gate, cheap KU-replay inner-loop validation, write-side breakdown run, LOCOMO+LongMemEval confirm, scratch-DB isolation, exact commands + env, result paths, accept/reject rule"
      min_lines: 70
  key_links:
    - from: ".planning/phases/42-token-and-cost-efficiency-audit/42-DEFERRED-RUN-RUNBOOK.md"
      to: "scripts/eval/replay-ku-harness.cjs"
      via: "the deferred cheap KU-replay inner-loop accuracy validation (the cheap filter before the expensive confirm), run with the 42-01 --config-override-key/value lever flags and a REAL (non-dry-run) pass"
      pattern: "replay-ku-harness"
    - from: ".planning/phases/42-token-and-cost-efficiency-audit/42-DEFERRED-RUN-RUNBOOK.md"
      to: "scripts/eval/42-lever-sweep-harness.cjs"
      via: "the deferred write-side breakdown runs this harness at the combined-best config with headless providers active"
      pattern: "42-lever-sweep-harness"
    - from: ".planning/phases/42-token-and-cost-efficiency-audit/42-DEFERRED-RUN-RUNBOOK.md"
      to: "scripts/eval/locomo-harness.cjs + scripts/eval/longmemeval-harness.cjs"
      via: "the deferred final no-regression confirm"
      pattern: "locomo-harness"
---

<objective>
Make the D-06 "defer the expensive run" half an explicit, checkable deliverable: a documented, runnable runbook for the cost-probe-gated reset-window run (the cheap KU-replay inner-loop accuracy validation + the full write-side token breakdown at the combined-best lever config + the LOCOMO/LongMemEval-S no-regression confirm), plus a blocking founder cost-probe checkpoint. Per the founder's 2026-06-24 build-only steer, even the cheap KU-replay accuracy validation is deferred here (built + dry-run-validated in 42-01; the real run lives in this runbook). None of these runs is executed in this phase — they are scheduled and documented. This closes the deferred portion of COST-01 (full write-side breakdown) and COST-02 (per-candidate KU filter + final no-regression confirm).

Purpose: honors the founder's build-now / run-at-reset constraint and the carried-forward cost gate without spending subscription tokens now (the weekly subscription limit is nearly exhausted).
Output: `42-DEFERRED-RUN-RUNBOOK.md` + a blocking cost-probe checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-token-and-cost-efficiency-audit/42-CONTEXT.md
@.planning/phases/42-token-and-cost-efficiency-audit/42-01-SUMMARY.md
@.planning/phases/42-token-and-cost-efficiency-audit/42-03-SUMMARY.md

Carried-forward cost posture (Phase-40 D-01/D-02/D-03, reaffirmed founder 2026-06-24): a 1-conversation cost probe is a HARD GATE before any deferred run; the heavy run is scheduled near a weekly reset. Currency = subscription tokens with a retail-$ translation. The combined-best lever config comes from 42-01's SUMMARY (ranked by the $0 token delta; the cheap KU filter in this runbook produces its real accuracy verdict).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the deferred-run runbook (cost-probe gate → cheap KU-replay validation → write-side breakdown → LOCOMO/LongMemEval confirm)</name>
  <files>.planning/phases/42-token-and-cost-efficiency-audit/42-DEFERRED-RUN-RUNBOOK.md</files>
  <read_first>
    - scripts/eval/replay-ku-harness.cjs (the cheap KU filter the deferred run executes for real; its `--config-override-key`/`--config-override-value` flags added in 42-01, `--out`/`--cache` flags, and the OPENAI_API_KEY-for-embeddings + judge/headless requirement for a REAL (non-dry-run) pass)
    - scripts/eval/42-lever-sweep-harness.cjs (the harness the deferred write-side breakdown runs; its `--lever`/`--values`/`--db`/`--out` flags — single aggregate result file; no `--out-dir`, OUT_DIR derived as `path.dirname(OUT)` — plus the headless-active requirement for measured:true)
    - scripts/eval/cost-benefit-harness.cjs (the cost-probe pattern + headless env requirement RECENSE_JUDGE_PROVIDER/RECENSE_EXTRACTOR_PROVIDER=claude-headless; PART A write ledger as the breakdown shape)
    - scripts/eval/locomo-harness.cjs + scripts/eval/longmemeval-harness.cjs (the final no-regression confirm arms; their `--run`/`--probe`/`--dry-run` gates)
    - .planning/phases/40-competitive-benchmark-baseline/40-BASELINE.md (the reproduction commands + worktree pattern + the frozen v7.0 baseline numbers to compare against)
    - .planning/phases/42-token-and-cost-efficiency-audit/42-01-SUMMARY.md (the combined-best lever config to validate + measure; note the KU gate was dry-run-validated only, real run deferred here)
    - .planning/phases/42-token-and-cost-efficiency-audit/42-CONTEXT.md (D-04 accuracy arms, D-05 noise band, D-06 defer-the-run, carried cost gate)
  </read_first>
  <action>
    Write `.planning/phases/42-token-and-cost-efficiency-audit/42-DEFERRED-RUN-RUNBOOK.md`. Sections:
    (1) Preconditions — schedule near a weekly subscription reset; headless providers active (`RECENSE_JUDGE_PROVIDER=claude-headless`, `RECENSE_EXTRACTOR_PROVIDER=claude-headless`) and `OPENAI_API_KEY` set for the KU-replay embeddings; the combined-best lever config from 42-01-SUMMARY; scratch-DB isolation (VACUUM INTO; live db read-only — never mutate customer-zero's brain).
    (2) STEP 1 — cost-probe HARD GATE (Phase-40 D-01): the exact 1-conversation probe command (cost-benefit-harness probe mode / a single-episode write-ledger run), the projected subscription-token + retail-$ figure it yields, and the rule: do NOT proceed to STEP 2 unless the founder approves the projected cost. State subscription marginal ≈ $0 alongside the retail-$ estimate. This gate covers the ENTIRE deferred battery below, including the cheap KU run.
    (3) STEP 2 — cheap KU-replay inner-loop accuracy validation (the cheap filter; first of the battery, before the expensive confirm): the exact `replay-ku-harness.cjs` REAL-run command per combined-best lever candidate, using the `--config-override-key`/`--config-override-value` flags built in 42-01 (NO `--dry-run` here — this is the real run 42-01 deferred). State the cost note: near-$0 but NOT zero (cached extraction, but needs `OPENAI_API_KEY` for embeddings + a judge / headless tokens). State the gate rule: each lever candidate passes only if its KU `ku_score` stays within the D-05 noise band (≤1pt / within CI) of the frozen v7.0 baseline; candidates that fail the band are DEMOTED before the expensive confirm runs — this is the cheap filter that prunes losers cheaply.
    (4) STEP 3 — full write-side token breakdown: the exact `42-lever-sweep-harness.cjs` invocation at the (KU-filtered) combined-best config with headless active (measured:true), using the single `--out` flag for the aggregate result file (NO `--out-dir` — the harness has no such flag; auxiliary files land in `path.dirname(--out)`), result path under scripts/eval/results/, and the per-model (Haiku extract / Sonnet judge) breakdown to record — this completes the deferred half of COST-01.
    (5) STEP 4 — final no-regression confirm: the exact `locomo-harness.cjs` + `longmemeval-harness.cjs` commands (with `--run`), the frozen v7.0 baseline numbers to compare against (from 40-BASELINE.md), and the accept/reject rule using the D-05 noise band (≤1pt / within CI) — a token win that drops accuracy outside the band is REJECTED (COST-02 guardrail).
    (6) Result locations + what to update — where the result JSONs land and which docs to update (ROADMAP Phase 42 success criteria, the cost-savings report) after the run.
    Use single-line chained commands (CLAUDE.md copy-paste hygiene). Do NOT execute any of these steps in this plan — the runbook documents the procedure only.
  </action>
  <verify>
    <automated>test -f .planning/phases/42-token-and-cost-efficiency-audit/42-DEFERRED-RUN-RUNBOOK.md && grep -qi "cost-probe\|cost probe" .planning/phases/42-token-and-cost-efficiency-audit/42-DEFERRED-RUN-RUNBOOK.md && grep -q "replay-ku-harness" .planning/phases/42-token-and-cost-efficiency-audit/42-DEFERRED-RUN-RUNBOOK.md && grep -q "42-lever-sweep-harness" .planning/phases/42-token-and-cost-efficiency-audit/42-DEFERRED-RUN-RUNBOOK.md && grep -qi "locomo" .planning/phases/42-token-and-cost-efficiency-audit/42-DEFERRED-RUN-RUNBOOK.md && grep -qi "noise band\|noise-band\|within CI\|1pt" .planning/phases/42-token-and-cost-efficiency-audit/42-DEFERRED-RUN-RUNBOOK.md && [ "$(grep -c 'out-dir' .planning/phases/42-token-and-cost-efficiency-audit/42-DEFERRED-RUN-RUNBOOK.md)" -eq 0 ]; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The runbook documents all four ordered steps: cost-probe HARD GATE → cheap KU-replay inner-loop validation → write-side breakdown → LOCOMO/LongMemEval confirm.
    - STEP 1 states the cost-probe is a hard gate requiring founder approval before the rest of the battery (including the cheap KU run), with subscription-token + retail-$ framing.
    - STEP 2 cites the exact `replay-ku-harness.cjs` REAL-run command (NO `--dry-run`) per combined-best lever candidate using the `--config-override-key`/`--config-override-value` flags, notes its near-$0-but-not-zero cost (OPENAI_API_KEY embeddings + judge/headless), and states the D-05 noise-band gate that demotes failing candidates before the expensive confirm.
    - STEP 3 cites the exact `42-lever-sweep-harness.cjs` invocation at the combined-best config with headless providers active (measured:true).
    - STEP 3's harness command uses the single `--out` flag, NOT `--out-dir` — the harness exposes no `--out-dir` flag (`grep -c 'out-dir' .planning/phases/42-token-and-cost-efficiency-audit/42-DEFERRED-RUN-RUNBOOK.md` == 0).
    - STEP 4 cites the LOCOMO + LongMemEval-S commands, the frozen v7.0 baseline to compare against, and the D-05 noise-band accept/reject rule.
    - Scratch-DB isolation (live read-only / VACUUM INTO) is stated; commands are single-line copy-paste-safe.
    - No expensive run (including the cheap KU run) is executed by this task (the runbook is documentation only).
  </acceptance_criteria>
  <done>A runnable, ordered runbook exists for the deferred reset-window run, with the cost-probe hard gate first, the cheap KU-replay accuracy filter as the first deferred run, and the no-regression accept/reject rule — nothing executed.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Cost-probe gate — founder approves or defers the scheduled deferred run battery</name>
  <decision>Whether to schedule + approve the deferred run battery (cheap KU-replay validation + write-side breakdown + LOCOMO/LongMemEval confirm) now, or hold it for a later weekly-reset window.</decision>
  <context>
    Per D-06 and the founder's 2026-06-24 build-only steer, NO deferred run is executed during this phase — everything is built and documented, including the cheap KU-replay accuracy validation (dry-run-validated only in 42-01). This checkpoint is the carried-forward Phase-40 D-01 cost-probe HARD GATE: the founder reviews the projected subscription-token + retail-$ cost of the full deferred battery (the cheap KU-replay accuracy validation — near-$0 but not zero, needs OpenAI embeddings + judge — PLUS the write-side sleep pass at the combined-best lever config PLUS the LOCOMO/LongMemEval-S confirm) against the runbook in Task 1, and decides whether/when to schedule it near a weekly reset. The gate covers the cheap KU run too. The runs themselves are out of scope for this phase regardless of the decision — this gate only records the go/defer call and the projected cost.
  </context>
  <options>
    <option id="defer-to-reset">
      <name>Defer to a weekly-reset window (default, D-06)</name>
      <pros>Honors build-now/run-at-reset; subscription tokens spent near a reset; no surprise cost while the weekly limit is nearly exhausted.</pros>
      <cons>Per-candidate KU accuracy verdict, final tuned write-side numbers, and the no-regression confirm remain pending until the run.</cons>
    </option>
    <option id="approve-schedule">
      <name>Approve + schedule the run now (still executed outside this phase)</name>
      <pros>Locks a concrete run date; the KU filter + tuned numbers land sooner.</pros>
      <cons>Commits subscription tokens; still must wait for the reset window per the cost posture.</cons>
    </option>
  </options>
  <resume-signal>Select: defer-to-reset or approve-schedule (and note the projected cost reviewed). Either way, no deferred run — including the cheap KU run — is executed in this phase.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook → deferred run battery | The documented procedure must enforce the cost-probe hard gate + scratch-DB isolation so a future run (cheap KU + write-side + confirm) never silently overspends or mutates the live brain. |
| founder checkpoint → subscription cost | The gate is the control point against unbudgeted subscription-token spend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-10 | Denial of Service | unbudgeted deferred run | mitigate | The cost-probe is a blocking founder checkpoint (autonomous:false); STEP 2/3/4 (including the cheap KU run) are gated behind it; every deferred run is explicitly out of scope this phase. |
| T-42-11 | Tampering | deferred runs vs live brain | mitigate | Runbook mandates VACUUM INTO scratch + live db read-only for the KU and write-side runs; no run mutates customer-zero memory. |
| T-42-12 | Information Disclosure | runbook + future result JSONs | mitigate | Runbook reuses the T-26-03 no-secret guard already in the harnesses; subscription-token vs retail-$ kept explicit, OPENAI_API_KEY / no API keys documented inline (referenced as env-only). |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (net-zero-dep invariant); the deferred runs reuse existing harnesses only. |
</threat_model>

<verification>
- The runbook documents the cost-probe gate → cheap KU-replay validation → write-side breakdown → LOCOMO/LongMemEval confirm in order, with the D-05 accept/reject rule applied in both the KU filter and the final confirm.
- The cost-probe checkpoint is a blocking founder decision; no deferred run (including the cheap KU run) executes in this phase.
</verification>

<success_criteria>
- COST-01 (deferred half): the full write-side token breakdown is a documented, runnable procedure.
- COST-02 (deferred half): the per-candidate KU-replay accuracy filter AND the final LOCOMO/LongMemEval no-regression confirm are documented with the D-05 noise-band accept/reject rule.
- D-06 defer-the-run (extended to the cheap KU run) + the carried cost gate are honored as explicit, checkable deliverables.
</success_criteria>

<output>
Create `.planning/phases/42-token-and-cost-efficiency-audit/42-04-SUMMARY.md` when done — record the cost-probe gate decision (defer or approve-schedule) and the projected cost reviewed for the full deferred battery (cheap KU + write-side + confirm).
</output>
