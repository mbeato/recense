---
phase: 34-visual-polish-pass
plan: 03
type: execute
wave: 3
depends_on: ["34-01", "34-02"]
files_modified: []
autonomous: false
requirements: [VIZ-POLISH-01, VIZ-POLISH-02, VIZ-POLISH-03]
user_setup: []

must_haves:
  truths:
    - "package.json runtime dependencies are unchanged (net-zero new deps)"
    - "No amber (#d9a05c / rgba(217, 160, 92) was introduced on any new rest/non-activation selector"
    - "dist reflects the CSS/JS/HTML edits (viz assets recopied)"
    - "The founder visually confirms each polished surface against the UI-SPEC per-surface checklist"
  artifacts:
    - path: ".planning/phases/34-visual-polish-pass/34-03-SUMMARY.md"
      provides: "Guard-grep results + founder visual sign-off record"
  key_links:
    - from: "src/viz changes (34-01, 34-02)"
      to: "dist/src/viz served by recense viz"
      via: "npm run build (tsc + copy-viz-assets.cjs)"
      pattern: "copy-viz-assets"
---

<objective>
Rebuild dist, run the VIZ-POLISH-03 guard checks (net-zero deps, no amber-at-rest, no structural/3D-density change), then run the founder visual checkpoint across all four polished surfaces using the 34-UI-SPEC per-surface prescriptions as the before/after checklist.

Purpose: Prove the polish landed correctly and the founder-locked guards hold before closing the phase.
Output: A rebuilt dist and a recorded founder sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-visual-polish-pass/34-UI-SPEC.md
@.planning/phases/34-visual-polish-pass/34-01-SUMMARY.md
@.planning/phases/34-visual-polish-pass/34-02-SUMMARY.md

# This phase changes only src/viz/* (css, modules/corpus.js, index.html). dist is rebuilt
# via `npm run build` (tsc) → postbuild runs scripts/copy-viz-assets.cjs which copies
# index.html + vendor + css + modules into dist/src/viz/. The served viz reads from dist.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild dist + VIZ-POLISH-03 guard greps</name>
  <files></files>
  <read_first>
    - package.json — the `build` + `postbuild` scripts (tsc → copy-viz-assets.cjs)
    - .planning/phases/34-visual-polish-pass/34-UI-SPEC.md — Color section ("NOT amber at any rest or non-activation state") and the Phase 34 ROADMAP success criterion 3 (VIZ-POLISH-03)
  </read_first>
  <action>
    Run `git diff --stat package.json` — it MUST be empty (no runtime dependency change; net-zero deps guard).

    Run `npm run build` from the repo root — tsc must exit 0, and the postbuild `copy-viz-assets.cjs` must report "copied index.html + vendor + css + modules → dist/src/viz/" so the served viz reflects the css/js/html edits.

    No-amber-at-rest guard: grep the diff introduced by 34-01 and 34-02 for newly-added amber on rest/non-activation selectors. Run `git diff src/viz/css/styles.css src/viz/modules/corpus.js` and confirm no ADDED line (a `+` line) introduces `#d9a05c` or `rgba(217, 160, 92` on a rest selector. The only legitimately amber-adjacent additions are none in this phase — the corpus-active border is muted rose `rgba(156, 112, 128, 0.55)` and the corpus-status palette is `#6b5f73` / `#8b7090`. If any added line introduces amber on a non-hover/non-activation selector, FAIL and flag it.

    No-structural-change guard: confirm `git diff --stat` lists only `src/viz/css/styles.css`, `src/viz/index.html`, and `src/viz/modules/corpus.js` (plus dist/ rebuild output) — no other source files touched. Confirm the `#graph` 3D brain density code (THREE node/edge density in src/viz/modules/) is NOT in the diff — the founder-locked density anchor is untouched.
  </action>
  <verify>
    <automated>cd /Users/vtx/brain-memory && test -z "$(git diff --stat package.json)" && npm run build 2>&1 | grep -q 'copied index.html' && ! git diff src/viz/css/styles.css src/viz/modules/corpus.js | grep -E '^\+' | grep -v '^\+\+\+' | grep -Eq '#d9a05c|rgba\(217, 160, 92'; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat package.json` is empty (no runtime dep change)
    - `npm run build` exits 0 and postbuild reports the viz-asset copy into dist/src/viz/
    - No ADDED (`+`) line in the styles.css / corpus.js diff introduces `#d9a05c` or `rgba(217, 160, 92` (no amber at rest)
    - `git diff --stat` source-file list is limited to styles.css, index.html, corpus.js — no structural files, no 3D-density code touched
  </acceptance_criteria>
  <done>dist is rebuilt from the edited viz assets; net-zero-deps, no-amber-at-rest, and no-structural-change guards all pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Founder visual checkpoint — all four surfaces</name>
  <what-built>
    R1 sticky reader close button; B2 HUD declutter (test-trace removed, log hidden in window mode, topics separated); B3 topics/search hidden in corpus view; C1 corpus toggle as an icon-only fixed book button with muted-rose active state; C2 contained corpus force layout; corpus loading/empty/error states; detail divider + title spacing normalized. dist rebuilt.
  </what-built>
  <how-to-verify>
    1. Start the viz: run `recense viz` (or the project's viz launch) and open the expanded window.
    2. Reader (R1): open a long doc (e.g. `?doc=<slug>&reader=1`), scroll the prose — the close `×` must stay pinned at the top in its sticky head with a solid backing (prose does not bleed through). Close button is muted mauve, slate on hover — never amber.
    3. HUD (B2): in expanded (.mode-window) mode the actions row shows only `[Show tombstones] [Reader]` — no Log button, no Test-trace button. The topics section is visibly separated from search by a hairline border + breathing room.
    4. Corpus button (C1): a book-icon fixed button sits between the collapse button (top) and the recenter button (pushed down to 82px). Clicking it opens the corpus; its active state shows a muted-rose border (NOT amber). Hover raises opacity smoothly.
    5. Corpus view (B3 + C2 + state): opening the corpus hides the topics + search sections (panel/node-count/SSE stay). The graph renders as a contained legible cluster, not a sparse edge-scatter. With a fresh/empty corpus DB you see `No docs yet` + the `recense generate-doc <slug>` code hint; a transient `Loading corpus…` shows during fetch; a fetch failure shows `Failed to load corpus`.
    6. Detail panel: divider spacing and title bottom margin read evenly (12px / 8px) — no cramped gap.
    7. Palette sweep: confirm nothing at rest turned amber — rests stay muted rose/slate/mauve; amber only on hover/activation. The 3D brain density looks unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" once every surface matches the UI-SPEC, or describe any mismatch to fix.</resume-signal>
</task>

</tasks>

<threat_model>
No new threats — this plan only rebuilds dist and runs a human visual review; it introduces no code changes of its own. The net-zero-deps and no-structural-change guards in Task 1 are themselves the security-relevant check (confirming the phase added no attack surface).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | n/a | dist rebuild + visual review | accept | No new attack surface across the whole phase — Task 1 guard grep confirms package.json unchanged and only viz presentation files diffed |
</threat_model>

<verification>
- package.json runtime deps unchanged; npm run build exits 0; dist viz assets recopied.
- No amber introduced on any rest/non-activation selector (diff grep).
- Only src/viz/{css,index.html,modules/corpus.js} changed; 3D density untouched.
- Founder visually signs off on all four surfaces against the UI-SPEC checklist.
</verification>

<success_criteria>
Closes all three ROADMAP success criteria for Phase 34:
- SC1 (VIZ-POLISH-01): founder confirms consistent spacing/alignment across the four surfaces.
- SC2 (VIZ-POLISH-02): founder confirms loading/empty/error + hover/focus + smooth transitions on every async/interactive surface.
- SC3 (VIZ-POLISH-03): guard greps prove no amber-at-rest, unchanged density, CSS+state-only diff, and unchanged package.json deps.
</success_criteria>

<output>
Create `.planning/phases/34-visual-polish-pass/34-03-SUMMARY.md` when done — record the guard-grep results and the founder sign-off.
</output>
