---
phase: 34-visual-polish-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/viz/css/styles.css
  - src/viz/index.html
autonomous: true
requirements: [VIZ-POLISH-01, VIZ-POLISH-02, VIZ-POLISH-03]
user_setup: []

must_haves:
  truths:
    - "The reader close button stays visible (sticky head) while scrolling a long doc"
    - "In expanded (.mode-window) mode the actions row shows only [Show tombstones] [Reader] — no Log, no Test trace"
    - "Search and topics sections have a visible separation (border + breathing room) in expanded mode"
    - "Detail panel divider and title spacing land on the canonical 4px scale"
  artifacts:
    - path: "src/viz/css/styles.css"
      provides: "Sticky #reader-head, .mode-window declutter rules, #topic-wrap separation, normalized .divider + #detail-title spacing"
      contains: "position: sticky"
    - path: "src/viz/index.html"
      provides: "HUD markup with #btn-test-trace removed"
  key_links:
    - from: "src/viz/css/styles.css #reader-head"
      to: "scroll containment in #reader"
      via: "position: sticky; top: 0 with solid background bleeding to reader edges"
      pattern: "position:\\s*sticky"
---

<objective>
Polish the Reader overlay (R1 sticky close button), the Brain HUD expanded-mode declutter (B2), and the Detail panel spacing normalization (detail-spacing audit). All changes are verbatim from the approved 34-UI-SPEC Polish Contract — no design discretion.

Purpose: Resolve three founder-flagged / audit-flagged rough edges along the spacing-alignment and states-transitions axes without any structural change.
Output: Modified src/viz/css/styles.css and src/viz/index.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-visual-polish-pass/34-UI-SPEC.md

# CONSTRAINT (CLAUDE.md): net-zero new runtime deps; this is the viz PRESENTATION layer.
# PALETTE GUARD (founder-locked): amber #d9a05c / rgba(217,160,92,…) is reserved for
# hover/activation ONLY. Rest states stay muted mauve/slate/rose. Do not introduce amber
# on any rest/non-activation selector touched here.
</context>

<tasks>

<task type="auto">
  <name>Task 1: R1 — sticky reader close button</name>
  <files>src/viz/css/styles.css</files>
  <read_first>
    - src/viz/css/styles.css — the `#reader {` rule (~line 562) and the `#reader-head {` rule (~line 593), plus `#reader-close` (~line 608) and `#reader-close:hover` (~line 628)
    - .planning/phases/34-visual-polish-pass/34-UI-SPEC.md — section "R1 — Sticky Reader Close Button"
  </read_first>
  <action>
    In the `#reader` rule, change its padding to `padding: 0 26px 40px;` (removing the original `22px` top — it relocates into the head; preserve the 26px horizontal / 40px bottom exactly).

    In the `#reader-head` rule, add these declarations to make it sticky with a solid backing that bleeds to the reader edges so scrolling prose does not show through: `position: sticky; top: 0; z-index: 1; background: rgba(20, 14, 26, 0.97);` then `margin-left: -26px; margin-right: -26px; padding-left: 26px; padding-right: 28px;` (the 28px preserves clearance for the absolutely-positioned `×`), then `padding-top: 20px; padding-bottom: 12px;` and `margin-bottom: 6px;` (6px is the preexisting "tight gap" sm exception — keep it, do not change to 8px).

    Do NOT touch `#reader-close` or `#reader-close:hover` — they stay muted mauve at rest (`#8b7090`) and slate on hover (`#c8cfd8`), never amber.
  </action>
  <verify>
    <automated>grep -q 'padding: 0 26px 40px;' src/viz/css/styles.css && grep -q 'position: sticky;' src/viz/css/styles.css && grep -q 'background: rgba(20, 14, 26, 0.97);' src/viz/css/styles.css; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - styles.css `#reader` rule contains `padding: 0 26px 40px;` and no longer contains `22px 26px 40px`
    - styles.css `#reader-head` rule contains `position: sticky;`, `top: 0;`, `z-index: 1;`, `background: rgba(20, 14, 26, 0.97);`, `margin-left: -26px;`, `margin-right: -26px;`, `padding-right: 28px;`, `padding-top: 20px;`, `padding-bottom: 12px;`, `margin-bottom: 6px;`
    - `#reader-close` and `#reader-close:hover` rules are byte-for-byte unchanged (no amber introduced)
  </acceptance_criteria>
  <done>The reader head is sticky with a solid backing; close × stays in view on scroll; reader prose padding preserved at 26px/40px.</done>
</task>

<task type="auto">
  <name>Task 2: B2 — HUD declutter in expanded mode</name>
  <files>src/viz/index.html, src/viz/css/styles.css</files>
  <read_first>
    - src/viz/index.html — the `.actions-row` div (~line 39) containing `#btn-corpus`, `#btn-log`, `#btn-test-trace` (~lines 44-47)
    - src/viz/css/styles.css — the `#topic-wrap` rule (~line 534)
    - .planning/phases/34-visual-polish-pass/34-UI-SPEC.md — section "B2 — HUD Declutter in Expanded Mode"
  </read_first>
  <action>
    index.html: Remove the entire `<button id="btn-test-trace" class="btn-sm">Test trace</button>` element (and its preceding "Temporary: manual trace trigger" HTML comment if present) from `.actions-row`. Leave `#btn-corpus` and `#btn-log` in place — `#btn-corpus` is relocated by plan 34-02 (C1), not here.

    styles.css: Add a new rule `.mode-window #btn-log { display: none; }` so the Log button is hidden only in expanded-window mode (it stays visible in tray/popover mode where devtools are absent).

    styles.css: In the existing `#topic-wrap` rule (currently `#topic-wrap { display: none; margin-top: 12px; }`), change `margin-top: 12px` to `margin-top: 16px;` (lg token) and add `border-top: 1px solid rgba(140, 150, 165, 0.08);` and `padding-top: 12px;` (md token). Keep `display: none` (the `.mode-window #topic-wrap { display: block; }` override stays).
  </action>
  <verify>
    <automated>! grep -q 'btn-test-trace' src/viz/index.html && grep -q '.mode-window #btn-log { display: none; }' src/viz/css/styles.css && grep -q 'margin-top: 16px;' src/viz/css/styles.css && grep -q 'border-top: 1px solid rgba(140, 150, 165, 0.08);' src/viz/css/styles.css; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - index.html does NOT contain the string `btn-test-trace`
    - styles.css contains `.mode-window #btn-log { display: none; }`
    - `#topic-wrap` rule contains `margin-top: 16px;`, `border-top: 1px solid rgba(140, 150, 165, 0.08);`, and `padding-top: 12px;`
    - No amber introduced on any of these selectors (the border tint is muted slate rgba(140,150,165,0.08))
  </acceptance_criteria>
  <done>Expanded-mode actions row carries only the focused controls; Log hidden in window mode; topics section visually separated from search.</done>
</task>

<task type="auto">
  <name>Task 3: Detail panel spacing normalization</name>
  <files>src/viz/css/styles.css</files>
  <read_first>
    - src/viz/css/styles.css — the `#detail-title {` rule (~line 294) and the `.divider {` rule (~line 321)
    - .planning/phases/34-visual-polish-pass/34-UI-SPEC.md — section "Detail Panel — Spacing Normalization (audit finding)"
  </read_first>
  <action>
    styles.css: In the `.divider` rule, change `margin: 10px 0;` to `margin: 12px 0;` (snap to md token).

    styles.css: In the `#detail-title` rule, change `margin: 8px 0 6px;` to `margin: 8px 0 8px;` (snap the 6px bottom to the base 8px token). Leave all other `#detail-title` declarations untouched.
  </action>
  <verify>
    <automated>grep -Eq '\.divider[^}]*margin: 12px 0;' src/viz/css/styles.css && grep -Eq '#detail-title[^}]*margin: 8px 0 8px;' src/viz/css/styles.css; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - styles.css `.divider` rule contains `margin: 12px 0;` and no longer contains `margin: 10px 0;`
    - styles.css `#detail-title` rule contains `margin: 8px 0 8px;` and no longer contains `margin: 8px 0 6px;`
  </acceptance_criteria>
  <done>Detail divider and title spacing land on the canonical 4px scale; ≤2px visual adjustment, no regression.</done>
</task>

</tasks>

<threat_model>
No new threats — this plan is CSS edits plus one static HTML button removal. No new inputs, endpoints, auth, data flows, or runtime dependencies are introduced. The single trust boundary in the viz (browser ↔ read-only viz server) is unchanged; no STRIDE category applies to a pure presentation-layer diff.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | n/a | CSS + static HTML only | accept | No new attack surface — no inputs/endpoints/data handling; net-zero deps (verified in plan 34-03 guard grep) |
</threat_model>

<verification>
- styles.css contains the R1 sticky-head rule, the B2 declutter rules, and the normalized .divider / #detail-title margins.
- index.html no longer references btn-test-trace.
- No amber (#d9a05c / rgba(217, 160, 92) added on any rest selector — confirmed by the guard grep in plan 34-03.
- Dist rebuild + founder visual confirmation happen in plan 34-03 (wave 3).
</verification>

<success_criteria>
Maps to ROADMAP success criteria:
- SC1 (VIZ-POLISH-01, spacing/alignment): B2 topic separation + detail-spacing normalization land on the canonical scale.
- SC2 (VIZ-POLISH-02, states & transitions): R1 sticky close button keeps the close affordance reachable on long-doc scroll.
- SC3 (VIZ-POLISH-03, guards): no structural change, no amber-at-rest, no new deps (final guard verification in 34-03).
</success_criteria>

<output>
Create `.planning/phases/34-visual-polish-pass/34-01-SUMMARY.md` when done.
</output>
