---
phase: 34-visual-polish-pass
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - src/viz/modules/corpus.js
  - src/viz/css/styles.css
  - src/viz/index.html
autonomous: true
requirements: [VIZ-POLISH-01, VIZ-POLISH-02, VIZ-POLISH-03]
user_setup: []

must_haves:
  truths:
    - "Opening the corpus view hides the topics and search sections; returning to brain restores them"
    - "The corpus toggle is a book-icon fixed button in the collapse/recenter cluster (no longer a text button in the actions row)"
    - "The corpus 2D graph renders as a contained legible cluster, not a sparse edge-scatter"
    - "The corpus fetch shows a loading indicator, an empty state when there are no docs, and an error message on fetch failure"
  artifacts:
    - path: "src/viz/modules/corpus.js"
      provides: "topic/search hide-show in showCorpus/showBrain/returnToCorpus, aria-label/title button state (no textContent), d3Force charge/link tuning, corpus-status loading/empty/error overlay"
      contains: "G.d3Force('charge')"
    - path: "src/viz/css/styles.css"
      provides: "Icon-only fixed #btn-corpus rules, recenter push-down, .corpus-status overlay styles"
      contains: ".corpus-status"
    - path: "src/viz/index.html"
      provides: "#btn-corpus relocated to top-level sibling of #btn-recenter with inline book SVG"
      contains: "aria-label=\"Corpus graph\""
  key_links:
    - from: "src/viz/modules/corpus.js showCorpus()"
      to: "#topic-wrap / #search-wrap display"
      via: "style.display = 'none' on open, '' on showBrain"
      pattern: "getElementById\\('topic-wrap'\\)"
    - from: "src/viz/modules/corpus.js buildCorpusGraph()"
      to: "d3-force charge/link held by the force-graph instance"
      via: "G.d3Force('charge').strength(-80) + G.d3Force('link').distance(50)"
      pattern: "d3Force\\('charge'\\)"
---

<objective>
Polish the Corpus surface: hide topics/search in corpus view (B3), relocate the corpus toggle to an icon-only fixed button in the button cluster (C1), tune the force layout for a contained graph (C2), and add loading/empty/error state coverage to the corpus fetch. All changes are verbatim from the approved 34-UI-SPEC Polish Contract.

Purpose: Resolve the corpus-surface rough edges along both the spacing-alignment (C1, C2) and states-transitions (B3, corpus-state) axes.
Output: Modified src/viz/modules/corpus.js, src/viz/css/styles.css, src/viz/index.html.

Sequencing: depends on 34-01 because both plans edit src/viz/css/styles.css and src/viz/index.html — they cannot run in the same wave (execute-phase parallelizes only disjoint files_modified). 34-01 owns the reader/HUD/detail edits; this plan owns the corpus-toggle button block (the #btn-corpus rules near line 743) and the corpus-status overlay — disjoint regions of the same files, applied sequentially.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-visual-polish-pass/34-UI-SPEC.md
@.planning/phases/34-visual-polish-pass/34-01-SUMMARY.md

<interfaces>
<!-- Anchor points in corpus.js the executor must edit (extracted from live source) -->
- `showCorpus()` (~line 194): open-block ends ~line 216 with `container.classList.add('open')` then `corpusBtn.textContent = 'Brain'; corpusBtn.classList.add('corpus-active'); setTimeout(... fitAndClamp, 350)`.
- `showBrain()` (~line 219): `container.classList.remove('open'); ... corpusBtn.textContent = 'Corpus'; corpusBtn.classList.remove('corpus-active')`.
- `ctx.returnToCorpus` (~line 241): `container.classList.add('open'); ... corpusBtn.textContent = 'Brain'; corpusBtn.classList.add('corpus-active')`.
- `buildCorpusGraph()` (~line 72): `fetch('/graph?type=doc')`; `data = await res.json()` in a try/catch (~lines 77-82); nodeSlugs/nodeLabels loop (~lines 86-94); the `ForceGraph()(container)` chain returns `G` at ~line 148.
- `corpusBtn` is `document.getElementById('btn-corpus')` (~line 56); `container` is the `#corpus-graph` element.
- The three `corpusBtn.textContent = 'Brain'|'Corpus'` assignments (lines ~212, ~223, ~245) MUST be removed and replaced with aria-label/title updates (C1). The `.corpus-active` class add/remove stays.
</interfaces>

# PALETTE GUARD (founder-locked): amber reserved for hover/activation ONLY.
# Corpus-status palette is muted mauve/slate (#6b5f73, #8b7090). #btn-corpus active is
# muted ROSE border rgba(156,112,128,0.55) — NOT amber. Do not introduce amber at rest.
# NET-ZERO DEPS: the d3Force tuning reads the force-graph instance's internal d3-force
# refs — no import, no new dependency.
</context>

<tasks>

<task type="auto">
  <name>Task 1: B3 — hide topics/search in corpus view</name>
  <files>src/viz/modules/corpus.js</files>
  <read_first>
    - src/viz/modules/corpus.js — `showCorpus()` (~line 194, open-block tail ~line 211), `showBrain()` (~line 219), `ctx.returnToCorpus` (~line 241)
    - .planning/phases/34-visual-polish-pass/34-UI-SPEC.md — section "B3 — Topics Hidden in Corpus View"
  </read_first>
  <action>
    In `showCorpus()`, after `container.classList.add('open')`, add: resolve `const topicWrap = document.getElementById('topic-wrap');` and `const searchWrap = document.getElementById('search-wrap');`, then `if (topicWrap) topicWrap.style.display = 'none';` and `if (searchWrap) searchWrap.style.display = 'none';`.

    In `showBrain()`, after `container.classList.remove('open')`, add the same two element lookups and set `topicWrap.style.display = '';` and `searchWrap.style.display = '';` (restore default).

    In `ctx.returnToCorpus`, after `container.classList.add('open')`, add the same lookups and set both to `'none'` (keep topics hidden when the reader closes back to corpus).

    Do NOT hide `#panel` (node count / SSE status stay visible in corpus view).
  </action>
  <verify>
    <automated>grep -c "getElementById('topic-wrap')" src/viz/modules/corpus.js | grep -q '^3$' && grep -q "topicWrap.style.display = 'none';" src/viz/modules/corpus.js && grep -q "topicWrap.style.display = '';" src/viz/modules/corpus.js; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - corpus.js references `getElementById('topic-wrap')` exactly 3 times (showCorpus, showBrain, returnToCorpus)
    - showCorpus and returnToCorpus set `topicWrap.style.display = 'none'` and `searchWrap.style.display = 'none'`
    - showBrain sets `topicWrap.style.display = ''` and `searchWrap.style.display = ''`
    - `#panel` is not hidden anywhere in this change
  </acceptance_criteria>
  <done>Topics and search sections hide on corpus open / return-to-corpus and restore on brain view.</done>
</task>

<task type="auto">
  <name>Task 2: C1 — corpus icon button in the fixed cluster</name>
  <files>src/viz/index.html, src/viz/css/styles.css, src/viz/modules/corpus.js</files>
  <read_first>
    - src/viz/index.html — `.actions-row` `#btn-corpus` (~line 44) and the top-level `#btn-recenter` (~line 81)
    - src/viz/css/styles.css — `#btn-corpus` / `.mode-window #btn-corpus` / `#btn-corpus.corpus-active` rules (~lines 743-752) and `.mode-window #btn-recenter` (~line 179)
    - src/viz/modules/corpus.js — the three `corpusBtn.textContent` assignments (~lines 212, 223, 245)
    - .planning/phases/34-visual-polish-pass/34-UI-SPEC.md — section "C1 — Corpus Icon Button in Fixed Button Cluster"
  </read_first>
  <action>
    index.html: Move `#btn-corpus` OUT of `.actions-row` so it is a top-level sibling of `#btn-recenter` (place it adjacent to `#btn-recenter`). Replace its text content with the inline book SVG from the UI-SPEC C1 section (a 14×14 viewBox="0 0 24 24" stroke="currentColor" book glyph with the two `<path>` elements given). Set its attributes to `id="btn-corpus" type="button" aria-label="Corpus graph" title="Corpus"` and remove the `class="btn-sm"`.

    styles.css: Replace the existing `#btn-corpus` / `.mode-window #btn-corpus` / `#btn-corpus.corpus-active` block (~lines 743-752) with the UI-SPEC C1 rules: `#btn-corpus { display: none; }`; a `.mode-window #btn-corpus { ... }` rule positioning it `position: fixed; top: 46px; right: 12px; width: 30px; height: 30px;` with `display: flex; align-items: center; justify-content: center; z-index: 70; border-radius: 7px; background: rgba(26, 18, 32, 0.7); border: none; color: #d9cbc0; opacity: 0.65;` plus `-webkit-app-region: no-drag;` and `transition: opacity 0.15s ease;`; a `.mode-window #btn-corpus:hover { opacity: 1; }`; and the active rule `.mode-window #btn-corpus.corpus-active { opacity: 1; color: #c8bcd0; border: 1px solid rgba(156, 112, 128, 0.55); }` (muted rose — NOT amber). Copy the exact values from the UI-SPEC.

    styles.css: Add/modify `.mode-window #btn-recenter { top: 82px; }` to push recenter below the new corpus button (per the UI-SPEC comment: 10 + 30 + 6 + 30 + 6 = 82px). Preserve all other `#btn-recenter` declarations.

    corpus.js: Remove all three `corpusBtn.textContent = 'Brain'|'Corpus'` assignments. In `showCorpus()` (and `returnToCorpus`) set `corpusBtn.setAttribute('aria-label', 'Show brain'); corpusBtn.setAttribute('title', 'Show brain');`. In `showBrain()` set `corpusBtn.setAttribute('aria-label', 'Corpus graph'); corpusBtn.setAttribute('title', 'Corpus');`. Keep the `.corpus-active` class add/remove exactly as-is.
  </action>
  <verify>
    <automated>grep -q 'aria-label="Corpus graph"' src/viz/index.html && ! grep -q 'btn-corpus" class="btn-sm"' src/viz/index.html && grep -q 'top: 46px;' src/viz/css/styles.css && grep -q 'border: 1px solid rgba(156, 112, 128, 0.55);' src/viz/css/styles.css && grep -q '.mode-window #btn-recenter { top: 82px; }' src/viz/css/styles.css && ! grep -q "corpusBtn.textContent" src/viz/modules/corpus.js; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - index.html `#btn-corpus` is a sibling of `#btn-recenter` (not inside `.actions-row`), carries `aria-label="Corpus graph"` and `title="Corpus"`, contains the inline `<svg>` book glyph, and no longer has `class="btn-sm"` or the text `Corpus`
    - styles.css `.mode-window #btn-corpus` is `position: fixed; top: 46px; right: 12px; width: 30px; height: 30px;` with `border: none; color: #d9cbc0; opacity: 0.65;` and `transition: opacity 0.15s ease;`
    - styles.css `.mode-window #btn-corpus.corpus-active` border is `rgba(156, 112, 128, 0.55)` (muted rose) — NOT amber / not rgba(217, 160, 92
    - styles.css contains `.mode-window #btn-recenter { top: 82px; }`
    - corpus.js contains zero `corpusBtn.textContent` assignments; state is conveyed via `setAttribute('aria-label', …)` / `setAttribute('title', …)` and the `.corpus-active` class
  </acceptance_criteria>
  <done>The corpus toggle is an icon-only fixed book button between collapse and recenter; active state shows a muted-rose border; button state is screen-reader-conveyed via aria-label/title.</done>
</task>

<task type="auto">
  <name>Task 3: C2 force tuning + corpus loading/empty/error state coverage</name>
  <files>src/viz/modules/corpus.js, src/viz/css/styles.css</files>
  <read_first>
    - src/viz/modules/corpus.js — `buildCorpusGraph()` (~line 72): the `fetch`/try-catch (~lines 77-82), the nodeSlugs/nodeLabels loop (~lines 86-94), the `ForceGraph()(container)` chain ending `return G;` (~line 148)
    - src/viz/css/styles.css — end of file (append the `.corpus-status` rules)
    - .planning/phases/34-visual-polish-pass/34-UI-SPEC.md — sections "C2 — Corpus Force Layout" and "Corpus State Coverage"
  </read_first>
  <action>
    C2 — force tuning (corpus.js): After the `ForceGraph()(container)…` chain (i.e. after `G` is assigned, before `return G;`), add a try/catch that reads the force-graph instance's internal d3-force refs and tunes them: `const charge = G.d3Force('charge'); if (charge && typeof charge.strength === 'function') charge.strength(-80);` and `const link = G.d3Force('link'); if (link && typeof link.distance === 'function') link.distance(50);` wrapped in `try { … } catch (_) { /* non-fatal */ }`. Do NOT add imports (net-zero deps). Leave `MAX_ZOOM`, `fitAndClamp()`, and the 350ms fallback in `showCorpus()` unchanged.

    Corpus state coverage (corpus.js): At the very start of `buildCorpusGraph()`, before the `fetch`, create the loading overlay: `const statusEl = document.createElement('div'); statusEl.className = 'corpus-status'; statusEl.textContent = 'Loading corpus…'; container.appendChild(statusEl);`. In the existing `catch` block around the fetch, set `statusEl.textContent = 'Failed to load corpus';`. After the nodeSlugs/nodeLabels loop, add the empty-state + cleanup: `if ((data.nodes || []).length === 0) { statusEl.innerHTML = 'No docs yet<br><code>recense generate-doc &lt;slug&gt;</code>'; return null; }` then `statusEl.remove();` (remove the loading overlay before the first graph paint when data is present). Copy the exact copy strings from the UI-SPEC Copywriting Contract.

    Corpus state coverage (styles.css): Append the `.corpus-status` and `.corpus-status code` rules from the UI-SPEC verbatim — `.corpus-status { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); text-align: center; pointer-events: none; color: #6b5f73; font-size: 13px; font-style: italic; line-height: 1.8; }` and `.corpus-status code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: 11px; color: #8b7090; font-style: normal; }`. Muted mauve/slate — no amber.
  </action>
  <verify>
    <automated>grep -q "G.d3Force('charge')" src/viz/modules/corpus.js && grep -q "charge.strength(-80)" src/viz/modules/corpus.js && grep -q "link.distance(50)" src/viz/modules/corpus.js && grep -q "statusEl.textContent = 'Loading corpus…'" src/viz/modules/corpus.js && grep -q "statusEl.textContent = 'Failed to load corpus'" src/viz/modules/corpus.js && grep -q 'No docs yet' src/viz/modules/corpus.js && grep -q '.corpus-status {' src/viz/css/styles.css; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - corpus.js `buildCorpusGraph()` calls `G.d3Force('charge')` with `.strength(-80)` and `G.d3Force('link')` with `.distance(50)`, both guarded in a try/catch, no new import
    - corpus.js creates a `.corpus-status` element with `Loading corpus…` before the fetch, sets `Failed to load corpus` in the catch, renders the `No docs yet` + `<code>recense generate-doc &lt;slug&gt;</code>` empty state and `return null` when `data.nodes.length === 0`, and calls `statusEl.remove()` before the first paint when data is present
    - styles.css contains `.corpus-status {` with `color: #6b5f73;` and `.corpus-status code {` with `color: #8b7090;` — no amber / no rgba(217, 160, 92 on these selectors
  </acceptance_criteria>
  <done>The corpus graph renders as a contained cluster; the fetch shows loading, empty, and error states in the muted palette.</done>
</task>

</tasks>

<threat_model>
No new threats — this plan is client-side DOM state toggling, force-param tuning on an already-loaded library, CSS, and one static HTML relocation. No new inputs, endpoints, auth, data flows, or runtime dependencies. The `/graph?type=doc` fetch already existed; the only change is rendering loading/empty/error UI around its existing result. No STRIDE category applies to a presentation-layer diff.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02 | n/a | corpus.js DOM toggling + CSS, no new fetch/input | accept | No new attack surface — empty/error copy is static literals (no interpolation of fetched data into innerHTML beyond fixed strings); net-zero deps (verified in 34-03 guard grep) |
</threat_model>

<verification>
- corpus.js: 3× topic/search hide-show, zero textContent assignments (replaced by aria-label/title), d3Force charge -80 / link 50, corpus-status loading/empty/error overlay.
- index.html: #btn-corpus relocated with inline SVG + aria-label.
- styles.css: icon-only #btn-corpus block, recenter top:82px, .corpus-status overlay; no amber at rest.
- Dist rebuild + founder visual confirmation in plan 34-03 (wave 3).
</verification>

<success_criteria>
Maps to ROADMAP success criteria:
- SC1 (VIZ-POLISH-01): C1 button placement + C2 contained framing resolve the corpus spacing/alignment rough edges.
- SC2 (VIZ-POLISH-02): B3 mode-state visibility + corpus loading/empty/error coverage close the states-transitions gap on the corpus surface.
- SC3 (VIZ-POLISH-03): corpus-active uses muted rose (not amber); no structural change; net-zero deps (final guard in 34-03).
</success_criteria>

<output>
Create `.planning/phases/34-visual-polish-pass/34-02-SUMMARY.md` when done.
</output>
