---
phase: 41-vector-index-and-hot-path-latency
plan: 03
type: execute
wave: 3
depends_on: ["41-02"]
files_modified:
  - scripts/eval/41-topk-equivalence.cjs
  - scripts/eval/results/41-topk-equivalence.json
  - scripts/eval/results/41-latency-after.json
  - .planning/phases/41-vector-index-and-hot-path-latency/41-PERF-REPORT.md
autonomous: true
requirements: [PERF-02, PERF-03]

must_haves:
  truths:
    - "Index top-k is asserted SET-identical to brute-force top-k over a query set (± identical-score float tie reorder) — PERF-03 by construction"
    - "LongMemEval-S, KU replay, and LOCOMO accuracy are unchanged vs the Phase 40 baseline result JSONs — a latency win that costs accuracy is rejected"
    - "Recall + cold SessionStart-inject p50/p95 are reported strictly below the Phase 40 baseline beyond run-to-run noise, with the cold wall-clock as the headline felt number"
    - "A soft latency target is set AFTER the numbers are in hand (no pre-committed hard ms SLA)"
  artifacts:
    - path: "scripts/eval/41-topk-equivalence.cjs"
      provides: "Direct index.topk == bruteforce.topk equivalence assertion over a query set"
      min_lines: 40
    - path: ".planning/phases/41-vector-index-and-hot-path-latency/41-PERF-REPORT.md"
      provides: "PERF-02 delta vs baseline + PERF-03 equivalence + 3-harness accuracy verdict"
      contains: "PERF-02"
  key_links:
    - from: "scripts/eval/41-topk-equivalence.cjs"
      to: "indexed CandidateRetriever + cosineSimF32 brute-force reference"
      via: "compare top-k sets per query"
      pattern: "cosineSimF32|topk"
    - from: "41-PERF-REPORT.md"
      to: "scripts/eval/results/ Phase 40 baselines"
      via: "delta comparison"
      pattern: "baseline|45"
---

<objective>
Prove the two hard gates of the phase: PERF-03 (no accuracy regression) and PERF-02 (measurable latency win vs the Phase 40 baseline). Because the index is exact (D-01), PERF-03 is cheap to assert directly — `index.topk == bruteforce.topk` over a query set — and is then confirmed by re-running the full eval suite. PERF-02 is reported as a delta vs the committed Phase 40 baseline, with the COLD SessionStart-inject wall-clock as the headline felt number (D-08), and a soft target set only after the numbers are in hand.

Purpose: Lock the win honestly. A latency win that costs accuracy is REJECTED. No inflated metrics — every number is a re-run recense produced itself against the committed baseline JSONs.

Output: An equivalence assertion script + a perf report that records the gate verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-vector-index-and-hot-path-latency/41-CONTEXT.md
@.planning/phases/40-competitive-benchmark-baseline/40-BASELINE.md
@.planning/phases/41-vector-index-and-hot-path-latency/41-SPIKE-FINDINGS.md

<interfaces>
<!-- Gate inputs. -->
- Brute-force reference: `cosineSimF32` + the original `CandidateRetriever.topk` scan in src/retrieval/topk.ts (D-10 compares against this).
- Indexed path: the same `CandidateRetriever.topk` constructed WITH the persisted index (from Plan 41-02).
- Accuracy harnesses (re-run all three for PERF-03): `scripts/eval/longmemeval-harness.cjs` (has the instrumentTopkResults tap), `scripts/eval/replay-ku-harness.cjs` (cached-extraction, cheap), `scripts/eval/locomo-harness.cjs` + `locomo-scorer.cjs` + `locomo10.json`.
- Baseline anchors (PERF-02/03 compare against these committed JSONs): `scripts/eval/results/locomo-d41d5c8.json`, `scripts/eval/results/locomo-latency-curve-d41d5c8.json`, and the live-brain 45/46 ms warm number in 40-BASELINE.md. Latency tooling to reuse: `scripts/eval/live-latency.cjs` (warm) + the spike's cold harness `scripts/eval/41-index-spike.cjs` (cold).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Direct top-k equivalence assertion (PERF-03 by construction)</name>
  <read_first>
    - src/retrieval/topk.ts (the indexed CandidateRetriever from 41-02 + cosineSimF32 = the brute-force reference)
    - .planning/phases/41-vector-index-and-hot-path-latency/41-CONTEXT.md (D-10: top-k SET identical, tolerating reorder only within identical-score float ties)
    - scripts/eval/41-index-spike.cjs (REUSE its read-only live-db open + embed + query set patterns)
  </read_first>
  <action>
    Create `scripts/eval/41-topk-equivalence.cjs` — a read-only, live-brain (or fixture) check that, for each query in a representative set, computes the top-k id list TWO ways: (a) the BRUTE-FORCE `cosineSimF32` scan (the D-10 reference), and (b) the INDEXED `CandidateRetriever.topk` constructed with the persisted index. Assert the two top-k id SETS are identical, tolerating reorder ONLY within identical-score float ties (compare scores at tie boundaries to allow C/SIMD-vs-JS float sum-order differences per D-10's "± tie reorder" allowance). Run at the live `candidateK` and at a larger k (e.g. 10/20) to cover the hybridTopk preK fetch. Emit `scripts/eval/results/41-topk-equivalence.json` with per-query pass/fail + an overall `equivalent: true/false`. Exit non-zero if any query's set diverges beyond a tie reorder.
  </action>
  <verify>
    <automated>node scripts/eval/41-topk-equivalence.cjs && node -e "const r=require('./scripts/eval/results/41-topk-equivalence.json'); if(!r.equivalent) process.exit(1); console.log('top-k equivalent')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/eval/41-topk-equivalence.cjs` exits 0 and writes `41-topk-equivalence.json` with `equivalent: true`.
    - For every query, the indexed top-k id set equals the brute-force `cosineSimF32` top-k id set, tolerating reorder only within identical-score float ties (D-10).
    - The check covers both `candidateK` and a larger k spanning the hybridTopk preK fetch.
    - The script is read-only on the live brain (no writes).
  </acceptance_criteria>
  <done>A direct, cheap assertion confirms the indexed top-k is set-equivalent to brute-force — PERF-03 satisfied by construction, independent of the expensive eval.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the three accuracy harnesses + cold/warm latency, write the PERF report</name>
  <read_first>
    - .planning/phases/40-competitive-benchmark-baseline/40-BASELINE.md (the committed baseline: 45/46 ms warm live, the locomo accuracy + latency-curve anchors, the frozen v7.0 config)
    - scripts/eval/replay-ku-harness.cjs (cheap cached-extraction re-run — start here)
    - scripts/eval/locomo-harness.cjs + scripts/eval/longmemeval-harness.cjs (the other two accuracy gates)
    - scripts/eval/live-latency.cjs (warm latency, reuse) + scripts/eval/41-index-spike.cjs (cold latency harness from 41-01)
    - scripts/eval/results/locomo-d41d5c8.json + scripts/eval/results/locomo-latency-curve-d41d5c8.json (the comparison anchors)
  </read_first>
  <action>
    With the index built (run one sleep pass or invoke the 41-02 build step so the persisted artifact exists), re-run the three accuracy harnesses on the indexed path and compare each score to its Phase 40 baseline result JSON: start with `replay-ku-harness.cjs` (cheap cached-extraction), then `locomo-harness.cjs` (compare to `locomo-d41d5c8.json`), then `longmemeval-harness.cjs`. The accuracy gate (PERF-03) is: scores UNCHANGED vs baseline (the index is exact, so any drift signals a wiring bug, not an approximation). If any harness has a paid-API cost of $3 or more, STOP and request approval before running it (budget discipline; KU replay and the equivalence check are the cheap primary signals).

    For PERF-02, measure latency on the INDEXED path two ways and compare to baseline: WARM via `live-latency.cjs` (vs the 45/46 ms baseline) and COLD via the `41-index-spike.cjs` cold harness (the felt SessionStart-inject / recall-cli path). Write `scripts/eval/results/41-latency-after.json` with the indexed p50/p95 for both modes.

    Then WRITE `.planning/phases/41-vector-index-and-hot-path-latency/41-PERF-REPORT.md` recording: (a) PERF-03 — the top-k equivalence result (Task 1) + the three-harness accuracy table (after vs baseline, must be unchanged); (b) PERF-02 — recall + cold SessionStart-inject p50/p95 after-vs-baseline with the COLD wall-clock as the headline felt number, stated as a delta strictly below baseline beyond run-to-run noise; (c) the SOFT latency target set NOW that the numbers are in hand (per D-08 — no hard ms SLA was pre-committed); (d) an honest-framing note (no inflated metrics: every number is a recense self-run vs the committed Phase 40 baseline). If PERF-02's win is within run-to-run noise, say so plainly and record it rather than inflating.
  </action>
  <verify>
    <automated>node scripts/eval/replay-ku-harness.cjs && test -f .planning/phases/41-vector-index-and-hot-path-latency/41-PERF-REPORT.md && grep -q "PERF-02" .planning/phases/41-vector-index-and-hot-path-latency/41-PERF-REPORT.md && grep -q "PERF-03" .planning/phases/41-vector-index-and-hot-path-latency/41-PERF-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - `replay-ku-harness.cjs` re-runs clean and its score matches the Phase 40 KU baseline (no regression).
    - LOCOMO score matches `locomo-d41d5c8.json` (no regression). PERF-03 is NOT complete until LongMemEval-S EITHER runs and matches its baseline, OR is explicitly approved (by the user, in their own words) as deferred-to-Phase-43 with the reason recorded in 41-PERF-REPORT.md. "Deferred, approval pending" is NOT a pass state — if its paid cost is at/over the $3 gate, request approval and block on it rather than recording a pass.
    - `41-latency-after.json` records indexed warm + cold p50/p95; `41-PERF-REPORT.md` states the PERF-02 delta vs baseline with the COLD wall-clock as the headline.
    - `41-PERF-REPORT.md` contains both `PERF-02` and `PERF-03` sections, a soft target set after the numbers, and an honest-framing note; any within-noise result is reported as such, not inflated.
  </acceptance_criteria>
  <done>PERF-03 (equivalence + unchanged 3-harness accuracy) and PERF-02 (cold+warm latency strictly below baseline beyond noise, cold headline) are both recorded honestly in 41-PERF-REPORT.md with a soft target set after the fact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate scripts → live recense.db | Read-only measurement over the founder's own brain. |
| eval harnesses → OpenAI embed / headless answer-gen | Existing external calls from the Phase 40 harnesses, reused unchanged. |

Honest surface read: these are read-only measurement + comparison scripts reusing the existing Phase 40 harness machinery. No new attack surface is introduced — the gate scripts only read the live brain and compare against committed baseline JSONs. The realistic threat surface is minimal.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07 | Spoofing | inflated / cherry-picked numbers in the report | mitigate | Hard-rule "no inflated metrics": every PERF number is a recense self-run vs the committed Phase 40 baseline JSON; within-noise results are reported as within-noise, not as wins. The equivalence assertion is mechanical (exit-non-zero on divergence). |
| T-41-08 | Tampering | gate scripts mutate the live brain | mitigate | All gate scripts open the live DB read-only; eval harnesses use their existing scratch/cached paths. |
| T-41-09 | Information disclosure | report contents | accept | The report holds only aggregate scores + timings from the founder's own local brain; no new exposure. |
</threat_model>

<verification>
- `node scripts/eval/41-topk-equivalence.cjs` exits 0 with `equivalent: true`.
- `replay-ku-harness.cjs` re-run matches the KU baseline; LOCOMO matches `locomo-d41d5c8.json`.
- `41-latency-after.json` + `41-PERF-REPORT.md` exist with PERF-02 (cold headline delta) and PERF-03 (equivalence + accuracy) sections and a post-hoc soft target.
</verification>

<success_criteria>
PERF-03: indexed top-k is set-equivalent to brute-force AND the three accuracy harnesses show no regression vs the Phase 40 baseline. PERF-02: recall + cold SessionStart-inject p50/p95 are strictly below the Phase 40 baseline beyond run-to-run noise, with the cold wall-clock as the headline felt number and a soft target set after the fact — reported honestly with no inflated metrics.
</success_criteria>

<output>
Create `.planning/phases/41-vector-index-and-hot-path-latency/41-03-SUMMARY.md` when done.
</output>
