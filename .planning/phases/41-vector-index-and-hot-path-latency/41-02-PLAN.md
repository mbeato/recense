---
phase: 41-vector-index-and-hot-path-latency
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - src/retrieval/topk.ts
  - src/consolidation/run-sleep-pass.ts
  - src/adapter/session-start-cli.ts
  - src/adapter/recall-cli.ts
  - src/adapter/ambient-recall.ts
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "Online recall (recall-cli, SessionStart inject, ambient) reads a persisted exact index instead of re-marshaling every node row per invocation"
    - "The index returns real cosine SCORES so the floor gate (engine.ts ~388) and hybridTopk's preK/score-map keep working unchanged"
    - "The index is built/refreshed at the END of the sleep pass and persisted to disk (derived, rebuildable, never authoritative) — D-05: rebuilt at end of sleep pass, build cost stays offline"
    - "The offline consolidator still uses brute-force topk (NOT the end-of-pass index) — D-07: online hot path only, offline consolidator stays brute-force"
    - "CandidateRetriever.topk / hybridTopk / topkTombstoned signatures are unchanged (HNSW-later seam stays implicit; no new VectorIndex interface) — D-01: exact KNN now, HNSW deferred behind the stable seam"
  artifacts:
    - path: "src/retrieval/topk.ts"
      provides: "Exact index loaded from a persisted artifact behind the existing CandidateRetriever methods"
      contains: "topk"
    - path: "src/consolidation/run-sleep-pass.ts"
      provides: "End-of-pass index build/persist hook (offline)"
      contains: "index: rebuilt"
  key_links:
    - from: "src/adapter/session-start-cli.ts"
      to: "persisted index artifact"
      via: "CandidateRetriever reads the index when present"
      pattern: "CandidateRetriever"
    - from: "src/consolidation/run-sleep-pass.ts"
      to: "index build at end of pass"
      via: "build/persist after consolidation completes"
      pattern: "Sleep pass complete"
---

<objective>
Build the exact vector index the spike chose (default zero-dep per D-04, with a documented escalation branch to sqlite-vec only if the spike recorded that JS failed the PERF-02 bar) BEHIND the existing `CandidateRetriever.topk` / `hybridTopk` / `topkTombstoned` signatures. The index is a derived, rebuildable cache (PERF-01): graph stays source of truth, the index is persisted so cold processes read a ready artifact (D-06), and it is built at the end of the offline sleep pass (D-05). The offline consolidator KEEPS brute-force (D-07).

Purpose: Kill the per-invocation cost on the online hot path — today every cold recall re-loads and re-decodes ~11k embedding BLOBs into ~11k `Float32Array` views. The index replaces only the cosine list; BM25 (FTS5), strength, and RRF fusion stay exactly as-is.

Output: An indexed online recall path that returns byte-equivalent cosine top-k, with the HNSW-later seam kept implicit (no new abstraction).

CRITICAL: This plan consumes the spike decision. Read `41-SPIKE-FINDINGS.md` FIRST and implement the mechanism it chose. Do NOT re-litigate the choice; do NOT hardcode the opposite of what the spike recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-vector-index-and-hot-path-latency/41-CONTEXT.md
@.planning/phases/41-vector-index-and-hot-path-latency/41-SPIKE-FINDINGS.md

<interfaces>
<!-- Stable seam to preserve. Extracted from src/retrieval/topk.ts. DO NOT change these signatures (D-02). -->

```typescript
export class CandidateRetriever {
  constructor(db: Database.Database); // EXISTING — extend to optionally load a persisted index
  topk(queryVec: Float32Array, k: number): Array<{ id: string; score: number }>;          // real cosine scores
  topkTombstoned(queryVec: Float32Array, k: number): Array<{ id: string; score: number }>;
  hybridTopk(queryVec: Float32Array, queryText: string, k: number, preK?: number,
             strengthWeight?: number, nowMs?: number, lambda?: number): Array<{ id: string; score: number }>;
}
export function cosineSimF32(a: Float32Array, b: Float32Array): number; // exact reference; index output must equal this
```

Consumers that depend on the SCORE contract (do NOT alter their behavior):
- engine.ts ~line 388 `retrieveRanked` — floor gate compares the returned cosine `score`.
- topk.ts `hybridTopk` — `cosineScoreMap` built from `topk(queryVec, preK)`; the score map MUST keep real cosine values (BM25-only hits stay score=0).
- engine.ts ~lines 538/551 `retrieve` — uses `topk` (live, candidateK) then `topkTombstoned` (deleted scan) for D-29 classification.

Consumer that must STAY brute-force (D-07):
- consolidator.ts line ~682: `this.retriever.topk(queryVec, this.config.candidateK)` runs MID-pass on embeddings it is actively writing, BEFORE the end-of-pass index exists. Its `CandidateRetriever` must be constructed WITHOUT the index (brute-force mode), so it never reads a stale end-of-pass index.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the exact persisted index behind CandidateRetriever, brute-force fallback preserved</name>
  <read_first>
    - .planning/phases/41-vector-index-and-hot-path-latency/41-SPIKE-FINDINGS.md (THE decision — which mechanism + persistence artifact to build)
    - src/retrieval/topk.ts (the seam: topk/topkTombstoned/hybridTopk + cosineSimF32; the embedding BLOB decode = Pitfall 5; the L-2 dim-mismatch skip)
    - src/lib/config.ts (embeddingDimensions=1536; candidateK; the path knobs the index must preserve)
    - src/consolidation/consolidator.ts (line ~682 — the consolidator's retriever.topk that MUST stay brute-force per D-07)
  </read_first>
  <behavior>
    - Test 1: index-backed topk returns the SAME top-k id set as the brute-force cosineSimF32 scan over a fixture DB (± identical-score tie reorder), with real cosine scores (not RRF, not 0).
    - Test 2: when no persisted index artifact is present, CandidateRetriever falls back to the existing brute-force scan (zero behavior change) — the consolidator path relies on this.
    - Test 3: topkTombstoned behavior is preserved (either index-backed or left brute-force per the spike's tombstoned-scan verdict); the 'deleted' classification still works.
    - Test 4: hybridTopk's returned `score` field still carries real cosine values from the index path; BM25-only hits retain score=0.
  </behavior>
  <action>
    In `src/retrieval/topk.ts`, add an exact index to `CandidateRetriever` BEHIND the existing methods — no new exported interface, the HNSW seam stays implicit (D-02). Implement the mechanism the spike chose in `41-SPIKE-FINDINGS.md`:
    - DEFAULT (zero-dep, D-04): load the persisted serialized flat-buffer sidecar (one contiguous `Float32Array` of all live embeddings + precomputed-norms array + parallel id array) ONCE in the constructor when the artifact exists; `topk` scans the flat buffer using cosine computed against precomputed norms, returning real cosine scores byte-equivalent to `cosineSimF32`. Preserve the L-2 dim-mismatch skip.
    - ESCALATION (only if the spike recorded zero-dep failed PERF-02): attach the on-disk `vec0` virtual table and run its exact KNN, mapping the distance back to a cosine score so the floor gate is fed real cosine.

    Construction contract: the index is OPT-IN. Add a constructor option / second arg (e.g. `{ indexPath }` or an `enableIndex` flag) defaulting such that EXISTING callers without it stay brute-force — specifically the consolidator's `CandidateRetriever` (consolidator.ts ~682) must remain brute-force (D-07). When the persisted artifact is absent or fails to load, fall back to the brute-force scan with a one-line warning (the index is a derived cache, never authoritative — graph is source of truth). `hybridTopk` keeps calling `this.topk(queryVec, preK)` so its `cosineScoreMap` automatically uses the indexed scores; do NOT touch the RRF/strength/BM25 fusion logic. Decide `topkTombstoned` per the spike's verdict (index it or leave brute-force — the tombstoned set is small). Keep all three method SIGNATURES unchanged.

    Add a vitest spec `src/retrieval/topk.index.test.ts` (or extend the existing topk test) implementing the four behaviors above against a small fixture DB.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/retrieval/topk.index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - The new/extended vitest spec passes all four behaviors; index-backed `topk` returns a top-k id set identical to the brute-force `cosineSimF32` scan over the fixture (± identical-score tie reorder) with real cosine scores.
    - `CandidateRetriever.topk` / `topkTombstoned` / `hybridTopk` signatures are byte-for-byte unchanged (no new exported VectorIndex type) — `git diff` shows only added members/options, not signature changes.
    - With no persisted artifact, `topk` falls back to the brute-force scan (a fixture without the index file still returns correct top-k).
    - `hybridTopk`'s returned `score` map carries real cosine values from the index path; BM25-only hits stay 0.
  </acceptance_criteria>
  <done>The exact index lives behind the unchanged CandidateRetriever methods, returns real cosine scores, falls back to brute-force when absent, and the consolidator stays brute-force.</done>
</task>

<task type="auto">
  <name>Task 2: Build + persist the index at end of sleep pass; wire cold online callers to read it</name>
  <read_first>
    - src/consolidation/run-sleep-pass.ts (lines ~645–672 — the end-of-pass sequence ending in `log('Sleep pass complete')`; runGraphHygiene precedent for an end-of-pass hook)
    - src/adapter/session-start-cli.ts (line ~125 — `new CandidateRetriever(db)`; this cold process must read the index)
    - src/adapter/recall-cli.ts (line ~145 — `new CandidateRetriever(db)`; cold process)
    - src/adapter/ambient-recall.ts (line ~103 — `new CandidateRetriever(db)`; cue-less no-fusion path)
    - .planning/phases/41-vector-index-and-hot-path-latency/41-SPIKE-FINDINGS.md (the persistence artifact + path convention)
  </read_first>
  <action>
    Add an end-of-sleep-pass index build/persist step in `src/consolidation/run-sleep-pass.ts`, placed AFTER consolidation + graph hygiene and immediately before `log('Sleep pass complete')` (so it reflects the last completed pass per D-05; all build cost stays offline). The step reads every live `node WHERE embedding IS NOT NULL AND tombstoned = 0`, builds the chosen index artifact, and persists it next to the DB (e.g. an `recense.index` flat-buffer sidecar beside `recense.db`, or populates the on-disk `vec0` table — per the spike's choice/path convention), logging a one-line `index: rebuilt N vectors -> <path>`. It is a derived cache: a build failure is logged, not thrown, and never aborts the pass (mirror the graph-hygiene error posture). Do NOT repoint the consolidator's mid-pass `retriever.topk` at this index (D-07) — the consolidator's retriever stays brute-force.

    Then wire the THREE cold online callers to construct their `CandidateRetriever` WITH the persisted index (pass the index path / enable flag added in Task 1): `session-start-cli.ts` (~125), `recall-cli.ts` (~145), `ambient-recall.ts` (~103). When the artifact is missing (e.g. first run before any sleep pass), they fall back to brute-force automatically (Task 1's fallback). Do NOT alter `engine.ts` `retrieve`/`retrieveCueless` behavior or the inject-budget logic — only the retriever construction changes; the floor gate and the topkTombstoned classification keep receiving real cosine scores.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --dir src/consolidation && npx vitest run --dir src/adapter && grep -q "index: rebuilt" src/consolidation/run-sleep-pass.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; existing consolidation + adapter vitest specs stay green.
    - `run-sleep-pass.ts` builds + persists the index AFTER consolidation/hygiene and BEFORE `log('Sleep pass complete')`; the build is wrapped so a failure logs and continues (never throws out of the pass).
    - The consolidator's mid-pass `retriever.topk` is unchanged (still brute-force) — `git diff` on consolidator.ts shows no retriever-construction change (D-07).
    - All three cold callers (`session-start-cli`, `recall-cli`, `ambient-recall`) construct `CandidateRetriever` with the index path/flag and fall back to brute-force when the artifact is absent.
    - `engine.ts` `retrieve`/`retrieveCueless`/inject-budget behavior is untouched.
  </acceptance_criteria>
  <done>The index is rebuilt + persisted at the end of every sleep pass as a derived cache, the cold online callers read it (brute-force fallback when absent), and the offline consolidator stays on brute-force.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted index artifact → online readers | The index is loaded into cold online processes; it is a derived cache over the founder's own embeddings. |
| sqlite-vec native extension → process (escalation branch only) | If the spike escalated, a native loadable extension is shipped + loaded at runtime. |

Honest surface read: the index is an internal, offline-built, local derived cache over the user's own embeddings. There is NO new external input on the online path — the online path stays read-only and LLM-free. The realistic threat surface is minimal. The one real concern is the escalation branch (loading a native binary), which only applies if the spike chose sqlite-vec.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | Tampering | stale/corrupt index returns wrong neighbors | mitigate | Index is a DERIVED cache (graph is source of truth). On load failure or dimension mismatch, fall back to brute-force; a corrupt artifact never becomes authoritative. PERF-03 gate (Plan 41-03) asserts top-k equivalence so a silently-wrong index is caught. |
| T-41-05 | Tampering | sqlite-vec native binary (escalation only) | mitigate | ONLY if the spike chose sqlite-vec: pin the binary version, verify the published checksum, and require a macOS+Linux CI matrix before shipping. If the spike chose zero-dep, this threat does not apply (no new binary). |
| T-41-SC | Tampering | npm/pip/cargo installs | mitigate | Default (zero-dep) adds NO package. Escalation to sqlite-vec is a package install gated by RESEARCH Package Legitimacy + a blocking human checkpoint — but the spike default protects the net-zero-deps streak, so no install is expected. |
| T-41-06 | Denial of service | index build at end of pass | accept | Build is offline (sleep pass), bounded by N, and failure-tolerant (logged, not thrown) — it cannot block the online path or abort the pass. Low risk. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- The topk index spec passes (index top-k == brute-force top-k ± tie reorder, real cosine scores).
- `CandidateRetriever` method signatures unchanged; no new exported VectorIndex interface.
- The end-of-pass index build is present and failure-tolerant; the consolidator stays brute-force.
- All three cold callers read the index with brute-force fallback.
</verification>

<success_criteria>
The online hot path reads a persisted exact index (built at end of sleep pass, derived/rebuildable) behind the unchanged `CandidateRetriever` seam, returning real cosine scores so the floor gate and hybridTopk keep working; the offline consolidator remains on brute-force (D-07); no new abstraction is introduced (D-02). PERF-01 satisfied.
</success_criteria>

<output>
Create `.planning/phases/41-vector-index-and-hot-path-latency/41-02-SUMMARY.md` when done.
</output>
