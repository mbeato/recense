---
phase: 33-synchronous-curated-write-recense-remember-lossless-single-f
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - "~/.claude/CLAUDE.md"
  - "~/.claude/projects/-Users-vtx-brain-memory/memory/*.md"
  - "~/.claude/projects/-Users-vtx-brain-memory/memory/.migrated/"
  - ".planning/phases/33-synchronous-curated-write-recense-remember-lossless-single-f/33-MIGRATION.md"
  - ".planning/phases/33-synchronous-curated-write-recense-remember-lossless-single-f/33-SETTINGS-FINDING.md"
autonomous: false
requirements: [REMEMBER-03]
user_setup: []
must_haves:
  truths:
    - "The global ~/.claude/CLAUDE.md carries an additive hard directive: all deliberate facts/memory → `recense remember`; never write native .md memory files"
    - "Whether settings.json (or a hook) can disable native file-based memory is investigated and the finding is recorded; a real switch is applied if one exists, otherwise the directive carries it alone"
    - "Each of the 12 ~/.claude/projects/-Users-vtx-brain-memory/memory/*.md files (plus the MEMORY.md index) is fed verbatim through `recense remember`, deterministically verified to exist as a live node by value_hash, THEN archived (moved, not deleted) — strict write→verify→archive per file"
    - "Migrated memories are scope-tagged brain-memory via the default cwd-derived scope"
  artifacts:
    - path: "~/.claude/CLAUDE.md"
      provides: "the D-06 global hard directive (additive)"
      contains: "recense remember"
    - path: "~/.claude/projects/-Users-vtx-brain-memory/memory/.migrated/"
      provides: "the archived (not deleted) original .md memory files + MEMORY.md"
    - path: ".planning/phases/33-synchronous-curated-write-recense-remember-lossless-single-f/33-MIGRATION.md"
      provides: "per-file write→verify→archive evidence (filename, node id, value_hash match, archived path)"
    - path: ".planning/phases/33-synchronous-curated-write-recense-remember-lossless-single-f/33-SETTINGS-FINDING.md"
      provides: "the D-07 settings.json kill-switch investigation result"
  key_links:
    - from: "migration script"
      to: "recense remember"
      via: "one verbatim remember call per .md file (NOT lossy import-memory)"
      pattern: "recense remember"
    - from: "verify gate"
      to: "node.value_hash"
      via: "deterministic sha256(value) live-node-exists check before archive (NOT cosine recall)"
      pattern: "value_hash"
---

<objective>
Complete the native-memory cutover (REMEMBER-03): (1) add the D-06 hard directive to the GLOBAL `~/.claude/CLAUDE.md` so all deliberate facts/memory flow through `recense remember` and never to native `.md` memory; (2) investigate whether a `settings.json` (or hook) kill-switch for native file-based memory genuinely exists and apply it if real; (3) run the founder-gated one-time migration of the 12 `~/.claude/projects/-Users-vtx-brain-memory/memory/*.md` files (plus the MEMORY.md index) through the NEW verbatim `recense remember` (from plan 33-01), with a deterministic per-file value_hash verify gate BEFORE archiving each file.

Purpose: Close the "replaces MEMORY.md" promise end-to-end. The engine (33-01) is the write path; this plan makes Claude Code actually USE it (directive + kill-switch) and brings the founder's existing curated memory into the brain losslessly, retiring the flat `.md` files.
Output: edited `~/.claude/CLAUDE.md`, archived `.md` files under `.migrated/`, `33-MIGRATION.md` evidence, `33-SETTINGS-FINDING.md`.

This plan touches the founder's LIVE `~/.claude` memory and the LIVE brain DB — it is `autonomous: false` and human-gated, matching the founder-gated live-run pattern used in 30-03 / 25-03 / 24-03.

Covers REMEMBER-03 (native-memory cutover).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-synchronous-curated-write-recense-remember-lossless-single-f/33-CONTEXT.md
@.planning/phases/33-synchronous-curated-write-recense-remember-lossless-single-f/33-PATTERNS.md
@.planning/phases/33-synchronous-curated-write-recense-remember-lossless-single-f/33-01-PLAN.md

<interfaces>
<!-- Reused from plan 33-01 + live enumeration glue. -->

The `recense remember "<fact>" [--scope <s>]` command from plan 33-01 is the ONLY write path used here (verbatim, NOT lossy import-memory).

Migration enumeration reuse — src/adapter/import-memory-cli.ts lines 125-191:
  - folderToCwd, defaultProjectsDir, planImport already enumerate ~/.claude/projects/*/memory/*.md and skip MEMORY.md / policy bundles / trackers.
  - REUSE planImport ONLY to enumerate; do NOT call its recordEvent (that is the lossy path). Call `recense remember` per file instead.
  - For THIS migration the target is specifically ~/.claude/projects/-Users-vtx-brain-memory/memory/, and per D-09 the MEMORY.md index is ALSO migrated+archived (planImport normally skips it — handle the index explicitly).

Deterministic verify gate (D-08) — value_hash:
  - node.value_hash = sha256(value) (semantic-store.ts upsertNode). The gate: after a remember of a file's verbatim content, confirm a live (tombstoned=0) node exists whose value_hash === sha256(<verbatim content as stored>). Query the live DB directly: SELECT id, tombstoned FROM node WHERE value_hash = ? AND tombstoned = 0. Do NOT use `recense recall` (cosine-gated, unreliable for a delete gate — D-08).
  - NOTE: remember trims the verbatim fact (33-01 Task 1). Compute the hash on the SAME trimmed string remember stored (or read the created node id back from remember output if it prints one), so the hashes match.

Scope (D-10): all files migrate under scope `brain-memory` — run each remember with `--scope brain-memory` for determinism.

Live DB path: ~/.config/recense/recense.db (resolveDbPath default). The migration targets the live DB directly (subscription-billed judge, ~$0 marginal, real token usage). WAL gotcha applies only if inspecting a copy; the migration writes the live DB directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-06 global directive in ~/.claude/CLAUDE.md + D-07 settings.json kill-switch investigation</name>
  <files>~/.claude/CLAUDE.md, .planning/phases/33-synchronous-curated-write-recense-remember-lossless-single-f/33-SETTINGS-FINDING.md</files>
  <read_first>
    - ~/.claude/CLAUDE.md (the live global instructions file — read fully before editing; the directive is ADDITIVE, do not remove or reorder existing sections)
    - ~/.claude/settings.json (if it exists — inspect for any native-memory / file-memory toggle)
    - .planning/phases/33-synchronous-curated-write-recense-remember-lossless-single-f/33-CONTEXT.md (D-06, D-07 wording)
  </read_first>
  <action>
    D-06: Append a new top-level section to `~/.claude/CLAUDE.md` (additive — do not modify existing sections) titled e.g. "## Memory — use recense, not native .md memory". Content: a hard directive that ALL deliberate facts/memory must be written via `recense remember "<fact>" [--scope <s>]` (the synchronous curated write path that runs in-place reconsolidation), and that native Claude Code `.md` memory files must NEVER be written. State it overrides the harness native-memory protocol per instruction-priority rules. Keep it concise and imperative, matching the existing CLAUDE.md voice (lowercase-leaning, directive prose). Note recall already fires at SessionStart so the brain is the read path too.

    D-07: Investigate whether native file-based memory can be disabled via `~/.claude/settings.json` or a documented setting/hook. Steps: inspect `~/.claude/settings.json` for any memory-related key; check the live Claude Code docs/config surface via WebFetch on the official docs for a "disable memory" / "memory" settings key. Record the result in `33-SETTINGS-FINDING.md`: the exact setting if one exists (and apply it as belt-and-suspenders on top of D-06), OR a clear statement that no genuine switch was found and the global directive carries it alone. DO NOT build a heavyweight PreToolUse-deny hook — that is explicitly deferred unless investigation proves it is the only real mechanism (and if so, document that conclusion and stop, leaving the hook to a follow-up rather than building it speculatively).
  </action>
  <verify>
    <automated>grep -q "recense remember" ~/.claude/CLAUDE.md && test -f .planning/phases/33-synchronous-curated-write-recense-remember-lossless-single-f/33-SETTINGS-FINDING.md && echo "directive + finding present"</automated>
  </verify>
  <acceptance_criteria>
    - `~/.claude/CLAUDE.md` contains a new section referencing `recense remember` and an explicit instruction to never write native `.md` memory files.
    - All previously-existing sections of `~/.claude/CLAUDE.md` are still present (purely additive — confirm a known prior heading like "## Hard rules" or "## Git" still exists after the edit).
    - `33-SETTINGS-FINDING.md` exists and states either the applied settings.json key (with the literal key/value set) or an explicit "no genuine native-memory kill-switch found; directive carries it" conclusion.
    - No new PreToolUse-deny hook file was created (no new memory-deny file under `~/.claude/hooks/`).
  </acceptance_criteria>
  <done>The global directive is live and additive; the settings.json investigation is recorded and any real switch applied; no speculative deny-hook built.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Founder approval gate BEFORE the live migration</name>
  <what-built>
    - The `recense remember` engine (plan 33-01) is built, tested, and tsc-clean.
    - The D-06 global directive is in `~/.claude/CLAUDE.md`; the D-07 settings investigation is recorded.
    - The migration is ready: enumerate the 12 `.md` files + MEMORY.md index under `~/.claude/projects/-Users-vtx-brain-memory/memory/`, then for EACH file run `recense remember "<verbatim contents>" --scope brain-memory` against the LIVE DB, deterministically verify the node by value_hash, THEN move the file to `.migrated/`.
  </what-built>
  <how-to-verify>
    This migration touches the founder's LIVE ~/.claude memory and the LIVE brain DB (subscription-billed judge calls, ~$0 marginal but real token usage). Before proceeding, confirm:
    1. File count: run `ls -1 ~/.claude/projects/-Users-vtx-brain-memory/memory/*.md | wc -l` and confirm the set to migrate (expect ~12 + MEMORY.md). Approve the exact list the executor prints.
    2. Confirm the live DB target `~/.config/recense/recense.db` is correct and an hourly sleep pass is not mid-run (the write lock fast-fails; the migration runs serially).
    3. Confirm scope `brain-memory` for all files (D-10 — cross-project ones like user-email are low-harm to mis-scope since retrieval is global).
    4. Approve the write→verify→archive (move to `.migrated/`, NOT delete) order.
  </how-to-verify>
  <read_first>
    - .planning/phases/33-synchronous-curated-write-recense-remember-lossless-single-f/33-CONTEXT.md (D-08, D-09, D-10)
  </read_first>
  <acceptance_criteria>
    - Founder has typed approval (or described changes) for the migration file list, scope, DB target, and write→verify→archive order.
  </acceptance_criteria>
  <resume-signal>Type "approved" to run the live migration, or list changes.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: One-time verbatim migration — write → value_hash verify → archive, per file</name>
  <files>~/.claude/projects/-Users-vtx-brain-memory/memory/*.md, ~/.claude/projects/-Users-vtx-brain-memory/memory/.migrated/, .planning/phases/33-synchronous-curated-write-recense-remember-lossless-single-f/33-MIGRATION.md</files>
  <read_first>
    - src/adapter/remember-cli.ts (the verbatim write path from 33-01 — the ONLY write used)
    - src/adapter/import-memory-cli.ts lines 125-191 (planImport / folderToCwd enumeration — reuse to LIST files, not to write)
    - src/db/semantic-store.ts (value_hash on node; getNode shape for the verify read)
    - .planning/phases/33-synchronous-curated-write-recense-remember-lossless-single-f/33-CONTEXT.md (D-08 deterministic gate, D-09 archive-not-delete, D-10 scope)
  </read_first>
  <action>
    Run the migration STRICTLY per-file in write→verify→archive order (D-08). Do NOT batch-write-then-batch-archive — verify each file's node exists before that file is moved, so a failure leaves the file in place.

    Enumerate: list every `.md` under `~/.claude/projects/-Users-vtx-brain-memory/memory/` INCLUDING `MEMORY.md` (the index is migrated+archived too, D-09). Print the list for the record. Skip files already under `.migrated/`.

    For each file:
    1. Read its full verbatim contents.
    2. Run `recense remember "<verbatim contents>" --scope brain-memory` against the live DB (`~/.config/recense/recense.db`). One file = one remember call = one verbatim node (no multi-fact splitting — out of scope). Some files may contradict an existing curated/imported belief → expect a `contradict_reconcile` (in-place update) and record it; that is correct, not an error.
    3. VERIFY (deterministic, NON-cosine — D-08): compute the value_hash the engine way (sha256 of the trimmed verbatim content remember stored) and query the live DB directly: `SELECT id, tombstoned FROM node WHERE value_hash = ? AND tombstoned = 0`. Require exactly one live matching node. If remember took the reconcile path, the live node is the new reconciled node (value_hash-matchable on the new verbatim value). If verify finds no live node → STOP, do NOT archive that file, log the failure, and surface it to the founder (do not proceed to the next file's archive past an unsurfaced failure).
    4. ARCHIVE (D-09 — move, NOT delete): `mv` the verified file to `~/.claude/projects/-Users-vtx-brain-memory/memory/.migrated/` (create the dir if absent), preserving the filename.

    Record each file's outcome in `33-MIGRATION.md`: filename, the remember event type (unrelated insert vs contradict_reconcile), the live node id, the value_hash match (yes/no), the archived path. Include a summary: N files migrated, N reconciled-in-place, N archived, 0 failures (or list failures), and the count of judge calls made (~$0 marginal, subscription-billed).

    Multi-line command hygiene: write the migration as a small script via the Write tool (e.g. `/tmp/recense-migrate.cjs`) and invoke it with a single-line command — do NOT paste a multi-line loop for copy-paste.
  </action>
  <verify>
    <automated>node -e "const D=require('os').homedir()+'/.claude/projects/-Users-vtx-brain-memory/memory'; const fs=require('fs'); const live=fs.readdirSync(D).filter(f=>f.endsWith('.md')); const arch=fs.existsSync(D+'/.migrated')?fs.readdirSync(D+'/.migrated').filter(f=>f.endsWith('.md')):[]; console.log('live remaining:',live.length,'archived:',arch.length); process.exit(live.length===0?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `33-MIGRATION.md` exists with one row per migrated file: filename, remember event type, live node id, value_hash match=yes, archived path.
    - Every `.md` file (incl. MEMORY.md) is in `.migrated/` and NONE remain in the memory dir root (the verify command exits 0: live remaining = 0).
    - For every migrated file, a live (tombstoned=0) node with the matching value_hash exists in `~/.config/recense/recense.db` (deterministic, NON-cosine gate — no `recense recall` used as the gate).
    - No `.md` file was hard-deleted (`rm`); all were moved (archived dir count equals the original file count).
    - `33-MIGRATION.md` reports 0 failures, or any failure is explicitly listed with its filename.
  </acceptance_criteria>
  <done>All 12 files + MEMORY.md are verbatim-stored in the live brain (value_hash-verified live nodes, scope brain-memory), then archived (moved) to `.migrated/`; per-file evidence recorded; zero hard-deletes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration script → live brain DB | The 12 `.md` contents cross into permanent live curated nodes |
| verify gate → archive (irreversible-ish) | A false-positive verify could archive a file whose content did not land |
| global directive → all future sessions | The CLAUDE.md edit changes write behavior across every project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06 | Repudiation / data loss | a file is archived but its content never landed | mitigate | strict per-file write→VERIFY(value_hash live-node-exists)→archive; on verify-fail the file stays in place and the failure is surfaced; deterministic non-cosine gate (D-08) avoids the cosine-recall false-negative trap |
| T-33-07 | Data loss | hard-deleting the original `.md` files | mitigate | D-09 archive-not-delete: `mv` to `.migrated/`, never `rm` (planning-dir-no-safety-net lesson); the archive is the safety net until the brain is trusted |
| T-33-08 | Tampering | concurrent hourly sleep pass holds the write lock mid-migration | mitigate | remember's acquireLock fast-fails per file; a failed remember leaves the file in place (no archive); migration runs serially and is resumable (skips already-archived files) |
| T-33-09 | Elevation / behavior change | the global directive over-broadly suppresses legitimate memory | accept | additive directive, founder-owned global file; recall fires at SessionStart so the brain is the read path; reversible by editing CLAUDE.md |
| T-33-10 | Tampering | settings.json kill-switch investigation applies a wrong/破坏ing key | mitigate | apply only a documented, verified native-memory key; otherwise record "none found" and rely on the directive; no speculative deny-hook |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | net-zero new runtime deps — no install task in this plan; nothing to audit |
</threat_model>

<verification>
- `~/.claude/CLAUDE.md` contains the additive directive; prior sections intact.
- `33-SETTINGS-FINDING.md` records the D-07 result (applied switch or "none found").
- After founder approval, every `.md` (incl. MEMORY.md) is value_hash-verified as a live node and moved to `.migrated/`; zero remain in the dir root; zero hard-deletes.
- `33-MIGRATION.md` has per-file evidence + a 0-failures (or listed-failures) summary.
- Net-zero new runtime deps; no schema change.
</verification>

<success_criteria>
- All deliberate-memory writes are directed to `recense remember` via the global CLAUDE.md directive (D-06), with any real settings.json kill-switch applied (D-07) or its absence documented.
- The 12 `.md` files + MEMORY.md are losslessly migrated through the verbatim `remember` path, each deterministically verified by value_hash BEFORE being archived (moved, not deleted) — closing the "replaces MEMORY.md" promise on the write side.
- Migrated facts carry scope `brain-memory`.
</success_criteria>

<output>
Create `.planning/phases/33-synchronous-curated-write-recense-remember-lossless-single-f/33-02-SUMMARY.md` when done.
</output>
