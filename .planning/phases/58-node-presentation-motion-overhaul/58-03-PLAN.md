---
phase: 58-node-presentation-motion-overhaul
plan: 03
type: execute
wave: 2
depends_on: [58-01]
files_modified:
  - src/viz/modules/labels.js
  - src/viz/modules/constants.js
  - src/viz/modules/app.js
  - tests/viz-activity-palette-invariants.test.ts
  - tests/viz-labels-selection.test.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-15]
user_setup: []

must_haves:
  truths:
    - "The top-N schemas by member count show crisp SDF names when the camera is close"
    - "Labels stay hidden in the overview constellation in both viewports"
    - "Labels are slate (never amber) and are occluded by opaque node meshes"
    - "The label slate color sits inside the D-02 luminance band and is machine-locked against amber drift"
  artifacts:
    - path: "src/viz/modules/labels.js"
      provides: "troika SDF schema-label layer (initLabels(ctx), top-N by member count, appear-on-approach)"
      min_lines: 40
      contains: "initLabels"
    - path: "src/viz/modules/constants.js"
      provides: "LABEL_TOP_N, LABEL_DISTANCE_THRESHOLD, LABEL_COLOR named constants"
      contains: "LABEL_COLOR"
    - path: "tests/viz-activity-palette-invariants.test.ts"
      provides: "LABEL_COLOR luminance-band + non-amber invariant (D-15)"
      contains: "LABEL_COLOR"
  key_links:
    - from: "src/viz/modules/labels.js"
      to: "ctx.schemaMembers (member counts from abstracts edges)"
      via: "top-N selection reads the existing /graph-derived map"
      pattern: "schemaMembers"
    - from: "src/viz/modules/app.js"
      to: "initLabels(ctx)"
      via: "init chain after initGraph, before or after initDetail"
      pattern: "initLabels"
    - from: "src/viz/modules/labels.js"
      to: "src/viz/vendor/troika/troika-three-text.esm.js"
      via: "import Text (import-map bare specifier)"
      pattern: "troika-three-text"
---

<objective>
Add a diegetic SDF schema-label layer (`labels.js`) using the vendored troika-three-text from Plan 01:
one `Text` mesh per top-N schema by member count, slate `0xaab3c4`-family at ~0.7 alpha, `depthTest: true`
so nodes occlude naturally, faded in only under a camera-distance threshold (appear-on-approach, D-03).
Machine-lock the label color in the Phase-57 invariants file (D-15).

Purpose: The highest-impact remaining "premium graph product" cue (research area 1 #2) — crisp in-scene
names on the anchor nodes. Satisfies D-01..D-04, D-15.
Output: labels.js module, three constants, an init wiring, and the label-color invariant + a top-N unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/58-node-presentation-motion-overhaul/58-CONTEXT.md
@.planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md
@.planning/phases/58-node-presentation-motion-overhaul/58-PATTERNS.md

<interfaces>
Module convention — search.js: `export function initSearch(ctx)` (no return; sets ctx fields; reads ctx.allNodes).
Vendored-import header convention — effects.js:1-23 (JSDoc @module + T-10-10 note + `../vendor/...` relative imports).
Member counts — lod.js builds `schemaMembers` (Map<schemaId, Set<memberId>>) and `memberSchema` from 'abstracts' edges (lod.js:56-68), exposed on ctx; schema nodes have `__cat === 'schema'`.
App init order — app.js:225-234: initStats→initHud→initLod→initGraph→initEffects→initTrace→initDetail→initSearch→initTopics… (initLod runs before initGraph, so schemaMembers is available).
registerTick — stats.js:42-45 (`ctx.registerTick = fn => callbacks.push(fn)`), the home for the appear-on-approach distance check.
XSS discipline (T-10-12) — graph.js:594 tooltip uses textContent only; troika Text.text must be a plain string assignment, never any HTML-interpreting API.
LOCKED palette — constants.js: neutral slate 0xaab3c4 (Y≈172, in-band, non-amber); HOT amber 0xffb866; BG_COLOR 0x170f1d.
Invariants harness — tests/viz-activity-palette-invariants.test.ts: relativeLuminance() :122-132, Y_MIN=170/Y_MAX=228 :137-138, the D-02 `describe` block :153-171, parseKindColorHex :157-161.
D-03 locked render mechanics: depthTest:true, distance-faded, slate 0xaab3c4-family @ ~0.7 alpha, never amber.
D-02 label scope: top-N by member count, N a named tunable constant (~30 starting point — planner picks 30).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build labels.js (top-N SDF schema labels, appear-on-approach) and wire into app.js</name>
  <read_first>
    - src/viz/modules/search.js (the initSearch(ctx) module shape, :35 onward)
    - src/viz/modules/effects.js (lines 1-23 — vendored-import + JSDoc header convention to mirror)
    - src/viz/modules/lod.js (:56-68 — how schemaMembers/memberSchema are built and exposed on ctx)
    - src/viz/modules/app.js (:155-234 — ctx field availability rules + the init call chain)
    - src/viz/modules/stats.js (:42-45 registerTick; :189-217 updateIdleDrift for the registerTick callback shape)
    - src/viz/vendor/troika/troika-three-text.esm.js (the exported `Text` class API — text/font/fontSize/color/material props, sync())
  </read_first>
  <action>
    Create `src/viz/modules/labels.js` exporting `initLabels(ctx)`, mirroring search.js's initX(ctx) shape and
    effects.js's JSDoc+T-10-10 header. Import `Text` from `../vendor/troika/troika-three-text.esm.js` (or the
    bare specifier mapped in Plan 01). Select the top-N schema nodes (N = new constant `LABEL_TOP_N`, value 30)
    by member count read from `ctx.schemaMembers` (Map<schemaId, Set>) — nodes with `__cat === 'schema'`, sorted
    by set size descending, first N. For each, create one `Text`: set `.text = node.value` via plain string
    assignment (never innerHTML — T-10-12), `.font` = the vendored `src/viz/vendor/fonts/JetBrainsMono-Regular.ttf`
    path, `.color = LABEL_COLOR`, small fontSize scaled to node radius, and configure its material for
    `depthTest: true` and initial opacity 0. Position each Text at its schema node's world position (offset
    slightly above the node), billboard it to face the camera each frame, and add to `ctx.Graph.scene()`.
    Call `.sync()` on all N at boot to pre-warm the SDF atlas (Open Question 1 — avoid first-approach pop-in).
    Register a `ctx.registerTick` callback implementing D-03 appear-on-approach: compute camera distance to
    each label's schema node; fade the Text material opacity toward ~0.7 when under `LABEL_DISTANCE_THRESHOLD`
    and toward 0 above it (a simple lerp/damp per frame — reuse THREE.MathUtils.damp). Add `initLabels(ctx)` to
    app.js's init chain AFTER initGraph (needs ctx.Graph + ctx.schemaMembers) and after initLod. Add the three
    constants to constants.js under a `// === Phase 58 — schema labels ===` section: `LABEL_TOP_N = 30`,
    `LABEL_DISTANCE_THRESHOLD` (provisional world-units — Claude's Discretion, tuned at Stage 1), and
    `LABEL_COLOR = 0xaab3c4` (the locked slate family, D-03) — each with a one-line JSDoc.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/viz/modules/labels.js','utf8'); const c=require('fs').readFileSync('src/viz/modules/constants.js','utf8'); const a=require('fs').readFileSync('src/viz/modules/app.js','utf8'); const ok=/initLabels/.test(s)&&/troika-three-text/.test(s)&&/depthTest/.test(s)&&/\.text\s*=/.test(s)&&/schemaMembers/.test(s)&&/LABEL_COLOR/.test(c)&&/LABEL_TOP_N/.test(c)&&/initLabels\s*\(\s*ctx\s*\)/.test(a); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - labels.js exports initLabels(ctx), imports Text from the vendored troika path, and has an effects.js-style @module + T-10-10 JSDoc header
    - Top-N selection reads `ctx.schemaMembers` set sizes and filters `__cat === 'schema'`; N = LABEL_TOP_N (30)
    - Each Text sets `.text` via plain string assignment (no innerHTML / HTML-interpreting API) and uses depthTest:true
    - Labels start at opacity 0 and a registerTick callback fades them toward ~0.7 only under LABEL_DISTANCE_THRESHOLD
    - `.sync()` is called on all labels at boot (pre-warm)
    - app.js calls initLabels(ctx) after initGraph/initLod
    - LABEL_TOP_N, LABEL_DISTANCE_THRESHOLD, LABEL_COLOR (=0xaab3c4) are exported from constants.js with JSDoc
  </acceptance_criteria>
  <done>Schema labels render on approach, slate and depth-occluded; overview stays label-free; init wired</done>
</task>

<task type="auto">
  <name>Task 2: Lock LABEL_COLOR (band + non-amber, D-15) and unit-test top-N selection</name>
  <read_first>
    - tests/viz-activity-palette-invariants.test.ts (the D-02 luminance-band describe block :153-171, relativeLuminance :122-132, Y_MIN/Y_MAX :137-138, the amber-family reasoning comments :124-183 in constants.js)
    - src/viz/modules/constants.js (the LABEL_COLOR export added in Task 1; the HOT amber 0xffb866 for the non-amber distance assertion)
  </read_first>
  <action>
    Extend tests/viz-activity-palette-invariants.test.ts with a new `describe('D-15 label color lock', ...)`
    block reusing the file's existing parse + relativeLuminance harness (no new harness code): parse
    `LABEL_COLOR` from constants.js, assert its relativeLuminance falls inside [Y_MIN, Y_MAX] (170..228), and
    assert it is NOT amber-family — i.e. it fails the warm-orange signature (amber is R≈G>B; assert the slate's
    blue channel is not dominated the amber way, e.g. B >= G or a hue-distance from HOT 0xffb866 above a
    threshold, matching the existing D-03(b) amber-family reasoning). Create a small unit test
    `tests/viz-labels-selection.test.ts` that exercises the top-N-by-member-count selection logic: feed a
    synthetic `ctx.schemaMembers` map + schema nodes, call the exported selection helper (export a pure
    `selectTopSchemas(ctx, n)` from labels.js for testability), and assert it returns exactly the N schemas with
    the largest member sets, in descending order. Mock troika `Text` minimally (a class stub) so labels.js
    imports without a browser, mirroring the haze test THREE-mock pattern.
  </action>
  <verify>
    <automated>npm test -- tests/viz-activity-palette-invariants.test.ts tests/viz-labels-selection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The invariants file has a D-15 label-color describe block asserting LABEL_COLOR is in [170,228] AND non-amber-family
    - labels.js exports a pure `selectTopSchemas(ctx, n)` helper used by both initLabels and the new test
    - tests/viz-labels-selection.test.ts asserts top-N returns the N largest-member schemas in descending order
    - `npm test -- tests/viz-activity-palette-invariants.test.ts tests/viz-labels-selection.test.ts` passes
  </acceptance_criteria>
  <done>Label color is machine-locked in-band + non-amber; top-N selection is unit-tested green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /graph payload (schema names) → in-scene text | Label text is corpus-derived; must render as inert text, never interpreted markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-03 | Injection (XSS via label text) | troika Text.text | mitigate | Set `.text` via plain string assignment only (troika renders it as SDF glyphs, never HTML) — same T-10-12 textContent-only discipline as graph.js:594 |
| T-58-01 (ref) | Information Disclosure | troika CDN fallback | mitigate (Plan 01) | Verified at Stage-1: DevTools Network shows zero cdn.jsdelivr.net requests with labels visible |
</threat_model>

<verification>
- Source scan: initLabels/troika import/depthTest/`.text =`/schemaMembers present; three constants exported; app.js wires initLabels
- `npm test -- tests/viz-activity-palette-invariants.test.ts tests/viz-labels-selection.test.ts` green
- Visual (Stage-1, Plan 05): labels crisp on approach, slate, node-occluded, absent in overview; DevTools Network zero cdn.jsdelivr.net
</verification>

<success_criteria>
- Top-N schema SDF labels appear on approach, slate + depth-occluded, absent in overview
- LABEL_COLOR machine-locked in-band + non-amber; top-N selection unit-tested
- No LOCKED constant drift; no CDN network call
</success_criteria>

<output>
Create `.planning/phases/58-node-presentation-motion-overhaul/58-03-SUMMARY.md` when done
</output>
