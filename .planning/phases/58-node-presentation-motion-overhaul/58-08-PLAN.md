---
phase: 58-node-presentation-motion-overhaul
plan: 08
type: execute
wave: 7
depends_on: [58-06, 58-07]
files_modified: []
autonomous: false
requirements: [D-13, D-14, D-15, D-16]
user_setup: []

must_haves:
  truths:
    - "Founder has reviewed the full motion grammar on the live install (hover, focus flight, transition, dim/fog)"
    - "Screen recordings of the motion moments are captured as the Stage-2 approval record"
    - "After-fps at both tiers is measured and compared one-line against the pre-phase baseline (D-16)"
    - "Final founder-tuned constant values are recorded; any fps regression is fixed before close"
  artifacts: []
  key_links: []
---

<objective>
Stage-2 founder checkpoint — MOTION (D-13). The full interaction grammar (damped asymmetric hover, orbit-then-
dolly focus flight with interruptibility, transition re-drive, focus dim/fog deepening) is tuned live and
signed off; short screen recordings are the durable approval record (D-14); before/after fps at both tiers is
compared (D-16). Final feel-constant values land here (D-15 keeps them UNLOCKED — founder-tuned, no ratchet
test). This is the phase-closing gate before `/gsd:verify-work`.

Purpose: Lock the motion feel against the real hull and confirm no perf regression.
Output: Founder motion sign-off, final constant values, screen recordings, and the before/after fps comparison.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-node-presentation-motion-overhaul/58-CONTEXT.md
@.planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Stage-2 MOTION review — hover, focus flight, transition, dim/fog + before/after fps</name>
  <read_first>
    - .planning/phases/58-node-presentation-motion-overhaul/58-CONTEXT.md (D-13 Stage 2; D-14 recordings; D-15 which constants stay UNLOCKED; D-16 perf; Specific Ideas — the interruptibility + asymmetry bar)
    - .planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md (Pitfall 4 — the CAM_LOOKAT_LAMBDA>CAM_POS_LAMBDA gaze-settles-first decision to get explicit founder feedback on; Validation Architecture — the `S` fps overlay)
    - .planning/phases/58-node-presentation-motion-overhaul/58-01-SUMMARY.md (the pre-phase baseline fps recorded in Plan 01 Task 1)
  </read_first>
  <what-built>
    Plans 06-07 landed (autonomous, merged): one damped interruptible camera system driving node focus
    (orbit-then-dolly + anticipation), the brain⇄corpus transition re-drive, recenter, and search fly-to; plus
    damped asymmetric hover and focus depth deepening (stronger dim + tighter fog). Presentation (haze/labels/
    matcap) was already signed off at Stage 1.
  </what-built>
  <how-to-verify>
    Run the live viz (both viewports). For each, tune the named feel constants to taste and record final values:
    1. HOVER: sweep the pointer across nodes — confirm grow has a slight overshoot (~1.05×, one oscillation) and
       shrink is clean, and that the feel is identical at idle (24fps) and active (60fps) tiers. Record a clip.
    2. FOCUS FLIGHT: click a node — confirm the anticipation pull-back → orbit-on-axis → dolly-in reads as
       deliberate (not a straight tween). Then click a SECOND node mid-flight — confirm it retargets smoothly,
       never queues, never jumps (interruptibility). Give explicit feedback on the gaze-settles-first feel
       (CAM_LOOKAT_LAMBDA > CAM_POS_LAMBDA, Pitfall 4). Record a clip.
    3. TRANSITION: toggle brain⇄corpus — confirm brain-recedes-first + crossfade feel is preserved and idle
       drift does not fight it after sitting idle. Record a clip.
    4. FOCUS DEPTH: confirm non-neighbor dim + tighter fog deepen the focus read and fully restore on deselect.
    5. PERF (D-16): press `S`, measure after-fps at overview-idle and focus-interaction; write the one-line
       before/after comparison against the Plan-01 baseline. Any regression must be fixed before close.
  </how-to-verify>
  <verify>
    <automated>MISSING — motion feel is a founder judgment; D-15 keeps these constants UNLOCKED (no ratchet test). Screen recordings + recorded final values + the fps comparison are the verification record (D-14/D-16). The palette-touching locks (LABEL_COLOR band, matcap grayscale, LOCKED-constant no-drift) remain machine-verified by `npm test` + `node scripts/gen-matcap.mjs --check`.</automated>
  </verify>
  <acceptance_criteria>
    - Founder sign-off recorded for hover, focus flight (incl. mid-flight interruptibility), transition, and focus depth
    - Explicit founder feedback recorded on the gaze-settles-first (CAM_LOOKAT_LAMBDA > CAM_POS_LAMBDA) choice
    - Final tuned values recorded for the UNLOCKED feel constants (camera lambdas + phase timings, HOVER_LAMBDA/OVERSHOOT, FOCUS_DIM_OPACITY, FOCUS_FOG_NEAR, MATCAP_MIX_LAMBDA)
    - Screen recordings of hover, focus flight, and transition saved (D-14)
    - One-line before/after fps comparison at both tiers recorded (D-16); any regression fixed before close
    - `npm test` full suite green and `node scripts/gen-matcap.mjs --check` green at close (machine locks intact)
  </acceptance_criteria>
  <done>Founder signs off the full motion grammar with recordings + final constants; fps compared and any regression fixed; machine locks green</done>
  <resume-signal>Type "approved" with the final constant values and the before/after fps line, or describe the motion revisions needed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. This checkpoint's security-relevant duty is confirming the palette-touching machine locks (D-15)
remain green at close.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-15 | Tampering (of LOCKED palette/bloom/tone) | drift during motion tuning | mitigate | `npm test` (invariants: LABEL_COLOR band + non-amber, LOCKED-constant no-drift) + `gen-matcap.mjs --check` (grayscale) must be green at close; feel-constant tuning touches only UNLOCKED values |
</threat_model>

<verification>
- Founder motion sign-off + final constant values + screen recordings captured
- Before/after fps compared at both tiers; regression (if any) fixed
- `npm test` full suite green; `node scripts/gen-matcap.mjs --check` green (D-15 locks intact)
</verification>

<success_criteria>
- Full motion grammar signed off with recordings and final tuned constants
- No perf regression at either tier (or fixed before close)
- All palette-touching machine locks green
</success_criteria>

<output>
Create `.planning/phases/58-node-presentation-motion-overhaul/58-08-SUMMARY.md` when done
</output>
