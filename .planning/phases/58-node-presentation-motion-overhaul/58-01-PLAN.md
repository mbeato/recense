---
phase: 58-node-presentation-motion-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/viz/index.html
  - src/viz/vendor/troika/troika-three-text.esm.js
  - src/viz/vendor/troika/troika-three-utils.esm.js
  - src/viz/vendor/troika/troika-worker-utils.esm.js
  - src/viz/vendor/troika/webgl-sdf-generator.esm.js
  - src/viz/vendor/troika/bidi-js.esm.js
  - src/viz/vendor/fonts/JetBrainsMono-Regular.ttf
autonomous: false
requirements: [D-01, D-04, D-15, D-16]
user_setup: []

must_haves:
  truths:
    - "The five troika ESM files and the mono font exist under src/viz/vendor/ and resolve via the import map"
    - "The running viz makes zero network calls to cdn.jsdelivr.net when labels render (Pitfall 1 patch applied)"
    - "Founder has verified the five [ASSUMED] npm packages on their registries before adoption"
  artifacts:
    - path: "src/viz/vendor/troika/troika-three-text.esm.js"
      provides: "Vendored SDF text renderer, CDN-fallback patched out"
      contains: "createTextDerivedMaterial"
    - path: "src/viz/vendor/fonts/JetBrainsMono-Regular.ttf"
      provides: "Vendored humanist mono font for the SDF atlas (D-04)"
    - path: "src/viz/index.html"
      provides: "Import-map entries for the five troika packages"
      contains: "troika-three-text"
  key_links:
    - from: "src/viz/index.html"
      to: "src/viz/vendor/troika/troika-three-text.esm.js"
      via: "importmap entry"
      pattern: "troika-three-text.*vendor/troika"
    - from: "src/viz/vendor/troika/troika-three-text.esm.js"
      to: "unicode-font-resolver CDN"
      via: "patched no-op (must NOT reach cdn.jsdelivr.net)"
      pattern: "cdn\\.jsdelivr\\.net"
---

<objective>
Vendor troika-three-text and its four transitive dependencies plus an open-licensed
humanist mono font into `src/viz/vendor/`, wire them into the existing import map, and
source-patch out troika's hidden render-time CDN fallback (RESEARCH Pitfall 1). This is
the dependency foundation the schema-label module (Plan 03) imports; no rendering changes
here.

Purpose: Satisfy D-01 (adopt troika, vendored) and the T-10-10 vendor-everything rule while
closing the one genuine security finding of this phase (troika phoning `cdn.jsdelivr.net`).
Output: Five vendored ESM files, one vendored TTF, import-map entries, one applied source patch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/58-node-presentation-motion-overhaul/58-CONTEXT.md
@.planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md
@.planning/phases/58-node-presentation-motion-overhaul/58-PATTERNS.md

<interfaces>
Existing import map — src/viz/index.html lines 9-13 (only "three" mapped today):
  "imports": { "three": "./vendor/three.module.js" }

Existing vendored-addon relative-import convention — src/viz/modules/effects.js:21-23:
  import { STLLoader } from '../vendor/STLLoader.js';
  import { UnrealBloomPass } from '../vendor/addons/postprocessing/UnrealBloomPass.js';

scripts/copy-viz-assets.cjs line 21: assets = ['index.html', 'vendor', 'css', 'modules']
  — the whole vendor/ tree is copied recursively into dist/; adding files needs NO build-script change.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate + pre-phase fps baseline</name>
  <read_first>
    - .planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md (Package Legitimacy Audit section — the five [ASSUMED] packages and their registry evidence)
  </read_first>
  <what-built>
    Nothing yet — this is a blocking gate BEFORE vendoring. slopcheck could not run in the
    research environment (broken local Python toolchain), so all five packages are tagged
    [ASSUMED] and adoption requires human verification per the graceful-degradation protocol.
  </what-built>
  <how-to-verify>
    1. Confirm each package on its registry page (created date + weekly downloads + real repo):
       - https://www.npmjs.com/package/troika-three-text (v0.52.4)
       - https://www.npmjs.com/package/troika-three-utils (v0.52.4)
       - https://www.npmjs.com/package/troika-worker-utils (v0.52.0)
       - https://www.npmjs.com/package/webgl-sdf-generator (v1.1.1)
       - https://www.npmjs.com/package/bidi-js (v1.0.3)
    2. Confirm each repo is the real protectwise/troika or lojjic project, MIT-licensed.
    3. While the CURRENT live install is running (no Phase-58 code yet), press `S` to toggle
       the stats overlay and record the pre-phase baseline fps at both tiers into the summary:
       overview-idle (~24fps target) and focus-interaction (~60fps target). This is the D-16
       "before" number the Stage-2 checkpoint compares against.
  </how-to-verify>
  <verify>
    <automated>MISSING — legitimacy verification is human-only per graceful-degradation protocol; not auto-approvable (workflow.auto_advance ignored)</automated>
  </verify>
  <acceptance_criteria>
    - Founder has confirmed all five packages on their npmjs.com pages and approved adoption
    - Pre-phase baseline fps (overview-idle + focus-interaction) is recorded in the plan summary
  </acceptance_criteria>
  <done>Founder types "approved" and baseline fps is recorded, OR names a package to reject (halts vendoring)</done>
  <resume-signal>Type "approved" with the two baseline fps numbers, or name a rejected package</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Vendor the five troika ESM dist files, the mono TTF, and add import-map entries</name>
  <read_first>
    - src/viz/index.html (the existing importmap block, lines 9-13)
    - src/viz/modules/effects.js (lines 1-23 — vendored-import header + relative-path convention)
    - scripts/copy-viz-assets.cjs (confirm vendor/ is copied recursively — no change needed)
  </read_first>
  <action>
    From a temp dir run `npm pack troika-three-text@0.52.4 troika-three-utils@0.52.4
    troika-worker-utils@0.52.0 webgl-sdf-generator@1.1.1 bidi-js@1.0.3`. Extract each tarball's
    ESM dist file (the `dist/*.esm.js` or `.mjs` entry — verify against each package.json
    "module"/"exports" field) and copy them into `src/viz/vendor/troika/` with the exact names
    listed in this plan's files_modified. Download the JetBrains Mono Regular TTF from the
    official GitHub Releases (OFL-1.1, Claude's Discretion A1 — JetBrains Mono over IBM Plex Mono)
    into `src/viz/vendor/fonts/JetBrainsMono-Regular.ttf`. Add one import-map entry per package to
    the existing `<script type="importmap">` block in `src/viz/index.html`, each mapping the bare
    specifier to `./vendor/troika/<file>.esm.js`, keeping the existing "three" entry unchanged.
    The vendored ESM files import their siblings by bare specifier (`from 'troika-worker-utils'`,
    `from 'three'`), so every bare specifier they use must have a matching import-map entry. Do NOT
    add any npm dependency to package.json — this mirrors how `three` itself is vendored (no npm entry).
  </action>
  <verify>
    <automated>test -f src/viz/vendor/troika/troika-three-text.esm.js && test -f src/viz/vendor/fonts/JetBrainsMono-Regular.ttf && grep -q "troika-three-text" src/viz/index.html && grep -q "webgl-sdf-generator" src/viz/index.html</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist under src/viz/vendor/troika/ with the exact names in files_modified
    - src/viz/vendor/fonts/JetBrainsMono-Regular.ttf exists and is a valid TTF (`file` reports TrueType)
    - src/viz/index.html importmap contains an entry for each of the five bare specifiers plus the unchanged "three" entry
    - package.json has NO new dependency added (git diff of package.json is empty)
    - Every bare specifier imported inside the vendored ESM files has a matching importmap entry (grep each `from '...'` bare specifier against index.html)
  </acceptance_criteria>
  <done>All six vendored files present, import map maps five specifiers, package.json untouched</done>
</task>

<task type="auto">
  <name>Task 3: Patch out troika's render-time CDN fallback (Pitfall 1 / T-58-01)</name>
  <read_first>
    - src/viz/vendor/troika/troika-three-text.esm.js (the file just vendored — locate the hardcoded
      `https://cdn.jsdelivr.net/gh/lojjic/unicode-font-resolver` constant and the getFontsForString /
      resolveFallbacks call site)
    - .planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md (Pitfall 1 — exact mechanism and recommended patch)
  </read_first>
  <action>
    In the vendored `troika-three-text.esm.js`, neutralize the unicode-font-resolver CDN path so no
    render-time fetch to `cdn.jsdelivr.net` can ever fire. Replace the body of the resolver's
    `getFontsForString` (or equivalently the `resolveFallbacks()` call site) with a no-op that resolves
    immediately using only the primary (vendored) font — uncovered glyphs render as tofu/blank rather
    than triggering a network call. Because the config option `unicodeFontsURL` only changes the FIRST
    URL tried and the code retries the real default CDN on failure (`if (l !== v) { l = v; ... retry }`),
    a config-only suppression is NOT sufficient — the source edit is required. Add an inline comment in
    the same terse style as graph.js:117-124's rim-injection note, documenting that this patch removes
    the T-10-10-violating CDN fallback and that schema names are engine-derived ASCII/Latin so the visible
    cost is ~zero. Do NOT alter any other troika behavior.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/viz/vendor/troika/troika-three-text.esm.js','utf8'); const active=s.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'); if(/getFontsForString\s*\([^)]*\)\s*\{\s*(return|resolve)/.test(active) && /PATCHED|no-op|T-10-10/.test(s)) process.exit(0); console.error('patch marker or no-op body not found'); process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - The vendored troika-three-text.esm.js contains a documented patch comment referencing T-10-10 / the CDN fallback removal
    - The active (non-comment) fallback path no longer calls fetch/network for the CDN URL — the resolver returns/resolves immediately with the primary font
    - The hardcoded `cdn.jsdelivr.net` constant, if still present in source, is unreachable by any active code path (only referenced inside the patched-out branch or a comment)
    - Manual DevTools Network-tab check (recorded at Stage-1 checkpoint) shows zero requests to cdn.jsdelivr.net with labels visible
  </acceptance_criteria>
  <done>The vendored file's CDN fallback is patched to a no-op and documented; no active code path can fetch the CDN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viz runtime → third-party CDN | Vendored troika could silently fetch a font-metadata CDN at render time, leaking corpus label content |
| dev machine → npm registry | Vendoring pulls tarballs whose integrity slopcheck could not confirm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Information Disclosure | troika unicode-font-resolver CDN fallback | mitigate | Task 3 source-patches the fallback to a no-op; DevTools Network check confirms zero cdn.jsdelivr.net requests at Stage-1 |
| T-58-SC | Tampering | npm packages (5× [ASSUMED], slopcheck unavailable) | mitigate | Task 1 blocking-human legitimacy gate verifies each on npmjs.com before vendoring; no postinstall scripts exist on any of the five (verified) |
</threat_model>

<verification>
- `test -f` for all six vendored artifacts; importmap grep for all five specifiers
- package.json git diff empty (no npm dependency added)
- Node source-scan confirms the CDN fallback is patched to a no-op with a T-10-10 comment marker
- Manual DevTools Network check (deferred to Stage-1 checkpoint, Plan 05): zero cdn.jsdelivr.net requests with labels visible
</verification>

<success_criteria>
- Five troika ESM files + JetBrains Mono TTF vendored under src/viz/vendor/
- Import map maps all five bare specifiers; "three" entry unchanged; package.json untouched
- troika CDN fallback patched out and documented (T-58-01 mitigated)
- Founder-approved package legitimacy + recorded pre-phase baseline fps
</success_criteria>

<output>
Create `.planning/phases/58-node-presentation-motion-overhaul/58-01-SUMMARY.md` when done
</output>
