---
phase: 58-node-presentation-motion-overhaul
plan: 06
type: execute
wave: 5
depends_on: [58-05]
files_modified:
  - src/viz/modules/camera.js
  - src/viz/modules/detail.js
  - src/viz/modules/transition.js
  - src/viz/modules/graph.js
  - src/viz/modules/search.js
  - src/viz/modules/app.js
  - src/viz/modules/constants.js
  - tests/viz-camera-damp.test.ts
autonomous: true
requirements: [D-05, D-06, D-08]
user_setup: []

must_haves:
  truths:
    - "Every camera move (node focus, transition, recenter, search fly-to) is driven by one damped target"
    - "Clicking node B mid-flight to node A retargets smoothly — never queues, never jumps (interruptibility)"
    - "Node focus is a two-phase orbit-then-dolly with an anticipation pull-back (D-06)"
    - "The brain⇄corpus transition keeps its pull-back/crossfade feel and all 4 patch-era lessons (D-08)"
  artifacts:
    - path: "src/viz/modules/camera.js"
      provides: "One damped-target camera system: setCameraTarget + registerTick damp via Graph.cameraPosition(pos,lookAt,0)"
      min_lines: 40
      contains: "setCameraTarget"
    - path: "src/viz/modules/constants.js"
      provides: "CAM_POS_LAMBDA, CAM_LOOKAT_LAMBDA + D-06 phase timing/anticipation constants"
      contains: "CAM_POS_LAMBDA"
  key_links:
    - from: "src/viz/modules/camera.js registerTick"
      to: "ctx.Graph.cameraPosition(dampedPos, dampedLookAt, 0)"
      via: "the verified synchronous (ms=0) branch — no internal TWEEN fight"
      pattern: "cameraPosition\\([^,]+,[^,]+,\\s*0\\s*\\)"
    - from: "detail.js focusCamera / transition.js pullBackCamera+diveCamera / graph.js recenter"
      to: "ctx.setCameraTarget(...)"
      via: "all replace their Graph.cameraPosition(pos,lookAt,ms>0) calls"
      pattern: "setCameraTarget"
---

<objective>
Build ONE damped, interruptible camera system (`camera.js`, D-05) and route every camera move through it:
node focus becomes a two-phase orbit-then-dolly with an anticipation pull-back (D-06), the brain⇄corpus
transition is re-driven without touching its crossfade/lessons (D-08), and recenter/home + search fly-to
migrate too. The system chases a retargetable `{pos, lookAt}` each frame via the verified synchronous
`Graph.cameraPosition(pos, lookAt, 0)` branch, so interruptibility holds by construction.

Purpose: The single behavioral property (interruptibility) separating Linear-grade feel from tween soup.
Output: camera.js, four migrated call sites, camera constants, and a damp-convergence unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/58-node-presentation-motion-overhaul/58-CONTEXT.md
@.planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md
@.planning/phases/58-node-presentation-motion-overhaul/58-PATTERNS.md

<interfaces>
Graph.cameraPosition accessor — RESEARCH Finding 2 / Pattern 1 (lines 270-316): ms truthy → internal TWEEN
group; ms falsy (0/undefined) → SYNCHRONOUS set of camera.position + controls.target. Drive it every frame
with ms=0. No-arg call reads current { x, y, z, lookAt }.
THREE.MathUtils.damp(x, y, lambda, dt) — verified in vendored three.core.js:372 (RESEARCH lines 442-450).
Migrate these call sites (RESEARCH Code Examples lines 431-440):
  - detail.js:402-414 focusCamera: cameraPosition({x+220,y+80,z+220},{x,y,z},800) — the D-06 orbit-then-dolly point
  - transition.js:43-58 pullBackCamera (PULL_K, DUR) + diveCamera (homeCam, DUR) — D-08 re-drive ONLY these two calls
  - graph.js:804-812 recenter(ms=700): compact + full cameraPosition calls
  - search.js:93-100 pick(): calls ctx.selectNode → detail.focusCamera; NO direct camera call — likely ZERO edits (PATTERNS note lines 24-25); VERIFY, do not edit blindly
transition.js MUST-NOT-REGRESS lessons (header :11-19): (1) exact-home capture+restore, (2) markActive() before each move, (3) opacity-only fades, (4) prepared-before-reveal. Only the two cameraPosition calls change.
Idle-drift coexistence — stats.js:189-217 updateIdleDrift mutates cam directly; gated by ctx.isIdle(); ctx.markActive() suppresses it. camera.js must call ctx.markActive() at the start of every retarget (transition.js lesson 2).
App init — app.js:225-234; add initCamera(ctx) AFTER initGraph (needs ctx.Graph) and BEFORE initDetail/initTransition (they call ctx.setCameraTarget). Note: detail is initialized at app.js:231; transition is created via corpus.js — confirm ordering so ctx.setCameraTarget exists before first use.
Pitfall 4 (RESEARCH lines 421-427): the old accessor tweened lookAt at ms/3 (gaze settles 3× faster). Reproduce the feel by giving CAM_LOOKAT_LAMBDA a higher value than CAM_POS_LAMBDA; flag for founder at Stage 2.
D-06 defaults (named constants, UNLOCKED): pull-back 3-5%, orbit ~300ms ease-out, dolly ~600ms ease-in-out.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: camera.js — one damped-target system, wired into app.js, with a convergence unit test</name>
  <read_first>
    - src/viz/modules/stats.js (:42-45 registerTick; :50-61 markActive; :189-217 updateIdleDrift — the closest per-frame-camera analog)
    - src/viz/modules/app.js (:155-234 ctx field-availability rules + init chain)
    - .planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md (Pattern 1 lines 270-316; the verified accessor body; Pitfall 4)
    - src/viz/modules/graph.js (:804-816 recenter — the existing home-framing camera call it will delegate to setCameraTarget)
  </read_first>
  <action>
    Create `src/viz/modules/camera.js` exporting `initCamera(ctx)` (initX(ctx) convention). Hold module-scope
    `targetPos` and `targetLookAt`, initialized from the current camera on init. Export `ctx.setCameraTarget(pos,
    lookAt)` which stores the new target AND calls `ctx.markActive()` (suppresses stats.js idle drift so the two
    never fight — transition.js lesson 2). Register a `ctx.registerTick(now)` callback that tracks dt in seconds,
    reads the current camera via `ctx.Graph.cameraPosition()` (no-arg), damps each of pos.{x,y,z} toward
    targetPos with `THREE.MathUtils.damp(cur, target, CAM_POS_LAMBDA, dt)` and lookAt.{x,y,z} with
    CAM_LOOKAT_LAMBDA (deliberately higher — Pitfall 4 gaze-settles-first feel), and writes back via
    `ctx.Graph.cameraPosition(dampedPos, dampedLookAt, 0)` (the synchronous branch). Skip the write when both
    pos and lookAt are within an epsilon of target (settled) to avoid needless allocation. Add
    `initCamera(ctx)` to app.js's chain after initGraph and before initDetail. Add CAM_POS_LAMBDA and
    CAM_LOOKAT_LAMBDA (CAM_LOOKAT_LAMBDA > CAM_POS_LAMBDA) to constants.js under a `// === Phase 58 — camera ===`
    section with JSDoc. Create `tests/viz-camera-damp.test.ts` that exercises the pure damp-step math (mock
    THREE.MathUtils.damp or import the real vendored formula): assert that repeated ticks converge current→target
    monotonically and that a mid-flight setCameraTarget change redirects toward the NEW target without a
    discontinuity (interruptibility). Export the pure step function from camera.js for testability.
  </action>
  <verify>
    <automated>npm test -- tests/viz-camera-damp.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - camera.js exports initCamera(ctx) and sets ctx.setCameraTarget; setCameraTarget calls ctx.markActive()
    - The registerTick callback writes ctx.Graph.cameraPosition(dampedPos, dampedLookAt, 0) (ms=0 synchronous branch) each frame
    - CAM_LOOKAT_LAMBDA > CAM_POS_LAMBDA in constants.js (Pitfall-4 gaze-settles-first), both exported with JSDoc
    - app.js calls initCamera(ctx) after initGraph, before initDetail
    - tests/viz-camera-damp.test.ts asserts monotonic convergence AND smooth mid-flight retarget; passes
  </acceptance_criteria>
  <done>One damped-target camera system exists, wired, with a passing convergence + interruptibility test</done>
</task>

<task type="auto">
  <name>Task 2: Orbit-then-dolly node focus (D-06) + recenter migration</name>
  <read_first>
    - src/viz/modules/detail.js (:402-414 focusCamera in full; :520-530 the selectNode call to focusCamera; the offset-vector math x+220,y+80,z+220)
    - src/viz/modules/graph.js (:800-816 recenter — compact FRAME_X path + full path)
    - src/viz/modules/camera.js (the setCameraTarget signature from Task 1)
    - .planning/phases/58-node-presentation-motion-overhaul/58-CONTEXT.md (D-06 — pull-back 3-5%, orbit ~300ms ease-out, dolly ~600ms ease-in-out, tuned at Stage 2)
  </read_first>
  <action>
    Replace detail.js focusCamera's single `ctx.Graph.cameraPosition(...,800)` with a two-phase orbit-then-dolly
    built on `ctx.setCameraTarget(...)` and the damped system (D-06): keep the offset-vector math
    (x+220,y+80,z+220 as the geometric dolly-in basis) and the guard clause. Sequence: (a) an anticipation
    pull-back target 3-5% further out along the current view vector (constant `FOCUS_ANTICIPATION_PCT`), then
    (b) a rotate-on-axis / orbit target that puts the node on-axis, then (c) the dolly-in target. Implement the
    sequencing by advancing the setCameraTarget at short intervals whose durations come from named constants
    (`FOCUS_ORBIT_MS` ~300, `FOCUS_DOLLY_MS` ~600) — the DAMP owns the smoothing; the timings only gate when the
    next target is issued, and any new selectNode interrupts the sequence cleanly (interruptibility). Migrate
    graph.js recenter to call ctx.setCameraTarget for its compact and full framing targets instead of
    Graph.cameraPosition(pos,lookAt,ms) — preserve the boot ms===0 behavior (at boot, set the target directly so
    framing is identical to today) and the markActive guard. Add FOCUS_ANTICIPATION_PCT, FOCUS_ORBIT_MS,
    FOCUS_DOLLY_MS to constants.js (Phase-58 camera section, UNLOCKED feel constants) with JSDoc.
  </action>
  <verify>
    <automated>node -e "const d=require('fs').readFileSync('src/viz/modules/detail.js','utf8'); const g=require('fs').readFileSync('src/viz/modules/graph.js','utf8'); const c=require('fs').readFileSync('src/viz/modules/constants.js','utf8'); const ok=/setCameraTarget/.test(d)&&!/cameraPosition\([^)]*800\)/.test(d)&&/setCameraTarget/.test(g)&&/FOCUS_ORBIT_MS/.test(c)&&/FOCUS_ANTICIPATION_PCT/.test(c); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - detail.js focusCamera no longer calls Graph.cameraPosition with a nonzero ms; it drives ctx.setCameraTarget through pull-back → orbit → dolly phases
    - The x+220/y+80/z+220 offset and the guard clause are preserved as the dolly-in basis
    - graph.js recenter uses ctx.setCameraTarget for compact + full framing; boot (ms===0) framing is byte-identical to prior behavior
    - FOCUS_ANTICIPATION_PCT, FOCUS_ORBIT_MS, FOCUS_DOLLY_MS exported from constants.js with JSDoc
    - A new selectNode during a focus flight retargets cleanly (verified at Stage-2 checkpoint)
    - `npm test` full suite passes
  </acceptance_criteria>
  <done>Node focus is an interruptible orbit-then-dolly with anticipation; recenter runs through the damped system</done>
</task>

<task type="auto">
  <name>Task 3: Transition re-drive (D-08) + verify search fly-to needs no change</name>
  <read_first>
    - src/viz/modules/transition.js (full file — especially the header lessons :11-19 and pullBackCamera/diveCamera :43-58)
    - src/viz/modules/search.js (:88-100 pick() — confirm it routes through ctx.selectNode, not a direct camera call)
    - src/viz/modules/camera.js (setCameraTarget signature)
    - .planning/phases/58-node-presentation-motion-overhaul/58-CONTEXT.md (D-08 — re-drive camera moves only; keep architecture + 4 lessons)
  </read_first>
  <action>
    In transition.js, replace ONLY the two `ctx.Graph.cameraPosition(pos, lookAt, DUR)` calls inside
    pullBackCamera() and diveCamera() with `ctx.setCameraTarget(pos, lookAt)` (the new damped grammar). Keep
    everything else byte-for-byte: the exact-home capture into `homeCam` (lesson 1), the markActive() calls
    (lesson 2), fadeBrain's opacity-only CSS transition (lesson 3), and the onBeforeReveal-before-reveal
    sequencing (lesson 4). Do not touch REVEAL_AT/PULL_K/DUR semantics beyond routing the camera targets. Then
    VERIFY (do not edit) that search.js pick() reaches the camera only via ctx.selectNode → detail.focusCamera,
    so D-05's search fly-to migration is satisfied for free by Task 2 — if that assumption holds (PATTERNS note),
    make zero search.js edits and record the verification; if pick() somehow calls Graph.cameraPosition directly,
    migrate that one call to ctx.setCameraTarget. Confirm the transition still works end-to-end (brain recedes
    first, map fades in) and idle drift does not fight it after an idle period.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('src/viz/modules/transition.js','utf8'); const s=require('fs').readFileSync('src/viz/modules/search.js','utf8'); const ok=/setCameraTarget/.test(t)&&!/cameraPosition\([^)]*DUR\)/.test(t)&&/homeCam/.test(t)&&/markActive/.test(t)&&!/Graph\.cameraPosition/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - transition.js pullBackCamera + diveCamera call ctx.setCameraTarget; no cameraPosition(...,DUR) remains
    - homeCam capture, markActive() calls, opacity-only fades, and prepared-before-reveal sequencing are unchanged (git diff shows only the two camera-call lines changed in those functions)
    - search.js makes no direct Graph.cameraPosition call (verified); zero or one-line change recorded
    - `npm test` full suite passes
    - Transition end-to-end + post-idle behavior confirmed at Stage-2 checkpoint (no drift fight, brain-recedes-first preserved)
  </acceptance_criteria>
  <done>Transition camera moves run through the damped system with all 4 lessons intact; search fly-to verified free</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — this plan is pure client-side interaction math. No input, network, or DOM surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-05 | Denial of Service (self, via jank) | dual animators writing camera.position | mitigate | All call sites move to ms=0/setCameraTarget so only the damp system writes camera each frame (RESEARCH Anti-Pattern); markActive() suppresses idle drift during retargets |
</threat_model>

<verification>
- `npm test -- tests/viz-camera-damp.test.ts` (convergence + interruptibility) and full `npm test` green
- Source scan: setCameraTarget at all four call sites; no nonzero-ms cameraPosition in detail/transition; no direct camera call in search
- transition.js diff limited to the two camera-call lines within pullBackCamera/diveCamera (4 lessons intact)
- Visual (Stage-2, Plan 08): orbit-then-dolly feel, mid-flight interruptibility, transition brain-recedes-first
</verification>

<success_criteria>
- One damped camera system drives node focus, transition, recenter, and search fly-to
- Interruptibility holds everywhere by construction; orbit-then-dolly with anticipation on focus
- transition.js architecture + 4 lessons preserved; full suite green
</success_criteria>

<output>
Create `.planning/phases/58-node-presentation-motion-overhaul/58-06-SUMMARY.md` when done
</output>
