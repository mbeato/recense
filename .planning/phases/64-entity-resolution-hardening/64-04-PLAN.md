---
phase: 64-entity-resolution-hardening
plan: 04
type: execute
wave: 3
depends_on: [64-03]
files_modified:
  - tests/resolution-conservation.test.ts
  - tests/intent-conservation.test.ts
autonomous: true
requirements: [RESOLVE-01, RESOLVE-02, RESOLVE-03]

must_haves:
  truths:
    - "Two consolidation passes over identical input, one with resolution enabled and one with it disabled via the floor dark switch, produce identical persisted database state across every table and every deterministic column"
    - "No table in the schema has a column whose name contains 'resolved'; no sqlite_master SQL text mentions the resolved-entity field names (D-09: no schema change)"
    - "No consolidation_event payload written during a resolving pass contains any resolved-entity field name — the most plausible leak channel is checked, not assumed (closes the 63 WR-02 blind spot for this phase's fields)"
    - "The full test suite and typecheck are green with resolution shipping default-on"
  artifacts:
    - path: "tests/resolution-conservation.test.ts"
      provides: "D-09 inertness proof: two-pass whole-DB snapshot equality (resolution on vs off), schema-column negative assertions, consolidation_event payload leak check"
      min_lines: 200
    - path: "tests/intent-conservation.test.ts"
      provides: "Header warning extended to cover the resolved-entity fields alongside the intent fields"
      contains: "review-blocking"
  key_links:
    - from: "tests/resolution-conservation.test.ts"
      to: "src/lib/config.ts entityResolutionFloor"
      via: "floor set to 2 (unreachable) as the resolution-OFF control arm"
      pattern: "entityResolutionFloor: 2"
    - from: "tests/resolution-conservation.test.ts"
      to: "consolidation_event.payload"
      via: "explicit substring scan for the resolved-entity field names"
      pattern: "consolidation_event"
---

<objective>
Prove Phase 64's output is genuinely **inert**: the two new `ClaimDecision` fields change no persisted byte, add no column, and leak into no event payload. Phase 63 established this discipline for its own fields; this plan extends it one step and closes the blind spot 63-REVIEW WR-02 flagged in the original conservation helper (it compared one privileged column per table and skipped `consolidation_event.payload`, the single most plausible future leak channel).

Then run the full suite and typecheck so the phase ships green with resolution default-on.

Purpose: D-09 states no DB schema change in Phase 64. A test, not a claim, is what makes that true and keeps it true.
Output: a conservation suite and a clean full-suite run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-entity-resolution-hardening/64-CONTEXT.md
@.planning/phases/64-entity-resolution-hardening/64-PATTERNS.md
@.planning/phases/64-entity-resolution-hardening/64-03-SUMMARY.md

<interfaces>
<!-- Verified precedent (2026-08-02). -->

`tests/intent-conservation.test.ts`
- `snapshotDb(db)` at lines 231-256 — enumerates every non-`sqlite_%` table from `sqlite_master`, records `COUNT(*)`, and additionally records the sorted `value` column when present, else the sorted `content` column. **Known gap (63-REVIEW WR-02): exactly one column per table; `consolidation_event.payload` is never compared.**
- Two-pass comparison at lines 258-318 — build two harnesses over identical seeds/episodes, run `consolidate()` on each, `expect(snapshotDb(hA.db)).toEqual(snapshotDb(hB.db))`
- Column-name and `sqlite_master` SQL-text negative assertions at lines 320-400 — "no table has a column whose name contains X" and "no `sqlite_master.sql` text contains the substring X"
- File header carries an explicit "a silent loosening of this assertion... is a review-blocking change" warning

`src/lib/config.ts` — `entityResolutionFloor: 2` is the documented unreachable value that makes every resolution abstain, reproducing pre-Phase-64 behavior exactly (shipped in 64-02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two-pass inertness conservation suite with a strengthened snapshot</name>
  <files>tests/resolution-conservation.test.ts</files>
  <read_first>
    - tests/intent-conservation.test.ts (full file — the `snapshotDb` helper at 231-256, the two-pass comparison at 258-318, and the negative-assertion block at 320-400)
    - src/consolidation/consolidator.ts (the resolution branch and the two `ClaimDecision` fields from 64-03)
    - src/db/schema.ts (the full schema — to write the column-name negative assertion against real table definitions)
    - .planning/phases/64-entity-resolution-hardening/64-CONTEXT.md (D-09)
    - .planning/phases/63-offline-intent-classification/63-VERIFICATION.md (the WR-02 row — the exact blind spot this task closes for Phase 64's fields)
  </read_first>
  <behavior>
    - Pass A: harness with shipped defaults (resolution ON), seeded tracked entity, gmail episodes whose claims carry intent fields that DO resolve. Pass B: identical seed, identical episodes, identical clock, harness config differing only in `entityResolutionFloor: 2` (resolution OFF). `snapshotDb(A)` equals `snapshotDb(B)`
    - The same equality holds for a second scenario where pass A resolves and pass B abstains through the margin path rather than the floor switch — resolution outcome must not move persisted state either way
    - No table in the live schema has a column whose name contains `resolved` or `claimresolved` (case-insensitive), checked via `PRAGMA table_info` over every table enumerated from `sqlite_master`
    - No `sqlite_master.sql` text contains `claimResolvedEntityId`, `claimResolvedEntityDescriptor`, or `entity_resolution` (case-insensitive)
    - After a resolving pass, no `consolidation_event.payload` value contains `resolved`, `claimResolved`, or the resolved node's id
    - Sanity control: the resolving pass genuinely resolved at least once (assert via the decision spy or the counters), so the equality above is not passing because resolution silently never fired
  </behavior>
  <action>
Create `tests/resolution-conservation.test.ts`, modelled on `tests/intent-conservation.test.ts`'s two-pass structure.

Copy `snapshotDb` and then **strengthen it** rather than reusing it as-is: after the existing `value` / `content` column handling, also record every table's `payload` column when present (sorted, same treatment as `value`), and record the sorted concatenation of all TEXT columns for `consolidation_event` specifically. This closes the 63-REVIEW WR-02 blind spot for Phase 64's fields — a leak into `consolidation_event.payload` would otherwise pass a `value`-only comparison silently. Note in a comment that the strengthened helper is deliberately a superset of the Phase 63 one and why.

Both passes must use a `FakeClock` seeded to the same instant and identical episode ids/content ordering, so any difference in the snapshot is attributable to resolution and nothing else. If any non-deterministic column (uuid ids, wall-clock timestamps) makes raw equality impossible, do NOT weaken the comparison to a single column — normalize that specific column explicitly and document the normalization in a comment naming what it hides.

Include the sanity control: the resolving pass must be shown to have actually resolved (capture decisions with the spy pattern and assert at least one `claimResolvedEntityId` is defined). A conservation test that passes because resolution never fired is the failure mode this control exists to prevent.

Add the schema and payload negative assertions described in the behavior block, enumerating tables from `sqlite_master` at runtime rather than hard-coding a table list — a future table must be covered automatically.

File header must state: this file is the D-09 inertness guard for RESOLVE's output fields; the fields are in-memory only and Phase 65 is the first consumer; a silent loosening of the snapshot comparison or the payload scan is a review-blocking change; and the strengthened `snapshotDb` closes 63-REVIEW WR-02's `consolidation_event.payload` gap for these fields.
  </action>
  <verify>
    <automated>npx vitest run tests/resolution-conservation.test.ts tests/intent-conservation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/resolution-conservation.test.ts` passes with at least 5 test cases
    - The strengthened `snapshotDb` in the new file records a `payload` column when present — verified by grep for `payload` inside the helper
    - The sanity control asserts at least one resolved decision in the pass-A arm
    - The schema negative assertions enumerate tables from `sqlite_master` at runtime (no hard-coded table list)
    - `grep -n "resolved" src/db/schema.ts` returns no matches
    - Mutation check, recorded in the SUMMARY: temporarily making the resolution branch write anything persisted (for example appending the resolved node id to a `consolidation_event` payload) makes the two-pass equality fail; revert afterwards
  </acceptance_criteria>
  <done>Five-plus-case conservation suite green with a sanity control that proves resolution fired, a payload-aware snapshot, and a mutation check confirming the equality assertion is load-bearing.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the Phase 63 conservation warning, then full-suite regression</name>
  <files>tests/intent-conservation.test.ts</files>
  <read_first>
    - tests/intent-conservation.test.ts (the file header warning block)
    - .planning/phases/64-entity-resolution-hardening/64-01-SUMMARY.md
    - .planning/phases/64-entity-resolution-hardening/64-02-SUMMARY.md
    - .planning/phases/64-entity-resolution-hardening/64-03-SUMMARY.md
  </read_first>
  <action>
Extend the header comment in `tests/intent-conservation.test.ts` so its review-blocking warning names the Phase 64 resolved-entity fields alongside the intent fields, and cross-references `tests/resolution-conservation.test.ts` as the companion guard with the strengthened (payload-aware) snapshot. Change no assertion in that file — header text only; if any assertion there needs changing, that is a finding to record, not to silently fix here.

Then run the full test suite and typecheck. Record in the SUMMARY, verbatim and un-rounded: total tests passed, total skipped, any failures, and the typecheck exit code. If any pre-existing failure appears that is unrelated to Phase 64, record it explicitly as pre-existing with the evidence that establishes that (for example, the same failure on a stash of this phase's changes) rather than attributing it or hand-waving it.

Finally, run a real `consolidate()` fixture through the resolving path once and capture the emitted `RESOLVE-64` log line verbatim into the SUMMARY, so the phase's observability claim (D-06) is backed by observed output rather than by the code that would produce it.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `tests/intent-conservation.test.ts` header names both `claimResolvedEntityId` and `claimResolvedEntityDescriptor` and references `tests/resolution-conservation.test.ts`
    - `git diff --stat tests/intent-conservation.test.ts` shows changes confined to comment lines (no assertion or expectation changed)
    - `npm run typecheck` exits 0
    - `npx vitest run` completes; the SUMMARY records the exact passed/skipped/failed counts as printed, with no rounding or approximation
    - The SUMMARY contains the verbatim `RESOLVE-64` log line from an actual fixture run, including its attempts/hits/abstains and per-channel numbers
  </acceptance_criteria>
  <done>Phase 63 guard warning extended without touching its assertions; full suite and typecheck green with exact counts recorded; observability claim backed by observed log output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory `ClaimDecision` → persisted database | The only boundary this plan defends. Phase 64's contract is that nothing crosses it; the conservation suite is the enforcement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-10 | Tampering | unintended persistence of resolution output | mitigate | Two-pass whole-DB snapshot equality (resolution on vs off) plus runtime-enumerated schema column-name and `sqlite_master` SQL-text negative assertions. Mutation-checked so the equality assertion is proven load-bearing. |
| T-64-11 | Information Disclosure | `consolidation_event.payload` leak channel | mitigate | Explicit payload substring scan for the resolved-entity field names and the resolved node id, plus a payload-aware `snapshotDb` — closing the blind spot 63-REVIEW WR-02 identified in the Phase 63 helper. |
| T-64-SC | Tampering | npm/pip/cargo installs | N/A | Zero package installs in this phase. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npx vitest run` — full suite green; counts recorded verbatim in the SUMMARY
- `npx vitest run tests/resolution-conservation.test.ts tests/intent-conservation.test.ts tests/entity-resolution.test.ts tests/consolidation-resolution.test.ts tests/resolution-sentinels.test.ts tests/intent-source-gate.test.ts` — all pass
- `grep -n "resolved" src/db/schema.ts` — no matches
</verification>

<success_criteria>
Resolution is provably inert: identical persisted state with resolution on and off across every table and every deterministic column including event payloads, no schema column or SQL text mentioning the new fields, and a sanity control proving resolution actually fired during the compared pass. Full suite and typecheck green, with exact counts and a real `RESOLVE-64` log line recorded.
</success_criteria>

<output>
Create `.planning/phases/64-entity-resolution-hardening/64-04-SUMMARY.md` when done. Record the mutation-check result, exact suite counts, and the verbatim `RESOLVE-64` log line.
</output>
</content>
</invoke>
