---
phase: 39.3-reader-generation-phase-status-lock-aware-queueing-inserted
verified: 2026-06-26T10:47:00Z
status: human_needed
score: 12/12
overrides_applied: 0
human_verification:
  - test: "Trigger a real doc regenerate via the reader and confirm the phase stepper renders named phases, the generating step shows a live time estimate, and prior steps render as muted discrete check-off steps."
    expected: "Stepper appears with queued/gathering/generating/verifying/finalizing labels. Active step is amber. Done steps are muted rose. Generating step shows elapsed/ETA text. The old single full-width estimate bar is gone."
    why_human: "DOM rendering and CSS visual behavior cannot be verified by grep or node --check."
  - test: "Force lock contention (start a sleep pass, then click regenerate in the reader)."
    expected: "Stepper shows 'queued' (parked). Once the lock frees, stepper advances through subsequent named phases. No silent bail, no generic 'timed out or errored' string."
    why_human: "Requires live process interaction: running a sleep pass concurrently with a regenerate click."
  - test: "Optional: set RECENSE_CLAUDE_HEADLESS_TIMEOUT_MS=3000 while the lock is held and trigger regenerate."
    expected: "Reader shows 'generation failed: engine stayed busy' via textContent and stops the spinner. No infinite spin."
    why_human: "Requires controlling process environment and observing the spinner stop in the browser."
  - test: "Palette guard: confirm amber appears only on the active/in-progress step; done/idle steps stay muted rose/mauve."
    expected: "Phase 34 palette invariant holds. No amber on rest or done steps."
    why_human: "CSS specificity and visual rendering cannot be verified statically."
---

# Phase 39.3: Reader Generation Phase Status + Lock-Aware Queueing — Verification Report

**Phase Goal:** Replace the silent lock bail and pure time-estimate bar with (a) named generation phases shown as a stepper in the reader UI, (b) a real lock-wait queue with bounded give-up-after-budget instead of an instant silent exit, and (c) a status file boundary letting the detached CLI write phase to the read-only viz server.
**Verified:** 2026-06-26T10:47:00Z
**Status:** human_needed — all automated checks pass; 4 visual/behavioral UAT items remain from Plan 04 Task 3 (explicit founder checkpoint in the plan).
**Re-verification:** No — initial verification.

---

## Goal Achievement

### Observable Truths (RGS-01, RGS-02, RGS-03)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A per-slug status file written by one process can be read back by another with the same phase/timestamps | VERIFIED | `gen-status.ts` writeStatus/readStatus round-trip; 20/20 tests pass in `tests/gen-status.test.ts` |
| 2 | A URL-supplied slug containing `../` or `/` cannot escape the status directory | VERIFIED | `statusPath()` uses `sha1(slug).slice(0,16)` hash; test asserts `../../etc/passwd` stays under `/tmp/recense-gen-status/` |
| 3 | A status file whose updatedAt is older than the stale threshold reads as absent (null) | VERIFIED | `STALE_MS = 15 * 60 * 1000`; `readStatus` returns null past threshold; staleness test covers back-dated updatedAt |
| 4 | `buildGeneratingEnvelope` omits phase/error keys entirely when status is null or has no error (never serializes error:undefined) | VERIFIED | Conditional object build confirmed in `gen-status.ts:228-233`; tests assert `!('phase' in env)` and `!('error' in env)` for null status; `JSON.stringify` test confirms no `"error"` substring |
| 5 | `generateDoc` and `generateDocForSchema` fire `onPhase('gathering'/'generating'/'verifying')` in order when an onPhase callback is supplied | VERIFIED | `doc-generator.ts` lines 330, 348, 365 and 639, 656, 667; 32/32 doc-generator tests pass including new onPhase ordering assertions |
| 6 | A regenerate spawned while the write lock is held parks in 'queued' and proceeds when the lock frees, instead of exiting 0 silently | VERIFIED | `waitForLock()` exported helper in `generate-doc-cli.ts` re-stamps `writeStatus(slug,'queued')` each poll iteration; 4/4 waitForLock unit tests pass; instant bail grep returns 0 |
| 7 | When the lock never frees within the doc-gen timeout budget, the CLI writes `phase:'failed' {error:'engine stayed busy'}` and exits | VERIFIED | `generate-doc-cli.ts:155-157` calls `writeStatus(slug,'failed',{error:'engine stayed busy'})`; waitForLock test 3 confirms budget-expiry path; `grep -c "engine stayed busy"` = 3 |
| 8 | The CLI writes finalizing around write+doc-graph, done+clearStatus after commit, and `failed {error}` in its catch | VERIFIED | `generate-doc-cli.ts:280` (finalizing), `324-325` (done+clearStatus post-commit), `328` (failed in catch); 14 writeStatus + 4 clearStatus occurrences confirmed |
| 9 | The 202 'generating' envelope carries the generator's current phase read from the status file | VERIFIED | `server.ts:450-452` calls `readStatus(slug)` then `buildGeneratingEnvelope(st, elapsedMs)`; `readStatus` and `buildGeneratingEnvelope` both imported (grep confirms 2 each) |
| 10 | When status file reports `phase:'failed'`, the 202 envelope carries `phase:'failed'` + error so the reader can stop | VERIFIED | `buildGeneratingEnvelope` passes through phase and error conditionally; `tests/gen-envelope.test.ts` asserts failed-status case (13/13 pass) |
| 11 | When no status file exists (null/stale), envelope omits phase and error keys entirely | VERIFIED | `buildGeneratingEnvelope(null,...)` returns `{status:'generating',elapsedMs}` only; key-absence confirmed by `in` operator tests and `JSON.stringify` check |
| 12 | The `prevGeneratedAt`/`fetchGeneratedAt` silent-no-op detection and the generic 'timed out or errored' string are deleted | VERIFIED | `grep -c "fetchGeneratedAt\|prevGeneratedAt\|timed out or errored" src/viz/modules/reader.js` = 0 |

**Score:** 12/12 truths verified (automated)

---

### Invariants — All Hold

| Invariant | Check | Status |
|-----------|-------|--------|
| Performance: viz server DB handle read-only, no LLM in server path | `grep -n "readonly: true" server.ts` present at line 188; `new Database(dbPath, {readonly:true})`; no INSERT/UPDATE/DELETE in server.ts | VERIFIED |
| doc-generator.ts FS-free (D-3) | `grep -nE "from '(fs|node:fs)'" src/reader/doc-generator.ts` = 0 matches; onPhase wiring is optional-chaining callback only | VERIFIED |
| No engine/consolidation/decay changes | Phase modifies only `src/adapter/gen-status.ts`, `src/adapter/generate-doc-cli.ts`, `src/viz/server.ts`, `src/viz/modules/reader.js`, `src/viz/css/styles.css`; no engine, consolidation, or decay files touched | VERIFIED |
| lockfile.ts acquireLock unchanged (loop in CLI only) | `git log --oneline -- src/adapter/lockfile.ts` shows no 39.3 commits; the bounded wait loop lives entirely in `generate-doc-cli.ts::waitForLock` | VERIFIED |
| gen-status.ts is a leaf module (no reader/viz/consolidation imports) | `grep -nE "from '\.\.\/(reader|viz|consolidation)" src/adapter/gen-status.ts` = 0 | VERIFIED |
| Phase 34 palette guard: amber only on active step | `.doc-progress-step--active` uses `rgba(200,169,74,...)` (amber); `.doc-progress-step--done` uses `rgba(156,112,128,...)` (muted rose); confirmed in `src/viz/css/styles.css:829-847` | VERIFIED (static) |
| T-10-12 / T-39.3-08: phase/error strings via textContent, not innerHTML | `grep -nE "innerHTML\s*=" reader.js` = 1 match (renderMarkdown only); phase/error rendered at lines 282, 348 via `textContent` | VERIFIED |

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/adapter/gen-status.ts` | PHASES, statusPath, writeStatus, readStatus, clearStatus, buildGeneratingEnvelope | VERIFIED | All 6 exports confirmed; renameSync atomic write (4 occurrences); sha1 path guard |
| `tests/gen-status.test.ts` | 20 tests covering round-trip, path-safety, staleness, buildGeneratingEnvelope | VERIFIED | 20/20 pass; 42 expect() calls |
| `src/reader/doc-generator.ts` | Optional onPhase callback on generateDoc + generateDocForSchema | VERIFIED | 8 onPhase occurrences (2 params + 6 fire sites); no fs import added |
| `src/adapter/generate-doc-cli.ts` | waitForLock + full status lifecycle; instant bail removed | VERIFIED | 14 writeStatus + 4 clearStatus occurrences; "Lock held by another process" grep = 0; "engine stayed busy" = 3 |
| `tests/generate-doc-cli.test.ts` | 4 waitForLock unit tests | VERIFIED | 4/4 pass; 13 expect() calls |
| `tests/doc-generator.test.ts` | onPhase ordering tests added | VERIFIED | 32/32 pass; 15 onPhase references |
| `src/viz/server.ts` | send202Generating reads gen-status; DB stays read-only | VERIFIED | readStatus + buildGeneratingEnvelope imported and called; readonly: true at line 188; no write SQL |
| `tests/gen-envelope.test.ts` | Envelope key-presence contract (failed, null, gathering) | VERIFIED | 13/13 pass; 13 expect() calls |
| `src/viz/modules/reader.js` | Phase-aware stepper + failed handling; hack deleted | VERIFIED (automated) | `.phase` at 4 sites in loadWithPoll; `phase === 'failed'` branch; `update(phase)` method; fetchGeneratedAt/prevGeneratedAt/timed-out counts = 0; `node --check` exits 0 |
| `src/viz/css/styles.css` | Stepper CSS: step row, active/done states, palette-compliant | VERIFIED (static) | `.doc-progress-steps`, `.doc-progress-step`, `.doc-progress-step--active` (amber), `.doc-progress-step--done` (muted rose) confirmed |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/adapter/generate-doc-cli.ts` | `src/adapter/gen-status.ts` | `writeStatus` / `clearStatus` calls + `onPhase` wiring | VERIFIED | Import at line 40; 14+4 call sites confirmed |
| `src/adapter/generate-doc-cli.ts` | `src/adapter/lockfile.ts` | bounded `acquireLock()` poll in `waitForLock` | VERIFIED | Import at line 39; `acquire: acquireLock` at line 153 |
| `src/viz/server.ts` | `src/adapter/gen-status.ts` | `readStatus(slug)` + `buildGeneratingEnvelope(st, elapsedMs)` inside `send202Generating` | VERIFIED | Lines 51-52 (imports), 450-452 (call sites) |
| `src/viz/modules/reader.js` | 202 envelope `phase`/`error` | `j.phase` in poll loop | VERIFIED | Lines 277, 290, 291, 292 |

---

### Data-Flow Trace (Level 4)

| Source | Path | Sink | Status |
|--------|------|------|--------|
| `writeStatus(slug, phase)` in CLI | `/tmp/recense-gen-status/<hash>.json` | `readStatus(slug)` in server → `buildGeneratingEnvelope` → `res.end(JSON.stringify(...))` | VERIFIED — real FS write flows to HTTP 202 response |
| `onPhase('gathering'/'generating'/'verifying')` in `doc-generator.ts` | Callback in `generate-doc-cli.ts:255` | `writeStatus(slug, phase)` → status file → server 202 | VERIFIED — full chain traceable in source |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All tests pass (gen-status, gen-envelope, generate-doc-cli) | `npx vitest run tests/gen-status.test.ts tests/gen-envelope.test.ts tests/generate-doc-cli.test.ts` | 37/37 pass | PASS |
| Full suite passes (2346 tests, 159 files) | `npx vitest run` | 2346 passed, 3 skipped, 0 failed | PASS |
| TypeScript compiles clean | `npx tsc --noEmit` | exit 0, no output | PASS |
| reader.js syntax valid | `node --check src/viz/modules/reader.js` | exit 0 | PASS |
| Hack removal confirmed | `grep -c "fetchGeneratedAt\|prevGeneratedAt\|timed out or errored" src/viz/modules/reader.js` | 0 | PASS |
| Instant bail removed | `grep -nE "Lock held by another process" src/adapter/generate-doc-cli.ts` | 0 matches | PASS |
| Server DB still read-only | `grep -n "readonly: true" src/viz/server.ts` | present at line 188 | PASS |
| No write SQL in server | `grep -ciE "INSERT INTO\|UPDATE .* SET\|DELETE FROM" src/viz/server.ts` | 0 | PASS |

---

### Requirements Coverage

| Requirement | Plans | Status | Evidence |
|-------------|-------|--------|----------|
| RGS-01: reader surfaces named generation phases as a stepper (queued→gathering→generating→verifying→finalizing) replacing pure time-estimate bar | 02, 03, 04 | VERIFIED (auto) / UAT pending | STEP_PHASES in reader.js; startGenProgress stepper; `update(phase)` method; generating keeps TAU fill, others discrete; visual verification pending (Task 3) |
| RGS-02: lock-contention becomes real wait-queue with give-up-after-budget, not a silent bail | 02 | VERIFIED | `waitForLock()` exported helper; queued re-stamp per poll; budget = RECENSE_CLAUDE_HEADLESS_TIMEOUT_MS; 4/4 unit tests green; instant bail removed |
| RGS-03: status flows child→server via per-slug status file; authoritative `failed` phase replaces `generated_at` silent-no-op hack; no engine/online-path change | 01, 02, 03, 04 | VERIFIED | gen-status.ts leaf module; CLI sole writer; server read-only consumer; prevGeneratedAt/fetchGeneratedAt deleted; `phase:'failed'` stops poll loop; no engine changes |

---

### Anti-Patterns Found

| File | Pattern | Severity | Assessment |
|------|---------|----------|------------|
| None found | — | — | grep for TBD/FIXME/XXX/placeholder/TODO across phase-modified files returned no blockers |

---

### Human Verification Required

Plan 04 Task 3 is an explicit `type="checkpoint:human-verify" gate="blocking"` task in the plan. The automated code deliverables (Tasks 1 and 2) are complete and verified. The following UAT items require founder visual confirmation:

#### 1. Phase Stepper Rendering

**Test:** Rebuild dist if needed (`npm run build`) and start `recense viz` (http://localhost:7811). Open a doc in the Reader and click 'regenerate'.
**Expected:** Stepper appears with named phases (queued/gathering/generating/verifying/finalizing). Active phase highlighted in amber. Prior phases muted rose (checked off). The 'generating' step shows live elapsed/ETA text. Other phases render as discrete filled steps. The old single full-width pure-estimate bar is gone.
**Why human:** DOM rendering and animated CSS behavior cannot be verified statically.

#### 2. Lock-Contention Queueing Behavior

**Test:** Start a sleep pass (`set -a; . ~/.config/recense/sleep.env; set +a; "$RECENSE_NODE_BIN" "$RECENSE_SLEEP_JS"`) to hold the write lock. While it runs, click 'regenerate' in the reader.
**Expected:** Stepper shows 'queued' (parked). NOT a silent no-op. NOT the generic 'timed out or errored' message. Once the lock frees, the stepper advances through subsequent phases.
**Why human:** Requires live concurrency between two processes and observation of the UI state transition.

#### 3. Honest Failure End-State (Optional)

**Test:** Set `RECENSE_CLAUDE_HEADLESS_TIMEOUT_MS=3000` while the lock is held and trigger regenerate.
**Expected:** Reader shows 'generation failed: engine stayed busy' (or the real error) via textContent. Spinner stops. No infinite spin.
**Why human:** Requires controlling process environment and observing the spinner stop live.

#### 4. Palette Guard (Phase 34)

**Test:** During any regenerate, observe step colors.
**Expected:** Rest/done steps stay muted rose/mauve. Amber appears ONLY on the active/in-progress step.
**Why human:** CSS rendering and color perception require visual inspection.

---

### Gaps Summary

None. All 12 automated must-haves are VERIFIED. The 4 outstanding items are deliberate Manual UAT from Plan 04 Task 3 (an explicit `checkpoint:human-verify` in the plan), not failures. They do not indicate implementation gaps — the code is complete and all automated assertions pass.

---

### Commits Verified

| Commit | Description |
|--------|-------------|
| c8fc718 | test(39.3-01): add failing tests for gen-status primitives (RED) |
| 9630a7c | feat(39.3-01): implement gen-status.ts status-file primitives (GREEN) |
| f2ad753 | test(39.3-02): add failing onPhase ordering tests (RED) |
| d77b02e | feat(39.3-02): add optional onPhase callback to generateDoc + generateDocForSchema |
| a4f2af2 | feat(39.3-02): bounded lock-wait loop + onPhase wiring + status transitions in CLI |
| c1e8103 | feat(39.3-03): forward gen phase in send202Generating via buildGeneratingEnvelope |
| f21b050 | test(39.3-03): add gen-envelope unit tests for envelope key-presence contract |
| a428e84 | feat(39.3-04): phase-aware stepper in startGenProgress + phase/error poll loop |
| f92d140 | feat(39.3-04): delete prevGeneratedAt/fetchGeneratedAt hack + add stepper CSS |
| d334e24 | fix(39.3-04): replace smart quotes with ASCII in regenerate() (HEAD) |

All 9 plan commits confirmed in git log. HEAD (d334e24) is a minor ASCII-correctness fix to reader.js; does not affect any requirement.

---

_Verified: 2026-06-26T10:47:00Z_
_Verifier: Claude (gsd-verifier)_
