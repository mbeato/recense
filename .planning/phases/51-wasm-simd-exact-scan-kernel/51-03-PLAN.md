---
phase: 51-wasm-simd-exact-scan-kernel
plan: 03
type: execute
wave: 3
depends_on: ["51-02"]
files_modified:
  - src/retrieval/topk.ts
  - tests/topk-simd.test.ts
  - scripts/eval/51-topk-equivalence.cjs
autonomous: true
requirements: [SCALE-03]

must_haves:
  truths:
    - "topkIndexed uses the WASM kernel when available and returns top-k set-identical to the scalar cosineSimF32 scan (recall@10=1.000 over the live brain — D-05/D-07)"
    - "When the kernel is unavailable (SIMD missing / dim%4≠0 / instantiation error), topkIndexed silently runs the verbatim scalar dot loop and still returns correct top-k — never throws, never breaks recall (D-08)"
    - "The kernel measurably accelerates the scan: ~4–5× kernel-only p95 and a materially faster full per-query p95 vs the scalar path over the live brain (Success Criterion 3)"
    - "Scalar fallbacks left untouched: cosineSimF32, brute-force topk, topkTombstoned unchanged (D-06)"
  artifacts:
    - path: "src/retrieval/topk.ts"
      provides: "topkIndexed wired to the SIMD kernel + partial-select, with verbatim scalar fallback"
      contains: "loadSimdKernel"
    - path: "tests/topk-simd.test.ts"
      provides: "CandidateRetriever integration: kernel set-identity + forced-kernel-unavailable fallback"
    - path: "scripts/eval/51-topk-equivalence.cjs"
      provides: "Live-brain set-identity gate (recall@10=1.000) + kernel-vs-scalar latency report"
  key_links:
    - from: "src/retrieval/topk.ts"
      to: "src/retrieval/simd-kernel.ts"
      via: "import loadSimdKernel + partialSelectTopK; call scanCosine in topkIndexed"
      pattern: "scanCosine|loadSimdKernel"
    - from: "scripts/eval/51-topk-equivalence.cjs"
      to: "CandidateRetriever indexed (WASM) path"
      via: "new CandidateRetriever(db,{indexPath}).topk vs bruteForce cosineSimF32"
      pattern: "CandidateRetriever"
---

<objective>
Wire the SIMD kernel into the one accelerated hot path — `topkIndexed` (D-05) — with a verbatim scalar fallback (D-08), prove exactness and fallback at the CandidateRetriever level, and gate the speedup + set-identity on the live brain.

Purpose: This realizes SCALE-03 end-to-end. The kernel loads once in the constructor alongside the index; `topkIndexed` calls `scanCosine` + `partialSelectTopK` when the kernel is present and otherwise runs the existing scalar dot loop unchanged. The scalar fallbacks (`cosineSimF32`, brute-force `topk`, `topkTombstoned`) stay untouched (D-06). Phase-50's regression gates plus this phase's `51-topk-equivalence` gate catch any retrieval regression.

Output: modified `src/retrieval/topk.ts`, `tests/topk-simd.test.ts`, and the live-brain gate `scripts/eval/51-topk-equivalence.cjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-wasm-simd-exact-scan-kernel/51-CONTEXT.md
@.planning/phases/51-wasm-simd-exact-scan-kernel/51-PATTERNS.md
@src/retrieval/topk.ts
@tests/topk-index.test.ts
@scripts/eval/41-topk-equivalence.cjs

<interfaces>
<!-- Contracts from Plan 02 — build directly against these, no exploration. -->

From src/retrieval/simd-kernel.ts:
  interface SimdKernel { scanCosine(query: Float32Array, qNorm: number): Float32Array }
  export function loadSimdKernel(
    data: Float32Array, norms: Float64Array, count: number, dim: number
  ): SimdKernel | null;        // null on SIMD-missing / dim%4≠0 / length mismatch / instantiate error — never throws
  export function partialSelectTopK(
    scores: Float32Array, ids: string[], k: number
  ): Array<{ id: string; score: number }>;   // set-identical to .sort((a,b)=>b.score-a.score).slice(0,k)

Existing topkIndexed it modifies (src/retrieval/topk.ts lines 379-402):
  private topkIndexed(queryVec: Float32Array, k: number): Array<{ id: string; score: number }>
  // PRESERVE: `queryVec.length !== idx.dim → return []` (line 383); `denom === 0 ? 0` guard (line 398)

Constructor index load (src/retrieval/topk.ts lines 318-327): this.index = loadVectorIndex(opts.indexPath); stderr warn on null.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the kernel into topkIndexed with verbatim scalar fallback</name>
  <files>src/retrieval/topk.ts</files>
  <read_first>
    - src/retrieval/topk.ts (lines 284-328 constructor + index field + stderr-warning pattern; lines 379-402 topkIndexed; lines 368-378 the byte-equivalence docstring to update for D-07; lines 200-211 cosineSimF32 to leave untouched; lines 340-366 brute-force topk + 409-426 topkTombstoned to leave untouched per D-06)
    - src/retrieval/simd-kernel.ts (the loadSimdKernel/SimdKernel/partialSelectTopK contracts)
    - .planning/phases/51-wasm-simd-exact-scan-kernel/51-PATTERNS.md (lines 152-194 the exact modification spec for topkIndexed; lines 231-249 silent-fallback + read-only shared patterns)
  </read_first>
  <action>
    Add `import { loadSimdKernel, partialSelectTopK, type SimdKernel } from './simd-kernel';` to `src/retrieval/topk.ts`.

    Constructor: add a private field `private readonly kernel: SimdKernel | null`. After the existing `this.index = loadVectorIndex(...)` block (lines 318-327), build the kernel ONCE: when `this.index !== null` call `loadSimdKernel(this.index.data, this.index.norms, this.index.count, this.index.dim)`; assign to `this.kernel`. When it returns null AND an index is present, emit ONE stderr line (mirror the existing line-321 warning style, e.g. `[recense] WASM SIMD kernel unavailable — using scalar exact scan\n`) — never stdout (hot path emits structured output). When `this.index === null`, set `this.kernel = null`. Loading the corpus into WASM memory happens inside loadSimdKernel (Plan 02), once, here — not per query.

    `topkIndexed`: keep the `queryVec.length !== idx.dim → return []` guard (line 383) and the qNorm precompute (lines 386-388) exactly. Then branch:
      - When `this.kernel !== null`: `const scores = this.kernel.scanCosine(queryVec, qNorm)` (a count-length Float32Array of cosine values, denom-guarded in the kernel), then `return partialSelectTopK(scores, idx.ids, k)`.
      - When `this.kernel === null`: run the EXISTING scalar dot loop verbatim (lines 390-399 — per-row dot + `denom === 0 ? 0 : dot/denom`) to build the score array, then select via `partialSelectTopK` instead of `.sort().slice(k)` (D-02; partial-select is set-identical to the old sort, so this stays byte-equivalent). The scalar dot computation is unchanged — only the selection method upgrades.

    Update the topkIndexed docstring (lines 368-378): note the score is now computed by the f32x4 WASM kernel when available, that f32x4 horizontal-sum ordering produces sub-ULP score differences from sequential scalar summation which are EXPECTED and accepted (D-07 set-identity bar, not bitwise), and that a kernel-unavailable fault falls back to the verbatim scalar scan (D-08).

    Leave UNTOUCHED (D-06): `cosineSimF32`, brute-force `topk` (the no-index path lines 350-365), `topkTombstoned`, `hybridTopk`, `buildVectorIndex`, `loadVectorIndex`.
  </action>
  <acceptance_criteria>
    - `topkIndexed` calls `this.kernel.scanCosine` + `partialSelectTopK` when kernel present; runs the verbatim scalar dot loop + `partialSelectTopK` when kernel null.
    - The `queryVec.length !== idx.dim → return []` guard and the `denom === 0 ? 0` semantics are preserved.
    - `git diff src/retrieval/topk.ts` shows NO changes to `cosineSimF32`, brute-force `topk`, `topkTombstoned`, `hybridTopk`, `buildVectorIndex`, `loadVectorIndex` (D-06).
    - The existing `tests/topk-index.test.ts` (DIM=8, %4===0 → exercises the SIMD path) stays green — no regression.
    - `npm run build` clean.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npx vitest run tests/topk-index.test.ts</automated>
  </verify>
  <done>topkIndexed accelerated via the kernel with verbatim scalar fallback; D-06 paths untouched; existing index tests green.</done>
</task>

<task type="auto">
  <name>Task 2: CandidateRetriever integration test — set-identity + forced fallback</name>
  <files>tests/topk-simd.test.ts</files>
  <read_first>
    - tests/topk-index.test.ts (lines 41-122 fixture: temp DB, SemanticStore.upsertNode/setEmbedding, buildVectorIndex, bruteforceTopk, idSet; lines 154-171 the missing-artifact fallback case to adapt for kernel-unavailability)
    - src/retrieval/topk.ts (the modified topkIndexed + constructor kernel load from Task 1)
  </read_first>
  <action>
    Create `tests/topk-simd.test.ts` reusing topk-index.test.ts's fixture machinery (temp file DB, `initSchema`, `SemanticStore`, seeded vectors via a `vec(seed)`, `buildVectorIndex`, `bruteforceTopk`, `idSet`). Use a DIM divisible by 4 (e.g. 8) so the WASM kernel path actually exercises.

    Case 1 (kernel set-identity through CandidateRetriever — the D-07 bar end-to-end): build the sidecar, `const indexed = new CandidateRetriever(db, { indexPath })`, run `indexed.topk(q, 10)` for several queries; assert `idSet(got)` equals `idSet(bruteforceTopk(livePairs, q, 10))`, and for each hit `expect(hit.score).toBeCloseTo(refScore.get(hit.id)!, 6)` and `.not.toBe(0)`. This proves the WASM-accelerated path is set-identical with real cosine scores.

    Case 2 (forced kernel-unavailable → scalar fallback set-identical, D-08): force the kernel unavailable while keeping the index loaded — the simplest deterministic lever is to seed/query at a dim NOT divisible by 4 (e.g. DIM=9) so `loadSimdKernel` returns null and `topkIndexed` runs the scalar fallback; assert the indexed retriever's top-k is still set-identical to brute-force (recall preserved, no throw). Optionally also assert no exception is thrown and results equal a fresh no-index `CandidateRetriever(db)` brute-force path. Document in a comment that this exercises the D-08 silent-fallback contract at the integration level (the loader-returns-null unit is covered in Plan 02).
  </action>
  <acceptance_criteria>
    - Case 1: `idSet(indexed.topk(q,10))` equals brute-force id set; scores `toBeCloseTo(...,6)`, non-zero.
    - Case 2: with dim%4≠0 the retriever falls back silently and returns a set-identical top-k to brute-force; no throw.
    - `npx vitest run tests/topk-simd.test.ts` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/topk-simd.test.ts</automated>
  </verify>
  <done>Integration test proves WASM-path set-identity and forced-kernel-unavailable silent scalar fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Live-brain equivalence + kernel-vs-scalar latency gate</name>
  <files>scripts/eval/51-topk-equivalence.cjs</files>
  <read_first>
    - scripts/eval/41-topk-equivalence.cjs (the full sibling: DIST require shape lines 45-47, query cue set lines 64-85, mockVector lines 90-102, bruteForceScored lines 120-132, boundary-tie topkEquivalent lines 137-153+, sidecar-present assertion)
    - scripts/eval/49-wasm-simd-probe.cjs (lines 101-126 the kernel-vs-scalar p95 timing methodology and the JSON results-file output convention)
    - src/retrieval/topk.ts (the now-WASM-accelerated indexed path the gate exercises)
  </read_first>
  <action>
    Create `scripts/eval/51-topk-equivalence.cjs` as a sibling of `41-topk-equivalence.cjs` (same conventions: `'use strict'`, read-only `Database(DB_PATH,{readonly:true,fileMustExist:true})`, DIST require of `CandidateRetriever`/`cosineSimF32`/`vectorIndexPath`, same 20-query cue set + `mockVector`, `--real-embed` opt, results JSON under `scripts/eval/results/`). Run from project root after `npm run build`.

    Equivalence half (Success Criteria 1 — set-identity / recall@10=1.000): ASSERT the sidecar exists (build via `buildVectorIndex` or fail loud — never let an absent sidecar make the indexed path fall back to brute-force and trivially pass against itself, per the 41- script's caveat). For each query, compare `new CandidateRetriever(db,{indexPath}).topk(q, k)` (now the WASM kernel path) against `bruteForceScored(db, q)` using the existing boundary-tie `topkEquivalent` logic (set-identical, tolerating only an equal-score reorder within `TIE_EPS`) at `candidateK` and a larger k (≥20). `process.exit(1)` if ANY query diverges beyond a boundary tie — this is the recall@10=1.000 / D-07 gate over the live brain.

    Latency half (Success Criterion 3 — ~4–5× kernel / materially faster full p95): adapt the probe's timing methodology (49-wasm-simd-probe.cjs lines 101-126). Over the live embedded set, time per-query (a) the scalar dot scan and (b) the WASM `scanCosine` kernel (kernel-only p95), and (c) the full per-query indexed `topk` vs a scalar-only `topk` (full p95), with warmup + REPEATS. Report `kernel_speedup` and `full_speedup` and the p95s. Write `scripts/eval/results/51-topk-equivalence.json`. Print a one-line stderr summary. The gate PASSES the latency check when kernel speedup is materially > 1 (target ~4–5× per the spike) and exact equivalence holds; treat a sub-1× kernel speedup as a regression worth flagging in the summary (do not hard-fail latency on hardware variance, but DO hard-fail the equivalence half).
  </action>
  <acceptance_criteria>
    - `node scripts/eval/51-topk-equivalence.cjs` exits 0 with the live sidecar present: every query's WASM-indexed top-k is set-identical to brute-force (recall@10=1.000, boundary-tie tolerant). Exits 1 on any genuine divergence.
    - Writes `scripts/eval/results/51-topk-equivalence.json` containing kernel-only and full per-query p95 for scalar vs WASM and the computed `kernel_speedup`/`full_speedup`.
    - The script asserts the sidecar is present (does not silently self-compare via brute-force fallback).
    - Phase-50 regression gates (`npm run gate` / `npm run gate:accuracy`) still pass — no retrieval accuracy regression introduced by the kernel.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && node scripts/eval/51-topk-equivalence.cjs; echo "exit=$?"</automated>
  </verify>
  <done>Live-brain gate proves WASM-path set-identity (recall@10=1.000) and reports the ~4–5× kernel / materially faster full p95 speedup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| derived-cache kernel → authoritative retrieval results | The WASM kernel is a derived accelerator over the derived sidecar; a fault must never become the authoritative top-k (graph is source of truth) |
| online retrieval hot path | topkIndexed runs on every SessionStart inject / recall / ambient — latency-sensitive, LLM-free |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-31 | Tampering (integrity) | topkIndexed kernel branch | mitigate | `this.kernel === null` → verbatim scalar dot loop (D-08); a kernel fault degrades to the verified scalar scan and is never authoritative. The 51-equivalence gate asserts set-identity vs `cosineSimF32` on the live brain, so any silent kernel score corruption fails the gate (recall@10 < 1.000). |
| T-51-32 | Repudiation / silent regression | retrieval accuracy | mitigate | Phase-50 regression gates + `scripts/eval/51-topk-equivalence.cjs` (set-identity, hard-fail on divergence) gate every change; `tests/topk-simd.test.ts` + the existing `tests/topk-index.test.ts` cover unit/integration equivalence and fallback. |
| T-51-33 | Tampering (scope creep) | D-06 scalar fallbacks | accept→mitigate | `cosineSimF32`, brute-force `topk`, `topkTombstoned` left untouched; an acceptance `git diff` check confirms no edits leaked into the rarely-run cold paths. |
</threat_model>

<verification>
- `npm run build && npx vitest run tests/topk-index.test.ts tests/topk-simd.test.ts` all green.
- `node scripts/eval/51-topk-equivalence.cjs` exits 0 (set-identity recall@10=1.000) and writes the latency JSON showing kernel speedup ~4–5×.
- `npm run gate` / `npm run gate:accuracy` (Phase-50 gates) still pass — no regression.
- `git diff src/retrieval/topk.ts` shows the D-06 paths untouched.
</verification>

<success_criteria>
SCALE-03 realized end-to-end: `topkIndexed` is WASM-accelerated with set-identical top-k to the scalar scan (recall@10=1.000 on the live brain), silently falls back to the verbatim scalar scan on any kernel fault (D-08), measurably faster (~4–5× kernel / materially faster full p95), with the cold scalar paths untouched (D-06) and Phase-50 gates green.
</success_criteria>

<output>
Create `.planning/phases/51-wasm-simd-exact-scan-kernel/51-03-SUMMARY.md` when done.
</output>
