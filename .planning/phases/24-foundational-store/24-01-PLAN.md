---
phase: 24-foundational-store
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [SCOPE-01, SCOPE-02]
decisions_covered: [D-01, D-02, D-03, D-04, D-05]
must_haves:
  truths:
    - "A manual sleep pass on the live DB completes with no FOREIGN KEY constraint error (D-01, D-03)"
    - "After the pass, the dirty sentinel file is cleared and PRAGMA foreign_key_check returns empty (D-02)"
    - "The hourly launchd sleep-pass agent is re-enabled and survives one full scheduled cycle without a FK error (D-04, D-05)"
    - "recall output for an existing consolidated fact carries a [scope] attribution prefix"
  artifacts:
    - path: "/tmp/recense-sleep.log"
      provides: "Evidence log of the clean manual sleep pass (no FK error)"
      contains: "no 'FOREIGN KEY constraint failed' substring"
    - path: ".planning/phases/24-foundational-store/24.1-SCOPE01-EVIDENCE.md"
      provides: "Captured gate evidence: log tail, pragma result, recall sample, hourly-cycle timestamp"
      min_lines: 20
  key_links:
    - from: "launchd com.recense.sleep-pass"
      to: "$RECENSE_SLEEP_JS (dist/src/adapter/sleep-pass-cli.js)"
      via: "scheduled StartCalendarInterval Minute=0 run"
      pattern: "/tmp/recense-sleep.log timestamp advances on the hour with no FK error"
    - from: "src/consolidation/schema-relations.ts child-wipe"
      to: "node_scope / node_temporal child rows"
      via: "DELETE child rows before DELETE FROM node (ab3b6c8 fix being verified)"
      pattern: "DELETE FROM node_scope.*super::"
---

<objective>
Verify the FK consolidation fix end-to-end (SCOPE-01) and confirm live scope attribution (SCOPE-02). This is the hard gate for all of v5.0: run a clean FK-free manual sleep pass on the LIVE DB, confirm the dirty sentinel clears and `PRAGMA foreign_key_check` is empty, then re-enable the hourly launchd agent and confirm it survives one full scheduled cycle. Separately confirm an existing consolidated fact surfaces with a `[scope]` prefix in recall output.

Purpose: The schema-relations DELETE-side FK fix (schema-relations.ts child-wipe, commit ab3b6c8) and the Phase 23 schema-relations FK-02 fix are unverified by a clean live pass. The hourly agent is currently `bootout`ed to prevent crash loops. Until this gate passes, the migration (24.3) must not run — it consolidates through the same path and would re-trigger the bug.

Output: A clean `/tmp/recense-sleep.log`, an empty `foreign_key_check`, a re-enabled hourly agent that survived a cycle, a recall sample showing `[scope]`, and a captured evidence file `24.1-SCOPE01-EVIDENCE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-foundational-store/24-CONTEXT.md
@docs/import-memory.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run a clean FK-free manual sleep pass on the live DB and verify it (SCOPE-01, D-01/D-02/D-03)</name>
  <files>/tmp/recense-sleep.log</files>
  <read_first>
    - .planning/STATE.md (§"Phase 24 — Critical Context": sleep-pass invocation, FK bug status, DB path)
    - .planning/phases/24-foundational-store/24-CONTEXT.md (D-01/D-02/D-03)
    - docs/import-memory.md (consolidate→verify→retire flow context)
    - src/consolidation/run-sleep-pass.ts (consolidation entry; FK-sensitive path)
    - src/consolidation/schema-relations.ts (the child-wipe fix being verified — lines ~197-210, ~478)
    - src/db/schema.ts (node_scope sidecar DDL, SCHEMA_VERSION=10)
  </read_first>
  <action>
    Confirm RECENSE_DB resolution agreement first: read `~/.config/recense/sleep.env` and assert `RECENSE_DB=/Users/vtx/.config/recense/recense.db`; also confirm the manual `recense` default points at the same canonical path (homedir default). Both the launchd agent and manual verification MUST read the same DB.
    Run the documented manual sleep pass against the live DB (NOT a copy — SCOPE-01 requires the live DB): hydrate env with `set -a; . ~/.config/recense/sleep.env; set +a`, then run `"$RECENSE_NODE_BIN" "$RECENSE_SLEEP_JS"`. Node-bin pinning is required (better-sqlite3 ABI under nvm). Tee/redirect output so `/tmp/recense-sleep.log` captures the run.
    After the run assert ALL three clean-pass conditions (D-02): (a) the log contains NO `FOREIGN KEY constraint failed` substring and exits 0; (b) the dirty sentinel at `$RECENSE_DIRTY_SENTINEL` (`/Users/vtx/.config/recense/.episodes-dirty`) is cleared/absent after the pass; (c) `PRAGMA foreign_key_check;` against `$RECENSE_DB` (via `sqlite3` or `$RECENSE_NODE_BIN -e` with better-sqlite3) returns an EMPTY result set.
    If any condition fails, STOP — this is a real bug in the already-landed fix, not an operational hiccup. Capture the failing log tail and `err.stack` (both sleep-pass error sites log stack per ab3b6c8) and surface it; do not proceed to Task 2 or any downstream plan.
  </action>
  <verify>
    <automated>! grep -q 'FOREIGN KEY constraint failed' /tmp/recense-sleep.log &amp;&amp; test ! -s /Users/vtx/.config/recense/.episodes-dirty</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/recense-sleep.log` exists and contains NO `FOREIGN KEY constraint failed` substring
    - The sleep-pass process exited 0
    - The dirty sentinel `/Users/vtx/.config/recense/.episodes-dirty` is cleared (absent or zero-length) after the pass
    - `PRAGMA foreign_key_check` against `/Users/vtx/.config/recense/recense.db` returns an empty result set (zero rows)
    - `~/.config/recense/sleep.env` `RECENSE_DB` equals `/Users/vtx/.config/recense/recense.db` and matches the manual `recense` default
  </acceptance_criteria>
  <done>All three D-02 clean-pass conditions hold against the live DB; log captured as evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Verify live [scope] attribution in recall (SCOPE-02, D-S6)</name>
  <files></files>
  <read_first>
    - src/adapter/ambient-recall.ts (buildHookOutput — the [scope] prefix emit, ~line 148)
    - src/adapter/session-start-cli.ts (recall output [scope] prefix)
    - .planning/phases/999.3-scope-aware-provenance-memory-importer/999.3-CONTEXT.md (D-S1/D-S6: provenance not tenancy; retrieval stays global; resume→global)
  </read_first>
  <action>
    Run `recense recall "<query>"` for at least two queries that match existing consolidated facts in the live DB (pick queries that hit facts known to have a project cwd origin, e.g. a brain-memory engine fact, and a personal/global fact). Assert the recall output renders a `[<scope>]` prefix on returned facts — at least one project-scoped marker (e.g. `[brain-memory]`) and confirm personal/multi-project origins render `[global]` per the cwd→scope mapping (D-S3). This is attribution only; retrieval stays GLOBAL (D-S1) — do not expect or add any project filtering.
    If existing facts surface with no `[scope]` prefix at all, that is a SCOPE-02 defect — capture the raw recall output and surface it before declaring SCOPE-02 verified.
  </action>
  <verify>
    <automated>recense recall "brain-memory engine" 2>&amp;1 | grep -qE '\[[a-z0-9-]+\]'</automated>
  </verify>
  <acceptance_criteria>
    - `recense recall` output for at least 2 queries shows a `[<scope>]` prefix on returned facts
    - At least one returned fact shows a project-slug scope (e.g. `[brain-memory]`)
    - Personal/multi-project-origin facts render `[global]` (consistent with D-S3 cwd→scope mapping)
    - Retrieval remains global (no project filtering applied — scope is attribution only)
  </acceptance_criteria>
  <done>Existing consolidated facts surface in recall with a correct `[scope]` prefix; SCOPE-02 verified live.</done>
</task>

<task type="auto">
  <name>Task 3: Re-enable the hourly launchd agent, confirm it survives one cycle, and capture gate evidence (SCOPE-01, D-04/D-05)</name>
  <files>.planning/phases/24-foundational-store/24.1-SCOPE01-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/24-foundational-store/24-CONTEXT.md (D-04/D-05: re-enable only after clean pass; survive one full cycle)
    - ~/Library/LaunchAgents/com.recense.sleep-pass.plist (label com.recense.sleep-pass, StartCalendarInterval Minute=0)
    - .planning/STATE.md (hourly agent currently bootout'ed)
  </read_first>
  <action>
    Only after Task 1 passed (clean manual pass): re-enable the hourly launchd sleep-pass agent. It is currently `bootout`ed. Re-enable via `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.recense.sleep-pass.plist` (or `enable` + `kickstart` if already bootstrapped). Confirm `launchctl print gui/$(id -u)/com.recense.sleep-pass` reports it loaded.
    The agent fires at `Minute=0` (top of the hour). D-05 requires it survive ONE FULL scheduled cycle — wait for / trigger the next on-the-hour run (you MAY `launchctl kickstart -k gui/$(id -u)/com.recense.sleep-pass` to force a scheduled-equivalent run, but the gate is satisfied by a clean run THROUGH launchd, not a bare manual invocation). After the scheduled/kickstarted run, assert `/tmp/recense-sleep.log` has a fresh timestamp newer than Task 1's run AND contains no `FOREIGN KEY constraint failed` for that run.
    Write `.planning/phases/24-foundational-store/24.1-SCOPE01-EVIDENCE.md` capturing: the manual-pass log tail, the `PRAGMA foreign_key_check` empty result, the dirty-sentinel-cleared check, the `[scope]` recall sample (Task 2), the `launchctl print` loaded status, and the post-launchd-run log timestamp + clean assertion. This file IS the SCOPE-01 gate evidence that downstream plans depend on.
  </action>
  <verify>
    <automated>launchctl print gui/$(id -u)/com.recense.sleep-pass &gt;/dev/null 2>&amp;1 &amp;&amp; test -f .planning/phases/24-foundational-store/24.1-SCOPE01-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `launchctl print gui/$(id -u)/com.recense.sleep-pass` reports the agent loaded (no longer bootout'ed)
    - A launchd-triggered (scheduled or kickstarted) run appended a fresh `/tmp/recense-sleep.log` entry with a timestamp newer than the Task 1 manual run
    - That launchd run contains NO `FOREIGN KEY constraint failed` substring
    - `24.1-SCOPE01-EVIDENCE.md` exists and contains: manual-pass log tail, empty foreign_key_check result, dirty-sentinel-cleared check, `[scope]` recall sample, launchctl loaded status, post-launchd-run timestamp
  </acceptance_criteria>
  <done>The hourly agent is re-enabled and survived a launchd-triggered cycle cleanly; SCOPE-01 gate evidence is written and persisted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launchd → engine process | The scheduled agent runs compiled sink code against the LIVE DB unattended; a re-enable before the FK fix is verified can re-enter a crash/corruption loop. |
| sleep-pass → live DB | The consolidation pass mutates the production `recense.db` (node eviction + child-row deletes); a regressed FK fix can leave dangling child rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Denial of Service | launchd hourly agent (D-04/D-05) | mitigate | Re-enable ONLY after Task 1's clean manual pass; gate re-enable on the same FK assertions. If the manual pass fails, the agent stays bootout'ed (Task 3 never runs). |
| T-24-02 | Tampering | live recense.db during sleep pass | mitigate | Run on the live DB per SCOPE-01 but assert `PRAGMA foreign_key_check` empty + no FK error after; the eviction child-wipe (ab3b6c8) prevents dangling node_scope/node_temporal rows. Stop on any FK violation. |
| T-24-03 | Repudiation | gate pass claim | mitigate | Persist `24.1-SCOPE01-EVIDENCE.md` with log tails + pragma output + timestamps so the gate is auditable, not self-reported. |
| T-24-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this plan installs no packages (net-zero deps invariant; verification only). No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `/tmp/recense-sleep.log` shows a clean manual run and a clean launchd-triggered run (no FK error in either).
- `PRAGMA foreign_key_check` on the live DB returns empty.
- Dirty sentinel cleared after the pass.
- `recense recall` surfaces `[scope]` prefixes.
- Hourly agent loaded per `launchctl print` and survived a cycle.
- `24.1-SCOPE01-EVIDENCE.md` exists with all required captures.
</verification>

<success_criteria>
SCOPE-01 satisfied: a clean FK-free manual pass completed on the live DB, the dirty sentinel cleared, `foreign_key_check` is empty, and the re-enabled hourly agent survived a cycle. SCOPE-02 verified: existing consolidated facts surface in recall with correct `[scope]` attribution. The migration (24.2/24.3) is now unblocked.
</success_criteria>

<output>
Create `.planning/phases/24-foundational-store/24.1-SUMMARY.md` when done.
</output>
