---
phase: 24-foundational-store
plan: 2
type: execute
wave: 2
depends_on: ["24-01"]
files_modified: []
autonomous: true
requirements: [SCOPE-03]
decisions_covered: [D-07]
must_haves:
  truths:
    - "recense import-memory --dry-run shows at least 193 facts to import (D-07)"
    - "The dry-run skips all 7 load-bearing policy bundles (0 policy-bundle leaks)"
    - "The dry-run skips MEMORY.md index files"
    - "The dry-run opens no DB, takes no lock, and writes nothing (no source file touched)"
  artifacts:
    - path: ".planning/phases/24-foundational-store/24.2-DRYRUN-EVIDENCE.md"
      provides: "Captured dry-run output: import count, skip set, policy-bundle skip confirmation, per-file scope sample"
      min_lines: 20
  key_links:
    - from: "recense import-memory --dry-run"
      to: "~/.claude/projects/*/memory/*.md"
      via: "scan-only file enumeration (no DB, no writes)"
      pattern: "import count >= 193 AND policy bundles in skip list"
---

<objective>
Verify SCOPE-03 via the safe dry-run gate: `recense import-memory --dry-run` must show at least 193 facts to import and 0 policy-bundle leaks (the 7 load-bearing policy bundles and all MEMORY.md index files skipped), matching the 2026-06-16 baseline. This gate-checks the importer before any paid real run in 24.3.

Purpose: The dry-run is the cheap, side-effect-free correctness check that proves the importer's skip logic (D-S5) still holds. The headline security risk for this phase is a policy bundle leaking in as a probabilistic fact — this plan proves that does NOT happen before the real run touches the live graph. If the count or skip-set has drifted from baseline, investigate before importing.

Output: A confirmed dry-run plan and captured evidence file `24.2-DRYRUN-EVIDENCE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-foundational-store/24-CONTEXT.md
@docs/import-memory.md
@.planning/phases/24-foundational-store/24.1-SCOPE01-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm SCOPE-01 gate is satisfied before any import work</name>
  <files></files>
  <read_first>
    - .planning/phases/24-foundational-store/24.1-SCOPE01-EVIDENCE.md (the gate evidence from 24.1)
    - .planning/phases/24-foundational-store/24-CONTEXT.md (SCOPE-01 is a hard prerequisite for the migration)
  </read_first>
  <action>
    Assert the SCOPE-01 gate passed before doing any import-related work. Read `24.1-SCOPE01-EVIDENCE.md` and confirm it records a clean manual pass (no FK error), empty `foreign_key_check`, cleared dirty sentinel, and a re-enabled hourly agent that survived a cycle. If that evidence file is missing or records a failure, STOP — the migration must not proceed (running the import consolidates through the same FK-sensitive path).
  </action>
  <verify>
    <automated>test -f .planning/phases/24-foundational-store/24.1-SCOPE01-EVIDENCE.md &amp;&amp; ! grep -qi 'FOREIGN KEY constraint failed' .planning/phases/24-foundational-store/24.1-SCOPE01-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `24.1-SCOPE01-EVIDENCE.md` exists and records a clean pass (no FK error, empty foreign_key_check, cleared sentinel, agent survived a cycle)
  </acceptance_criteria>
  <done>SCOPE-01 gate confirmed satisfied; safe to proceed to the dry-run.</done>
</task>

<task type="auto">
  <name>Task 2: Run import-memory --dry-run and gate-check counts + policy-bundle skips (SCOPE-03, D-07)</name>
  <files>.planning/phases/24-foundational-store/24.2-DRYRUN-EVIDENCE.md</files>
  <read_first>
    - docs/import-memory.md (Usage + "What gets imported vs skipped (D-S5)" + dry-run is side-effect-free)
    - .planning/phases/24-foundational-store/24-CONTEXT.md (D-07: ≥193 facts, 0 policy-bundle leaks, 7 bundles + indexes skipped; D-08 idempotency)
    - .planning/phases/999.3-scope-aware-provenance-memory-importer/999.3-CONTEXT.md (D-S5 skip set; cwd→scope mapping incl. resume→global)
    - src/adapter/recense.ts (CLI dispatcher; import-memory routes to import-memory-cli.js)
  </read_first>
  <action>
    Run `recense import-memory --dry-run` (it opens no DB, takes no lock, writes nothing per the runbook). Gate-check the output against the 2026-06-16 baseline (D-07): at least 193 facts to import, exactly 7 load-bearing policy bundles skipped (`voice_profile`, `feedback_no_inflated_metrics`, `feedback_drop_concentrations`, `outreach_framework`, `user_job_search_strategy`, `reference_linkedin_playbook`, `user_profile`), and all `MEMORY.md` index files skipped. Confirm 0 policy-bundle leaks — none of those 7 filenames appear in the import (not the skip) list. Spot-check a few per-file scope derivations look right (project slug from folder basename; `resume` project → `global`).
    If the import count is below 193 or any policy bundle appears in the import list, STOP and investigate the drift before any real run — do not proceed to 24.3.
    Write `.planning/phases/24-foundational-store/24.2-DRYRUN-EVIDENCE.md` capturing: total import count, total skip count, the policy-bundle skip list, confirmation of 0 leaks, MEMORY.md-index-skipped confirmation, and a per-file scope sample for ≥3 projects.
  </action>
  <verify>
    <automated>test -f .planning/phases/24-foundational-store/24.2-DRYRUN-EVIDENCE.md &amp;&amp; recense import-memory --dry-run 2>&amp;1 | tee /tmp/recense-import-dryrun.log &gt;/dev/null; ! grep -qiE 'voice_profile|outreach_framework|user_profile' <(grep -iE 'import' /tmp/recense-import-dryrun.log)</automated>
  </verify>
  <acceptance_criteria>
    - `recense import-memory --dry-run` reports at least 193 facts to import
    - All 7 named policy bundles appear in the SKIP list, none in the IMPORT list (0 policy-bundle leaks)
    - `MEMORY.md` index files appear in the SKIP list
    - The dry-run wrote nothing and touched no source file (side-effect-free per runbook)
    - `24.2-DRYRUN-EVIDENCE.md` exists with import count, skip count, policy-bundle skip list, leak=0 confirmation, and a per-file scope sample for ≥3 projects
  </acceptance_criteria>
  <done>The dry-run matches the 2026-06-16 baseline (≥193 import, 7 bundles + indexes skipped, 0 leaks); evidence captured. The real run is gated and ready for founder budget approval in 24.3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| importer → ~/.claude/projects/*/memory/*.md | The importer reads founder memory files; a misclassification could import a load-bearing policy bundle as a probabilistic fact (the headline risk). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Tampering / Information Disclosure | policy-bundle skip logic (D-S5) | mitigate | Dry-run gate asserts the 7 named policy bundles are in the SKIP list and 0 appear in the IMPORT list BEFORE any real run. A leak here would drop a deterministic load-bearing rule (voice profile, no-inflated-metrics) into probabilistic retrieval. |
| T-24-05 | Spoofing | import count drift | mitigate | Gate against the 2026-06-16 baseline (≥193); investigate any drift before importing rather than blindly importing whatever the scan returns. |
| T-24-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs in this plan (dry-run only; net-zero deps). |
</threat_model>

<verification>
- `recense import-memory --dry-run` reports ≥193 import / 7 policy bundles + MEMORY.md indexes skipped / 0 leaks.
- `24.2-DRYRUN-EVIDENCE.md` captures the counts and skip set.
- SCOPE-01 gate evidence confirmed present and clean before the dry-run.
</verification>

<success_criteria>
SCOPE-03 verified: the dry-run shows ≥193 importable facts with 0 policy-bundle leaks, matching baseline. The importer's skip logic is confirmed correct. The real import is gated, scoped, and ready for founder budget sign-off in 24.3.
</success_criteria>

<output>
Create `.planning/phases/24-foundational-store/24.2-SUMMARY.md` when done.
</output>
