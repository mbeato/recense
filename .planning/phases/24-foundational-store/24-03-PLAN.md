---
phase: 24-foundational-store
plan: 3
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - .planning/phases/24-foundational-store/999.3-MIGRATION.md
autonomous: false
requirements: [SCOPE-04]
decisions_covered: [D-06, D-08, D-09, D-10, D-11, D-12, D-13]
user_setup: []
must_haves:
  truths:
    - "The real import lands all importable founder facts as episodes without touching any source file (D-06, D-08, D-13)"
    - "After a sleep pass, at least 3 imported facts per project across at least 3 projects are retrievable via recense recall (D-09)"
    - "Each verified fact returns with the correct [scope] prefix matching its origin project"
    - "A written migration report (999.3-MIGRATION.md) exists with counts, cost, and a per-sample verification table (D-10)"
    - "Source fact files are archived (moved, never deleted) only after verification AND explicit founder sign-off (D-11, D-12)"
  artifacts:
    - path: ".planning/phases/24-foundational-store/999.3-MIGRATION.md"
      provides: "Migration report: imported/skipped counts, actual embedding cost, per-sample verification table, non-surfacing facts"
      min_lines: 30
      contains: "per-sample verification table"
  key_links:
    - from: "recense import-memory (real run)"
      to: "episodes (source='memory-import', stable external_id)"
      via: "IngestionPipeline.recordEvent with RECENSE_ENABLED_SOURCES= (adapters disabled)"
      pattern: "external_id memory-import:<project>:<filename>"
    - from: "recense sleep-pass"
      to: "consolidated facts with node_scope stamped"
      via: "scope derived from contributing episode cwd"
      pattern: "recense recall returns imported fact with [<scope>] prefix"
    - from: "founder sign-off"
      to: "~/.claude/projects-memory-archive-<date>/"
      via: "move (never delete) after verification report exists"
      pattern: "MEMORY.md indexes + policy bundles left in place"
---

<objective>
Run the human-gated consolidate→verify→retire migration (SCOPE-04, D-S7): the real (paid) `recense import-memory` + `recense sleep-pass` with source adapters disabled, verify ≥3 imported facts per project across ≥3 projects surface in recall with correct `[scope]`, write `999.3-MIGRATION.md`, then — only after the founder explicitly signs off — archive (move, never delete) the migrated source fact files.

Purpose: This is the actual migration that brings the founder's MEMORY.md facts into recense under correct scope provenance, retiring the redundant per-project fact stores. It carries two NON-AUTONOMOUS gates: the ~$1–2 paid embedding run (budget approval against ~$14–15 remaining, D-13) and source retirement (founder sign-off, D-11/D-12). Safety order is strictly consolidate→verify→retire; never delete a source before its facts are verified in recense.

Output: Imported facts live in recense under correct scope; `999.3-MIGRATION.md` written; source fact files archived only after sign-off (MEMORY.md indexes + policy bundles left in place).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-foundational-store/24-CONTEXT.md
@docs/import-memory.md
@.planning/phases/24-foundational-store/24.2-DRYRUN-EVIDENCE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Budget gate — surface projected cost and get founder go-ahead for the paid run (D-13)</name>
  <read_first>
    - .planning/phases/24-foundational-store/24-CONTEXT.md (D-13: confirm ~$1–2 embedding cost vs ~$14–15 remaining; state projected cost first)
    - .planning/STATE.md (§Budget Constraints: ~$14–15 remaining; Phase 24 budgeted ~$1–2)
    - .planning/phases/24-foundational-store/24.2-DRYRUN-EVIDENCE.md (the import count that drives the cost estimate)
  </read_first>
  <what-built>
    The dry-run (24.2) confirmed the importable fact count and 0 policy-bundle leaks. The next step (Task 2) is the real paid run: `recense import-memory` then `recense sleep-pass`, which incurs ~$1–2 in embedding cost (DeepSeek is the configured judge; embedding is the cost driver) against the ~$14–15 remaining budget.
  </what-built>
  <how-to-verify>
    1. Review the projected cost: ~$1–2 embedding for the consolidation of the imported episodes (state the exact import count from 24.2-DRYRUN-EVIDENCE.md).
    2. Confirm remaining budget (~$14–15) and that this run fits well within it.
    3. Approve the paid run, or hold.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the ~$1–2 paid import+sleep-pass run, or describe a hold.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run the real import + sleep pass with adapters disabled (SCOPE-04, D-06/D-08)</name>
  <files></files>
  <read_first>
    - docs/import-memory.md (consolidate→verify→retire flow steps 2-3: import with adapters off, then consolidate manually)
    - .planning/phases/24-foundational-store/24-CONTEXT.md (D-06: RECENSE_ENABLED_SOURCES= for both commands; D-08: idempotent, never touches source files)
    - .planning/STATE.md (sleep-pass invocation; node-bin pinning under nvm)
    - src/adapter/recense.ts (import-memory + sleep-pass dispatch)
    - src/adapter/runtime-config.ts (resolveEnabledSources: empty RECENSE_ENABLED_SOURCES = no adapters)
  </read_first>
  <action>
    Run the real import with source adapters disabled so the import is the only thing entering the graph this cycle (D-06): `RECENSE_ENABLED_SOURCES= recense import-memory`. This appends episodes (`source='memory-import'`, stable `external_id`) but does not consolidate. Confirm it touched NO source file (importer never modifies/deletes sources, D-08) and reports an imported-episode count consistent with the dry-run.
    Then consolidate with adapters still disabled: hydrate `set -a; . ~/.config/recense/sleep.env; set +a`, then run `RECENSE_ENABLED_SOURCES= recense sleep-pass` (or the pinned `"$RECENSE_NODE_BIN" "$RECENSE_SLEEP_JS"` with `RECENSE_ENABLED_SOURCES=` exported). This consolidates the imported episodes into facts and stamps their scope. Capture `/tmp/recense-sleep.log` and assert NO `FOREIGN KEY constraint failed` and `PRAGMA foreign_key_check` empty after the pass (the same clean-pass invariant as SCOPE-01 — the migration runs the FK-sensitive path).
    Record the actual embedding cost incurred (for the migration report). If the import or sleep pass errors, STOP and surface the failure before verification — do not retire anything.
  </action>
  <verify>
    <automated>! grep -q 'FOREIGN KEY constraint failed' /tmp/recense-sleep.log</automated>
  </verify>
  <acceptance_criteria>
    - `RECENSE_ENABLED_SOURCES= recense import-memory` completed and reports an imported-episode count consistent with the 24.2 dry-run (≥193)
    - No source file under `~/.claude/projects/*/memory/` was modified or deleted by the import (mtimes unchanged)
    - `RECENSE_ENABLED_SOURCES= recense sleep-pass` completed with no `FOREIGN KEY constraint failed` in `/tmp/recense-sleep.log`
    - `PRAGMA foreign_key_check` on the live DB returns empty after the consolidation pass
    - The actual embedding cost incurred is recorded for the report
  </acceptance_criteria>
  <done>Imported episodes are consolidated into scope-stamped facts; the FK-sensitive path stayed clean; no source file was touched; cost recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Verify retrievability with correct [scope] and write 999.3-MIGRATION.md (SCOPE-04, D-09/D-10)</name>
  <files>.planning/phases/24-foundational-store/999.3-MIGRATION.md</files>
  <read_first>
    - docs/import-memory.md (flow steps 4-5: verify retrievability ≥3 facts/project across ≥3 projects; write 999.3-MIGRATION.md)
    - .planning/phases/24-foundational-store/24-CONTEXT.md (D-09 verification depth; D-10 report contents and the 999.3- filename convention)
    - .planning/phases/999.3-scope-aware-provenance-memory-importer/999.3-CONTEXT.md (cwd→scope mapping for expected-scope-per-project; resume→global)
    - src/adapter/ambient-recall.ts (recall [scope] prefix)
  </read_first>
  <action>
    For a sample of migrated facts, run `recense recall "<query>"` and confirm the fact returns with the correct `[<scope>]` marker (D-09): at least 3 distinct imported facts per project across at least 3 projects (e.g. brain-memory, tonos, vtx; resume-origin facts should surface as `[global]`). Pick queries that exercise distinct facts per project (Claude's discretion on exact strings).
    Investigate any fact that fails to surface BEFORE retiring its source — a non-surfacing fact is a verification failure, not an acceptable loss.
    Write `.planning/phases/24-foundational-store/999.3-MIGRATION.md` (keep the runbook's `999.3-` filename even though it lands in the Phase 24 dir, D-10) containing: imported count, skipped count (7 policy bundles + MEMORY.md indexes), actual embedding cost incurred, a per-sample verification table (columns: query → fact returned? → scope correct?) covering ≥3 facts × ≥3 projects, and a list of any non-surfacing facts with notes.
  </action>
  <verify>
    <automated>test -f .planning/phases/24-foundational-store/999.3-MIGRATION.md &amp;&amp; grep -qiE 'verification table|query.*scope' .planning/phases/24-foundational-store/999.3-MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `recense recall` returns ≥3 distinct imported facts per project across ≥3 projects, each with the correct `[<scope>]` prefix (resume-origin → `[global]`)
    - Any fact that fails to surface is documented in the report and investigated (not silently dropped)
    - `999.3-MIGRATION.md` exists with: imported count, skipped count, actual embedding cost, a per-sample verification table (query → returned? → scope correct?), and a non-surfacing-facts list
  </acceptance_criteria>
  <done>Migrated facts are verified retrievable with correct scope; the migration report is written. Retirement is now eligible (pending founder sign-off).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Founder sign-off, then archive source fact files (move, never delete) (SCOPE-04, D-11/D-12)</name>
  <read_first>
    - docs/import-memory.md (flow step 6: retire = move never delete, founder-gated, leave MEMORY.md indexes + policy bundles in place)
    - .planning/phases/24-foundational-store/24-CONTEXT.md (D-11/D-12: move not delete; only after verification + explicit sign-off; archive dir convention)
    - .planning/phases/24-foundational-store/999.3-MIGRATION.md (the verification report that must exist first)
  </read_first>
  <what-built>
    The migration is complete and verified: imported facts surface in recall with correct scope, and `999.3-MIGRATION.md` documents the counts, cost, and per-sample verification. The final step is retirement — moving (never deleting) the migrated `projects/*/memory/*.md` FACT files to a dated archive dir. This is NON-AUTONOMOUS: it touches real founder files and requires explicit sign-off. MEMORY.md indexes and all 7 policy bundles are left in place.
  </what-built>
  <how-to-verify>
    1. Review `999.3-MIGRATION.md` — confirm verification passed (facts surface with correct scope, no unresolved non-surfacing facts).
    2. Confirm the retirement plan: migrated fact files will be MOVED (not deleted) to `~/.claude/projects-memory-archive-<date>/` (e.g. `~/.claude/projects-memory-archive-2026-06-17/`); MEMORY.md indexes and policy bundles stay in place.
    3. Sign off to authorize the move, or hold.
    After sign-off, execute the move (never `rm`), then assert: the migrated fact files now live under the dated archive dir, and the MEMORY.md indexes + 7 policy bundles remain in their original locations untouched.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize moving migrated fact files to the dated archive dir, or describe a hold.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| importer → live recense.db | The real run writes episodes and consolidates them into the production graph on the FK-sensitive path. |
| retirement → founder filesystem | The retire step moves real founder memory files; a delete-before-verified or a wrong-file move is irreversible data loss. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06 | Tampering / Repudiation | source retirement (D-11/D-12) | mitigate | Retire = MOVE never delete, into a dated archive dir; gated behind a blocking-human checkpoint that requires the `999.3-MIGRATION.md` verification report to exist first. Consolidate→verify→retire order enforced by task sequencing (Task 4 depends on Task 3's report). MEMORY.md indexes + policy bundles explicitly left in place. |
| T-24-07 | Information Disclosure | live-DB write during real import | mitigate | Run with `RECENSE_ENABLED_SOURCES=` so only the import enters the graph this cycle (no concurrent adapter writes); assert `foreign_key_check` empty after the consolidation pass (reuses the SCOPE-01 invariant). |
| T-24-08 | Denial of Service / cost | paid embedding run | mitigate | Blocking-human budget gate (Task 1) surfaces projected ~$1–2 cost vs ~$14–15 remaining before any spend (D-13). |
| T-24-04 | Tampering | policy-bundle skip (carried) | mitigate | The 24.2 dry-run already proved 0 policy-bundle leaks; the real import uses the identical skip logic, and retirement leaves all 7 bundles in place. |
| T-24-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs (migration runs existing CLIs; net-zero deps). |
</threat_model>

<verification>
- Budget gate approved before the paid run.
- Real import landed ≥193 episodes without touching source files; sleep pass clean (no FK error, empty foreign_key_check).
- ≥3 facts × ≥3 projects surface in recall with correct `[scope]`.
- `999.3-MIGRATION.md` written with counts, cost, verification table, non-surfacing list.
- Source retirement executed only after founder sign-off, as a move (never delete), with MEMORY.md indexes + policy bundles left in place.
</verification>

<success_criteria>
SCOPE-04 satisfied (D-S7 migration complete): the founder's MEMORY.md recall-facts are imported into recense under correct scope provenance, verified retrievable with correct `[scope]`, documented in `999.3-MIGRATION.md`, and the source fact files archived (moved, never deleted) only after explicit founder sign-off. Phase 24 closes; Phase 25 is unblocked.
</success_criteria>

<output>
Create `.planning/phases/24-foundational-store/24.3-SUMMARY.md` when done.
</output>
