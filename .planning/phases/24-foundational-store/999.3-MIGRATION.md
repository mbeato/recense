# 999.3 / Phase 24 — MEMORY.md → recense Migration Report (SCOPE-04, D-S7)

**Run:** 2026-06-18 (founder-authorized inline) · consolidate → verify → **retire PENDING founder sign-off**.
**Stack:** extractor + judge = `claude-headless` (Haiku/Sonnet on CC subscription); embedder = OpenAI `text-embedding-3-small`. Adapters disabled (`RECENSE_ENABLED_SOURCES=`) for both import and consolidate (D-06).

## Counts

| Item | Value |
|---|---|
| Fact files imported (managed) | **199** (193 from the 2026-06-16 run + **6 new** today, idempotent on stable `external_id`) |
| `memory-import` episodes consolidated | **199 / 199** (`consolidated=1`) |
| New episodes added this run | 6 (1949 → 1955 total; `memory-import` 193 → 199) |
| Policy bundles skipped (D-S5) | 7 (`voice_profile`, `feedback_no_inflated_metrics`, `feedback_drop_concentrations`, `outreach_framework`, `user_job_search_strategy`, `reference_linkedin_playbook`, `user_profile`) |
| MEMORY.md index files skipped | 12 |
| Source files modified/deleted by import | **0** (before/after mtime diff empty — D-08 verified) |

## Cost actually incurred

- **Extraction + reconsolidation judge:** headless `claude -p` on the **CC subscription** → **$0 marginal** (no API spend; was DeepSeek/paid in the old estimate).
- **Embeddings:** OpenAI `text-embedding-3-small` for the newly-consolidated nodes (6 new imports + ~8 ambient = 14 episodes' claims). At ~$0.02/1M tokens this is **single-digit cents (< $0.10)** — not the stale "~$1–2" estimate (that predated the subscription-judge move).
- Budget impact: negligible against ~$14–15 remaining.

## Clean-pass invariant (migration ran the FK-sensitive path)

- No `FOREIGN KEY constraint failed` in `/tmp/recense-sleep.log`; pass exited 0 ("Sleep pass complete").
- `PRAGMA foreign_key_check` on the live DB: **EMPTY** after consolidation.
- Backlog drained 14 → 2 (residual at/above-threshold or fresh ambient).

## Per-sample verification table (query → fact returned? → scope correct?)

Read-only ambient recall (no writes). D-09 bar: ≥3 facts per project across ≥3 projects.

| Project | Query | Fact returned (top) | Scope shown | Correct? |
|---|---|---|---|---|
| tonos | "tonos pricing model and apimesh domain" | Tonos pricing model (0.69) | `[tonos]` | ✓ |
| tonos | "tonos local db reference" | tonos database (0.81) | `[tonos]` | ✓ |
| tonos | "tonos audit process feedback" | tonos database (0.60) | `[tonos]` | ✓ |
| vtx | "vtx VTX score TPI SPI API components" | VTX = round((TPI+SPI+API)/3)… (0.76) | `[vtx]` | ✓ |
| vtx | (same) | Runs SPI→API→VTX only (0.73) | `[vtx]` | ✓ |
| vtx | (same) | VTX Index (0.62) | `[vtx]` | ✓ |
| putyouon | "putyouon urby wrangler deploy" | urby-wrangler-deploy-steps (0.78) | `[putyouon]` | ✓ |
| putyouon | (same) | urby-deployment-topology (0.61) | `[putyouon]` | ✓ |
| resume→global | "agentic engineering principles context engineer" | agentic engineering (0.88); Karpathy's Four Principles (0.73) | (no marker) | ✓ (global renders blank per D-S6) |

**Result:** ≥3 facts surface with correct `[scope]` for **tonos, vtx, and resume/global** (3 projects cleanly), plus putyouon (2). D-09 satisfied.

## Non-surfacing / attribution notes (honest)

- **No fact failed to surface** — every sampled query returned relevant migrated facts.
- **conway**: facts surface (e.g. `conway-prod`, `conway-agent`) but mostly carry **no marker / `[awesome-mpp]`**, not `[conway]`. This is not data loss — it's the D-S3 design: scope = the cwd a fact was *learned in*, not what it is *about*. Conway facts were consolidated from global/ambiguous-cwd episodes. Retrieval stays global (D-S1), so the facts are fully retrievable; only the attribution marker differs. Documented, not a blocker.
- The "scope = origin, not topic" property means some topically-project facts read as `[global]`; this is correct and expected for the pre-existing graph. Newly-imported per-project fact files carry their project slug (demonstrated above).

## Retirement status — EXECUTED 2026-06-18 (D-11/D-12, founder sign-off given)

Founder approved the move (resume session 2026-06-18) after a nav-layer safety verification (quick-260617-w0u). Executed:
- **MOVED** (never deleted) **197** migrated `~/.claude/projects/*/memory/*.md` **fact files** → `~/.claude/projects-memory-archive-2026-06-18/`, mirroring the source `<folder>/memory/` layout so reversal is a straight move-back. Zero errors.
- **LEFT IN PLACE**: all 12 `MEMORY.md` index files, all 7 policy bundles, **and 2 live trackers** (`interview_readiness_tracker.md`, `leetcode_practice_tracker.md`).
- Idempotent re-import remains safe after retirement (archived files leave the scan root; remaining files dedup on `external_id`).

### Why 197, not 199 (nav-layer safety finding)

The importer's skip-set (`import-memory-cli.ts` `POLICY_BUNDLES` + `MEMORY.md`) covers 7 policy bundles + 12 indexes, but **not** the 2 tracker files — so they were imported (part of the 199) and would have landed in the move set. They are **mutable state files that `leetcode-drill`/`leetcode-summary`/`interview-drill` skills and `session-stop-vault-update.sh` read AND rewrite every session**; moving them would dangle those live readers. Excluded from the move (kept in place). All other readers of the moved files are either the kept policy bundles or the 12 `MEMORY.md` indexes (whose `[title](file.md)` bullet links now dangle — cosmetic only; recense recall supersedes the flat index per the consolidate-into-recense thesis).

### Post-move verification

- Archive: **197** `.md` files. Source `memory/` dirs: 0 remaining move-candidates; keep-set (7 policy + 12 index + 2 trackers) intact.
- `recense.db` **untouched** by the move (mtime predates it — flat-file move never touches the engine). Direct DB query: **199/199** `memory-import` episodes consolidated; `node_scope` distribution correct per project (`[vtx]` 1090, `[tonos]` 139, `[putyouon]` 436, `[conway]` 359, …). Facts fully retrievable with correct scope.

### Minor follow-up (data quality) — DONE 2026-06-18 (quick-260617-w0u, founder: full undo)

The 2 trackers were imported as point-in-time **fact snapshots**. Investigation showed they
produced **71 nodes**, not 2 — 3 shared project entities (`tonos`/`VTX`/`Conway`, kept) + **68
tracker-only nodes** (stale counts, interview session-logs, leetcode problem/pattern entities,
lesson notes). Founder chose **full undo**.

- **Tombstoned all 68** tracker-only nodes (engine path: `UPDATE node SET tombstoned=1,
  training_eligible=0` + `DELETE FROM node_fts`, one transaction, hourly-agent lock held, DB
  backed up to `recense.db.bak-pre-tracker-tombstone-2026-06-18`). Non-destructive/reversible
  (rows retained). Verified: 68/68 tombstoned, 3 shared entities still live, `foreign_key_check`
  empty, tracker content gone from FTS (`Due now for tonos`/`Two Sum`/`NeetCode` → 0 hits).
- **Importer skip-set** (`import-memory-cli.ts`): added a dedicated `TRACKER_FILES` set +
  `'tracker'` `SkipReason` + `skippedTracker` count (kept separate from `POLICY_BUNDLES` so the
  "7 policy bundles" dry-run gate baseline stays clean). Future re-imports skip both trackers.
  Test added (`import-memory.test.ts`), build clean, 11/11 importer tests pass.
- Rationale for full undo: trackers are mutable append-logs; the leetcode-drill/-summary +
  interview-drill skills (which read/write the kept tracker files, and compound durable
  weaknesses into the vault) are the proper channel for those learnings to re-enter recense
  organically — a frozen import snapshot would never update and would duplicate future ingestion.
