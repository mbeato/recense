---
phase: 57-viz-activity-palette-redesign
plan: 07
type: execute
wave: 6
depends_on: [57-05, 57-06]
files_modified:
  - src/viz/modules/constants.js
  - src/viz/modules/effects.js
  - src/viz/modules/graph.js
  - tests/viz-activity-palette-invariants.test.ts
  - docs/superpowers/evidence/57-stage2-system/
autonomous: false
requirements: [VIZ-PAL-02, VIZ-PAL-06, VIZ-PAL-07, VIZ-PAL-05]

must_haves:
  truths:
    - "The founder has tuned the full system live — motion profiles + dim floors + bloom/exposure — over the real hull and approved it"
    - "Every provisional value is ratcheted to its founder-approved value; the D-12 invariants are tightened to the locked band/floors/ordering"
    - "Per-layer screenshots of each layer firing over the hull are captured as the durable approval record (D-15)"
    - "The full test suite is green with no honesty-guard regression"
  artifacts:
    - path: "docs/superpowers/evidence/57-stage2-system/"
      provides: "Per-layer firing screenshots — the durable Stage-2 approval record (D-15)"
    - path: "tests/viz-activity-palette-invariants.test.ts"
      provides: "Ratcheted (tightened) band/floor/ordering invariants locked to approved values (D-09)"
    - path: "src/viz/modules/constants.js"
      provides: "Founder-approved locked motion profiles + dim floors"
  key_links:
    - from: "tests/viz-activity-palette-invariants.test.ts"
      to: "src/viz/modules/constants.js"
      via: "invariant bounds tightened around approved values with a locked tolerance"
      pattern: "FLOOR_BOUND|Y_MIN"
---

<objective>
Stage-2 closing founder checkpoint (D-16): the founder tunes the whole system live — motion profiles, dim floors, bloom, and exposure — over the real hull; then ratchet every provisional value to its approved lock (D-09), tighten the D-12 invariants, capture per-layer evidence (D-15), and gate the full suite green.

Purpose: This is where the designed layer characters (live sharp+big, replay soft echo, spontaneous slow drift, twinkle micro-breathe) and the global glow/exposure get dialed to what actually reads right on the real install. The provisional numbers shipped across 57-04/57-05/57-06 become locked, ratcheted test values. The approved screenshots are the durable "what right looks like" record.

Output: locked/ratcheted constants + tightened invariants + Stage-2 screenshots; full 2558-test suite green; phase-complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/57-viz-activity-palette-redesign/57-CONTEXT.md
@.planning/phases/57-viz-activity-palette-redesign/57-04-SUMMARY.md
@.planning/phases/57-viz-activity-palette-redesign/57-05-SUMMARY.md
@.planning/phases/57-viz-activity-palette-redesign/57-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dist and stage the full system for live tuning</name>
  <read_first>
    - scripts/copy-viz-assets.cjs (postbuild copy)
    - src/viz/modules/constants.js (provisional motion profiles / floors)
    - src/viz/modules/effects.js, src/viz/modules/graph.js (provisional bloom/exposure)
    - package.json (build + viz launch)
  </read_first>
  <action>
    Run the full build (tsc + copy-viz-assets) so dist reflects the palette (57-03), motion profiles (57-04), bloom/exposure (57-05), and fade rewrite (57-06). Confirm dist/src/viz/modules/ carries the current constants.js/trace.js/effects.js. Launch recense viz. Prepare live triggering of each layer (live recall, replay echo, spontaneous, ingestion kinds, twinkle) and note where each tunable lives (motion profile consts in constants.js; bloom args in effects.js; exposure in graph.js) so the founder's adjustments can be applied precisely. Do not auto-tune — the founder dials at the checkpoint.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && node -e "const s=require('fs').readFileSync('dist/src/viz/modules/trace.js','utf8'); if(/ctx\.traceNodes\.clear\(\)/.test(s))throw new Error('dist trace.js stale'); console.log('dist current: fades scoped, system staged')"</automated>
  </verify>
  <done>dist reflects all prior 57 plans; the viz is launchable with each layer triggerable and each tunable located.</done>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `dist/src/viz/modules/trace.js` has no global `traceNodes.clear()` (proves 57-06 shipped into dist)
    - Written steps exist for triggering each layer and applying founder tweaks to the right file/const
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task: Stage-2 founder checkpoint — full-system live tune</name>
  <action>BLOCKING human checkpoint (autonomous: false) — the founder tunes the full system (motion profiles + dim floors + bloom + exposure) live over the real hull and supplies the approved numbers. The executor does NOT auto-tune; it presents the live render and waits for the resume signal, then Task 2 ratchet-locks the approved values. See <what-built>/<how-to-verify>/<resume-signal>.</action>
  <what-built>The full activity system — luminance-banded palette + per-layer motion profiles (attack/halo/pulse/cadence/density) + floored brightness + recalibrated global bloom/exposure — rendered live over the real hull, with own-trace-scoped fades. Ready for the founder's closing full-system tune.</what-built>
  <how-to-verify>
    1. Open the running recense viz.
    2. Trigger each layer and watch the salience ordering read through MOTION/SCALE: live = sharp attack + big halo (the event); replay = soft echo; spontaneous = slow calm drift; twinkle = micro-breathe. Confirm live > replay > spontaneous > twinkle reads clearly WITHOUT any layer vanishing.
    3. Force an idle-to-live transition while spontaneous is firing; confirm the live pathway is NOT clobbered mid-animation (WR-06 fix).
    4. Judge the global bloom/exposure: activation flares glow; resting tissue does not bloom or wash out.
    5. Dial any motion-profile value, dim floor, bloom arg, or exposure to taste — give the executor the exact approved numbers.
    6. Capture a screenshot of each layer firing over the hull for the durable record.
  </how-to-verify>
  <resume-signal>Reply "approved: [final numbers or 'as-is']" or list values to change. Real founder decision — not auto-approvable.</resume-signal>
  <verify><human-check>Founder has judged the full system live (salience ordering via motion/scale, WR-06 non-clobber, bloom/exposure) and replied with approved numbers or "as-is".</human-check></verify>
  <done>Founder has approved the full system (final numbers or "as-is" captured) with per-layer screenshots; Task 2 ratchet-lock can proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Ratchet all approved values + tighten D-12 invariants + store evidence (D-09/D-15)</name>
  <read_first>
    - src/viz/modules/constants.js (provisional motion profiles / floors to lock)
    - src/viz/modules/effects.js, src/viz/modules/graph.js (provisional bloom/exposure to lock)
    - tests/viz-activity-palette-invariants.test.ts (FLOOR_BOUND + ordering + band bounds to tighten)
    - .planning/phases/57-viz-activity-palette-redesign/57-CONTEXT.md (D-09 ratchet rule; tolerance is Claude's discretion)
  </read_first>
  <action>
    Apply the founder-approved numbers into constants.js (motion profiles, dim floors), effects.js (bloom args), and graph.js (exposure) — replacing PROVISIONAL values and updating each JSDoc/comment to "founder-approved at Stage-2 57-07". Ratchet the D-12 invariants (D-09): tighten FLOOR_BOUND to the approved floor (± documented tolerance), tighten the band bounds if the founder nudged hues, and confirm the monotonic attack/halo ordering still holds on the approved values — update the comments from "PROVISIONAL"/"locked at Stage-1" to "ratcheted at Stage-2 57-07 (± tolerance)". Save the founder's per-layer screenshots into docs/superpowers/evidence/57-stage2-system/ (one per layer). Re-run the full invariants file.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-activity-palette-invariants.test.ts 2>&1 | tail -15 && ls docs/superpowers/evidence/57-stage2-system/ | head</automated>
  </verify>
  <done>All approved values locked; invariants ratcheted+passing; Stage-2 per-layer screenshots stored.</done>
  <acceptance_criteria>
    - constants.js/effects.js/graph.js values match the founder-approved numbers; JSDoc records "founder-approved at Stage-2 57-07"
    - FLOOR_BOUND and band bounds are ratcheted with "Stage-2" comments; monotonic ordering + floor + band invariants pass
    - `docs/superpowers/evidence/57-stage2-system/` contains ≥1 screenshot per layer (live, replay, spontaneous, twinkle, and the ingestion kinds)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Full-suite gate + no-regression + tray-leak check</name>
  <read_first>
    - tests/viz-activity-palette-invariants.test.ts, tests/viz-ambient-liveliness.test.ts, tests/spontaneous-idle-activation.test.ts (viz invariant + honesty guards)
    - apps/tray/src/ (confirm no color assumptions to regress)
  </read_first>
  <action>
    Run the full build and full test suite. Confirm the 2558-test suite is green with no new failures and no regression of the Phase 52/54/55/56 honesty guards (honest-trace, ambient-liveliness, spontaneous-idle-activation). Run a source-guard grep confirming the tray consumes no activity colors (no KIND_COLOR / REPLAY_ / SPONT_ / recall_hop / 6-digit hex color literals in apps/tray/src) so the palette redesign cannot have leaked a color assumption there. Do a final dist rebuild so the shipped install matches the locked source.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && npm test 2>&1 | tail -15</automated>
  </verify>
  <done>Full suite green; no honesty-guard regression; tray confirmed color-free; dist matches locked source.</done>
  <acceptance_criteria>
    - `npm test` passes with no new failures (2558-test suite green)
    - `grep -rEc "KIND_COLOR|REPLAY_|SPONT_|recall_hop|0x[0-9a-fA-F]{6}" apps/tray/src/` returns 0 across files (no tray color leak)
    - `npm run build` exits 0 and dist reflects the locked source
    - Phase 52/54/55/56 honesty guard tests all pass (no regression)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder ↔ local viz install | manual full-system tune on 127.0.0.1; no external surface |
| browser client ↔ localhost SSE | unchanged; render/tuning only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-09 | Repudiation | full-system approval | mitigate | Per-layer Stage-2 screenshots captured as durable approval evidence (D-15) |
| T-57-10 | Tampering | honesty-guard regression | mitigate | Task 3 gates the full suite + explicitly re-runs Phase 52/54/55/56 honesty guards; palette is presentation-only by contract |
| T-57-SC | Tampering | package installs | n/a | No installs (net-zero deps); no audit table required |
</threat_model>

<verification>
- Founder full-system approval captured with per-layer evidence
- All provisional values ratcheted+locked; D-12 invariants tightened and passing
- Full 2558-test suite green; honesty guards not regressed
- Tray confirmed free of activity-color assumptions
</verification>

<success_criteria>
- The full activity system is founder-approved live over the real hull
- Every locked value is ratcheted with a Stage-2 comment and enforced by the D-12 invariants
- Durable Stage-2 evidence exists; suite green; phase complete
</success_criteria>

<output>
Create `.planning/phases/57-viz-activity-palette-redesign/57-07-SUMMARY.md` when done
</output>
