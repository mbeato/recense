---
phase: 57-viz-activity-palette-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/viz/modules/constants.js
  - src/viz/server.ts
  - tests/viz-activity-palette-invariants.test.ts
autonomous: true
requirements: [VIZ-PAL-03, VIZ-PAL-05]

must_haves:
  truths:
    - "The viz server reads its scheduler cadences/counts from constants.js, not from its own hand-typed literals"
    - "There is exactly ONE authored declaration of each shared scheduler scalar in the codebase"
    - "A test fails if server.ts reintroduces an independent literal for any shared scheduler constant"
  artifacts:
    - path: "src/viz/modules/constants.js"
      provides: "Single authored home for all scheduler scalars (REPLAY_* + SPONT_*), each a JSDoc'd named export const"
      contains: "export const SPONT_SEED_COUNT"
    - path: "src/viz/server.ts"
      provides: "Sync source-parse of scheduler scalars from constants.js at startVizServer init; hand-mirrored const block removed"
    - path: "tests/viz-activity-palette-invariants.test.ts"
      provides: "Dedicated D-12 invariants file, seeded with the shared-source-sync lock"
      contains: "shared-source"
  key_links:
    - from: "src/viz/server.ts"
      to: "src/viz/modules/constants.js"
      via: "fs.readFileSync + export-const regex at startVizServer init"
      pattern: "readFileSync.*constants\\.js"
    - from: "tests/viz-activity-palette-invariants.test.ts"
      to: "src/viz/server.ts"
      via: "source-text assertion that no duplicate scheduler literal is re-declared"
      pattern: "REPLAY_CADENCE_MS"
---

<objective>
Establish the single shared source of truth for the viz scheduler constants (D-10), structurally killing the WR-01 client/server mirror-drift class, and create the dedicated invariants test file (D-12) that every later plan in this phase extends.

Purpose: WR-01 happened because `server.ts` hand-copies `constants.js` scheduler values behind a "keep in sync" comment with no compiler/test enforcement — `SPONT_HOP_TOPN` already drifted (6 vs 3). This plan removes the second literal entirely so drift becomes structurally impossible, and stands up the one test file that owns all Phase-57 palette/motion locks.

Output: `constants.js` becomes the sole authored home of every scheduler scalar the server needs; `server.ts` derives them at init; a new `tests/viz-activity-palette-invariants.test.ts` locks the anti-duplication rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-viz-activity-palette-redesign/57-CONTEXT.md
@.planning/phases/57-viz-activity-palette-redesign/57-PATTERNS.md
@.planning/phases/56-spontaneous-1-hop-idle-activation/56-REVIEW.md

<interfaces>
<!-- Shared scheduler scalars today (two literal homes — the drift bug WR-01). -->
constants.js exports (client home, ESM, JSDoc'd):
  REPLAY_IDLE_GAP_MS = 5000, REPLAY_CADENCE_MS = 2500, REPLAY_HISTORY_N = 40,
  SPONT_CADENCE_MS = 2500, SPONT_HOP_TOPN = 3   (plus client-only SPONT_DIM, SPONT_PULSE_SCALE)

server.ts hand-mirrored block (lines 65-77 — the literals to DELETE):
  REPLAY_IDLE_GAP_MS, REPLAY_CADENCE_MS, REPLAY_HISTORY_N,
  SPONT_CADENCE_MS, SPONT_SEED_COUNT = 2, SPONT_HOP_TOPN, SPONT_POOL_REFRESH_MS = 60000

server.ts consumers: setInterval(replay, REPLAY_CADENCE_MS) ~492; setInterval(spont, SPONT_CADENCE_MS) ~555;
  pickSpontaneousSeeds(pool, SPONT_SEED_COUNT, Math.random) ~560; startVizServer is a SYNCHRONOUS export function (~209).

Invariants-test source-parse convention (tests/viz-ambient-liveliness.test.ts:105-122):
  readFileSync(ROOT + 'src/viz/modules/constants.js','utf8'); stripComments(src) filters /^\s*(\*|\/\/)/ lines;
  value regex: /export\s+const\s+NAME\s*=\s*([\d.]+)/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make constants.js the sole authored home of every scheduler scalar</name>
  <read_first>
    - src/viz/modules/constants.js (lines 327-398 — the existing layer-constants blocks and JSDoc convention)
    - src/viz/server.ts (lines 65-77 — the hand-mirrored block being retired; lines 492-573 — the setInterval/pickSpontaneousSeeds consumers)
    - .planning/phases/57-viz-activity-palette-redesign/57-PATTERNS.md (constants.js named-tunable convention; server.ts mirror-drift anti-pattern)
  </read_first>
  <action>
    In constants.js, ensure the two server-only scalars that currently live ONLY in server.ts are added as JSDoc'd named `export const`s in the Phase-56 layer block (alongside the existing SPONT_* consts): `SPONT_SEED_COUNT = 2` (distinct live seeds sampled per tick; 56-05 founder tuning 3→2) and `SPONT_POOL_REFRESH_MS = 60000` (eligible-seed pool refresh interval). Do NOT change any existing numeric value — this is a relocation, not a retune. Update the inline comment on `REPLAY_CADENCE_MS`, `REPLAY_HISTORY_N`, `SPONT_CADENCE_MS`, `SPONT_HOP_TOPN` to drop the now-obsolete "keep in sync with server.ts — server is the authoritative copy" language and state instead that constants.js is now the single authored source consumed by the server via source-parse. Keep every value's tuning-history JSDoc intact (named-tunable convention).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/viz/modules/constants.js','utf8'); ['SPONT_SEED_COUNT','SPONT_POOL_REFRESH_MS','REPLAY_CADENCE_MS','REPLAY_HISTORY_N','SPONT_CADENCE_MS','SPONT_HOP_TOPN','REPLAY_IDLE_GAP_MS'].forEach(n=>{if(!new RegExp('export\\\\s+const\\\\s+'+n+'\\\\s*=').test(s))throw new Error('missing '+n)}); console.log('all scheduler scalars authored in constants.js')"</automated>
  </verify>
  <done>constants.js declares all seven scheduler scalars as JSDoc'd named exports with unchanged values; sync-with-server comments removed.</done>
  <acceptance_criteria>
    - `grep -c 'export const SPONT_SEED_COUNT' src/viz/modules/constants.js` returns 1
    - `grep -c 'export const SPONT_POOL_REFRESH_MS' src/viz/modules/constants.js` returns 1
    - The numeric values of REPLAY_IDLE_GAP_MS(5000), REPLAY_CADENCE_MS(2500), REPLAY_HISTORY_N(40), SPONT_CADENCE_MS(2500), SPONT_SEED_COUNT(2), SPONT_HOP_TOPN(3), SPONT_POOL_REFRESH_MS(60000) are unchanged from their pre-plan values
    - No occurrence of the phrase "server-side scheduler is the authoritative copy" remains in constants.js
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Server derives scheduler scalars from constants.js via sync source-parse (kill WR-01)</name>
  <read_first>
    - src/viz/server.ts (lines 38-77 import + mirrored block; 209 startVizServer signature; 492-573 scheduler consumers)
    - tests/viz-ambient-liveliness.test.ts (lines 105-122 — the readFileSync + stripComments + export-const regex helpers to mirror)
    - src/viz/modules/constants.js (updated Task 1 home of the scalars)
    - scripts/copy-viz-assets.cjs (confirms modules/ is copied to dist/src/viz/modules/ so __dirname resolution holds in dist)
  </read_first>
  <action>
    Delete the hand-mirrored `const` block at server.ts:65-77 (REPLAY_* and SPONT_* literals). Add a small sync helper at module scope (or top of startVizServer) that reads `constants.js` text once via `fs.readFileSync(path.join(__dirname, 'modules', 'constants.js'), 'utf8')` and extracts each needed scalar with the established `/export\s+const\s+NAME\s*=\s*([\d.]+)/` regex, parsing to Number and throwing a clear error if any name is absent (fail-fast, not silent-zero). Resolve the seven scalars (REPLAY_IDLE_GAP_MS, REPLAY_CADENCE_MS, REPLAY_HISTORY_N, SPONT_CADENCE_MS, SPONT_SEED_COUNT, SPONT_HOP_TOPN, SPONT_POOL_REFRESH_MS) into local const bindings used by the existing setInterval/pickSpontaneousSeeds call sites — those call sites keep their current shape (D-10 implies no scheduler restructuring). Because `startVizServer` is synchronous and `__dirname` resolves to `src/viz/` (dev) and `dist/src/viz/` (prod, per copy-viz-assets), the sync read works in both without making startup async. Rationale to record in a comment: constants.js is the single authored source; the server derives (never re-declares) these values, so the WR-01 mirror class cannot recur.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | head -5; echo "tsc exit ${PIPESTATUS[0]}"</automated>
  </verify>
  <done>server.ts contains no independent literal for any scheduler scalar; all seven are parsed from constants.js at init; tsc is clean.</done>
  <acceptance_criteria>
    - `grep -Ec 'const +(REPLAY_CADENCE_MS|REPLAY_HISTORY_N|REPLAY_IDLE_GAP_MS|SPONT_CADENCE_MS|SPONT_SEED_COUNT|SPONT_HOP_TOPN|SPONT_POOL_REFRESH_MS) *=' src/viz/server.ts` shows zero hardcoded-literal re-declarations (the names may appear only as destructured/derived bindings from the parse helper)
    - server.ts contains a `readFileSync` call resolving `modules/constants.js`
    - `npx tsc --noEmit` exits 0
    - Behavioral: launching `recense viz` (or the existing server integration test) still schedules replay at 2500ms and spontaneous at 2500ms — no runtime error at startup
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create the dedicated invariants file with the shared-source-sync lock (D-12 seed)</name>
  <read_first>
    - tests/viz-ambient-liveliness.test.ts (lines 1-12 header convention; 104-122 source-parse helpers; 482-489 the REPLAY_DIM lock pattern)
    - tests/spontaneous-idle-activation.test.ts (marker-slice static-guard pattern, ~181-200)
    - src/viz/server.ts (post-Task-2 state — reads scalars from constants.js)
    - src/viz/modules/constants.js (authored scalar home)
  </read_first>
  <behavior>
    - Test A (WR-01 anti-duplication lock): server.ts source contains NO line matching `const NAME =<number>` for any of the seven scheduler scalar names — proving the server no longer re-declares a literal.
    - Test B (single-source presence): each of the seven scalars is declared exactly once as `export const NAME =` in constants.js.
    - Test C (runtime sync): the value server.ts would parse for REPLAY_CADENCE_MS and SPONT_HOP_TOPN equals the value declared in constants.js (parse both source texts with the shared regex, assert equal) — a red-if-drift guard replacing the never-enforced "keep in sync" comment.
  </behavior>
  <action>
    Create `tests/viz-activity-palette-invariants.test.ts` following the existing viz-test header convention (declare which files it mirrors: viz-ambient-liveliness.test.ts + spontaneous-idle-activation.test.ts). Reuse the `readFileSync`/`stripComments`/export-const-regex helpers. Implement Tests A/B/C above under a `describe('D-10 shared source of truth', ...)` block. Use `stripComments` before the anti-duplication assertion so a comment mentioning a name does not false-positive. This file is the home all later Phase-57 plans extend (luminance band, motion ordering, dim floors) — leave a clearly-commented section header for each future invariant group so the structure is obvious.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-activity-palette-invariants.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>The new invariants file passes; it fails if a scheduler literal is re-added to server.ts or if constants.js/server.ts scalar values diverge.</done>
  <acceptance_criteria>
    - `npx vitest run tests/viz-activity-palette-invariants.test.ts` reports all tests passing
    - Negative check (manual/ephemeral): temporarily re-adding `const REPLAY_CADENCE_MS = 9999;` to server.ts makes Test A fail (revert after confirming)
    - The file header names the two analog test files it mirrors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client ↔ localhost SSE (127.0.0.1) | existing read-only trace stream; unchanged by this plan |
| server ↔ local filesystem (constants.js) | new: server reads a repo-local, developer-authored JS file at init |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | server source-parse of constants.js | accept | File is a repo-controlled build artifact copied by copy-viz-assets; not user-writable at runtime; parse is fail-fast (throws on missing name) so corruption surfaces immediately, never silently defaults |
| T-57-02 | Denial of Service | startVizServer sync file read | accept | One-time small local read at init; no per-request I/O added; localhost-only server |
| T-57-SC | Tampering | package installs | n/a | This phase installs NO packages (net-zero deps); no npm/pip/cargo audit table required |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx vitest run tests/viz-activity-palette-invariants.test.ts` green
- Full existing suite (`npm test`) stays green (2558 tests) — no regression from the scheduler-source relocation
</verification>

<success_criteria>
- Exactly one authored declaration exists for each scheduler scalar (in constants.js)
- server.ts derives all seven scalars from constants.js with no independent literal
- The dedicated invariants file exists and locks the WR-01 anti-duplication rule
</success_criteria>

<output>
Create `.planning/phases/57-viz-activity-palette-redesign/57-01-SUMMARY.md` when done
</output>
