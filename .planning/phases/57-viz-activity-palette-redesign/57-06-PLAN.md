---
phase: 57-viz-activity-palette-redesign
plan: 06
type: execute
wave: 5
depends_on: [57-04]
files_modified:
  - src/viz/modules/trace.js
  - tests/viz-ambient-liveliness.test.ts
autonomous: true
requirements: [VIZ-PAL-02, VIZ-PAL-04]

must_haves:
  truths:
    - "Each activity layer renders with its own motion profile (attack, halo/scale, pulse thickness) — salience comes from motion/scale, not hue-brightness"
    - "Each trace fade deletes ONLY the node ids that trace added — a concurrent trace of a different kind is never clobbered"
    - "All three fade branches (live, replay, spontaneous) are own-trace-scoped"
    - "The live layer's rendered behavior is unchanged (pixel-equivalent re-home)"
  artifacts:
    - path: "src/viz/modules/trace.js"
      provides: "Per-layer motion-profile consumption + own-trace-scoped fades in all three branches (WR-06/D-11)"
      contains: "addedIds"
  key_links:
    - from: "src/viz/modules/trace.js"
      to: "src/viz/modules/constants.js"
      via: "each branch reads its layer's attack/halo/pulse profile consts"
      pattern: "ATTACK|HALO"
    - from: "trace.js fade timeouts"
      to: "ctx.traceNodes"
      via: "delete(id) per added id instead of clear()"
      pattern: "traceNodes\\.delete"
---

<objective>
Rewrite trace.js to consume the four per-layer motion profiles (D-06) so salience reads from motion/scale, and fix WR-06 (D-11) by making all three fade branches own-trace-scoped — deleting only the ids each trace added instead of globally clearing the shared sets.

Purpose: WR-06 is a live race: spontaneous fires every 2.5s during idle, a live recall is exactly the event that ends idle, and a live trace arriving inside a pending spontaneous fade window has its just-added traceNodes wiped by the spontaneous timeout's global `.clear()`. Phase 56 raised the collision frequency (every idle→live transition now races a pending spontaneous fade). This plan replaces the three global clears with scoped deletion and wires the motion profiles the founder tunes at Stage 2.

Output: trace.js consumes motion profiles for all four layers; all three fade branches are own-trace-scoped; live behavior preserved; behavioral tests updated to the new constant names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/57-viz-activity-palette-redesign/57-CONTEXT.md
@.planning/phases/57-viz-activity-palette-redesign/57-PATTERNS.md
@.planning/phases/56-spontaneous-1-hop-idle-activation/56-REVIEW.md

<interfaces>
<!-- The three fade branches to make own-trace-scoped (D-11). Each already builds a local pathNodes array. -->
Branch 1 replay (trace.js:692-697): fadeMs = DECAY_ATTACK_MS+DECAY_HOLD_MS+500; setTimeout → ctx.traceNodes.clear(); ctx.traceLinks.clear(); ctx.revealTrace(pathNodes,[])
Branch 2 spontaneous (trace.js:766-771): identical shape
Branch 3 live recall (trace.js:865-870): identical shape

<!-- WR-06 fix sketch (56-REVIEW.md:180-189) -->
const addedIds = pathNodes.map(n => n.id);
setTimeout(() => { addedIds.forEach(id => ctx.traceNodes.delete(id)); if (ctx.revealTrace) ctx.revealTrace(pathNodes, []); }, fadeMs);
// link ids: if a branch adds link keys to ctx.traceLinks, track them in a local set and delete those too — do NOT global-clear.

<!-- Motion profiles authored in 57-04 (constants.js). trace.js is the sole consumer (69 refs today). -->
Live: ACT_SCALE_GAIN/ACT_BRIGHTEN_GAIN/ACT_HAZE_LERP + decay envelope + live halo/pulse consts
Replay/Spont/Twinkle: per-layer attack ms, halo/scale, pulse thickness, dim (floored) consts

<!-- Behavioral tests that import layer consts (must stay green / update names) -->
viz-ambient-liveliness.test.ts:102 imports ACT_SCALE_GAIN, REPLAY_DIM, DECAY_ATTACK_MS; replay-peak tests assert peak = score*REPLAY_DIM
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consume per-layer motion profiles in all four branches (D-06/D-08)</name>
  <read_first>
    - src/viz/modules/trace.js (the live/replay/spontaneous branches + the twinkle/breathe path; the activate() intensity+color calls; imports from constants.js)
    - src/viz/modules/constants.js (the 57-04 motion-profile blocks — attack/halo/pulse/dim per layer)
    - tests/viz-ambient-liveliness.test.ts (behavioral replay-peak + live-amplification tests that lock current behavior)
  </read_first>
  <action>
    Update trace.js so each branch drives its render from its layer's motion-profile consts: attack sharpness (envelope attack ms), halo/scale (node scale multiplier), and pulse thickness (edge-pulse width) come from the per-layer profile, while brightness uses the floored dim factor as the bounded secondary cue (D-05). Live keeps its exact current behavior (D-08 pixel-equivalent — same ACT_*/DECAY_* values, now sourced from the live profile block). Replay/spontaneous/twinkle read their own attack/halo/pulse/dim consts instead of the old flat REPLAY_DIM/SPONT_DIM-only path, so subordination is expressed through motion/scale first. Do NOT change what is emitted/stored/retrieved — this is render wiring only. Where a behavioral test imported a renamed const, keep imports resolvable (57-04 preserved the live names; only add-ons are new). If any subordinate-layer behavioral assertion in viz-ambient-liveliness.test.ts now expects a different (floored) dim value, update that test's expected number to match the new floored constant (documenting the change as the D-05 floor, not a behavior regression).
  </action>
  <verify>
    <automated>npx vitest run tests/viz-ambient-liveliness.test.ts 2>&1 | tail -12 && npx tsc --noEmit 2>&1 | head -3</automated>
  </verify>
  <done>All four branches render from their motion profiles; live behavior unchanged; ambient-liveliness tests pass (dim expectations updated to floored values where needed).</done>
  <acceptance_criteria>
    - trace.js references the per-layer attack/halo/pulse consts for replay, spontaneous, and twinkle
    - Live-layer render path uses the same ACT_*/DECAY_* values as before (no live behavior change)
    - `npx vitest run tests/viz-ambient-liveliness.test.ts` passes; any changed expected-dim numbers are annotated as the D-05 floor
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Own-trace-scoped fades in all three branches (WR-06 / D-11)</name>
  <read_first>
    - src/viz/modules/trace.js (692-697 replay, 766-771 spontaneous, 865-870 live — the three global-clear timeouts; each branch's local pathNodes construction just above)
    - .planning/phases/56-spontaneous-1-hop-idle-activation/56-REVIEW.md (WR-06 finding + fix sketch, 168-189)
  </read_first>
  <action>
    In each of the three fade branches, replace the global `ctx.traceNodes.clear(); ctx.traceLinks.clear();` with own-trace-scoped deletion: build `const addedIds = pathNodes.map(n => n.id)` (each branch already computes pathNodes locally) and in the setTimeout delete only those ids from ctx.traceNodes; keep the `ctx.revealTrace(pathNodes, [])` delta-sync. For ctx.traceLinks: if a branch added link keys to the shared set, track them in a local set at add-time and delete only those in the timeout — never global-clear. Apply to ALL THREE branches (D-11 requires it — the phase rewrites fade mechanics and must not inherit the idle→live clobber race). Update each branch's comment to state the fade is now own-trace-scoped and why (concurrent traces of different kinds must not clobber each other).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/viz/modules/trace.js','utf8'); const clears=(s.match(/ctx\.traceNodes\.clear\(\)/g)||[]).length; const dels=(s.match(/ctx\.traceNodes\.delete\(/g)||[]).length; if(clears>0)throw new Error('global traceNodes.clear still present ('+clears+')'); if(dels<3)throw new Error('expected >=3 scoped delete sites, found '+dels); console.log('all three fades own-trace-scoped: '+dels+' delete sites, 0 global clears')"</automated>
  </verify>
  <done>Zero global `traceNodes.clear()` remain; all three branches delete only their own added ids.</done>
  <acceptance_criteria>
    - `grep -c 'ctx.traceNodes.clear()' src/viz/modules/trace.js` returns 0
    - `grep -c 'ctx.traceNodes.delete(' src/viz/modules/trace.js` returns ≥3 (one per branch)
    - Any traceLinks cleanup is scoped (no `ctx.traceLinks.clear()` global clear in a per-trace fade)
    - `npm test` shows no new failures; add or extend a test asserting a live trace's added node id survives a concurrent spontaneous fade timeout (behavioral WR-06 regression guard) OR a source-guard asserting no global clear in the fade timeouts
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client ↔ localhost SSE | unchanged; trace render/fade logic is client-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-07 | Tampering | concurrent-trace clobber (WR-06 race) | mitigate | Own-trace-scoped deletion in all three branches; verified by zero-global-clear grep + a concurrent-fade regression guard |
| T-57-08 | Denial of Service | leftover ids in shared sets | accept | Scoped deletion removes exactly the ids a trace added; revealTrace delta-sync unchanged; bounded per-trace pathNodes size |
| T-57-SC | Tampering | package installs | n/a | No installs (net-zero deps) |
</threat_model>

<verification>
- Zero global `traceNodes.clear()` in fade timeouts; ≥3 scoped delete sites (all branches)
- Live behavior pixel-equivalent; ambient-liveliness tests green
- `npx tsc --noEmit` clean; `npm test` no new failures (2558-test suite stays green)
- Honesty guards (Phase 52/54/55/56) not regressed — only render/fade wiring changed
</verification>

<success_criteria>
- All four layers render from motion profiles (salience from motion/scale)
- WR-06 fixed in all three branches (own-trace-scoped fades)
- Live layer unchanged
</success_criteria>

<output>
Create `.planning/phases/57-viz-activity-palette-redesign/57-06-SUMMARY.md` when done
</output>
