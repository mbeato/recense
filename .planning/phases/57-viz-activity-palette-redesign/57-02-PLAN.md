---
phase: 57-viz-activity-palette-redesign
plan: 02
type: execute
wave: 2
depends_on: [57-01]
files_modified:
  - src/viz/modules/constants.js
  - src/viz/modules/trace.js
  - tests/viz-activity-palette-invariants.test.ts
autonomous: true
requirements: [VIZ-PAL-01, VIZ-PAL-05]

must_haves:
  truths:
    - "Every activity kind (7 KIND_COLOR entries + a new replay identity hue) has a hand-picked identity hue whose computed luminance sits inside a named band"
    - "Replay renders in its OWN identity hue, not a bit-shift-dimmed copy of recall_hop"
    - "Oscillation no longer uses an amber-family hue (D-03b)"
    - "A test fails if any palette entry's computed luminance falls outside the provisional band"
  artifacts:
    - path: "src/viz/modules/constants.js"
      provides: "Luminance-equalized KIND_COLOR (8 identity hues incl. replay) + a documented luminance-band helper/bounds"
      contains: "KIND_COLOR"
    - path: "src/viz/modules/trace.js"
      provides: "Replay-hop color read directly from the replay identity hue (REPLAY_HOP_COLOR bit-shift construction removed)"
    - path: "tests/viz-activity-palette-invariants.test.ts"
      provides: "Luminance-band membership invariant across all 8 identity hues"
      contains: "luminance"
  key_links:
    - from: "src/viz/modules/trace.js"
      to: "src/viz/modules/constants.js"
      via: "KIND_COLORS pre-build reads KIND_COLOR.replay identity hue"
      pattern: "KIND_COLOR\\.replay"
    - from: "tests/viz-activity-palette-invariants.test.ts"
      to: "src/viz/modules/constants.js"
      via: "computes relative luminance of each hue and asserts band membership"
      pattern: "0\\.2126"
---

<objective>
Redesign every activity hue into a luminance-equalized identity palette (D-01/D-02/D-03/D-04) so hue carries identity and dimming never drives a layer below the perceptual floor, and give replay its own identity hue so it stops rendering as a dimmed copy of live cyan.

Purpose: The 56-05 lesson — saturated indigo (Y≈138) vanished; pastel lavender (Y≈193) survived — generalizes to the whole palette. Today reconsolidation (Y≈146), oscillation (Y≈141, and amber-family in violation of D-03b), and neutral (Y≈146) sit below a survivable band. This plan brings all eight identity hues into a machine-tested band and hands replay a first-class hue. This is the palette the founder signs off at the Stage-1 checkpoint (57-03) before any motion work builds on it.

Output: A coherent, luminance-banded palette in constants.js; replay hue wired through trace.js; a band-membership invariant added to the D-12 file. All values PROVISIONAL — ratcheted at Stage 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-viz-activity-palette-redesign/57-CONTEXT.md
@.planning/phases/57-viz-activity-palette-redesign/57-PATTERNS.md

<interfaces>
<!-- KIND_COLOR today (constants.js:148-173). Y = relative luminance, Rec.709 on 0-255 channels. -->
recall_seed 0xffb866 (Y≈193, in band), recall_hop 0x66d9ff (Y≈195, in band),
new_node 0x8fbf9e (Y≈178, in band), spontaneous 0xc9b8ff (Y≈193, in band),
reconsolidation 0xc481a4 (Y≈146, BELOW band), oscillation 0xc9824e (Y≈141, BELOW + amber-family, D-03b violation),
neutral 0x8a93a6 (Y≈146, BELOW band).
HOT amber 0xffb866 (Y≈193) is the live-identity anchor; amber reserved for live/hover ONLY (D-04).

<!-- trace.js:236-275 — the construction to restructure -->
KIND_COLORS pre-build: new THREE.Color(KIND_COLOR.<key>) per kind.
REPLAY_HOP_COLOR: bit-shift dim of recall_hop by REPLAY_DIM (lines ~256-263) — REMOVE, replace with KIND_COLOR.replay read.
SPONT_HOP_COLOR (lines ~264-275) is the forward template: identity hue × own pulse-scale, not borrowed hue × dim.
Consumers: replay branch spawnPulse(...REPLAY_HOP_COLOR) at ~688.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Luminance-band helper + invariant (write the band lock first)</name>
  <read_first>
    - tests/viz-activity-palette-invariants.test.ts (existing D-10 section from 57-01; extend it)
    - tests/viz-ambient-liveliness.test.ts (source-parse helpers to reuse)
    - src/viz/modules/constants.js (KIND_COLOR map at 148-173; the 56-05 pastel-lavender rationale comment)
  </read_first>
  <behavior>
    - Metric: relative luminance Y = 0.2126*R + 0.7152*G + 0.0722*B on 0-255 channels (Rec.709), documented inline as the chosen D-02 metric.
    - Provisional band: 170 ≤ Y ≤ 235 (lower bound = "pastel lavender Y≈193 survived, saturated indigo Y≈138 vanished, HOT amber Y≈193 anchor"; upper bound keeps hue identity below near-white). Documented as PROVISIONAL — ratcheted at Stage 1 (D-09).
    - Test: for each of the eight identity keys (recall_seed, recall_hop, new_node, reconsolidation, oscillation, neutral, spontaneous, replay), parse the hex from constants.js, compute Y, assert 170 ≤ Y ≤ 235.
  </behavior>
  <action>
    Add a `describe('D-02 luminance-band membership', ...)` section to the invariants file. Implement a small `relativeLuminance(hex)` helper in the test (unpack R/G/B, apply the Rec.709 weights). Parse each KIND_COLOR entry's hex out of constants.js source (regex on the `key: 0xNNNNNN` lines) and assert band membership against provisional bounds `Y_MIN=170`, `Y_MAX=235` declared as named test constants with a comment that they ratchet at Stage 1. Include `replay` in the key list even though the hue is added in Task 2 — this test is RED until Task 2 lands the in-band hues (expected: reconsolidation/oscillation/neutral fail until retuned, replay fails until added).
  </action>
  <verify>
    <automated>npx vitest run tests/viz-activity-palette-invariants.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>The band-membership test exists and correctly flags the below-band and missing hues (RED before Task 2).</done>
  <acceptance_criteria>
    - The test file contains `relativeLuminance` using the weights 0.2126 / 0.7152 / 0.0722
    - `Y_MIN` and `Y_MAX` are named constants with a "PROVISIONAL — ratchets at Stage 1" comment
    - Before Task 2, the band test reports failures for reconsolidation, oscillation, neutral, and replay (proving the lock is live)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Redesign the 8 identity hues into the band (D-01/02/03/04)</name>
  <read_first>
    - src/viz/modules/constants.js (KIND_COLOR 148-173; the 56-05 rationale comment as the per-hue documentation template; TYPE_COLOR/BG/HOT LOCKED at 84-106 — do not touch)
    - .planning/phases/57-viz-activity-palette-redesign/57-CONTEXT.md (D-01..D-04, roadmap starting hues)
    - tests/viz-activity-palette-invariants.test.ts (band bounds from Task 1)
  </read_first>
  <action>
    In constants.js KIND_COLOR: (a) add a new `replay` identity hue in the ice-cyan direction (roadmap starting direction; a cooler sibling of recall_hop cyan per D-04 family rule) with Y in [170,235]; (b) retune `reconsolidation`, `oscillation`, `neutral` to hand-picked hues with Y in band — oscillation MUST leave the amber family (D-03b: amber reserved for live only), pick a non-amber "unsettled" hue direction (documented); keep reconsolidation's rose-mauve IDENTITY but raise luminance into band; keep neutral non-amber slate identity but raise into band; (c) keep recall_seed (amber-gold, live identity), recall_hop (cyan), new_node (sage), spontaneous (lavender 0xc9b8ff, already landed) as-is unless a tiny nudge is needed to sit in band. Each new/changed hue gets a JSDoc comment in the 56-05 template style: state the hue IDENTITY, its computed Y, that it is PROVISIONAL (Stage-1 ratchet), and that subordination comes from motion/scale not the hue. Do NOT touch TYPE_COLOR, BG_COLOR, HOT, or the hull.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-activity-palette-invariants.test.ts -t "luminance-band" 2>&1 | tail -15</automated>
  </verify>
  <done>All eight identity hues (incl. replay) compute Y within [170,235]; oscillation is non-amber; locked hues untouched.</done>
  <acceptance_criteria>
    - The band-membership test passes for all eight keys
    - `grep -c 'replay:' src/viz/modules/constants.js` shows a replay entry inside KIND_COLOR
    - TYPE_COLOR (0x9c7080/0x6d7890/0x82698c), BG_COLOR (0x170f1d), HOT (0xffb866) are byte-for-byte unchanged
    - The oscillation hex is not in the amber family (its hue is not amber/orange — R≈G>B warm-orange signature removed); documented in its JSDoc
    - Each changed hue has a JSDoc noting identity + computed Y + PROVISIONAL/Stage-1-ratchet
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire replay identity hue through trace.js (remove REPLAY_HOP_COLOR bit-shift)</name>
  <read_first>
    - src/viz/modules/trace.js (lines 236-275 KIND_COLORS pre-build + REPLAY_HOP_COLOR construction; SPONT_HOP_COLOR as the template; line ~688 replay spawnPulse consumer)
    - src/viz/modules/constants.js (new replay hue from Task 2)
    - tests/viz-ambient-liveliness.test.ts (line ~348 "trace.js replay branch uses REPLAY_DIM constant" source-guard — note it may need updating if replay color no longer derives from REPLAY_DIM)
  </read_first>
  <action>
    In trace.js: add `replay: new THREE.Color(KIND_COLOR.replay)` to the KIND_COLORS pre-build map. Remove the `REPLAY_HOP_COLOR` bit-shift-dim-from-recall_hop construction (lines ~256-263) and its `_rh` temp. Update the replay branch pulse call (~688) to spawn the replay-hop pulse from `KIND_COLORS.replay` directly (identity hue), mirroring how the spontaneous branch reads `SPONT_HOP_COLOR` = identity × own pulse-scale — replay's line brightness now comes from its in-band identity hue, not from scaling live cyan. Note: replay NODE-activation intensity still uses `REPLAY_DIM` (unchanged in this plan; the dim floor is retuned in 57-04) — only the pulse COLOR construction changes here. Update the comment block to reflect that replay has its own identity hue (D-01) and no longer borrows/dims recall_hop.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/viz/modules/trace.js','utf8'); if(/REPLAY_HOP_COLOR/.test(s))throw new Error('REPLAY_HOP_COLOR bit-shift still present'); if(!/KIND_COLOR(S)?\.replay|replay:\s*new THREE\.Color\(KIND_COLOR\.replay\)/.test(s))throw new Error('replay identity hue not wired'); console.log('replay identity hue wired, bit-shift removed')"</automated>
  </verify>
  <done>trace.js reads replay's identity hue directly; the REPLAY_HOP_COLOR bit-shift construction is gone; behavior otherwise unchanged.</done>
  <acceptance_criteria>
    - `grep -c 'REPLAY_HOP_COLOR' src/viz/modules/trace.js` returns 0
    - trace.js KIND_COLORS map includes a `replay` THREE.Color built from `KIND_COLOR.replay`
    - `npx vitest run tests/viz-ambient-liveliness.test.ts` stays green (update the replay-color source-guard at ~348 if it asserted the old bit-shift; replay node-intensity REPLAY_DIM behavior unchanged)
    - `npx tsc --noEmit` clean and `npm test` shows no new failures
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client ↔ localhost SSE | unchanged; this plan only changes render colors client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-03 | Information Disclosure | palette hues | accept | Decorative chrome only; no data encoded in color; faithfulness clause — engine mechanisms untouched |
| T-57-SC | Tampering | package installs | n/a | No package installs (net-zero deps); no audit table required |
</threat_model>

<verification>
- `npx vitest run tests/viz-activity-palette-invariants.test.ts` green (all eight hues in band)
- `npx tsc --noEmit` clean; `npm test` no new failures (2558-test suite stays green)
- Honesty guard: no change to what is emitted/stored/retrieved — only KIND_COLOR hex values and trace.js color construction changed
</verification>

<success_criteria>
- Eight luminance-banded identity hues (incl. replay); oscillation off amber-family; locked palette untouched
- Replay renders its own identity hue; bit-shift construction removed
- Band-membership invariant live in the D-12 file
</success_criteria>

<output>
Create `.planning/phases/57-viz-activity-palette-redesign/57-02-SUMMARY.md` when done
</output>
