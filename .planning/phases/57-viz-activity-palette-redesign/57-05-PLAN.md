---
phase: 57-viz-activity-palette-redesign
plan: 05
type: execute
wave: 4
depends_on: [57-03]
files_modified:
  - src/viz/modules/effects.js
  - src/viz/modules/graph.js
autonomous: true
requirements: [VIZ-PAL-06]

must_haves:
  truths:
    - "The bloom composer and renderer tone-mapping/exposure expose provisional calibration values tuned against the real hull backdrop"
    - "There is still exactly ONE global composer (no second EffectComposer, no per-layer selective bloom)"
    - "Bg color, hull material, tissue palette, and additive trace blending are untouched"
  artifacts:
    - path: "src/viz/modules/effects.js"
      provides: "Provisional UnrealBloomPass strength/radius/threshold, named/commented for the Stage-2 tune"
      contains: "UnrealBloomPass"
    - path: "src/viz/modules/graph.js"
      provides: "Renderer tone-mapping/exposure calibration hook (if an explicit exposure setter is needed)"
  key_links:
    - from: "src/viz/modules/effects.js"
      to: "Graph.postProcessingComposer()"
      via: "single global composer append (RenderPass → UnrealBloomPass → OutputPass)"
      pattern: "postProcessingComposer"
---

<objective>
Set provisional global bloom + tone-mapping/exposure calibration (D-13) against the new luminance-banded palette, keeping a single global composer (D-14). This preps the Stage-2 calibration surface so the founder tunes glow/exposure alongside the motion profiles at the closing checkpoint.

Purpose: The new pastel/bioluminescent palette sits at a different luminance than the old saturated one, so the bloom threshold/strength and renderer exposure need re-calibrating so activation flares read without washing the resting tissue. This plan lands provisional values; the founder dials them live at Stage 2 (57-07). Global-only — per-layer selective bloom is rejected (D-14) as a second salience system.

Output: provisional bloom + exposure values in effects.js/graph.js, single composer preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/57-viz-activity-palette-redesign/57-CONTEXT.md
@.planning/phases/57-viz-activity-palette-redesign/57-PATTERNS.md

<interfaces>
<!-- effects.js:140-151 — the D-13 calibration surface -->
composer = Graph.postProcessingComposer()  // single composer — do NOT construct a second (Pitfall 2 / T-15-DBLCOMP)
UnrealBloomPass(Vector2, strength=0.7, radius=0.4, threshold=0.75)
composer.addPass(OutputPass)  // tone-mapping + sRGB; MUST be last
ctx.bloomPass = bloomPass  // read by stats.js (adaptive quality) — keep as a live UnrealBloomPass ref

<!-- graph.js:835 — Graph.renderer() is where THREE.WebGLRenderer.toneMappingExposure lives if an explicit setter is needed -->
LOCKED (do NOT touch): BG_COLOR 0x170f1d, hull material, TYPE_COLOR tissue palette, additive trace blending
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provisional bloom recalibration in effects.js (D-13/D-14)</name>
  <read_first>
    - src/viz/modules/effects.js (131-151 — the UnrealBloomPass + OutputPass block and the single-composer guard comment)
    - src/viz/modules/stats.js (lines ~78, ~167 — confirms ctx.bloomPass must stay a live UnrealBloomPass reference for adaptive quality)
    - .planning/phases/57-viz-activity-palette-redesign/57-CONTEXT.md (D-13/D-14)
  </read_first>
  <action>
    In effects.js, set provisional UnrealBloomPass strength/radius/threshold recalibrated for the new higher-luminance identity palette (the palette now sits nearer the bloom gate, so threshold likely rises and/or strength eases so resting tissue does not bloom while activation flares still breach the gate). Keep the three args as clearly-commented inline values recording they are PROVISIONAL — founder-tuned at Stage 2 (57-07). Do NOT construct a second EffectComposer (respect the existing Pitfall-2 / T-15-DBLCOMP guard) and do NOT add per-layer/selective bloom (D-14). Keep `ctx.bloomPass` assigned to the single UnrealBloomPass instance so stats.js adaptive-quality resize/toggle still works. Update the stale threshold comment ("HOT = 0xffe08a, luminance ≈ 0.88") to reference the new live-identity hue luminance so the reasoning stays accurate.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/viz/modules/effects.js','utf8'); const c=(s.match(/postProcessingComposer\(/g)||[]).length; const e=(s.match(/new EffectComposer/g)||[]).length; if(e>0)throw new Error('second EffectComposer introduced'); if(!/UnrealBloomPass/.test(s))throw new Error('bloom pass missing'); if(!/ctx\.bloomPass\s*=/.test(s))throw new Error('ctx.bloomPass ref lost'); console.log('single global composer preserved, bloom recalibrated')"</automated>
  </verify>
  <done>Provisional bloom values set; single global composer preserved; ctx.bloomPass intact; no selective bloom.</done>
  <acceptance_criteria>
    - `grep -c 'new EffectComposer' src/viz/modules/effects.js` returns 0
    - UnrealBloomPass strength/radius/threshold are present with PROVISIONAL/Stage-2 comments
    - `ctx.bloomPass =` assignment retained
    - No per-layer/selective bloom code added
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Renderer tone-mapping/exposure hook (D-13, if needed)</name>
  <read_first>
    - src/viz/modules/graph.js (line ~835 Graph.renderer() and surrounding renderer setup)
    - src/viz/modules/effects.js (OutputPass ordering — tone mapping applied last)
    - .planning/phases/57-viz-activity-palette-redesign/57-CONTEXT.md (D-13 — exposure/tone-mapping is in the calibration surface)
  </read_first>
  <action>
    Locate the renderer construction in graph.js (Graph.renderer()). If an explicit tone-mapping/exposure setter is warranted for calibrating the new palette against the hull (e.g. setting `renderer.toneMappingExposure` and/or the tone-mapping operator), add it as a provisional, clearly-commented value marked "PROVISIONAL — founder-tuned at Stage 2 (57-07)". If the OutputPass default tone-mapping is sufficient and no explicit exposure setter is needed, add a short comment at the renderer site documenting that exposure is left at default and why (so the Stage-2 tuner knows the knob exists and where it is). Do NOT change BG_COLOR, hull material, tissue palette, or trace blending modes.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/viz/modules/graph.js','utf8'); if(!/toneMapping|Exposure|exposure/.test(s))throw new Error('no tone-mapping/exposure calibration note or setter present'); console.log('exposure calibration surface addressed')"</automated>
  </verify>
  <done>Renderer exposure/tone-mapping is either provisionally set or explicitly documented as default with the knob located for Stage 2.</done>
  <acceptance_criteria>
    - graph.js contains either a provisional `toneMappingExposure`/tone-mapping setter (commented PROVISIONAL/Stage-2) OR a comment at the renderer site documenting the default-exposure decision and the knob location
    - BG_COLOR, hull material, tissue palette, trace blending unchanged
    - `npx tsc --noEmit` clean (graph.js is ESM but referenced by the build) and `npm test` no new failures
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client render pipeline | local WebGL post-processing only; no data boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-06 | Tampering | double-composer / selective bloom regression | mitigate | Task-1 verify greps for a second EffectComposer (0) and preserves the single-composer guard; D-14 held |
| T-57-SC | Tampering | package installs | n/a | No installs (net-zero deps) |
</threat_model>

<verification>
- Single global composer preserved; no selective bloom (D-14)
- Exposure/tone-mapping calibration surface set or documented (D-13)
- Locked surfaces (bg/hull/tissue/blending) untouched
- `npx tsc --noEmit` clean; `npm test` no new failures
</verification>

<success_criteria>
- Provisional bloom + exposure values ready for the Stage-2 founder tune
- D-14 single-global-composer invariant intact
</success_criteria>

<output>
Create `.planning/phases/57-viz-activity-palette-redesign/57-05-SUMMARY.md` when done
</output>
