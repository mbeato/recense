---
phase: 39.1-corpus-quality-project-hub-and-subject-docs-via-zero-interve
plan: 03
type: execute
wave: 3
depends_on: ["39.1-01", "39.1-02"]
files_modified:
  - src/consolidation/corpus-generator.ts
  - src/consolidation/run-sleep-pass.ts
  - tests/corpus-budget-cap.test.ts
  - tests/corpus-generator.test.ts
autonomous: true
requirements: [D-05, D-07]

must_haves:
  truths:
    - "Covers decisions D-05, D-07 (two-stage cost model orchestration: gated generation wired into the sleep pass; per-pass budget cap + self-draining priority queue extending RECENSE_CORPUS_GEN_MAX)"
    - "The sleep pass runs subject promotion AFTER consolidate() and BEFORE the generation loop"
    - "Empty hub and subject stubs are filled by the generation loop using the new generators"
    - "The hub generator receives a LINKED subject index ({name, docId}) built from the hub's doc_containment children, never bare slug strings"
    - "Subject generation rebuilds its schemaIds from the subject-schema-ids meta key (no extra LLM call)"
    - "Per-pass generation is capped; subjects beyond the cap are deferred to the next pass via a self-draining marker queue prioritized by mass/staleness"
  artifacts:
    - path: "src/consolidation/corpus-generator.ts"
      provides: "hub + subject dispatch in the generation loop; budget cap with deferred-marker writes"
      contains: "generateDocForSubject"
    - path: "src/consolidation/run-sleep-pass.ts"
      provides: "exhaust-gate wiring (promoteSubjects) + deferred-subject-gen queue drain"
      contains: "promoteSubjects"
  key_links:
    - from: "run-sleep-pass.ts CORPUS-06 block"
      to: "corpusPromoter.promoteSubjects(scope)"
      via: "call after consolidate(), before generateCorpusDocs"
      pattern: "promoteSubjects"
    - from: "corpus-generator.ts hub dispatch"
      to: "hub's doc_containment children → {name, docId}[]"
      via: "fetch child doc nodes' slug+id, build subjectDocs array for generateDocForHub"
      pattern: "doc_containment"
    - from: "corpus-generator.ts subject dispatch"
      to: "subject-schema-ids:<slug> meta key"
      via: "store.getMeta + JSON.parse to supply schemaIds (no LLM)"
      pattern: "subject-schema-ids:"
    - from: "corpus-generator.ts generation loop"
      to: "generateDocForHub / generateDocForSubject"
      via: "slug-shape dispatch (scope hub vs scope:name subject)"
      pattern: "generateDocForSubject"
    - from: "budget overflow"
      to: "pending-subject-doc-gen:<scope>:<slug> meta marker"
      via: "setMeta on defer, deleteMeta on success"
      pattern: "pending-subject-doc-gen"
---

<objective>
Wire the exhaust-gate into the sleep pass and teach the generation loop the new taxonomy. In run-sleep-pass.ts: after `consolidate()` (so schema induction is complete — Pitfall 6) and before `generateCorpusDocs`, call `corpusPromoter.promoteSubjects(scope)` inside the existing CORPUS-06 env-gated block, best-effort (non-fatal catch). In corpus-generator.ts: the generation loop dispatches empty stubs to `generateDocForHub` (bare scope slug, fed a LINKED `{name, docId}[]` index built from the hub's doc_containment children) or `generateDocForSubject` (scope:name slug, fed schemaIds rebuilt from the subject-schema-ids meta key) instead of only the schema-chapter path, and enforces the per-pass budget cap (D-07) — capping how many stubs are filled per pass and writing `pending-subject-doc-gen:<scope>:<slug>` markers for the overflow, prioritized by mass/staleness, drained next pass via the existing crash-safe marker pattern.

Purpose: D-05 Stage-2 integration (generation only when a gate opened) and D-07 (budget cap + self-draining priority queue). This is the wiring that makes the corpus stay fresh with zero user intervention.
Output: extended generation loop + sleep-pass wiring + a deferred-queue drain + the budget-cap test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-corpus-quality-project-hub-and-subject-docs-via-zero-interve/39.1-CONTEXT.md
@.planning/phases/39.1-corpus-quality-project-hub-and-subject-docs-via-zero-interve/39.1-RESEARCH.md
@.planning/phases/39.1-corpus-quality-project-hub-and-subject-docs-via-zero-interve/39.1-PATTERNS.md
@.planning/phases/39.1-corpus-quality-project-hub-and-subject-docs-via-zero-interve/39.1-01-SUMMARY.md
@.planning/phases/39.1-corpus-quality-project-hub-and-subject-docs-via-zero-interve/39.1-02-SUMMARY.md

<interfaces>
<!-- Read these contracts before writing. -->
<!-- generateCorpusDocs (corpus-generator.ts ~L93-241): stub query (length(value)=0), schema-chapter vs landing dispatch, -->
<!--   maxDocs slice + deferred count (~L152-154), per-doc failure isolation (~L225-233), idempotency contract. -->
<!-- run-sleep-pass.ts CORPUS-06 block (~L541-577): env gate RECENSE_CORPUS_GEN !== '0', consumePendingCorpusMarkers, -->
<!--   generateCorpusDocs(maxDocs from RECENSE_CORPUS_GEN_MAX default 25), best-effort try/catch. -->
<!-- consumePendingCorpusMarkers / marker scan+consume crash-safe pattern (~L289-319): setMeta/deleteMeta-on-success. -->
<!-- promoteSubjects(scope) from Plan 02 returns { proposed, created, refreshQueued, hubDocId, subjectDocIds }. -->
<!-- generateDocForHub(deps, scope, subjectDocs, opts) from Plan 01 takes subjectDocs: { name, docId }[] and emits -->
<!--   recense://doc/<docId> index refs (NOT bare slug strings). generateDocForSubject(deps, {scope, subjectName, schemaIds}, opts). -->
<!-- Plan 02 persisted each subject's schema set at store.getMeta('subject-schema-ids:<subjectSlug>') = JSON array. -->
<!-- doc_containment edges (kind='doc_containment') link the hub stub (slug=scope) to each subject stub (slug=scope:name). -->
<!-- cwdToScope() resolves the active scope in run-sleep-pass.ts. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/corpus-budget-cap.test.ts + extend the generation loop with hub + subject dispatch + budget-cap deferred markers</name>
  <read_first>
    - src/consolidation/corpus-generator.ts (the file being modified — read generateCorpusDocs IN FULL: the empty-stub query, the schema-chapter/landing dispatch, the maxDocs slice + deferred count, per-doc failure isolation)
    - src/reader/doc-generator.ts (generateDocForHub signature {name, docId}[], generateDocForSubject from Plan 01)
    - src/consolidation/corpus-promoter.ts (Plan 02 — the subject-schema-ids:<slug> meta key format and the hub→subject doc_containment edges)
    - src/consolidation/doc-writer.ts writeDoc (the write path each generated doc goes through; doc_link edges are written ONLY from recense://doc/<id> refs, L191-207 — the hub index must carry them)
    - tests/corpus-generator.test.ts (the harness model — copy its in-memory DB + mock provider setup for the new budget-cap test)
    - .planning/phases/39.1-corpus-quality-project-hub-and-subject-docs-via-zero-interve/39.1-PATTERNS.md (corpus-generator.ts section: loop dispatch, idempotency contract, per-doc failure isolation, budget-cap pattern; doc_link / recense://doc ref pattern)
    - .planning/phases/39.1-corpus-quality-project-hub-and-subject-docs-via-zero-interve/39.1-RESEARCH.md (Pattern 3/4; State of the Art — generateDocForSchema no longer called by the loop)
  </read_first>
  <action>
    In `generateCorpusDocs`: keep the existing empty-stub query (`length(n.value)=0`) and idempotency contract (only fills empty stubs). Add slug-shape dispatch. For a bare scope slug (no `:` and not a UUID) → resolve the hub's `doc_containment` child doc nodes, build a `subjectDocs: { name, docId }[]` array (name from each child's slug suffix after `scope:`, docId = each child node id), and call `generateDocForHub(scope, subjectDocs)` — pass the FULL `{name, docId}` array, NOT a bare-name/slug list, so the hub index renders `recense://doc/<docId>` refs and writeDoc materializes the doc_link edges (D-04 navigable index / BLOCKER-1). For a `scope:name` slug → derive scope + subjectName from the slug, then rebuild schemaIds by reading `store.getMeta('subject-schema-ids:<slug>')` and `JSON.parse`-ing it (NO second LLM call — BLOCKER-2 / D-05 constraint; if the meta key is missing, log and skip rather than calling an LLM), and call `generateDocForSubject(deps, { scope, subjectName, schemaIds })`. The sleep pass no longer fills schema-UUID chapter stubs via `generateDocForSchema` (D-03 demotion — RESEARCH State of the Art); leave the function importable but not called from the loop. Enforce the budget cap (D-07): sort the empty-stub list by priority (mass/staleness — hubs first, then subjects by descending drift/mass), `slice(0, maxDocs)`, and for every stub BEYOND the cap write a `pending-subject-doc-gen:<scope>:<slug>` meta marker (`store.setMeta(key, '1')`) so it is retried next pass. Each generated doc still goes through `writeDoc` and keeps per-doc failure isolation (log + count + continue). On successful generation of a previously-deferred stub, `store.deleteMeta` its marker. CREATE `tests/corpus-budget-cap.test.ts` (this task owns the file — it must exist before its verify runs): seed an in-memory DB with a hub stub + subject stubs joined by doc_containment edges + the subject-schema-ids meta keys, mock the provider, and assert the behaviors below.
  </action>
  <verify>
    <automated>npx vitest run tests/corpus-budget-cap.test.ts tests/corpus-generator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File creation: this task creates `tests/corpus-budget-cap.test.ts` (the file referenced by `<verify>` does not exist before this task's action).
    - Source assertion: `grep -n "generateDocForHub\|generateDocForSubject" src/consolidation/corpus-generator.ts` matches both.
    - Source assertion: `grep -n "subject-schema-ids:" src/consolidation/corpus-generator.ts` shows the meta read that rebuilds schemaIds.
    - Source assertion: `grep -n "pending-subject-doc-gen" src/consolidation/corpus-generator.ts` matches the marker key.
    - Source assertion: the loop no longer calls `generateDocForSchema` (grep returns 0 call sites in corpus-generator.ts).
    - Behavior assertion (BLOCKER-1 / WARNING-6): when the test DB is seeded with hub→subject `doc_containment` edges, the hub generator's `subjectDocs` argument received by the mock has `length > 0` and each entry carries both a `name` and a `docId` (asserts the hub never generates an empty index, and that linked refs — not bare slugs — are passed).
    - Behavior assertion (BLOCKER-2): a `scope:name` subject stub is generated with schemaIds parsed from `subject-schema-ids:<slug>` meta and ZERO additional provider.generate calls are issued to reconstruct the schema set.
    - Behavior assertion (D-07, tests/corpus-budget-cap.test.ts): with maxDocs=1 and 3 empty subject stubs, exactly 1 doc is generated and 2 `pending-subject-doc-gen:*` markers are written; a second pass (maxDocs=1) generates the next and clears one marker (self-draining).
    - Behavior assertion: a bare-scope empty stub is dispatched to the hub generator (mock receives a hub-shaped prompt); a `scope:name` stub goes to the subject generator.
  </acceptance_criteria>
  <done>tests/corpus-budget-cap.test.ts created; loop dispatches hub (with {name,docId} linked index) / subject (with meta-rebuilt schemaIds) by slug shape, no longer fills schema-UUID chapters, enforces budget cap with self-draining deferred markers; budget-cap + corpus-generator tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire promoteSubjects + deferred-queue drain into the sleep pass</name>
  <read_first>
    - src/consolidation/run-sleep-pass.ts (the file being modified — read the CORPUS-06 env-gated block ~L541-577 IN FULL, consumePendingCorpusMarkers ~L289-319, cwdToScope, and the runGraphHygiene best-effort posture)
    - src/consolidation/corpus-promoter.ts promoteSubjects (Plan 02)
    - src/consolidation/corpus-generator.ts (Task 1's deferred-marker writes)
    - .planning/phases/39.1-corpus-quality-project-hub-and-subject-docs-via-zero-interve/39.1-PATTERNS.md (run-sleep-pass.ts section: env-gate insertion point, pending-marker consume, best-effort catch; Shared Patterns: best-effort block, pending-marker crash-safe queue)
    - .planning/phases/39.1-corpus-quality-project-hub-and-subject-docs-via-zero-interve/39.1-RESEARCH.md (Pitfall 6 — tap runs AFTER consolidate(); Architectural Responsibility Map — budget cap lives in run-sleep-pass.ts)
  </read_first>
  <action>
    Inside the existing `if (env['RECENSE_CORPUS_GEN'] !== '0')` CORPUS-06 block, between `consumePendingCorpusMarkers(...)` and `generateCorpusDocs(...)`, add a best-effort call to `corpusPromoter.promoteSubjects(scope)` (scope from `cwdToScope()`), wrapped in its own try/catch that logs `EXHAUST-GATE: ...` and never rethrows (the sleep pass must complete — Shared Pattern best-effort). This runs AFTER `await consolidator.consolidate()` (already true at this point in the body, so schema induction for the pass is complete — Pitfall 6) and BEFORE `generateCorpusDocs` (so the new hub/subject stubs exist before the fill loop). Add a deferred-subject-gen queue drain that scans `meta WHERE key LIKE 'pending-subject-doc-gen:%'` and feeds those slugs into the same generation path next pass (reuse the consumePendingCorpusMarkers crash-safe pattern: deleteMeta ONLY on success). Read the env override `RECENSE_CORPUS_SUBJECT_DRIFT_THRESHOLD` (parseInt … ?? '3' || 3) and pass it into the promoter config if not already on config. Keep the existing `RECENSE_CORPUS_GEN_MAX` parse as the budget cap passed to generateCorpusDocs.
  </action>
  <verify>
    <automated>npx vitest run tests/corpus-budget-cap.test.ts && npx tsc --noEmit 2>&1 | grep -c "run-sleep-pass" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: in run-sleep-pass.ts, the `promoteSubjects` call appears textually AFTER the consolidate() call site and BEFORE the `generateCorpusDocs` call site (order verifiable by line numbers).
    - Source assertion: the promoteSubjects call is inside a try/catch that logs and does not rethrow (best-effort — `grep` shows the catch logs `EXHAUST-GATE` and no `throw` in the block).
    - Source assertion: `grep -n "pending-subject-doc-gen" src/consolidation/run-sleep-pass.ts` matches the drain scan.
    - Behavior assertion: a thrown error from promoteSubjects does not abort the pass (sleep-pass test or a unit harness confirms generateCorpusDocs still runs after a promoter throw).
    - `npx tsc --noEmit` clean for run-sleep-pass.ts.
  </acceptance_criteria>
  <done>promoteSubjects wired best-effort between consolidate() and generateCorpusDocs; deferred-subject-gen queue drained crash-safe; env knob honored; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sleep-pass orchestration → LLM generation | All doc-gen LLM cost must stay offline (online paths LLM-free) |
| budget overflow → meta marker queue | Uncapped generation = surprise subscription-token bill |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-08 | Denial of Service | unbounded per-pass generation | mitigate | RECENSE_CORPUS_GEN_MAX budget cap (D-07); overflow deferred to a self-draining marker queue, not generated this pass — bounds token spend on a big drift/ingest event |
| T-39.1-09 | Tampering | exhaust-gate aborting the sleep pass | mitigate | best-effort try/catch around promoteSubjects — a promoter failure logs and continues; consolidation + hygiene still complete |
| T-39.1-10 | Tampering | online path triggering generation | accept/verify | Generation + promotion run only inside the sleep-pass CORPUS-06 block (offline); no online route added — online paths stay LLM-free (engine invariant) |

No network/auth surface, no deletes. The material risks are a token-cost blowout (budget cap mitigates) and a doc-gen failure cascading into the consolidation path (best-effort isolation mitigates).
</threat_model>

<verification>
- `npx vitest run tests/corpus-budget-cap.test.ts tests/corpus-generator.test.ts` green; broader `npx vitest run tests/corpus-*.test.ts tests/doc-*.test.ts` green.
- `npx tsc --noEmit` clean.
- promoteSubjects call ordered after consolidate() and before generateCorpusDocs (line-number check).
- Hub generation receives subjectDocs {name, docId}[] (linked index); subject generation rebuilds schemaIds from meta with no extra LLM call.
</verification>

<success_criteria>
- Sleep pass calls promoteSubjects best-effort after consolidate() and before generateCorpusDocs.
- Generation loop dispatches hub (linked {name,docId} index) / subject (meta-rebuilt schemaIds) by slug shape; schema-UUID chapter fill removed from the loop.
- Per-pass budget cap enforced; overflow deferred via pending-subject-doc-gen markers; self-draining next pass.
- No online LLM path introduced; sleep pass survives a promoter throw.
- Budget-cap + corpus-generator tests green; tsc clean.
</success_criteria>

<output>
Create `.planning/phases/39.1-corpus-quality-project-hub-and-subject-docs-via-zero-interve/39.1-03-SUMMARY.md` when done.
</output>
