# Phase 39.1: Corpus Quality - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-22
**Phase:** 39.1-corpus-quality-project-hub-and-subject-docs-via-zero-interve
**Areas discussed:** Doc taxonomy, Exhaust-gate mechanism, Junk-doc cleanup, recense + vtx ingestion

---

## Doc Taxonomy

### Q1 — Mapping project-hub/subject onto today's landing + schema-chapter model

| Option | Description | Selected |
|--------|-------------|----------|
| Hub = landing, subjects replace chapters | Same 2-tier shape, better second tier | |
| New global hub above landings | New top-level index over all landings | |
| Full restructure | Abandon landing+schema-chapter; hub + topic-grouped subjects; schema clustering internal | ✓ |
| Let me describe it | — | |

**User's choice:** Full restructure

### Q2 — What defines a "subject"

| Option | Description | Selected |
|--------|-------------|----------|
| LLM names the subjects | Sleep-pass LLM proposes named subjects from content | ✓ |
| Schema clusters, merged + named | Keep clusters as signal, LLM merges/names | |
| Entity-anchored subjects | One doc per major entity | |
| Let me describe it | — | |

**User's choice:** LLM names the subjects

### Q3 — project-hub doc content

| Option | Description | Selected |
|--------|-------------|----------|
| Synthesized overview + index | Real LLM overview + linked index of subjects | ✓ |
| Thin index / TOC | Mostly navigation, minimal prose | |
| Overview only, backlinks do nav | Pure overview; nav via Phase-39 surfaces | |
| Let me describe it | — | |

**User's choice:** Synthesized overview + index

---

## Exhaust-gate mechanism

### Q1 — What is the "LLM exhaust"

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse sleep-pass LLM work | Docs piggyback on extract/judge byproduct, ~no extra calls | |
| Dedicated gen, gated cheaply | Own LLM pass, gated, background tail of sleep | |
| Both: cheap signal, gen on threshold | Free detection from existing calls → paid gen only when gate opens | ✓ |
| Let me describe it | — | |

**User's choice:** Both: cheap signal, gen on threshold

### Q2 — What opens the gate

| Option | Description | Selected |
|--------|-------------|----------|
| Create on mass, refresh on drift | Two gates; mirrors PE-gated update philosophy | ✓ |
| Mass threshold only | Generate once, regen on manual trigger | |
| Pure drift / staleness | Regen on any material change regardless of size | |
| Let me describe it | — | |

**User's choice:** Create on mass, refresh on drift

### Q3 — Per-pass cost bounding

| Option | Description | Selected |
|--------|-------------|----------|
| Per-pass budget cap + priority | Extend RECENSE_CORPUS_GEN_MAX; prioritize, defer overflow (self-draining queue) | ✓ |
| Hard cap, drop overflow | Simple cap, no priority | |
| No cap, trust the gate | Gate conservative enough; risk on bulk ingest | |
| Let me describe it | — | |

**User's choice:** Per-pass budget cap + priority

---

## Junk-doc cleanup

### Q1 — What counts as junk

| Option | Description | Selected |
|--------|-------------|----------|
| Obsolete taxonomy + structural junk | Old schema-UUID chapter docs + empty stubs + noise-schema docs; deterministic | ✓ |
| Structural junk only | Empties + orphans only | |
| Structural + LLM quality judge | Add LLM scoring of remaining docs | |
| Let me describe it | — | |

**User's choice:** Obsolete taxonomy + structural junk

### Q2 — Cleanup execution model

| Option | Description | Selected |
|--------|-------------|----------|
| Dry-run → snapshot → reviewed apply | Report → approve → VACUUM INTO snapshot → hard-delete | ✓ |
| Auto tombstone, reversible | Mark tombstoned, unattended, recoverable | |
| Snapshot + auto hard-delete | Snapshot then unattended delete, no report | |
| Let me describe it | — | |

**User's choice:** Dry-run → snapshot → reviewed apply

---

## recense + vtx ingestion

### Q1 — Source material

| Option | Description | Selected |
|--------|-------------|----------|
| Curated docs first, survey optional | High-signal human docs primary; survey secondary | |
| Full code survey (pipeline default) | ingest-project LLM repo survey on both | ✓ |
| Survey + docs combined | Both; richest but highest junk risk | |
| Let me describe it | — | |

**User's choice:** Full code survey (pipeline default)

### Q2 — Sequencing of the four threads

| Option | Description | Selected |
|--------|-------------|----------|
| Build → clean → ingest | Restructure+gate, then cleanup, then ingest as validation | ✓ |
| Clean → build → ingest | Clear junk first, then build | |
| Build+clean now, ingest deferred | Ship build+cleanup; ingest as follow-on phase | |
| Let me describe it | — | |

**User's choice:** Build → clean → ingest

---

## Claude's Discretion

- Scope slugs for ingestion (default: recense → `brain-memory`, vtx → `vtx`).
- Drift-measurement mechanics (how "enough new/changed atoms since last gen" is computed).
- Hub↔subject link surfacing in the reader (reuse Phase-39 index/backlinks vs duplicate).

## Deferred Ideas

- LLM doc-quality judge for cleanup (false-positive risk; revisit if structural cleanup insufficient).
- Clustering at 100+ docs (Phase 39 backlog — large-scale index nav, not doc quality).
- `promoteScope('tonos')` ingestion (future target after recense + vtx).
- Markdown export (deliberate divergence per Phase 39).
- Cross-value semantic entity merge (deliberately not built — false-merge risk, Phase 37).
