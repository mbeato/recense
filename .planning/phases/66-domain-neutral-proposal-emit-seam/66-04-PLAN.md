---
phase: 66-domain-neutral-proposal-emit-seam
plan: 04
type: execute
wave: 3
depends_on: ["66-01", "66-02"]
files_modified:
  - src/consolidation/consolidator.ts
  - src/consolidation/run-sleep-pass.ts
  - src/adapter/recense-doctor.ts
  - tests/action-proposal-emission.test.ts
  - tests/emission-hold-sentinel.test.ts
autonomous: true
requirements: [EMIT-01, EMIT-06]

must_haves:
  truths:
    - "A decisive belief update carrying a resolved entity and an intent classification emits exactly one proposal, inside the same transaction as the graph write it describes (D-05, D-06)"
    - "A held (non-decisive) contradiction emits no proposal under the REAL sink, not just under the interface-shaped identifier scan Phase 65 shipped (D-05, 65's D-13 promise)"
    - "An oscillation, confirm, extend, or unrelated outcome emits no proposal — the gate is `isEmissionEligible` imported from status-drift.ts, never re-derived (D-05)"
    - "A decisive outcome whose decision carries no resolved entity emits nothing rather than a partial or guessed proposal (D-05, Phase 64 D-07 all-or-nothing)"
    - "The emitted `evidence_quote` is the stored episode content byte-for-byte, taken from the row the sleep pass already read (D-12, EMIT-06)"
    - "An install with `actionProposalSinkEnabled` false gets `NoopActionProposalSink` from the live sleep pass and writes zero rows (D-04, EMIT-01)"
  artifacts:
    - path: "src/consolidation/consolidator.ts"
      provides: "proposalSink constructor param (Noop default), maybeEmitProposal helper, 9 gated call sites, episodeContent threading, EMIT-66 pass counter"
      contains: "maybeEmitProposal"
    - path: "src/consolidation/run-sleep-pass.ts"
      provides: "config-gated SQLiteActionProposalSink | NoopActionProposalSink injection"
      contains: "actionProposalSinkEnabled"
    - path: "src/adapter/recense-doctor.ts"
      provides: "dimension 10 — proposal sink posture + pending count"
      contains: "checkProposalSink"
    - path: "tests/action-proposal-emission.test.ts"
      provides: "eligibility matrix, all-or-nothing entity gate, verbatim quote, same-transaction atomicity, replay collapse through a real consolidate() pass"
    - path: "tests/emission-hold-sentinel.test.ts"
      provides: "65's D-13 sentinel extended to the real ActionProposalSink (the promise made when it shipped)"
  key_links:
    - from: "src/consolidation/consolidator.ts"
      to: "src/consolidation/status-drift.ts"
      via: "imported isEmissionEligible, never re-derived"
      pattern: "isEmissionEligible"
    - from: "src/consolidation/consolidator.ts"
      to: "src/consolidation/action-proposal-sink.ts"
      via: "this.proposalSink.emit inside applyDecision's decisive branches"
      pattern: "this\\.proposalSink\\.emit"
    - from: "src/consolidation/run-sleep-pass.ts"
      to: "config.actionProposalSinkEnabled"
      via: "ternary selecting SQLite vs Noop sink"
      pattern: "actionProposalSinkEnabled"
---

<objective>
Wire the proposal sink into the one place that may ever produce a proposal: `applyDecision`'s decisive branches. Inject it from the live sleep pass behind the dark knob, surface the posture in `recense doctor`, and discharge Phase 65's explicit promise by extending its shipped hold sentinel to the real sink.

Purpose: Phase 65 built and exported `EMISSION_ELIGIBLE_EVENT_TYPES`/`isEmissionEligible` specifically so this phase would gate on a guarded seam instead of inventing one, and shipped `tests/emission-hold-sentinel.test.ts` with a review-blocking header instructing Phase 66 to extend it rather than write a parallel test. This plan honours both.
Output: consolidator wiring, sleep-pass injection, doctor dimension 10, one new emission test file, one extended sentinel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-domain-neutral-proposal-emit-seam/66-CONTEXT.md
@.planning/phases/66-domain-neutral-proposal-emit-seam/66-PATTERNS.md
@.planning/phases/66-domain-neutral-proposal-emit-seam/66-01-SUMMARY.md
@.planning/phases/66-domain-neutral-proposal-emit-seam/66-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from live source — do not go exploring. -->

From src/consolidation/status-drift.ts lines 170-188 (IMPORT this; the header comment on it is
an instruction addressed to this phase):
```typescript
export const EMISSION_ELIGIBLE_EVENT_TYPES: ReadonlySet<ConsolidationEventType> = new Set([
  'contradict_reconcile',
  'contradict_append_new',
  'contradict_force_destabilize',
]);
export function isEmissionEligible(eventType: ConsolidationEventType): boolean {
  return EMISSION_ELIGIBLE_EVENT_TYPES.has(eventType);
}
```

From src/consolidation/action-proposal-sink.ts (shipped by 66-02):
```typescript
export const BELIEF_CHANGE_FIELD_STATUS = 'status';
export interface ActionProposalInput {
  kind: 'belief';
  entity_node_id: string; entity_descriptor: string; belief_node_id: string;
  change_field: string; change_from: string | null; change_to: string;
  evidence_episode: string; evidence_quote: string; confidence: IntentConfidence;
}
export interface ActionProposalSink { emit(proposal: ActionProposalInput): void; }
export class SQLiteActionProposalSink implements ActionProposalSink { constructor(store: ActionProposalStore, clock: Clock); }
export class NoopActionProposalSink implements ActionProposalSink {}
export class MockActionProposalSink implements ActionProposalSink { readonly proposals: ActionProposalInput[]; reset(): void; }
```

From src/consolidation/consolidator.ts lines 197-235 (the `ClaimDecision` fields this phase is
the first consumer of — note the JSDoc on `claimIntentStatus` literally says "Phase 66 persists
via action_proposal"):
```typescript
  claimIntentStatus?: IntentStatus;              // all-or-nothing with the next two (D-05)
  claimIntentEntity?: string;
  claimIntentConfidence?: IntentConfidence;
  claimResolvedEntityId?: string;                // all-or-nothing with the next (Phase 64 D-07)
  claimResolvedEntityDescriptor?: string;
```

From src/consolidation/consolidator.ts lines 326-344 (the constructor — the new param is one
more trailing `Type = new NoopType()` in the established style):
```typescript
  constructor(
    db, episodes, store, strength, retriever, provider, inducer, config,
    clock: Clock = realClock,
    sink: ConsolidationSink = new NoopConsolidationSink(),
    log: (msg: string) => void = () => {},
    deriver: SchemaRelationDeriver | NoopSchemaRelationDeriver = new NoopSchemaRelationDeriver(),
    corpusPromoter: CorpusPromoter | NoopCorpusPromoter = new NoopCorpusPromoter(),
    insightReflector: InsightReflector | NoopInsightReflector = new NoopInsightReflector(),
    docGraphDeriver: DocGraphDeriver | NoopDocGraphDeriver = new NoopDocGraphDeriver(),
  ) {
```

From src/consolidation/consolidator.ts line ~1140 (the ONLY applyDecision call site, inside the
per-episode `db.transaction(...).immediate()` — `episode` is in scope here):
```typescript
        this.db.transaction(() => {
          for (const decision of decisions) {
            this.applyDecision(decision, episodeId);
          }
          /* ...typed-edge upserts... */
          this.episodes.markConsolidated(episodeId);
        }).immediate();
```

From src/consolidation/run-sleep-pass.ts lines 534-602:
```typescript
  const eventStore = new EventStore(db);
  const sink = new SQLiteConsolidationSink(eventStore, realClock);
  /* ...inducer, deriver, corpusPromoter... */
  const consolidator = new Consolidator(
    db, episodes, store, strength, retriever, activeConsolidatorProvider, inducer,
    config, realClock, sink, log, deriver, corpusPromoter, insightReflector,
    docGraphDeriver,  // D-11/D-20 (Phase 39.2): sole doc-edge owner, runs after promote()
  );
```
`config` here is `loadMergedConfig(dbPath, env)` (line 483), so `config.actionProposalSinkEnabled`
is already resolved through settings.json overrides.

From src/adapter/recense-doctor.ts (the dimension convention):
```typescript
export interface CheckResult { ok: boolean; detail: string; }
function pass(detail: string): CheckResult { return { ok: true,  detail }; }
function fail(detail: string): CheckResult { return { ok: false, detail }; }
// ...
  const dimensions: DoctorDimension[] = [
    { name: 'DB',          result: checkDb(dbPath)           },
    /* ...8 more... */
    { name: 'claude CLI',  result: checkClaudeCli()          },
  ];
```
`checkDb` opens the DB with `{ readonly: true }` (T-09-10) — copy that discipline.
</interfaces>

<emission_site_map>
<!-- The complete, verified list of `this.sink.emit(...)` call sites inside applyDecision.
     66-PATTERNS.md lists six; the live source has TEN. Use this list, not that one. -->

| # | Locate by this existing anchor comment / context      | event_type literal              | belief node id var        | changeFrom argument      | gated? |
|---|-------------------------------------------------------|---------------------------------|---------------------------|--------------------------|--------|
| 1 | `// Always emit — records the confirm event for audit` | `'confirm'`                     | `decision.bestCandidateId`| `null`                   | yes    |
| 2 | `// SEAM-02 D-49: new node_id + bestCandidateId as candidate_id` | `'extend'`            | `newId_`                  | `null`                   | yes    |
| 3 | `// SEAM-02 D-49: defensive standalone counts as extend` | `'extend'`                    | `standaloneId`            | `null`                   | yes    |
| 4 | `// SEAM-02 D-49: standalone new node`                 | `'unrelated'`                   | `unrelatedId`             | `null`                   | yes    |
| 5 | `// SEAM-02 D-49: oscillation escalated from reconcile`| `'contradict_oscillation'`      | `oscId`                   | `node.value`             | yes    |
| 6 | `// SEAM-02 D-49: tombstone-and-replace`               | `'contradict_reconcile'`        | `reconciledId`            | `node.value`             | yes    |
| 7 | `// SEAM-02 D-49: extreme divergence`                  | `'contradict_append_new'`       | `appendNewId`             | `node.value`             | yes    |
| 8 | `// SEAM-02 D-49: force-destabilize (oscillation variant)` | `'contradict_force_destabilize'` | `fdOscId`            | `updatedNode.value`      | yes    |
| 9 | `// SEAM-02 D-49: N-distinct force-destabilize`        | `'contradict_force_destabilize'`| `fdId`                    | `updatedNode.value`      | yes    |
| 10| `// action === 'hold'` arm — `'contradict_hold'` emit  | `'contradict_hold'`             | —                         | —                        | **NO** |

Sites 1-4 and 5/8 pass the gate call but fail `isEmissionEligible` and return immediately — that
is deliberate (D-05: call the gate at every decisive site so a future new branch cannot silently
bypass it). Site 10 gets NO gate call at all: `tests/emission-hold-sentinel.test.ts` Half B scans
the hold arms for emission-shaped identifiers and is a shipped, review-blocking invariant.
`applySecondaryContradiction`'s emits get no gate call either — secondaries never mint, and D-05
scopes emission to `applyDecision`'s decisive branches.
</emission_site_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidator — proposalSink param, maybeEmitProposal helper, 9 gated call sites</name>
  <files>src/consolidation/consolidator.ts</files>
  <read_first>
    - src/consolidation/consolidator.ts lines 140-240 (`ClaimDecision`, especially the `claimIntent*` and `claimResolvedEntity*` JSDoc), 290-395 (instance counter fields + the constructor + field assignments), 1090-1160 (the resolution loop and the `db.transaction` block containing the single `applyDecision` call site), 1160-1200 (the end-of-pass `this.log(...)` counter lines this task adds one to), 1274-1630 (all of `applyDecision`, every `this.sink.emit` site)
    - src/consolidation/status-drift.ts lines 168-190 (the exported predicate and its Phase-66-addressed header comment)
    - src/consolidation/action-proposal-sink.ts (shipped by 66-02)
    - .planning/phases/66-domain-neutral-proposal-emit-seam/66-CONTEXT.md decisions D-05, D-06, D-12
  </read_first>
  <action>
    Import `isEmissionEligible` from `./status-drift` and `BELIEF_CHANGE_FIELD_STATUS`, `NoopActionProposalSink`, and the types `ActionProposalSink` from `./action-proposal-sink`. Do NOT re-declare, copy, or re-derive the eligible-event set anywhere in this file — D-05 is explicit, and the exported predicate's own doc comment names this phase.

    Add `private readonly proposalSink: ActionProposalSink;` beside the existing `sink` field, and add `proposalSink: ActionProposalSink = new NoopActionProposalSink(),` as one more trailing constructor parameter after `docGraphDeriver`, assigning it in the constructor body. Trailing position with a Noop default is required so the ~10 existing `new Consolidator(...)` call sites across tests keep compiling unchanged.

    Add `private proposalsEmitted = 0;` beside the `driftEvaluations`/`driftDamped` counter fields, reset it at the start of `consolidate()` alongside those, and emit one end-of-pass log line beside the existing `RESOLVE-64 attempts=...` line: `EMIT-66 proposals=${this.proposalsEmitted}`. This is the observability Phase 65's DRIFT-05 pass and any later calibration will read.

    Change `applyDecision(decision: ClaimDecision, episodeId: string)` to `applyDecision(decision: ClaimDecision, episodeId: string, episodeContent: string)`, and update the single call site inside the `db.transaction` block to `this.applyDecision(decision, episodeId, episode.content)`. `episode` is already in scope there (`const episodeId = episode.id;` a few lines above). Threading the content rather than re-reading the episode inside `applyDecision` is deliberate: it is the exact row the loop already holds — post strip-hidden, post redactSecrets, post `capContent` — which is precisely what D-12 defines "verbatim" against, and it adds zero DB reads inside the transaction.

    Add a private method immediately after `maybeWriteNodeTemporal`:
    `private maybeEmitProposal(eventType: ConsolidationEventType, decision: ClaimDecision, episodeId: string, episodeContent: string, beliefNodeId: string, changeFrom: string | null): void`
    Body, in this order:
      1. `if (!isEmissionEligible(eventType)) return;`
      2. Destructure `claimResolvedEntityId`, `claimResolvedEntityDescriptor`, `claimIntentStatus`, `claimIntentConfidence` from `decision`; if ANY is `undefined`, return. Comment: Phase 63's D-05 and Phase 64's D-07 both ship all-or-nothing field pairs, so a partial set means the classifier or the resolver abstained — the seam emits nothing rather than a guess (research Pitfall 4).
      3. `this.proposalSink.emit({ kind: 'belief', entity_node_id: claimResolvedEntityId, entity_descriptor: claimResolvedEntityDescriptor, belief_node_id: beliefNodeId, change_field: BELIEF_CHANGE_FIELD_STATUS, change_from: changeFrom, change_to: claimIntentStatus, evidence_episode: episodeId, evidence_quote: episodeContent, confidence: claimIntentConfidence });`
      4. `this.proposalsEmitted++;`
    Doc-comment the method with: D-06 (it is called synchronously inside the per-episode transaction already open around `applyDecision`, so the proposal row and the graph mutation it describes commit or roll back together — no `await` may ever be introduced here); D-05 (the gate is the imported predicate, never a local list); and the deliberate `change_from`/`change_to` asymmetry — `change_from` is recense's prior belief TEXT verbatim (human-readable, often a full sentence) while `change_to` is a token from recense's own closed `IntentStatus` vocabulary. A consumer maps on `change_to`; `change_from` exists so an approver sees what recense believed before. This asymmetry is intentional and must not be "normalised" by paraphrasing either side — paraphrase is exactly the confused-deputy surface Pitfall 2 forbids.

    Insert a `this.maybeEmitProposal(...)` call immediately after each of the NINE `this.sink.emit({...})` calls listed in the `<emission_site_map>` above, passing the event-type string literal that the adjacent `sink.emit` payload uses at that same site, plus the mapped belief-node-id variable and `changeFrom` argument. Do NOT add a call inside the `// action === 'hold'` arm, and do NOT add any call inside `applySecondaryContradiction`.

    Change nothing else in `applyDecision`: no reordering, no altered `sink.emit` payloads, no changes to routing, drift evaluation, oscillation handling, or the D-19 provenance gate.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/emission-hold-sentinel.test.ts tests/status-drift-wiring.test.ts tests/consolidation-intent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `grep -c 'this.maybeEmitProposal(' src/consolidation/consolidator.ts` returns exactly 10 (9 call sites + the method declaration line does not match this pattern, so confirm the true count is 9 calls by `grep -c '^\s*this\.maybeEmitProposal('`, which must return 9).
    - `grep -c 'this.proposalSink.emit(' src/consolidation/consolidator.ts` returns exactly 1 — all emission funnels through the single gated helper.
    - `grep -c "EMISSION_ELIGIBLE_EVENT_TYPES\|'contradict_reconcile',$" src/consolidation/consolidator.ts` shows no locally re-declared eligible set; specifically `grep -c 'new Set<ConsolidationEventType>' src/consolidation/consolidator.ts` returns 0.
    - `grep -c "isEmissionEligible" src/consolidation/consolidator.ts` returns at least 2 (import + use).
    - Extracting the `// action === 'hold'` balanced block and grepping it for `maybeEmitProposal` or `proposalSink` returns zero hits — verified by `npx vitest run tests/emission-hold-sentinel.test.ts` passing unchanged.
    - `grep -c 'maybeEmitProposal\|proposalSink' <(sed -n '/private applySecondaryContradiction/,/^  }/p' src/consolidation/consolidator.ts)` returns 0.
    - `grep -c 'await' <(sed -n '/private maybeEmitProposal/,/^  }/p' src/consolidation/consolidator.ts)` returns 0.
    - `npm test` shows no new failures versus the pre-plan baseline — the trailing Noop-default constructor param must leave every existing `new Consolidator(...)` call site compiling and behaving identically.
  </acceptance_criteria>
  <done>Emission is wired at exactly the nine decisive sites, funnels through one gated helper that imports the Phase 65 predicate, never touches the hold arm or secondaries, and the whole suite is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Sleep-pass injection behind the dark knob + doctor dimension 10</name>
  <files>src/consolidation/run-sleep-pass.ts, src/adapter/recense-doctor.ts</files>
  <read_first>
    - src/consolidation/run-sleep-pass.ts lines 475-490 (`loadMergedConfig` call producing `config`), 530-605 (`eventStore`/`sink` construction through the `new Consolidator(...)` argument list)
    - src/adapter/recense-doctor.ts lines 1-30 (header dimension list + threat-mitigation block), 60-120 (`CheckResult`, `pass`/`fail`, `checkDb`'s `{ readonly: true }` discipline), 340-400 (`checkGmailAccounts` — the most recent dimension added, and the closest analog), 530-560 (the `dimensions` array)
    - src/adapter/settings-loader.ts lines 100-140 (`loadMergedConfig` signature — doctor will call it to read the knob)
    - src/lib/config.ts (the `actionProposalSinkEnabled` knob added by 66-02)
  </read_first>
  <action>
    In `src/consolidation/run-sleep-pass.ts`, immediately after `const sink = new SQLiteConsolidationSink(eventStore, realClock);`, add the proposal sink selection:
    `const proposalSink: ActionProposalSink = config.actionProposalSinkEnabled ? new SQLiteActionProposalSink(new ActionProposalStore(db, realClock), realClock) : new NoopActionProposalSink();`
    with a comment naming EMIT-01 and D-04: the knob is the "a consumer is configured" signal, and with it off the live pass gets the same Noop the Consolidator constructor defaults to, so no `action_proposal` row is ever written. Add the imports for `ActionProposalSink`, `SQLiteActionProposalSink`, `NoopActionProposalSink` from `./action-proposal-sink` and `ActionProposalStore` from `../db/action-proposal-store`.

    Add `proposalSink,` as the final argument to the existing `new Consolidator(...)` call, after `docGraphDeriver`, with a trailing comment `// EMIT-01 (Phase 66): Noop unless actionProposalSinkEnabled`.

    In `src/adapter/recense-doctor.ts`, add a tenth dimension `checkProposalSink`:
    - Signature `export function checkProposalSink(dbPath: string, envPath: string = sleepEnvPath(), settingsPath?: string): CheckResult`, mirroring `checkGmailAccounts`'s exported-with-test-overrides convention.
    - Read the knob via `loadMergedConfig(dbPath, loadConfiguredEnv(envPath), ...)` — match whatever argument shape `loadMergedConfig` actually declares; if the settings path parameter is not optional in a way that composes cleanly, pass the default explicitly rather than duplicating the resolution logic.
    - When the knob is `false`: return `pass` with a detail stating the sink is Noop, no `action_proposal` rows are written, and naming the settings key to set (`overrides.actionProposalSinkEnabled`) to enable it. A dark knob being off is a posture, not a fault — this dimension must never `fail` on it.
    - When the knob is `true`: open the DB `{ readonly: true }` (T-09-10), read `SELECT COUNT(*) FROM action_proposal WHERE status = 'pending'`, and return `pass` with a detail reporting "sink enabled" and the pending count. Wrap the DB read so a missing table or unreadable DB degrades to a `pass` with a "sink enabled; proposal count unavailable" detail rather than throwing — `checkDb` already owns DB-reachability failure, and two dimensions failing for one cause is noise.
    - The detail string reports counts and posture only — never a proposal id, entity descriptor, change value, or evidence quote. Add a `T-66-05:` line to the file's threat-mitigation header block saying exactly that, and add the new dimension to the header's numbered list (update "9-dimension"/"nine" wording to ten).
    - Register `{ name: 'Proposal sink', result: checkProposalSink(dbPath) }` at the end of the `dimensions` array.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/recense-doctor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `npx vitest run tests/recense-doctor.test.ts tests/recense-doctor-gmail-accounts.test.ts` passes with no new failures.
    - `grep -c 'actionProposalSinkEnabled' src/consolidation/run-sleep-pass.ts` returns 1.
    - `grep -c 'proposalSink,' src/consolidation/run-sleep-pass.ts` returns 1 (the Consolidator argument).
    - `grep -c 'export function checkProposalSink' src/adapter/recense-doctor.ts` returns 1.
    - `grep -c "name: 'Proposal sink'" src/adapter/recense-doctor.ts` returns 1.
    - `grep -c 'T-66-05' src/adapter/recense-doctor.ts` returns 1.
    - `grep -ci 'nine' src/adapter/recense-doctor.ts` returns 0 (header wording updated to ten).
    - `checkProposalSink` never returns `fail` — verified by reading the function: it contains no call to the `fail(` helper.
    - `git diff src/consolidation/run-sleep-pass.ts` changes nothing except the two added imports, the `proposalSink` const, and the one added Consolidator argument.
  </acceptance_criteria>
  <done>The live sleep pass injects the SQLite sink only when the knob is on, defaults to Noop otherwise, and `recense doctor` reports the posture without ever failing on a dark knob.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emission behavioral tests + extend Phase 65's hold sentinel to the real sink</name>
  <files>tests/action-proposal-emission.test.ts, tests/emission-hold-sentinel.test.ts</files>
  <read_first>
    - tests/emission-hold-sentinel.test.ts (full file, 523 lines — the header's Phase-66 instructions at lines 4-15, `makeHarness`, `makeConsolidatorWithRealSink` at 140-147, the `MarkerProvider` content-keyed extraction/judge stub, the Half A behavioral assertions incl. the force-destabilize counterexample, and the Half B `FORBIDDEN_EMISSION_IDENTIFIERS` scan at 415-470)
    - src/consolidation/consolidator.ts (as modified by Task 1)
    - src/consolidation/action-proposal-sink.ts and src/db/action-proposal-store.ts
    - .planning/phases/66-domain-neutral-proposal-emit-seam/66-PATTERNS.md section "`tests/emission-hold-sentinel.test.ts` (test, event-driven — EXTEND not replace)"
  </read_first>
  <behavior>
    New file `tests/action-proposal-emission.test.ts`:
    - A real `consolidate()` pass driving a decisive contradiction (`contradict_force_destabilize` via three distinct-provenance contradictions, reusing the shipped counterexample recipe) on a decision carrying all four intent/resolved-entity fields writes exactly one `action_proposal` row.
    - That row's `evidence_quote` is byte-identical to the `episode.content` column of the row identified by its `evidence_episode`.
    - That row's `change_to` equals the decision's `claimIntentStatus`, `entity_node_id`/`entity_descriptor` equal the resolved-entity pair, `change_field` is `'status'`, `status` is `'pending'`, `schema_version` is the current `SCHEMA_VERSION`.
    - The same pass with `claimResolvedEntityId`/`claimResolvedEntityDescriptor` absent (resolver abstained) writes zero rows while still writing the `consolidation_event` row — the graph write happens, the proposal does not.
    - The same pass with the intent fields absent (non-gmail episode) writes zero rows.
    - A `confirm`, an `extend`, and an `unrelated` outcome each write zero rows even when all four fields are present.
    - An oscillation-escalated outcome writes zero rows even when all four fields are present.
    - Same-transaction atomicity: when the transaction wrapping `applyDecision` is made to throw after the emit, neither the `action_proposal` row nor the graph mutation persists. Drive this by making a later statement inside the same per-episode transaction fail (for example `markConsolidated` against a poisoned handle) rather than by stubbing the sink, so the property tested is the real transaction boundary.
    - Re-running `consolidate()` over an episode that produces the identical change (same entity node, field, from, to, episode) leaves exactly one row.
    - With `NoopActionProposalSink` injected, every one of the above decisive cases writes zero rows.

    Extended file `tests/emission-hold-sentinel.test.ts`:
    - `makeConsolidatorWithRealSink` also injects a real `SQLiteActionProposalSink` over a real `ActionProposalStore` on the same in-memory DB.
    - The existing "ambiguous low-confidence contradiction produces zero emission-eligible rows" case additionally asserts zero `action_proposal` rows.
    - The existing force-destabilize counterexample additionally asserts exactly one `action_proposal` row, keeping the negative assertions non-vacuous under the real sink.
    - Half B still passes unchanged against the modified `consolidator.ts`.
  </behavior>
  <action>
    Create `tests/action-proposal-emission.test.ts` covering every bullet in the first `<behavior>` group. Reuse the harness shape from `tests/emission-hold-sentinel.test.ts` — the in-memory DB, `FakeClock`, `MarkerProvider`-style content-keyed extraction/judge stub, and the three-distinct-provenance recipe that crosses the force-destabilization threshold — rather than inventing a second harness. Copy the `MarkerProvider` per that file's documented "each consumer keeps its own copy" convention; do not export it from the sentinel file.

    Read `action_proposal` rows out of SQLite directly (`SELECT * FROM action_proposal`) rather than through a mock, so the assertions exercise the real DDL, the real CHECK constraints, and the real `INSERT OR IGNORE`.

    Then extend `tests/emission-hold-sentinel.test.ts` in place — do not create a parallel file, and do not weaken any existing assertion. Concretely:
    - Widen `makeConsolidatorWithRealSink` to construct a real `ActionProposalStore` and `SQLiteActionProposalSink` over `h.db` and pass it as the new trailing constructor argument.
    - Add an `actionProposalRowsForEntity(h, entityNodeId)` helper beside `consolidationEventRowsForCandidate`, following its documented style.
    - Add the parallel `action_proposal` assertions to the two Half A cases named in `<behavior>`.
    - Update the file header in place: the promise at lines 9-11 ("Phase 66 MUST gate its ActionProposalSink on the exported isEmissionEligible predicate and MUST extend THIS file's behavioral sentinel to the real sink") is now discharged — say so, name the real sink class, and state that Half B needed no change because `'proposalSink'` and `'ActionProposalSink'` were already in `FORBIDDEN_EMISSION_IDENTIFIERS`. Re-verify that claim against the identifier list and the class names 66-02 actually shipped; if the shipped names are not covered by the existing tokens, add the missing token rather than editing the header to match.
  </action>
  <verify>
    <automated>npx vitest run tests/action-proposal-emission.test.ts tests/emission-hold-sentinel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/action-proposal-emission.test.ts` passes with at least 10 test cases.
    - `npx vitest run tests/emission-hold-sentinel.test.ts` passes, and its Half B structural scan still asserts both anchors ARE present in the real source (non-vacuousness preserved).
    - `grep -c 'SQLiteActionProposalSink' tests/emission-hold-sentinel.test.ts` returns at least 1.
    - `grep -c 'action_proposal' tests/emission-hold-sentinel.test.ts` returns at least 2.
    - The header of `tests/emission-hold-sentinel.test.ts` no longer says the extension is owed to a future phase; it states it is discharged and names the class.
    - `git diff tests/emission-hold-sentinel.test.ts` removes no existing `expect(` assertion (verify with `git diff -U0 tests/emission-hold-sentinel.test.ts | grep -c '^-.*expect('` returning 0).
    - The verbatim-quote assertion uses strict equality against the `episode.content` column read back from the DB, not a substring or trimmed comparison.
    - The atomicity test fails the transaction from inside the real transaction body, not by stubbing the sink to throw — confirm by reading the test.
  </acceptance_criteria>
  <done>Emission is proven at the behavioral level against the real DDL, and Phase 65's shipped sentinel now runs against the real `ActionProposalSink` with its promise discharged in the header.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-classified intent → persisted proposal | LLM output (offline, sleep pass) determines what change is proposed to an external system of record |
| attacker-controlled email content → `evidence_quote` | Untrusted free text is copied verbatim into a persisted, later-served field |
| config → live sleep pass | A settings.json value decides whether the engine writes at all |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-14 | Tampering | emission on a non-decisive outcome | mitigate | Gate is `isEmissionEligible` imported from `status-drift.ts` — grep-enforced that no local event-type set exists in `consolidator.ts`. A hold reaches no gate call at all, and Phase 65's Half B structural scan of the hold arms keeps it that way |
| T-66-15 | Spoofing | proposal for an entity the resolver never confidently matched | mitigate | All-or-nothing check on the four `claimIntent*`/`claimResolvedEntity*` fields before emit; a partial set emits nothing rather than a low-confidence guess (Pitfall 4). Covered by two dedicated negative tests |
| T-66-06 | Elevation of Privilege | classifier prose reaching the approver instead of raw evidence | mitigate | The emitted payload carries only structured fields plus `episode.content` verbatim — `maybeEmitProposal` constructs no sentence, calls no model, and has no `ModelProvider` in scope. Byte-equality test against the stored episode row is the proof (Pitfall 2 / EMIT-06) |
| T-66-16 | Tampering | crash between graph write and proposal write | mitigate | `emit` is synchronous inside the already-open per-episode `db.transaction(...).immediate()`; a grep gate forbids `await` inside `maybeEmitProposal`, and an atomicity test fails the real transaction and asserts neither artifact persists (D-06) |
| T-66-04 | Repudiation | replayed sleep pass double-emitting | mitigate | Deterministic content-hash PK + `INSERT OR IGNORE` (66-02); proven end-to-end here by a re-run of `consolidate()` leaving one row |
| T-66-12 | Tampering | silent default-on emission | mitigate | Two independent barriers: `actionProposalSinkEnabled` defaults `false`, and the Consolidator constructor parameter defaults to `NoopActionProposalSink`. A Noop-injected test asserts zero rows on every decisive case |
| T-66-05 | Information Disclosure | doctor / sleep-pass log output | mitigate | The new pass log emits a count only (`EMIT-66 proposals=N`); `checkProposalSink`'s detail reports posture and a count, never a proposal field. Stated as `T-66-05` in the doctor header |
| T-66-SC | Tampering | package installs | accept | No package-manager install in this plan; nothing for the Package Legitimacy Gate to audit |
</threat_model>

<verification>
1. `npm run typecheck` exits 0.
2. `npx vitest run tests/action-proposal-emission.test.ts tests/emission-hold-sentinel.test.ts` passes.
3. `npm test` shows no new failures versus the pre-plan baseline (record both totals in the SUMMARY — the trailing-Noop-default constructor change touches every existing Consolidator construction).
4. `grep -c '^\s*this\.maybeEmitProposal(' src/consolidation/consolidator.ts` returns 9, and the hold arm contains none.
</verification>

<success_criteria>
- Emission fires only at `applyDecision`'s decisive branches, gated on the imported Phase 65 predicate and on the complete intent + resolved-entity field set.
- `evidence_quote` is provably the stored episode content byte-for-byte.
- The proposal row and the graph mutation it describes commit or roll back together.
- The live sleep pass gets a Noop sink unless the knob is on, and `recense doctor` reports which.
- Phase 65's hold sentinel now runs against the real sink, with its header promise discharged and no existing assertion weakened.
</success_criteria>

<output>
Create `.planning/phases/66-domain-neutral-proposal-emit-seam/66-04-SUMMARY.md` when done.
</output>
</content>
