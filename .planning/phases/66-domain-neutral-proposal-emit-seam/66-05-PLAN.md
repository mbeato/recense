---
phase: 66-domain-neutral-proposal-emit-seam
plan: 05
type: execute
wave: 4
depends_on: ["66-03", "66-04"]
files_modified:
  - tests/action-proposal-write-isolation.test.ts
  - tests/online-llm-free-sentinel.test.ts
autonomous: true
requirements: [EMIT-03, EMIT-05]

must_haves:
  truths:
    - "A named \"D-43-for-proposals\" sentinel exists, greppable by that exact string, so the lineage to D-43 and v9.0's live C-2 stays traceable (D-09, EMIT-05)"
    - "Layer (a): no module on the approve/reject path contains a node/edge write statement or imports a semantic-store write API — proven by an exported predicate that also flags a planted offender (D-09a)"
    - "Layer (b): approving one proposal and rejecting another leaves the whole `node` and `edge` tables byte-identical, while the two proposals provably reached their terminal statuses (D-09b)"
    - "The `/v1/proposals` routes make zero generate/judge/judgeBatch calls and zero embed calls — Phase 63's D-13 online-LLM-free regression now covers them, as promised when it shipped (D-11, EMIT-03)"
  artifacts:
    - path: "tests/action-proposal-write-isolation.test.ts"
      provides: "the named D-43-for-proposals two-layer sentinel — exported structural predicate + planted offender + whole-table runtime snapshot"
      contains: "D-43-for-proposals"
      min_lines: 150
    - path: "tests/online-llm-free-sentinel.test.ts"
      provides: "fourth dynamic describe block exercising GET /v1/proposals and POST /v1/proposals/:id/approve|reject through a fail-if-called ModelProvider"
      contains: "/v1/proposals"
  key_links:
    - from: "tests/action-proposal-write-isolation.test.ts"
      to: "src/adapter/memory-ops.ts approveProposal/rejectProposal bodies"
      via: "balanced-block extraction + comment-stripped forbidden-token scan"
      pattern: "approveProposal"
    - from: "tests/action-proposal-write-isolation.test.ts"
      to: "node + edge tables"
      via: "SELECT * before/after toEqual snapshot across one approve and one reject"
      pattern: "SELECT \\* FROM (node|edge)"
    - from: "tests/online-llm-free-sentinel.test.ts"
      to: "createBrainHttpServer + makeLlmFreeProvider"
      via: "fourth dynamic block over the proposal routes"
      pattern: "/v1/proposals"
---

<objective>
Close the milestone's largest new correctness risk structurally. Ship the named "D-43-for-proposals" sentinel — a two-layer test, one static and one runtime — proving a consumer's approve/reject decision can never write recense's belief, and extend Phase 63's online-LLM-free regression to the two new routes.

Purpose: this defect class has shipped in this codebase twice — a pre-launch critical finding and v9.0's live C-2. Both times it was covered by review discipline and convention comments, and both times it shipped anyway. EMIT-05's own requirement text demands a *named* sentinel, and 66-CONTEXT.md D-09 is explicit that prose is not acceptable closure.
Output: `tests/action-proposal-write-isolation.test.ts` (new), `tests/online-llm-free-sentinel.test.ts` (extended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-domain-neutral-proposal-emit-seam/66-CONTEXT.md
@.planning/phases/66-domain-neutral-proposal-emit-seam/66-PATTERNS.md
@.planning/phases/66-domain-neutral-proposal-emit-seam/66-03-SUMMARY.md
@.planning/phases/66-domain-neutral-proposal-emit-seam/66-04-SUMMARY.md

<interfaces>
<!-- Contracts and shipped test patterns the executor needs. -->

The exported-predicate / real-scan / planted-offender shape to follow, from
tests/no-ats-domain-table.test.ts:
```typescript
export const ATS_DOMAIN_TOKENS: readonly string[] = [ /* the SAME list the predicate uses */ ];
export function findAtsDomainOffenders(files: Array<{ path: string; text: string }>): string[] { /* ... */ }
// test 1: walk the real src/ tree → expect([])
// test 2: plant a synthetic offender through the SAME exported function → expect non-empty
```

The balanced-block extraction + comment-stripping idiom, from
tests/emission-hold-sentinel.test.ts lines 415-470 (local to that file — copy it into the new
file per that file's own "each consumer keeps its own copy" convention; do NOT export it from
the sentinel file and do NOT import across test files):
```typescript
function extractBalancedBlockFromBrace(src: string, openBraceIdx: number): string { /* depth counter */ }
// find anchor in UNSTRIPPED source → lastIndexOf('{') → extract balanced block → stripComments(block) → match
// an anchor that is not found is silently skipped, so the function stays usable on synthetic
// fixtures; the real-source test separately asserts every anchor IS present
```

From tests/online-llm-free-sentinel.test.ts lines 1-130 (the file being extended — note the
header's own instruction at lines 17-19):
```
 * PHASE 66 EXTENSION POINT: when `/v1/proposals` lands (EMIT-* work), this file MUST gain
 * a fourth dynamic describe block exercising that route the same way the `GET /v1/surface`
 * block does below (D-13).
```
```typescript
function makeLlmFreeProvider(embedFn: (text: string) => Float32Array):
  ModelProvider & { readonly embedCount: number };
// embed delegates + counts; generate/judge/judgeBatch each throw
import { createBrainHttpServer, BrainHttpServer } from '../src/adapter/serve-cli';
function makeTempDbPath(prefix: string): string;
```

From src/adapter/memory-ops.ts (shipped by 66-03 — the functions layer (a) scans):
```typescript
export function wireMemoryEngine(opts): Promise<{ ops: MemoryOps; close: () => void }>;
// ops.listProposals(), ops.approveProposal(id), ops.rejectProposal(id)
async function approveProposal(id: string): Promise<{ status: string }>   // per-call lock
async function rejectProposal(id: string): Promise<{ status: string }>    // per-call lock
```

From src/db/action-proposal-store.ts (66-01):
```typescript
export class ActionProposalStore {
  insert(row: ActionProposalRecord): void;
  updateStatus(id: string, status: ProposalStatus, nowMs: number): void;  // the ONLY UPDATE
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The named "D-43-for-proposals" two-layer sentinel</name>
  <files>tests/action-proposal-write-isolation.test.ts</files>
  <read_first>
    - tests/no-ats-domain-table.test.ts (full — the exported-predicate / real-scan / planted-offender shape, including the comment explaining why the token list is exported rather than duplicated)
    - tests/emission-hold-sentinel.test.ts lines 400-523 (the balanced-block extraction, `stripComments`, the anchor-scan function, and its two non-vacuousness tests)
    - tests/src-import-boundary.test.ts lines 1-70 (the import-boundary predicate shape)
    - src/db/action-proposal-store.ts, src/adapter/memory-ops.ts (`approveProposal`/`rejectProposal`), src/adapter/serve-cli.ts (the two `/v1/proposals` blocks) — the three scan targets, read as they actually shipped
    - .planning/phases/66-domain-neutral-proposal-emit-seam/66-CONTEXT.md decisions D-08, D-09 and the `<specifics>` note on why the name must be verbatim
    - .planning/REQUIREMENTS.md EMIT-05
  </read_first>
  <behavior>
    Layer (a) — structural:
    - `findProposalWriteIsolationOffenders(files)` returns `[]` when run over the three real scan targets.
    - It returns a non-empty offender list when handed a synthetic fixture whose approve-path block contains `UPDATE node SET s = 1`.
    - It returns a non-empty offender list for each forbidden token individually, driven from the SAME exported token list the predicate uses (table-driven, one case per token).
    - It returns `[]` for a fixture where the only occurrence of a forbidden token is inside a `//` comment or a `/* */` block — comments are stripped before matching.
    - It returns `[]` for a fixture containing `SELECT tombstoned FROM node WHERE id = @id` — reads against `node` are explicitly permitted; only writes are forbidden.
    - `findProposalStoreImportOffenders(text)` returns non-empty when the store module text imports `semantic-store`, `StrengthDecayManager`, or `../strength/decay`, and `[]` for the real module.
    - The real-source test separately asserts every anchor WAS found, so a renamed function cannot make the scan silently vacuous.

    Layer (b) — runtime:
    - Given two seeded pending proposals over a graph with several nodes and edges carrying non-default `s`/`c` values, `SELECT * FROM node ORDER BY id` and `SELECT * FROM edge ORDER BY src, dst, rel` snapshots taken before one `approveProposal` and one `rejectProposal` are `toEqual`-identical to the snapshots taken after.
    - Non-vacuousness: after the same two calls the two proposals' statuses are `'approved'` and `'rejected'` respectively, and `updated_at` moved — so the snapshot equality is not passing because nothing happened.
    - An approve that is refused as stale (belief node tombstoned before the call) also leaves `node`/`edge` byte-identical apart from the tombstone the test itself applied — snapshot after the tombstone, not before.
  </behavior>
  <action>
    Create `tests/action-proposal-write-isolation.test.ts`.

    Header doc block, written for the next reader rather than for this phase: name the sentinel `D-43-for-proposals` in the first line and use that exact hyphenated string verbatim (66-CONTEXT `<specifics>` requires it be greppable so the lineage to D-43 and to v9.0's live C-2 stays discoverable). State the invariant in one sentence — a consumer's approve or reject decision writes proposal status and nothing else, ever — and state why two layers exist: this defect class shipped twice under review discipline alone, so the static layer catches a future edit at review time and the runtime layer catches an indirect write the token list did not anticipate.

    Layer (a). Export the token list so the planted-offender tests draw from the same source the predicate checks — two independently maintained lists are the exact duplication defect this guard exists to prevent (the `ATS_DOMAIN_TOKENS` rationale, restated):
    `export const FORBIDDEN_BELIEF_WRITE_TOKENS: readonly string[]` containing the SQL write forms `UPDATE node`, `INSERT INTO node`, `DELETE FROM node`, `UPDATE edge`, `INSERT INTO edge`, `DELETE FROM edge` (matched case-insensitively) and the API forms `upsertNode`, `upsertEdge`, `setEmbedding`, `upsertNodeTemporal`, `recordContradiction`, `.tombstone(`, `.strengthen(` (matched case-sensitively). Comment each group with what it defends: the SQL group catches a hand-written statement, the API group catches a call through `SemanticStore`/`StrengthDecayManager`.

    `export function findProposalWriteIsolationOffenders(files: Array<{ path: string; text: string }>): string[]` — for each file, locate the scan regions by stable anchors in the UNSTRIPPED text, extract each region's balanced `{...}` block, strip comments from just that block, then match. Regions:
      - `src/db/action-proposal-store.ts` — the WHOLE file is the region (it exists only to own this table).
      - `src/adapter/memory-ops.ts` — the balanced bodies of `approveProposal` and `rejectProposal`, anchored on `async function approveProposal(` and `async function rejectProposal(`.
      - `src/adapter/serve-cli.ts` — the balanced bodies of the two proposal route blocks, anchored on `url === '/v1/proposals' && req.method === 'GET'` and `startsWith('/v1/proposals/')`.
    Follow the sentinel-file convention exactly: an anchor that is not found is silently skipped (so synthetic fixtures work), and the real-source test separately asserts each anchor WAS present.

    `export function findProposalStoreImportOffenders(text: string): string[]` — flag any import specifier containing `semantic-store`, `strength/decay`, or the identifier `StrengthDecayManager`. Explicitly permit `better-sqlite3`, `../lib/clock`, `../db/schema`, and `../model/claim-extractor` type imports.

    Write the real-source test (walk the three files off disk with `readFileSync`, expect `[]` from both predicates, and assert all five anchors were found), plus the planted-offender tests enumerated in `<behavior>`, driven from `FORBIDDEN_BELIEF_WRITE_TOKENS` with `it.each`.

    Layer (b). Build a real engine via `wireMemoryEngine({ dbPath, provider, source: 'http', separateReadHandle: true })` against a temp file DB. Seed, through a directly-opened handle: several `node` rows with deliberately varied `s`, `c`, `prev_value`, `pending_contradictions`, and `last_access` values; at least two `edge` rows; an `episode`; and two pending `action_proposal` rows via `ActionProposalStore.insert`. Varying `s`/`c` matters — a snapshot over uniformly-default rows would not distinguish "nothing was written" from "everything was written to the same defaults".

    Snapshot both tables with `SELECT *` and a deterministic `ORDER BY`, call `ops.approveProposal(idA)` and `ops.rejectProposal(idB)`, re-snapshot, and `expect(after).toEqual(before)` for each table. Then assert the two proposal rows reached `'approved'`/`'rejected'` and that their `updated_at` differs from `created_at`, so the equality assertion is provably non-vacuous. Add the stale-refusal variant described in `<behavior>`.

    Close the engine and remove the temp DB in `afterEach`.
  </action>
  <verify>
    <automated>npx vitest run tests/action-proposal-write-isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/action-proposal-write-isolation.test.ts` passes with at least 12 test cases.
    - `grep -c 'D-43-for-proposals' tests/action-proposal-write-isolation.test.ts` returns at least 1, spelled exactly with that hyphenation.
    - The file exports `FORBIDDEN_BELIEF_WRITE_TOKENS`, `findProposalWriteIsolationOffenders`, and `findProposalStoreImportOffenders`.
    - The planted-offender suite is `it.each`-driven from `FORBIDDEN_BELIEF_WRITE_TOKENS` — there is no second hand-written token list in the file.
    - The real-source test asserts all five anchors were located (an explicit assertion, not an implicit consequence of the scan returning `[]`).
    - The comment-only fixture case and the `SELECT ... FROM node` permitted-read case both exist and pass.
    - The runtime half compares `SELECT *` rows via `toEqual`, not a `COUNT(*)` or a checksum.
    - The seeded nodes have at least three distinct `s` values and at least two distinct `c` values.
    - Temporarily adding `UPDATE node SET s = 1` inside `approveProposal` makes layer (a) fail and layer (b) fail — perform this check, record both failure messages in the SUMMARY, and revert.
  </acceptance_criteria>
  <done>The named sentinel exists, both layers pass against real source and a real engine, both are demonstrated non-vacuous against a planted offender, and the token list has exactly one definition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the D-13 online-LLM-free regression to /v1/proposals</name>
  <files>tests/online-llm-free-sentinel.test.ts</files>
  <read_first>
    - tests/online-llm-free-sentinel.test.ts (full file — the header's own "PHASE 66 EXTENSION POINT" instruction at lines 17-19, `makeLlmFreeProvider`, `makeTempDbPath`, the `config()` helper, and the existing `GET /v1/surface` dynamic block whose shape the new block copies, including its positive-completion assertions and its structural-half source set)
    - src/adapter/serve-cli.ts (the two proposal routes as shipped by 66-03)
    - src/db/action-proposal-store.ts (for seeding fixtures)
    - .planning/phases/66-domain-neutral-proposal-emit-seam/66-CONTEXT.md decision D-11 (the extension is explicitly promised, not optional)
  </read_first>
  <behavior>
    - `GET /v1/proposals` against a server wired with `makeLlmFreeProvider` returns 200 and a non-empty `items` array (positive completion — a route that short-circuits to an empty response must not pass as "LLM-free").
    - `POST /v1/proposals/:id/approve` returns 200 `{status:'approved'}` against the same server.
    - `POST /v1/proposals/:id/reject` returns 200 `{status:'rejected'}` against the same server.
    - Across all three calls, `provider.embedCount` is exactly 0 — unlike the inject path, the proposal routes make no embedding call at all, so the bound here is tighter than the `/v1/surface` block's.
    - No `generate`, `judge`, or `judgeBatch` throw fires (implied by the three 200s, but asserted explicitly by wrapping the block so a throw surfaces as a test failure rather than a 500 body).
    - The structural half's scanned source set includes `src/db/action-proposal-store.ts` and still reports zero offenders.
  </behavior>
  <action>
    Add a fourth dynamic `describe` block to `tests/online-llm-free-sentinel.test.ts`, immediately after the existing `GET /v1/surface` block, copying its shape: temp file DB, `initSchema` through a directly-opened handle to seed fixtures, `createBrainHttpServer({ dbPath, provider: makeLlmFreeProvider(...), token })`, `server.listen(0)`, real `http` requests with the Bearer header, and `afterEach` close + unlink.

    Seed via the direct handle: one entity `node`, one belief `node`, one `episode`, and two pending `action_proposal` rows through `ActionProposalStore.insert` with `expires_at` comfortably in the future so the approve path reaches its success branch rather than the staleness refusal.

    Cover every bullet in `<behavior>`. Assert `provider.embedCount === 0` after all three requests, and add a comment stating why this bound is tighter than the `/v1/surface` block's: the proposal routes perform no retrieval, so any embed call at all would mean something on the path started doing work it has no business doing.

    Extend the structural half's scanned source set to include `src/db/action-proposal-store.ts` alongside the existing `ambient-recall.ts` + `src/retrieval/*.ts` set, and confirm it still returns zero offenders. Leave the existing planted-offender and comment-only false-positive cases untouched.

    Update the header in place: replace the "PHASE 66 EXTENSION POINT" paragraph with a statement that the extension is discharged, naming the three routes now covered and the tighter `embedCount === 0` bound. Do not delete the D-13 rationale above it, and do not weaken any existing assertion in the file.
  </action>
  <verify>
    <automated>npx vitest run tests/online-llm-free-sentinel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/online-llm-free-sentinel.test.ts` passes, with at least 4 test cases more than before this task (record both counts in the SUMMARY).
    - `grep -c '/v1/proposals' tests/online-llm-free-sentinel.test.ts` returns at least 3.
    - `grep -c 'PHASE 66 EXTENSION POINT' tests/online-llm-free-sentinel.test.ts` returns 0 — the placeholder is replaced by the discharge statement.
    - `grep -c 'action-proposal-store' tests/online-llm-free-sentinel.test.ts` returns at least 1 (the structural-half source-set addition).
    - The new block asserts `embedCount` is exactly `0`, not merely bounded.
    - `git diff -U0 tests/online-llm-free-sentinel.test.ts | grep -c '^-.*expect('` returns 0 — no existing assertion removed.
  </acceptance_criteria>
  <done>The three proposal routes are exercised in-process through a fail-if-called provider with positive completion assertions and a zero-embed bound, and the header no longer owes the extension to a future phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer approve/reject → belief graph | The self-confirmation vector this plan exists to seal |
| online HTTP request → model provider | An LLM call on a request-blocking path would violate the engine's latency and cost invariants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-03 | Tampering | approve/reject write path (D-43-for-proposals) | mitigate | Two independent layers: an exported static predicate over the three modules on the path, proven non-vacuous by a planted offender per forbidden token; and a whole-table `node`+`edge` `SELECT *` snapshot across a real approve and a real reject, proven non-vacuous by asserting the proposals reached terminal statuses |
| T-66-17 | Tampering | sentinel decays into a vacuous pass | mitigate | Anchors are asserted present in the real source; the token list has exactly one definition and drives both the predicate and the planted-offender cases; a deliberate offender is injected during execution and both layers are confirmed to fail before reverting |
| T-66-18 | Tampering | indirect belief write the token list did not anticipate | mitigate | Layer (b) is token-agnostic: it compares full table contents, so a write through any API, statement, or trigger the static list never heard of still fails it |
| T-66-06 | Elevation of Privilege | a model call reaching the proposal routes | mitigate | Fail-if-called `ModelProvider` exercised through all three routes with positive-completion assertions and a strict `embedCount === 0` bound (D-11 / Phase 63 D-13) |
| T-66-19 | Information Disclosure | test fixtures leaking into the repo | accept | Fixtures are synthetic strings in temp DBs removed in `afterEach`; no real inbox content, no token, no key |
| T-66-SC | Tampering | package installs | accept | No package-manager install in this plan; nothing for the Package Legitimacy Gate to audit |
</threat_model>

<verification>
1. `npx vitest run tests/action-proposal-write-isolation.test.ts tests/online-llm-free-sentinel.test.ts` passes.
2. `npm test` shows no new failures versus the pre-plan baseline.
3. `grep -rc 'D-43-for-proposals' tests/ src/ | grep -v ':0'` lists at least `tests/action-proposal-write-isolation.test.ts`, `src/db/action-proposal-store.ts`, and `src/adapter/memory-ops.ts` — the lineage is greppable across the code and the test that guards it.
4. The deliberate-offender check described in Task 1's acceptance criteria was performed and both layers failed; the failure messages are pasted into the SUMMARY.
</verification>

<success_criteria>
- A sentinel named exactly "D-43-for-proposals" exists and is greppable.
- Layer (a) scans the three real modules on the approve/reject path and is proven non-vacuous per forbidden token, per comment-stripping, and per permitted-read.
- Layer (b) proves the `node` and `edge` tables are byte-identical across a real approve and a real reject over a graph with varied strengths and confidences.
- The `/v1/proposals` routes are covered by the D-13 online-LLM-free regression with a zero-embed bound, discharging the promise in that file's header.
</success_criteria>

<output>
Create `.planning/phases/66-domain-neutral-proposal-emit-seam/66-05-SUMMARY.md` when done.
</output>
</content>
