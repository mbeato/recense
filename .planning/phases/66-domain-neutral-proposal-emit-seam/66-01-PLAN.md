---
phase: 66-domain-neutral-proposal-emit-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/db/action-proposal-store.ts
  - tests/action-proposal-contract.test.ts
autonomous: true
requirements: [EMIT-02, EMIT-04, EMIT-07]

must_haves:
  truths:
    - "The `action_proposal` table exists on a fresh DB and on an upgraded v16 DB, with no change to any existing table (D-01)"
    - "The proposal record's field set is frozen and mechanically enforced — a field added to the TS type without updating the frozen list is a compile error, and a column added to the DDL without updating the frozen list fails a test (D-02, D-03)"
    - "A replayed emission of the same underlying change collapses onto one row rather than creating a duplicate (D-07)"
    - "A proposal whose entity node is tombstoned, whose belief node is tombstoned, or which is past its expiry is classifiable as refusable without any DB write and without any LLM call (D-10)"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "action_proposal DDL + v17 migration block + SCHEMA_VERSION=17"
      contains: "CREATE TABLE IF NOT EXISTS action_proposal"
    - path: "src/db/action-proposal-store.ts"
      provides: "ActionProposalRecord type, ACTION_PROPOSAL_FIELDS exhaustive map, ActionProposalStore, classifyProposalStaleness, PROPOSAL_TTL_MS, PROPOSAL_LIST_LIMIT"
      exports: ["ActionProposalRecord", "ACTION_PROPOSAL_FIELDS", "ActionProposalStore", "classifyProposalStaleness", "PROPOSAL_TTL_MS", "PROPOSAL_LIST_LIMIT", "ProposalStatus", "StalenessVerdict"]
      min_lines: 150
    - path: "tests/action-proposal-contract.test.ts"
      provides: "EMIT-02 frozen-contract lock (DDL column set == TS key set == frozen list) + EMIT-04 INSERT OR IGNORE replay proof + D-10 staleness precedence table"
  key_links:
    - from: "src/db/action-proposal-store.ts"
      to: "action_proposal table"
      via: "prepared statements with named bound params"
      pattern: "action_proposal"
    - from: "tests/action-proposal-contract.test.ts"
      to: "PRAGMA table_info(action_proposal)"
      via: "runtime DDL introspection compared against ACTION_PROPOSAL_FIELDS"
      pattern: "table_info\\(action_proposal\\)"
---

<objective>
Freeze the `action_proposal` contract — the one artifact both of this phase's workstreams (emission logic, HTTP surface) must agree on before either can start. Ships the additive schema-v17 table, the store module that owns every SQL statement touching it, and the pure staleness classifier EMIT-07's refusal path will call.

Purpose: the ROADMAP folded research's standalone "Proposal Schema & Sink Foundation" phase into Phase 66 on the explicit condition that "declaring the schema as Phase 66's first task achieves the identical freeze". This plan is that task. Nothing else in the phase can be written against a shape that does not exist yet.
Output: `action_proposal` table (v17), `src/db/action-proposal-store.ts`, `tests/action-proposal-contract.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-domain-neutral-proposal-emit-seam/66-CONTEXT.md
@.planning/phases/66-domain-neutral-proposal-emit-seam/66-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Do NOT go exploring for these — they are here. -->

Frozen `action_proposal` field set (16 fields — LOCKED by D-02/D-03; adding, removing or
renaming ANY of these is out of scope for this plan and for this phase):

  id                TEXT    PRIMARY KEY      -- sha256 hex, deterministic (D-07); minted by plan 66-02
  kind              TEXT    NOT NULL         -- CHECK(kind IN ('belief')); discriminator for Phase 68's union
  entity_node_id    TEXT    NOT NULL         -- REFERENCES node(id); Phase 64 D-07 claimResolvedEntityId
  entity_descriptor TEXT    NOT NULL         -- Phase 64 D-08 claimResolvedEntityDescriptor, verbatim
  belief_node_id    TEXT    NOT NULL         -- REFERENCES node(id); the node applyDecision minted for change_to
  change_field      TEXT    NOT NULL         -- free string, NO CHECK constraint (Pitfall 10)
  change_from       TEXT                     -- nullable; prior belief TEXT (see asymmetry note in the action)
  change_to         TEXT    NOT NULL         -- free string, NO CHECK constraint (Pitfall 10)
  evidence_episode  TEXT    NOT NULL         -- REFERENCES episode(id)
  evidence_quote    TEXT    NOT NULL         -- verbatim stored episode content (EMIT-06/D-12)
  confidence        TEXT    NOT NULL         -- CHECK(confidence IN ('high','medium','low')) — Phase 63 D-06
  schema_version    INTEGER NOT NULL         -- Pitfall 11
  status            TEXT    NOT NULL         -- CHECK(status IN ('pending','approved','rejected','superseded','expired'))
  created_at        INTEGER NOT NULL         -- epoch ms; immutable
  updated_at        INTEGER NOT NULL         -- epoch ms
  expires_at        INTEGER NOT NULL         -- epoch ms; created_at + PROPOSAL_TTL_MS (Pitfall 13 backstop)

From src/db/schema.ts (current head — this plan bumps it):
```typescript
// v16: episode.event_ts (EMAIL-04: source-asserted event time)
export const SCHEMA_VERSION = 16;
```

From src/db/schema.ts (`surfaced_event` DDL — the exact style to mirror, lines 198-212):
```sql
  CREATE TABLE IF NOT EXISTS surfaced_event (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    node_id           TEXT    NOT NULL REFERENCES node(id),
    outcome           TEXT    NOT NULL DEFAULT 'surfaced'
                              CHECK(outcome IN ('surfaced','seen','snoozed','completed','dismissed')),
    created_at        INTEGER NOT NULL,
    updated_at        INTEGER NOT NULL,
    UNIQUE(node_id, occurrence_due_at)
  );
```

From src/db/schema.ts (v14 brand-new-table migration block, lines 625-633 — the correct
precedent shape: three comments then an index-only `db.exec`, NO ALTER TABLE):
```typescript
  // v14 migration: token_usage_ledger + ts index (Phase 44, COST-01, D-08/D-09/D-10).
  // Table uses CREATE TABLE IF NOT EXISTS in DDL above → idempotent on fresh DBs.
  // Existing v13 DBs: token_usage_ledger absent → DDL above creates it (IF NOT EXISTS catches it).
  // No ALTER TABLE needed — the whole table is new (no column additions to existing tables).
  db.exec(`
    CREATE INDEX IF NOT EXISTS idx_token_usage_ledger_ts
      ON token_usage_ledger (ts);
  `);
```

From src/db/surface-store.ts (the store-module shape to mirror — prepared statements built
once in the constructor, named bound params only, pure business-logic helper kept OUTSIDE
the class, exported row/opts interfaces):
```typescript
export class SurfaceStore {
  private readonly db: Database.Database;
  private readonly clock: Clock;
  private readonly stmtEligible: Database.Statement;
  constructor(db: Database.Database, clock: Clock) { /* db.prepare(...) x N */ }
}
function isExcluded(evt: SurfacedEventRow | undefined, nowMs: number): boolean { /* pure */ }
```

From src/model/claim-extractor.ts (the closed vocabularies this table CHECKs against):
```typescript
export type IntentStatus = 'applied' | 'interviewing' | 'rejected' | 'offer';
export type IntentConfidence = 'high' | 'medium' | 'low';
```

From src/lib/clock.ts: `interface Clock { nowMs(): number }`, `realClock`, `FakeClock`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: action_proposal DDL + v17 migration + SCHEMA_VERSION bump</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - src/db/schema.ts lines 1-20 (SCHEMA_VERSION + header), 190-265 (surfaced_event DDL, end of DDL template literal, v2 migration + uq_episode_source_external precedent), 620-666 (v14/v15 migration blocks + the tail stamp guard this new block must sit ABOVE)
    - .planning/phases/66-domain-neutral-proposal-emit-seam/66-PATTERNS.md section "`src/db/schema.ts` (migration, DDL)"
  </read_first>
  <action>
    Append the `action_proposal` table to the existing `DDL` template literal in `src/db/schema.ts`, immediately after the `surfaced_event` block and before the closing backtick. Use exactly the 16 columns, types, nullability, and CHECK constraints listed in the `<interfaces>` frozen field set above — no additions, no omissions, no renames.

    Constraint details that are load-bearing and must not be "improved":
    - `id TEXT PRIMARY KEY` — NOT `INTEGER PRIMARY KEY AUTOINCREMENT`. The PK is the deterministic sha256 content hash (D-07), which is what makes `INSERT OR IGNORE` the whole replay mechanism (EMIT-04). Cite `uq_episode_source_external` (schema.ts:246-253) in the DDL comment as the shipped natural-key-dedup precedent.
    - `change_field`, `change_from`, `change_to` carry NO CHECK constraint. A CHECK'd enum here would encode a consumer's status vocabulary into recense's seam and falsify the domain-neutral claim (research Pitfall 10). Write that reason as a comment on those three columns.
    - `kind`, `confidence`, `status` DO carry inline CHECK constraints in the `surfaced_event` style, because all three are recense's OWN closed vocabularies.
    - `entity_node_id` and `belief_node_id` both `REFERENCES node(id)`; `evidence_episode REFERENCES episode(id)`. All three are safe under `foreign_keys = ON`: `SemanticStore.tombstone` sets a `tombstoned` flag rather than deleting, and no code path anywhere issues `DELETE FROM episode`.
    - `status TEXT NOT NULL DEFAULT 'pending'`.

    Add a header comment above the table in the `surfaced_event` style: state that this is an operational table (EMIT-01/EMIT-02), that the sleep pass writes it and the serve path updates only `status`/`updated_at`, and that the idempotency key is the content-hash PK.

    Add the `// v17 migration: action_proposal operational table (Phase 66, EMIT-01/EMIT-02).` block immediately after the v15 `activation_trace.kind` ALTER guard and BEFORE the `// Stamp schema version` block at the tail of `initSchema`. Follow the v14 three-comment shape verbatim (DDL-covers-fresh / existing-v16-DBs-get-it-from-IF-NOT-EXISTS / no-ALTER-needed-whole-table-new), then a single `db.exec` creating three indexes:
      `idx_action_proposal_status_created ON action_proposal(status, created_at)` — backs the pending-list query
      `idx_action_proposal_entity ON action_proposal(entity_node_id, status)` — backs per-entity lookups
      `idx_action_proposal_status_expires ON action_proposal(status, expires_at)` — backs the expiry check

    Change line 11-12 to `// v17: action_proposal operational table (EMIT-01/EMIT-02)` / `export const SCHEMA_VERSION = 17;`.

    Do not touch any other table, index, ALTER guard, or the stamp guard.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/schema.test.ts 2>/dev/null || npx vitest run --reporter=basic 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export const SCHEMA_VERSION = 17;' src/db/schema.ts` returns 1.
    - `grep -c 'CREATE TABLE IF NOT EXISTS action_proposal' src/db/schema.ts` returns 1.
    - `grep -c 'v17 migration: action_proposal' src/db/schema.ts` returns 1.
    - `node -e "const D=require('better-sqlite3');const{initSchema}=require('./dist/src/db/schema');const db=new D(':memory:');initSchema(db);console.log(db.pragma('table_info(action_proposal)').map(r=>r.name).sort().join(','))"` (after `npm run build`) prints exactly: `belief_node_id,change_field,change_from,change_to,confidence,created_at,entity_descriptor,entity_node_id,evidence_episode,evidence_quote,expires_at,id,kind,schema_version,status,updated_at`
    - Calling `initSchema` twice on the same DB handle does not throw (idempotency).
    - `git diff src/db/schema.ts` shows zero modifications to the `episode`, `node`, `edge`, `meta`, `surfaced_event`, `node_temporal`, `activation_trace`, `token_usage_ledger`, `consolidation_event`, `node_scope`, `node_doc`, or `node_insight` definitions.
    - `grep -v '^\s*--' src/db/schema.ts | grep -c "CHECK(change_" ` returns 0 (no CHECK on any change_* column).
  </acceptance_criteria>
  <done>A fresh in-memory DB and an upgraded v16 DB both have `action_proposal` with exactly the 16 frozen columns and the three indexes; `SCHEMA_VERSION` is 17; no existing table changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ActionProposalStore + frozen record type + pure staleness classifier</name>
  <files>src/db/action-proposal-store.ts</files>
  <read_first>
    - src/db/surface-store.ts (full file — the exact module shape being mirrored: prepared statements in constructor, named bound params, pure helper outside the class, exported row/opts interfaces, "read-only by construction — D-43" doc comment convention)
    - src/db/schema.ts (the `action_proposal` DDL just written in Task 1, plus `SCHEMA_VERSION`)
    - src/lib/clock.ts (Clock interface)
    - src/model/claim-extractor.ts lines 59-110 (IntentStatus / IntentConfidence closed vocabularies)
    - .planning/phases/66-domain-neutral-proposal-emit-seam/66-CONTEXT.md decisions D-02, D-03, D-08, D-10
  </read_first>
  <behavior>
    - `classifyProposalStaleness({ expiresAt: t+1, entityTombstoned: false, beliefTombstoned: false, nowMs: t })` → `'ok'`
    - `classifyProposalStaleness({ expiresAt: t+1, entityTombstoned: true, beliefTombstoned: false, nowMs: t })` → `'entity_gone'`
    - `classifyProposalStaleness({ expiresAt: t+1, entityTombstoned: false, beliefTombstoned: true, nowMs: t })` → `'superseded'`
    - `classifyProposalStaleness({ expiresAt: t, entityTombstoned: false, beliefTombstoned: false, nowMs: t })` → `'expired'` (boundary: `expiresAt <= nowMs` is expired)
    - Precedence when several hold at once: `entity_gone` > `superseded` > `expired`. An entity-gone-AND-expired proposal classifies `'entity_gone'`.
    - `Object.keys(ACTION_PROPOSAL_FIELDS)` has length 16 and equals the frozen field list.
    - `insert()` called twice with the same `id` leaves exactly one row, and the second call does not overwrite the first row's `created_at`.
    - `listPending()` returns only `status='pending'` rows, oldest `created_at` first, at most `PROPOSAL_LIST_LIMIT` rows.
    - `updateStatus(id, 'approved', now)` changes `status` and `updated_at` and leaves `created_at` unchanged.
  </behavior>
  <action>
    Create `src/db/action-proposal-store.ts`. Name the module `action-proposal-store.ts` and the class `ActionProposalStore` — deliberately NOT the `proposal-store.ts` that 66-CONTEXT.md floated as a suggestion, because `clients/telegram/proposal-store.ts` already exists and Phase 68 edits it; two files named `proposal-store.ts` in one repo would make every grep, import, and structural scan in this phase and the next ambiguous. Record that rationale in the file header.

    Header doc block, in the `surface-store.ts` style: what this module owns (every SQL statement that touches `action_proposal`, and nothing else), the D-43-for-proposals rule stated up front (this module NEVER writes `node` or `edge` — it holds SELECT-only reads against `node` for the staleness check and no other node access), T-01-SQL bound-parameter discipline, and the note that `listPending` is read-only by construction because it backs the lock-free `GET /v1/proposals`.

    Export, in this order:
    - `export type ProposalKind = 'belief';`
    - `export type ProposalStatus = 'pending' | 'approved' | 'rejected' | 'superseded' | 'expired';`
    - `export type StalenessVerdict = 'ok' | 'entity_gone' | 'superseded' | 'expired';`
    - `export const PROPOSAL_TTL_MS = 14 * 24 * 60 * 60 * 1000;` — Pitfall 13's "practical staleness window even without a superseding event". Doc-comment the 14-day choice as a calibration placeholder, not a law.
    - `export const PROPOSAL_LIST_LIMIT = 100;` — bounds the `GET /v1/proposals` response (T-66-07).
    - `export interface ActionProposalRecord` with exactly the 16 frozen fields, TS types mirroring the DDL (`change_from: string | null`, `schema_version: number`, timestamps `number`, everything else `string`, `kind: ProposalKind`, `status: ProposalStatus`, `confidence: IntentConfidence` imported as a type from `../model/claim-extractor`).
    - `export const ACTION_PROPOSAL_FIELDS = { id: true, kind: true, ... } satisfies Record<keyof ActionProposalRecord, true>;` — a key-exhaustive total map. This is EMIT-02's compile-time half of the D-03 lock: adding a field to `ActionProposalRecord` without adding it here fails `tsc`. Doc-comment it as such so nobody deletes it as redundant.
    - `export function classifyProposalStaleness(input: { expiresAt: number; entityTombstoned: boolean; beliefTombstoned: boolean; nowMs: number }): StalenessVerdict` — pure, no DB, no I/O, no clock read. Implement the precedence in the `<behavior>` block and doc-comment WHY each verdict exists (D-10 a/b/c) and why the precedence is fixed (a deterministic refusal reason is what makes the 409 `detail` reproducible).
    - `export class ActionProposalStore` with `constructor(db: Database.Database, clock: Clock)` and prepared statements compiled once:
        `insert(row: ActionProposalRecord): void` — `INSERT OR IGNORE INTO action_proposal (...) VALUES (@id, @kind, ...)`. Doc-comment: OR IGNORE is EMIT-04's whole replay mechanism; the PK is a content hash so a collision means "same change, already recorded", never data loss.
        `listPending(limit: number = PROPOSAL_LIST_LIMIT): ActionProposalRecord[]` — `WHERE status = 'pending' ORDER BY created_at ASC LIMIT @limit`.
        `getById(id: string): ActionProposalRecord | null`
        `getStalenessInputs(id: string): { expiresAt: number; entityTombstoned: boolean; beliefTombstoned: boolean } | null` — one SELECT joining `action_proposal` to `node` twice (`e.id = p.entity_node_id`, `b.id = p.belief_node_id`) reading `tombstoned` from each. SELECT ONLY. Convert the SQLite 0/1 integers to booleans here so `classifyProposalStaleness` takes real booleans.
        `updateStatus(id: string, status: ProposalStatus, nowMs: number): void` — `UPDATE action_proposal SET status = @status, updated_at = @now WHERE id = @id`. This is the ONLY UPDATE statement in the module. Carry the verbatim comment `// D-43-for-proposals: the only write path here touches action_proposal.status + updated_at. Never node, never edge.`

    Every statement uses named bound parameters (`@id`, `@status`, `@now`, `@limit`) — no string interpolation anywhere (T-01-SQL).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `grep -c 'satisfies Record<keyof ActionProposalRecord, true>' src/db/action-proposal-store.ts` returns 1.
    - `grep -ciE 'UPDATE node|INSERT INTO node|DELETE FROM node|UPDATE edge|INSERT INTO edge|DELETE FROM edge' src/db/action-proposal-store.ts` returns 0.
    - `grep -c "from '../db/semantic-store'\|from './semantic-store'" src/db/action-proposal-store.ts` returns 0.
    - `grep -cE '\$\{' src/db/action-proposal-store.ts` returns 0 (no template interpolation inside SQL).
    - `grep -c 'D-43-for-proposals' src/db/action-proposal-store.ts` is at least 1.
    - The file exports all of: `ActionProposalRecord`, `ACTION_PROPOSAL_FIELDS`, `ActionProposalStore`, `classifyProposalStaleness`, `PROPOSAL_TTL_MS`, `PROPOSAL_LIST_LIMIT`, `ProposalStatus`, `StalenessVerdict`, `ProposalKind`.
    - The module contains exactly one `UPDATE ` SQL statement.
  </acceptance_criteria>
  <done>`ActionProposalStore` compiles, owns every `action_proposal` SQL statement, imports no semantic-store write API, and `classifyProposalStaleness` is a pure function with the locked precedence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Frozen-contract test — DDL key set == TS key set, replay collapse, staleness table</name>
  <files>tests/action-proposal-contract.test.ts</files>
  <read_first>
    - src/db/action-proposal-store.ts (written in Task 2)
    - src/db/schema.ts (the DDL written in Task 1)
    - tests/emission-hold-sentinel.test.ts lines 100-160 (in-memory `Database(':memory:')` + `initSchema` + `FakeClock` harness convention)
    - .planning/phases/66-domain-neutral-proposal-emit-seam/66-CONTEXT.md decision D-03 (why the lock is an exhaustive key check, NOT a forbidden-vocabulary grep)
  </read_first>
  <behavior>
    - `PRAGMA table_info(action_proposal)` column-name set equals `Object.keys(ACTION_PROPOSAL_FIELDS)` exactly, in both directions (no extra column, no missing column).
    - The frozen list literal spelled out in the test equals `Object.keys(ACTION_PROPOSAL_FIELDS)` — a third, independently-typed copy so the test cannot pass by both sides drifting together.
    - Inserting the same `ActionProposalRecord` twice yields exactly one row; the surviving row's `created_at` is the first insert's value.
    - Inserting two records that differ only in `belief_node_id` (same `id`) still yields one row — proving `belief_node_id` is deliberately outside the id hash.
    - `classifyProposalStaleness` returns the locked verdict for each of the 8 combinations of (entityTombstoned, beliefTombstoned, expired).
    - `listPending` excludes non-pending rows and orders by `created_at` ascending.
    - `updateStatus` mutates `status` + `updated_at` only; a full-row refetch shows every other column byte-identical.
  </behavior>
  <action>
    Create `tests/action-proposal-contract.test.ts` with a vitest suite using an in-memory `better-sqlite3` handle and `initSchema`, plus a `FakeClock`.

    Declare a `const FROZEN_PROPOSAL_FIELDS: readonly string[]` literal inside the test file listing all 16 field names, sorted. Assert three-way equality between: (a) that literal, (b) `Object.keys(ACTION_PROPOSAL_FIELDS)` from the store module, and (c) `PRAGMA table_info(action_proposal)` names. Add a comment stating why three copies exist: (b) is the compile-time lock, (c) is the runtime DDL, (a) is the reviewer-visible statement of intent — any two agreeing while the third disagrees is exactly the drift EMIT-02 must not survive.

    Add the replay test: build a record, `insert()` it, `insert()` it again with a different `belief_node_id`/`created_at`/`updated_at` but the same `id`, assert `SELECT COUNT(*)` is 1 and the surviving row matches the first insert field-for-field.

    Add a table-driven staleness test iterating all 8 boolean/expiry combinations against `classifyProposalStaleness`, asserting the locked verdicts and precedence.

    Add the `updateStatus` isolation test: snapshot the full row via `SELECT *`, call `updateStatus`, refetch, and assert every column except `status` and `updated_at` is `toEqual`-identical to the snapshot.

    Seed real `node` and `episode` rows before inserting proposals so the foreign keys resolve under `foreign_keys = ON` — do not disable the pragma to make the test pass.
  </action>
  <verify>
    <automated>npx vitest run tests/action-proposal-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/action-proposal-contract.test.ts` passes with at least 5 test cases.
    - The suite fails (verified by temporarily adding a 17th column to the DDL, running, and reverting) when DDL and `ACTION_PROPOSAL_FIELDS` disagree — record the observed failure message in the plan SUMMARY.
    - The test file contains a literal 16-name `FROZEN_PROPOSAL_FIELDS` array, not a value derived from `ACTION_PROPOSAL_FIELDS`.
    - No test in the file calls `db.pragma('foreign_keys = OFF')`.
  </acceptance_criteria>
  <done>The frozen contract is mechanically locked in three independent places, replay collapse is proven, and the staleness precedence is pinned by a table-driven test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sleep pass → `action_proposal` | Engine-internal write of a row derived from attacker-controlled email content |
| `action_proposal` → serve read path | Stored proposal data leaves the engine toward an external consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01-SQL | Tampering | `ActionProposalStore` statements | mitigate | Every statement uses named bound parameters (`@id`, `@status`, `@now`, `@limit`); acceptance criterion greps for zero `${` occurrences in the module |
| T-66-04 | Repudiation | `insert()` replay | mitigate | Deterministic content-hash TEXT PK + `INSERT OR IGNORE`; proven by the replay-collapse test rather than asserted in prose |
| T-66-07 | Denial of Service | `listPending()` | mitigate | Hard `PROPOSAL_LIST_LIMIT = 100` bound compiled into the statement's `LIMIT @limit`, defaulted rather than caller-supplied |
| T-66-03a | Tampering | `action-proposal-store.ts` write surface | mitigate | Module contains exactly one `UPDATE` statement, touching only `status`/`updated_at`; no `node`/`edge` write SQL and no `semantic-store` import (grep-enforced here, sentinel-enforced in 66-05) |
| T-66-09 | Tampering | schema migration | mitigate | Additive-only: brand-new table, zero `ALTER TABLE` on existing tables, `initSchema` idempotent, downgrade stamp guard untouched |
| T-66-SC | Tampering | package installs | accept | This phase installs no npm/pip/cargo package (net-zero new runtime dependencies holds; `sha256` comes from the shipped `node:crypto` wrapper in `src/lib/hash.ts`). The Package Legitimacy Gate has no packages to audit. |
</threat_model>

<verification>
1. `npm run typecheck` exits 0.
2. `npx vitest run tests/action-proposal-contract.test.ts` passes.
3. Full suite still green: `npm test` shows no new failures versus the pre-plan baseline (record both counts in the SUMMARY).
4. `PRAGMA table_info(action_proposal)` on an upgraded copy of a real v16 DB returns the 16 frozen columns and `meta.schema_version` reads 17.
</verification>

<success_criteria>
- `action_proposal` exists at schema v17 with exactly the 16 frozen columns and three indexes; no existing table touched.
- `src/db/action-proposal-store.ts` is the sole owner of `action_proposal` SQL, has one `UPDATE`, and imports no semantic-store write API.
- The EMIT-02 contract is locked three ways (literal list, `satisfies` map, live DDL introspection) and the lock is demonstrated non-vacuous.
- EMIT-04's replay collapse and EMIT-07's staleness precedence are both proven by executing tests, not documentation.
</success_criteria>

<output>
Create `.planning/phases/66-domain-neutral-proposal-emit-seam/66-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
