---
phase: 28-schema-anchored-corpus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - tests/schema-v12-migration.test.ts
  - tests/corpus-promoter.test.ts
  - tests/doc-gather.test.ts
  - tests/viz-corpus-graph.test.ts
autonomous: true
requirements: [CORPUS-03, CORPUS-04]
must_haves:
  truths:
    - "The edge.kind CHECK constraint accepts 'doc_containment' and 'doc_reference' (in addition to the existing 5 kinds)"
    - "doc_link remains a legal edge kind (NOT retired) — scope-doc cross-links still work"
    - "A v11 DB upgrades to v12 idempotently with PRAGMA foreign_key_check empty"
    - "Wave-0 test scaffolds exist for the three MISSING test files so downstream plans have a failing target to make green"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "v12 migration: edge.kind CHECK adds doc_containment + doc_reference; SCHEMA_VERSION 11→12"
      contains: "doc_containment"
    - path: "tests/schema-v12-migration.test.ts"
      provides: "v12 migration idempotency + FK-clean + doc_link-preserved tests"
    - path: "tests/corpus-promoter.test.ts"
      provides: "Wave-0 scaffold for CORPUS-02/03/05 (failing stubs the promoter plan makes green)"
    - path: "tests/doc-gather.test.ts"
      provides: "extended with gatherFactsForSchema Wave-0 scaffold (CORPUS-01)"
    - path: "tests/viz-corpus-graph.test.ts"
      provides: "extended with doc_containment/doc_reference endpoint Wave-0 scaffold (CORPUS-04)"
  key_links:
    - from: "src/db/schema.ts edge DDL"
      to: "CorpusPromoter.promote() (plan 03)"
      via: "edge.kind CHECK must permit doc_containment/doc_reference before promoter inserts them"
      pattern: "kind IN \\('relation','abstracts','schema_rel','cites','doc_link','doc_containment','doc_reference'\\)"
---

<objective>
Land the v12 schema migration that adds two new doc-corpus edge kinds — `doc_containment` (directed parent→child) and `doc_reference` (undirected) — to the `edge.kind` CHECK constraint, bump `SCHEMA_VERSION` 11→12, and create the Wave-0 test scaffolds the three downstream plans (promoter, gather, endpoint) will make green.

Purpose: SQLite cannot `ALTER` a CHECK constraint, so the new edge kinds require the table-recreation migration pattern. Every plan that writes `doc_containment`/`doc_reference` edges (plan 03) or renders them (plan 04) is blocked until the CHECK constraint permits them. This is the foundational Wave-1 task.

Output: `src/db/schema.ts` v12 migration block + `SCHEMA_VERSION = 12`; three Wave-0 test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-schema-anchored-corpus/28-SPEC.md
@.planning/phases/28-schema-anchored-corpus/28-RESEARCH.md
@.planning/phases/28-schema-anchored-corpus/28-VALIDATION.md

<interfaces>
<!-- Live v11 edge.kind CHECK (schema.ts:419) — the constraint being extended. -->
<!-- edge: kind TEXT NOT NULL CHECK(kind IN ('relation','abstracts','schema_rel','cites','doc_link')) -->
<!-- v11 migration pattern (schema.ts:406-429) — MIRROR THIS for v12: -->
<!--   guard via live DDL string check; foreign_keys=OFF; BEGIN/CREATE edge_vN/INSERT SELECT/DROP/RENAME/COMMIT; recreate idx_edge_dst; foreign_keys=ON -->
<!-- SCHEMA_VERSION (schema.ts:11) = 11 today; downgrade guard at schema.ts:446 throws if stored > SCHEMA_VERSION. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: v12 migration — add doc_containment + doc_reference edge kinds</name>
  <read_first>
    - src/db/schema.ts (the full file — especially SCHEMA_VERSION at line 11, the live edge DDL at line 63, the v11 edge-recreation guard at lines 406-429, and the version-stamp guard at 440-456)
    - 28-RESEARCH.md §"DDL Extension Shape (v12 Migration)" and §"Pitfall 5: FK Violation in v12 DDL Migration"
  </read_first>
  <files>src/db/schema.ts</files>
  <action>
    Bump `SCHEMA_VERSION` from 11 to 12 (line 11). Update the canonical edge DDL string (line 63) so a fresh DB is built with the extended CHECK: `kind TEXT NOT NULL CHECK(kind IN ('relation','abstracts','schema_rel','cites','doc_link','doc_containment','doc_reference'))`. Add a v12 edge-recreation migration block AFTER the v11 edge block (after line 429), mirroring the v11 pattern EXACTLY: read live edge DDL from `sqlite_master`; guard with `if (!edgeDdlV12.includes("'doc_containment'"))`; `db.pragma('foreign_keys = OFF')`; `db.exec` a `BEGIN; CREATE TABLE edge_v12 (... same columns as v11 but extended CHECK ...); INSERT INTO edge_v12 SELECT * FROM edge; DROP TABLE edge; ALTER TABLE edge_v12 RENAME TO edge; COMMIT;`; recreate `CREATE INDEX IF NOT EXISTS idx_edge_dst ON edge(dst);`; `db.pragma('foreign_keys = ON')`. Do NOT retire `doc_link` — it stays in the CHECK list (scope-doc→scope-doc links still use it per RESEARCH §"Note on doc_link"). No `node` table change is needed (node.type already includes 'doc' from v11). The version-stamp guard at the bottom handles the 11→12 stamp automatically (stored < SCHEMA_VERSION branch).
  </action>
  <verify>
    <automated>npx vitest run tests/schema-v12-migration.test.ts tests/schema-v11-migration.test.ts tests/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c "doc_containment" src/db/schema.ts` returns ≥2 (canonical DDL + v12 migration CREATE).
    - SOURCE: `grep "export const SCHEMA_VERSION" src/db/schema.ts` shows `= 12`.
    - SOURCE: the v12 edge CHECK list still contains `'doc_link'` (not retired): `grep "'doc_link','doc_containment','doc_reference'" src/db/schema.ts` matches.
    - BEHAVIOR: a fresh `:memory:` DB after `initSchema` permits an `INSERT INTO edge (...) VALUES (..., 'doc_containment')` and `... 'doc_reference'` without a CHECK failure; an insert with a bogus kind still throws.
    - BEHAVIOR: re-running `initSchema` on an already-v12 DB is a no-op (guard skips, no error).
    - BEHAVIOR: `PRAGMA foreign_key_check` returns empty after the migration runs on a DB pre-seeded with node + edge rows.
  </acceptance_criteria>
  <done>SCHEMA_VERSION=12; edge.kind CHECK accepts doc_containment + doc_reference and still accepts doc_link; migration is idempotent and FK-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Write v12 migration test + three Wave-0 scaffolds</name>
  <read_first>
    - tests/schema-v11-migration.test.ts (mirror its structure: seed a v10/v11-shaped DB, run initSchema, assert constraint + FK-clean + idempotency)
    - tests/doc-gather.test.ts (existing — extend, do not rewrite)
    - tests/viz-corpus-graph.test.ts (existing — extend, do not rewrite)
    - 28-VALIDATION.md §"Wave 0 Requirements" + §"Per-Task Verification Map" (the three MISSING files + their requirement coverage)
    - 28-RESEARCH.md §"Phase Requirements → Test Map"
  </read_first>
  <files>tests/schema-v12-migration.test.ts, tests/corpus-promoter.test.ts, tests/doc-gather.test.ts, tests/viz-corpus-graph.test.ts</files>
  <action>
    Create `tests/schema-v12-migration.test.ts`: build a v11-shaped edge table (kind CHECK without the two new kinds), seed node + edge rows, run `initSchema`, assert (a) `doc_containment`/`doc_reference` inserts now succeed, (b) `doc_link` insert still succeeds, (c) `PRAGMA foreign_key_check` empty, (d) second `initSchema` is a no-op, (e) `meta.schema_version` == '12'.
    Create `tests/corpus-promoter.test.ts` as a Wave-0 SCAFFOLD: import the (not-yet-existing) `CorpusPromoter` from `../src/consolidation/corpus-promoter` inside `describe.skip(...)` blocks (or `it.todo`) so the file is parseable but does NOT fail the suite. Stub three describe blocks named for CORPUS-02 (gate 15–60 + noise filter), CORPUS-03 (cosine+mass ladder yields ≥1 directed + reference edges between doc nodes), CORPUS-05 (source-schema snapshot unchanged). Plan 03 replaces the skips with real assertions.
    Extend `tests/doc-gather.test.ts`: add a `describe.skip('gatherFactsForSchema (CORPUS-01)')` Wave-0 stub referencing the not-yet-exported `gatherFactsForSchema`. Plan 02 unskips it. Leave all existing `gatherFacts` tests untouched (regression baseline).
    Extend `tests/viz-corpus-graph.test.ts`: add a `describe.skip('doc_containment/doc_reference edges (CORPUS-04)')` Wave-0 stub. Plan 04 unskips it.
    Use `describe.skip` / `it.todo` so `npx vitest run` stays GREEN after this plan (scaffolds are pending, not failing).
  </action>
  <verify>
    <automated>npx vitest run tests/schema-v12-migration.test.ts tests/corpus-promoter.test.ts tests/doc-gather.test.ts tests/viz-corpus-graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `npx vitest run` is GREEN (0 failing) — the three scaffolds are `skip`/`todo`, not red.
    - SOURCE: `tests/schema-v12-migration.test.ts` exists and asserts both new kinds + doc_link + FK-clean + idempotency + version stamp.
    - SOURCE: `tests/corpus-promoter.test.ts` exists with describe blocks naming CORPUS-02, CORPUS-03, CORPUS-05.
    - SOURCE: `grep -c "gatherFactsForSchema" tests/doc-gather.test.ts` ≥1; existing `gatherFacts` test count unchanged (regression baseline preserved).
    - SOURCE: `grep -c "doc_containment\|doc_reference" tests/viz-corpus-graph.test.ts` ≥1.
  </acceptance_criteria>
  <done>v12 migration test passes; three Wave-0 scaffolds present and skipped (suite green); downstream plans have failing targets to convert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | A botched table-recreation can drop the `edge` table and lose all relations (the .planning dir is gitignored; the live DB is the only copy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-FK | Denial of Service | v12 edge table-recreation in schema.ts | mitigate | Mirror v11 pattern: `foreign_keys=OFF` BEFORE the BEGIN/COMMIT swap, recreate idx_edge_dst after, `foreign_keys=ON`; guard on live DDL string for idempotency (RESEARCH Pitfall 5). Test asserts `PRAGMA foreign_key_check` empty post-migration. |
| T-28-DG | Tampering | downgrade-stamp by a stale binary | accept | Existing schema.ts:446 guard already throws when stored > SCHEMA_VERSION; v12 inherits it unchanged. |
</threat_model>

<verification>
- `npx vitest run tests/schema-v12-migration.test.ts` green.
- `npx vitest run` green overall (scaffolds skipped, not failing).
- Manual: on a WAL-safe copy of the canonical DB, run a binary built from this schema and confirm `PRAGMA foreign_key_check` is empty and `meta.schema_version` == '12'.
</verification>

<success_criteria>
- edge.kind CHECK permits doc_containment + doc_reference; doc_link retained; SCHEMA_VERSION=12.
- Migration idempotent + FK-clean.
- Three Wave-0 test scaffolds present; full suite green.
</success_criteria>

<output>
Create `.planning/phases/28-schema-anchored-corpus/28-01-SUMMARY.md` when done.
</output>
