---
phase: 28-schema-anchored-corpus
plan: 04
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/viz/server.ts
  - src/viz/modules/corpus.js
  - tests/viz-corpus-graph.test.ts
autonomous: false
requirements: [CORPUS-04]
must_haves:
  truths:
    - "GET /graph?type=doc returns schema-doc nodes + doc_containment + doc_reference edges (plus doc_link), with no dependency on doc_link-between-projects"
    - "Only edges whose both endpoints are live doc nodes are returned (no dangling refs)"
    - "The flat 2D corpus renders containment edges solid+directed (arrow) and reference edges faint+dashed+undirected (D-08)"
    - "Clicking a corpus node opens that doc in-place (reusing 27-03/27-05); a scope/project doc still appears as a degenerate node"
  artifacts:
    - path: "src/viz/server.ts"
      provides: "stmtDocLinks replaced to fetch doc_link + doc_containment + doc_reference between live doc nodes"
      contains: "doc_containment"
    - path: "src/viz/modules/corpus.js"
      provides: "link.kind-aware styling: linkColor/linkWidth/linkDirectionalArrowLength/linkLineDash by kind"
      contains: "doc_containment"
  key_links:
    - from: "/graph?type=doc endpoint"
      to: "corpus.js renderer"
      via: "LinkRecord.kind field (already in GraphPayload) carries doc_containment/doc_reference"
      pattern: "kind"
    - from: "corpus.js link styling"
      to: "vendored force-graph 1.43.5 API"
      via: "linkDirectionalArrowLength + linkLineDash (CONFIRMED present in src/viz/vendor/force-graph.min.js)"
      pattern: "linkDirectionalArrowLength|linkLineDash"
---

<objective>
Render the schema-anchored corpus (CORPUS-04, supersedes READER-04's project-doc `doc_link` corpus): replace the `/graph?type=doc` edge statement in `server.ts` so it returns `doc_link` + `doc_containment` + `doc_reference` edges between LIVE doc nodes, and make `corpus.js` style edges by `link.kind` — containment as a solid directed spine (arrow), reference as faint dashed undirected cross-links (D-08). Clicking a node still opens its doc in-place (27-03/27-05 reused unchanged); a scope/project doc still appears as a degenerate node.

Purpose: CORPUS-04 — corpus edges come from structure (the centroid-cosine + mass ladder built by plan 03), not project-name luck. The legible parent→child hierarchy is the flagship-viz payoff; it must not look like an undifferentiated hairball.

Output: modified `server.ts` (`stmtDocLinks` replacement), modified `corpus.js` (link-kind styling), extended `viz-corpus-graph.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-schema-anchored-corpus/28-SPEC.md
@.planning/phases/28-schema-anchored-corpus/28-RESEARCH.md
@.planning/phases/28-schema-anchored-corpus/28-CONTEXT.md

<interfaces>
<!-- src/viz/server.ts (CURRENT — the two statements + the /graph?type=doc handler): -->
<!--   stmtDocNodes (line 149-154): SELECT n.id,n.type,n.value,n.s,n.c,n.origin,n.tombstoned, nd.slug FROM node n JOIN node_doc nd ON nd.node_id=n.id WHERE n.type='doc' AND n.tombstoned=0   ← UNCHANGED (slug already works for schema docs, slug=schemaId) -->
<!--   stmtDocLinks (line 155-157): "SELECT src,dst,rel,w,kind FROM edge WHERE kind='doc_link'"   ← REPLACE -->
<!--   handler (line 326-348): if type==='doc' → nodes=stmtDocNodes.all(); edgeRows=stmtDocLinks.all(); links map keeps kind (line 346). -->
<!--   LinkRecord (line 57-64) already has `kind: string` — frontend contract LOCKED. -->

<!-- REPLACEMENT stmt (RESEARCH §"/graph?type=doc Endpoint Change Shape"): -->
<!--   SELECT src,dst,rel,w,kind FROM edge -->
<!--     WHERE kind IN ('doc_link','doc_containment','doc_reference') -->
<!--       AND src IN (SELECT id FROM node WHERE type='doc' AND tombstoned=0) -->
<!--       AND dst IN (SELECT id FROM node WHERE type='doc' AND tombstoned=0) -->

<!-- src/viz/modules/corpus.js (CURRENT styling — line 31, 121-122): -->
<!--   const LINK_REST = 'rgba(130,105,140,0.35)';  // muted mauve -->
<!--   .linkColor(() => LINK_REST).linkWidth(1)   ← REPLACE with kind-aware callbacks -->
<!--   onNodeClick → openDocReader(node.id) → ctx.openReader(slug,{from:'corpus'})  ← UNCHANGED -->

<!-- VENDORED force-graph API (CONFIRMED present in src/viz/vendor/force-graph.min.js — A4 resolved): -->
<!--   .linkDirectionalArrowLength(fn) , .linkDirectionalArrowRelPos(fn) , .linkDirectionalArrowColor(fn) , .linkLineDash(fn) -->
<!--   Palette rule (founder-locked): muted rose/slate/mauve at rest; amber ONLY on hover. No cyan. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /graph?type=doc returns doc_containment + doc_reference edges</name>
  <read_first>
    - src/viz/server.ts lines 137-157 (stmtNodes/stmtEdges/stmtDocNodes/stmtDocLinks) + lines 323-355 (the /graph handler, the type==='doc' branch, the LinkRecord map that preserves kind)
    - 28-RESEARCH.md §"/graph?type=doc Endpoint Change Shape" (the exact replacement statement; doc_link is PRESERVED, not retired)
    - tests/viz-corpus-graph.test.ts (existing — extend; the Wave-0 scaffold from plan 01 is here)
  </read_first>
  <files>src/viz/server.ts, tests/viz-corpus-graph.test.ts</files>
  <action>
    Replace the `stmtDocLinks` prepared statement (line 155-157) with the three-kind, both-endpoints-live-doc statement from the interface block: `SELECT src,dst,rel,w,kind FROM edge WHERE kind IN ('doc_link','doc_containment','doc_reference') AND src IN (SELECT id FROM node WHERE type='doc' AND tombstoned=0) AND dst IN (SELECT id FROM node WHERE type='doc' AND tombstoned=0)`. Leave `stmtDocNodes` UNCHANGED (slug already resolves for schema docs since slug=schemaId — Pitfall 4). Leave the handler's `type==='doc'` branch and the `LinkRecord` map (which already carries `kind`) UNCHANGED — the kind now flows through to the payload automatically. Do NOT retire `doc_link` (scope-doc cross-links still render). Unskip + flesh out the CORPUS-04 describe block scaffolded in plan 01: seed a DB with 3 doc nodes + a doc_containment + a doc_reference + a doc_link edge, hit `/graph?type=doc`, assert all three kinds appear in `links[].kind` and that an edge with a tombstoned-doc endpoint is excluded.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-corpus-graph.test.ts tests/viz-server.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c "doc_containment','doc_reference\|doc_reference','doc_containment\|'doc_link','doc_containment','doc_reference'" src/viz/server.ts` ≥1 (the replacement statement).
    - SOURCE: `stmtDocNodes` is unchanged (still JOINs node_doc for slug).
    - BEHAVIOR: `/graph?type=doc` payload `links` includes records with `kind` ∈ {doc_link, doc_containment, doc_reference} when those edges exist.
    - BEHAVIOR: an edge with a tombstoned doc endpoint is EXCLUDED from the payload (both-endpoints-live guard).
    - BEHAVIOR: the default `/graph` (full brain) is unchanged (existing viz-server tests green).
  </acceptance_criteria>
  <done>The corpus endpoint emits schema-doc nodes + doc_containment/doc_reference/doc_link edges between live doc nodes only; full-brain /graph unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: corpus.js link-kind styling (containment solid/directed; reference faint/dashed)</name>
  <read_first>
    - src/viz/modules/corpus.js (the WHOLE file — the ForceGraph() builder chain at line 87-130, LINK_REST at line 31, the palette constants, onNodeClick→openDocReader which stays unchanged)
    - 28-CONTEXT.md §"D-08" (containment solid/directed = hierarchy spine; reference faint/dashed/undirected; scope-doc woven via reference edges, not orphaned)
    - 28-RESEARCH.md §"D-08 Corpus Rendering" (the exact force-graph callbacks) and §"Assumption A4" (linkDirectionalArrowLength + linkLineDash — verified present in the vendored file, so use them directly; fallback only if a runtime error surfaces)
    - The founder-locked palette rule in CLAUDE.md / STATE.md: muted rose/slate/mauve at rest, amber ONLY on hover, never cyan; do NOT re-tune the brain density anchor (corpus is a separate flat graph)
  </read_first>
  <files>src/viz/modules/corpus.js, tests/viz-corpus-graph.test.ts</files>
  <action>
    Add a containment color constant in the muted palette (e.g. a slightly stronger slate/mauve than `LINK_REST`, still NOT amber — amber is hover-only). Replace `.linkColor(() => LINK_REST).linkWidth(1)` in the builder chain with kind-aware callbacks:
    `.linkColor(link => link.kind === 'doc_containment' ? CONTAINMENT_COLOR : LINK_REST)`,
    `.linkWidth(link => link.kind === 'doc_containment' ? 2 : 1)`,
    `.linkDirectionalArrowLength(link => link.kind === 'doc_containment' ? 4 : 0)`,
    `.linkDirectionalArrowRelPos(1)`,
    `.linkLineDash(link => link.kind === 'doc_reference' ? [2, 2] : null)`.
    Containment = solid (no dash) + directed (arrow at dst = child) + heavier; reference = faint + dashed + no arrow; doc_link keeps the existing faint mauve treatment (solid, no arrow). Leave `onNodeClick`/`openDocReader`/`nodeCanvasObject`/the hover logic UNCHANGED. Add a corpus.js unit/DOM-shape test (or extend viz-corpus-graph.test.ts) asserting the styling callbacks return the expected values per kind — if corpus.js is browser-only and hard to unit-test, assert the callback functions exist and branch on `link.kind` (source-level assertion) and rely on the manual verify below for the visual.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-corpus-graph.test.ts && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c "doc_containment" src/viz/modules/corpus.js` ≥2 (color + arrow + width branches).
    - SOURCE: `grep -c "linkDirectionalArrowLength\|linkLineDash" src/viz/modules/corpus.js` ≥1 each.
    - SOURCE: no amber/cyan introduced for rest-state links (`grep -niE "amber|ff8c00|cyan|00bfff" src/viz/modules/corpus.js` shows only pre-existing hover usage, not new link styling).
    - SOURCE: onNodeClick → openDocReader chain unchanged (the in-place open still works; CORPUS-04 "click opens doc in-place").
    - BEHAVIOR: dist/build succeeds (corpus.js is vendored into the served bundle if the build copies it).
  </acceptance_criteria>
  <done>Corpus renders containment as a solid directed heavier spine and reference as faint dashed cross-links, palette-compliant; node-click in-place open preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Hero verify — legible forest on the live brain (calibrate cosine threshold)</name>
  <read_first>
    - 28-VALIDATION.md §"Manual-Only Verifications" (legible forest, ≥1 parent→child nest, first render not dominated by dogfooding-noise)
    - 28-RESEARCH.md §"Pitfall 3" (WAL-copy gotcha — copy .db + -wal + -shm together or VACUUM INTO)
    - 28-CONTEXT.md §"Claude's Discretion" (tune the cosine threshold against the live brain; tighten if over-connected)
  </read_first>
  <what-built>
    The schema-anchored corpus end-to-end: plan 03's `recense promote-corpus` writes doc stubs + doc_containment/doc_reference edges on a WAL-safe copy of the canonical DB; this plan's endpoint + corpus.js render them. The founder reload-confirms the corpus is a LEGIBLE FOREST (solid directed containment spine + faint dashed reference cross-links), shows ≥1 visible parent→child nest, and is NOT dominated by dogfooding-noise schemas. The cosine threshold (set in plan 03) is tuned here against the live render.
  </what-built>
  <how-to-verify>
    1. Make a WAL-safe copy: `cp ~/.config/recense/recense.db /tmp/corpus28.db && cp ~/.config/recense/recense.db-wal /tmp/corpus28.db-wal 2>/dev/null; cp ~/.config/recense/recense.db-shm /tmp/corpus28.db-shm 2>/dev/null` (or use `VACUUM INTO`). The binary must be built from plan-01 schema (v12) + plan-03 promoter.
    2. Run `recense promote-corpus --db /tmp/corpus28.db` (subscription-billed only if prose is generated lazily later; promote itself is LLM-free).
    3. `recense viz --db /tmp/corpus28.db`, open the corpus view (the expanded-only #btn-corpus toggle from 27-05).
    4. Confirm: (a) containment edges are solid + arrowed; reference edges are faint + dashed; (b) ≥1 visible parent→child nest; (c) the promoted nodes are content-meaningful (NOT "Output file paths"/"Git commit hashes"/"Tool identifiers"); (d) it reads as a forest, not a hairball.
    5. If over-connected (hairball): RAISE `corpusCosineThreshold` in plan-03's CorpusPromoter constants and re-run. If too sparse / no nest: LOWER it. Record the final value in the SUMMARY.
    6. Click a node → confirm its doc opens in-place.
  </how-to-verify>
  <verify>
    <human-check>Founder confirms a legible forest with ≥1 parent→child nest, noise-free promoted set, correct edge styling, and working in-place node-click open.</human-check>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: corpus renders ≥1 directed parent→child containment nest on the live brain.
    - BEHAVIOR: containment vs reference are visually distinct (solid+arrow vs faint+dashed).
    - BEHAVIOR: the first render is not dominated by dogfooding-noise schemas (D-07 holds on live data).
    - BEHAVIOR: clicking a corpus node opens that doc in-place.
    - The final tuned `corpusCosineThreshold` (+ massGapMin) is recorded in the SUMMARY.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the corpus reads as a legible forest with ≥1 visible parent→child nest, distinct edge styling, a noise-free promoted set, and working node-click open — or describe what to retune (cosine threshold / mass gap).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB edges → /graph payload | a dangling edge (endpoint tombstoned/missing) would render a broken corpus node |
| served corpus.js → browser | loopback-only viz server (no remote exposure); XSS surface is node labels (slug) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-DANGLE | Tampering | /graph?type=doc edge payload | mitigate | The replacement statement filters both endpoints to live (tombstoned=0) doc nodes — no dangling refs reach the renderer; asserted by the tombstoned-endpoint exclusion test. |
| T-28-XSS | Tampering | corpus node label (slug) | accept | Loopback-only viz; slug is schemaId (UUID) or a project slug (alphanumeric) — canvas `fillText` is not an HTML sink (no innerHTML). Inherited Phase-27 posture. |
| T-28-VIZ | Information Disclosure | viz server read-only | accept | The endpoint is read-only (SELECT only); no write path added — consistent with the T-27-11 read-only server posture. |
</threat_model>

<verification>
- `npx vitest run tests/viz-corpus-graph.test.ts tests/viz-server.test.ts` green.
- `npm run build` succeeds.
- Manual hero verify (Task 3) on a WAL-safe live copy: legible forest, ≥1 nest, distinct styling, noise-free, node-click open.
</verification>

<success_criteria>
- /graph?type=doc returns doc_containment + doc_reference + doc_link between live doc nodes; full-brain /graph unchanged.
- corpus.js styles containment solid/directed and reference faint/dashed (palette-compliant); node-click in-place open preserved.
- Live render is a legible forest with ≥1 parent→child nest, not dominated by noise.
</success_criteria>

<output>
Create `.planning/phases/28-schema-anchored-corpus/28-04-SUMMARY.md` when done.
</output>
