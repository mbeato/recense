---
phase: 28-schema-anchored-corpus
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/consolidation/corpus-promoter.ts
  - src/adapter/promote-corpus-cli.ts
  - src/consolidation/consolidator.ts
  - src/consolidation/run-sleep-pass.ts
  - src/adapter/recense.ts
  - tests/corpus-promoter.test.ts
autonomous: false
requirements: [CORPUS-02, CORPUS-03, CORPUS-05]
must_haves:
  truths:
    - "An LLM-free SQL/COUNT mass gate + noise filter returns 15–60 schema candidates on the live brain, deterministic for a fixed snapshot"
    - "promote() derives doc_containment (directed parent→child, parent = larger evidence mass) + doc_reference edges between schema-DOC nodes using centroid-cosine similarity (D-01R), enriched past the ~12 schema_rel baseline among promoted schemas"
    - "≥1 directed parent→child containment edge exists after promote() on a representative seeded/live DB (forest, not hairball)"
    - "promote() writes ONLY type='doc' nodes and doc→doc edges; the source schema's s/c/incident-edge-weights/members are byte-for-byte unchanged after promote() (self-confirmation guard — BLOCKING)"
    - "promote() is idempotent (wipe-and-rebuild); a second run produces the same edge set"
    - "Hysteresis: promote at mass≥HIGH_MASS(10); tombstone a doc only when mass<LOW_MASS(7)"
    - "Prose stays lazy — promote() creates eager doc-node stubs only, never calls the LLM"
  artifacts:
    - path: "src/consolidation/corpus-promoter.ts"
      provides: "CorpusPromoter.promote() — gate → cosine+mass ladder → eager doc stubs → wipe+rebuild doc-corpus edges"
      contains: "class CorpusPromoter"
      min_lines: 120
    - path: "src/adapter/promote-corpus-cli.ts"
      provides: "recense promote-corpus [--db <path>] [--dry-run] — lock-guarded, WR-02 path validate"
      contains: "promote-corpus"
    - path: "src/consolidation/consolidator.ts"
      provides: "corpusPromoter DI param; promote() called in Phase C after deriveSchemaRelations(), before runEvictionSweep()"
      contains: "corpusPromoter"
  key_links:
    - from: "consolidator.ts Phase C"
      to: "CorpusPromoter.promote()"
      via: "called after this.deriver.deriveSchemaRelations(), before this.strength.runEvictionSweep()"
      pattern: "corpusPromoter\\.promote\\(\\)"
    - from: "CorpusPromoter doc-node stubs"
      to: "doc-writer.writeDoc"
      via: "lifecycle-exempt write (or upsertNode type='doc' s=0 + node_doc) — NEVER touches source schema"
      pattern: "type.*'doc'"
    - from: "doc-corpus edges"
      to: "edge.kind doc_containment/doc_reference"
      via: "src/dst are doc node ids only (D-03 self-confirmation by construction)"
      pattern: "doc_containment|doc_reference"
---

<objective>
Implement the D-04 idempotent `CorpusPromoter.promote()` pass — the heart of Phase 28 — as a new `src/consolidation/corpus-promoter.ts`, expose it as `recense promote-corpus`, and wire it into sleep-pass Phase C immediately after `deriveSchemaRelations()`. One transaction: (1) LLM-free mass gate + token-shape noise filter selects 15–60 doc-worthy schemas; (2) centroid-cosine ladder derivation (D-01R) builds `doc_containment` (directed, parent = larger evidence mass per D-06) + `doc_reference` edges among promoted schemas, enriching past the thin baseline; (3) eager doc-node stubs via the lifecycle-exempt writer; (4) wipe+rebuild the doc-corpus edges between DOC nodes only (D-03). Prose stays lazy.

Purpose: CORPUS-02 (mass-gated promotion + noise filter), CORPUS-03 (cosine+mass ladder enrichment + ≥1 parent→child nest), CORPUS-05 (read-only self-confirmation guard — the load-bearing correctness invariant). This plan implements the CRITICAL_ARCHITECTURE_CORRECTION: the spine is **centroid-cosine similarity** between schema member-centroids (the SREL-01 signal, enriched by lowering the threshold), with **direction from evidence mass** — NOT the falsified shared-evidence containment (which yields zero edges on the live brain).

Output: `corpus-promoter.ts`, `promote-corpus-cli.ts`, consolidator + run-sleep-pass wiring, recense.ts dispatch, fleshed-out `corpus-promoter.test.ts` (incl. the BLOCKING CORPUS-05 snapshot-diff test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-schema-anchored-corpus/28-SPEC.md
@.planning/phases/28-schema-anchored-corpus/28-RESEARCH.md
@.planning/phases/28-schema-anchored-corpus/28-CONTEXT.md
@.planning/phases/28-schema-anchored-corpus/28-VALIDATION.md

# Only after plan 02 lands (centroid-seeded gather shares the centroid math):
@.planning/phases/28-schema-anchored-corpus/28-02-SUMMARY.md

<interfaces>
<!-- CENTROID MATH to mirror — src/consolidation/schema-relations.ts:271-337 (SchemaRelationDeriver): -->
<!--   stmtGetClusterableNodes: SELECT id, origin, embedding FROM node WHERE tombstoned=0 AND origin!='inferred' AND type IN ('fact','entity') AND embedding IS NOT NULL  (D-37 firewall — REUSE) -->
<!--   stmtGetSchemaMembers: SELECT dst FROM edge WHERE src=? AND kind='abstracts' -->
<!--   decode: new Float32Array(buf.buffer, buf.byteOffset, buf.byteLength/4)  (Pitfall 5) -->
<!--   centroid = mean of member vecs (skip members not in clusterableById); cosineSimF32(a,b) from '../retrieval/topk' -->
<!--   config.schemaRelSimilarityThreshold is the EXISTING high threshold (~12 schema_rel) — the enrichment knob LOWERS/expands this for the corpus ladder; do NOT mutate the shared config field. -->

<!-- DOC WRITE — src/consolidation/doc-writer.ts: writeDoc(store, db, { docId, slug, markdown, citedFactIds, linkedDocRefs?, now }) -->
<!--   For an eager STUB: markdown='' is not allowed (generateDoc throws on empty) — write the stub via store.upsertNode({id, type:'doc', value:'', origin:'inferred', s:0, c:1.0}) + store.upsertNodeDoc({node_id, slug:schemaId, generated_at, updated_at}) + FTS delete, OR a doc-writer stub variant. The lazy /doc?slug=<schemaId> path regenerates prose on first open. Decide + document; the constraint is: NO embed, s=0, FTS-suppressed, slug=schemaId (Pitfall 4). -->

<!-- DI WIRING — src/consolidation/consolidator.ts: -->
<!--   constructor(...args, deriver = new NoopSchemaRelationDeriver())  // deriver is the LAST param at line 195 -->
<!--   Phase C (line 720-723): await this.deriver.deriveSchemaRelations();  this.strength.runEvictionSweep(); -->
<!--   ADD corpusPromoter as a new optional DI param AFTER deriver; call this.corpusPromoter.promote() BETWEEN those two lines. -->
<!--   run-sleep-pass.ts:341-356: const deriver = new SchemaRelationDeriver(db, store, config, realClock); new Consolidator(..., deriver); -->
<!--   ADD: const corpusPromoter = new CorpusPromoter(db, store, realClock, { highMass:10, lowMass:7, noiseCap:0.5, ...thresholds }); pass it as the new ctor arg. -->

<!-- CLI ANALOG — src/adapter/generate-doc-cli.ts: WR-02 (validate dbPath BEFORE acquireLock), acquireLock/releaseLock in finally, initSchema(db), require.main guard, --db via resolveSharedDbPath. recense.ts dispatches via spawnScript. -->

<!-- MASS + NOISE (RESEARCH §D-06/D-07): -->
<!--   mass: SELECT src, COUNT(DISTINCT dst) FROM edge WHERE kind='abstracts' AND dst IN (SELECT id FROM node WHERE type IN ('fact','entity') AND tombstoned=0) GROUP BY src -->
<!--   NOISE_PATTERNS (RESEARCH §Token-Shape): /^\/private\//, /^\/tmp\//, /^\/Users\//, /^toolu_[A-Za-z0-9]+$/, /^[Cc]ommit\s+[`]?[0-9a-f]{6,}/, /^worktreePath:/, /^\.claude\/worktrees/ -->
<!--   noise_frac = (noise members)/(total members); exclude when noise_frac >= NOISE_CAP(0.5). -->
<!--   Live gate: HIGH_MASS=10 → 17 candidates; LOW_MASS=7 → ~47. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CorpusPromoter — mass gate + noise filter + cosine+mass ladder + eager stubs + edge rebuild</name>
  <read_first>
    - src/consolidation/schema-relations.ts (the WHOLE file — CorpusPromoter is a structural sibling: constructor-compiled prepared statements, Phase A async-free centroid collection, Phase B single .immediate() transaction, wipe-and-rebuild derived-cache discipline, D-37 clusterable firewall, Pitfall-5 byteOffset decode)
    - src/consolidation/doc-writer.ts (writeDoc + the lifecycle-exempt upsert pattern — reuse for stubs; NEVER touch source schemas)
    - 28-RESEARCH.md §"D-06 Mass Computation", §"D-07 Noise Filter" + §"Token-Shape Regex Calibration", §"Pitfall 4" (slug=schemaId), §"Pitfall 6" (self-confirmation via promoter touching schema nodes), §"D-05 Hysteresis Band"
    - 28-CONTEXT.md §"D-01R/D-02R" (centroid-cosine spine + mass-direction — THE architecture) and the CRITICAL_ARCHITECTURE_CORRECTION
  </read_first>
  <files>src/consolidation/corpus-promoter.ts, tests/corpus-promoter.test.ts</files>
  <behavior>
    - Test (CORPUS-02 gate): on a seeded DB with schemas of varying mass, the gate selects only schemas with distinct-evidence-mass ≥ HIGH_MASS and noise_frac < NOISE_CAP; returns a count in 15–60 on a live-shaped fixture (or a scaled fixture asserting the predicate + determinism).
    - Test (CORPUS-02 noise): a schema whose members are 90% file-paths/tool-ids/commit-hashes (noise_frac ≥ 0.5) is EXCLUDED; a schema at 21% noise PASSES.
    - Test (CORPUS-02 determinism): two promote() runs on the same fixed snapshot produce the identical candidate set and identical edge set (LLM-free, deterministic).
    - Test (CORPUS-03 ladder): two promoted schemas whose member centroids exceed the corpus cosine threshold get a doc_reference (equal mass) OR a doc_containment (unequal mass, parent = larger mass) edge between their DOC nodes; ≥1 directed doc_containment edge exists on the multi-schema fixture.
    - Test (CORPUS-03 forest): each child keeps only its single strongest (highest-cosine) qualifying parent (no multi-parent hairball).
    - Test (CORPUS-05 BLOCKING — see Task 2): source schema s/c/edge-weights/members snapshot unchanged after promote().
    - Test (D-05 hysteresis): a schema with an existing doc stub and mass in [LOW_MASS, HIGH_MASS) keeps its doc; mass < LOW_MASS tombstones it.
    - Test (idempotency): second promote() = same edges (wipe-and-rebuild).
  </behavior>
  <action>
    Create `src/consolidation/corpus-promoter.ts` exporting `class CorpusPromoter` with constructor `(db, store, clock, opts: { highMass: number; lowMass: number; noiseCap: number; corpusCosineThreshold: number; massGapMin?: number; minMembers?: number })` and `async promote(opts?: { dryRun?: boolean }): Promise<{ promoted: string[]; containment: number; reference: number; tombstoned: number }>`. Compile all SQL as prepared statements in the constructor (T-01-SQL).
    PHASE A (async-free, read-only): (a) compute `mass(schemaId) = COUNT(DISTINCT dst)` over live fact/entity abstracts members (D-06 SQL above). (b) For each schema, compute `noise_frac` over member VALUES using the NOISE_PATTERNS regex set; exclude when `noise_frac >= noiseCap`. (c) Apply hysteresis: a schema is PROMOTED if `mass >= highMass`, OR (`mass >= lowMass` AND a live doc stub already exists for it). (d) Compute the per-schema member CENTROID reusing the SchemaRelationDeriver math VERBATIM (clusterableById from the D-37-gated query, byteOffset decode, mean, null when empty). (e) For each promoted-schema pair, `cosineSimF32(centroidA, centroidB)`; a pair is RELATED when sim ≥ `corpusCosineThreshold` (the enrichment knob — set LOWER than `config.schemaRelSimilarityThreshold` so usable relations among promoted schemas exceed the ~12 schema_rel baseline; do NOT mutate the shared config field — take it as an `opts` value). Apply the `minMembers` (~4) floor so tiny schemas aren't spuriously nested. (f) DIRECTION via D-06 mass: for a related pair with mass gap ≥ `massGapMin`, the larger-mass schema is the PARENT → a `doc_containment` candidate; keep only the child's single strongest (highest-sim) parent → a forest. Equal-mass / sub-gap related pairs → `doc_reference` candidates (also fold in any existing `schema_rel` + SREL-02 super-schema sibling pairs among promoted schemas as reference edges).
    PHASE B (one `db.transaction(...).immediate()`, NO await inside — T-02-ASYNC): for each promoted schema with NO live doc stub, create an eager stub (lifecycle-exempt: `store.upsertNode({id:<newId>, type:'doc', value:'', origin:'inferred', s:0, c:1.0, last_access:now})` + FTS delete + `store.upsertNodeDoc({node_id, slug:schemaId, generated_at:now, updated_at:now})` + `store.upsertNodeScope({node_id, scope:schemaId, updated_at:now})`). Tombstone doc stubs for schemas that fell below LOW_MASS. Then WIPE all `doc_containment` + `doc_reference` edges (`DELETE FROM edge WHERE kind IN ('doc_containment','doc_reference')`) and REBUILD from the Phase-A candidates, writing `store.upsertEdge({ src: parentDocId, dst: childDocId, rel:'doc_containment', kind:'doc_containment', w: sim, last_access: now })` for containment (src=parent DOC, dst=child DOC) and `kind:'doc_reference'` for reference pairs (both endpoints are DOC node ids — D-03). NEVER call strengthen/setEmbedding/upsertEdge on a source schema (Pitfall 6). `dryRun` returns the counts without writing.
    Add a `NoopCorpusPromoter` (mirrors `NoopSchemaRelationDeriver`) for the Consolidator default DI.
  </action>
  <verify>
    <automated>npx vitest run tests/corpus-promoter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c "class CorpusPromoter" src/consolidation/corpus-promoter.ts` == 1; `grep -c "class NoopCorpusPromoter" ...` == 1.
    - SOURCE: containment uses centroid-cosine + mass-direction, NOT shared-evidence intersection — `grep -niE "cosineSimF32|centroid" src/consolidation/corpus-promoter.ts` matches; there is NO `intersection`/`A.ev ∩ B.ev` shared-evidence ratio code.
    - SOURCE: edges are DOC→DOC only — `grep -n "doc_containment\|doc_reference" src/consolidation/corpus-promoter.ts` shows src/dst bound to doc node ids; no `upsertEdge` with a schema id as src/dst.
    - SOURCE: Phase B is a single `.immediate()` transaction with no `await` inside (T-02-ASYNC).
    - BEHAVIOR: gate predicate test green (mass≥HIGH ∪ hysteresis; noise_frac<CAP); on a live-shaped fixture candidate count ∈ [15,60].
    - BEHAVIOR: ≥1 doc_containment (directed) + ≥1 doc_reference edge on the multi-schema fixture; child has ≤1 parent (forest).
    - BEHAVIOR: idempotent — two promote() runs give identical edge sets.
    - BEHAVIOR: promote() makes ZERO LLM calls (no provider injected; stub-prose deferred to lazy gen).
  </acceptance_criteria>
  <done>CorpusPromoter selects 15–60 candidates (gate+noise+hysteresis), derives a centroid-cosine + mass-direction forest of doc_containment/doc_reference edges between doc nodes only, creates eager lifecycle-exempt stubs, and is idempotent + LLM-free.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: CORPUS-05 self-confirmation snapshot-diff test (BLOCKING)</name>
  <read_first>
    - src/consolidation/doc-writer.ts (the lifecycle-exemption invariants — the snapshot test asserts these hold under schema-anchoring)
    - 28-SPEC.md Req 5 acceptance criterion (snapshot s/c/incident-edge-weights/supporting-nodes; promote+nest+link; re-snapshot unchanged)
    - 28-RESEARCH.md §"Pitfall 6" + §"Security Domain" (D-43 — every CORPUS-05 test is a security gate; planner MUST make it blocking)
  </read_first>
  <what-built>
    A BLOCKING unit test in `tests/corpus-promoter.test.ts` that snapshots every source schema's `s`, `c`, incident edge weights (both directions), and its `abstracts` member set BEFORE `promote()`, runs `promote()` (gate + stubs + containment + reference), then re-snapshots and asserts byte-for-byte equality — proving the inferred doc output never strengthens or mutates its source (D-43 self-confirmation guard). The test also asserts: (a) the only NEW nodes are `type='doc'`; (b) the only NEW edges have kind ∈ {doc_containment, doc_reference, cites}; (c) `PRAGMA foreign_key_check` is empty after promote().
  </what-built>
  <how-to-verify>
    1. Run `npx vitest run tests/corpus-promoter.test.ts` — the CORPUS-05 snapshot-diff test must be GREEN.
    2. Confirm the test fails LOUDLY if a regression is introduced: temporarily add a `store.strengthen(schemaId)` call inside promote(), re-run, confirm the snapshot test goes RED, then revert.
    3. Confirm the test is NOT skipped/todo (it is an executing `it(...)`, not `it.skip`).
    4. Confirm `PRAGMA foreign_key_check` assertion is present and empty.
  </how-to-verify>
  <verify>
    <automated>npx vitest run tests/corpus-promoter.test.ts -t "self-confirmation"</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: the CORPUS-05 snapshot test asserts source-schema s/c/incident-edge-weights/member-set unchanged after promote().
    - BEHAVIOR: the test asserts new nodes are exclusively type='doc' and new edges are exclusively doc_containment/doc_reference/cites.
    - BEHAVIOR: `PRAGMA foreign_key_check` empty after promote().
    - BEHAVIOR: the test is executing (not skip/todo) and is verified to go RED under an injected strengthen() regression.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the CORPUS-05 snapshot-diff test is green, executing (not skipped), and verified to fail under an injected source-mutation regression.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: promote-corpus CLI + consolidator + sleep-pass wiring</name>
  <read_first>
    - src/adapter/generate-doc-cli.ts (the CLI template — WR-02 path-validate-before-lock, acquireLock/releaseLock in finally, initSchema, require.main guard, JSON result line, --db via resolveSharedDbPath)
    - src/consolidation/consolidator.ts lines 183-207 (constructor DI params) + lines 714-723 (Phase C — the insertion point)
    - src/consolidation/run-sleep-pass.ts lines 340-356 (deriver instantiation + Consolidator construction — add corpusPromoter alongside)
    - src/adapter/recense.ts (the command dispatch table — add `promote-corpus` → spawnScript('promote-corpus-cli.js', ...))
    - 28-RESEARCH.md §"D-04 Idempotent Promote-Corpus Pass" + §"Sleep-Pass Phase C Wiring Point" + §"Security Domain" (T-10-07 path traversal; reject non-UUID schema slug if a positional schema arg is accepted)
  </read_first>
  <files>src/adapter/promote-corpus-cli.ts, src/consolidation/consolidator.ts, src/consolidation/run-sleep-pass.ts, src/adapter/recense.ts</files>
  <action>
    CONSOLIDATOR: add `corpusPromoter: CorpusPromoter | NoopCorpusPromoter = new NoopCorpusPromoter()` as a new optional ctor param AFTER `deriver` (line 195); assign `this.corpusPromoter`. In Phase C insert `await this.corpusPromoter.promote();` BETWEEN `await this.deriver.deriveSchemaRelations();` and `this.strength.runEvictionSweep();` (line ~722). Insertion rationale (comment): promote() needs fresh schema_rel/super-schema edges from deriveSchemaRelations(), and must run before eviction so the new s=0 doc stubs are present (eviction leaves s=0 lifecycle-exempt nodes alone).
    RUN-SLEEP-PASS: after `const deriver = new SchemaRelationDeriver(...)` (line 341), add `const corpusPromoter = new CorpusPromoter(db, store, realClock, { highMass: 10, lowMass: 7, noiseCap: 0.5, corpusCosineThreshold: <the enrichment value validated in plan 03 Task 1>, massGapMin: <value>, minMembers: 4 });` and pass it as the new final arg to `new Consolidator(...)`.
    CLI: create `src/adapter/promote-corpus-cli.ts` mirroring generate-doc-cli structure: parse `--db` + `--dry-run`; validate dbPath via `resolveSharedDbPath(argv, {fallbackToDefault:false})` BEFORE `acquireLock()` (WR-02); on lock-held exit 0; `initSchema(db)`; `db.pragma('foreign_keys = ON')`; construct `SemanticStore` + `CorpusPromoter` (same constants as sleep-pass); call `await promoter.promote({ dryRun })`; emit a JSON result line `{promoted, containment, reference, tombstoned, dryRun}`; `releaseLock()` in finally; `require.main` guard. If a positional schema-id arg is supported, validate it is a UUID (reject otherwise — T-10-07 / V5).
    RECENSE DISPATCH: register `promote-corpus` in recense.ts so `recense promote-corpus [--db <path>] [--dry-run]` spawns the CLI (mirror the `generate-doc` registration).
  </action>
  <verify>
    <automated>npx vitest run tests/corpus-promoter.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c "corpusPromoter.promote()" src/consolidation/consolidator.ts` == 1, positioned after `deriveSchemaRelations()` and before `runEvictionSweep()` (verify line order).
    - SOURCE: `grep -c "new CorpusPromoter" src/consolidation/run-sleep-pass.ts` == 1 and it is passed to `new Consolidator(...)`.
    - SOURCE: `src/adapter/promote-corpus-cli.ts` validates dbPath BEFORE `acquireLock` (WR-02) and releases the lock in `finally`.
    - SOURCE: `grep -c "promote-corpus" src/adapter/recense.ts` ≥1 (command registered).
    - BEHAVIOR: `npx tsc --noEmit` clean (DI signature change compiles across all call sites).
    - BEHAVIOR: `npx vitest run` green overall (no Consolidator construction regressions — existing tests pass the Noop default).
  </acceptance_criteria>
  <done>promote() runs in sleep-pass Phase C after schema-relation derivation; `recense promote-corpus [--db] [--dry-run]` is lock-guarded + WR-02-safe; tsc + full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inferred doc output → source schema | THE dominant phase threat: a doc is inferred output; it must never strengthen the schema it renders (D-43) |
| CLI --db arg → filesystem | path-traversal / wrong-DB-mutation surface |
| promoter writes → derived edge cache | a non-idempotent or partial write corrupts the corpus graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-D43 | Elevation of Privilege | CorpusPromoter.promote() touching source schemas | mitigate | D-03 by construction: all writes are to type='doc' nodes + doc→doc edges only; the BLOCKING CORPUS-05 snapshot-diff test (Task 2) asserts source s/c/edge-weights/members unchanged and is verified to fail under an injected strengthen() regression. Pitfall 6. |
| T-28-PATH | Tampering | promote-corpus-cli --db arg | mitigate | Validate dbPath exists BEFORE acquireLock (WR-02, mirrors generate-doc-cli); reject non-UUID positional schema arg (V5 input validation, T-10-07). |
| T-28-TX | Denial of Service | partial edge wipe/rebuild on crash | mitigate | Single `db.transaction().immediate()` (T-02-ASYNC, no await inside); wipe+rebuild is atomic — a mid-pass crash leaves the prior derived cache intact or fully rebuilt next pass (SchemaRelationDeriver discipline). |
| T-28-DUP | Tampering | doc-stub thrash under auto-refresh | mitigate | D-05 hysteresis (HIGH=10/LOW=7): a stub is tombstoned only below LOW_MASS; flapping in the band keeps the earned doc — deterministic, no pass-to-pass flap. |
</threat_model>

<verification>
- `npx vitest run tests/corpus-promoter.test.ts` green (gate, noise, ladder, hysteresis, idempotency, and the BLOCKING CORPUS-05 snapshot test).
- `npx tsc --noEmit` clean.
- `npx vitest run` green overall.
- Manual (live, founder-gated, WAL-safe copy): `cp` the canonical DB + -wal + -shm (Pitfall 3) → `recense promote-corpus --db /tmp/corpus28.db --dry-run` reports 15–60 promoted + containment ≥1 + reference count; eyeball that the promoted set is content-meaningful (NOT dominated by "Output file paths"/"Git commit hashes"/"Tool identifiers").
</verification>

<success_criteria>
- Mass gate + noise filter returns 15–60 deterministic LLM-free candidates.
- Centroid-cosine + mass-direction ladder yields ≥1 directed parent→child containment + reference edges (forest, not hairball, past the ~12 schema_rel baseline).
- CORPUS-05 snapshot-diff test green, blocking, regression-verified.
- promote() idempotent, LLM-free, wired into Phase C + the CLI.
</success_criteria>

<output>
Create `.planning/phases/28-schema-anchored-corpus/28-03-SUMMARY.md` when done.
</output>
