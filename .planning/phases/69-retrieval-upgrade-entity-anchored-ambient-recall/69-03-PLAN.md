---
phase: 69-retrieval-upgrade-entity-anchored-ambient-recall
plan: 03
type: execute
wave: 2
depends_on: [69-01, 69-02]
files_modified:
  - src/adapter/ambient-recall.ts
  - src/adapter/turn-capture-cli.ts
  - src/db/semantic-store.ts
  - tests/ambient-recall.test.ts
autonomous: true
requirements: [RECALL-01, RECALL-02]
must_haves:
  truths:
    - "The ambient path knows which project the prompt came from — `cwd` is threaded from the hook payload into ambientRecall (the gap 69-PATTERNS identified; it does not exist today)"
    - "A fact reachable only by a proper noun in the prompt can occupy an injected slot even though its cosine is below AMBIENT_FLOOR (RECALL-01, D-03)"
    - "Scope changes ORDER only: it never gates existence, never enters the floor, and never changes a displayed score (D-01 — the bounded D-S1 reversal)"
    - "Cross-project recall is preserved — a foreign-scope fact is still injectable; only its rank position can move (D-01, F1 nuance)"
    - "With all knobs at their dark defaults the injected block is byte-identical to pre-phase (D-09)"
    - "`recense recall --scope` and every other getNodeScope(s) caller keep the original filter-never semantics; the carve-out is documented in place, not left to drift (D-01)"
  artifacts:
    - path: "src/adapter/ambient-recall.ts"
      provides: "cwd-aware ambientRecall: anchored-candidate union, reserved anchor slots, ordering-only scope nudge"
      contains: "entityAnchoringEnabled"
    - path: "src/adapter/turn-capture-cli.ts"
      provides: "cwd threaded from the UserPromptSubmit hook payload into ambientRecall"
      contains: "ambientRecall(db, promptText, provider, config, realClock, cwd)"
    - path: "src/db/semantic-store.ts"
      provides: "getNodeScopes doc-comment carve-out recording the D-01 bounded reversal"
      contains: "D-01"
  key_links:
    - from: "src/adapter/ambient-recall.ts"
      to: "src/retrieval/entity-anchor.ts"
      via: "collectAnchoredFacts, gated on config.entityAnchoringEnabled"
      pattern: "collectAnchoredFacts"
    - from: "src/adapter/ambient-recall.ts"
      to: "src/retrieval/engine.ts"
      via: "retrieveRanked opts.anchoredIds"
      pattern: "anchoredIds"
    - from: "src/adapter/turn-capture-cli.ts"
      to: "src/lib/scope.ts"
      via: "cwd → cwdToScope inside ambientRecall (session-start-cli.ts:134-136 precedent)"
      pattern: "cwdToScope"
---

<objective>
Wire entity anchoring and the bounded scope nudge into the ambient injected path, and close
the cwd-threading gap that makes "same project" knowable there for the first time.

Purpose: this is where RECALL-01 and RECALL-02's ranking half actually change what the founder
sees on a prompt. Both ride dark knobs (69-01) and are un-gated only by 69-06's eval run (D-09).
Rendering (doc title+link, hop lines, block budget) is deliberately NOT here — it is 69-04, so
this plan's diff stays reviewable and its behavior change is purely *which facts and in what order*.

Output: a cwd-aware `ambientRecall` that unions anchored facts, reserves slots for them, and
re-orders by an ordering-only scope nudge — with the D-01 carve-out documented at the
`getNodeScopes` source of truth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-retrieval-upgrade-entity-anchored-ambient-recall/69-CONTEXT.md
@.planning/phases/69-retrieval-upgrade-entity-anchored-ambient-recall/69-PATTERNS.md
@.planning/phases/69-retrieval-upgrade-entity-anchored-ambient-recall/69-01-SUMMARY.md
@.planning/phases/69-retrieval-upgrade-entity-anchored-ambient-recall/69-02-SUMMARY.md

<interfaces>
From 69-01 — `src/retrieval/entity-anchor.ts`:
```typescript
export interface AnchoredFact { id: string; value: string; entityId: string; entityValue: string; via: 'edge' | 'name-fts' }
export function collectAnchoredFacts(store: SemanticStore, resolver: EntityResolver, promptText: string): AnchoredFact[];
```

From 69-02 — `retrieveRanked` opts:
```typescript
opts?: { temporalAnnotate?: boolean; vizFloor?: number; anchoredIds?: string[];
         hopCollector?: (hops: Array<{ node_id: string; src: string; rel: string; score: null; hop: 1 }>) => void }
```

From 69-01 — new EngineConfig fields: `entityAnchoringEnabled: boolean`, `sameProjectRankNudge: number`, `foreignDocDemotion: number` (dark: false/0/0).

From src/lib/scope.ts: `cwdToScope(cwd: string | undefined): string` (pure), `GLOBAL_SCOPE = 'global'`. `/Users/vtx/resume` maps to `'global'` (PERSONAL_SLUGS).

From src/db/semantic-store.ts: `getNodeScopes(nodeIds: string[]): Map<string, string>` — nodes absent from the map are treated as `'global'`.

Existing ambient constants (src/adapter/ambient-recall.ts): `AMBIENT_K = 5`, `AMBIENT_FLOOR = 0.45`, `AMBIENT_VIZ_FLOOR = 0.25`, `MAX_VALUE_CHARS = 200`.

Current hook call site (src/adapter/turn-capture-cli.ts:118): `ambientRecall(db, promptText, provider, config, realClock)` — 5 args. `cwd` is already extracted at line 68 and passed to `pipeline.recordEvent`, but never to recall.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread cwd into ambientRecall and record the D-01 carve-out at the scope source of truth</name>
  <files>src/adapter/turn-capture-cli.ts, src/adapter/ambient-recall.ts, src/db/semantic-store.ts</files>
  <read_first>
    - `src/adapter/turn-capture-cli.ts` lines 61-128 (cwd extraction at :68, the recall call at :118)
    - `src/adapter/ambient-recall.ts` (whole file, 169 lines)
    - `src/adapter/session-start-cli.ts` lines 128-140 (the `retrieveCueless(cwd)` soft-scoping precedent to mirror in comment style)
    - `src/db/semantic-store.ts` lines 645-680 (`getNodeScope`, `getNodeScopes` and their D-S1 doc comments)
    - `src/lib/scope.ts` (whole file, 92 lines)
  </read_first>
  <action>
    Add a sixth optional parameter `cwd: string = ''` to `ambientRecall` (after `clock`), and pass the
    already-extracted `cwd` from `turn-capture-cli.ts:118`. Default `''` so every existing 5-arg caller
    and test compiles unchanged and derives `GLOBAL_SCOPE` (which makes the nudge inert — the correct
    conservative behavior for an unknown origin).

    Inside `ambientRecall`, derive `const currentScope = cwdToScope(cwd);` right after the collaborators
    are constructed, and import `cwdToScope`/`GLOBAL_SCOPE` from `../lib/scope`. Comment it in the
    `session-start-cli.ts:134-136` style, naming DEBT-06's precedent and stating that a `'global'`
    currentScope disables the nudge entirely.

    Move the existing `store.getNodeScopes(...)` batch read so it runs over ALL rows returned by
    `retrieveRanked` BEFORE any slicing (today it runs after `results.slice(0, AMBIENT_K)` at :144).
    One read serves both the existing `[slug]` display marker AND the new nudge — do not add a second
    read. Update the in-file comment at :140-143: keep the D-S6 provenance-surfacing explanation, and
    append that as of Phase 69 D-01 this map ALSO feeds a bounded ordering nudge on this path only.

    In `src/db/semantic-store.ts`, amend the `getNodeScopes` doc comment (and `getNodeScope`'s, if it
    carries the same claim) which currently reads "Display-only — this read happens AFTER ranking and
    never influences selection/order (D-S1)". Replace that sentence with an explicit, bounded carve-out:
    D-S1 still holds for existence and for every other caller (`RecallEngine.recall --scope`, corpus,
    viz); Phase 69 D-01 partially reverses it for `src/adapter/ambient-recall.ts` ONLY, where the map
    feeds an ordering-only rank nudge that never filters, never enters the cosine floor, and never
    changes a displayed score. Name the ambient-recall file explicitly so a future reader can find the
    single carve-out call site. Do NOT weaken the comment on any other method.
  </action>
  <acceptance_criteria>
    - `grep -c "cwd" src/adapter/ambient-recall.ts` >= 3 (param, derivation, comment)
    - `turn-capture-cli.ts` passes `cwd` as the 6th argument to `ambientRecall`
    - `grep -v '^\s*\*' src/db/semantic-store.ts | grep -c "getNodeScopes"` unchanged in count — only the doc comment above it changed, no signature or body edit
    - `grep -c "D-01" src/db/semantic-store.ts` >= 1 and the carve-out sentence names `ambient-recall`
    - Existing `tests/ambient-recall.test.ts` and `tests/recall-scope.test.ts` pass with no edits (the new param is defaulted)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/ambient-recall.test.ts tests/recall-scope.test.ts</automated>
  </verify>
  <done>The ambient path knows its project, reads scopes once for both display and ranking, and the D-S1 carve-out is recorded where the invariant is stated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Anchored-candidate union with reserved slots, and the ordering-only scope nudge</name>
  <files>src/adapter/ambient-recall.ts</files>
  <read_first>
    - `src/adapter/ambient-recall.ts` (as modified in Task 1)
    - `src/retrieval/entity-anchor.ts` (69-01) — `collectAnchoredFacts` contract and caps
    - `src/consolidation/entity-resolution.ts` lines 144-166 (the `EntityResolver` constructor DI shape to mirror when instantiating it here)
    - `.planning/phases/.../69-CONTEXT.md` D-01 and D-05 (nudge is bounded, never a filter; magnitudes are eval-tuned)
  </read_first>
  <behavior>
    - Test 1: knobs dark (`entityAnchoringEnabled:false`, both weights `0`) → the injected block is byte-identical to the pre-phase block for the same seeded graph.
    - Test 2: `entityAnchoringEnabled:true` with a seeded entity + an orthogonally-embedded connected fact → that fact appears in the block even though it cannot clear AMBIENT_FLOOR (the F2 acceptance case, end to end).
    - Test 3: anchored facts never displace more than ANCHOR_RESERVED_SLOTS of the AMBIENT_K slots — with 5 strong cosine hits and 4 anchored facts, exactly 3 cosine hits and 2 anchored facts are injected.
    - Test 4: `sameProjectRankNudge > 0` moves an own-project fact above a higher-cosine foreign fact, while BOTH remain present (nudge is not a filter — the D-01 boundary).
    - Test 5: displayed scores are the true cosines — the nudge magnitude never appears in the rendered `score X.XX` value.
    - Test 6: `foreignDocDemotion > 0` demotes a foreign-scope `type:'doc'` node below an own-project fact; a GLOBAL-scope doc is NOT demoted (global is not foreign).
    - Test 7: `AMBIENT_FLOOR` still gates non-anchored candidates — a below-floor unanchored node never appears regardless of knob state.
  </behavior>
  <action>
    Add two private module constants with 55-01-style doc comments:
      `const ANCHOR_RESERVED_SLOTS = 2;` — max injected slots an anchored (floor-exempt) fact may take,
      so an unsolicited low-cosine line can never crowd out the whole block (F5's precision argument);
      `const MAX_ANCHOR_PROBE_CHARS = MAX_QUERY_CHARS;` — reuse the existing prompt bound for tokenizing.

    ANCHORING (gated on `config.entityAnchoringEnabled`): before the `retrieveRanked` call, when the knob
    is on, construct an `EntityResolver(db, store, config)` in-place — same DI pattern as the
    `RetrievalEngine` construction already in this file (D-02 note (a): no new exports needed) — and call
    `collectAnchoredFacts(store, resolver, promptText.slice(0, MAX_ANCHOR_PROBE_CHARS))`. Wrap the whole
    anchoring block in try/catch: on ANY throw, log nothing, set `anchored = []`, and continue — the hook
    must never fail because anchoring failed (fail-open, mirrors the file's T-RT1-03 posture). Pass
    `anchoredIds: anchored.map(a => a.id)` in the `retrieveRanked` opts. Keep the 4th positional argument
    `undefined` — flipping it to `promptText` would silently re-enable `bm25FusionWeight` and violate D-04.
    Retain the `anchored` array in scope keyed by id (a `Map<string, AnchoredFact>`) — 69-04's renderer
    needs `entityValue` for the `via` provenance marker.

    NUDGE + SELECTION — replace the current `results.slice(0, AMBIENT_K)` with an explicit selection step:
      1. `const scopes = store.getNodeScopes(results.map(r => r.id))` (moved in Task 1).
      2. `rankScore(r) = r.score
            + (currentScope !== GLOBAL_SCOPE && (scopes.get(r.id) ?? GLOBAL_SCOPE) === currentScope
                 ? config.sameProjectRankNudge : 0)
            - (isForeignDoc(r) ? config.foreignDocDemotion : 0)`
         where `isForeignDoc(r)` is `store.getNode(r.id)?.type === 'doc'` AND its scope is neither
         `currentScope` nor `GLOBAL_SCOPE`. Reuse the `getNode` call the renderer already makes per row
         (cache node rows in a Map so each id is read once).
      3. Stable-sort a copy of `results` by `rankScore` desc, preserving original index order on ties —
         so with both weights `0` the order is byte-identical to `retrieveRanked`'s output (the dark
         guarantee).
      4. Select: take the top `AMBIENT_K - reserved` by rank order, where
         `reserved = Math.min(ANCHOR_RESERVED_SLOTS, count of anchored ids present in results)`; then fill
         remaining slots with the highest-ranked anchored rows not already selected; then, if slots remain,
         continue down the general rank order. Finally emit the selected rows in rank order.
    Document in a block comment above this step, in the file's threat-header prose style, that (a) the
    nudge is ORDERING-ONLY — `r.score` is untouched and is what gets rendered, (b) it can never remove a
    row (D-01: nudge, never filter), (c) `currentScope === GLOBAL_SCOPE` disables it, and (d) both
    magnitudes are eval-tuned in 69-06 and ship at 0.

    Add a new `T-RT1-06` bullet to the file's threat-mitigations header: entity anchoring is read-only,
    indexed-lookup-only, provider-free, fail-open, and bounded by the anchor module's caps.
  </action>
  <acceptance_criteria>
    - `grep -c "entityAnchoringEnabled\|sameProjectRankNudge\|foreignDocDemotion" src/adapter/ambient-recall.ts` >= 3
    - The `retrieveRanked` call still passes `undefined` as its 4th positional argument — `grep -c "retrieveRanked(vec, AMBIENT_K, AMBIENT_FLOOR, undefined" src/adapter/ambient-recall.ts` returns 1
    - No line in the file adds a nudge/demotion value into `r.score` or into the rendered score string
    - `store.getNodeScopes` is called exactly once per `ambientRecall` invocation — `grep -c "getNodeScopes" src/adapter/ambient-recall.ts` returns 1
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/ambient-recall.test.ts</automated>
  </verify>
  <done>Anchored facts reach the block through reserved slots and scope re-orders without filtering, all inert at dark defaults.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end ambient tests for anchoring, reserved slots, and the bounded nudge</name>
  <files>tests/ambient-recall.test.ts</files>
  <read_first>
    - `tests/ambient-recall.test.ts` (whole file — extend the existing `describe('ambientRecall', ...)` block, continuing the lettered-case convention past `e.`)
    - `tests/recall-scope.test.ts` (node_scope seeding pattern)
    - `src/adapter/ambient-recall.ts` (as modified in Task 2)
  </read_first>
  <action>
    Extend the existing `describe('ambientRecall', ...)` block with `it` blocks `f.` through `l.` covering
    every case in Task 2's `<behavior>` list. Reuse the file's existing `makeTempDbPath`, `FIXED_VEC`,
    `ORTHO_VEC`, `seedNode`, `setFlag` helpers verbatim; add a `seedScope(db, nodeId, scope)` helper that
    writes the `node_scope` sidecar (copy the mechanism from `tests/recall-scope.test.ts`).

    All prompts and node values must be hand-written synthetic text. The F2 end-to-end case (Test 2) uses
    a synthetic entity/fact pair in the same shape as 69-01's unit test — assert on the fact's value
    appearing in the returned block string, and assert the SAME call with `entityAnchoringEnabled:false`
    returns a block that does NOT contain it (the knob is the difference, proving the dark default).

    For Test 1 (byte-identity), capture the block string produced with `DEFAULT_CONFIG` and assert it
    equals the exact expected string, so any future default-flip that lands without an eval gate fails
    this test loudly.

    For Test 4/6, pass a `cwd` of `/Users/tester/projA` and seed `node_scope` rows of `projA`, `projb`,
    and `global`, asserting order changes and, critically, that the foreign row is still PRESENT in the
    block (D-01: never a filter). Add an explicit test name containing the words "never a filter".
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/ambient-recall.test.ts` passes; the file gains >= 7 new `it` blocks
    - One test name contains "never a filter" and asserts a foreign-scope row is still present in the block
    - One test asserts the rendered score equals the true cosine while the nudge is non-zero
    - The dark-default byte-identity test asserts against a hard-coded expected block string
    - Zero content copied from `scripts/eval/results/`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/ambient-recall.test.ts tests/entity-anchor.test.ts tests/retrieval-anchor-union.test.ts</automated>
  </verify>
  <done>Anchoring, reserved slots, ordering-only nudging, cross-project preservation, and the dark default are all locked end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UserPromptSubmit hook payload → ambientRecall | Untrusted `prompt` and `cwd` strings from the harness |
| ambientRecall → injected additionalContext | Engine output crossing into the agent's context window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-03-DOS | Denial of Service | added hook latency | mitigate | Anchoring is knob-gated, indexed-only (no dense scan), bounded by the anchor module's caps, and adds ZERO new network I/O (the single embed call is unchanged). Measured p50/p95 in 69-06 before the knob flips; the whole block is fail-open so a slow/failed anchor cannot block the prompt. |
| T-69-03-PATH | Spoofing | attacker-controlled `cwd` | mitigate | `cwdToScope` already refuses to treat any path not directly under a `/Users|/home/<user>/` root as a project (T-S3-01) and falls back to `'global'`, which disables the nudge. `cwd` is never used as a filesystem path here, only as a pure string derivation. |
| T-69-03-FILTER | Information Disclosure (loss of) | scope nudge over-reaching into a filter | mitigate | The nudge is a sort key only; selection never drops a row on scope grounds. Test-locked by the "never a filter" case. D-01's boundary is stated in code comments at both the call site and `getNodeScopes`. |
| T-69-03-PRECISION | Tampering | low-cosine anchored lines crowding the block | mitigate | `ANCHOR_RESERVED_SLOTS = 2` caps floor-exempt lines at 2 of 5; the anchor module's `ANCHOR_LEX_FLOOR` is the admission bar; the whole path ships dark until the eval gate passes. |
| T-69-03-SQLI | Tampering | prompt tokens reaching SQL/FTS | mitigate | Inherited from 69-01: prepared statements + `ftsQueryFromText` sanitizer only. This plan adds no SQL. |
| T-69-03-WRITE | Elevation of Privilege | read-only ambient invariant (T-RT1-05) | mitigate | No new write path; the only write on this path remains the flag-gated capped `activation_trace` ring. `EntityResolver` and `collectAnchoredFacts` are read-only by construction (69-01 grep gate). |
| T-69-SC | Tampering | package installs | mitigate | No installs. Net-zero new runtime dependencies. |
</threat_model>

<verification>
- `npm run typecheck` clean; `npx vitest run` full suite passes.
- `git diff src/db/semantic-store.ts` shows a doc-comment-only change (no signature or body edits).
- Manual read-back: with `DEFAULT_CONFIG` unchanged, the diff cannot alter runtime behavior — every new branch is behind `entityAnchoringEnabled` or a `0`-valued weight.
</verification>

<success_criteria>
- `cwd` reaches `ambientRecall`; `cwdToScope` derives the current project; scopes are read once.
- Anchored facts occupy at most 2 of 5 slots and can be floor-exempt.
- The nudge changes order only, never membership, never a displayed score; foreign facts stay injectable.
- Dark defaults reproduce the pre-phase block byte-for-byte, test-locked.
- The D-01 carve-out is written at `getNodeScopes` naming ambient-recall as the single exception.
</success_criteria>

<output>
Create `.planning/phases/69-retrieval-upgrade-entity-anchored-ambient-recall/69-03-SUMMARY.md` when done.
The SUMMARY MUST restate the D-01 D-S1 partial reversal verbatim in scope: ambient ranking only, nudge never filter, `recense recall --scope` unchanged.
</output>
