---
phase: 65-belief-gated-status-drift-provenance-distinctness-fix
plan: 09
type: tdd
wave: 6
depends_on: ["65-08"]
files_modified:
  - tests/emission-hold-sentinel.test.ts
  - tests/drift-belief-correction-e2e.test.ts
autonomous: true
requirements: [DRIFT-01, DRIFT-02, DRIFT-04]

must_haves:
  truths:
    - "A single ambiguous status email holds the belief instead of flipping it, and the pass emits no emission-eligible consolidation event for that node"
    - "The hold branch of the consolidator contains no emission-shaped call, proven by a comment-stripped source scan with a planted-offender check"
    - "Three genuinely independent status emails cross the distinct-provenance threshold and the belief IS corrected, through the unmodified PE machinery"
    - "Three forwards of one thread do NOT cross the threshold and the belief holds"
    - "A rejection email dated before an existing offer, ingested and consolidated afterwards, does not revert the offer"
    - "The same out-of-order pair in chronological order DOES apply — proving the guard discriminates on time, not on content"
    - "Every one of these outcomes is produced by a real consolidate() pass over real episodes, not by calling the decision helpers directly"
  artifacts:
    - path: "tests/emission-hold-sentinel.test.ts"
      provides: "D-13 sentinel — contradict_hold is unreachable from the emission seam, behaviorally and structurally"
      min_lines: 150
    - path: "tests/drift-belief-correction-e2e.test.ts"
      provides: "DRIFT-02 and DRIFT-04 proven end to end through real consolidate() passes"
      min_lines: 240
  key_links:
    - from: "tests/emission-hold-sentinel.test.ts"
      to: "src/consolidation/status-drift.ts"
      via: "isEmissionEligible applied to every consolidation_event row the pass produced"
      pattern: "isEmissionEligible"
    - from: "tests/drift-belief-correction-e2e.test.ts"
      to: "src/consolidation/consolidator.ts"
      via: "real consolidate() passes over seeded gmail episodes"
      pattern: "consolidate\\("
---

<objective>
Prove the phase's two behavioral success criteria end to end, and establish D-13's "a held update produces nothing downstream" structurally before Phase 66's sink exists.

Purpose: every prior plan proved a component. This plan proves the claims ROADMAP actually makes — that a single ambiguous email holds and produces nothing downstream (DRIFT-02), that genuine corroboration crosses the gate while forwarded duplication does not (DRIFT-03's behavioral consequence), and that out-of-order backfilled evidence cannot silently revert a newer status (DRIFT-04) — by running real `consolidate()` passes rather than by calling decision helpers.
Output: two end-to-end suites.

**Not in this plan:** no source changes of any kind. No `ActionProposalSink`, no `action_proposal` table, no `/v1/proposals` — Phase 66 owns all three and extends this plan's sentinel to the real sink.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-CONTEXT.md
@.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Live-verified 2026-08-02, plus the shipped Phase 65 plans. -->

From Plan 65-05 (`src/consolidation/status-drift.ts`):
- `EMISSION_ELIGIBLE_EVENT_TYPES: ReadonlySet<ConsolidationEventType>` = `{'contradict_reconcile', 'contradict_append_new', 'contradict_force_destabilize'}`
- `isEmissionEligible(eventType: ConsolidationEventType): boolean`

`src/consolidation/sink.ts` — twelve `ConsolidationEventType` members; the sink writes one `consolidation_event` row per `applyDecision` branch, inside the same per-episode transaction as the graph mutation (D-48/D-49).
`src/db/schema.ts:94-106` — `consolidation_event(id, ts, schema_version, event_type, node_id, candidate_id, episode_id, value, origin, magnitude, payload)`, plus `idx_consolidation_event_node` and `idx_consolidation_event_episode`.

`src/consolidation/consolidator.ts` (as wired by Plan 65-08):
- primary contradict branch ~1324-1520; the hold sub-branch ~1427-1515 whose only sink call is `event_type: 'contradict_hold'`
- secondary mirror `applySecondaryContradiction` ~1543-1630
- force-destabilization gate: `distinctCount >= (contradictionNBySource[source] ?? contradictionN)` at both distinct-count sites
- `DEFAULT_CONFIG.contradictionN` is `3`

Harness precedents — reuse, do not reinvent:
- `tests/consolidation-intent.test.ts` (563 lines) — in-memory DB + `initSchema` + `FakeClock` + `makeSyntheticEmbedFn`/`makeZeroEmbedFn`/`makeAlwaysSameEmbedFn` + the instance-level spy technique (`applyDecision` is a TS `private`, so subclassing collides)
- `tests/backfill-chronological-order.test.ts` — the content-keyed `DynamicReconcileProvider`: `generate()` branches on a marker substring in its prompt argument and THROWS on an unrecognized prompt; the judge resolves `best_candidate_id` from the live `candidates` array because the target node is minted at runtime. Its header explains why a call-order-indexed script produces a green test that proves nothing — that reasoning binds this plan too.
- `tests/no-ats-domain-table.test.ts` — exported pure predicate + real-source scan + planted-offender non-vacuousness check
- `tests/intent-conservation.test.ts:231-256` — `snapshotDb`
- `tests/status-drift-wiring.test.ts` (Plan 65-08) — the drift-layer spy and fixture conventions; extend the same fixture vocabulary rather than inventing a second one

From Plan 65-04: `deriveGmailProvenanceKey` — use it (or the real Gmail adapter) to produce provenance keys in fixtures rather than hand-writing key strings.
From Plan 65-03: `provenanceDistinctnessEnabled` defaults `false`; these end-to-end suites must set it `true` explicitly and say why in a comment (the dark default is the shipped stance; the suites test the enabled behavior the phase is delivering).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-13 sentinel — contradict_hold is unreachable from the emission seam</name>
  <files>tests/emission-hold-sentinel.test.ts</files>
  <read_first>
    - src/consolidation/status-drift.ts (the `EMISSION_ELIGIBLE_EVENT_TYPES` set and its block comment)
    - src/consolidation/consolidator.ts (lines 1427-1520 — the primary hold branch; lines 1582-1630 — the secondary hold branch; note every `sink.emit` call site in both)
    - src/consolidation/sink.ts (the event-type enum and the emit signature)
    - tests/consolidation-intent.test.ts (the harness to reuse)
    - tests/no-ats-domain-table.test.ts (the structural-guard shape for the source-scan half)
    - .planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-CONTEXT.md (D-13)
    - .planning/research/PITFALLS.md (Pitfall 7 — "never emit on contradict_hold", and the UX table row on hold-derived noise)
  </read_first>
  <behavior>
    - **Behavioral sentinel:** a single gmail episode carrying an ambiguous, low-confidence status contradiction against an established node produces, for that node, at least one `consolidation_event` row and ZERO rows for which `isEmissionEligible(event_type)` is true
    - **The belief did not move:** after that pass the candidate node is still live (not tombstoned), its `value` is unchanged, and exactly one new `pending_contradictions` entry exists
    - **The counterexample fires:** the SAME fixture repeated until the distinct-provenance threshold is crossed DOES produce an emission-eligible row (`contradict_force_destabilize`) — without this control the sentinel would pass on a harness that never routes anything
    - **Secondary hold:** a verdict listing a secondary contradicted id that routes to hold likewise produces zero emission-eligible rows for the secondary node
    - **Structural scan:** the comment-stripped bodies of the two hold sub-branches in `consolidator.ts` contain no call to any emission-shaped identifier (`emitProposal`, `proposalSink`, `ActionProposalSink`, `onDecisive`, `proposals.`) — a forward guard against Phase 66 wiring its sink into the wrong branch
    - **Set integrity:** `EMISSION_ELIGIBLE_EVENT_TYPES` does not contain `'contradict_hold'`, and its size is `3`
    - **Non-vacuousness:** a synthetic source string containing `proposalSink.emit(...)` inside a hold-branch-shaped block is flagged by the SAME predicate the real scan uses
  </behavior>
  <action>
Create `tests/emission-hold-sentinel.test.ts` with two halves.

**Half A — behavioral.** Reuse `tests/consolidation-intent.test.ts`'s harness and `tests/backfill-chronological-order.test.ts`'s content-keyed dynamic provider. Seed an established belief node (run a first `consolidate()` pass that mints it and strengthens it enough to have real resistance), then run a second pass with ONE gmail episode whose extracted claim carries `intent_confidence: 'low'` and contradicts it. Read every `consolidation_event` row for the candidate node out of SQLite and assert `rows.filter(r => isEmissionEligible(r.event_type))` is empty, importing the real predicate from `src/consolidation/status-drift`.

Assert the three "belief did not move" properties directly against the `node` row.

Then add the **counterexample**: repeat with three episodes carrying three genuinely distinct provenance keys until the threshold is crossed, and assert at least one emission-eligible row now exists. State in a comment that this control is what makes the sentinel non-vacuous — a harness that silently never reaches the contradict branch would pass the negative assertion trivially.

Add the **secondary-hold** case using a judge verdict whose `contradicted_ids` lists a second live node, and assert the same emptiness for that node.

**Half B — structural.** Export a pure predicate `findEmissionCallsInHoldBranches(source: string): string[]`. It must: strip `//` and `/* */` comments first (mandatory — the consolidator's own comments and this test's header discuss emission by name); locate the two hold sub-branches by their stable anchors (the `// action === 'hold'` comment in `applyDecision` and the `} else {` hold arm in `applySecondaryContradiction`, each followed to the end of its balanced block); and report any occurrence of `emitProposal`, `proposalSink`, `ActionProposalSink`, `onDecisive`, or `proposals.` inside them. Run it over the live `src/consolidation/consolidator.ts` read from disk and assert `[]`. Then plant a synthetic hold-branch-shaped source containing `proposalSink.emit(payload);` and assert the same predicate reports it.

If the anchors prove brittle to locate reliably, fall back to scanning the whole `applyDecision` and `applySecondaryContradiction` method bodies for those identifiers and assert zero occurrences anywhere in them — a stricter, simpler invariant. Record in the SUMMARY which form you shipped and why.

File header must state, and mark review-blocking:
  - This is D-13's structural establishment: "only decisive outcomes may ever feed an emission point" is enforced in THIS phase, before the sink exists, so Phase 66 inherits a guarded seam rather than creating one.
  - Phase 66 MUST gate its `ActionProposalSink` on the exported `isEmissionEligible` predicate and MUST extend this file's behavioral sentinel to the real sink rather than writing a parallel one.
  - Research Pitfall 7's reasoning: a hold is explicitly "not enough evidence yet"; surfacing it as a proposal is the low-value noise that trains an approver to stop reading, and approval fatigue is the milestone's documented UX failure mode.
  </action>
  <verify>
    <automated>npx vitest run tests/emission-hold-sentinel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/emission-hold-sentinel.test.ts` passes with at least 8 cases
    - The behavioral half imports the real `isEmissionEligible` from `src/consolidation/status-drift` and applies it to rows read back from SQLite (not to a synthetic list)
    - The counterexample case exists and asserts at least one emission-eligible row, so the negative assertions are provably non-vacuous
    - The structural predicate strips comments before matching and is proven by a planted offender through the same exported function
    - `EMISSION_ELIGIBLE_EVENT_TYPES.has('contradict_hold')` is asserted `false` and `.size` is asserted `3`
    - Mutation check, recorded in the SUMMARY: temporarily add a `sink.emit({ event_type: 'contradict_force_destabilize', ... })` call inside the primary hold branch, confirm the behavioral sentinel FAILS; revert
    - No file under `src/` is modified by this plan (`git diff --stat -- src/` is empty)
  </acceptance_criteria>
  <done>The D-13 sentinel ships in both forms — a behavioral assertion over real pass output with a firing counterexample, and a comment-stripped structural scan with a planted offender — and the mutation check confirms it bites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DRIFT-02 and DRIFT-04 proven end to end through real consolidate() passes</name>
  <files>tests/drift-belief-correction-e2e.test.ts</files>
  <read_first>
    - tests/backfill-chronological-order.test.ts (full file — the closest existing end-to-end belief-correction test; its fixture design notes explain exactly how salience, ordering, and reconcile-last-wins interact, and getting this wrong is how the fixture ends up proving nothing)
    - tests/consolidation-intent.test.ts (the harness and embed helpers)
    - tests/status-drift-wiring.test.ts (Plan 65-08 — reuse its fixture vocabulary and its provider conventions)
    - src/consolidation/consolidator.ts (as wired by Plan 65-08 — the contradict branch and the force-destabilization gate)
    - src/consolidation/status-drift.ts (the decision semantics being exercised)
    - src/source/provenance-key.ts (use the real derivation for fixture keys)
    - .planning/REQUIREMENTS.md (DRIFT-02 and DRIFT-04 wording)
    - .planning/ROADMAP.md (Phase 65 success criteria 2, 3, and 4)
    - .planning/research/PITFALLS.md (Pitfall 5 — its "Warning signs" list names exactly the fixture this task must build)
  </read_first>
  <behavior>
    - **DRIFT-02 hold:** one established status belief plus ONE ambiguous low-confidence contradicting gmail email → after `consolidate()`, the node is still live, its value unchanged, and one pending contradiction recorded
    - **DRIFT-02 release:** the same belief plus THREE genuinely independent status emails (three sender domains, three thread ids, real bodies) → after `consolidate()`, the belief IS corrected: the old node is tombstoned and a new node carries the new value with `prev_value` set to the old one
    - **DRIFT-03 behavioral consequence:** the same belief plus THREE forwards of one thread (three forwarding senders, three thread ids, pure-quotation bodies) → the belief is NOT corrected; the node is still live and unchanged
    - **Dark default:** the DRIFT-02-release fixture run with the stock `provenanceDistinctnessEnabled: false` does NOT correct the belief — this is the honest demonstration that the phase's differentiator is gated behind the switch the Plan 65-10 dry-run must clear
    - **DRIFT-04 no-revert:** an offer belief established from a gmail episode dated day 30, then a rejection email dated day 10 ingested and consolidated afterwards → the offer belief survives unchanged and no pending contradiction is recorded for it
    - **DRIFT-04 discriminates on time, not content:** the identical pair processed in chronological order (rejection at day 10 first, offer at day 30 second) DOES leave the offer as the surviving belief — proving the guard is temporal rather than a blanket suppression
    - **DRIFT-04 abstains honestly:** the same out-of-order pair where the rejection episode has `event_ts: null` falls back to pre-Phase-65 behavior rather than dropping
    - **DRIFT-01 machinery:** across every case above, `routeContradiction` and `isOscillation` are the unmodified shipped functions — assert by running `tests/pe-machinery-lock.test.ts`'s pinned check as part of this suite's verification rather than duplicating the pin here
  </behavior>
  <action>
Create `tests/drift-belief-correction-e2e.test.ts` covering every case in the behavior block. Every assertion must follow a real `consolidate()` pass over real seeded episodes — no direct calls to `StatusDrift.evaluate`, `routeContradiction`, or `countDistinctProvenance` in the assertion path. Those are proven in their own suites; this file's whole value is that it exercises the composition.

**Fixture discipline** (read `tests/backfill-chronological-order.test.ts`'s design notes before writing any of these):
  - Use a content-keyed dynamic provider that branches on a marker substring embedded in each episode's content and THROWS on an unrecognized prompt. A call-order-indexed queue would return the right verdict regardless of which physical episode was processed, and this codebase's "second-processed episode wins" reconcile semantics would then make the final-value assertion pass either way.
  - The judge must resolve `best_candidate_id` from the live `candidates` array, because the contradiction target is minted at runtime by an earlier pass.
  - Produce fixture provenance keys by calling the real `deriveGmailProvenanceKey` (or by driving the real Gmail adapter with fake-fetched messages) so a derivation regression surfaces here too.
  - Set `provenanceDistinctnessEnabled: true` in the harness config for every case EXCEPT the dark-default case, and comment why: the shipped default is `false` pending the Plan 65-10 dry-run, so these suites are testing the behavior the switch unlocks, and the dark-default case is what keeps that fact visible rather than buried.
  - For the DRIFT-04 cases, establish the prior belief with a REAL first pass over a dated gmail episode so the drift layer's staleness lookup exercises the genuine `consolidation_event` → `episode.event_ts` join, not a hand-inserted row.

Structure the file as three `describe` blocks — `DRIFT-02: a single ambiguous email holds`, `DRIFT-03 consequence: independence releases, forwarding does not`, and `DRIFT-04: out-of-order evidence cannot revert` — each opening with a comment quoting the requirement text it proves, verbatim from `.planning/REQUIREMENTS.md`, so a future reader can see the mapping without cross-referencing.

Assert belief state by reading `node` rows directly: `tombstoned`, `value`, `prev_value`, and the parsed `pending_contradictions` length. Assert the correction shape specifically — DRIFT-01 requires tombstone-and-mint-new, so a case that merely observes "the value changed" would also pass under an in-place rewrite the phase forbids.

File header must state which requirement each block proves, that every outcome comes from a real `consolidate()` pass, and that the dark-default case is deliberate honesty rather than a redundant duplicate.
  </action>
  <verify>
    <automated>npx vitest run tests/drift-belief-correction-e2e.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/drift-belief-correction-e2e.test.ts` passes with at least 10 cases
    - No assertion path calls `StatusDrift.evaluate`, `routeContradiction`, or `countDistinctProvenance` directly (`grep -c "routeContradiction\|countDistinctProvenance\|\.evaluate(" tests/drift-belief-correction-e2e.test.ts` returns `0`)
    - The provider is content-keyed and throws on an unrecognized prompt
    - Fixture provenance keys come from the real `deriveGmailProvenanceKey` or the real adapter, not hand-written strings
    - The DRIFT-02-release case asserts tombstone-and-mint-new specifically: old node `tombstoned = 1`, a new node exists with the new value, and that new node's `prev_value` equals the old value
    - The three-forwards case and the three-independent case differ ONLY in the message bodies and the sender domains — not in confidence, magnitude, or salience (verify by reading; if they differ in anything else, the test proves the wrong thing)
    - The dark-default case exists and asserts no correction under stock config
    - The DRIFT-04 chronological-order control exists and asserts the opposite outcome from the out-of-order case
    - The `event_ts: null` abstention case exists
    - Mutation check #1, recorded in the SUMMARY: temporarily set `statusDriftEventTsGuard: false`, confirm the DRIFT-04 no-revert case FAILS; revert
    - Mutation check #2, recorded in the SUMMARY: temporarily set `provenanceMinResidualChars: 0`, confirm the three-forwards case FAILS (the forwards now farm three provenances); revert
    - No file under `src/` is modified by this plan (`git diff --stat -- src/` is empty)
  </acceptance_criteria>
  <done>Ten-plus-case end-to-end suite green: a single ambiguous email holds, three independent emails correct the belief by tombstone-and-mint-new, three forwards do not, the stock dark default does not, out-of-order evidence cannot revert while the chronological control does apply, and both mutation checks confirm the guards are what produce those outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → confidence in the shipped guarantees | This plan produces no runtime code. Its threat surface is epistemic: a fixture that passes for the wrong reason converts a real defect into a green build, which is precisely how the `62-VERIFICATION.md` gap and threat T-62-36 ("a green test that proves nothing") were recorded in this codebase. |
| this phase's emission seam → Phase 66's sink | If `contradict_hold` becomes reachable from the emission point later, a not-enough-evidence event reaches a human approver and, after a rubber-stamped approval, an external system of record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-09-VACUOUS | Repudiation | Green tests that prove nothing | mitigate | Every negative assertion is paired with a firing counterexample (the threshold-crossing control for the sentinel; the chronological-order control for DRIFT-04; the three-independent control for the forwarding case). Providers are content-keyed and throw on unrecognized prompts, following the shipped `backfill-chronological-order.test.ts` remedy for exactly this defect. Four mutation checks are required and recorded. |
| T-65-09-HOLDEMIT | Information Disclosure / UX | `contradict_hold` reaching an emission point in Phase 66 | mitigate | Guarded twice: behaviorally over real pass output using the exported predicate, and structurally by a comment-stripped scan of the hold branches with a planted offender. The file header instructs Phase 66 to extend this sentinel rather than write a parallel one (research Pitfall 7). |
| T-65-09-REVERT | Tampering | Stale backfilled evidence reverting a newer status | mitigate | Proven end to end through a real two-pass `consolidate()` sequence whose prior belief is established by a genuine pass, so the staleness lookup exercises the real `consolidation_event` → `episode.event_ts` join. Mutation-checked by disabling the guard. |
| T-65-09-FARM | Tampering | Forwarded duplication crossing the force-destabilization threshold | mitigate | Proven end to end at the belief level, with the three-forwards and three-independent fixtures differing only in bodies and sender domains so the outcome is attributable. Mutation-checked by setting the residual threshold to `0`. |
| T-65-09-OVERCLAIM | Repudiation | Presenting the differentiator as shipped when it is behind a dark switch | mitigate | The dark-default case asserts that stock config does NOT correct the belief, keeping the gating visible in the test suite rather than only in a config comment. D-14/D-15's honesty discipline applied to the test layer. |
| T-65-SC | Tampering | npm/pip/cargo installs | N/A | Zero package installs. Test files only; no source change. Net-zero new runtime dependencies invariant holds. |
</threat_model>

<verification>
- `npx vitest run tests/emission-hold-sentinel.test.ts tests/drift-belief-correction-e2e.test.ts` — all pass
- `npx vitest run tests/pe-machinery-lock.test.ts tests/status-drift.test.ts tests/status-drift-wiring.test.ts tests/update-decision.test.ts tests/provenance-key.test.ts tests/strip-quoted.test.ts` — the whole Phase 65 test surface is green together
- `npx vitest run tests/consolidation.test.ts tests/consolidator.test.ts tests/consolidation-intent.test.ts tests/backfill-chronological-order.test.ts tests/intent-conservation.test.ts tests/sink.test.ts` — no collateral regression
- `npm run typecheck` exits 0
- `git diff --stat -- src/` is empty — this plan changes no source file
</verification>

<success_criteria>
Real `consolidate()` passes demonstrate that a single ambiguous status email holds and emits nothing emission-eligible, that three genuinely independent emails correct the belief by tombstone-and-mint-new through the unmodified PE machinery while three forwards of one thread do not, that stock dark config corrects nothing, and that out-of-order evidence cannot revert a newer status while the chronological control does apply — with every negative assertion paired to a firing counterexample and four mutation checks recorded.
</success_criteria>

<output>
Create `.planning/phases/65-belief-gated-status-drift-provenance-distinctness-fix/65-09-SUMMARY.md` when done. Record which structural-scan form Task 1 shipped and why, the observed belief states for each end-to-end case, and all three mutation-check results.
</output>
