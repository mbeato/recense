---
phase: 69-retrieval-upgrade-entity-anchored-ambient-recall
plan: 06
type: execute
wave: 4
depends_on: [69-03, 69-04]
files_modified:
  - scripts/eval/recall-audit-gate.cjs
  - scripts/eval/69-entity-anchor-latency.cjs
  - package.json
  - src/lib/config.ts
autonomous: true
requirements: [RECALL-05]
must_haves:
  truths:
    - "The 58-prompt memory-shaped eval set is replayed against the changed pipeline and graded on injected-line relevance — `ambient_hit` labels are treated as outcome observations, never ground truth (D-08; SC5)"
    - "All four hard gates are encoded and evaluated: contract-with-vtx class surfaces contract facts; foreign deep-dives no longer outrank own-project facts; zero regression on the 42 currently-hit prompts; token budget held (D-08, verbatim from the seed)"
    - "The gate fails CLOSED — a missing eval set, missing DB, or missing embed key exits non-zero rather than reporting a trivial pass (gate-accuracy-runner precedent)"
    - "No verbatim personal prompt is ever written to a gate output file or committed; gitignored JSONLs are read-only inputs (CLAUDE.md data rule; D-08)"
    - "A knob's default flips ON only because the gate passed with it on — never because the code merged (D-09)"
    - "The added hook latency of entity anchoring is measured (p50/p95) and reported honestly, not asserted away (D-03)"
  artifacts:
    - path: "scripts/eval/recall-audit-gate.cjs"
      provides: "Mode-guarded, fail-closed baseline/run replay harness encoding the four hard gates"
    - path: "scripts/eval/69-entity-anchor-latency.cjs"
      provides: "p50/p95 ambientRecall latency with anchoring off vs on, against the live graph"
    - path: "package.json"
      provides: "eval:recall-gate script entry"
      contains: "eval:recall-gate"
  key_links:
    - from: "scripts/eval/recall-audit-gate.cjs"
      to: "dist/src/adapter/ambient-recall.js"
      via: "ambientRecall(db, prompt, provider, configOverride, clock, cwd) replay per eval row"
      pattern: "ambientRecall"
    - from: "scripts/eval/recall-audit-gate.cjs"
      to: "scripts/eval/results/recall-audit/memory-shaped-evalset.jsonl"
      via: "--set, fail-closed when absent; never echoed into output"
      pattern: "memory-shaped-evalset"
---

<objective>
Build the eval gate that decides whether any of this phase's behavior ships live, run it, and
flip only the knobs it passes.

Purpose: D-08 is eval-FIRST and D-09 makes the gate — not the merge — the un-gate. The 58-prompt
set is the live-distribution test LoCoMo wasn't: real prompts, real graph, real misses. This plan
is where the phase either earns its defaults or records an honest null.

Output: a fail-closed gate runner, a latency probe, measured numbers, and a `DEFAULT_CONFIG` diff
containing exactly the knobs that passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-retrieval-upgrade-entity-anchored-ambient-recall/69-CONTEXT.md
@.planning/phases/69-retrieval-upgrade-entity-anchored-ambient-recall/69-PATTERNS.md
@.planning/seeds/SEED-005-retrieval-upgrade-recall-audit.md
@.planning/phases/69-retrieval-upgrade-entity-anchored-ambient-recall/69-03-SUMMARY.md
@.planning/phases/69-retrieval-upgrade-entity-anchored-ambient-recall/69-04-SUMMARY.md

<interfaces>
Eval-set row shape (one JSON object per line, produced by `scripts/eval/recall-audit-evalset.py`):
```json
{"project": "brain-memory", "session": "...", "ts": 0, "prompt": "...", "ambient_hit": true,
 "ambient_n": 3, "ambient_scores": [0.61, 0.52], "ambient_block": "...",
 "followed_by_search": false, "manual_recall_used": false}
```
58 rows; `project` values observed in the audit: resume, jobfill, brain-memory, VTX, aurevion.
Location: `scripts/eval/results/recall-audit/memory-shaped-evalset.jsonl` — **gitignored, verbatim
personal prompts, present locally (verified 2026-08-03: 58 lines)**. NEVER commit it, never copy
its contents into a test fixture, never echo a prompt into a gate output file.

Replay entry point (built to `dist/`): `ambientRecall(db, promptText, provider, config, clock, cwd)`
— the 6th `cwd` param arrives in 69-03. `cwdToScope('/Users/<u>/resume')` returns `'global'`
(PERSONAL_SLUGS), which is exactly the live behavior the replay must reproduce — derive
`cwd = '/Users/evaluser/' + row.project.toLowerCase()` so scoping matches production semantics
without touching the filesystem.

Knobs to sweep (69-01): `entityAnchoringEnabled`, `sameProjectRankNudge`, `foreignDocDemotion`,
`ambientDocLinkRenderEnabled`, `ambientHopInjectionEnabled`.

Budget constant (69-04): `AMBIENT_BLOCK_CHAR_BUDGET = AMBIENT_K * MAX_VALUE_CHARS` = 1000, measured
excluding the header line. Line grammar is recorded in 69-04-SUMMARY.md.

Live DB default: `~/.config/recense/recense.db` (`src/adapter/runtime-config.ts:23`).

Precedents: `scripts/eval/gate-accuracy-runner.cjs:37-84` (mode-guard + fail-closed baseline read),
`scripts/eval/drift-05-dry-run.cjs:1-60` (header Run:/requirements/gotchas structure),
`scripts/eval/live-latency.cjs` (percentile helper + warm-up-then-measure loop).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The fail-closed replay gate encoding the four hard gates</name>
  <files>scripts/eval/recall-audit-gate.cjs, package.json</files>
  <read_first>
    - `scripts/eval/gate-accuracy-runner.cjs` lines 1-90 (header, mode-guard, fail-closed input read)
    - `scripts/eval/drift-05-dry-run.cjs` lines 1-60 (header convention)
    - `scripts/eval/recall-audit-evalset.py` (the exact row keys and the PRIVACY note to carry forward)
    - `.planning/phases/.../69-04-SUMMARY.md` (the rendered line grammar the gate parses)
    - `.gitignore` lines 34-48 (results/ is ignored with an explicit `!` allowlist — the gate's output must NOT be added to that allowlist)
  </read_first>
  <action>
    Create `scripts/eval/recall-audit-gate.cjs`. Header doc comment follows the `drift-05-dry-run.cjs`
    structure: a `Run:` block, key requirements, and gotchas. State up front: this replays REAL personal
    prompts from a gitignored input, writes ONLY aggregate output, and is never part of `npm run gate`.

    Mode guard (copy `gate-accuracy-runner.cjs:37-54`): require exactly one of `--baseline` or `--run`;
    no mode → usage + `process.exit(1)`.

    Args: `--set <path>` (default the gitignored eval-set path), `--db <path>` (default `RECENSE_DB` then
    `~/.config/recense/recense.db`), `--out <path>` (default
    `scripts/eval/results/recall-audit/69-gate-<baseline|run>.json`), `--baseline-json <path>` (the
    baseline artifact `--run` compares against), `--nudge <n>` / `--demote <n>` (sweep overrides for the
    two rank weights).

    Fail closed, each with a distinct `GATE FAIL:` message and exit 1 (mirror
    `gate-accuracy-runner.cjs:76-84`): eval set missing or unparseable; DB file missing;
    `OPENAI_API_KEY` unset (the replay needs one embed per prompt — 58 embeds, the only network I/O);
    `--run` with a missing/unparseable baseline artifact; zero rows parsed. Never treat an absent input
    as a trivial pass.

    Replay loop, one row at a time, against a READ-ONLY-intent handle on the live DB (open normally but
    never write; assert at the end that `activation_trace` row count is unchanged — the viz flag should
    be off in this DB, and if it is on, note it in the output rather than mutating the flag):
      - `--baseline`: config = `{ ...DEFAULT_CONFIG, dbPath }` with all five Phase-69 knobs forced to
        their dark values, regardless of what `DEFAULT_CONFIG` currently holds. Baseline must be
        reproducible after the defaults flip in Task 3.
      - `--run`: same but with `entityAnchoringEnabled: true`, `ambientDocLinkRenderEnabled: true`,
        `ambientHopInjectionEnabled: true`, and the two weights from `--nudge`/`--demote`.
      - `cwd = '/Users/evaluser/' + row.project.toLowerCase()`.
      - Capture per row: `prompt_id` = first 12 hex chars of `sha256(prompt)`; `project`; line count;
        block char count excluding the header; per-line `{node_id, type, scope, score, is_doc,
        is_anchored, is_hop}` parsed from the rendered block per 69-04's grammar plus a direct
        `getNodeScopes`/`getNode` lookup on the ids; and `relevance`.
      - `relevance` (default grading, LLM-free and deterministic): mean over injected fact lines of the
        Dice overlap between the normalized prompt token set and the normalized line-value token set,
        reusing the same normalization the engine uses (`require('../../dist/src/consolidation/normalize.js').normalizeValue`).
        Document in the header that this is a lexical PROXY for relevance, not a judge, and that its
        absolute value is meaningless — only baseline-vs-run deltas on the same prompt are interpretable.
        Add an opt-in `--judge` flag that swaps in an LLM relevance grade; gate it behind the same
        explicit-paid-tier discipline `gate-accuracy-runner.cjs` uses and NEVER make it the default.

    Encode the four hard gates verbatim from D-08, each evaluated in `--run` mode against the baseline
    artifact, each reported individually, and ALL of them checked before exiting (never short-circuit —
    the operator needs the full picture):
      G1 CONTRACT-CLASS: rows whose prompt matches `/contract/i` AND `/vtx/i` must have >= 1 injected
         fact line whose value matches a committed contract-term regex
         (`/contract|ciiaa|agreement|employment|equity|compensation|salary|terms|signed/i`). Document
         the term list as a heuristic proxy for "surfaces contract facts" and print the matched
         prompt_ids (never the prompts). If the class has zero rows in the set, that is a GATE FAIL
         (the set is wrong), not a pass.
      G2 NO-REGRESSION: for every row with baseline line count >= 1, the run must have line count >= 1
         AND `relevance_run >= relevance_baseline - 0.01`. Report the count of qualifying rows and
         assert it equals the number of currently-hit prompts in the baseline (expected 42 — read it,
         do not hard-code it).
      G3 FOREIGN-DOC: in no run block may a line that is (`is_doc` AND scope not in
         {row project scope, 'global'}) appear ABOVE a line whose scope is in that set. Report every
         violating prompt_id.
      G4 BUDGET: every run block's char count excluding the header must be <= AMBIENT_BLOCK_CHAR_BUDGET
         (read the constant from `dist/src/adapter/ambient-recall.js`, do not duplicate the literal) and
         line count <= AMBIENT_K.

    Output JSON (aggregate only): run mode, git commit, timestamp, knob values used, row count,
    per-gate `{pass, detail}` where detail carries counts and `prompt_id` hashes, and distribution
    summaries (mean/median block chars, mean lines, mean relevance delta, count of blocks containing an
    anchored line). **Never write `prompt`, `ambient_block`, or any node `value` string into the output
    file.** State that rule in the header and add a final self-check before writing: reject the write
    (exit 1) if any string field in the output object exceeds 200 chars or matches any prompt in the
    input set. Write to a path under the already-gitignored `scripts/eval/results/` tree and do NOT add
    an `!` allowlist entry for it in `.gitignore`.

    Add `"eval:recall-gate": "npm run build && node scripts/eval/recall-audit-gate.cjs"` to
    `package.json` scripts (no mode flag — so the bare npm script prints usage and exits 1, matching
    the mode-guard intent).
  </action>
  <acceptance_criteria>
    - `node scripts/eval/recall-audit-gate.cjs` with no mode flag prints usage and exits 1
    - `node scripts/eval/recall-audit-gate.cjs --run --set /nonexistent.jsonl` prints a `GATE FAIL:` line and exits 1
    - `grep -c "prompt_id\|sha256" scripts/eval/recall-audit-gate.cjs` >= 2 and no code path writes `row.prompt` or `row.ambient_block` into the output object
    - The four gates appear as separately-reported named checks G1..G4 with the seed's wording quoted in comments
    - `.gitignore` is NOT modified; the output path resolves under `scripts/eval/results/`
    - `git status --porcelain scripts/eval/results/` shows nothing after a run
  </acceptance_criteria>
  <verify>
    <automated>npm run build && node scripts/eval/recall-audit-gate.cjs; test $? -eq 1 && node scripts/eval/recall-audit-gate.cjs --run --set /nonexistent.jsonl; test $? -eq 1</automated>
  </verify>
  <done>A mode-guarded, fail-closed, privacy-safe replay gate exists that encodes all four of D-08's hard gates and can never report a trivial pass.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the added hook latency honestly</name>
  <files>scripts/eval/69-entity-anchor-latency.cjs</files>
  <read_first>
    - `scripts/eval/live-latency.cjs` (percentile helper, warm-up-then-measure loop shape)
    - `scripts/eval/41-latency-after.cjs` (before/after comparison output shape)
    - `.planning/phases/.../69-01-SUMMARY.md` (the anchor module's caps, which bound the work being measured)
  </read_first>
  <action>
    Create `scripts/eval/69-entity-anchor-latency.cjs` following `live-latency.cjs`'s structure: warm up,
    then measure N repetitions (default 30, `--reps`) of `ambientRecall` against the live DB for a small
    committed set of SYNTHETIC probe prompts (hand-written, entity-heavy, never taken from the eval set),
    once with `entityAnchoringEnabled:false` and once with `true`, reporting p50/p95/max per arm and the
    delta.

    Isolate the embedding call from the measurement: pre-embed each probe once and inject a
    `MockModelProvider`-style stub that returns the cached vector, so the reported delta is the
    anchoring work, not OpenAI network jitter. Say so explicitly in the output and the header — the
    number's meaning depends on it.

    Require `--db <path>`; fail closed if absent or missing. Print a single-line honest summary suitable
    for pasting into the SUMMARY: `anchoring OFF p50/p95 = A/B ms; ON p50/p95 = C/D ms; delta p50/p95 =
    +E/+F ms over N reps x M probes on a graph of K live nodes`. Include the node count so the number is
    interpretable. No threshold assertion and no `process.exit(1)` on a slow result — this is a probe
    that reports, not a gate that guesses an acceptable budget (a threshold in a probe is a flaky test
    on dev-machine variance).
  </action>
  <acceptance_criteria>
    - `node scripts/eval/69-entity-anchor-latency.cjs` without `--db` exits 1 with a clear message
    - The script contains no `expect(`/assertion and never exits non-zero on a latency magnitude
    - Probe prompts are hand-written literals in the script; no read of `scripts/eval/results/`
    - Output includes live node count and both arms' p50/p95
  </acceptance_criteria>
  <verify>
    <automated>npm run build && node scripts/eval/69-entity-anchor-latency.cjs; test $? -eq 1</automated>
  </verify>
  <done>The hot-path cost of anchoring is measurable and reported with its methodology, per D-03.</done>
</task>

<task type="auto">
  <name>Task 3: Run the gate, tune, and flip only what passes</name>
  <files>src/lib/config.ts</files>
  <read_first>
    - `scripts/eval/recall-audit-gate.cjs` (Task 1) and `scripts/eval/69-entity-anchor-latency.cjs` (Task 2)
    - `src/lib/config.ts` `DEFAULT_CONFIG` block (lines ~961-1005) and the five Phase-69 knob comments from 69-01
    - `.planning/phases/.../69-CONTEXT.md` D-08 and D-09; `CLAUDE.md` no-inflated-metrics rule
  </read_first>
  <action>
    EXECUTION EXPECTATIONS — read before starting. This task consumes machine-local, gitignored data and
    one API key:
      - `scripts/eval/results/recall-audit/memory-shaped-evalset.jsonl` (58 rows) and
        `turn-records.jsonl` (1,942 rows) exist locally and are gitignored. If the eval set is absent,
        regenerate it with the committed scripts:
        `python3 scripts/eval/recall-audit.py` then `python3 scripts/eval/recall-audit-evalset.py`.
        NEVER `git add` either file, and never paste their contents into a plan, summary, test, or commit.
      - The live graph DB (`~/.config/recense/recense.db`) and `OPENAI_API_KEY` are required.
      - If any of these is genuinely unavailable, STOP: leave every knob dark, write the SUMMARY recording
        exactly which input was missing, and do NOT flip a default. A knob shipping on without a passing
        gate run is the one outcome D-09 forbids.

    Steps:
      1. `npm run build`.
      2. `node scripts/eval/recall-audit-gate.cjs --baseline --db <live db>` → baseline artifact.
      3. `node scripts/eval/recall-audit-gate.cjs --run --db <live db> --baseline-json <artifact>` with an
         initial `--nudge 0.05 --demote 0.10` (the score distribution compresses into 0.45-0.65 with
         median 0.52, so a 0.05 nudge is roughly a quarter of the usable spread — start there, do not
         guess larger). Sweep at least three (nudge, demote) pairs, e.g. (0.03,0.05), (0.05,0.10),
         (0.08,0.15). Choose the pair that passes G1-G4 with the best G2 relevance delta; on a tie prefer
         the SMALLER magnitudes (least perturbation of a working system).
      4. Run the latency probe and record p50/p95 for both arms.
      4b. (checker warning fix — G5 soft latency gate) Compute the p95 delta between arms. If anchored-arm
          p95 exceeds 2x the pre-phase arm's p95 (an outlier regression, not dev-machine noise — the probe
          reports both arms from the same warmed process), treat that as a fifth soft gate G5 FAILING for
          `entityAnchoringEnabled` specifically: keep that knob dark with an honest-null note citing the
          measured delta, mirroring the G1-G4 honest-null pattern. G5 gates only the anchoring knob; it
          never exits non-zero and never blocks the other knobs' independent decisions.
      5. Flip `DEFAULT_CONFIG` for exactly the knobs whose gates passed, appending to each knob's existing
         doc comment a one-line dated result (`Phase 69 D-08 gate 2026-08-XX: enabled at <value> —
         G1..G4 pass, relevance delta +X.XX on N rows`). A knob whose gate did not pass STAYS AT ITS DARK
         DEFAULT and gets an honest-null note in its comment, exactly as `bm25FusionWeight: 0` and
         `rankStrengthWeight: 0` do today. Per-knob decisions are independent — a G3 failure does not
         justify leaving anchoring dark if G1/G2/G4 passed for it.
      6. Record all numbers in the SUMMARY with methodology attached: sample size (58 prompts), that the
         relevance grade is a lexical proxy not a judge, that these are founder-personal single-user
         numbers, and the exact knob values shipped. No rounding up, no "significant improvement" prose
         without the delta beside it.

    Do NOT commit any file under `scripts/eval/results/`. Verify with `git status --porcelain` before
    committing that only `src/lib/config.ts` (and the two scripts + package.json from Tasks 1-2) are staged.
  </action>
  <acceptance_criteria>
    - The gate ran in both `--baseline` and `--run` modes and produced artifacts (paths recorded in the SUMMARY)
    - Every `DEFAULT_CONFIG` change is accompanied by a dated gate-result line in that knob's doc comment naming which of G1..G4 passed
    - Any knob whose gate did not pass remains at its dark default with an honest-null note
    - `git status --porcelain scripts/eval/results/` is empty; `git diff --cached --name-only` contains no `.jsonl`
    - `npx vitest run` passes after the default flip — including 69-03's byte-identity test, which must be UPDATED (not deleted) to pin the new shipped defaults, with its old expectation retained as an explicit dark-config case
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run && test -z "$(git status --porcelain scripts/eval/results/)"</automated>
  </verify>
  <done>Defaults reflect measured gate outcomes, honestly recorded, with no personal data committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gitignored personal transcripts → gate script | 1,942 verbatim personal prompts across 5 projects |
| gate output → repository | Anything written here is a commit candidate |
| gate → live production graph | The founder's real DB, opened by an eval script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-06-PII | Information Disclosure | gate output + commits | mitigate | Output carries only SHA-256 prompt_id prefixes and aggregate counts; a pre-write self-check rejects any output string over 200 chars or matching an input prompt; the output path stays under the already-ignored `scripts/eval/results/` with no `!` allowlist entry; Task 3's acceptance gate asserts `git status --porcelain scripts/eval/results/` is empty. |
| T-69-06-FAILOPEN | Repudiation | a gate that passes because inputs are missing | mitigate | Fail-closed on missing/unparseable eval set, missing DB, missing embed key, missing baseline, and zero parsed rows — each a distinct `GATE FAIL:` + exit 1 (gate-accuracy-runner precedent). G1 fails if the contract class is empty rather than vacuously passing. |
| T-69-06-INFLATE | Repudiation | overstated eval numbers | mitigate | Relevance is a documented lexical proxy, not a judge; absolute values are declared uninterpretable and only per-prompt deltas are used; SUMMARY must carry sample size, methodology, and single-user caveat (CLAUDE.md no-inflated-metrics). |
| T-69-06-WRITE | Tampering | eval script mutating the live graph | mitigate | The replay calls only `ambientRecall`, whose sole write is the flag-gated capped `activation_trace` ring; the gate asserts that row count is unchanged and never touches the `viz_trace_enabled` flag. No consolidation, no strengthen, no upsert. |
| T-69-06-PREMATURE | Elevation of Privilege | a default flipping without a passing gate | mitigate | Task 3's stop-condition is explicit: missing inputs → knobs stay dark and the SUMMARY records why. Each flip requires a dated per-gate result line in the knob's comment. |
| T-69-06-COST | Denial of Service | unintended paid LLM spend | mitigate | Default grading is LLM-free; `--judge` is opt-in behind the paid-tier discipline and is never wired into `npm run gate` or `npm run eval:recall-gate`. Replay cost is 58 embeddings per arm. |
| T-69-SC | Tampering | package installs | mitigate | No installs — the gate is plain Node CJS over `dist/`, matching every existing script in `scripts/eval/`. Net-zero new runtime dependencies. |
</threat_model>

<verification>
- `node scripts/eval/recall-audit-gate.cjs` (no mode) exits 1 with usage.
- Both fail-closed paths (missing set, missing DB) exit 1 with a `GATE FAIL:` message.
- Full `npx vitest run` passes after the default flip, with 69-03's identity test updated to pin the shipped defaults and retain a dark-config case.
- `git status --porcelain scripts/eval/results/` is empty; no `.jsonl` is staged.
- SUMMARY carries the four gate outcomes, the chosen knob values, the relevance deltas with sample size, and both latency arms.
</verification>

<success_criteria>
- The 58-prompt set is replayable against the changed pipeline with one command per arm.
- G1-G4 are individually reported; the gate cannot pass with a missing input.
- Every shipped default change traces to a dated gate result; unpassed knobs stay dark with an honest null.
- Added hook latency is measured p50/p95 with its methodology stated.
- Zero personal data leaves the machine-local gitignored tree.
</success_criteria>

<output>
Create `.planning/phases/69-retrieval-upgrade-entity-anchored-ambient-recall/69-06-SUMMARY.md` when done.
The SUMMARY MUST: (1) restate the D-01 D-S1 partial reversal and its bounds, (2) report G1-G4 individually with numbers and methodology, (3) list the exact `DEFAULT_CONFIG` values shipped and which stayed dark, (4) report both latency arms, (5) confirm no eval JSONL was committed.
</output>
