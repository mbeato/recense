---
phase: 69-retrieval-upgrade-entity-anchored-ambient-recall
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - src/lib/config.ts
  - src/retrieval/entity-anchor.ts
  - tests/entity-anchor.test.ts
autonomous: true
requirements: [RECALL-01]
must_haves:
  truths:
    - "RECALL-01..05 are registered in REQUIREMENTS.md with Phase-69 traceability rows (planner's first-task obligation, 69-CONTEXT.md domain block)"
    - "A distinctive proper-noun token in a prompt resolves to a live entity node and yields that entity's facts, with zero LLM calls and zero full-table scans (D-03)"
    - "Every new behavior knob ships with a default that reproduces pre-phase behavior exactly (D-09)"
    - "The anchor module cannot call a model provider — enforced at the type level, not by discipline (D-03, mirrors Phase 64 D-04)"
  artifacts:
    - path: "src/retrieval/entity-anchor.ts"
      provides: "extractAnchorTokens + collectAnchoredFacts — LLM-free indexed prompt-token → entity → facts anchoring (D-02/D-03)"
      exports: ["extractAnchorTokens", "collectAnchoredFacts", "AnchoredFact", "MAX_ANCHOR_TOKENS", "MAX_ANCHORED_ENTITIES", "ANCHOR_LEX_FLOOR", "ANCHOR_FACTS_PER_ENTITY", "MAX_ANCHORED_FACTS"]
    - path: "tests/entity-anchor.test.ts"
      provides: "The F2 'contract with vtx' regression: a name-matched fact surfaces with an ORTHOGONAL embedding"
    - path: ".planning/REQUIREMENTS.md"
      provides: "RECALL section (RECALL-01..05) + traceability rows"
      contains: "RECALL-01"
    - path: "src/lib/config.ts"
      provides: "Five dark knobs whose defaults reproduce pre-phase behavior (D-05/D-09)"
      contains: "entityAnchoringEnabled"
  key_links:
    - from: "src/retrieval/entity-anchor.ts"
      to: "src/consolidation/entity-resolution.ts"
      via: "EntityResolver.generateCandidates — reuse, never a second generator (D-02)"
      pattern: "generateCandidates"
    - from: "src/retrieval/entity-anchor.ts"
      to: "src/db/semantic-store.ts"
      via: "getEdgesForNode + getNode — indexed read-only traversal (D-03)"
      pattern: "getEdgesForNode"
---

<objective>
Register the phase's requirements, ship the five dark knobs, and build the LLM-free
prompt-token → entity → facts anchoring module that closes the audit's F2 whiff class.

Purpose: RECALL-01's mechanism lives in one standalone, provider-free module so the hot
path gains a *second indexed candidate channel* without a second divergent candidate
generator (D-02) and without a model call (D-03). Nothing is wired into the ambient path
in this plan — the module is pure capability, consumed by 69-03.

Output: `src/retrieval/entity-anchor.ts`, its test file, five `EngineConfig` knobs at
dark defaults, and the RECALL-01..05 registration in REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-retrieval-upgrade-entity-anchored-ambient-recall/69-CONTEXT.md
@.planning/phases/69-retrieval-upgrade-entity-anchored-ambient-recall/69-PATTERNS.md
@.planning/seeds/SEED-005-retrieval-upgrade-recall-audit.md

<interfaces>
<!-- Contracts the executor needs. Do NOT go exploring for these. -->

From src/consolidation/entity-resolution.ts (Phase 64 — the designated reuse seam):
```typescript
export type ChannelName = 'exact' | 'bm25' | 'dense';
export interface EntityCandidate {
  id: string; value: string; lex: number; dense: number; score: number; channels: ChannelName[];
}
export interface ChannelCounts { exact: number; bm25: number; dense: number }
export class EntityResolver {
  constructor(db: Database.Database, store: SemanticStore, config: EngineConfig);
  generateCandidates(opts: { text: string; vec?: Float32Array; nodeType?: string; k?: number }):
    { candidates: EntityCandidate[]; channelCounts: ChannelCounts };
  resolve(opts: { descriptor: string; vec?: Float32Array }): ResolutionResult;
}
export function lexicalScore(a: string, b: string): number; // Dice over normalizeValue tokens
```

From src/db/semantic-store.ts:
```typescript
getNode(id: string): NodeRow | null;                 // NodeRow has type, value, embedding, tombstoned
getEdgesForNode(nodeId: string): Array<EdgeRow>;      // BOTH directions; EdgeRow = {src,dst,rel,w,kind,last_access}
getOutEdgesWithRel(nodeId: string): Array<{ dst: string; rel: string; w: number; kind: string }>;
resolveEntityByName(name: string): string | null;     // called INTERNALLY by generateCandidates' exact channel
```

From src/consolidation/normalize.ts: `normalizeValue(s: string): string` (lowercased, punctuation-stripped, space-joined).
From src/model/typed-predicates.ts: `PRED_SET: Set<string>` — the LANDMINE-2 filter (`kind==='relation' && PRED_SET.has(rel)`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register RECALL-01..05 and ship the five dark knobs</name>
  <files>.planning/REQUIREMENTS.md, src/lib/config.ts</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 1-40 (section shape) and lines 107-155 (Traceability table + coverage footer)
    - `.planning/phases/69-retrieval-upgrade-entity-anchored-ambient-recall/69-CONTEXT.md` `<domain>` "Derived Requirements" block — the RECALL-01..05 text is authored there and must be transcribed, not re-derived
    - `src/lib/config.ts` lines 380-403 (`entityResolutionFloor`/`entityResolutionMargin` doc shape), 476-496 (`rankStrengthWeight`/`bm25FusionWeight` isolation-switch shape), 961-1005 (`DEFAULT_CONFIG` inline-comment convention)
  </read_first>
  <action>
    Add a `### RECALL — Entity-anchored ambient recall` section to `.planning/REQUIREMENTS.md`
    immediately after `### APPROVE`, containing RECALL-01..05 as unchecked `- [ ] **RECALL-0N**:`
    bullets whose text is transcribed from 69-CONTEXT.md's Derived Requirements block (including
    the parenthetical `(F2; SC1)`-style grounding tags). Prefix the section with a one-paragraph
    blockquote noting these are derived from SEED-005's 1,942-turn live audit and that Phase 69
    sits outside the v10.0 EMAIL→APPROVE chain (it is a retrieval-track phase promoted from a seed).
    Append five rows to the Traceability table (`| RECALL-01 | 69 | Planned |` … through RECALL-05)
    and update the coverage footer to state v10.0 = 30/30 plus 5 Phase-69 RECALL requirements
    tracked separately — do NOT inflate the v10.0 count.

    In `src/lib/config.ts`, add five fields to `EngineConfig` and matching `DEFAULT_CONFIG` entries.
    Place `entityAnchoringEnabled` adjacent to `entityResolutionFloor`/`entityResolutionMargin`; place
    the two rank weights adjacent to `rankStrengthWeight`/`bm25FusionWeight`; place the two render
    booleans adjacent to `insightSurfacingEnabled`. Each doc comment MUST carry, in the shape of the
    `bm25FusionWeight` comment: (1) `Phase 69 RECALL-0N (D-0M)` tag, (2) what it controls, (3) the
    exact dark default and why that value reproduces pre-phase behavior byte-for-byte, (4) that the
    eval gate in 69-06 — not a code merge — is what flips it (D-09).

      entityAnchoringEnabled: boolean       // default false  — RECALL-01, D-03/D-09
      sameProjectRankNudge: number          // default 0      — RECALL-02, D-01/D-05
      foreignDocDemotion: number            // default 0      — RECALL-02, D-01/D-05
      ambientDocLinkRenderEnabled: boolean  // default false  — RECALL-02 (F4 rendering), D-05
      ambientHopInjectionEnabled: boolean   // default false  — RECALL-03, D-06

    In the two numeric knobs' comments state explicitly that they are *ordering-only* adjustments:
    they never enter the cosine floor gate and never change a displayed score (that constraint is
    implemented in 69-03; the comment is the contract). In `entityAnchoringEnabled`'s comment, record
    the D-01 boundary verbatim in one line: scope becomes a rank nudge on the ambient path only,
    never a filter, and `recense recall --scope` semantics are unchanged.

    Do NOT add a knob for evidence mode — D-07 frames it as a caller-selected CLI flag (69-05).
  </action>
  <acceptance_criteria>
    - `grep -c "RECALL-0[1-5]" .planning/REQUIREMENTS.md` returns >= 10 (5 definitions + 5 traceability rows)
    - The Traceability table contains exactly one row per RECALL-01..05, each mapped to phase `69`
    - `grep -v '^\s*\*' src/lib/config.ts | grep -c "entityAnchoringEnabled\|sameProjectRankNudge\|foreignDocDemotion\|ambientDocLinkRenderEnabled\|ambientHopInjectionEnabled"` returns >= 10 (interface field + DEFAULT_CONFIG entry for each of the five)
    - `DEFAULT_CONFIG` sets the two booleans to `false` and both numeric knobs to `0`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && grep -c "RECALL-0[1-5]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md registers RECALL-01..05 with Phase-69 traceability rows; five knobs exist with dark defaults and D-tagged doc comments; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the LLM-free entity-anchor module</name>
  <files>src/retrieval/entity-anchor.ts</files>
  <read_first>
    - `src/consolidation/entity-resolution.ts` lines 1-60 (header contract + types) and 167-268 (`generateCandidates` body — note the dense channel is a FULL table scan gated on `opts.vec`)
    - `src/db/semantic-store.ts` lines 482-560 (`getOutEdgesWithRel`, `resolveEntityByName`, `getInEdges`, `getEdgesForNode`)
    - `src/retrieval/honest-trace.ts` lines 36-65 (the weight-desc + dst-asc deterministic ordering and liveness-before-slot discipline to mirror)
    - `src/consolidation/normalize.ts` (`normalizeValue` behavior)
  </read_first>
  <behavior>
    extractAnchorTokens (pure, no DB):
    - Test 1: `extractAnchorTokens('do you remember anything from the contract i have with vtx')` includes `'contract'` and `'vtx'` and excludes `'do'`, `'you'`, `'the'`, `'with'`, `'i'`.
    - Test 2: lowercase proper nouns survive — capitalization is NOT a filter (the audit's sharpest case is all-lowercase 'vtx'; a capitalization heuristic would fail it).
    - Test 3: adjacent bigrams of surviving tokens are emitted alongside unigrams (`'world triathlon'` recoverable from 'what were the world triathlon tiers').
    - Test 4: output is deduped, prompt-ordered, and capped at MAX_ANCHOR_TOKENS.
    - Test 5: tokens shorter than 3 chars and pure-numeric tokens are dropped.
    collectAnchoredFacts (indexed reads only):
    - Test 6: an entity node named 'vtx' plus a fact node connected by a `kind='relation'` PRED_SET edge yields that fact with `via:'edge'` and `entityValue:'vtx'`.
    - Test 7: a fact whose value contains the entity name but has NO edge is still returned with `via:'name-fts'`.
    - Test 8: a tombstoned fact is never returned.
    - Test 9: an entity candidate whose Dice score is below ANCHOR_LEX_FLOOR is rejected (no facts from it).
    - Test 10: results are deduped by fact id and capped at MAX_ANCHORED_FACTS; ordering is deterministic across repeat calls on identical input.
  </behavior>
  <action>
    Create `src/retrieval/entity-anchor.ts`. Header doc comment mirrors `entity-resolution.ts:1-49`:
    state (a) this is RECALL-01's indexed anchoring channel, (b) D-02 — it CONSUMES
    `EntityResolver.generateCandidates`, never reimplements candidate generation, (c) D-03 — the
    exported functions take no `ModelProvider` parameter, which is the TYPE-LEVEL guarantee of
    LLM-freeness, (d) read-only: no `upsertNode`/`upsertEdge`/`strengthen`/`tombstone`, no transaction.

    Exported module constants (private-magnitude convention per 55-01 — plain `export const`, not
    `EngineConfig` fields, because they are caps not behavior toggles; exported only so tests can
    assert the literals):
      MAX_ANCHOR_TOKENS = 12; MAX_ANCHORED_ENTITIES = 3; ANCHOR_LEX_FLOOR = 0.5;
      ANCHOR_FACTS_PER_ENTITY = 3; MAX_ANCHORED_FACTS = 6; ANCHOR_CANDIDATE_K = 5;

    Export `interface AnchoredFact { id: string; value: string; entityId: string; entityValue: string;
    via: 'edge' | 'name-fts' }`.

    `export function extractAnchorTokens(promptText: string): string[]` — pure. Run `normalizeValue`
    over the prompt, split on spaces, drop tokens with length < 3, drop pure-digit tokens, drop members
    of a module-level `STOPWORDS` Set. Author STOPWORDS as a curated single-purpose list of English
    function words plus the highest-frequency conversational/coding filler observed in the audit
    (pronouns, auxiliaries, determiners, prepositions, 'remember', 'anything', 'thing', 'file',
    'code', 'test', 'please', 'need', 'want', 'know'); keep it in one alphabetized literal with a
    comment saying it is a rarity proxy, not linguistics, and is eval-tuned in 69-06. Emit surviving
    unigrams in prompt order followed by adjacent-surviving-pair bigrams, dedupe, slice to
    MAX_ANCHOR_TOKENS. NO capitalization heuristic — record in a comment that the audit's sharpest
    case ('...contract i have with vtx') is entirely lowercase, so capitalization would falsify it.

    `export function collectAnchoredFacts(store: SemanticStore, resolver: EntityResolver,
    promptText: string): AnchoredFact[]`:
      1. tokens = extractAnchorTokens(promptText).
      2. For each token call `resolver.generateCandidates({ text: token, nodeType: 'entity',
         k: ANCHOR_CANDIDATE_K })` — pass NO `vec`. Comment the omission as load-bearing
         (T-69-01-DOS): the dense channel is an O(N) full-table embedding scan + decode over the whole
         node table and would blow the hook latency budget; exact + BM25 are indexed. Accept the
         highest-`score` candidate iff `score >= ANCHOR_LEX_FLOOR`; dedupe entity ids across tokens;
         stop after MAX_ANCHORED_ENTITIES accepted entities.
      3. Per accepted entity, edge channel: `store.getEdgesForNode(entityId)` filtered to
         `kind === 'relation' && PRED_SET.has(rel)`; the neighbour is the end that is not entityId;
         sort by `w` desc with a neighbour-id-asc tiebreak (mirror honest-trace.ts:50); skip neighbours
         whose `getNode()` is null or `tombstoned === 1` (liveness BEFORE the slot, honest-trace D-07
         Pitfall 2); take up to ANCHOR_FACTS_PER_ENTITY, `via:'edge'`.
      4. Per accepted entity, name channel: `resolver.generateCandidates({ text: entityValue,
         nodeType: 'fact', k: ANCHOR_CANDIDATE_K })` — again NO `vec`. This is what reaches facts that
         MENTION the entity without carrying an edge to it (the audit's contract facts). Keep
         candidates with `score > 0`, skip ids already taken, skip tombstoned, take up to
         ANCHOR_FACTS_PER_ENTITY by score desc with id-asc tiebreak, `via:'name-fts'`.
      5. Concatenate in (entity order, edge-before-name, then the per-channel order above), dedupe by
         fact id keeping the first occurrence, slice to MAX_ANCHORED_FACTS, return.
    Never return the entity nodes themselves (their value is just a name — wasted injected tokens).
    Every DB touch goes through `store`/`resolver`; no `db.prepare` in this file.
  </action>
  <acceptance_criteria>
    - `src/retrieval/entity-anchor.ts` exports `extractAnchorTokens`, `collectAnchoredFacts`, `AnchoredFact`, and all six caps
    - `grep -c "ModelProvider\|provider\.\(embed\|generate\)\|db\.prepare\|upsertNode\|upsertEdge\|strengthen\|tombstone(" src/retrieval/entity-anchor.ts` returns 0
    - Every `generateCandidates` call site in the file omits `vec` — `grep -A4 "generateCandidates({" src/retrieval/entity-anchor.ts | grep -c "vec"` returns 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && test 0 -eq "$(grep -c 'ModelProvider\|db\.prepare\|upsertNode\|upsertEdge' src/retrieval/entity-anchor.ts)"</automated>
  </verify>
  <done>The module resolves prompt tokens to entities and returns their facts through two indexed channels, with a type-level no-provider guarantee and no full-table scan.</done>
</task>

<task type="auto">
  <name>Task 3: The F2 regression test — a name-matched fact surfaces with an orthogonal embedding</name>
  <files>tests/entity-anchor.test.ts</files>
  <read_first>
    - `tests/recall-scope.test.ts` lines 1-51 (temp-DB + fixed-vec deterministic harness to copy)
    - `tests/ambient-recall.test.ts` lines 36-70 (`makeTempDbPath`, `FIXED_VEC`, `ORTHO_VEC`, seed helpers)
    - `src/retrieval/entity-anchor.ts` (written in Task 2)
  </read_first>
  <action>
    Create `tests/entity-anchor.test.ts` using the `recall-scope.test.ts` harness shape: temp FILE DB
    under `os.tmpdir()`, `initSchema`, `FakeClock`, `SemanticStore`, `EntityResolver` constructed with
    `{ ...DEFAULT_CONFIG, dbPath }`. SCRATCH DB only, zero network.

    Cover every case in this task's Task-2 `<behavior>` block as individual `it(...)` blocks inside two
    `describe` blocks (`extractAnchorTokens`, `collectAnchoredFacts`).

    The load-bearing case, named explicitly `F2 regression: the "contract with vtx" class`: seed an
    `type:'entity'` node valued `vtx`, a `type:'fact'` node valued
    `the ciiaa signed with vtx sets the contract terms` (SYNTHETIC — never copy a real prompt or a real
    fact from the gitignored eval set), give the FACT an embedding ORTHOGONAL to the query vector so it
    could never clear a cosine floor, and assert `collectAnchoredFacts(store, resolver, 'do you
    remember anything from the contract i have with vtx')` returns that fact id. Add a sibling
    assertion that the same prompt with the entity node removed returns `[]` (the anchor, not the
    lexical soup, is what produced the hit).

    Add a determinism case: two consecutive `collectAnchoredFacts` calls on identical input return
    identical id arrays. Add a cap case: seed 12 connected facts, assert `<= MAX_ANCHORED_FACTS`
    returned. Add a source-guard case asserting the module text contains no `ModelProvider` import
    (read the file with `fs.readFileSync` — mirrors the `tests/spontaneous-idle-activation.test.ts:199`
    source-grep precedent).
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/entity-anchor.test.ts` passes with >= 12 assertions-bearing `it` blocks
    - The F2 case fails if `collectAnchoredFacts` is stubbed to return `[]` (it asserts a non-empty result containing the seeded fact id)
    - No test string in the file originates from `scripts/eval/results/` — all prompts and fact values are hand-written synthetic text
    - The suite passes with no network access and writes only under `os.tmpdir()`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/entity-anchor.test.ts</automated>
  </verify>
  <done>The F2 whiff class is a shipped regression test: a fact reachable only by name surfaces despite an orthogonal embedding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user prompt → entity-anchor tokenizer | Fully untrusted free text, arriving on every keystroke-submitted turn |
| tokenizer → SQLite (FTS MATCH + prepared SELECTs) | Prompt-derived tokens reach the query layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01-SQLI | Tampering | `collectAnchoredFacts` → `generateCandidates` | mitigate | All SQL goes through `EntityResolver`'s prepared statements; FTS MATCH is reachable only via `ftsQueryFromText` (its own sanitizer, T-17-02-T). This module never calls `db.prepare` and never builds SQL — asserted by the Task-2 grep gate. |
| T-69-01-DOS | Denial of Service | hot-path anchoring cost | mitigate | `generateCandidates` is called WITHOUT `vec`, excluding the O(N) full-table embedding scan; work is bounded by MAX_ANCHOR_TOKENS (12 indexed probes) × MAX_ANCHORED_ENTITIES (3) × ANCHOR_FACTS_PER_ENTITY (3). Measured end-to-end in 69-06's latency probe. |
| T-69-01-WRITE | Elevation of Privilege | read-only contract | mitigate | No `upsertNode`/`upsertEdge`/`strengthen`/`tombstone`/transaction; no `ModelProvider` parameter anywhere in the module (type-level, mirrors Phase 64 D-04). Grep-asserted. |
| T-69-01-PII | Information Disclosure | test fixtures | mitigate | Test prompts and fact values are hand-written synthetic strings; the gitignored `scripts/eval/results/recall-audit/*.jsonl` is never read by or copied into a test. |
| T-69-01-DARK | Tampering | premature behavior change | mitigate | All five knobs default to the pre-phase value; this plan wires nothing into a live path, so merging it is a byte-for-byte no-op at runtime (D-09). |
| T-69-SC | Tampering | package installs | mitigate | This plan installs nothing — net-zero new runtime dependencies holds (STATE.md engine invariant). No `npm install` task exists; if one becomes necessary, planning stops for a legitimacy gate. |
</threat_model>

<verification>
- `npm run typecheck` clean.
- `npx vitest run tests/entity-anchor.test.ts` passes.
- `npx vitest run` full suite passes — this plan must not perturb any existing test (nothing it adds is called from a shipped path yet).
- `git diff --stat` touches exactly the four declared files.
</verification>

<success_criteria>
- RECALL-01..05 registered in REQUIREMENTS.md with five Phase-69 traceability rows.
- Five knobs exist at pre-phase defaults with D-tagged doc comments naming 69-06's eval gate as the un-gate.
- `entity-anchor.ts` returns entity-anchored facts through indexed channels only, provably provider-free.
- The F2 "contract with vtx" class is a passing regression test against an orthogonal embedding.
</success_criteria>

<output>
Create `.planning/phases/69-retrieval-upgrade-entity-anchored-ambient-recall/69-01-SUMMARY.md` when done.
Restate in the SUMMARY: the D-01 D-S1 partial reversal recorded in the `entityAnchoringEnabled`/rank-knob comments, and the exact dark defaults shipped.
</output>
