---
milestone: v10.0
audited: 2026-08-09T00:00:00Z
reaudited: 2026-08-09 (post BUILD-01 fix)
status: tech_debt
scores:
  requirements: 30/30
  phases: 8/8
  integration: 8/8
  flows: 8/8
gaps:  # Critical blockers
  requirements: []
  integration: []
  flows: []
resolved_gaps:
  - id: "BUILD-01"
    status: "resolved"
    phase: "69-retrieval-upgrade-entity-anchored-ambient-recall"
    description: "`npm run typecheck` failed at 2e90557: tests/ambient-recall.test.ts(574,12) TS2532 under noUncheckedIndexedAccess, introduced by phase-69 review-fix commit a5dd60e (WR-04)."
    resolution: "Fixed via quick task 260809-1n3 (commit ec9d6e1, merged 7650e00): non-null assert on betaMentions[0] in the WR-03 dedup test. Re-verified at HEAD 7650e00: `npm run typecheck` green (both tsconfig projects), tests/ambient-recall.test.ts 33/33 passing. Only code delta since the original audit is this one-line test fix — no src/ changes, so the 2026-08-09 integration-checker results (8/8 seams) remain valid."
  - id: "SEAM-08-E2E"
    status: "resolved"
    phase: "milestone-wide"
    description: "Integration-checker WARNING: no single test file exercised all 8 E2E stages (gmail raw bytes -> telegram button) in one continuous run; the chain was proven only by composition across drift-belief-correction-e2e.test.ts (stages 1-6), proposal-reference-e2e.test.ts/telegram-belief-e2e.test.ts (stages 6-8), and action-proposal-emission.test.ts (the emit->store join) — a regression exactly at the emit->store boundary could theoretically slip between the composed suites."
    resolution: "Closed via quick task 260809-2qe: tests/full-chain-e2e.test.ts drives one fixture Gmail contradiction through all 8 stages (gmail raw bytes -> normalize/strip -> ingest pipeline -> consolidation -> emission -> store -> /v1/proposals -> Telegram approval) in a single continuous `it`, real seams throughout (GmailAdapter/runPullPhase/IngestionPipeline/Consolidator/SQLiteConsolidationSink/SQLiteActionProposalSink/createBrainHttpServer/Telegram belief-bridge), LLM calls stubbed exactly as the composed suites already stub them. The emit->store join is a first-class assertion (recomputed proposalId, evidence_episode -> episode -> gmail external_id, byte-identical evidence_quote, and emitted-id === listed-id === approved-id conservation across the HTTP seam). Non-vacuity confirmed: deliberately breaking the evidence_episode assertion made the test fail, then reverted. `npm run typecheck` green; full suite 238 files / 4021 passed, 6 expected-fail, 33 skipped, no new failures."
  - id: "DIST-SUITE"
    status: "resolved"
    phase: "all (62-69)"
    description: "~23-25 pre-existing full-suite vitest failures across CLI-subprocess test files that spawn compiled CLIs from dist/, which fresh worktrees never build. Documented independently in every phase's deferred-items.md."
    resolution: "Closed via quick task 260809-1vg (commit 4a20210): shared tests/support/dist-build.ts gate; the 8 affected files (adapter-capture, adapter-inject, eval-harness-smoke, locomo-scorer, locomo-harness, locomo-latency-curve, drift-05-harness-smoke, episodic-dryrun-gate) now skip with a clear message when dist/ is absent. Full suite unbuilt: 0 failed (was 24); built tree: 0 new skips. Side-finding for triage: tests/recense-viz-no-open.test.ts gates on a stale pre-rename dist path and has been silently skipping regardless of build state."
  - id: "STRIP-HIDDEN-PERF"
    status: "resolved"
    phase: "62-multi-inbox-email-ingest-hardening"
    description: "tests/strip-hidden.test.ts absolute wall-clock perf assertions flake under full-suite parallel load (observed in 62-30, 64-01, 68-02, 69-01 runs)."
    resolution: "Closed via quick task 260809-1vg (commit 6e52633): all 17 absolute bounds converted to the WR-07/gmail-hidden-content calibration-relative form (elapsed <= 10 x max(referenceElapsed, 1) against a same-size benign-body baseline; observed benign/reference ratios 0.02-1.52, guarded regressions historically 10^2-10^4x, so the bound still fires). Growth-ratio assertions, timeMin helper, and timeouts untouched."
  - id: "WR-03"
    status: "resolved"
    phase: "65-belief-gated-status-drift-provenance-distinctness-fix"
    description: "stripQuotedForwarded violated its documented idempotence guarantee for >=4-space-indented quote lines and leaked indented quoted bodies when the attribution line was indented (both defects erred toward MORE residual — the farming direction)."
    resolution: "Closed via quick task 260809-2fo (TDD: 13 RED regression cases in commit 9d8348b, GREEN fix in b5bbc1f): indent-tolerant [ \\t]* anchors on all 4 line regexes plus a whitespace-preserving trim replacing .trim(). Idempotence now holds structurally. 156/156 strip/provenance tests pass; deriveGmailProvenanceKey residual handling verified unaffected; full suite green."
  - id: "WR-01-COMMENT"
    status: "resolved (comment); decision residue remains with founder"
    phase: "65-belief-gated-status-drift-provenance-distinctness-fix"
    description: "provenance-key.ts:19 rationale comment overstated the farming bar (claimed N sender domains; real bar = one sender domain + N freely-mintable Gmail threads)."
    resolution: "Comment corrected in provenance-key.ts and gmail-adapter.ts via quick task 260809-2fo (commit b49bef4, comment-only, grep-gated zero executable-line change). The threadId-in-key vs raising contradictionNBySource.gmail decision is NOT made here — it remains part of the founder's provenanceDistinctnessEnabled enablement review, per the audit's original scoping."
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases: ["62", "63", "64", "65", "66", "67", "68", "69"]
  overall: missing
tech_debt:
  - phase: 62-multi-inbox-email-ingest-hardening
    items:
      - "EMAIL-02 scoping is backfill-only (users.history.list accepts no q param) — honestly documented + surfaced by recense doctor. Deferred alternative: labelId-based incremental enforcement via per-account Gmail labels, revisit if backfill-only proves insufficient."
  - phase: 65-belief-gated-status-drift-provenance-distinctness-fix
    items:
      - "HUMAN GATE (designed pre-decision state, not an oversight): provenanceDistinctnessEnabled stays dark pending founder review of the DRIFT-05 dry-run harness against a real multi-inbox export (`npm run eval:drift05 -- --inbox <path>`), recording ENABLE/HOLD in a 65-10-SUMMARY.md revision. Pre-enablement code items WR-01 (comment) and WR-03 (idempotence) are now RESOLVED (see resolved_gaps); the remaining enablement-review items are the DRIFT-05 dry-run itself and the threadId-in-key vs contradictionNBySource.gmail decision (or explicit residual-risk acceptance)."
  - phase: 69-retrieval-upgrade-entity-anchored-ambient-recall
    items:
      - "RECALL-01 ACCEPTED-NULL: entityAnchoringEnabled ships dark. Three gate runs fail G2 relevance (lexical 20/53 then 16/51 regressed; LLM-judge re-gate 2026-08-07 fails harder, 30/54, mean judged relevance 0.70->0.55, refuting the mean-dilution hypothesis). Next attempt must revisit anchoring SELECTION, not grading."
      - "RECALL-02 PARTIAL: ranking half live (sameProjectRankNudge=0.05, foreignDocDemotion=0.10); doc-link render half accepted-null — vacuous through three gate runs incl. the 2026-08-07 refreshed-set re-gate (0/57 doc rows). Blocker is doc-candidate SURFACING, not the eval set."
  - phase: milestone-wide
    items:
      - "Confidence calibration against real approve/reject outcomes — correctly a v10.x follow-up (outcome data must exist first)."
      - "Nyquist VALIDATION.md missing for all 8 phases (nyquist_validation enabled in config). Discovery-only flag: run /gsd:validate-phase per phase if Nyquist coverage is wanted before archive."
---

# Milestone v10.0 "Action Proposals" — Audit Report

**Audited:** 2026-08-09 · **Re-audited:** 2026-08-09 after BUILD-01 fix
**Scope:** Phases 62–69 (8 phases, all marked Complete in ROADMAP.md 2026-08-02/03)
**Verdict:** `tech_debt` — 30/30 requirements satisfied, 8/8 integration seams wired, typecheck green at HEAD (BUILD-01 resolved). No critical blockers; accumulated deferred items listed below for review before archive.

## 1. Requirements Coverage (3-source cross-reference)

Sources: REQUIREMENTS.md traceability table (reconciled 2026-08-07) × phase VERIFICATION.md requirement tables × SUMMARY.md `requirements-completed` frontmatter.

| REQ group | Phase | VERIFICATION | SUMMARY frontmatter | Traceability | Final |
|---|---|---|---|---|---|
| EMAIL-01..04 | 62 | passed (pass 6, 4/4) | all 4 listed | `[x]` Satisfied | **satisfied** |
| CLASSIFY-01..04 | 63 | passed (4/4) | 01/02/04 listed; 03 evidenced in VERIFICATION only | `[x]` Complete | **satisfied** (CLASSIFY-03 manually verified: `tests/online-llm-free-sentinel.test.ts`, scoped per D-13, Phase-66 extension DISCHARGED — confirmed by integration checker) |
| RESOLVE-01..03 | 64 | passed (14/14) | all 3 listed | `[x]` Complete | **satisfied** |
| DRIFT-01..05 | 65 | human_needed (5/5 verified) | all 5 listed | `[x]` Complete (DRIFT-03 annotated: knob dark pending founder UAT) | **satisfied** (mechanism verified; enablement is a separate founder gate, by design) |
| EMIT-01..07 | 66 | passed (5/5 SC, 7/7 REQ) | all 7 listed | `[x]` Complete | **satisfied** |
| CONSUME-01..03 | 67 | passed (17/17) | all 3 listed | `[x]` Complete | **satisfied** |
| APPROVE-01..04 | 68 | passed (4/4 SC, 17/17 truths) | all 4 listed | `[x]` Complete | **satisfied** |

**v10.0 core: 30/30 satisfied. Zero orphaned requirements** — every REQ-ID appears in at least one VERIFICATION.md requirements table.

**RECALL-01..05 (Phase 69 — tracked separately, outside the 30-count per REQUIREMENTS.md):**

| REQ | Status | Note |
|---|---|---|
| RECALL-01 | **accepted-null** | Built + tested + review-hardened; `entityAnchoringEnabled` dark — G2 relevance failed 3× incl. LLM-judge re-gate. D-09: the gate decides. |
| RECALL-02 | **partial** | Ranking half live; doc-link render half accepted-null (vacuous 3×; doc-candidate surfacing is the blocker). |
| RECALL-03 | satisfied | `ambientHopInjectionEnabled` live. |
| RECALL-04 | satisfied | `recense recall --evidence` live. |
| RECALL-05 | satisfied | Fail-closed 58-prompt gate built + run; `--judge` grader added 2026-08-07. |

The two non-green RECALL rows are the eval-first design working as intended (D-08/D-09: knobs flip only on gate pass; honest nulls recorded, not papered over). They do not gate the milestone.

## 2. Phase Verifications

| Phase | Status | Score | Note |
|---|---|---|---|
| 62 | passed | 4/4 | Pass 6 after 7 gap-closure rounds (31 plans); EMAIL-03 closed via conformant css-tree + parse5 layers |
| 63 | passed | 4/4 | |
| 64 | passed | 14/14 | |
| 65 | human_needed | 5/5 verified | All must-haves verified; 3 human items gate *enablement* of the dark `provenanceDistinctnessEnabled` knob, not phase closure |
| 66 | passed | 5/5 (7/7 REQ) | D-43-for-proposals sentinel shipped |
| 67 | passed | 17/17 | |
| 68 | passed | 4/4 | |
| 69 | passed_with_open_item | 4/6 | Both open items re-gated post-fix, confirmed honest nulls, accepted per D-09 |

No unverified phases.

## 3. Cross-Phase Integration (checker report, 2026-08-09)

**8/8 load-bearing seams WIRED; 0 orphaned exports; 0 missing connections.**

1. **62→63** WIRED — `stripHiddenContent` runs inside `normalizeGmailMessage` (`gmail-adapter.ts:530-533`) before provenance-header join; that exact content reaches the extraction prompt (`consolidator.ts:800-819`). No bypass path.
2. **63→64→65** WIRED — hitl hard-stop at `consolidator.ts:767` precedes all 4 intent-fill sites, the entity-resolution loop (`:1096-1122`), and `StatusDrift.evaluate()` (`:1487`); composition proven by `tests/drift-belief-correction-e2e.test.ts` through real `consolidate()`.
3. **65→66** WIRED — `isEmissionEligible` excludes `hold`/`contradict_oscillation`; the hold branch never calls `maybeEmitProposal` (DRIFT-02/APPROVE-03 structurally inherited).
4. **63's Phase-66 obligation** WIRED — LLM-free sentinel extended to `/v1/proposals` (`tests/online-llm-free-sentinel.test.ts:300-536`, "PHASE 66 EXTENSION DISCHARGED" in docblock). CLASSIFY-03 fully closed.
5. **66→67** WIRED — `tests/proposal-reference-e2e.test.ts` drives the adapter's own client against a real server: happy path, 409 refusal, replay, reject, auth. Import-boundary test non-vacuous (floor + planted offenders).
6. **66→68** WIRED — telegram belief-kind rides the real `/v1/proposals` routes (`tests/telegram-belief-e2e.test.ts`), wired into the live poll loop (`clients/telegram/index.ts:1448`) and callback handler (`:447-456`); hold-exclusion inherited from seam 3.
7. **69 knobs** WIRED — dark knobs confirmed dark (`entityAnchoringEnabled:false`, `ambientDocLinkRenderEnabled:false`), live knobs confirmed live (`sameProjectRankNudge:0.05`, `foreignDocDemotion:0.10`, `ambientHopInjectionEnabled:true`); `--evidence` dispatch confirmed (`recall-cli.ts:114,195`).
8. **Full E2E chain** WIRED directly — `tests/full-chain-e2e.test.ts` (quick task `260809-2qe`) now drives one fixture Gmail message's raw bytes through all 8 stages (gmail raw bytes -> normalize/strip -> ingest pipeline -> consolidation -> emission -> store -> `/v1/proposals` -> Telegram approval) in a single continuous run, pinning the emit->store join as a first-class assertion (recomputed `proposalId`, `evidence_episode` -> episode -> gmail `external_id`, and the emitted-id === listed-id === approved-id conservation across the HTTP seam) instead of an inference across the three composed suites.

## 4. Build State — RESOLVED

Original audit found `npm run typecheck` red at `2e90557` (tests/ambient-recall.test.ts(574,12) TS2532 under `noUncheckedIndexedAccess`, introduced by phase-69 review-fix commit `a5dd60e`).

**Resolved** via quick task 260809-1n3 (commit `ec9d6e1`, merged `7650e00`): non-null assert on `betaMentions[0]`. Re-verified at HEAD `7650e00`: `npm run typecheck` green across both tsconfig projects; `tests/ambient-recall.test.ts` 33/33 passing. The only code delta since the original audit is this one-line test fix, so all other audit findings (requirements, integration seams, flows) carry forward unchanged.

## 5. Nyquist Compliance

`workflow.nyquist_validation` is enabled; **no phase in 62–69 has a VALIDATION.md** → all 8 classify MISSING. Discovery-only: run `/gsd:validate-phase <N>` per phase if Nyquist coverage is wanted; prior milestones (e.g. 28, 30) did produce VALIDATION.md files, v10.0 phases did not.

## 6. Tech Debt Aggregate

See frontmatter `tech_debt` for the remaining structured list and `resolved_gaps` for items closed post-audit. Remaining debt: EMAIL-02 backfill-only scoping (Gmail API constraint, deferred alternative recorded), Phase 65's founder enablement gate for `provenanceDistinctnessEnabled` (DRIFT-05 dry-run + threadId-in-key decision — the WR-01/WR-03 code prerequisites are now resolved), Phase 69's two accepted-null knobs with recorded next steps, confidence calibration (needs real approve/reject outcome data), and the missing Nyquist VALIDATION.md files (validate-phase tooling not installed). Resolved 2026-08-09 via quick tasks 260809-1vg / 260809-2fo / 260809-2qe: dist/-dependent full-suite failures (now graceful skips, 0 failed unbuilt), strip-hidden flaky perf assertions (calibration-relative), WR-03 idempotence defects, WR-01 comment, and the single continuous 8-stage E2E test (`tests/full-chain-e2e.test.ts`).
