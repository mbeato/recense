---
phase: 48-correctness-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/consolidation.test.ts
  - tests/schema.test.ts
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03]
must_haves:
  truths:
    - "An assistant-role confirm does NOT strengthen a node (C-2 self-confirmation guard airtight, including the D-17 exact-match fast path)"
    - "The consolidator Phase-B write transaction runs in IMMEDIATE mode (M-5 — no SQLITE_BUSY_SNAPSHOT abort on upgrade race)"
    - "initSchema throws when stored schema_version > SCHEMA_VERSION (M-9 downgrade guard)"
    - "Each of the three guards has a regression test labeled with its HARD-0X requirement id"
  artifacts:
    - path: "tests/consolidation.test.ts"
      provides: "HARD-01 labeled C-2 no-strengthen test + new HARD-02 IMMEDIATE-mode test"
      contains: "HARD-02"
    - path: "tests/schema.test.ts"
      provides: "HARD-03 labeled M-9 downgrade-throw test"
      contains: "HARD-03"
  key_links:
    - from: "tests/consolidation.test.ts HARD-02 test"
      to: "h.db.transaction(...).immediate()"
      via: "spy wrapping db.transaction to count .immediate() calls on the Phase-B write path"
      pattern: "immediate"
    - from: "src/consolidation/consolidator.ts Phase-B write"
      to: "this.db.transaction(...).immediate()"
      via: "audit confirms lines 507 and 953-969 are .immediate()"
      pattern: "\\.immediate\\(\\)"
---

<objective>
Lock three ARCH-REVIEW guards that already ship in live code, each behind a requirement-labeled regression test (a hard roadmap requirement: a guard without its reproducing test does not count as closed):

- **HARD-01 (C-2):** assistant-role episodes must not strengthen — guard exists at `consolidator.ts:1079-1082`, test exists at `consolidation.test.ts:1302`. Work = audit airtightness against the D-17 exact-match fast path + label the test.
- **HARD-02 (M-5):** every consolidator Phase-B write txn runs `.immediate()` — guards exist at `consolidator.ts:507` and `:953-969` and `txUpsertNode` (`semantic-store.ts:296`). Work = audit + write the one missing regression test.
- **HARD-03 (M-9):** `initSchema` reads stored version first and throws on `stored > SCHEMA_VERSION` — guard exists at `schema.ts:607-623`, test exists at `schema.test.ts:74`. Work = label the test.

Purpose: verify-first, not greenfield. Live code wins over the REQUIREMENTS.md "Pending" status. Do NOT re-implement these guards. The only deliverables here are: an audit pass per guard, one new test (HARD-02), and requirement labels on the two existing tests.
Output: a new HARD-02 IMMEDIATE-mode test, plus `HARD-01` / `HARD-03` labels on the existing C-2 and M-9 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-correctness-hardening/48-CONTEXT.md
@.planning/phases/48-correctness-hardening/48-PATTERNS.md

<interfaces>
<!-- Extracted from live code 2026-06-27 — executor uses these directly, no exploration needed. -->

src/consolidation/consolidator.ts — Phase-B write transactions (ALL already .immediate()):
  :507  this.db.transaction(() => this.episodes.markConsolidated(episodeId)).immediate();
  :953-969  this.db.transaction(() => { ...per-episode multi-statement graph write... }).immediate();
  C-2 strengthen gate (:1075-1085):
    if (decision.episodeRole !== 'assistant') { this.strength.strengthen(decision.bestCandidateId, decision.claimOrigin); }

src/db/schema.ts — M-9 downgrade guard (:607-623):
  reads stored schema_version, throws /newer than this binary/ when stored > SCHEMA_VERSION, stamps only on fresh/upgrade.

tests/consolidation.test.ts — harness + existing tests:
  makeHarness() → { db, clock, episodes, store, strength, retriever, config } (:115-132)
  makeZeroEmbedFn(dims) defined at :86; MockModelProvider ctor pattern at :148-165; Consolidator ctor at :259-261
  C-2 test (HARD-01) at :1302-1344 — 'C-2: assistant-role confirm does NOT strengthen (s and c unchanged)'
  D-48 in-transaction test analog at :1753-1783 (custom sink technique)

tests/schema.test.ts — M-9 test (HARD-03) at :74-86; uses try/finally { db.close() }, not beforeEach.
  imports: Database from 'better-sqlite3'; { initSchema, SCHEMA_VERSION } from '../src/db/schema'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit + label HARD-01 (C-2 no-strengthen) and HARD-03 (M-9 downgrade)</name>
  <files>tests/consolidation.test.ts, tests/schema.test.ts</files>
  <read_first>
    - src/consolidation/consolidator.ts:1075-1095 (C-2 strengthen gate + the D-17 exact-match fast path it must cover)
    - tests/consolidation.test.ts:1295-1344 (existing C-2 test to label)
    - src/db/schema.ts:607-623 (M-9 downgrade guard)
    - tests/schema.test.ts:74-86 (existing M-9 test to label)
  </read_first>
  <action>
    Two audit-then-label steps, no production code change expected:
    (1) HARD-01: Trace the C-2 gate at `consolidator.ts:1079-1082`. Confirm `decision.episodeRole !== 'assistant'` is the only path into `this.strength.strengthen(...)` for the `confirm` relation, AND that the D-17 normalized-exact-match fast path (which auto-confirms with no judge) flows through this same `applyDecision`/confirm branch — so an assistant-role exact-match echo is structurally blocked from strengthening. Per ARCH-REVIEW C-2, assistant claims may still extend/append (by-design) — only strengthen is gated. If the audit finds a real residual gap where an assistant-role exact-match strengthens, STOP and report it (do not silently widen scope). If airtight (expected), add a `HARD-01 (C-2)` label comment to the existing test at `tests/consolidation.test.ts:1302` — do not change its assertions.
    (2) HARD-03: Confirm `schema.ts:607-623` reads stored version before stamping and throws on `stored > SCHEMA_VERSION`. Add a `HARD-03 (M-9)` label comment to the existing test at `tests/schema.test.ts:74` — do not change its assertions.
    Record both audit conclusions (airtight / gap-found) in the SUMMARY.
  </action>
  <verify>
    <automated>grep -n "HARD-01" tests/consolidation.test.ts && grep -n "HARD-03" tests/schema.test.ts && npx vitest run tests/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The existing C-2 test (`tests/consolidation.test.ts:1302`) carries a `HARD-01` label comment; its assertions are unchanged
    - The existing M-9 test (`tests/schema.test.ts:74`) carries a `HARD-03` label comment; its assertions are unchanged
    - SUMMARY records the C-2 audit verdict (airtight vs gap), explicitly covering the D-17 exact-match fast path
    - `npx vitest run tests/schema.test.ts` exits 0
  </acceptance_criteria>
  <done>Both existing guards audited and confirmed airtight (or a gap reported), and their tests carry HARD-01 / HARD-03 labels with no assertion changes.</done>
</task>

<task type="auto">
  <name>Task 2: Audit HARD-02 + write the M-5 IMMEDIATE-mode regression test</name>
  <files>tests/consolidation.test.ts</files>
  <read_first>
    - src/consolidation/consolidator.ts:500-510 and :920-970 (the two Phase-B write transactions — both must be .immediate())
    - src/db/semantic-store.ts:285-296 (txUpsertNode .immediate() wrapper)
    - tests/consolidation.test.ts:115-165 (makeHarness, makeZeroEmbedFn, MockModelProvider/Consolidator construction)
    - tests/consolidation.test.ts:1753-1783 (D-48 in-transaction test — the closest analog for inspecting Phase-B txn behavior)
  </read_first>
  <action>
    (1) Audit: grep-confirm every `this.db.transaction(...)` in `consolidator.ts` that performs a write in Phase B is chained `.immediate()` (live state: `:507` and `:953-969`). If any Phase-B write transaction is found NOT `.immediate()`, STOP and report (this would be the one genuine code gap for HARD-02). Expected: all already `.immediate()` — no production code change.
    (2) Write a new `HARD-02 (M-5)` test inside the existing `describe('Consolidator')` block in `tests/consolidation.test.ts`. Use the D-48 inspection technique adapted to count IMMEDIATE calls: monkey-patch `h.db.transaction` so each returned transaction's `.immediate` is wrapped to increment a counter, then call the real `.immediate`. Drive a normal consolidate() pass (build a Consolidator from the harness with a MockModelProvider whose generateScript mints one new fact, judgeScript empty; append one user-role episode; `await consolidator.consolidate()`). Assert the IMMEDIATE counter is `>= 1` (the Phase-B per-episode write fired in IMMEDIATE mode). Prefer this call-path assertion over a flaky real-concurrency SQLITE_BUSY_SNAPSHOT race (per CONTEXT D-04). Restore the original `h.db.transaction` after the test (or rely on per-test fresh harness). Use `makeZeroEmbedFn(h.config.embeddingDimensions)` for the embedder. Name the test with the literal `HARD-02`.
  </action>
  <verify>
    <automated>grep -n "HARD-02" tests/consolidation.test.ts && npx vitest run tests/consolidation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records the audit verdict that all consolidator Phase-B write transactions are `.immediate()` (or reports a gap)
    - `tests/consolidation.test.ts` contains a test whose name includes `HARD-02` asserting the Phase-B write transaction runs in IMMEDIATE mode (counter `>= 1`)
    - The test uses the spy/call-path technique, not a timing-dependent concurrency race
    - `npx vitest run tests/consolidation.test.ts` exits 0
  </acceptance_criteria>
  <done>HARD-02 audit confirms IMMEDIATE discipline across Phase-B writers, and a labeled regression test proves the Phase-B write transaction is opened `.immediate()`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase adds no external input, network, or auth surface — these are internal single-tenant correctness guards verified by tests. The relevant boundaries are integrity controls on the engine's own state.

| Boundary | Description |
|----------|-------------|
| assistant output → semantic graph | C-2: the engine's own restated output must not strengthen a belief (self-confirmation) |
| stale binary → on-disk DB | M-9: a downgrade-stamp by an old binary must be refused |
| concurrent episode writer → sleep-pass write | M-5: WAL upgrade race must not abort the pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-02 | Repudiation / Integrity | consolidator confirm→strengthen (C-2) | mitigate | Existing assistant-role strengthen gate (`consolidator.ts:1079-1082`) upholds the never-strengthen-from-self-output invariant; this plan locks it with the HARD-01 labeled test. |
| T-48-03 | Tampering | initSchema version stamp (M-9) | mitigate | Existing read-first downgrade guard (`schema.ts:607-623`) prevents a stale binary corrupting a future-schema DB; HARD-03 labeled test proves the throw fires. |
| T-48-04 | Denial of Service | consolidator Phase-B write txn (M-5) | mitigate | Existing `.immediate()` discipline prevents SQLITE_BUSY_SNAPSHOT aborting the sleep pass under writer contention; HARD-02 test asserts the IMMEDIATE call path. |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (net-zero new runtime deps). No legitimacy gate needed. |
</threat_model>

<verification>
- `grep -n "HARD-01" tests/consolidation.test.ts`, `grep -n "HARD-02" tests/consolidation.test.ts`, `grep -n "HARD-03" tests/schema.test.ts` each return a match.
- `npx vitest run tests/consolidation.test.ts tests/schema.test.ts` exits 0.
- Full suite green: `npm test` exits 0 with no new failures (baseline 2383 passed / 3 skipped).
- SUMMARY records the three audit verdicts (C-2 airtight incl. D-17 fast path; M-9 read-first; M-5 all Phase-B writers `.immediate()`).
</verification>

<success_criteria>
- HARD-01, HARD-02, HARD-03 each closed with a requirement-labeled regression test.
- No production code change (verify-first) unless an audit surfaces a genuine residual gap, in which case it is reported, not silently patched.
- No accuracy/clean-case regression: the HARD-01 user-role-DOES-strengthen counterpart and the full suite stay green.
</success_criteria>

<output>
Create `.planning/phases/48-correctness-hardening/48-02-SUMMARY.md` when done.
</output>
