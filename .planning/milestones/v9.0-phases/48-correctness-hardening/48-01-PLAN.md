---
phase: 48-correctness-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/semantic-store.ts
  - tests/store.test.ts
autonomous: true
requirements: [HARD-04]
must_haves:
  truths:
    - "setEmbedding stamps embedding_model in meta on the first embed"
    - "A subsequent embed under a different model on the same DB throws fail-closed (no silent NaN-cosine corruption)"
    - "A subsequent embed under the matching model does NOT throw (clean-case behavior preserved)"
    - "The model-mismatch failure has a named HARD-04 regression test"
  artifacts:
    - path: "src/db/semantic-store.ts"
      provides: "embedding_model stamp/assert inside setEmbedding, mirroring the embedding_dims guard"
      contains: "embedding_model"
    - path: "tests/store.test.ts"
      provides: "HARD-04 model stamp + mismatch + happy-path regression tests"
      contains: "embedding_model mismatch"
  key_links:
    - from: "src/db/semantic-store.ts setEmbedding"
      to: "this.config.openaiEmbedModel"
      via: "read config model name, stamp/assert via getMeta/setMeta('embedding_model')"
      pattern: "this\\.config\\.openaiEmbedModel"
    - from: "tests/store.test.ts"
      to: "store.setEmbedding"
      via: "two SemanticStore instances with differing openaiEmbedModel over one DB"
      pattern: "embedding_model mismatch"
---

<objective>
Close HARD-04 (ARCH-REVIEW L-2): the embedding **model** is not stamped or asserted in meta, so a future embed-provider/model swap would silently produce NaN cosines that scramble every sort. The dims half of L-2 already ships (`semantic-store.ts:384-393`); this plan adds the model half by mirroring that exact guard, then adds the mandated regression tests.

Purpose: a model change must fail closed (throw) the way a dims change already does — never warn-and-continue, never silent corruption of the derived vector cache.
Output: a 7-line `embedding_model` stamp/assert block in `setEmbedding`, plus three colocated regression tests (stamp, mismatch-throws, matching-succeeds).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-correctness-hardening/48-CONTEXT.md
@.planning/phases/48-correctness-hardening/48-PATTERNS.md

<interfaces>
<!-- Extracted from live code 2026-06-27 — executor uses these directly, no exploration needed. -->

src/db/semantic-store.ts — the single writer of node.embedding (lines 377-401):
  setEmbedding(id: string, vec: Float32Array, expectedValueHash?: string): void
  - holds `private readonly config: EngineConfig` (line 37) — already has the model name, no signature change
  - getMeta(key: string): string | null   // null = key absent (first write)
  - setMeta(key: string, value: string): void
  - The existing dims guard to mirror (lines 384-393):
      const dims = vec.length;
      const storedDims = this.getMeta('embedding_dims');
      if (storedDims === null) { this.setMeta('embedding_dims', String(dims)); }
      else if (parseInt(storedDims,10) !== dims) { throw new Error(`embedding_dims mismatch: ...`); }
  - INSERT the model block immediately after the dims block, before the `Buffer.from(vec.buffer, ...)` line.

src/lib/config.ts — EngineConfig.openaiEmbedModel: string (line 222), DEFAULT_CONFIG default 'text-embedding-3-small' (line 781).

tests/store.test.ts — testConfig = { ...DEFAULT_CONFIG, dbPath: ':memory:' } (line 17);
  beforeEach builds `store = new SemanticStore(db, clock, testConfig)` (lines 57-65);
  existing L-2 dims tests live in the `describe('setEmbedding')` block (lines ~203-224).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stamp and assert embedding_model in setEmbedding</name>
  <files>src/db/semantic-store.ts</files>
  <read_first>
    - src/db/semantic-store.ts:374-401 (the setEmbedding function + the embedding_dims guard analog being mirrored)
    - src/db/semantic-store.ts:30-40 (class field `private readonly config: EngineConfig`)
    - src/lib/config.ts:222 and :781 (openaiEmbedModel field + default)
  </read_first>
  <behavior>
    - First setEmbedding on a fresh DB: getMeta('embedding_model') is null → setMeta('embedding_model', this.config.openaiEmbedModel). No throw.
    - Subsequent setEmbedding with stored model === this.config.openaiEmbedModel → no throw.
    - Subsequent setEmbedding with stored model !== this.config.openaiEmbedModel → throws Error matching /embedding_model mismatch/.
  </behavior>
  <action>
    Insert a `HARD-04 (L-2)` model-stamp block immediately after the existing `embedding_dims` block (currently ending at line 393) and before the `Buffer.from(vec.buffer, ...)` line. Read the model name from `this.config.openaiEmbedModel` (no signature change — `config` is already a class field). Mirror the dims-guard shape verbatim: `getMeta('embedding_model')`; if null, `setMeta('embedding_model', model)`; else if `storedModel !== model`, throw an Error whose message follows the established convention `embedding_model mismatch: stored=<x>, received=<y> for node <id> — embedding provider model changed`. Fail closed (throw), never warn-and-continue. Add a one-line comment labeling it `HARD-04 (L-2)`. Do NOT touch the dims block, the L-1 stale-vector guard, or the Buffer write. No new config key, no new meta plumbing.
  </action>
  <verify>
    <automated>grep -n "embedding_model" src/db/semantic-store.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/semantic-store.ts` setEmbedding stamps meta key `embedding_model` from `this.config.openaiEmbedModel` on first embed and throws on mismatch
    - The throw message matches /embedding_model mismatch/
    - `grep -c 'this.config.openaiEmbedModel' src/db/semantic-store.ts` returns >= 1
    - `npm run build` (tsc) exits 0 — no signature change, compiles clean
  </acceptance_criteria>
  <done>The model stamp/assert block exists in setEmbedding, mirrors the dims guard, reads this.config.openaiEmbedModel, and tsc compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HARD-04 regression tests (stamp, mismatch-throws, matching-succeeds)</name>
  <files>tests/store.test.ts</files>
  <read_first>
    - tests/store.test.ts:1-70 (imports, testConfig, beforeEach/afterEach harness)
    - tests/store.test.ts:203-224 (the existing L-2 dims tests — the exact analog to mirror)
  </read_first>
  <behavior>
    - Test A (stamp): a SemanticStore built with openaiEmbedModel='test-model-v1' → after setEmbedding, getMeta('embedding_model') === 'test-model-v1'.
    - Test B (mismatch fail-closed): storeA (model-a) stamps the DB; storeB (model-b) on the SAME db → setEmbedding throws /embedding_model mismatch/.
    - Test C (happy path): same model across two writes → setEmbedding does NOT throw (clean-case behavior preserved, engine invariant).
  </behavior>
  <action>
    Add three `HARD-04 (L-2)` tests inside the existing `describe('setEmbedding')` block, directly after the existing L-2 dims tests (~line 224). Construct stores via `new SemanticStore(db, clock, { ...testConfig, openaiEmbedModel: '<name>' })` to control the model name without new config surface — both model-A and model-B stores share the one in-memory `db` so the meta key is shared. upsertNode each node before setEmbedding (a node must exist or setEmbedding is a no-op). Use `new Float32Array(8)` for vectors. Assert: stamp via `expect(store.getMeta('embedding_model')).toBe(...)`; mismatch via `expect(() => ...).toThrow(/embedding_model mismatch/)`; happy path via `expect(() => ...).not.toThrow()`. Label each test name with `HARD-04 (L-2)`. Do not modify the existing dims tests.
  </action>
  <verify>
    <automated>npx vitest run tests/store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/store.test.ts` contains a test asserting a model-mismatch setEmbedding throws (`/embedding_model mismatch/`)
    - `tests/store.test.ts` contains a happy-path test asserting matching model does NOT throw
    - All three new test names contain the literal `HARD-04`
    - `npx vitest run tests/store.test.ts` exits 0
  </acceptance_criteria>
  <done>Three labeled HARD-04 tests pass; the mismatch test reproduces the L-2 failure mode (throws), the happy-path test proves no clean-case regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase adds no external input, network, or auth surface — it is an internal single-tenant correctness guard on the engine's own write primitive. The only boundary is engine-config → derived vector cache.

| Boundary | Description |
|----------|-------------|
| config (openaiEmbedModel) → vector store | A model/provider swap is the trusted-config event this guard fails-closed on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering / Integrity | semantic-store.setEmbedding (derived vector cache) | mitigate | The HARD-04 model stamp/assert IS the mitigation: a silent embedder/model swap would corrupt every cosine sort; the fail-closed throw is an integrity control that halts on mismatch rather than producing NaN-scrambled rankings. |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (net-zero new runtime deps — engine invariant). No legitimacy gate needed. |
</threat_model>

<verification>
- `npm run build` exits 0 (tsc clean, no signature change).
- `npx vitest run tests/store.test.ts` exits 0 with the three new HARD-04 tests green and the existing dims tests untouched.
- Full suite green: `npm test` exits 0 with no new failures (baseline at v8.0 close: 2383 passed / 3 skipped).
</verification>

<success_criteria>
- HARD-04 closed: embedding_model stamped at first embed and asserted fail-closed on write, mirroring the dims guard.
- Three requirement-labeled regression tests exist (stamp, mismatch-throws, matching-succeeds).
- No accuracy/clean-case regression: matching-model writes still succeed.
</success_criteria>

<output>
Create `.planning/phases/48-correctness-hardening/48-01-SUMMARY.md` when done.
</output>
