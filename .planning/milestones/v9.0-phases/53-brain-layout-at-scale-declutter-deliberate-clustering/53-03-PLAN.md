---
phase: 53-brain-layout-at-scale-declutter-deliberate-clustering
plan: 03
type: execute
wave: 3
depends_on: ["53-01", "53-02"]
files_modified:
  - tests/viz-layout-guards.test.ts
  - src/viz/modules/constants.js # tuning only (conditional on founder checkpoint)
autonomous: false
requirements: [D-09]

must_haves:
  truths:
    - "The honesty/locked guarantees can't silently regress: automated guards assert no Math.random in the seed path, cooldownTicks(12) gone, SETTLE_BUDGET_MS wired, revealSettled() body + BRAIN_SCALE=460 + nodeRadius untouched (D-09 machine-check, ROADMAP SC4)"
    - "Phase 52 honest-traces flashes still render: the full viz suite (haze activation/selection, remember bridge, sleep-pass lighting, lod density) stays green (ROADMAP SC4)"
    - "Settle stays bounded by wall-clock: reveal is a single SETTLE_BUDGET_MS timeout, no unbounded cooldown (D-03/D-09, ROADMAP SC3 perf guard)"
    - "Founder confirms the felt qualities at ~15k: gridlines gone (SC1), clustered/deliberate (SC3), not-too-full (SC2), frame-rate not worse than baseline (SC3) — and tunes SETTLE_BUDGET_MS / OVERVIEW_NODE_CAP (D-09 checkpoint)"
  artifacts:
    - path: "tests/viz-layout-guards.test.ts"
      provides: "machine-checkable regression guards for the layout + locked-anchor + Phase-52 invariants"
      contains: "SETTLE_BUDGET_MS"
  key_links:
    - from: "tests/viz-layout-guards.test.ts"
      to: "src/viz/modules/graph.js + constants.js"
      via: "source-text + structural assertions on the locked anchors and seed/settle invariants"
      pattern: "revealSettled"
---

<objective>
Lock the layout invariants behind automated guards, prove Phase 52 didn't regress, then take the founder visual checkpoint to confirm the felt qualities and tune the two named constants.

This mirrors Phase 52's "structural-test-plus-checkpoint" idiom (D-09). Machine-check everything that can silently regress — the locked anchors (`BRAIN_SCALE=460`, `nodeRadius`, `revealSettled()` body), the seed/settle invariants (no `Math.random` in the seed path, `cooldownTicks(12)` gone, `SETTLE_BUDGET_MS` wired), and the full Phase 52 viz suite — then hand the founder a running brain at ~15k for the gridlines-gone / clustered / not-too-full / frame-rate calls and the constant-tuning pass.

Purpose: prevents silent regression of the honesty + locked guarantees; gets founder sign-off on the felt targets (ROADMAP SC1/SC2/SC3) that cannot be machine-verified.
Output: `tests/viz-layout-guards.test.ts`, a green full viz suite, a tuned `SETTLE_BUDGET_MS` / `OVERVIEW_NODE_CAP`, and founder approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-CONTEXT.md
@.planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-PATTERNS.md
@.planning/phases/52-brain-viz-honest-traces/52-CONTEXT.md
@.planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-01-SUMMARY.md
@.planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-02-SUMMARY.md

<interfaces>
<!-- Locked anchors the guard test asserts (PATTERNS "Locked Anchors"): -->
constants.js: BRAIN_SCALE = 460
graph.js:67-71 nodeRadius()                     // schema 3 + sqrt(members)*0.8; member 2.5; haze 2
graph.js:603-611 revealSettled() body           // pin fx/fy/fz + opacity 0 -> 0.35s ease -> 1
graph.js settle: setTimeout(revealSettled, SETTLE_BUDGET_MS); NO cooldownTicks(12)
graph.js seedNodePositions: no Math.random in the brainVol branch (deterministic, D-08)

<!-- Phase 52 suite that must stay green (the no-regress set): -->
tests/viz-haze-activation.test.ts, tests/viz-haze-selection.test.ts,
tests/remember-viz-bridge.test.ts, tests/sleep-pass-viz-lighting.test.ts,
tests/viz-lod-density.test.ts, tests/viz-corpus-graph.test.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/viz-layout-guards.test.ts (machine-checkable invariants)</name>
  <files>tests/viz-layout-guards.test.ts</files>
  <read_first>
    - src/viz/modules/graph.js (155-235 seed path; 594-614 settle/reveal; 603-611 locked revealSettled body; 67-71 nodeRadius)
    - src/viz/modules/constants.js (SETTLE_BUDGET_MS + OVERVIEW_NODE_CAP from 53-01/53-02; BRAIN_SCALE=460)
    - .planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-PATTERNS.md ("Locked Anchors" table; "Hidden-then-fade reveal (LOCKED)")
    - tests/viz-lod-density.test.ts (the read-the-module-as-text + headless-import patterns to reuse)
  </read_first>
  <action>
    Create `tests/viz-layout-guards.test.ts` (vitest). Read `src/viz/modules/graph.js` and `constants.js` as text and assert the locked/seed/settle invariants — comment lines MUST be stripped before token assertions (filter out lines matching `^\s*(\*|//)` so JSDoc prose can neither satisfy nor invalidate a gate):
      1. `constants.js` exports `BRAIN_SCALE = 460` (unchanged anchor), `SETTLE_BUDGET_MS`, and `OVERVIEW_NODE_CAP`.
      2. graph.js settle block contains `setTimeout(revealSettled, SETTLE_BUDGET_MS)` and does NOT contain `cooldownTicks(12)` (bounded wall-clock settle, D-03/D-09 perf guard).
      3. graph.js `revealSettled()` body retains the locked fade sequence: `graphEl.style.transition = 'opacity 0.35s ease'` and the `n.fx = n.x` pin (Phase 52 no-regress, D-07).
      4. graph.js `nodeRadius` definition (67-71) is unchanged from the locked form (assert the schema/member/haze magnitudes present).
      5. The brainVol branch of `seedNodePositions` contains NO `Math.random` (determinism, D-08) — scope the assertion to the seed-function body (slice between the function signature and the next top-level function) so the null-fallback branch may retain Math.random.
    Determinism itself is already behaviorally covered by 53-01's `tests/viz-seed-determinism.test.ts` — reference it, do not duplicate. Keep this file guard-only; do NOT modify any src/viz module.
  </action>
  <verify>
    <automated>npm test -- tests/viz-layout-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/viz-layout-guards.test.ts` green; every assertion strips comment lines before token checks (no self-invalidating prose gate).
    - Asserts: BRAIN_SCALE=460 + SETTLE_BUDGET_MS + OVERVIEW_NODE_CAP exported; `setTimeout(revealSettled, SETTLE_BUDGET_MS)` present; `cooldownTicks(12)` absent; revealSettled fade + pin intact; nodeRadius magnitudes intact; no Math.random in the brainVol seed branch.
    - The test modifies no src/viz file.
  </acceptance_criteria>
  <done>Layout/locked/Phase-52 invariants are machine-guarded and green.</done>
</task>

<task type="auto">
  <name>Task 2: Full viz suite green + build dist + prepare the ~15k checkpoint</name>
  <files>none — build + verification only (touches no source module)</files>
  <read_first>
    - scripts/copy-viz-assets.cjs (postbuild copies src/viz -> dist/src/viz so `recense viz` serves the edited modules)
    - src/viz/server.ts (116-136, 582-587 — modules served from dist/src/viz/modules)
    - package.json (scripts: build = tsc, postbuild = copy-viz-assets; test = vitest run)
  </read_first>
  <action>
    Run the full viz regression set to prove Phase 52 honest-traces did not regress, then build so the founder checkpoint sees the new code. Run the named viz tests (see verify). Then run `npm run build` (tsc + postbuild copy-viz-assets) so the edited `graph.js` / `lod.js` / `constants.js` land in `dist/src/viz/modules` and `recense viz` serves the new layout. Confirm a brain DB at ~15k nodes (the symptom scale) is available and record the exact `recense viz` invocation for the checkpoint. Do NOT edit any source module here — build + verification only.
  </action>
  <verify>
    <automated>npm test -- tests/viz-haze-activation.test.ts tests/viz-haze-selection.test.ts tests/remember-viz-bridge.test.ts tests/sleep-pass-viz-lighting.test.ts tests/viz-lod-density.test.ts tests/viz-corpus-graph.test.ts tests/viz-seed-determinism.test.ts tests/viz-layout-guards.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All named viz tests pass (Phase 52 honest-traces + Phase 53 layout/determinism/cap guards) — ROADMAP SC4 no-regress.
    - `npm run build` succeeds; `dist/src/viz/modules/{graph,lod,constants}.js` reflect the Phase 53 edits.
    - The `recense viz` invocation + ~15k DB for the checkpoint are confirmed and recorded.
  </acceptance_criteria>
  <done>Full viz suite green; dist rebuilt with the new layout; checkpoint ready.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: FOUNDER VISUAL CHECKPOINT — felt qualities at ~15k + tune constants (D-09)</name>
  <files>src/viz/modules/constants.js (tuning only: SETTLE_BUDGET_MS / OVERVIEW_NODE_CAP)</files>
  <action>Human-only verification — no autonomous code path. The founder opens the running `recense viz` at ~15k nodes and judges the felt qualities per how-to-verify (gridlines gone SC1, clustered/deliberate SC3, not-too-full SC2, frame-rate not worse SC3, stable reload D-08, Phase 52 flashes intact). The ONLY edits permitted here are tuning the two named constants `SETTLE_BUDGET_MS` and `OVERVIEW_NODE_CAP` in `src/viz/modules/constants.js`, followed by `npm run build` and a re-check. No seed/settle/lod logic changes at this checkpoint.</action>
  <what-built>
    A reworked `recense viz` brain layout: continuous in-hull cluster-aware seeding (gridlines gone, related nodes grouped), a wall-clock `SETTLE_BUDGET_MS`-bounded settle, deterministic re-seed (stable across reloads), and an `OVERVIEW_NODE_CAP` density cap holding screen fullness in the calibrated band at scale. Engine/retrieval/data-model untouched; Phase 52 honest-traces flashes preserved (machine-guarded).
  </what-built>
  <how-to-verify>
    1. Start the viz against the ~15k-node brain (the invocation recorded in Task 2, e.g. `recense viz`), open it in the browser, wait for the fade-in.
    2. SC1 — gridlines: confirm NO visible lattice/grid; nodes fill space continuously.
    3. SC3 — clustered/deliberate: confirm related nodes read as intentional groups, not a uniform scatter and not a snapped grid.
    4. SC2 — not-too-full: confirm the overview reads as readable/deliberate, in the founder-dialed band (~2,700–3,200 feel), not an undifferentiated full cloud.
    5. SC3 — performance: confirm frame-rate at ~15k is not worse than today's baseline; the reveal latency feels acceptable.
    6. Reload twice: confirm the same corpus reproduces the same layout (D-08 stability).
    7. Phase 52 sanity: run a recall + a `recense remember` and confirm the amber/cyan recall flashes + magenta reconsolidation hero still fire correctly.
    8. TUNE: if any felt quality is off, adjust `SETTLE_BUDGET_MS` (graph settle time) and/or `OVERVIEW_NODE_CAP` (fullness) in `src/viz/modules/constants.js`, re-run `npm run build`, and re-check. These two named constants are the designated tuning knobs (D-03/D-06/D-09).
  </how-to-verify>
  <resume-signal>Type "approved" (optionally with the final SETTLE_BUDGET_MS / OVERVIEW_NODE_CAP values), or describe what still looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification + checkpoint only. Reads source as text, runs tests, builds dist, renders the existing client viz. No new data, network, auth, or input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-05 | Tampering | layout/locked-anchor regression | mitigate | `tests/viz-layout-guards.test.ts` + the full viz suite assert the locked anchors and Phase-52 honesty path cannot silently regress (D-09). |

No high-severity threats: this plan adds a guard test and a build/checkpoint; it introduces no runtime surface.
</threat_model>

<verification>
- `npm test -- tests/viz-layout-guards.test.ts` green.
- Full named viz suite green (Phase 52 + Phase 53 guards) and `npm run build` succeeds.
- Founder visual checkpoint approved at ~15k (SC1/SC2/SC3 felt qualities + Phase 52 flashes intact), final constant values recorded.
</verification>

<success_criteria>
- Locked anchors + seed/settle invariants + Phase 52 honesty path machine-guarded (D-09 / ROADMAP SC4).
- Settle bounded by SETTLE_BUDGET_MS proven structurally (ROADMAP SC3 perf guard).
- Founder confirms gridlines-gone / clustered / not-too-full / no frame-rate regression at ~15k and tunes the two constants (D-09 / ROADMAP SC1/SC2/SC3).
</success_criteria>

<output>
Create `.planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-03-SUMMARY.md` when done.
</output>
