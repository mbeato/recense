---
phase: 53-brain-layout-at-scale-declutter-deliberate-clustering
plan: 02
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - src/viz/modules/lod.js
  - src/viz/modules/constants.js
autonomous: true
requirements: [D-05, D-06]

must_haves:
  truths:
    - "Overview fullness holds in the calibrated band regardless of total corpus size: overview node count is capped at OVERVIEW_NODE_CAP at 15k / 50k / etc. (D-05, ROADMAP SC2)"
    - "The cap drops the long tail deliberately: schema super-nodes kept (ranked largest-first by member count), haze fills the remaining budget, surplus haze suppressed (D-06)"
    - "Members/detail still reachable: drill-in and trace reveal suppressed nodes unchanged (D-05 scope)"
  artifacts:
    - path: "src/viz/modules/constants.js"
      provides: "OVERVIEW_NODE_CAP named tunable"
      contains: "OVERVIEW_NODE_CAP"
    - path: "src/viz/modules/lod.js"
      provides: "overview cap applied after overviewCount: largest-schema-first ranking, haze remainder, surplus suppressed"
      contains: "OVERVIEW_NODE_CAP"
  key_links:
    - from: "src/viz/modules/lod.js density block"
      to: "OVERVIEW_NODE_CAP (constants.js)"
      via: "import + cap check after overviewCount"
      pattern: "OVERVIEW_NODE_CAP"
    - from: "src/viz/modules/lod.js cap ranking"
      to: "schemaMembers Map (.size)"
      via: "largest-schema-first survive-ranking (DENSITY_FILL idiom)"
      pattern: "schemaMembers"
---

<objective>
Hold the overview at a roughly constant fullness as the corpus grows, in `lod.js` (plus one tunable in `constants.js`).

After `overviewCount` (#schema + #haze) is computed, if it exceeds `OVERVIEW_NODE_CAP`, suppress the long tail so the screen stays in the founder-calibrated ~2,700–3,200 band at 15k / 50k / etc. (decoupled from total corpus size). Survive-ranking: keep schema super-nodes (the constellation), ranked largest-first by member count — the existing `DENSITY_FILL` "largest schemas first" idiom — then let haze fill the remaining budget; suppress haze beyond the cap. Members and detail still appear on drill-in or trace (unchanged).

Purpose: closes founder symptom (2) "feels too full" at scale.
Output: `OVERVIEW_NODE_CAP` constant + the cap logic appended to the lod.js density block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-CONTEXT.md
@.planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-PATTERNS.md

<interfaces>
<!-- lod.js density block (lod.js:88-126) — current state: -->
overviewCount = count of n where n.__cat === 'schema' || n.__cat === 'haze'
schemaMembers: Map<schemaId, Set<memberId>>   // built at lod.js:58-68; .size = member count (ranking signal)
densityRevealed: Set<memberId>                 // sparse-fill set (DENSITY_FILL_BELOW path) — keep
hazeOpacityScale                               // DENSITY_THIN lerp — keep
DENSITY_FILL "largest schemas first":  [...schemaMembers.entries()].sort((a,b)=>b[1].size-a[1].size)  // ranking idiom to reuse

<!-- constants.js (locked anchors): -->
BRAIN_SCALE = 460        // LOCKED — cap acts on COUNTS only, never absolute sizing
nodeRadius()             // LOCKED

<!-- From plan 53-01 (wave 1): constants.js already has SETTLE_BUDGET_MS. This plan ADDS OVERVIEW_NODE_CAP to the same file. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OVERVIEW_NODE_CAP tunable to constants.js</name>
  <files>src/viz/modules/constants.js</files>
  <read_first>
    - src/viz/modules/constants.js (lines 184-226 + the SETTLE_BUDGET_MS const added by 53-01; copy the JSDoc + `export const` pattern)
    - .planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-PATTERNS.md (section "constants.js:194-226" — exact new-constant shape, Group 2)
  </read_first>
  <behavior>
    - constants.js exports `OVERVIEW_NODE_CAP` with initial value 3000 (number).
    - JSDoc states it caps overview (schema + haze) regardless of corpus size, sits in the calibrated 2,700–3,200 band, suppressed nodes still appear on drill-in/trace, and is tuned at the founder checkpoint.
    - BRAIN_SCALE stays 460; SETTLE_BUDGET_MS (from 53-01) untouched.
  </behavior>
  <action>
    Append `export const OVERVIEW_NODE_CAP = 3000;` to `src/viz/modules/constants.js` alongside the density band, following the existing JSDoc + `export const` pattern. Initial value 3000 (inside the calibrated ~2,700–3,200 band, headroom over the 2,692 install). JSDoc must state: caps total overview nodes (schema + haze) regardless of corpus size (D-05); long-tail haze suppressed beyond the cap but still revealed on drill-in/trace; survive-ranking is largest-schema-first (D-06); tuned at the founder visual checkpoint. A named tunable, NOT a magic number. Do NOT touch BRAIN_SCALE, nodeRadius, SETTLE_BUDGET_MS, or any density constant value.
  </action>
  <verify>
    <automated>grep -v '^\s*\*' src/viz/modules/constants.js | grep -v '^\s*//' | grep -Eq "export const OVERVIEW_NODE_CAP = 3000" && grep -q "export const BRAIN_SCALE = 460" src/viz/modules/constants.js && grep -q "export const SETTLE_BUDGET_MS" src/viz/modules/constants.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `OVERVIEW_NODE_CAP = 3000` exported (asserted outside comments).
    - `BRAIN_SCALE = 460` and `SETTLE_BUDGET_MS` (from 53-01) both still present and unchanged.
    - git diff shows only the additive const + JSDoc.
  </acceptance_criteria>
  <done>OVERVIEW_NODE_CAP exported; locked anchors and 53-01's constant intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply overview cap in lod.js density adaptation</name>
  <files>src/viz/modules/lod.js, tests/viz-lod-density.test.ts</files>
  <read_first>
    - src/viz/modules/lod.js (lines 33-40 imports; lines 58-68 schemaMembers build; lines 88-126 density block — overviewCount, DENSITY_FILL "largest schemas first", densityRevealed, hazeOpacityScale)
    - .planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-PATTERNS.md (section "lod.js:88-125 overview count / density" — exact change: cap is additive after overviewCount; import to add)
    - tests/viz-lod-density.test.ts (the buildCtx + overviewOf harness this task extends)
  </read_first>
  <behavior>
    - When overviewCount > OVERVIEW_NODE_CAP, the number of nodes classified into the overview (schema + haze visible) is held at ≤ OVERVIEW_NODE_CAP.
    - Schema super-nodes survive first, ranked largest-first by member count (schemaMembers .size); haze fills the remaining budget; surplus haze is suppressed from the overview.
    - In-band (overviewCount ≤ cap): pure no-op — existing densityRevealed and hazeOpacityScale behavior unchanged (founder ~2,700 look intact).
    - Suppressed nodes remain reachable via drill-in/trace (their reveal paths are untouched).
  </behavior>
  <action>
    Add `OVERVIEW_NODE_CAP` to the `./constants.js` import list (lod.js:33-40). After `overviewCount` is computed (lod.js:101-102), add a cap branch (additive — do NOT alter the existing `DENSITY_FILL_BELOW` sparse-fill or the `DENSITY_THIN` hazeOpacityScale lerp): when `overviewCount > OVERVIEW_NODE_CAP`, suppress the long-tail haze nodes so the visible overview count is held at the cap (D-05). Survive-ranking (D-06): keep ALL schema super-nodes (the constellation is the point), then admit haze up to `OVERVIEW_NODE_CAP - (#schema kept)`; suppress haze beyond that. Reuse the `DENSITY_FILL` largest-schema-first idiom (`[...schemaMembers.entries()].sort((a,b)=>b[1].size-a[1].size)`) only as the documented ranking reference — the actual survive set here is schema-first then haze-remainder. Suppress by flipping the surplus haze nodes out of the overview classification (e.g. demote `__cat` away from 'haze' so `nodeVisible` hides them) WITHOUT touching the member drill-in or trace-reveal paths. BRAIN_SCALE/nodeRadius are LOCKED — the cap acts on COUNTS only, never on sizing.
  </action>
  <verify>
    <automated>npm test -- tests/viz-lod-density.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New cap cases added to tests/viz-lod-density.test.ts: a ctx with overview far above OVERVIEW_NODE_CAP (e.g. cap+thousands of haze) yields a visible overview count ≤ OVERVIEW_NODE_CAP; schema nodes are all retained; a sub-cap ctx stays a pure no-op (existing assertions still pass).
    - `lod.js` imports `OVERVIEW_NODE_CAP` from constants.js and references it in the density block.
    - Existing in-band / sparse / dense assertions in viz-lod-density.test.ts still pass (no regression to the founder anchor band).
    - No change to nodeRadius/BRAIN_SCALE usage; member drill-in and trace-reveal logic untouched.
  </acceptance_criteria>
  <done>Overview held at OVERVIEW_NODE_CAP with schema-first ranking; lod-density suite (old + new cases) green; drill-in/trace unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph payload → client LOD | Existing, unchanged. The cap only chooses how many already-delivered nodes are rendered at overview; no new data, no new input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03 | Information Disclosure | overview cap suppression | accept | Suppression is render-only and decorative; suppressed nodes are still in the client payload and reachable via the existing drill-in/trace paths — the cap hides nothing not already client-side. No new boundary. |
| T-53-04 | Denial of Service | density adaptation at scale | mitigate | Capping the overview node count bounds render work as the corpus grows (D-05) — reduces, not adds, scale pressure. |

No high-severity threats: presentation-layer count math over an already-delivered payload; zero new data/network/auth/input surface.
</threat_model>

<verification>
- `npm test -- tests/viz-lod-density.test.ts` green (existing band assertions + new cap cases).
- Source greps: `OVERVIEW_NODE_CAP` exported in constants.js and imported/used in lod.js; BRAIN_SCALE = 460 and SETTLE_BUDGET_MS intact.
</verification>

<success_criteria>
- Overview fullness held at OVERVIEW_NODE_CAP regardless of corpus size (D-05 / ROADMAP SC2).
- Long tail dropped deliberately via schema-largest-first ranking (D-06).
- Members/detail still reachable on drill-in/trace (scope preserved).
</success_criteria>

<output>
Create `.planning/phases/53-brain-layout-at-scale-declutter-deliberate-clustering/53-02-SUMMARY.md` when done.
</output>
