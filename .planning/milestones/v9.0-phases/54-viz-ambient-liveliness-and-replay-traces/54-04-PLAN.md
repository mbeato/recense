---
phase: 54-viz-ambient-liveliness-and-replay-traces
plan: 04
type: execute
wave: 3
depends_on: [54-01, 54-02, 54-03]
files_modified:
  - tests/viz-ambient-liveliness.test.ts
autonomous: true
requirements: [SC1, SC3, SC5]

must_haves:
  truths:
    - "A new vitest guard file machine-checks the structural invariants of all three layers so they cannot silently regress (SC5 — machine-verified)"
    - "Guards assert: 10 constants exported + REPLAY_DIM<1; live coefficients sourced from gains; replay branch + replay:true plumbed; twinkle uses setColorAt-only with no spawnPulse/halo; server keeps exactly one read-only DB handle and no cursor rewind inside the replay-scheduler block (SC1/SC3/SC5 — machine-verified)"
    - "Existing Phase 52 honesty guards still pass under the same run (no fabricated edges — SC3)"
  artifacts:
    - path: "tests/viz-ambient-liveliness.test.ts"
      provides: "Phase 54 machine guards for SC1/SC3/SC5"
      contains: "ACT_SCALE_GAIN"
  key_links:
    - from: "tests/viz-ambient-liveliness.test.ts"
      to: "constants.js + trace.js + server.ts"
      via: "source-text token assertions (stripComments pattern from viz-layout-guards)"
      pattern: "readFileSync"
---

<objective>
Create `tests/viz-ambient-liveliness.test.ts` — the consolidated machine-checkable guard file for Phase 54. Modeled on the Phase 53 `tests/viz-layout-guards.test.ts` source-text pattern (read file as text, strip comment lines, assert locked structural tokens), it locks the SC1/SC3/SC5 invariants across all three implemented files so they cannot silently regress. This file is written ONCE here (single owner) after the implementation plans land, avoiding any shared-test-file merge conflict.

Purpose: Make the honesty hierarchy (live > replay > twinkle), the read-only/no-fetch server boundary, the forward-only cursor, and the no-fabricated-edges invariant machine-enforced — not just founder-felt.
Output: a passing vitest guard file covering the machine-checkable slices of SC1, SC3, SC5 (SC2/SC4 felt-quality remain the founder checkpoint in Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Design contract — success criteria SC1-SC5 + "Constraints preserved"
@docs/superpowers/specs/2026-06-30-viz-ambient-liveliness-replay-traces-design.md
# Research — "Validation Architecture" (test framework + req→test map) + honesty-guard table
@.planning/phases/54-viz-ambient-liveliness-and-replay-traces/54-RESEARCH.md

<interfaces>
<!-- Reuse the exact source-text-guard pattern from tests/viz-layout-guards.test.ts: -->
<!--   import {describe,it,expect} from 'vitest'; fs.readFileSync(...,'utf8'); -->
<!--   stripComments(src) drops lines starting with `*` or `//` so JSDoc prose can't satisfy a gate; -->
<!--   ROOT = path.resolve(__dirname, '..'); read src/viz/modules/{constants,trace}.js + src/viz/server.ts. -->
<!-- vitest run command: npx vitest run tests/viz-ambient-liveliness.test.ts -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase 54 source-text guard suite</name>
  <files>tests/viz-ambient-liveliness.test.ts</files>
  <read_first>
    - tests/viz-layout-guards.test.ts (full — copy the stripComments + readFileSync + ROOT pattern exactly)
    - tests/trace-honest-recall.test.ts (the Phase 52 honesty guards this suite must NOT duplicate but must coexist with)
    - src/viz/modules/constants.js (the 10 new constants — Plan 01 landed)
    - src/viz/modules/trace.js (Plan 02: ACT_SCALE_GAIN/ACT_BRIGHTEN_GAIN/ACT_HAZE_LERP swaps, row.replay branch, twinkleTick)
    - src/viz/server.ts (Plan 03: replayBuffer, replay:true, single read-only Database, replay scheduler — note the baseline cursor assignments at ~line 381 declaration and ~line 415 poll update)
    - .planning/phases/54-viz-ambient-liveliness-and-replay-traces/54-RESEARCH.md ("Phase Requirements → Test Map" + "Security Domain" threat→guard table)
  </read_first>
  <action>
    Create `tests/viz-ambient-liveliness.test.ts` using the `viz-layout-guards.test.ts` structure (readFileSync + stripComments on `*`/`//` lines + token assertions on the stripped source; do NOT import the browser modules at runtime). Assert, grouped by success criterion:
    SC5/constants — constants.js (stripped) exports all 10 names; assert `REPLAY_DIM` numeric value parses to `< 1`.
    SC2-machine/Layer1 — trace.js (stripped) contains `1 + a * ACT_SCALE_GAIN`, `a * ACT_BRIGHTEN_GAIN`, `a * ACT_HAZE_LERP`; and `setMatrixAt` does NOT appear anywhere in trace.js (perf invariant).
    SC3/Layer2-client — trace.js contains a `row.replay === true` branch and a replay `activate(` call multiplied by `REPLAY_DIM`; the replay branch references `traceEdgesFromHops` (honest hops, no ctx.adj).
    SC1+SC3/Layer3 — trace.js registers `twinkleTick` via `ctx.registerTick`; the twinkle code path contains no `spawnPulse(` reachable from twinkle and no `setMatrixAt` (assert global trace.js `setMatrixAt` absence covers this).
    SC1+SC5/server — server.ts (stripped) contains `replay: true`, declares `replayBuffer`, has exactly one `new Database(` occurrence, contains no `fetch(`/`embed(`/`provider` token, AND the replay-scheduler block does NOT rewind/reassign the forward-only `cursor`. Implement this cursor guard precisely: locate the replay-scheduler block by its opening marker `replayInterval = setInterval(`, extract that block's body (from the opening marker to its matching closing of the setInterval callback), and assert NO `cursor =` / `cursor=` assignment appears INSIDE that extracted block. Do NOT use a whole-file `cursor =` count gate: the live server.ts already has TWO baseline assignments at rest — the `let cursor = ...` declaration (~line 381) and the `cursor = fresh[...].id` forward poll update (~line 415) — so a `count == 1` (or `== 2`) whole-file gate is both wrong and trivially defeatable. The real invariant is that the replay path NEVER touches `cursor`; encode exactly that (zero `cursor =` assignments inside the replay-scheduler block).
    Use the stripComments helper so comment prose cannot satisfy or invalidate a gate. Keep each assertion as its own `it(...)` with a descriptive name tied to its SC.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-ambient-liveliness.test.ts tests/trace-honest-recall.test.ts tests/viz-layout-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/viz-ambient-liveliness.test.ts` exists and `npx vitest run tests/viz-ambient-liveliness.test.ts` exits 0
    - Suite asserts: 10 constants exported + REPLAY_DIM<1 (SC5); the three Layer-1 gain tokens present + no setMatrixAt in trace.js (SC2/SC5); row.replay branch + REPLAY_DIM in activate + traceEdgesFromHops (SC3); twinkleTick registered + no spawnPulse/setMatrixAt in twinkle (SC1/SC3); server replay:true + replayBuffer + exactly one `new Database(` + no fetch/embed/provider + zero `cursor =` assignments inside the replay-scheduler block (SC1/SC5)
    - Uses the stripComments source-text pattern (no runtime import of browser modules)
    - Phase 52 honesty guards still pass in the same run: `tests/trace-honest-recall.test.ts` exits 0
  </acceptance_criteria>
  <done>The Phase 54 guard suite is green and locks the SC1/SC3/SC5 structural invariants across constants.js, trace.js, and server.ts; Phase 52 honesty + Phase 53 layout guards unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression gate</name>
  <files>tests/viz-ambient-liveliness.test.ts</files>
  <read_first>
    - .planning/phases/54-viz-ambient-liveliness-and-replay-traces/54-RESEARCH.md ("Sampling Rate" — per-wave-merge runs the full suite; "Phase gate: full suite green")
  </read_first>
  <action>
    Run the full vitest suite to confirm Phase 54 changes (constants.js, trace.js, server.ts, new test file) introduce no regression anywhere — viz tests, server tests, and engine tests. If any pre-existing-unrelated failure surfaces, record it in the SUMMARY as a known-prior failure (do NOT fix out-of-scope code); if a failure traces to a Phase 54 change, fix the responsible plan's file. No new source edits beyond the test file are expected here.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (full suite green), OR any non-zero is attributable solely to a documented pre-existing failure unrelated to Phase 54, recorded in the SUMMARY
    - No Phase 54 file (constants.js, trace.js, server.ts, viz-ambient-liveliness.test.ts) is the cause of any failure
  </acceptance_criteria>
  <done>Full suite green (or only pre-existing unrelated failures, documented); Phase 54 introduces no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test ↔ source | Tests read source as text only; they assert the boundary/honesty invariants the other plans must hold. No runtime execution of browser modules. |

## STRIDE Threat Register

No external threat surface — this plan adds a read-only test file. Its purpose is to MACHINE-ENFORCE the other plans' boundary invariants (the phase's real "threats"). Stated explicitly.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-09 | Tampering (regression) | all three Phase-54 invariants | mitigate | This suite is the enforcement: REPLAY_DIM<1, setColorAt-only, single read-only DB handle, single forward cursor, no fabricated edges all asserted as CI gates. |
</threat_model>

<verification>
- `npx vitest run tests/viz-ambient-liveliness.test.ts` green; `npx vitest run` (full suite) green or only documented prior failures.
</verification>

<success_criteria>
- SC1/SC3/SC5 machine slices locked as CI guards; Phase 52/53 guards unaffected. SC2/SC4 felt-quality deferred to Plan 05 founder checkpoint.
</success_criteria>

<output>
Create `.planning/phases/54-viz-ambient-liveliness-and-replay-traces/54-04-SUMMARY.md` when done.
</output>
</output>
