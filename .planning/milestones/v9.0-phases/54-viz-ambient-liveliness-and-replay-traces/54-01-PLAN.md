---
phase: 54-viz-ambient-liveliness-and-replay-traces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/viz/modules/constants.js
autonomous: true
requirements: [SC5]

must_haves:
  truths:
    - "All 10 Phase-54 tunables are exported from constants.js and importable by trace.js + readable by guard tests (SC5 — machine-verified)"
    - "REPLAY_DIM < 1 so a replay echo can never escalate above a live recall by construction (SC3 — machine-verified)"
  artifacts:
    - path: "src/viz/modules/constants.js"
      provides: "Phase 54 ambient-liveliness tunables (10 new exports)"
      contains: "export const ACT_SCALE_GAIN"
  key_links:
    - from: "src/viz/modules/trace.js"
      to: "src/viz/modules/constants.js"
      via: "ESM import of ACT_SCALE_GAIN / ACT_BRIGHTEN_GAIN / ACT_HAZE_LERP / REPLAY_DIM / TWINKLE_*"
      pattern: "import .*constants"
---

<objective>
Add the 10 Phase-54 tunable constants to `src/viz/modules/constants.js` as the shared, single-source-of-truth foundation that Layer 1 (amplification, Plan 02), Layer 2 (replay, Plans 02+03), and Layer 3 (twinkle, Plan 02) all consume. Starting values are the spec midpoints; the founder dials them at the visual checkpoint (Plan 05).

Purpose: Eliminate the interface scavenger-hunt — downstream plans import named constants rather than hard-coding magic numbers, and the guard test (Plan 04) asserts they exist.
Output: `constants.js` with a new "Phase 54 — ambient liveliness" export block (10 `export const` lines matching the existing style).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Design contract (source of truth) — "Tunable constants" section
@docs/superpowers/specs/2026-06-30-viz-ambient-liveliness-replay-traces-design.md
# Research — "Constants Export Pattern" section gives exact values + style
@.planning/phases/54-viz-ambient-liveliness-and-replay-traces/54-RESEARCH.md

<interfaces>
<!-- Existing constants.js export style (VERIFIED live source, research lines 275-292). -->
<!-- Every constant is a flat top-level `export const NAME = value;` with a JSDoc comment -->
<!-- above. No namespacing, no default export, no object grouping. Match exactly. -->
<!-- Existing FPS constants live near the tail: IDLE_FPS=24, FULL_FPS=60, DEGRADE_FPS=45. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 10 Phase-54 ambient-liveliness constants</name>
  <files>src/viz/modules/constants.js</files>
  <read_first>
    - src/viz/modules/constants.js (full — observe the flat `export const NAME = value;` + JSDoc style; locate the FPS block IDLE_FPS/FULL_FPS/DEGRADE_FPS to append after)
    - docs/superpowers/specs/2026-06-30-viz-ambient-liveliness-replay-traces-design.md ("Tunable constants" section — the 10 names + their tuning ranges)
    - .planning/phases/54-viz-ambient-liveliness-and-replay-traces/54-RESEARCH.md ("Constants Export Pattern" — exact starting values, all spec midpoints)
  </read_first>
  <action>
    Append a new JSDoc-headed block "Phase 54 — ambient liveliness (tuned at founder visual checkpoint)" to the end of constants.js, adding exactly these 10 flat exports with the research starting values (spec midpoints):
    Layer 1 — `ACT_SCALE_GAIN = 0.9` (size-pulse gain; replaces the inline `0.35`), `ACT_BRIGHTEN_GAIN = 0.55` (opacity gain; replaces inline `0.4`), `ACT_HAZE_LERP = 0.95` (haze color-lerp factor; replaces inline `0.8`).
    Layer 2 — `REPLAY_IDLE_GAP_MS = 5000`, `REPLAY_CADENCE_MS = 4000`, `REPLAY_DIM = 0.5`, `REPLAY_HISTORY_N = 20`.
    Layer 3 — `TWINKLE_COUNT = 80`, `TWINKLE_PERIOD_MS = 2000`, `TWINKLE_AMP = 0.15`.
    Match the existing `export const NAME = value; // comment` style verbatim. Each comment must note the spec tuning range so the founder knows the safe envelope. Do NOT modify any existing constant (leave DECAY_ATTACK_MS=140 untouched — its optional shortening is a founder-checkpoint tuning candidate, deferred to Plan 05). Do NOT add object grouping, default exports, or namespacing.
  </action>
  <verify>
    <automated>grep -E "^export const (ACT_SCALE_GAIN|ACT_BRIGHTEN_GAIN|ACT_HAZE_LERP|REPLAY_IDLE_GAP_MS|REPLAY_CADENCE_MS|REPLAY_DIM|REPLAY_HISTORY_N|TWINKLE_COUNT|TWINKLE_PERIOD_MS|TWINKLE_AMP) " src/viz/modules/constants.js | grep -c "" | grep -qx 10 && node --input-type=module -e "import('./src/viz/modules/constants.js').then(m=>{const ok=m.REPLAY_DIM<1 && m.ACT_SCALE_GAIN>0 && m.TWINKLE_COUNT>0; process.exit(ok?0:1)})"</automated>
  </verify>
  <acceptance_criteria>
    - constants.js exports `ACT_SCALE_GAIN`, `ACT_BRIGHTEN_GAIN`, `ACT_HAZE_LERP`, `REPLAY_IDLE_GAP_MS`, `REPLAY_CADENCE_MS`, `REPLAY_DIM`, `REPLAY_HISTORY_N`, `TWINKLE_COUNT`, `TWINKLE_PERIOD_MS`, `TWINKLE_AMP` (exactly 10 new flat exports)
    - `REPLAY_DIM` value is strictly `< 1` (honesty-by-construction: replay can never escalate above live — SC3)
    - No existing constant is modified (DECAY_ATTACK_MS still `140`)
    - `npx vitest run tests/viz-layout-guards.test.ts` exits 0 (existing constants.js guards still pass — no regression)
  </acceptance_criteria>
  <done>All 10 constants exported in the existing flat style; REPLAY_DIM < 1; existing constants untouched; layout/constants guard test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure constant declarations; no input, no I/O, no execution surface. |

## STRIDE Threat Register

No high-severity external threats exist for this plan — it adds inert numeric exports with no input, network, DB, or execution path. Stated explicitly rather than inventing impossible cases.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | REPLAY_DIM value | mitigate | Boundary invariant `REPLAY_DIM < 1` is asserted in the verify gate and re-asserted by the Plan 04 guard test — replay cannot be tuned to escalate above live. |
</threat_model>

<verification>
- 10 named exports present and importable; REPLAY_DIM < 1; existing constants.js guards green (`tests/viz-layout-guards.test.ts`).
</verification>

<success_criteria>
SC5 (foundation slice): all Phase-54 tunables are exported and importable, with the replay-can-never-escalate invariant (REPLAY_DIM < 1) baked into the value itself.
</success_criteria>

<output>
Create `.planning/phases/54-viz-ambient-liveliness-and-replay-traces/54-01-SUMMARY.md` when done.
</output>
</output>
