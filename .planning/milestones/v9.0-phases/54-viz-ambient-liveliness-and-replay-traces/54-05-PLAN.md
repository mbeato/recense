---
phase: 54-viz-ambient-liveliness-and-replay-traces
plan: 05
type: execute
wave: 4
depends_on: [54-02, 54-03, 54-04]
files_modified:
  - src/viz/modules/constants.js
autonomous: false
requirements: [SC1, SC2, SC3, SC4]

must_haves:
  truths:
    - "On the live viz, pinned with no user recalls, the brain shows continuous gentle twinkle punctuated by periodic replay echoes of real recent recalls (SC1 — FOUNDER-CONFIRMED)"
    - "A real recall is clearly noticeable at overview zoom, distinctly stronger than a replay echo (SC2 — FOUNDER-CONFIRMED)"
    - "live > replay > twinkle is always visually legible; replays read as rehearsal, not live; no fabricated edges (SC3 — FOUNDER-CONFIRMED visual; SC3 structure machine-verified in Plan 04)"
    - "The ambient layers read as meaningful, not loud/noisy (SC4 — FOUNDER-CONFIRMED felt-quality)"
    - "Final tuned constant values are committed to constants.js after the founder dials them"
  artifacts:
    - path: "src/viz/modules/constants.js"
      provides: "founder-tuned Phase 54 constant values"
      contains: "ACT_SCALE_GAIN"
  key_links:
    - from: "founder visual judgment on the live viz"
      to: "src/viz/modules/constants.js tunables"
      via: "iterative dial-in at the checkpoint (Phase 52/53 idiom)"
      pattern: "ACT_SCALE_GAIN"
---

<objective>
Run the founder visual checkpoint on the live `recense viz` to confirm the felt-quality success criteria (SC1–SC4) and dial the 9 visual-tuning constants to taste (of the 10 Phase-54 exports; REPLAY_HISTORY_N is structural, not a felt dial) — the established Phase 52/53 idiom ("founder checkpoint approved"). The implementation (Plans 02/03) and machine guards (Plan 04) are already green; this plan is the human felt-quality gate plus the final constant tuning. It is the only non-autonomous plan in the phase.

Purpose: SC1/SC2/SC4 (and the visual half of SC3) are felt-quality calls that only the founder can make on the live brain. This plan captures that judgment and bakes the chosen values in.
Output: founder-approved liveliness on the live viz; final tuned values committed to constants.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Design contract — "Success criteria" (SC1-SC4 felt-quality) + "Goal feel" line
@docs/superpowers/specs/2026-06-30-viz-ambient-liveliness-replay-traces-design.md
# Research — Pitfall 4 (ACT_SCALE_GAIN overlap on dense schema neighborhoods) + starting values
@.planning/phases/54-viz-ambient-liveliness-and-replay-traces/54-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare and launch the live viz for visual review</name>
  <files>(no source change — operational)</files>
  <read_first>
    - src/viz/server.ts (confirm the launch path / CLI entry used to start `recense viz`)
    - .planning/phases/54-viz-ambient-liveliness-and-replay-traces/54-RESEARCH.md ("Validation Architecture" + the three-layer system diagram for what the founder should see)
  </read_first>
  <action>
    Confirm the full guard suite is green (`npx vitest run tests/viz-ambient-liveliness.test.ts tests/trace-honest-recall.test.ts tests/viz-layout-guards.test.ts`), then start the live viz the same way the founder runs it (`recense viz`) against the real brain DB. Surface the local URL. Provide the founder a brief on-screen orientation: (1) leave it pinned/idle to observe twinkle + periodic replay echoes; (2) trigger a real recall (run a normal Claude Code prompt that exercises retrieval) to observe the amplified live flash; (3) compare live vs replay vs twinkle intensity. Note Pitfall 4: ask the founder to specifically check dense SCHEMA neighborhoods for node overlap at peak activation (ACT_SCALE_GAIN × the schema __actGain=1.2 can overlap neighbors — drop ACT_SCALE_GAIN toward 0.7 if so).
  </action>
  <verify>
    <automated>npx vitest run tests/viz-ambient-liveliness.test.ts tests/trace-honest-recall.test.ts tests/viz-layout-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Guard suite green before launch
    - Live viz running against the real brain DB; local URL surfaced to the founder
    - Founder oriented on what to observe for SC1 (idle twinkle+replay), SC2 (live pop), SC3 (hierarchy legibility), SC4 (not distracting), plus the Pitfall-4 schema-overlap check
  </acceptance_criteria>
  <done>Guards green, live viz up against the real DB, founder oriented for the SC1–SC4 review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Founder visual checkpoint — confirm SC1–SC4 and dial constants</name>
  <what-built>
    Three-layer ambient activity on the live viz: amplified live recall flash (Layer 1), idle replay echoes of recent real recalls (Layer 2), and continuous neutral twinkle (Layer 3) — all machine-guarded for honesty (live > replay > twinkle, no fabricated edges, read-only server boundary).
  </what-built>
  <how-to-verify>
    On the live viz (URL from Task 1):
    1. SC1 (alive at idle): pin it, run NO recalls for ~30s. Confirm continuous gentle twinkle, punctuated by periodic replay echoes (~every REPLAY_CADENCE_MS after ~REPLAY_IDLE_GAP_MS of quiet).
    2. SC2 (events pop): trigger a real recall. Confirm the live flash is clearly noticeable at overview zoom and distinctly STRONGER than a replay echo.
    3. SC3 (honest hierarchy): confirm live > replay > twinkle is always legible — replays read as rehearsal (dimmer/no strong cue), not live; no fabricated edges appear; twinkle never uses the event palette.
    4. SC4 (not distracting): confirm the ambient layers read as meaningful, not loud/noisy. "Noticeable and meaningful, never loud/distracting" (spec goal feel).
    5. Pitfall 4: in dense schema clusters, confirm peak-activation nodes do not visibly overlap/merge.
    Iterate on the 9 visual-tuning constants in `src/viz/modules/constants.js` until the feel is right (of the 10 exports; REPLAY_HISTORY_N is structural, not tuned to taste — ACT_SCALE_GAIN/ACT_BRIGHTEN_GAIN/ACT_HAZE_LERP for live pop; REPLAY_IDLE_GAP_MS/REPLAY_CADENCE_MS/REPLAY_DIM for echo cadence+dimness; TWINKLE_COUNT/PERIOD/AMP for ambient feel). Optionally shorten DECAY_ATTACK_MS for a snappier attack (spec D-06: keep the hold/fade envelope). Keep REPLAY_DIM < 1 (honesty invariant — the Plan 04 guard enforces it). Reload the viz to pick up edits.
  </how-to-verify>
  <resume-signal>Type "approved" once SC1–SC4 hold and the constants are dialed, or describe what still feels off (too subtle / too loud / replay indistinguishable from live / schema overlap) for another tuning pass.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit the founder-tuned constants</name>
  <files>src/viz/modules/constants.js</files>
  <read_first>
    - src/viz/modules/constants.js (the Phase 54 block — confirm the founder's dialed values are in place and REPLAY_DIM is still < 1)
    - tests/viz-ambient-liveliness.test.ts (the guard that enforces REPLAY_DIM < 1 + the 10 exports)
  </read_first>
  <action>
    Persist the founder's final tuned values in the Phase 54 block of constants.js (the values are whatever the founder settled on during Task 2). If DECAY_ATTACK_MS was shortened, keep DECAY_HOLD_MS/DECAY_FADE_MS untouched (D-06 envelope shape). Re-run the guard suite to confirm REPLAY_DIM < 1 and the 10 exports still hold after tuning. Do NOT touch trace.js or server.ts (logic is final; this is values-only).
  </action>
  <verify>
    <automated>npx vitest run tests/viz-ambient-liveliness.test.ts tests/trace-honest-recall.test.ts tests/viz-layout-guards.test.ts && grep -E "^export const REPLAY_DIM " src/viz/modules/constants.js</automated>
  </verify>
  <acceptance_criteria>
    - Founder-tuned values committed in constants.js; REPLAY_DIM still `< 1` (guard green)
    - `npx vitest run tests/viz-ambient-liveliness.test.ts` exits 0 after tuning
    - trace.js and server.ts unchanged by this plan (values-only)
  </acceptance_criteria>
  <done>Final founder-approved constant values are committed; the honesty guard (REPLAY_DIM < 1, 10 exports) still passes; logic files untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder ↔ tunables | Founder dials decorative-chrome constants. The honesty invariant (REPLAY_DIM<1) is machine-guarded so tuning cannot break the live>replay>twinkle hierarchy. |

## STRIDE Threat Register

No external threat surface — value tuning of inert constants plus a human visual review. The one invariant a careless tuning could violate (replay escalating above live) is machine-guarded. Stated explicitly.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-10 | Tampering | REPLAY_DIM during tuning | mitigate | Plan 04 guard `REPLAY_DIM < 1` is re-run after tuning (Task 3 verify) — a tuning that breaks the honesty hierarchy fails the gate. |
</threat_model>

<verification>
- Founder types "approved" for SC1–SC4 on the live viz.
- Post-tuning: `npx vitest run tests/viz-ambient-liveliness.test.ts tests/trace-honest-recall.test.ts tests/viz-layout-guards.test.ts` green; REPLAY_DIM < 1.
</verification>

<success_criteria>
- SC1, SC2, SC4 and the visual half of SC3 FOUNDER-CONFIRMED on the live viz.
- Final tuned constants committed; honesty guard still green.
</success_criteria>

<output>
Create `.planning/phases/54-viz-ambient-liveliness-and-replay-traces/54-05-SUMMARY.md` when done.
</output>
