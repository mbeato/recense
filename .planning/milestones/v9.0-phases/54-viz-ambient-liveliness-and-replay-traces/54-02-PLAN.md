---
phase: 54-viz-ambient-liveliness-and-replay-traces
plan: 02
type: execute
wave: 2
depends_on: [54-01]
files_modified:
  - src/viz/modules/trace.js
autonomous: true
requirements: [SC1, SC2, SC3]

must_haves:
  truths:
    - "A real recall flash is amplified (size-pulse and brighten driven by ACT_SCALE_GAIN / ACT_BRIGHTEN_GAIN / ACT_HAZE_LERP) so it pops at overview zoom (SC2 — machine-verified token swap; felt-quality founder-confirmed in Plan 05)"
    - "applyTrace renders a row.replay===true row as the SAME real hops at REPLAY_DIM intensity with no extra-strong live cue, so a replay reads as rehearsal not live (SC3 — machine-verified)"
    - "A twinkle tick on the master rAF loop gently breathes a small rotating subset of haze nodes with a neutral tint, no pulses/halos/event-colors, giving continuous idle life (SC1 — machine-verified structure; felt-quality founder-confirmed in Plan 05)"
    - "Twinkle and replay fabricate no edges and never call spawnPulse/revealTrace from the twinkle path — Phase 52 honesty invariant intact (SC3 — machine-verified)"
    - "Haze nodes are touched via setColorAt only (never setMatrixAt) in amplify/replay/twinkle — perf invariant preserved (SC5 — machine-verified)"
  artifacts:
    - path: "src/viz/modules/trace.js"
      provides: "Layer1 amplification + Layer2 client replay branch + Layer3 twinkle tick"
      contains: "ACT_SCALE_GAIN"
  key_links:
    - from: "src/viz/modules/trace.js applyTrace"
      to: "row.replay flag from server.ts (Plan 03)"
      via: "early replay branch before kind dispatch"
      pattern: "row.replay"
    - from: "src/viz/modules/trace.js twinkleTick"
      to: "stats.js master rAF loop"
      via: "ctx.registerTick"
      pattern: "ctx.registerTick"
---

<objective>
Implement all three client-side render layers in `src/viz/modules/trace.js` in a single plan so this file has exactly ONE owner this wave (no merge conflict with any other plan):
- **Layer 1 — Live amplification:** route the existing real-event flash through the new gain constants so it pops at node scale.
- **Layer 2 — Replay echo (client):** render `row.replay === true` rows as a dimmer echo of the same real hops.
- **Layer 3 — Twinkle:** a bounded ambient brightness shimmer on a rotating haze subset via the master tick.

Purpose: Make the brain feel alive at idle and make real recalls noticeable, while preserving the Phase 52 honesty invariant (pulses/halos only on real hops; nothing fabricates edges) and the haze perf invariant (`setColorAt` only).
Output: amplified live render, a `row.replay` branch in `applyTrace`, and a `twinkleTick` registered via `ctx.registerTick`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Design contract (source of truth) — Layers 1, 2 (client), 3
@docs/superpowers/specs/2026-06-30-viz-ambient-liveliness-replay-traces-design.md
# Phase 52 honesty predecessor — the invariant that MUST NOT regress
@docs/superpowers/specs/2026-06-29-brain-viz-honest-traces-design.md
# Research — exact line anchors + RESOLVED twinkle keep-alive question + pitfalls
@.planning/phases/54-viz-ambient-liveliness-and-replay-traces/54-RESEARCH.md

<interfaces>
<!-- VERIFIED live source. Layer 1 regular-node path, trace.js ~399-402: -->
<!--   const a = Math.max(0, node.__act) * (node.__actGain || 1); -->
<!--   if (node.__base) node.__mat.color.copy(node.__base).lerp(node.__actColor||HOT_COLOR, a*0.8); -->
<!--   node.__mat.opacity = Math.min(1, (node.__baseOp||0.85) + a*0.4); -->
<!--   node.__mesh.scale.setScalar((node.__baseR||1) * (1 + a*0.35)); -->
<!-- Layer 1 haze path, trace.js ~362-366 (setColorAt ONLY, never setMatrixAt): -->
<!--   const activationColor = node.__hazeBase.clone().lerp(node.__actColor||HOT_COLOR, a*0.8); -->
<!--   ctx.hazeMesh.setColorAt(node.__hazeIdx, activationColor); -->
<!-- activate(node, level, kindColor) — trace.js 250: halo intensity = level; -->
<!--   calling activate(node, level*REPLAY_DIM) yields a proportionally dimmer halo. -->
<!-- applyTrace(row) — trace.js 510: back-compat guard (512-514) → kind dispatch (517-521) → recall path. -->
<!--   The row.replay branch slots AFTER the array back-compat guard, BEFORE the kind dispatch. -->
<!-- ctx.registerTick(fn) — stats.js 45: fn called every frame; master loop runs at IDLE_FPS(24) -->
<!--   when idle (does NOT sleep — research RESOLVED; no keep-alive needed). -->
<!-- ctx.hazeMesh / __hazeIdx / __hazeBase: haze instance addressing. ctx.allNodes: all nodes. -->
<!--   __cat ('haze'|'schema'|'member') from lod.js. Twinkle subset = __cat==='haze' && __hazeIdx!=null. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Layer 1 — amplify the live recall flash through gain constants</name>
  <files>src/viz/modules/trace.js</files>
  <behavior>
    - The regular-node scale at peak activation `a` is `(__baseR) * (1 + a * ACT_SCALE_GAIN)` — strictly larger than the old `1 + a*0.35` for the same `a` (so it pops).
    - The regular-node opacity gain reads from `ACT_BRIGHTEN_GAIN` (replacing the inline `a*0.4`).
    - The haze color lerp reads from `ACT_HAZE_LERP` (replacing the inline `a*0.8`), still via `setColorAt` only.
    - No change to WHICH nodes/edges light — only intensity. Phase 52 recall path (seed/hop selection) byte-unchanged.
  </behavior>
  <read_first>
    - src/viz/modules/trace.js lines 360-403 (the haze color path ~362-366 and the regular-node scale/opacity path ~399-402; note the expired-node restore at 391-396 uses `__baseR` — do NOT touch it)
    - src/viz/modules/trace.js top import line (add the 3 new constants to the existing constants import)
    - docs/superpowers/specs/2026-06-30-viz-ambient-liveliness-replay-traces-design.md ("Layer 1 — Live recall amplification")
    - .planning/phases/54-viz-ambient-liveliness-and-replay-traces/54-RESEARCH.md ("Layer 1 — Exact Integration Points" + Pitfall 4 on ACT_SCALE_GAIN overlap)
  </read_first>
  <action>
    Import `ACT_SCALE_GAIN`, `ACT_BRIGHTEN_GAIN`, `ACT_HAZE_LERP` from `./constants.js`. At the regular-node render (~line 402) replace the scale coefficient `1 + a * 0.35` with `1 + a * ACT_SCALE_GAIN`; at ~line 401 replace the opacity term `a * 0.4` with `a * ACT_BRIGHTEN_GAIN`. At the haze render (~line 364) replace the lerp factor `a * 0.8` with `a * ACT_HAZE_LERP` — keep it `setColorAt` only, never introduce `setMatrixAt`. Do NOT change `node.__actGain` (schema 1.2× still multiplies on top), do NOT change the expired-node restore block (391-396), do NOT alter seed/hop selection. Leave the decay-envelope constants alone (DECAY_ATTACK_MS tuning is deferred to the founder checkpoint).
  </action>
  <verify>
    <automated>grep -E "1 \+ a \* ACT_SCALE_GAIN" src/viz/modules/trace.js && grep -E "a \* ACT_BRIGHTEN_GAIN" src/viz/modules/trace.js && grep -E "a \* ACT_HAZE_LERP" src/viz/modules/trace.js && ! grep -nE "setMatrixAt" src/viz/modules/trace.js && npx vitest run tests/trace-honest-recall.test.ts tests/viz-haze-activation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - trace.js regular-node scale line contains `1 + a * ACT_SCALE_GAIN` (old `* 0.35` gone)
    - trace.js opacity line contains `a * ACT_BRIGHTEN_GAIN`; haze lerp contains `a * ACT_HAZE_LERP`
    - `grep setMatrixAt src/viz/modules/trace.js` returns nothing (perf invariant: haze is color-only)
    - `npx vitest run tests/trace-honest-recall.test.ts` exits 0 (Phase 52 honesty guards: no-BFS, score-intensity, null-score, back-compat all still pass)
    - `npx vitest run tests/viz-haze-activation.test.ts` exits 0
  </acceptance_criteria>
  <done>The three live-flash coefficients are sourced from the new constants; setMatrixAt never appears; Phase 52 honesty + haze tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Layer 2 (client) — replay echo branch in applyTrace</name>
  <files>src/viz/modules/trace.js</files>
  <behavior>
    - A row with `row.replay === true` lights the SAME seeds + same real hops as a live recall but at reduced intensity (each `activate(node, level * REPLAY_DIM)`), producing a proportionally dimmer halo (halo intensity = level).
    - A replay never produces an intensity ≥ the live equivalent for the same row (REPLAY_DIM < 1 guarantees it).
    - A replay fabricates no new edges and reuses the honest hop set from `row.hops` (no ctx.adj traversal).
    - A non-replay row behaves exactly as today (the branch returns before reaching the kind dispatch / recall path only when replay===true).
  </behavior>
  <read_first>
    - src/viz/modules/trace.js lines 250-301 (activate signature: halo level = `level`; calling with `level*REPLAY_DIM` dims proportionally)
    - src/viz/modules/trace.js lines 510-592 (applyTrace: back-compat guard 512-514, kind dispatch 517-521, recall path 523-591 — the replay branch slots between 514 and 517)
    - src/viz/modules/trace.js lines 530-583 (seed/hop normalization + activate calls to mirror at reduced intensity)
    - docs/superpowers/specs/2026-06-30-viz-ambient-liveliness-replay-traces-design.md ("Layer 2 — client" paragraph)
    - .planning/phases/54-viz-ambient-liveliness-and-replay-traces/54-RESEARCH.md ("applyTrace slot for row.replay" + "Simpler approach confirmed" + Pitfall 3 empty rows)
  </read_first>
  <action>
    Import `REPLAY_DIM` from `./constants.js`. In `applyTrace`, immediately AFTER the array back-compat guard (~line 514) and BEFORE the `const kind = row.kind ?? null` dispatch (~line 517), add `if (row.replay === true) { ... return; }`. Inside that branch, resolve seeds + hops using the SAME honest path as the recall branch (normalizeSeed for seeds, traceEdgesFromHops(row, ctx.idMap) for hops — never ctx.adj), then call `activate(node, intensity * REPLAY_DIM)` for every resolved seed and hop so both the per-node flash and its halo are proportionally dimmer. Reuse `ctx.revealTrace(pathNodes, [])` and the same fade-back setTimeout so the replay decays like a recall. Guard against empty/malformed rows: if no seed resolves (seedEntries empty), return without side effects (Pitfall 3). Do NOT spawn any extra-strong live-only cue. Do NOT mutate the recall path (523-591) — it stays byte-identical for non-replay rows.
  </action>
  <verify>
    <automated>grep -nE "row\.replay === true" src/viz/modules/trace.js && grep -nE "REPLAY_DIM" src/viz/modules/trace.js && grep -nE "traceEdgesFromHops" src/viz/modules/trace.js && npx vitest run tests/trace-honest-recall.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - trace.js `applyTrace` contains a `row.replay === true` branch that early-returns (placed before the `kind` dispatch)
    - The replay branch calls `activate(...)` with a `* REPLAY_DIM` factor (replay strictly dimmer than live — SC3)
    - The replay branch resolves hops via `traceEdgesFromHops` (honest hops; no ctx.adj — no fabricated edges)
    - The recall path (lines ~523-591) is byte-unchanged for non-replay rows: `npx vitest run tests/trace-honest-recall.test.ts` exits 0
  </acceptance_criteria>
  <done>applyTrace handles row.replay===true by re-rendering real hops at REPLAY_DIM intensity, fabricates no edges, and leaves the live recall path untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Layer 3 — bounded ambient twinkle tick</name>
  <files>src/viz/modules/trace.js</files>
  <read_first>
    - src/viz/modules/trace.js lines 328-403 (the existing tick contract comment + the haze setColorAt path to mirror)
    - src/viz/modules/trace.js line 489 (`ctx.registerTick(tick)` — register the twinkle tick the same way) and 212-250 (initTrace scope where state arrays/lazy init live)
    - src/viz/modules/stats.js lines 45, 219-266 (registerTick + the idle-throttle proof: master loop ticks at IDLE_FPS=24 when idle, no keep-alive needed)
    - src/viz/modules/lod.js line 83 (`__cat` classification — haze subset selection)
    - docs/superpowers/specs/2026-06-30-viz-ambient-liveliness-replay-traces-design.md ("Layer 3 — Ambient twinkle")
    - .planning/phases/54-viz-ambient-liveliness-and-replay-traces/54-RESEARCH.md ("Layer 3 Exact Integration Points", Pitfall 2 lazy rotation, Open Question 1 lazy init timing, Don't-Hand-Roll sine row)
  </read_first>
  <action>
    Import `TWINKLE_COUNT`, `TWINKLE_PERIOD_MS`, `TWINKLE_AMP` from `./constants.js`. Add a `twinkleTick(now)` function inside `initTrace` and register it with `ctx.registerTick(twinkleTick)` alongside the existing `ctx.registerTick(tick)` at ~line 489. Lazily build the twinkle subset on the FIRST tick (Open Question 1: ctx.hazeMesh / __hazeIdx may not exist when initTrace runs) — guard `if (!ctx.hazeMesh || !ctx.allNodes?.length) return;`, then on first run select up to TWINKLE_COUNT nodes filtered to `__cat === 'haze' && __hazeIdx != null` into a persistent `twinkleSubset` array with a `twinklePtr` (Pitfall 2: build once + rotate, do NOT re-filter allNodes every frame). Each frame, for each subset node, compute a stateless sine breathe `Math.sin((now + node.__hazeIdx * offset) / TWINKLE_PERIOD_MS * 2*Math.PI)` scaled to TWINKLE_AMP and lerp `node.__hazeBase` toward a NEUTRAL/COOL tint (NOT HOT_COLOR, NOT any KIND_COLOR/event palette), writing via `ctx.hazeMesh.setColorAt` + a single `instanceColor.needsUpdate = true` per tick. Skip any node currently in the active Set (don't fight a live/replay flash). Periodically rotate the subset (e.g. re-pick every TWINKLE_COUNT frames). Hard constraints: NO size change, NO spawnPulse, NO halo, NO ctx.revealTrace, NO setMatrixAt — pure color breathe on the haze instance buffer only.
  </action>
  <verify>
    <automated>grep -nE "ctx\.registerTick\(twinkleTick\)|registerTick\(.*twinkle" src/viz/modules/trace.js && grep -nE "TWINKLE_COUNT|TWINKLE_PERIOD_MS|TWINKLE_AMP" src/viz/modules/trace.js && ! grep -nE "setMatrixAt" src/viz/modules/trace.js && npx vitest run tests/trace-honest-recall.test.ts tests/viz-haze-activation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - trace.js registers a second tick (`twinkleTick`) via `ctx.registerTick` (SC1 — continuous idle life)
    - The twinkle subset is built lazily/once and rotated (no per-frame O(N) filter of ctx.allNodes — Pitfall 2)
    - twinkleTick touches haze via `setColorAt` only; `grep setMatrixAt src/viz/modules/trace.js` returns nothing (SC5 perf invariant)
    - twinkleTick contains no `spawnPulse`, no halo spawn, no `ctx.revealTrace`, no size/scale change (SC3 honesty: twinkle fabricates nothing)
    - `npx vitest run tests/trace-honest-recall.test.ts tests/viz-haze-activation.test.ts` exits 0
  </acceptance_criteria>
  <done>A bounded twinkle tick breathes a rotating haze subset with a neutral tint on the master loop, fabricates no edges/halos, uses setColorAt only, and leaves real-event paths untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client render ← SSE row | `applyTrace` receives rows (incl. `replay:true`) from the server SSE; rows originate from the read-only DB, not user input. |
| ambient layers vs honesty invariant | Twinkle/replay must never fabricate engine activity (edges/hops) or escalate above a live recall. |

## STRIDE Threat Register

The only meaningful "threats" here are the boundary-preservation invariants (the security block's framing). No high-severity external threats exist — this is browser render code with no network/DB/exec surface. Stated explicitly.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-02 | Tampering | twinkle/replay intensity | mitigate | Replay uses `activate(node, level*REPLAY_DIM)` with REPLAY_DIM<1; twinkle amplitude bounded by TWINKLE_AMP and writes a neutral tint only — cannot exceed or imitate a live flash. Asserted by Plan 04 guard. |
| T-54-03 | Repudiation/Tampering | fabricated edges | mitigate | Twinkle calls no spawnPulse/revealTrace; replay resolves hops via `traceEdgesFromHops` (row.hops only, no ctx.adj). Phase 52 `trace-honest-recall.test.ts` re-run each task. |
| T-54-04 | Denial of Service (perf) | haze instance buffer | mitigate | All haze writes are `setColorAt` (never `setMatrixAt`); twinkle subset bounded to TWINKLE_COUNT nodes/frame, built once + rotated (no per-frame O(N) scan). |
</threat_model>

<verification>
- Phase 52 honesty guards (`tests/trace-honest-recall.test.ts`) and haze activation (`tests/viz-haze-activation.test.ts`) green after all 3 tasks.
- `grep setMatrixAt src/viz/modules/trace.js` returns nothing.
- Machine guards for the token swaps + replay branch + twinkle tick are formalized in Plan 04.
</verification>

<success_criteria>
- SC2 (machine slice): live flash coefficients sourced from amplification gains; felt-quality confirmed in Plan 05.
- SC3 (machine slice): replay strictly dimmer (REPLAY_DIM), no fabricated edges in replay/twinkle.
- SC1 (machine slice): twinkle tick registered on the idle-running master loop; felt-quality confirmed in Plan 05.
</success_criteria>

<output>
Create `.planning/phases/54-viz-ambient-liveliness-and-replay-traces/54-02-SUMMARY.md` when done.
</output>
