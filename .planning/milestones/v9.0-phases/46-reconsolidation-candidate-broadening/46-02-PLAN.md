---
phase: 46-reconsolidation-candidate-broadening
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - scripts/eval/results/46-recon03-ku-bm25on.json
  - scripts/eval/results/46-recon03-ku-bm25off.json
autonomous: false
requirements: [RECON-03, RECON-04]
user_setup:
  - service: openai
    why: "Embedding model for the KU replay harness (embed step)"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI dashboard → API keys (already used by existing eval harnesses)"
  - service: anthropic
    why: "Headless claude -p judge transport for the consolidation sleep pass"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic console (or the founder Max-subscription claude -p transport already configured)"
must_haves:
  truths:
    - "On LongMemEval-KU with bm25CandidateK=5, the reconsolidation judge fires on real contradictions — the contradict-event counter reads > 0"
    - "With bm25CandidateK=0 (isolation A/B), the same run reproduces the pre-Phase-46 contradict count (the zero/near-zero baseline)"
    - "EVAL-02 clean-case belief-correction shows no regression vs the pre-Phase-46 baseline"
    - "No self-confirmation minting and no evidence-backed node deleted by decay during the validation runs"
  artifacts:
    - path: "scripts/eval/results/46-recon03-ku-bm25on.json"
      provides: "KU replay judge-engagement result with bm25CandidateK=5 (RECON-03 pass signal)"
    - path: "scripts/eval/results/46-recon03-ku-bm25off.json"
      provides: "KU replay judge-engagement result with bm25CandidateK=0 (isolation baseline)"
  key_links:
    - from: "scripts/eval/replay-ku-harness.cjs"
      to: "consolidation_event contradict_* counter"
      via: "queryJudgeEngagement → contradicts total"
      pattern: "contradict"
---

<objective>
Measure and PROVE the Phase 46 behavioral change on real data: run the LongMemEval-KU replay
harness with BM25 broadening ON (bm25CandidateK=5) and OFF (=0), record the judge-fire
(contradict-event) counter for both, and confirm the EVAL-02 clean-case belief-correction does
not regress.

Purpose: RECON-03 ("judge fires on real contradictions, > 0") and RECON-04 ("KU belief-correction
improves vs baseline AND EVAL-02 clean-case holds, invariants intact") are eval-measured — they
cannot be proven from source or unit tests alone. The KU harness's `contradicts` counter (count of
`contradict_*` rows in `consolidation_event`) is the phase's authoritative pass/fail signal. This
plan does NOT lock a CI regression gate — that is Phase 50's job; Phase 46 only needs to prove the
counter moves above zero and that clean cases do not regress.

Output: two result JSONs (BM25 on/off A/B), the recorded contradict/tombstone/duplicate-mint
counts, the EVAL-02 clean-case before/after, and a founder sign-off on the measured numbers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/46-reconsolidation-candidate-broadening/46-CONTEXT.md
@.planning/phases/46-reconsolidation-candidate-broadening/46-01-SUMMARY.md

<interfaces>
<!-- The KU replay harness already measures judge engagement and supports a generic config override. -->
<!-- Extracted from scripts/eval/replay-ku-harness.cjs — use these flags; do NOT modify the harness. -->

scripts/eval/replay-ku-harness.cjs:
  --dry-run                                 # ingest + scratch-DB build, ZERO API/LLM calls, exit 0
  --out <path>                              # result JSON target
  --config-override-key <key>               # generic top-level EngineConfig override (line 107, 261)
  --config-override-value <value>           # numeric values auto-coerced (line 110-111)
  queryJudgeEngagement(db) → { tombstones, contradicts, duplicateMints }
    # contradicts = count of consolidation_event rows whose event_type starts with 'contradict_'
    # this IS the RECON-03 judge-fire counter (independent of the in-code D-06 counter)
  # Config spread: { ...DEFAULT_CONFIG, [key]: value } — so bm25CandidateK flows through the
  # generic override path (line 261), no harness change needed for the A/B.

EVAL-02 clean-case no-regression harness: scripts/eval/correctness-harness.cjs
  (driven by scripts/eval/eval02-sweep.sh — clean-case belief-correction)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run KU replay A/B (BM25 on vs off) + EVAL-02 clean-case no-regression</name>
  <files>scripts/eval/results/46-recon03-ku-bm25on.json, scripts/eval/results/46-recon03-ku-bm25off.json</files>
  <read_first>
    - scripts/eval/replay-ku-harness.cjs lines 1-115 (header doc: run invocation, keys required, --config-override-key/value, --dry-run) and lines 290-410 (queryJudgeEngagement + consolidateCase — what `contradicts` measures)
    - scripts/eval/eval02-sweep.sh and scripts/eval/correctness-harness.cjs header (the clean-case belief-correction harness for RECON-04 no-regression)
    - .planning/phases/46-reconsolidation-candidate-broadening/46-01-SUMMARY.md (any node_fts fixture/index detail and the D-06 counter behavior recorded by Plan 46-01)
  </read_first>
  <action>
    Build first (`npm run build` — the harness consumes dist/). Smoke-test wiring with
    `node scripts/eval/replay-ku-harness.cjs --dry-run --out /tmp/recon03-dry.json` (zero API calls,
    must exit 0). Then run the real A/B:
    (a) BM25 ON (default): `node scripts/eval/replay-ku-harness.cjs --out scripts/eval/results/46-recon03-ku-bm25on.json`
        (bm25CandidateK defaults to 5 — no override needed).
    (b) BM25 OFF (isolation): same command plus `--config-override-key bm25CandidateK --config-override-value 0
        --out scripts/eval/results/46-recon03-ku-bm25off.json`.
    Record the `contradicts`, `tombstones`, and `duplicateMints` totals from each result JSON.
    Then run the EVAL-02 clean-case belief-correction harness (scripts/eval/eval02-sweep.sh /
    correctness-harness.cjs) before-vs-after, or compare the ON run's clean-case behavior against the
    pre-Phase-46 baseline recorded in STATE.md, to confirm no clean-case regression (RECON-04).
    Real runs require OPENAI_API_KEY (embed) and the Anthropic/claude -p judge transport; if a key is
    missing the harness errors — surface that as an auth gate, do not fabricate numbers. Do NOT modify
    the harness or any src/ file. Per CLAUDE.md, validate the magnitude on our own data — report the
    measured counts, never an assumed/borrowed figure.
  </action>
  <acceptance_criteria>
    - Behavior (RECON-03, PASS/FAIL signal): the bm25-ON result JSON reports `contradicts > 0` on LongMemEval-KU. If it reads 0, the BM25 query text or the D-04 gate wiring is wrong — re-open Plan 46-01 (do not pass the phase).
    - Behavior (isolation): the bm25-OFF result JSON reproduces the pre-Phase-46 contradict count (the near-zero baseline) — proving the delta is attributable to BM25 broadening, not noise.
    - Behavior (RECON-04 no-regression): EVAL-02 clean-case belief-correction is not below the pre-Phase-46 baseline (no clean-case regression).
    - Source: both result JSONs exist under scripts/eval/results/ with a meta block (embedder model, commit, date).
    - Invariant: no self-confirmation mint and no evidence-backed node deleted by decay observed in the run output (consistent with the unchanged guards).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/vtx/brain-memory && node scripts/eval/replay-ku-harness.cjs --dry-run --out /tmp/recon03-dry.json 2>&1 | tail -5 && ls -la scripts/eval/results/46-recon03-ku-bm25on.json scripts/eval/results/46-recon03-ku-bm25off.json 2>&1</automated>
  </verify>
  <done>KU A/B and EVAL-02 clean-case runs complete; both result JSONs written; contradict/tombstone/duplicate-mint counts and clean-case before/after recorded for the checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Founder sign-off on RECON-03/04 measured numbers</name>
  <read_first>
    - scripts/eval/results/46-recon03-ku-bm25on.json and scripts/eval/results/46-recon03-ku-bm25off.json (the A/B contradict/tombstone/duplicate-mint counts produced by Task 1)
    - .planning/STATE.md (pre-Phase-46 baseline — the prior ZERO judge-fire on KU)
  </read_first>
  <what-built>BM25 candidate broadening, measured on LongMemEval-KU (A/B) + EVAL-02 clean-case.</what-built>
  <how-to-verify>
    Review the recorded numbers from Task 1:
    1. BM25-ON contradict (judge-fire) count on KU — RECON-03 requires > 0; ideally a majority of the
       true-contradiction cases. Confirm it moved above the previous ZERO.
    2. BM25-OFF contradict count — confirm it reproduces the pre-Phase-46 near-zero baseline (the delta
       is attributable to BM25, per CLAUDE.md "validate magnitude on own data").
    3. EVAL-02 clean-case belief-correction — confirm no regression vs the pre-Phase-46 baseline.
    4. Confirm no self-confirmation minting and no evidence-backed-node decay-delete in the run output.
    If RECON-03 reads 0, the phase has NOT passed — send Plan 46-01 back (BM25 query/text or D-04 gate
    wiring is wrong, per CONTEXT.md §Specific Ideas).
  </how-to-verify>
  <resume-signal>Type "approved" (RECON-03 counter > 0, EVAL-02 no regression) or describe the failing metric.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval harness → scratch DB (os.tmpdir) | Validation runs against fresh scratch DBs; the live brain DB is never read or written |
| external magnitude claims → reported numbers | June-2026 preprint "judge-fires" figures must be validated on our own data, not asserted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02-01 | Information Disclosure / Integrity | KU replay scratch DB | accept | Harness uses a fresh scratch DB under os.tmpdir per case (T-14-DB); the live DB env var is never read. No production data mutated by validation. |
| T-46-02-02 | Repudiation (false metric) | reported RECON-03 figure | mitigate | Only numbers emitted by the harness result JSON are reported; the A/B (BM25 on vs off) isolates the BM25 delta; no borrowed/preprint magnitude is presented as our result (CLAUDE.md hard rule). |
| T-46-02-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies — reuses the existing eval harness. No package-install task. |
</threat_model>

<verification>
- `--dry-run` harness smoke passes (zero API calls, exit 0).
- Both result JSONs written under scripts/eval/results/ with meta.
- bm25-ON contradict count > 0 (RECON-03); bm25-OFF reproduces baseline; EVAL-02 clean-case no regression (RECON-04).
- Founder checkpoint approved.
</verification>

<success_criteria>
- RECON-03: judge-fire (contradict) counter > 0 on LongMemEval-KU with BM25 ON — proven, not asserted.
- RECON-04: KU belief-correction improves vs the bm25=0 baseline AND EVAL-02 clean-case holds; invariants intact.
- A/B isolation confirms the delta is attributable to BM25 broadening.
</success_criteria>

<output>
Create `.planning/phases/46-reconsolidation-candidate-broadening/46-02-SUMMARY.md` when done.
Record: the bm25-ON vs bm25-OFF contradict/tombstone/duplicate-mint counts, the EVAL-02 clean-case
before/after, the result-JSON paths, and whether RECON-03/04 passed. Flag for Phase 50 if the gate
should be locked into CI.
</output>
