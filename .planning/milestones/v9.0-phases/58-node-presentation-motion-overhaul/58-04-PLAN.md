---
phase: 58-node-presentation-motion-overhaul
plan: 04
type: execute
wave: 3
depends_on: [58-02]
files_modified:
  - scripts/gen-matcap.mjs
  - src/viz/vendor/matcaps/focus-matcap.png
  - src/viz/modules/graph.js
  - src/viz/modules/detail.js
  - src/viz/modules/constants.js
autonomous: true
requirements: [D-09, D-10, D-11, D-12, D-15]
user_setup: []

must_haves:
  truths:
    - "On selection, the focused node + 1-hop neighbors read as lit glass beads (matcap luminance shaping)"
    - "The matcap contributes luminance only; hue stays the node's own palette color (D-04-safe)"
    - "Overview-tier nodes never get matcap — the quiet flat+rim identity is preserved"
    - "The matcap mix fades in on focus and out on deselect (damped, not a snap)"
    - "The selected node uses the 32-seg focus geometry so its silhouette reads smooth up close"
  artifacts:
    - path: "scripts/gen-matcap.mjs"
      provides: "Offline regenerable grayscale matcap generator with a --check self-verify mode"
      contains: "--check"
    - path: "src/viz/vendor/matcaps/focus-matcap.png"
      provides: "Committed grayscale matcap asset (R≈G≈B)"
    - path: "src/viz/modules/graph.js"
      provides: "Matcap onBeforeCompile extension + _focusGeo 32-seg geometry + focus geometry swap"
      contains: "uMatcapMix"
  key_links:
    - from: "src/viz/modules/detail.js selectNode/clearSelection"
      to: "mat.userData.matcapMixUniform"
      via: "sets a per-node target mix (1 on focus, 0 on deselect), damped in registerTick"
      pattern: "matcapMix"
    - from: "graph.js makeNodeObject onBeforeCompile"
      to: "src/viz/vendor/matcaps/focus-matcap.png"
      via: "uMatcapTex sampler uniform"
      pattern: "uMatcapTex"
---

<objective>
Add focus-tier material richness (research area 1 #3-4): a script-generated grayscale matcap (D-09) sampled
inside the EXISTING shared `onBeforeCompile` node program, multiplied by each node's own palette color so hue
stays on-palette (D-10, D-04-safe), applied only to the selected node + 1-hop neighbors, damped 0→1 on focus
and back out on deselect (D-11); plus a shared 32-seg `SphereGeometry(1,32,32)` swapped onto the focused node
(D-12).

Purpose: The "lit glass bead" focus read the research flags as premium dessert — 100% on the LOCKED palette.
Output: gen-matcap.mjs + a committed grayscale PNG, the matcap shader extension, _focusGeo, the damped mix, constants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/58-node-presentation-motion-overhaul/58-CONTEXT.md
@.planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md
@.planning/phases/58-node-presentation-motion-overhaul/58-PATTERNS.md

<interfaces>
Existing node material + rim onBeforeCompile — graph.js:105-150 (single shared compiled program; identical
injected source across all nodes is load-bearing per the :117-124 header comment). Matcap MUST extend THIS
same function body (RESEARCH Pattern 2 lines 318-349) — never the haze InstancedMesh material (Anti-Pattern:
haze never gets matcap, D-10).
node annotations — graph.js:142-148: node.__mesh/__mat/__base/__baseOp/__baseR (mirror this for the mix uniform on mat.userData).
Shared geometry — graph.js:37: _sharedGeo = SphereGeometry(1,16,16); add sibling _focusGeo = SphereGeometry(1,32,32) (D-12).
Selection lifecycle — detail.js: selectNode (:520-530 focusCamera + focus dim), clearSelection (:~440), getNeighbors() for the 1-hop set, applyFocusDim keep-set logic (:150-165).
registerTick — stats.js:42-45; the matcap mix damp tick lives here reading mat.userData.matcapMixUniform.
Offline-script convention — scripts/gen-simd-kernel.cjs:1-24 (STANDALONE MANUAL DEV COMMAND, MUST NOT run at install/build, `--check` mode re-verifies committed asset). node v25.5.0 available.
copy-viz-assets.cjs:21 copies vendor/ recursively — putting the PNG under src/viz/vendor/matcaps/ needs no build change.
RESEARCH Pattern 2 excerpt (matcap UV via view-space normal + grayscale × node hue + mix by uMatcapMix): 58-RESEARCH.md lines 324-349.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: gen-matcap.mjs offline generator + --check grayscale self-verify + committed PNG</name>
  <read_first>
    - scripts/gen-simd-kernel.cjs (:1-24 header + the `--check` compare-committed-asset pattern)
    - scripts/copy-viz-assets.cjs (confirm vendor/ is copied — asset path choice)
    - .planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md (D-09 self-generated matcap rationale; Assets license table — nidorx never shipped)
  </read_first>
  <action>
    Create `scripts/gen-matcap.mjs` following the gen-simd-kernel.cjs convention: a STANDALONE MANUAL DEV
    COMMAND (JSDoc header stating it MUST NOT run at install/build time), `node scripts/gen-matcap.mjs` writes
    the asset, `node scripts/gen-matcap.mjs --check` re-verifies the committed asset without writing and exits
    non-zero on drift. Render a studio-lit grayscale sphere to a square PNG (canvas or headless three.js —
    Claude's Discretion on the lighting rig) and write it to `src/viz/vendor/matcaps/focus-matcap.png`. The
    output MUST be grayscale: assert R≈G≈B per sampled pixel (within a small tolerance) BEFORE writing, and in
    `--check` mode read the committed PNG and assert the same, so a hue can never be baked into the asset (D-15
    matcap-grayscale lock). Commit the generated PNG. Do NOT ship any nidorx matcap.
  </action>
  <verify>
    <automated>node scripts/gen-matcap.mjs --check</automated>
  </verify>
  <acceptance_criteria>
    - scripts/gen-matcap.mjs exists with a "MUST NOT run at install/build" header and a `--check` mode
    - `node scripts/gen-matcap.mjs --check` exits 0 against the committed PNG and would exit non-zero if the PNG were non-grayscale
    - src/viz/vendor/matcaps/focus-matcap.png exists and is grayscale (R≈G≈B within tolerance)
    - No nidorx or third-party matcap asset is added
  </acceptance_criteria>
  <done>A regenerable grayscale matcap PNG is committed with a passing --check grayscale self-verify</done>
</task>

<task type="auto">
  <name>Task 2: Matcap onBeforeCompile extension + 32-seg _focusGeo + focus geometry swap (graph.js)</name>
  <read_first>
    - src/viz/modules/graph.js (:105-150 node material + rim onBeforeCompile in full; :30-37 _sharedGeo; :142-148 node annotations)
    - .planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md (Pattern 2 lines 318-349; Anti-Pattern "never extend haze onBeforeCompile" line 382)
    - .planning/phases/58-node-presentation-motion-overhaul/58-PATTERNS.md (matcap-mix section lines 66-92; 32-seg section lines 96-108)
  </read_first>
  <action>
    Extend the EXISTING node-material `onBeforeCompile` function body (graph.js:125-136) per RESEARCH Pattern 2 —
    inside the SAME function (do not add a second onBeforeCompile; identical injected source is what keeps the
    one-shared-program property): add a `vViewNormal` varying computed as `normalize(normalMatrix * normal)` in
    the vertex shader, and in the fragment shader add uniforms `uMatcapTex` (sampler2D, the grayscale PNG loaded
    once via THREE.TextureLoader from src/viz/vendor/matcaps/focus-matcap.png) and `uMatcapMix` (float, per-node,
    default 0). Sample `matcapUv = vViewNormal.xy * 0.495 + 0.5`, multiply the sampled grayscale by the current
    `gl_FragColor.rgb` (node's own hue — D-10 luminance-only), and `mix()` it into gl_FragColor.rgb by uMatcapMix,
    appended AFTER the existing rim line (do not replace the rim). Store the mix uniform on
    `mat.userData.matcapMixUniform` (mirrors the __baseOp tracking) so detail.js can drive it. Add a module-scope
    `const _focusGeo = new THREE.SphereGeometry(1, 32, 32)` sibling to _sharedGeo, with the same comment
    discipline (explain: focused nodes are large/close enough that 16-seg faceting shows). Expose helpers so
    detail.js can swap `mesh.geometry = _focusGeo` on focus and restore `_sharedGeo` on deselect (or export
    _focusGeo/_sharedGeo refs). Do NOT touch buildHazeLayer's material (haze never gets matcap, D-10).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/viz/modules/graph.js','utf8'); const haze=(s.split('function buildHazeLayer')[1]||''); const ok=/uMatcapMix/.test(s)&&/uMatcapTex/.test(s)&&/vViewNormal/.test(s)&&/SphereGeometry\(\s*1\s*,\s*32\s*,\s*32\s*\)/.test(s)&&/matcapMixUniform/.test(s)&&!/uMatcapMix/.test(haze); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - The matcap uniforms/varying are added inside the existing single node onBeforeCompile (not a second one; not on the haze material)
    - The matcap is multiplied by the node's own gl_FragColor.rgb (luminance-only) then mix()'d by uMatcapMix, appended after the rim line
    - `mat.userData.matcapMixUniform` holds the uMatcapMix uniform for per-node control
    - `_focusGeo = SphereGeometry(1,32,32)` exists at module scope with a rationale comment; detail.js can swap to it and restore _sharedGeo
    - No uMatcapMix/uMatcapTex plumbing appears in buildHazeLayer
    - `npm test -- tests/viz-haze-activation.test.ts tests/viz-haze-selection.test.ts` still passes (no haze regression)
  </acceptance_criteria>
  <done>The shared node program gains a matcap capability gated by uMatcapMix; a 32-seg focus geometry exists and is swappable</done>
</task>

<task type="auto">
  <name>Task 3: Drive the matcap mix on select/deselect + damped fade in registerTick (detail.js)</name>
  <read_first>
    - src/viz/modules/detail.js (:150-165 applyFocusDim keep-set / getNeighbors 1-hop; :440 clearSelection; :520-530 selectNode focus path)
    - src/viz/modules/graph.js (the _focusGeo swap helpers + mat.userData.matcapMixUniform added in Task 2)
    - src/viz/modules/stats.js (:42-45 registerTick; :189-217 for the per-frame damp callback shape)
    - .planning/phases/58-node-presentation-motion-overhaul/58-CONTEXT.md (D-10/D-11 — selection+1-hop only, damped fade)
  </read_first>
  <action>
    In detail.js selectNode: after computing the 1-hop keep-set (reuse the same getNeighbors set applyFocusDim
    builds), set a target matcap mix of 1 on the focused node's `mat.userData.matcapMixUniform` and its 1-hop
    neighbors, and swap those meshes' geometry to `_focusGeo` (D-12, focused node at minimum; neighbors optional
    per Claude's Discretion). In clearSelection/deselect: set the target mix back to 0 for the previously-focused
    set and restore `_sharedGeo`. Register ONE `ctx.registerTick` callback that damps each active node's current
    uMatcapMix value toward its target using `THREE.MathUtils.damp(current, target, MATCAP_MIX_LAMBDA, dt)`
    (D-11 — one coherent damped motion event, alongside the focus flight). Track the small set of nodes with a
    non-settled mix so the tick is O(focused+neighbors), not O(allNodes). Add `MATCAP_MIX_LAMBDA` (feel constant,
    UNLOCKED per D-15) to constants.js under the Phase-58 section with a one-line JSDoc. Never apply matcap to
    overview/haze tiers (D-10) — the target is only ever set on committed-focus nodes.
  </action>
  <verify>
    <automated>node -e "const d=require('fs').readFileSync('src/viz/modules/detail.js','utf8'); const c=require('fs').readFileSync('src/viz/modules/constants.js','utf8'); const ok=/matcapMix/i.test(d)&&/registerTick/.test(d)&&/damp/.test(d)&&/_focusGeo/.test(d)&&/MATCAP_MIX_LAMBDA/.test(c); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - selectNode sets matcap-mix target 1 on the focused node + 1-hop neighbors and swaps the focused mesh to _focusGeo
    - clearSelection/deselect sets the target back to 0 and restores _sharedGeo
    - A single registerTick callback damps uMatcapMix toward its target via THREE.MathUtils.damp with MATCAP_MIX_LAMBDA
    - The damp tick only iterates the focused+neighbor set (not all nodes)
    - No matcap target is ever set on overview or haze nodes
    - MATCAP_MIX_LAMBDA is exported from constants.js under the Phase-58 section
    - `npm test` full suite passes (no regression in existing viz tests)
  </acceptance_criteria>
  <done>Matcap fades in on focus (node + 1-hop) and out on deselect, damped; overview/haze never affected</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed matcap asset → LOCKED palette | A hue accidentally baked into the matcap PNG would re-grade node color off-palette (D-04 risk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04 | Tampering (of LOCKED palette) | matcap PNG hue leak | mitigate | gen-matcap.mjs asserts R≈G≈B before write; `--check` re-verifies the committed asset (D-15 grayscale lock); shader multiplies grayscale by node hue so luminance-only |
| T-58-SC (ref) | Tampering | matcap source (self-generated, not vendored 3rd-party) | accept | Asset is generated in-repo from our own script; zero external license/supply-chain exposure (nidorx explicitly not shipped) |

No new network/input/DOM surface.
</threat_model>

<verification>
- `node scripts/gen-matcap.mjs --check` exits 0 (grayscale committed asset)
- Source scan: matcap uniforms in the single node onBeforeCompile only; _focusGeo 32-seg present; detail.js damps the mix
- `npm test` full suite green
- Visual (Stage-1, Plan 05): focused node + 1-hop read as lit beads, hue unchanged; overview unaffected
</verification>

<success_criteria>
- Focused node + 1-hop show damped matcap luminance shaping on their own hue; overview/haze never do
- 32-seg focus geometry on the selected node
- Grayscale matcap machine-locked via --check; no LOCKED palette drift; full suite green
</success_criteria>

<output>
Create `.planning/phases/58-node-presentation-motion-overhaul/58-04-SUMMARY.md` when done
</output>
