---
phase: 58-node-presentation-motion-overhaul
plan: 07
type: execute
wave: 6
depends_on: [58-06]
files_modified:
  - src/viz/modules/graph.js
  - src/viz/modules/detail.js
  - src/viz/modules/constants.js
autonomous: true
requirements: [HOVER-DAMP, D-07]
user_setup: []

must_haves:
  truths:
    - "Hover scale grows with a slight overshoot and settles; un-hover shrinks with no overshoot (asymmetric)"
    - "Hover scaling is frame-rate-independent across the 24fps idle / 60fps active tier swing"
    - "During focus, non-neighbor nodes dim slightly harder and the fog near-plane tightens"
    - "Focus dim + fog fully restore on deselect"
  artifacts:
    - path: "src/viz/modules/graph.js"
      provides: "Damped asymmetric hover (target scale + registerTick damp) + focus fog near-plane save/restore"
      contains: "HOVER_LAMBDA"
    - path: "src/viz/modules/constants.js"
      provides: "HOVER_LAMBDA, HOVER_OVERSHOOT, FOCUS_DIM_OPACITY (moved), FOCUS_FOG_NEAR"
      contains: "FOCUS_FOG_NEAR"
  key_links:
    - from: "graph.js onNodeHover"
      to: "registerTick hover-damp callback"
      via: "hover-enter/leave set node.__hoverTarget; the tick damps mesh.scale toward it"
      pattern: "__hoverTarget"
    - from: "detail.js applyFocusDim/clearFocusDim"
      to: "Graph.scene().fog.near"
      via: "tighten to FOCUS_FOG_NEAR on focus, restore BRAIN_SCALE*1.8 on deselect"
      pattern: "fog"
---

<objective>
Replace the `HOVER_SCALE` snap with frame-rate-independent damped hover — asymmetric feel: grow with a slight
overshoot (~1.05×, one oscillation max), shrink with none (research area 2 #1). And deepen the focus read
(D-07): a slightly stronger non-neighbor dim plus a tightened fog near-plane while a node is focused, both
restored on deselect. All damping lives in `registerTick`; all values are named constants.

Purpose: Crafted asymmetric interaction feel + a cheap legible focus-pull (the research's chosen alternative
to rejected DOF).
Output: Damped hover in graph.js, focus fog/dim tuning across graph.js + detail.js, and the feel constants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/58-node-presentation-motion-overhaul/58-CONTEXT.md
@.planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md
@.planning/phases/58-node-presentation-motion-overhaul/58-PATTERNS.md

<interfaces>
Hover handler — graph.js onNodeHover :569-606: currently `mesh.scale.setScalar(baseR * HOVER_SCALE)` on enter
and `setScalar(baseR)` on leave (the snaps to replace); node.__mesh/__baseR annotations (graph.js:142-148);
HOVER_SCALE = 1.8 (constants.js:216).
registerTick — stats.js:42-45; the hover damp tick lives here reading each hovering node's target.
THREE.MathUtils.damp(x, y, lambda, dt) — vendored three.core.js:372.
Focus dim — detail.js: FOCUS_DIM_OPACITY = 0.05 (detail.js:26, MOVE it to constants.js per PATTERNS Shared
Patterns); applyFocusDim :150-165 (keep-set + haze dim); clearFocusDim :167-184 (restore via __baseOp / _baseOpacity).
Fog — graph.js:690: Graph.scene().fog = new THREE.Fog(BG_COLOR, BRAIN_SCALE*1.8, BRAIN_SCALE*4.2). BRAIN_SCALE=460.
D-07: small named-constant adjustments to the dim strength and fog near-plane while focused; tuned at Stage 2 (UNLOCKED).
Coexistence: hover damp must not fight the matcap-mix damp (Plan 04) or the camera damp (Plan 06) — each owns its own registerTick callback and its own state; no shared mutable field.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Damped asymmetric hover (target scale + registerTick damp)</name>
  <read_first>
    - src/viz/modules/graph.js (:569-606 onNodeHover in full; :142-148 node annotations; :37 _sharedGeo scale idiom)
    - src/viz/modules/stats.js (:42-45 registerTick; :189-217 updateIdleDrift for the per-frame damp shape and the dt clamp)
    - .planning/phases/58-node-presentation-motion-overhaul/58-PATTERNS.md (damped-hover section lines 112-139 — the target-scale + overshoot state machine)
    - .planning/phases/58-node-presentation-motion-overhaul/58-CONTEXT.md (Specific Ideas — asymmetry: overshoot in ~1.05×, one oscillation, none out)
  </read_first>
  <action>
    In graph.js onNodeHover, replace the two `mesh.scale.setScalar(...)` snap-writes with target-setting only:
    on hover-enter set `node.__hoverTarget = HOVER_SCALE`; on hover-leave (and on switching to a new node) set
    the previously-hovered node's `__hoverTarget = 1`. Keep the tooltip logic unchanged. Register ONE
    `ctx.registerTick(now)` callback that, for the current hovered node and any node still settling toward 1,
    damps `mesh.scale` toward `node.__baseR * __hoverTarget` using `THREE.MathUtils.damp(current, target,
    HOVER_LAMBDA, dt)` with a clamped dt (mirror updateIdleDrift's `Math.min(0.1, ...)`). Implement the
    asymmetry: on grow (target > current) allow a slight overshoot to ~`HOVER_SCALE * HOVER_OVERSHOOT` (one
    oscillation max) before settling; on shrink (target < current) use pure damp, no overshoot. Track the small
    set of non-settled nodes so the tick is O(hovering), not O(allNodes), and drop a node from the set once it
    is within epsilon of its target. Add HOVER_LAMBDA and HOVER_OVERSHOOT (~1.05) to constants.js under the
    Phase-58 section with JSDoc (UNLOCKED feel constants). Keep HOVER_SCALE (1.8) as the grow target.
  </action>
  <verify>
    <automated>node -e "const g=require('fs').readFileSync('src/viz/modules/graph.js','utf8'); const c=require('fs').readFileSync('src/viz/modules/constants.js','utf8'); const hover=g.slice(g.indexOf('onNodeHover'), g.indexOf('onNodeHover')+1200); const ok=/__hoverTarget/.test(g)&&/registerTick/.test(g)&&/damp/.test(g)&&!/setScalar\([^)]*HOVER_SCALE\)/.test(hover)&&/HOVER_LAMBDA/.test(c)&&/HOVER_OVERSHOOT/.test(c); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - onNodeHover sets node.__hoverTarget on enter/leave instead of calling setScalar with HOVER_SCALE directly
    - A registerTick callback damps mesh.scale toward __baseR * __hoverTarget via THREE.MathUtils.damp(HOVER_LAMBDA) with a clamped dt
    - Grow path overshoots to ~HOVER_SCALE*HOVER_OVERSHOOT (one oscillation) then settles; shrink path has no overshoot
    - The damp tick iterates only the hovering/settling set (not all nodes)
    - HOVER_LAMBDA and HOVER_OVERSHOOT exported from constants.js with JSDoc; HOVER_SCALE retained as the grow target
    - `npm test` full suite passes
  </acceptance_criteria>
  <done>Hover grows with a slight overshoot and shrinks clean, frame-rate-independent across tiers</done>
</task>

<task type="auto">
  <name>Task 2: Focus depth deepening — stronger non-neighbor dim + tightened fog near-plane (D-07)</name>
  <read_first>
    - src/viz/modules/detail.js (:26 FOCUS_DIM_OPACITY; :150-165 applyFocusDim; :167-184 clearFocusDim restore)
    - src/viz/modules/graph.js (:684-690 the fog setup; BRAIN_SCALE usage)
    - src/viz/modules/constants.js (the Phase-58 section; BRAIN_SCALE=460 export)
    - .planning/phases/58-node-presentation-motion-overhaul/58-PATTERNS.md (fog near-plane section lines 280-289 — the save+restore-on-deselect idiom)
  </read_first>
  <action>
    Move `FOCUS_DIM_OPACITY` from detail.js:26 into constants.js under the Phase-58 section (import it back into
    detail.js) so it joins the named-constant discipline, and tune it for a slightly stronger non-neighbor dim
    during focus (D-07 — value is Claude's provisional, ratcheted at Stage 2; UNLOCKED). Add a new constant
    `FOCUS_FOG_NEAR` (a smaller near-plane than the resting `BRAIN_SCALE * 1.8`, e.g. `BRAIN_SCALE * 1.4` —
    provisional). In detail.js applyFocusDim: after dimming, tighten the scene fog near-plane by setting
    `ctx.Graph.scene().fog.near = FOCUS_FOG_NEAR` (save the prior value first, mirroring the haze `_hazeDimmed`
    save/restore idiom). In clearFocusDim: restore `fog.near` to its saved resting value (`BRAIN_SCALE * 1.8`)
    alongside the existing opacity restores. The applyFocusDim/clearFocusDim keep-set + haze-dim bookkeeping is
    otherwise unchanged. Expose the fog object on ctx if detail.js cannot already reach it (graph.js sets
    Graph.scene().fog — detail.js can read ctx.Graph.scene().fog).
  </action>
  <verify>
    <automated>node -e "const d=require('fs').readFileSync('src/viz/modules/detail.js','utf8'); const c=require('fs').readFileSync('src/viz/modules/constants.js','utf8'); const ok=/FOCUS_FOG_NEAR/.test(c)&&/FOCUS_DIM_OPACITY/.test(c)&&/fog/.test(d)&&/FOCUS_FOG_NEAR/.test(d)&&!/^const FOCUS_DIM_OPACITY/m.test(d); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - FOCUS_DIM_OPACITY is exported from constants.js (no longer a local const in detail.js) and imported into detail.js
    - FOCUS_FOG_NEAR exported from constants.js with JSDoc
    - applyFocusDim tightens fog.near to FOCUS_FOG_NEAR after saving the prior value; clearFocusDim restores it to BRAIN_SCALE*1.8
    - The keep-set / haze-dim bookkeeping in applyFocusDim/clearFocusDim is otherwise unchanged (git diff limited to the dim value + fog save/restore lines)
    - `npm test` full suite passes; no LOCKED constant drift
  </acceptance_criteria>
  <done>Focus dims non-neighbors harder and tightens fog; both fully restore on deselect</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — pure client-side interaction/scene-parameter math. No input, network, or DOM surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-06 | Tampering (of LOCKED palette hierarchy) | focus dim/fog constants | accept | D-07 constants are UNLOCKED feel values (not palette/bloom/tone); they modulate opacity/fog only, never hue — no D-02/D-04 surface touched |
</threat_model>

<verification>
- Source scan: __hoverTarget + registerTick damp (no HOVER_SCALE snap); FOCUS_FOG_NEAR + moved FOCUS_DIM_OPACITY; fog save/restore
- `npm test` full suite green; no LOCKED constant drift
- Visual (Stage-2, Plan 08): asymmetric hover (overshoot in, clean out); focus deepening reads; full restore on deselect
</verification>

<success_criteria>
- Frame-rate-independent asymmetric damped hover replaces the snap
- Focus deepening (dim + fog) applies on focus and restores on deselect, all via named constants
- Full suite green; no LOCKED constant drift
</success_criteria>

<output>
Create `.planning/phases/58-node-presentation-motion-overhaul/58-07-SUMMARY.md` when done
</output>
