---
phase: 58-node-presentation-motion-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/viz/modules/graph.js
  - src/viz/modules/constants.js
  - tests/viz-haze-activation.test.ts
  - tests/viz-haze-selection.test.ts
autonomous: true
requirements: [HAZE-IMPOSTOR, D-16]
user_setup: []

must_haves:
  truths:
    - "The haze tier renders as soft radial-falloff atmosphere, not dim opaque spheres"
    - "Haze instances still occlude and are occluded correctly (no hard rectangular silhouettes)"
    - "Dense overlapping haze never crosses the LOCKED 0.72 bloom threshold (no fake flare)"
    - "Clicking a haze node still selects it (raycast + proximity fallback both work)"
  artifacts:
    - path: "src/viz/modules/graph.js"
      provides: "buildHazeLayer using a custom billboard ShaderMaterial + PlaneGeometry + invisible sphere raycast proxy"
      contains: "ShaderMaterial"
    - path: "src/viz/modules/constants.js"
      provides: "HAZE_FALLOFF / ALPHA_TEST_THRESHOLD named constants"
  key_links:
    - from: "src/viz/modules/graph.js buildHazeLayer"
      to: "ctx.hazeMesh / ctx.hazeInstanceMap / ctx.hazeNodeIdMap"
      via: "unchanged instancing bookkeeping (positions/colors/maps preserved)"
      pattern: "ctx.hazeInstanceMap"
    - from: "graph.js haze raycast (line ~965)"
      to: "invisible sphere proxy InstancedMesh"
      via: "hazeRay.intersectObject against the orientation-agnostic proxy"
      pattern: "intersectObject"
---

<objective>
Replace the haze `InstancedMesh`'s shared sphere geometry + Fresnel-rim MeshBasicMaterial with
instanced camera-facing quads carrying a procedural radial-falloff `ShaderMaterial` (RESEARCH
Pattern 3), using `alphaTest` discard + normal blending (NOT additive) to protect the LOCKED
0.72 bloom threshold. Keep all per-instance instancing bookkeeping unchanged. Add an invisible
sphere-proxy InstancedMesh so haze clicks stay accurate after the billboard swap.

Purpose: The single biggest "default three.js orbs → crafted atmosphere" upgrade (research area 1 #1).
Output: A soft-sprite haze layer; updated haze test mocks; two new named constants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md
@.planning/phases/58-node-presentation-motion-overhaul/58-PATTERNS.md

<interfaces>
buildHazeLayer to change — graph.js:392-535. Load-bearing bookkeeping to PRESERVE verbatim:
  - per-instance Halton loop (graph.js:464-518): dummy.scale.setScalar(radius), setMatrixAt, setColorAt, hazeInstanceMap/hazeNodeIdMap population
  - hazeMat._baseOpacity = hazeMat.opacity (line ~525) — detail.js clearFocusDim reads it
  - ctx.hazeMesh / ctx.hazeMat / ctx.hazeInstanceMap / ctx.hazeNodeIdMap registration (lines ~528-534)

Shared geometry — graph.js:37: const _sharedGeo = new THREE.SphereGeometry(1, 16, 16)  (reuse for the proxy)
Raycast — graph.js:965: hazeRay.intersectObject(ctx.hazeMesh, false); proximity fallback graph.js:976-996 is geometry-agnostic (no change).
LOCKED bloom — effects.js: UnrealBloomPass(0.6, 0.4, 0.72); RESEARCH Pitfall 2 (depthWrite:true + alphaTest) and the additive-blending anti-pattern.
RESEARCH Pattern 3 code excerpt: 58-RESEARCH.md lines 358-376 (billboard vertex basis + radial smoothstep fragment + alphaTest discard).

Existing THREE mocks: tests/viz-haze-activation.test.ts:66-94 and viz-haze-selection.test.ts:53-101
  currently export a SphereGeometry/ShaderMaterial mock; a PlaneGeometry mock must be added.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap haze geometry+material to a billboard radial-falloff ShaderMaterial + raycast proxy</name>
  <read_first>
    - src/viz/modules/graph.js (buildHazeLayer :392-535 in full, the _sharedGeo declaration :30-37, the node material rim injection :105-136 for the .replace() convention, the haze raycast :960-1000)
    - .planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md (Pattern 3 lines 352-376; Pitfall 2; Anti-Patterns lines 378-383; Open Question 2 raycast-proxy recommendation lines 478-481)
    - src/viz/modules/effects.js (lines 139-165 — the bloom threshold margin analysis the alphaTest choice protects)
  </read_first>
  <action>
    In buildHazeLayer, replace `hazeMat` (the MeshBasicMaterial + Fresnel-rim onBeforeCompile) with a new
    `THREE.ShaderMaterial` implementing RESEARCH Pattern 3: a vertex shader that billboards each instance by
    rebuilding a camera-facing basis from viewMatrix columns (keep instance position+uniform scale from
    instanceMatrix, discard its rotation), and a fragment shader doing `smoothstep(1.0, 0.0, dist)` radial
    falloff scaled by the haze opacity uniform, with an `if (alpha < ALPHA_TEST_THRESHOLD) discard;` before
    output. Set `transparent: true`, `depthWrite: true`, and NORMAL blending — do NOT use `AdditiveBlending`
    (RESEARCH Anti-Pattern: additive stacking in dense clusters can cross the LOCKED 0.72 bloom threshold and
    fake a flare, violating D-04). Pass per-instance color through the existing instanceColor attribute
    (declare `attribute` usage the same way the current InstancedMesh does). Swap `_sharedGeo` for a new
    module-scope `const _hazeQuadGeo = new THREE.PlaneGeometry(2, 2)` (sibling to _sharedGeo, same "one
    geometry, N instances" discipline). PRESERVE verbatim: the entire Halton per-instance loop, setMatrixAt,
    setColorAt, hazeInstanceMap/hazeNodeIdMap population, `hazeMat._baseOpacity = hazeMat.opacity`, and the
    ctx.hazeMesh/hazeMat/maps registration. Add an invisible raycast proxy per Open Question 2: build a second
    `visible: false` InstancedMesh reusing `_sharedGeo` (spheres — orientation-agnostic) sharing the SAME
    instance matrices, register it as `ctx.hazeRayProxy`, add it to the scene, and change the raycast at
    graph.js:965 to `hazeRay.intersectObject(ctx.hazeRayProxy, false)`. The geometry-agnostic proximity
    fallback (:976-996) stays unchanged.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/viz/modules/graph.js','utf8'); const ok=/ShaderMaterial/.test(s)&&/PlaneGeometry/.test(s)&&/discard/.test(s)&&!/AdditiveBlending/.test(s.split('buildHazeLayer')[1]||'')&&/hazeRayProxy/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - buildHazeLayer constructs a THREE.ShaderMaterial (billboard vertex + radial-falloff fragment with a discard), not a MeshBasicMaterial
    - The haze material uses normal blending; no `AdditiveBlending` appears anywhere in buildHazeLayer
    - A module-scope `_hazeQuadGeo` PlaneGeometry backs the visible haze InstancedMesh
    - `hazeMat._baseOpacity`, hazeInstanceMap, hazeNodeIdMap, and the ctx registrations are all still set (grep confirms)
    - `ctx.hazeRayProxy` is a `visible:false` sphere InstancedMesh sharing instance transforms; the primary raycast intersects it, not the quad mesh
    - `npm test -- tests/viz-haze-activation.test.ts tests/viz-haze-selection.test.ts` — deferred to Task 2 after mocks update
  </acceptance_criteria>
  <done>Haze renders via billboard quads with radial falloff + alphaTest; clicks select via the sphere proxy + proximity fallback; bookkeeping preserved</done>
</task>

<task type="auto">
  <name>Task 2: Add HAZE falloff/alphaTest constants and update haze test mocks</name>
  <read_first>
    - src/viz/modules/constants.js (the Sizing section :205-230 for the named-constant JSDoc discipline; the existing HAZE_* constants)
    - tests/viz-haze-activation.test.ts (the THREE mock block :60-94 — SphereGeometry/ShaderMaterial stubs)
    - tests/viz-haze-selection.test.ts (the THREE mock block :50-101)
  </read_first>
  <action>
    Add two named constants to constants.js under a new `// === Phase 58 — haze impostor ===` section header
    (mirroring the existing `// === Adaptive density (Phase 19 Item 2) ===` precedent): `ALPHA_TEST_THRESHOLD`
    (starting value ~0.02, one-line JSDoc: below this alpha the impostor fragment is discarded to keep correct
    depth occlusion without a sort) and `HAZE_FALLOFF_SOFTNESS` if the shader parameterizes the smoothstep edge
    (else omit and note the curve is inline — Claude's Discretion per CONTEXT). Import whichever are used into
    graph.js. In BOTH haze test files, add a `class PlaneGeometry { constructor(_w?: number, _h?: number) {} dispose?() {} }`
    stub to the mocked THREE module and include `PlaneGeometry` in the returned mock object, matching the existing
    SphereGeometry stub shape. If either test asserts sphere-specific haze behavior that the quad swap breaks,
    re-scope the assertion to the invariant that survives (instance count / color / map population), not the
    geometry type. Run both files to green.
  </action>
  <verify>
    <automated>npm test -- tests/viz-haze-activation.test.ts tests/viz-haze-selection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ALPHA_TEST_THRESHOLD` is exported from constants.js with a one-line JSDoc under a Phase-58 section header
    - Both haze test files define and export a PlaneGeometry mock stub
    - `npm test -- tests/viz-haze-activation.test.ts tests/viz-haze-selection.test.ts` passes
    - No LOCKED constant (bloom/tone/motion-profile/palette) in constants.js was modified (git diff shows only additions under the new Phase-58 section)
  </acceptance_criteria>
  <done>New haze constants exist; both haze test suites pass against the quad-swapped buildHazeLayer</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPU fragment output → bloom pass | Overlapping translucent haze could sum luminance and cross the LOCKED bloom threshold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-02 | Tampering (of the LOCKED luminance hierarchy) | haze impostor blending | mitigate | Use alphaTest discard + normal blending, never additive — dense overlap cannot stack past 0.72 (RESEARCH Pitfall 2 / Anti-Pattern). Verified visually at Stage-1 checkpoint (no fake flare in dense regions) |

No new input, network, or DOM surface — pure GPU rendering change.
</threat_model>

<verification>
- Source scan: ShaderMaterial + PlaneGeometry + discard present; no AdditiveBlending in buildHazeLayer; hazeRayProxy present
- `npm test -- tests/viz-haze-activation.test.ts tests/viz-haze-selection.test.ts` green
- constants.js git diff is additive-only (no LOCKED value touched)
- Visual (Stage-1 checkpoint, Plan 05): haze reads as atmosphere; no rectangular occlusion; no fake flare in dense clusters
</verification>

<success_criteria>
- Haze tier renders as soft radial-falloff billboards with correct occlusion and no bloom-threshold stacking
- Haze selection (raycast proxy + proximity fallback) still works
- Both haze test suites pass; no LOCKED constant drift
</success_criteria>

<output>
Create `.planning/phases/58-node-presentation-motion-overhaul/58-02-SUMMARY.md` when done
</output>
