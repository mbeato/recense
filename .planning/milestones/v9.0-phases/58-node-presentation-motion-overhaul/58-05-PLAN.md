---
phase: 58-node-presentation-motion-overhaul
plan: 05
type: execute
wave: 4
depends_on: [58-02, 58-03, 58-04]
files_modified: []
autonomous: false
requirements: [D-01, D-13, D-14, D-16]
user_setup: []

must_haves:
  truths:
    - "Founder has reviewed haze impostors, SDF labels, and focus-tier matcap on the live install"
    - "The troika keep/kill verdict is recorded before any motion work builds on labels"
    - "DevTools Network shows zero cdn.jsdelivr.net requests with labels visible (T-58-01 confirmed)"
    - "Screenshots capture each technique as the durable Stage-1 approval record"
  artifacts: []
  key_links: []
---

<objective>
Stage-1 founder checkpoint — LOOK (D-13). The static presentation stack (haze impostors + SDF labels +
focus-tier matcap) is reviewed on the live install before any motion work builds on it. The troika keep/kill
verdict lands here (D-01: the checkpoint judges the RESULT, not the adoption). Evidence = per-technique
screenshots (D-14). This gate blocks Wave 5 (motion).

Purpose: De-risk the biggest visual bets and the one new dependency before the motion phase depends on labels.
Output: A recorded founder verdict, a troika keep/kill decision, screenshots, and a confirmed no-CDN network trace.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-node-presentation-motion-overhaul/58-CONTEXT.md
@.planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Stage-1 LOOK review — haze + labels + matcap, troika keep/kill, no-CDN check</name>
  <read_first>
    - .planning/phases/58-node-presentation-motion-overhaul/58-CONTEXT.md (D-13 two-stage checkpoint; D-14 evidence; the Specific Ideas "haze as atmosphere / labels as place-names" bar)
    - .planning/phases/58-node-presentation-motion-overhaul/58-RESEARCH.md (Pitfall 1 warning signs — the DevTools Network check; Pitfall 3 — hull depthWrite:false is by design, labels showing through the hull is correct)
  </read_first>
  <what-built>
    Plans 02-04 landed on the live install (autonomous, already merged): soft-sprite haze impostors, top-N SDF
    schema labels (appear-on-approach, slate, depth-occluded), and focus-tier matcap mix (selection + 1-hop,
    32-seg focus geometry). No motion changes yet — hover/camera still use the pre-phase behavior.
  </what-built>
  <how-to-verify>
    1. Launch the live viz (both viewports: full window and the ≤500px compact tray popover).
    2. HAZE: confirm the haze tier reads as soft atmosphere, not dim geometry; check dense/overlapping haze
       regions show NO fake flare (bloom must not stack) and NO hard rectangular occlusion edges. Screenshot.
    3. LABELS: approach a schema cluster — confirm crisp SDF names fade in only when close, stay slate (never
       amber), and are occluded by opaque nodes; confirm the OVERVIEW stays label-free in both viewports.
       Labels showing THROUGH the translucent hull is correct (Pitfall 3), not a bug. Screenshot.
    4. TROIKA KEEP/KILL (D-01): judge the rendered label look. Verdict: keep, tune N/threshold, or kill.
    5. NETWORK (T-58-01): open DevTools Network, render labels including any unusual glyph, confirm ZERO
       requests to cdn.jsdelivr.net. Record the result.
    6. MATCAP: click a node — confirm the focused node + 1-hop read as lit glass beads with their OWN hue
       (no gray wash, no off-palette tint), the selected node's silhouette is smooth (32-seg), and overview
       nodes are unaffected. Screenshot.
    7. Record the D-16 note: does overview-idle still sit near the pre-phase baseline fps captured in Plan 01?
  </how-to-verify>
  <verify>
    <automated>MISSING — Stage-1 is a visual founder judgment (feel/look); D-15 excludes these from machine locks. Screenshots + the recorded verdict are the verification record (D-14).</automated>
  </verify>
  <acceptance_criteria>
    - Founder verdict recorded for each technique (haze / labels / matcap): approved, tune, or revise
    - Troika keep/kill decision recorded (D-01); if "tune", the LABEL_TOP_N / distance-threshold values to ratchet are noted
    - DevTools Network result recorded: zero cdn.jsdelivr.net requests with labels visible (T-58-01 confirmed)
    - Per-technique screenshots saved as the Stage-1 evidence record (D-14)
    - Overview-idle fps compared against the Plan-01 baseline (D-16); any regression flagged for fix before close
  </acceptance_criteria>
  <done>Founder signs off (or lists revisions) on all three techniques + the troika keep/kill verdict; no-CDN confirmed; screenshots saved</done>
  <resume-signal>Type "approved" (with any tune values), or describe the revisions needed per technique</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viz runtime → third-party CDN | This checkpoint is where T-58-01 (troika CDN phone-home) is empirically confirmed closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Information Disclosure | troika CDN fallback | mitigate (verify) | DevTools Network check during this checkpoint confirms Plan 01's patch — zero cdn.jsdelivr.net requests with labels visible |
</threat_model>

<verification>
- Founder verdict + troika keep/kill recorded; per-technique screenshots saved
- DevTools Network: zero cdn.jsdelivr.net requests (T-58-01 closed)
- Overview-idle fps within tolerance of the Plan-01 baseline
</verification>

<success_criteria>
- All three presentation techniques signed off (or revisions listed)
- Troika keep/kill verdict landed before motion work begins
- No-CDN network trace confirmed; Stage-1 screenshot evidence captured
</success_criteria>

<output>
Create `.planning/phases/58-node-presentation-motion-overhaul/58-05-SUMMARY.md` when done
</output>
