---
phase: 55-honest-1-hop-pathways-on-ambient-recall
plan: 02
type: execute
wave: 2
depends_on:
  - 55-01
files_modified: []
autonomous: false
requirements:
  - "SC3 honesty invariant (no fabricated edges) preserved and machine-verified"
user_setup: []

must_haves:
  truths:
    - "A per-prompt (ambient) recall lights real 1-hop pathway pulses in the recense viz, not just seed nodes (SC1)"
    - "Pathway density reads intentional at AMBIENT_HOP_TOPN=6, or N is tuned at the founder's direction"
    - "No fabricated-looking pulses: pathways trace real edges (honesty invariant preserved, confirmed visually)"
  artifacts: []
  key_links:
    - from: "retrieveRanked emit (Plan 55-01)"
      to: "recense viz trace animation"
      via: "activation_trace row -> SSE trace event -> hud.js -> trace.js applyTrace"
      pattern: "hops"
---

<objective>
Founder visual confirmation of SC1: with Plan 55-01 shipped, ordinary per-prompt (ambient) recalls
now light real 1-hop spreading-activation pathways in `recense viz` — the Phase 54 checkpoint
symptom ("live flashes light seed nodes but rarely show pathways") is resolved. Also the tuning
gate for the AMBIENT_HOP_TOPN density lever (D-02: default 6, err denser).

Purpose: SC1 is inherently visual — no automated test can confirm the rendered "feels alive"
pathway density. This plan gates that human judgment and captures any N adjustment.
Output: founder sign-off (or an N change to apply back in engine.ts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/55-honest-1-hop-pathways-on-ambient-recall/55-CONTEXT.md
@.planning/phases/55-honest-1-hop-pathways-on-ambient-recall/55-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Founder visual verification of ambient 1-hop pathways</name>
  <what-built>
    Plan 55-01 changed both `retrieveRanked` emit sites so every per-prompt ambient recall now emits
    each seed's real top-N (default 6) live `relation` 1-hop out-edges as `hops` (score:null,
    rank-only), with seeds carrying their real cosine/RRF magnitude. The viz consumes these unchanged
    (Phase 52). SC2/SC3 honesty is already machine-locked by the Plan 55-01 guards.
  </what-built>
  <how-to-verify>
    1. Start the viz server: `recense viz` (or the project's documented viz start command) and open the viz in a browser.
    2. Trigger several ordinary recalls — send a few normal prompts / memory_ask queries so the ambient `retrieveRanked` path fires (NOT the rare curated path).
    3. Confirm SC1: on ordinary recalls you now see pathway-hop pulses spreading from lit seed nodes (cyan hops off amber seeds), not just isolated seed flashes.
    4. Judge density: at N=6 do pathways read alive-and-intentional, too sparse, or too cluttered? Some object-heavy seeds lighting sparsely is expected/honest (RESEARCH Founder Checkpoint item 1: asymmetric out-edge sparsity, not a bug).
    5. Optional density adjustment: if too sparse/cluttered, decide a new AMBIENT_HOP_TOPN value; the executor updates the single named constant in `src/retrieval/engine.ts` (the top-N cap guard in Plan 55-01 tracks the constant, so re-run `npx vitest run tests/activation-trace-wiring.test.ts` after any change).
  </how-to-verify>
  <resume-signal>Type "approved" (pathways light on ordinary recalls, density acceptable), or specify a new AMBIENT_HOP_TOPN value / describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
No new attack surface. This plan is a human visual verification of Plan 55-01's already-shipped,
already-machine-locked change; any optional edit is a single-constant value change to the existing
named tunable (bounded by the D-01/D-02 cap and the RING_CAP ring buffer). See Plan 55-01's threat
model for the STRIDE register.
</threat_model>

<verification>
- Founder confirms ordinary (ambient) recalls light real 1-hop pathways in the viz (SC1).
- If N changed: `npx vitest run tests/activation-trace-wiring.test.ts` still green.
</verification>

<success_criteria>
- SC1 confirmed: per-prompt recall lights real 1-hop pathways in the viz.
- AMBIENT_HOP_TOPN density accepted at 6 or tuned at the founder's direction.
</success_criteria>

<output>
Create `.planning/phases/55-honest-1-hop-pathways-on-ambient-recall/55-02-SUMMARY.md` when done.
</output>
