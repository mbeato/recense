---
phase: 47-hybrid-retrieval-recall
plan: 03
type: execute
wave: 3
depends_on: ["47-01", "47-02"]
files_modified:
  - src/responder/index.ts
  - src/lib/config.ts
autonomous: true
requirements: [RETR-01, RETR-04]
user_setup: []
must_haves:
  truths:
    - "The product responder facts-first path passes the user-derived query through retrieveRanked, so the live QA path uses hybrid BM25+dense fusion (D-07)"
    - "The shipped config default bm25FusionWeight is w* (the held-out-selected value from Plan 02), not 0"
    - "Setting bm25FusionWeight=0 instantly reverts the responder to pure cosine (the dark fallback still works)"
    - "No online LLM call is added to the retrieval path; the only pre-retrieval LLM call remains the existing LEVER-3 Q→declarative rewrite"
    - "queryText fed to retrieval is user/question-derived (boundedQuery or its LEVER-3 rewrite), never an LLM answer/hypothesis (T-04-03-I)"
  artifacts:
    - path: "src/responder/index.ts"
      provides: "retrieveRanked call passes queryForEmbed as the 4th arg (enables LEVER 1 hybrid on the product path)"
      contains: "queryForEmbed"
    - path: "src/lib/config.ts"
      provides: "DEFAULT_CONFIG.bm25FusionWeight set to w* (from Plan 02)"
      contains: "bm25FusionWeight"
  key_links:
    - from: "src/responder/index.ts respond()"
      to: "src/retrieval/engine.ts retrieveRanked"
      via: "retrieveRanked(cueVec, rankedRetrievalK, rankedRetrievalFloor, queryForEmbed)"
      pattern: "retrieveRanked\\([^)]*queryForEmbed"
---

<objective>
Ship hybrid fusion on the live product QA path (D-07) and set the shipped default to the held-out-selected `w*` from Plan 02. Thread the already-in-scope, user-derived `queryForEmbed` into `responder.respond → retrieveRanked` as the 4th argument so the facts-first path uses LEVER 1 hybrid fusion immediately, and flip `DEFAULT_CONFIG.bm25FusionWeight` from the dark 0 to `w*`.

This is the "not shipping dark" step — the phase's point is lifting LIVE recall. The held-out per-category gate (Plan 02, D-04) de-risked it; the `w=0` knob remains the instant fallback.

GATE: this plan runs only after Plan 02's checkpoint approved a `w*`. If Plan 02 concluded `w*=0` (no positive weight passed the held-out gate), set the default to 0 and STILL thread queryForEmbed (mechanism shipped, weight neutral) — record the null result; do not invent a non-zero weight.

Purpose: complete RETR-01 (fusion on the live LLM-free hot path) and confirm RETR-04 (no online LLM added, latency intact).
Output: live hybrid responder + shipped `w*` default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/47-hybrid-retrieval-recall/47-CONTEXT.md
@.planning/phases/47-hybrid-retrieval-recall/47-PATTERNS.md
@.planning/phases/47-hybrid-retrieval-recall/47-01-SUMMARY.md
@.planning/phases/47-hybrid-retrieval-recall/47-02-SUMMARY.md

<interfaces>
<!-- Verified against live source 2026-06-28. -->

src/responder/index.ts respond():
  - line 160: let queryForEmbed = boundedQuery;          // user-derived
  - lines 161-173: possibly rewritten to a declarative statement (LEVER 3 — the ONLY pre-retrieval LLM call, unchanged)
  - line 177: const [cueVec] = await this.provider.embed([queryForEmbed]);
  - lines 186-190: comment block currently explaining why LEVER 1 is "intentionally absent" (UPDATE this)
  - lines 191-195: const ranked = this.retrieval.retrieveRanked(cueVec, this.config.rankedRetrievalK, this.config.rankedRetrievalFloor);  // EDIT SITE — add queryForEmbed 4th arg

src/retrieval/engine.ts retrieveRanked(queryVec, k, floor, queryText?, opts?) — supplying queryText routes through hybridTopk (LEVER 1) + LEVER 2 temporal sort; engine reads this.config.bm25FusionWeight (added in Plan 01).

src/lib/config.ts DEFAULT_CONFIG.bm25FusionWeight (added in Plan 01, currently 0).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread queryForEmbed into the responder facts-first retrieveRanked call (D-07)</name>
  <files>src/responder/index.ts</files>
  <read_first>
    - src/responder/index.ts (read 130-200 — the respond() facts-first flow: queryForEmbed at 160, LEVER-3 rewrite 161-173, embed 177, the "LEVER 1 intentionally absent" comment 186-190, the retrieveRanked call 191-195)
    - src/retrieval/engine.ts (retrieveRanked 388-407 — confirm the 4th-arg queryText routing through hybridTopk)
    - .planning/phases/47-hybrid-retrieval-recall/47-02-SUMMARY.md (confirm the approved w*)
  </read_first>
  <action>
    Add `queryForEmbed` as the 4th argument to the facts-first `retrieveRanked(cueVec, this.config.rankedRetrievalK, this.config.rankedRetrievalFloor)` call (lines 191-195), enabling LEVER 1 hybrid fusion on the product path. `queryForEmbed` is already in scope (set line 160, possibly LEVER-3-rewritten) and is user-derived — satisfies T-04-03-I (queryText must never be LLM answer output; the LEVER-3 rewrite is a transformation of the user question, not generated content). Per CONTEXT Claude's-Discretion, pass `queryForEmbed` (the embed input) for consistency with the cue vector. Replace the "LEVER 1 intentionally absent" comment block (186-190) with a note that LEVER 1 is now ON via the `bm25FusionWeight` config scalar, that `w=0` is the instant pure-cosine fallback, and that the D-04 held-out per-category gate (Plan 02) is the regression guard against the 9ea5eabc stale-fact failure. Do NOT add any new LLM call — only the existing LEVER-3 rewrite remains. SCHEMA_VERSION unchanged.
  </action>
  <acceptance_criteria>
    - Source: src/responder/index.ts retrieveRanked call passes 4 args ending in `queryForEmbed`.
    - Source: the 186-190 comment block no longer says LEVER 1 is "intentionally absent"; it documents LEVER 1 ON + w=0 fallback + the D-04 gate.
    - Source: no new `this.provider`/LLM call is introduced in the retrieval path (only the pre-existing LEVER-3 rewrite remains).
    - Build: `npm run build` (tsc) clean.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The live responder facts-first path uses hybrid fusion; the comment reflects the new posture; no online LLM added; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Flip DEFAULT_CONFIG.bm25FusionWeight to w* and verify the live path + fallback</name>
  <files>src/lib/config.ts</files>
  <read_first>
    - src/lib/config.ts (the bm25FusionWeight default line added in Plan 01, ~795; neighbors rankStrengthWeight 794 / rankedRetrievalFloor 807)
    - .planning/phases/47-hybrid-retrieval-recall/47-02-SUMMARY.md (the approved w* value and the held-out gate verdict)
    - tests/recall-engine.test.ts (analog for any responder/engine integration assertion, if extending)
  </read_first>
  <action>
    Set `DEFAULT_CONFIG.bm25FusionWeight` to the approved `w*` from the Plan 02 checkpoint (NOT 0, unless Plan 02 concluded w*=0 as a null result). Update the inline comment to cite the held-out selection: `// Phase 47 D-05: w* = <value> from held-out LoCoMo sweep (R@5 max, zero per-category regression); set 0 to revert to pure cosine`. Keep the EngineConfig interface JSDoc accurate. Do NOT change any other knob. The `w=0` instant-fallback property is preserved because the value is a plain config override.
  </action>
  <acceptance_criteria>
    - Source: src/lib/config.ts DEFAULT_CONFIG.bm25FusionWeight == the approved w* (matches 47-02-SUMMARY); inline comment cites the held-out source.
    - Behavior: existing retrieval/responder tests still pass under the new default (`npm test` — no regression); if the new default perturbs a brittle ordering assertion, fix the test to assert hybrid behavior, not to re-pin pure cosine.
    - Fallback: a config override of `bm25FusionWeight: 0` reproduces pure cosine (already proven by Plan 01's isolation test — reference it, do not duplicate).
    - Build: `npm run build` clean.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <done>Shipped default is w* from the held-out gate; pure-cosine fallback intact; tsc clean and full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user question → retrieval | the responder's user-derived query (boundedQuery / LEVER-3 rewrite) crosses into retrieveRanked → hybridTopk → FTS MATCH |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-02 | Tampering / self-confirmation (LLM output reaching the query path) | responder.respond → retrieveRanked queryText | mitigate | The 4th arg is `queryForEmbed`, which is `boundedQuery` or its LEVER-3 declarative rewrite — both user/question-derived, NOT an LLM answer/hypothesis (T-04-03-I). Acceptance criterion forbids introducing a new LLM call in the retrieval path. The self-confirmation invariant (inferred output never strengthens a fact) is untouched: retrieval reads, it does not write strength. |
| T-47-01 | Tampering (FTS MATCH injection) | retrieveRanked → hybridTopk → ftsQueryFromText | mitigate | Sanitizer path unchanged from Plan 01; queryText is sanitized via ftsQueryFromText before MATCH (T-17-02-T). |
| T-47-SC | Tampering (supply chain) | npm installs | accept | No package installs in this plan — net-zero new runtime deps. No install task. |
</threat_model>

<verification>
- `npm run build` clean.
- `npm test` green (full suite, including Plan 01's isolation test and existing responder/retrieval tests).
- Responder retrieveRanked call carries queryForEmbed; config default == approved w*.
- No online LLM call added to retrieval (only the pre-existing LEVER-3 rewrite remains).
</verification>

<success_criteria>
- Live product QA path uses BM25+dense hybrid fusion (RETR-01) on the LLM-free hot path.
- Shipped default = held-out-selected w* (RETR-02 selection consumed); w=0 instant fallback intact.
- No online LLM call introduced (RETR-04); latency budget held (per Plan 02 measurement).
- queryText is user-derived (T-04-03-I); SCHEMA_VERSION unchanged; net-zero new deps.
</success_criteria>

<output>
Create `.planning/phases/47-hybrid-retrieval-recall/47-03-SUMMARY.md` when done.
</output>
