---
phase: 47-hybrid-retrieval-recall
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - scripts/eval/locomo-harness.cjs
  - scripts/eval/results/bm25-weight-sweep.md
autonomous: false
requirements: [RETR-02, RETR-03, RETR-04]
user_setup: []
must_haves:
  truths:
    - "The LoCoMo harness has a hybrid retrieval arm that exercises the SAME fusion+temporal path as the responder (buildRetrievalEngine + retrieveRanked with floor=0, queryText), not the bare CandidateRetriever.topk path (D-08)"
    - "The harness can run retrieval-only (no answer generation) so the weight sweep is LLM-free (D-06)"
    - "Conversations are partitioned into a deterministic tune/test split; the sweep tunes on TUNE only and reports on TEST only (D-05)"
    - "A grid sweep of bm25FusionWeight selects w* = argmax(R@5) on the TUNE set subject to the no-per-category-regression constraint"
    - "On the held-out TEST set, hybrid w* R@5 and R@10 are >= pure-cosine (w=0) baseline AND no LoCoMo category regresses (THE D-04 HARD GATE)"
    - "Hybrid retrieval p50 latency stays within the live-brain ~45ms budget (RETR-04), measured not assumed"
  artifacts:
    - path: "scripts/eval/locomo-harness.cjs"
      provides: "hybrid arm (config-weight-driven retrieveRanked, floor=0), retrieval-only mode, deterministic tune/test split, per-category R@5/R@10 aggregation, per-weight sweep over the once-built brain"
      contains: "bm25FusionWeight"
    - path: "scripts/eval/results/bm25-weight-sweep.md"
      provides: "recorded sweep table (per-weight R@5/R@10 on tune), chosen w*, held-out TEST per-category R@K (cosine vs hybrid), retrieval p50/p95"
  key_links:
    - from: "scripts/eval/locomo-harness.cjs"
      to: "src/retrieval/engine.ts retrieveRanked"
      via: "buildRetrievalEngine(...) + retrieveRanked(queryVec, TOP_K, 0, questionText) with config.bm25FusionWeight set per sweep point"
      pattern: "retrieveRanked\\("
    - from: "harness per-weight loop"
      to: "config.bm25FusionWeight"
      via: "config override per grid point over the already-built scratch brain"
      pattern: "bm25FusionWeight"
---

<objective>
Make hybrid recall measurable and select the shipped weight on held-out data (D-04/D-05/D-06/D-08). Add a hybrid retrieval arm to the LoCoMo harness that exercises the exact fusion+temporal path the responder will use, run an LLM-free grid sweep of `bm25FusionWeight` on a deterministic TUNE split, lock `w* = argmax(R@5)` subject to zero per-category regression, and report R@5/R@10 + per-category + latency on the held-out TEST split only.

This plan produces the number Plan 03 ships (`w*`) and the gate verdict that authorizes shipping. The sweep is retrieval-only (R@K from retrieved node-ids vs gold node-ids) — no answer generation, no LLM billing for the tuning itself (D-06).

Purpose: prove RETR-03 (R@K up, no per-category regression) and RETR-04 (latency budget) empirically; select the RETR-02 default.
Output: instrumented harness + a recorded sweep result with the chosen `w*` and the held-out gate verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/47-hybrid-retrieval-recall/47-CONTEXT.md
@.planning/phases/47-hybrid-retrieval-recall/47-PATTERNS.md
@.planning/phases/47-hybrid-retrieval-recall/47-01-SUMMARY.md

<interfaces>
<!-- Verified against live source 2026-06-28. -->

scripts/eval/locomo-harness.cjs:
  - line 158: function buildRetrievalEngine(db, dbPath) → returns new RetrievalEngine(...) with full LEVER 2 temporal sort. USE THIS for the hybrid arm (faithful to responder path).
  - line 432: const evalRetriever = new CandidateRetriever(scratch.db)  // current bare retriever
  - line 464: const topkResults = evalRetriever.topk(queryVec, TOP_K)   // CURRENT pure-cosine arm (edit site)
  - line 467-468: topkIds = topkResults.map(r => r.id); topkScores = topkResults.map(r => r.score)
  - lines 481-498: retrievedSessionsForIds(nodeIds) → Set of session indices
  - lines 500-504: sessionsAt5/sessionsAt10 + hit5/hit10 (R@K computation — reuse unchanged, just feed hybrid topkIds)
  - line 363: scoreableQA = allQA.filter(qa => qa.category !== 5)  // category 5 (adversarial) excluded from scoring
  - lines 563-578: per-QA output record (carries category, hit5, hit10, retrieval_ms)
  - arg helper line 69-70: arg('--flag', default); flags via process.argv; env via process.env
  - TOP_K line 95 (default 10)

  CAUTION — UNCOMMITTED DIFF (out of scope): lines ~515-545 carry an answerer-V2 abstention experiment (RECENSE_LOCOMO_ANSWER_V2). DO NOT touch, rebase, or entangle it. Phase 47 edits live in lines 432-510 (retrieval + R@K) and a new sweep/aggregation block — non-overlapping with the answerer diff.

src/retrieval/engine.ts retrieveRanked(queryVec, k, floor, queryText?, opts?) — floor=0 lets all fused results through for eval (BM25-only hits carry score=0).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument the harness — hybrid arm, retrieval-only mode, tune/test split, per-category R@K, per-weight sweep</name>
  <files>scripts/eval/locomo-harness.cjs</files>
  <read_first>
    - scripts/eval/locomo-harness.cjs (read 50-120 imports+args, 158-165 buildRetrievalEngine, 360-470 the QA loop + retrieval call, 480-510 R@K computation, 560-645 output + final console — to place per-category aggregation and avoid the answerer-V2 diff region at 515-545)
    - src/retrieval/engine.ts (retrieveRanked 388-421) — confirm floor=0 semantics for the hybrid branch
    - .planning/phases/47-hybrid-retrieval-recall/47-PATTERNS.md (the locomo-harness.cjs section, edit sites + buildRetrievalEngine guidance)
  </read_first>
  <action>
    Surgically extend the harness (do NOT touch the 515-545 answerer-V2 region):
    1. Hybrid arm: replace the line-464 `evalRetriever.topk(queryVec, TOP_K)` with a retrieval path that calls a RetrievalEngine built via `buildRetrievalEngine(scratch.db, dbPath)` and `retrieveRanked(queryVec, TOP_K, 0, questionText)` (floor=0). `questionText` is the user-derived LoCoMo question (already in scope) — it flows through ftsQueryFromText internally, satisfying T-17-02-T; never pass raw text to stmtBm25. The engine's `config.bm25FusionWeight` is the per-run weight (see step 5). Map results to `topkIds`/`topkScores` identically (same `{id,score}` shape) so the existing R@K block (500-504) is unchanged.
    2. Retrieval-only mode: add a `--retrieval-only` flag (via the existing `arg`/`process.argv` pattern) that runs steps 1-4 (embed + retrieve + R@K) and SKIPS answer generation (the LLM step at ~506+). This keeps the sweep LLM-free (D-06). Guard the answer-gen block with `if (!RETRIEVAL_ONLY)`.
    3. Deterministic tune/test split: sort conversations by `sample_id` ascending; assign even sorted-index → TUNE, odd sorted-index → TEST (a 5/5 conversation split for the 10-conversation LoCoMo set). Add a `--split <tune|test|all>` flag (default `all`) that filters conversations to the chosen partition. Tag each output record with its split.
    4. Per-category R@K aggregation: at the end of a run, aggregate `hit5`/`hit10` by `qa.category` across all scored QA (category !== 5) and print a console table: category, n, R@5, R@10 — plus an overall row. This is the D-04 gate hook (currently aggregation is post-hoc from JSONL; add it in-harness).
    5. Per-weight sweep over the once-built brain: add a `--sweep <comma-list>` flag (e.g. `--sweep 0,0.25,0.5,0.75,1.0,1.5,2.0`). When set, after a conversation's scratch brain is built+consolidated ONCE, run the retrieval+R@K loop for EACH weight value (constructing the RetrievalEngine with `config.bm25FusionWeight` = that value over the same scratch.db) and record per-weight per-category R@K. Consolidation (the expensive LLM part) happens once per conversation; only LLM-free retrieval repeats per weight (D-06). Print per-weight per-category tables at the end.
    Use the existing import set (RetrievalEngine, CandidateRetriever, DEFAULT_CONFIG, StrengthDecayManager, AllocationGate, NoopActivationTraceSink all already imported) — no new imports.
  </action>
  <acceptance_criteria>
    - CLI: `node scripts/eval/locomo-harness.cjs --dry-run` still runs (no crash from the new flags' defaults).
    - Source: the harness QA loop calls `retrieveRanked(queryVec, TOP_K, 0, questionText)` via a buildRetrievalEngine-constructed engine (hybrid arm), not `evalRetriever.topk`.
    - Source: a `--retrieval-only` guard wraps the answer-gen step; a `--split` filter and a `--sweep` loop exist; the answerer-V2 region (lines ~515-545) is byte-unchanged in the diff.
    - Behavior: a small `--run --probe`-scale invocation prints a per-category R@5/R@10 console table including an overall row.
    - Safety: `questionText` reaches BM25 only through retrieveRanked/hybridTopk (ftsQueryFromText sanitized); no raw text passed to stmtBm25.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/eval/locomo-harness.cjs --dry-run && test "$(git diff scripts/eval/locomo-harness.cjs | grep -E '^[+-]' | grep -c RECENSE_LOCOMO_ANSWER_V2)" = "0"</automated>
  </verify>
  <done>Harness has a faithful hybrid arm, retrieval-only mode, deterministic tune/test split, in-harness per-category R@K, and a per-weight sweep over the once-built brain — answerer-V2 diff untouched (grep returns 0 added/removed touching that token).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the held-out sweep, lock w*, confirm the D-04 gate + RETR-04 latency</name>
  <read_first>
    - scripts/eval/locomo-harness.cjs (as instrumented in Task 1 — confirm flag names before running)
    - .planning/phases/47-hybrid-retrieval-recall/47-CONTEXT.md (D-04 gate, D-05 split/sweep rules)
    - STATE.md v8.0 baseline (LoCoMo R@5/R@10 = 77.3/82.2%, retrieval p50/p95 = 45/46ms) — the comparison baseline
  </read_first>
  <what-built>
    The instrumented harness (Task 1). The executor runs the LLM-free retrieval sweep and records results, then a human confirms the chosen w* and the gate verdict before it is shipped (Plan 03).
  </what-built>
  <action>
    Execute and record (LLM-free retrieval-only — D-06):
    1. TUNE sweep: run `node scripts/eval/locomo-harness.cjs --run --retrieval-only --split tune --sweep 0,0.25,0.5,0.75,1.0,1.5,2.0` (build each tune conversation's brain once; sweep weights over it). Record per-weight overall R@5 and per-category R@5/R@10.
    2. Select w* = argmax(R@5) over the grid on TUNE, SUBJECT TO the constraint that at w* no LoCoMo category's R@5 or R@10 falls below the w=0 (pure-cosine) value on TUNE (D-04 applied during selection). If the unconstrained argmax violates per-category no-regression, step down the grid to the highest-R@5 weight that satisfies it. If NO positive weight satisfies it, w* = 0 and that is a valid (null-result) outcome — record it honestly (no inflated metrics).
    3. TEST report: run `... --run --retrieval-only --split test --sweep 0,<w*>` to get the held-out cosine baseline (w=0) and hybrid (w*) side by side. Record per-category R@5/R@10 for both and the overall delta.
    4. Latency (RETR-04): from the TEST run, record hybrid retrieval p50/p95 (the harness logs retrieval_ms per QA) and confirm p50 is within the ~45ms live-brain budget. If retrieval-only timing differs materially from the live path, note it.
    5. Write `scripts/eval/results/bm25-weight-sweep.md` with: the tune sweep table, the chosen w* + the constraint check, the held-out TEST per-category table (cosine vs hybrid), and p50/p95. All numbers measured, none claimed.
  </action>
  <how-to-verify>
    Open `scripts/eval/results/bm25-weight-sweep.md` and confirm:
    1. The TUNE sweep table covers all grid points and shows R@5 per weight.
    2. The chosen w* maximizes TUNE R@5 subject to zero per-category regression (the constraint is shown, not just the winner).
    3. The held-out TEST table shows hybrid(w*) R@5 AND R@10 >= cosine(w=0), with NO category regressing (THE GATE). Pay specific attention to the temporal-reasoning category (the 9ea5eabc failure mode).
    4. Hybrid retrieval p50 is within the ~45ms budget.
    5. If w* = 0 (no positive weight passes the gate), that null result is recorded plainly — do not ship a regressing weight.
  </how-to-verify>
  <acceptance_criteria>
    - CLI: the sweep and test commands produce per-category R@5/R@10 tables for both cosine and hybrid arms.
    - Gate: held-out TEST R@5(hybrid w*) >= R@5(cosine) AND R@10(hybrid w*) >= R@10(cosine) AND no per-category R@K regression — OR w*=0 recorded as a null result.
    - Latency: hybrid retrieval p50 recorded and within ~45ms (RETR-04); if not, flagged for the human.
    - Artifact: scripts/eval/results/bm25-weight-sweep.md exists with all measured numbers.
  </acceptance_criteria>
  <resume-signal>Type "approved: w*=<value>" to authorize Plan 03 shipping that default, or describe issues / request a different grid.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LoCoMo question → FTS5 MATCH | the dataset question string crosses into stmtBm25 MATCH via retrieveRanked → hybridTopk → ftsQueryFromText |
| LoCoMo JSON file → harness | local eval dataset read from disk (offline, batch) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering (FTS MATCH injection) | harness → retrieveRanked → hybridTopk → ftsQueryFromText (topk.ts) | mitigate | The harness passes `questionText` only through `retrieveRanked`/`hybridTopk`, which internally call `ftsQueryFromText` (T-17-02-T). Acceptance criterion forbids passing raw text to stmtBm25. |
| T-47-03 | Tampering (eval data) | LoCoMo JSON parsing | accept | Offline harness over a local, founder-controlled dataset; no untrusted network input. Existing T-40-SCHEMA-01 JSON-shape handling is unchanged. |
| T-47-SC | Tampering (supply chain) | npm installs | accept | No package installs in this plan — net-zero new runtime deps. No install task. |
</threat_model>

<verification>
- `node scripts/eval/locomo-harness.cjs --dry-run` runs clean with the new flags.
- The answerer-V2 uncommitted diff region is untouched (grep gate in Task 1 verify).
- The held-out TEST gate (D-04) is satisfied or a null result is recorded.
- Hybrid retrieval p50 within budget (RETR-04).
</verification>

<success_criteria>
- Harness exercises the same fusion+temporal path the responder will use (D-08).
- Sweep is LLM-free (retrieval-only) and tunes on TUNE, reports on TEST (D-05/D-06).
- w* selected as argmax(R@5) subject to zero per-category regression (D-04/D-05).
- Held-out TEST: hybrid(w*) R@5/R@10 >= cosine, no per-category regression — OR honest w*=0 null result.
- Retrieval p50 within ~45ms (RETR-04).
- bm25-weight-sweep.md records all measured numbers.
</success_criteria>

<output>
Create `.planning/phases/47-hybrid-retrieval-recall/47-02-SUMMARY.md` when done (record the chosen w* and the gate verdict prominently — Plan 03 consumes w*).
</output>
