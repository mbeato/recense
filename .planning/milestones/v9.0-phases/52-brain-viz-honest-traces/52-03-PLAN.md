---
phase: 52-brain-viz-honest-traces
plan: 03
type: execute
wave: 3
depends_on: [52-02]
files_modified:
  - src/viz/modules/trace.js
autonomous: true
requirements: [SPEC-SC3, D-03, D-05, D-07-magnitude]
must_haves:
  truths:
    - "applyTrace branches on row.kind: recall (amber/cyan), new_node green blip, oscillation orange wavering, reconsolidation magenta hero"
    - "Reconsolidation plays the full choreography: green arriving blip from candidate_id merges INTO the existing node, magenta error-flash, settles — no duplicate node added to the rendered graph"
    - "Non-hero / unknown kinds render as the muted neutral color (never amber)"
  artifacts:
    - path: "src/viz/modules/trace.js"
      provides: "per-kind color/motion vocabulary + reconsolidation hero choreography"
      contains: "reconsolidation"
  key_links:
    - from: "src/viz/modules/trace.js"
      to: "KIND_COLOR"
      via: "row.kind branch"
      pattern: "row\\.kind"
---

<objective>
Add the per-event color + motion vocabulary on top of the honest recall core (Plan 02). Render the
three ingestion hero kinds — `new_node` (quiet green blip), `oscillation` (orange wavering/strobing
pulse), and the signature `reconsolidation` magenta belief-update-in-place — plus a muted-neutral
fallback for every other consolidation kind. Reconsolidation gets the FULL D-05 choreography: incoming
evidence (`candidate_id`) arrives as a subordinate green blip that travels/merges INTO the existing
node, which error-flashes magenta and settles. NO duplicate node is spawned in the rendered graph.

Purpose: this is the moment that sells recense's differentiator — prediction-error updating a belief
in place rather than piling on a stale duplicate.
Output: `applyTrace` branches on `row.kind`; the hero choreography animates on existing rendered nodes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-brain-viz-honest-traces/52-CONTEXT.md
@docs/superpowers/specs/2026-06-29-brain-viz-honest-traces-design.md

<interfaces>
Ingestion row shape on the SSE stream (produced by Plans 04/05):
  { kind: 'new_node'|'reconsolidation'|'oscillation'|<consolidation-neutral>,
    seeds: [node_id],            // the node to light (existing/updated node for reconsolidation)
    hops: [{node_id: candidate_id, score, hop:1}] | [],   // arriving-evidence source for reconsolidation
    query_id, id, ts }
  Magnitude rides in via score (D-07): modulates flash intensity the way recall score modulates brightness.

SEED SHAPE (Plan 02): `seeds` entries may be a bare-string id (ingestion/bridge rows AND legacy recall)
  OR `{node_id, score}` (post-52 recall). Resolve seed ids via Plan 02's seed normalize helper
  (`seedId = typeof s === 'string' ? s : s.node_id`) — do NOT assume `row.seeds[0]` is a raw string.
  For reconsolidation the bridge (Plan 05) emits `seeds:[supersededNodeId]` (bare string) = the EXISTING
  node; `hops:[{node_id: newNodeId, score:<mid>, hop:1}]` = the arriving candidate. Normalize, then animate.

From src/viz/modules/constants.js (Plan 02): KIND_COLOR { recall_seed, recall_hop, new_node, reconsolidation, oscillation, neutral }
From trace.js (Plan 02): ctx.activate(node, level), ctx.spawnPulse(from, to), the per-frame tick decay envelope, the seed normalize helper.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branch applyTrace on row.kind — recall / new_node / oscillation + neutral fallback</name>
  <read_first>
    - src/viz/modules/trace.js (the Plan-02 applyTrace + spawnPulse + activate + tick + seed normalize helper)
    - src/viz/modules/constants.js KIND_COLOR (Plan 02)
    - 52-CONTEXT.md D-03 (color 3 hero kinds, everything else muted neutral) + the spec motion table (lines 100-106)
  </read_first>
  <action>
    Make `applyTrace(row)` switch on `row.kind` (absent / `'recall'` / NULL → the existing recall path
    from Plan 02). Resolve the seed node via Plan 02's normalize helper (seeds may be string or
    {node_id,score}). For `new_node`: a quiet single green (`KIND_COLOR.new_node`) blip — activate the seed
    node once at a modest intensity, no spreading, fast fade. For `oscillation`: an orange
    (`KIND_COLOR.oscillation`) wavering/strobing pulse on the seed — visibly unsettled (re-trigger
    activation a few times with a sub-second period so it reads as oscillating). For any other kind value
    (consolidation-neutral — `confirm`/`extend`/`schema_emitted`/etc. mapped upstream): activate the seed
    at low intensity in `KIND_COLOR.neutral` (muted slate, non-amber) — background consolidation that does
    not compete with the amber/cyan recall vocabulary. Pulse/flash intensity scales with the hop/seed
    score (magnitude proxy, D-07). The wavefront shader color must follow the kind (parameterize the amber
    `HOT_COLOR` so spawnPulse / activation can take a per-kind color rather than hardcoding amber).
  </action>
  <acceptance_criteria>
    - `applyTrace` reads `row.kind` and dispatches the four cases (grep `row.kind` present).
    - new_node uses `KIND_COLOR.new_node`; oscillation uses `KIND_COLOR.oscillation`; unknown/non-hero uses `KIND_COLOR.neutral` — NOT `HOT`/amber (assertable: the neutral branch references `KIND_COLOR.neutral`).
    - Recall path (absent kind) is byte-unchanged in behavior from Plan 02 (regression: tests/trace-honest-recall.test.ts still green).
    - oscillation produces a repeating/wavering activation (multiple activate calls on the seed), not a single static pulse.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/trace-honest-recall.test.ts</automated>
  </verify>
  <done>Each kind renders its spec color/motion; non-hero kinds are muted neutral; recall unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Reconsolidation hero choreography (D-05) — arriving green → magenta flash → settle, no duplicate</name>
  <read_first>
    - src/viz/modules/trace.js (spawnPulse travel mechanics + activate + tick decay + seed normalize helper)
    - 52-CONTEXT.md D-05 (full choreography, no duplicate spawns) + spec motion table reconsolidation row
  </read_first>
  <action>
    Implement the `reconsolidation` branch as the full choreography. The seed (normalize `row.seeds[0]`) is
    the EXISTING/updated belief node; the arriving evidence is `row.hops[0].node_id` (the candidate_id).
    Sequence: (1) brief subordinate green (`KIND_COLOR.new_node`) "arriving" blip that travels from the
    candidate node toward the existing node — reuse the per-kind spawnPulse travel, candidate→existing,
    then let it vanish; (2) on arrival, a magenta (`KIND_COLOR.reconsolidation`, #ff3b6b) error-flash on
    the EXISTING node (bright attack, slightly higher than a recall seed), intensity scaled by score/
    magnitude; (3) settle back to normal via the standard decay envelope. CRITICAL: animate ONLY on nodes
    already in `ctx.idMap` — do NOT add any node to the rendered graph (no `Graph.graphData` mutation, no
    new mesh). If the candidate node is not in idMap, skip the arriving blip and flash the existing node
    directly (graceful degradation — never fabricate a position). Guard the whole branch so a missing seed
    node is a no-op (never throws).
  </action>
  <acceptance_criteria>
    - The reconsolidation branch flashes `KIND_COLOR.reconsolidation` on the seed (existing) node and spawns the arriving green blip from the candidate (hops[0]) toward it.
    - No code path in the reconsolidation branch calls `ctx.Graph.graphData` / adds a node or mesh to the graph (grep the branch: zero graph-mutation calls) — the "no duplicate node spawns" guarantee at the render layer.
    - Missing seed in idMap → branch returns without throwing; missing candidate → flash-only (no blip), still no throw.
    - tests/trace-honest-recall.test.ts (Plan 02) remains green (recall path untouched).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/trace-honest-recall.test.ts</automated>
  </verify>
  <done>Reconsolidation plays evidence-arrives → magenta in-place flash → settle, strictly on existing rendered nodes — the hero moment, no duplicate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE row.kind → client renderer | server-sourced enum string; client branches but never evals it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06 | Tampering (render integrity) | reconsolidation branch | mitigate | branch animates only on existing idMap nodes; never mutates graphData — "no duplicate node" enforced at render layer + grep-asserted |
| T-52-07 | Denial of Service (unbounded motion) | oscillation strobe | accept | strobe is a fixed, bounded re-trigger count on a single seed; decays via the standard envelope |
</threat_model>

<verification>
- `npx vitest run tests/trace-honest-recall.test.ts` green (recall regression intact).
- Manual/founder verification of the hero motion is deferred to Plan 06 (checkpoint).
</verification>

<success_criteria>
applyTrace renders the full per-kind vocabulary; reconsolidation is the magenta in-place belief update with an arriving green evidence blip and zero duplicate nodes.
</success_criteria>

<output>
Create `.planning/phases/52-brain-viz-honest-traces/52-03-SUMMARY.md` when done.
</output>
