---
phase: 52-brain-viz-honest-traces
plan: 04
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - src/viz/server.ts
  - src/consolidation/run-sleep-pass.ts
  - tests/sleep-pass-viz-lighting.test.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-07-seedshape, SPEC-bridge-transport]
must_haves:
  truths:
    - "The SSE /events trace payload carries row.kind so the client can branch on it"
    - "The SSE payload reships parsed seeds verbatim — a recall row whose seeds are [{node_id, score}] objects (Plan 02) ships those objects unchanged; legacy/ingestion bare-string seeds also pass through (shape-transparent)"
    - "The existing capped lightConsolidatedNodes cascade tags each emit with a kind derived from its consolidation event_type"
    - "The cascade still lights ALL consolidated nodes (D-03); hero kinds get hero colors, every other event_type maps to consolidation-neutral"
  artifacts:
    - path: "src/viz/server.ts"
      provides: "kind selected + shipped in the SSE trace payload; seeds reshipped verbatim (dual shape)"
      contains: "kind"
    - path: "src/consolidation/run-sleep-pass.ts"
      provides: "event_type→kind mapping in lightConsolidatedNodes, emitted on the existing cascade"
      contains: "kind"
  key_links:
    - from: "src/consolidation/run-sleep-pass.ts"
      to: "activation_trace.kind"
      via: "traceSink.emit({ ..., kind })"
      pattern: "kind"
    - from: "src/viz/server.ts"
      to: "SSE trace payload"
      via: "stmtTrace SELECT + JSON payload"
      pattern: "kind"
---

<objective>
Plumb `kind` through the read/replay backend WITHOUT building a second stream (D-01/D-02): (1) the
viz server selects + ships `kind` in the SSE `trace` payload (and reships `seeds` verbatim — now a
dual shape: legacy `string[]` or post-52 `[{node_id, score}]` from Plan 02); (2) the EXISTING capped
`lightConsolidatedNodes` cascade (`CASCADE_MAX=24` / `CASCADE_GAP_MS=300`) is upgraded to derive a
`kind` from each node-op's consolidation `event_type` and emit it. The sleep pass already fires this
same set of events at tens-to-hundreds per pass — reusing the already-throttled cascade is the
flood-safe resolution (D-02); we do NOT stream raw sleep-pass events into the 50-row ring.

Purpose: consolidated nodes render with their correct per-event colors during the sleep pass while
honoring the ring buffer's POLL_MS cadence; recall rows carry per-seed score to the client.
Output: SSE payload carries kind + dual-shape seeds; cascade emits kind; existing "light all nodes" behavior preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-brain-viz-honest-traces/52-CONTEXT.md
@docs/superpowers/specs/2026-06-29-brain-viz-honest-traces-design.md

<interfaces>
From src/viz/server.ts:367-413 — stmtTrace `SELECT id, ts, query_id, seeds, hops FROM activation_trace WHERE id > ? ORDER BY id ASC`; poll loop `JSON.parse`s seeds/hops into `unknown` and ships them verbatim in the `event: trace\ndata: {…}` payload. seeds is shape-transparent here (the server never indexes into seed elements) — a `[{node_id, score}]` array reships unchanged.

From src/consolidation/run-sleep-pass.ts:85-111 — lightConsolidatedNodes: SwitchableActivationTraceSink, selects `node_id FROM consolidation_event WHERE ts >= ? AND node_id IS NOT NULL ORDER BY ts ASC LIMIT CASCADE_MAX`, emits `{ query_id, seeds:[node_id], hops:[] }` per op (bare-string seeds — back-compat union accepts this), spaced CASCADE_GAP_MS. Whole body in try/catch (fire-and-forget, T-10-05).

From src/consolidation/sink.ts:53-65 — ConsolidationEventType enum (confirm, extend, unrelated, contradict_hold, contradict_reconcile, contradict_oscillation, contradict_append_new, contradict_force_destabilize, schema_emitted, schema_falsified, entity_merge, fact_merge).

ActivationTraceInput now accepts `kind?: string|null` and `seeds: Array<string|{node_id,score}>` (Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship kind in the SSE trace payload + verbatim dual-shape seeds (server.ts)</name>
  <read_first>
    - src/viz/server.ts:367-413 (stmtTrace SELECT + poll loop payload at 402-411)
  </read_first>
  <action>
    Add `kind` to the `stmtTrace` SELECT column list. In the poll loop, include `kind: row.kind ?? null`
    in the JSON `data` object of the `event: trace` payload (alongside id/ts/query_id/seeds/hops). No new
    event type, no new channel — the existing `trace` event gains one field. The corrupt-row try/catch and
    monotonic cursor are unchanged. The read-only handle still never writes. The `seeds` value is already
    reshipped verbatim after `JSON.parse` — it may now be a legacy `string[]` OR the post-52
    `[{node_id, score}]` shape (Plan 02 engine.ts); server.ts is shape-transparent and ships whatever the
    column holds, so NO per-element handling is added here. Add a brief comment noting the dual seed shape
    so the passthrough contract is explicit (the client normalizes — Plan 02).
  </action>
  <acceptance_criteria>
    - `stmtTrace` SELECT names `kind`; the payload `data` object includes a `kind` key.
    - A recall row with NULL kind serializes `kind: null` (back-compat — client treats as recall).
    - The SSE payload reships parsed `seeds` verbatim: a row whose seeds are `[{node_id, score}]` objects ships those objects unchanged (no flattening to ids, no per-element handling).
    - `npx vitest run tests/viz-server.test.ts` passes (no regression to existing SSE wiring).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/viz-server.test.ts</automated>
  </verify>
  <done>The SSE trace event carries kind end-to-end (null for legacy recall rows) and reships seeds verbatim in either shape.</done>
</task>

<task type="auto">
  <name>Task 2: Map event_type→kind in the cascade; emit kind (run-sleep-pass.ts)</name>
  <read_first>
    - src/consolidation/run-sleep-pass.ts:51-111 (CASCADE_MAX/GAP + lightConsolidatedNodes)
    - src/consolidation/sink.ts:53-65 (the 12 event types)
    - tests/sleep-pass-viz-lighting.test.ts:1-60 (test harness: recordEvent inserts consolidation_event rows; traceSeeds reads back)
  </read_first>
  <action>
    Extend the cascade query to also select `event_type` (`SELECT node_id, event_type FROM
    consolidation_event WHERE ts >= ? AND node_id IS NOT NULL ORDER BY ts ASC LIMIT ?`). Add a pure
    `consolidationKind(event_type)` mapper: `unrelated` → `'new_node'`, `contradict_reconcile` →
    `'reconsolidation'`, `contradict_oscillation` → `'oscillation'`, and EVERY other event_type
    (`confirm`/`extend`/`contradict_hold`/`contradict_append_new`/`contradict_force_destabilize`/
    `schema_emitted`/`schema_falsified`/`entity_merge`/`fact_merge`/anything unknown) → `'consolidate'`
    (the neutral kind the client maps to muted slate, D-03). Pass `kind: consolidationKind(op.event_type)`
    on each `traceSink.emit(...)`. Keep lighting ALL ops (D-03 — do NOT filter; the cascade preserves
    current ambient behavior). The cascade stays inside its existing try/catch — emitting kind MUST NOT
    add any throw path that could perturb the pass (T-10-05). Extend tests/sleep-pass-viz-lighting.test.ts:
    assert the three hero event types map to their hero kinds and a non-hero type (e.g. `confirm`) maps to
    `'consolidate'`, read back from activation_trace.kind.
  </action>
  <acceptance_criteria>
    - `consolidationKind` maps the 3 hero event types to new_node/reconsolidation/oscillation and all others to `'consolidate'` (unit-asserted).
    - Each cascade emit carries `kind`; activation_trace rows written by the cascade have non-null kind.
    - The cascade still emits one trace per touched op (no filtering) — existing "light all nodes" assertions stay green.
    - The body remains fully wrapped so a kind-mapping error cannot escape lightConsolidatedNodes (consolidation result unaffected).
    - `npx vitest run tests/sleep-pass-viz-lighting.test.ts` passes including new kind assertions.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/sleep-pass-viz-lighting.test.ts</automated>
  </verify>
  <done>The capped sleep-pass cascade tags every consolidated node with the correct kind; hero kinds colored, rest neutral; flood-safe and fire-and-forget.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consolidation_event → cascade emit | internal provenance read inside the sleep-pass lock; no external input |
| viz server read-only handle → SSE | server never writes; client is a passive renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-08 | DoS (ring overflow) | sleep-pass flood | mitigate | reuse the existing CASCADE_MAX=24 / CASCADE_GAP_MS=300 cap (D-02) — no raw unthrottled stream into the 50-row ring |
| T-52-09 | Tampering (consolidation safety) | lightConsolidatedNodes | mitigate | kind mapping is a pure function inside the existing try/catch; emit failure swallowed — never perturbs the pass (mirrors T-10-05) |
| T-52-10 | Injection | event_type→kind | mitigate | kind comes from a closed switch over the known enum (default `'consolidate'`); written as a bound param by the sink |
</threat_model>

<verification>
- `npx vitest run tests/sleep-pass-viz-lighting.test.ts tests/viz-server.test.ts` green.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
SSE ships kind + verbatim dual-shape seeds; the existing capped cascade colors every consolidated node by its event_type (hero kinds + neutral) without a second stream and without any path that can perturb consolidation.
</success_criteria>

<output>
Create `.planning/phases/52-brain-viz-honest-traces/52-04-SUMMARY.md` when done.
</output>
