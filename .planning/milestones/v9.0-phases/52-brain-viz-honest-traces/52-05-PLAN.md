---
phase: 52-brain-viz-honest-traces
plan: 05
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - src/adapter/remember-viz-bridge.ts
  - src/adapter/remember-cli.ts
  - tests/remember-viz-bridge.test.ts
autonomous: true
requirements: [D-01, D-05, D-08-zero-duplicate, WARN-1-crossseam, SPEC-SC3]
must_haves:
  truths:
    - "A synchronous `recense remember` emits exactly one activation_trace row tagged with the kind matching its action (insert→new_node, reconcile→reconsolidation, oscillation→oscillation)"
    - "A contradiction (reconcile) produces ONE reconsolidation trace on the existing node and ZERO new_node traces — no duplicate blip spawned (D-08 #2)"
    - "The reconsolidation row the bridge emits (seeds:[supersededNodeId], hops:[{node_id:newNodeId, score:<mid>, hop:1}], kind:'reconsolidation') is EXACTLY the shape Plan 03's reconsolidation applyTrace branch consumes (existing node from seeds[0], arriving candidate from hops[0]) — pinned by a cross-seam assertion (WARN-1)"
    - "The bridge is fire-and-forget: a sink/flag failure never throws into the remember path; with the viz flag off it writes nothing (Noop)"
  artifacts:
    - path: "src/adapter/remember-viz-bridge.ts"
      provides: "pure rememberTraceInput mapper + fire-and-forget bridgeRememberToViz"
      contains: "reconsolidation"
    - path: "tests/remember-viz-bridge.test.ts"
      provides: "zero-duplicate-on-contradiction + per-action kind mapping + cross-seam shape assertion (D-08 #2, WARN-1)"
      contains: "new_node"
  key_links:
    - from: "src/adapter/remember-cli.ts"
      to: "bridgeRememberToViz"
      via: "post-result fire-and-forget call in main()"
      pattern: "bridgeRememberToViz"
    - from: "src/adapter/remember-viz-bridge.ts"
      to: "activation_trace"
      via: "SwitchableActivationTraceSink.emit"
      pattern: "SwitchableActivationTraceSink"
---

<objective>
Bridge the synchronous `recense remember` path onto the same `activation_trace` SSE stream so a curated
fact lights the brain live (≈1 event/fact — no flood risk, D-01). Map the `RememberResult.action` to the
right `kind`: `insert` → `new_node` (green), `reconcile` → `reconsolidation` (magenta hero, on the
existing node, arriving evidence = the new value), `oscillation` → `oscillation` (orange). The bridge is
fire-and-forget — wrapped in try/catch, gated on `viz_trace_enabled` via `SwitchableActivationTraceSink`,
and MUST NEVER perturb the remember write (project `[keep]` guard / engine emit guard T-10-05).

Purpose: makes ingestion — especially prediction-error reconsolidation — visible in real time, the
single most valuable moment in recense, which today fires nothing.
Output: a small bridge module + one wiring call in `remember-cli` main + the zero-duplicate test (D-08 #2)
+ the cross-seam shape assertion (WARN-1) that locks the bridge↔Plan-03 row contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-brain-viz-honest-traces/52-CONTEXT.md
@docs/superpowers/specs/2026-06-29-brain-viz-honest-traces-design.md

<interfaces>
From src/adapter/remember-cli.ts:147-153 — RememberResult:
  { action: 'insert'|'reconcile'|'oscillation'; newNodeId: string; supersededNodeId?: string; prevValue?: string; scope: string }
  main() at 401-477: builds db/store/sink, calls runRemember at 457-460 → `result`, then writes output 463-475.

From src/viz/activation-sink.ts — SwitchableActivationTraceSink(db, clock): refresh() returns enabled; emit({query_id, seeds, hops, kind}). seeds accepts the back-compat union (bare string OR {node_id, score}); the bridge uses bare-string seeds. Default Noop until refresh() confirms flag '1' (fail-closed).

PLAN 03 RECONSOLIDATION CONSUMER CONTRACT (the seam WARN-1 pins): applyTrace's reconsolidation branch
reads `row.seeds[0]` as the EXISTING node (flashed magenta) and `row.hops[0].node_id` as the arriving
candidate (green blip → existing). The bridge MUST emit seeds[0]=supersededNodeId, hops[0].node_id=newNodeId.

Switchable-sink wiring precedent: src/adapter/ambient-recall.ts:115-116 (`new SwitchableActivationTraceSink(db, clock); traceSink.refresh();`).
Fire-and-forget posture precedent: src/consolidation/run-sleep-pass.ts:91-110 (whole body in try/catch, flag-off early return).

From src/lib/hash — newId() for query_id.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: remember-viz-bridge.ts — pure mapper + fire-and-forget bridge + cross-seam shape lock</name>
  <read_first>
    - src/adapter/remember-cli.ts:147-153 (RememberResult), 220-376 (the three action paths so the mapping is semantically grounded: reconcile tombstones supersededNodeId + mints newNodeId; oscillation appends newNodeId keeping supersededNodeId)
    - src/adapter/ambient-recall.ts:114-116 (SwitchableActivationTraceSink wiring to mirror)
    - src/consolidation/run-sleep-pass.ts:91-110 (try/catch fire-and-forget posture to mirror)
    - src/viz/activation-sink.ts:43-56,130-163 (ActivationTraceInput + Switchable sink)
    - 52-03-PLAN.md <interfaces> (Plan 03 reconsolidation consumer contract — seeds[0]=existing, hops[0]=candidate)
  </read_first>
  <behavior>
    - rememberTraceInput({action:'insert', newNodeId:'A'}) → { kind:'new_node', seeds:['A'], hops:[] }
    - rememberTraceInput({action:'reconcile', newNodeId:'NEW', supersededNodeId:'OLD'}) → { kind:'reconsolidation', seeds:['OLD'], hops:[{node_id:'NEW', score:<mid>, hop:1}] }  // flash the EXISTING node, new value arrives
    - rememberTraceInput({action:'oscillation', newNodeId:'OSC', supersededNodeId:'OLD'}) → { kind:'oscillation', seeds:['OSC'], hops:[] }
    - Every result yields exactly ONE input with exactly ONE kind — never an insert/new_node alongside a reconcile.
    - CROSS-SEAM (WARN-1): for the reconcile mapping, `seeds[0] === supersededNodeId` (the EXISTING node Plan 03 flashes magenta) AND `hops[0].node_id === newNodeId` (the arriving candidate Plan 03 animates as the green blip) AND `kind === 'reconsolidation'` — exactly the shape Plan 03's reconsolidation branch consumes.
  </behavior>
  <action>
    Create src/adapter/remember-viz-bridge.ts. Export a PURE `rememberTraceInput(result: RememberResult):
    ActivationTraceInput` implementing the mapping in <behavior> (reconcile flashes the EXISTING node —
    `supersededNodeId` as the seed — with the new value `newNodeId` as the single arriving-evidence hop so
    Plan 03's choreography animates evidence→existing→magenta; use a fixed mid score for the hop per D-07,
    not a fabricated magnitude). Export `bridgeRememberToViz(db, clock, result): void` that constructs a
    `SwitchableActivationTraceSink`, calls `refresh()`, returns immediately if disabled (Noop/zero-cost),
    else emits `{ query_id: newId(), ...rememberTraceInput(result) }`. Wrap the ENTIRE body in try/catch
    that swallows — a flag read or sink failure must never surface to the caller (mirror
    run-sleep-pass.ts try/catch). Synchronous, no I/O beyond the shared handle.
  </action>
  <acceptance_criteria>
    - `rememberTraceInput` returns exactly the three mappings in <behavior> (unit-asserted); reconcile seed is `supersededNodeId`, hop[0] is `newNodeId`.
    - CROSS-SEAM (WARN-1): a unit assertion pins the reconcile row shape EXACTLY to Plan 03's consumer contract — `seeds[0] === supersededNodeId` AND `hops[0].node_id === newNodeId` AND `kind === 'reconsolidation'`. A shape drift fails the test, not the founder checkpoint.
    - `bridgeRememberToViz` never throws: with a sink stub that throws on emit, the call returns normally (asserted).
    - With `viz_trace_enabled` unset/off, `bridgeRememberToViz` writes ZERO activation_trace rows.
    - `npx vitest run tests/remember-viz-bridge.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/remember-viz-bridge.test.ts</automated>
  </verify>
  <done>A pure, tested action→kind mapper (with the Plan-03 reconsolidation shape pinned cross-seam) plus a fire-and-forget, flag-gated bridge that cannot perturb remember.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire bridge into remember-cli main + zero-duplicate structural test (D-08 #2)</name>
  <read_first>
    - src/adapter/remember-cli.ts:433-477 (main() — db built at 437, result at 457-460, output 463-475)
    - tests/memory-ops-trace.test.ts:1-50 (temp FILE-DB harness pattern — better-sqlite3 can't open :memory: readonly; flag-flip via meta insert)
  </read_first>
  <behavior>
    - reconcile result → exactly ONE activation_trace row with kind='reconsolidation', ZERO rows with kind='new_node' (the no-duplicate-blip guarantee, asserted at the data layer)
    - insert result → exactly ONE row kind='new_node'
    - oscillation result → exactly ONE row kind='oscillation'
    - flag off → zero rows regardless of action
  </behavior>
  <action>
    In remember-cli `main()`, after `result` is obtained (after line 460) and using the same open `db` +
    `realClock`, call `bridgeRememberToViz(db, realClock, result)` fire-and-forget BEFORE the process exits
    (it must run while the handle is open — place it before the output writes or just after, inside the
    existing try). Do not change the lock/transaction flow. Add tests/remember-viz-bridge.test.ts cases
    (temp FILE DB, flag=1 via meta insert) driving `bridgeRememberToViz` directly with each RememberResult
    shape and asserting the <behavior> activation_trace row counts by kind — the load-bearing case is
    reconcile → one reconsolidation + zero new_node (D-08 #2 honesty guarantee).
  </action>
  <acceptance_criteria>
    - `main()` calls `bridgeRememberToViz(db, realClock, result)` (grep present) while db is open.
    - Test: a reconcile RememberResult yields `SELECT COUNT(*) WHERE kind='reconsolidation'` == 1 AND `WHERE kind='new_node'` == 0.
    - Test: insert → 1 new_node; oscillation → 1 oscillation; flag-off → 0 rows.
    - `npx tsc --noEmit` clean; `npx vitest run tests/remember-viz-bridge.test.ts` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/remember-viz-bridge.test.ts</automated>
  </verify>
  <done>remember lights the brain with the correct kind; the contradiction→one-magenta-zero-duplicate guarantee is test-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remember write path → viz bridge | the load-bearing boundary: the bridge reads the remember RESULT only; it must not write/alter belief state or block the path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-11 | Tampering (consolidation safety — PRIMARY) | bridgeRememberToViz | mitigate | entire body in try/catch swallow; flag-gated Switchable sink; called AFTER the transaction with only the result — cannot perturb the belief write (mirrors T-10-05). Acceptance-tested: throwing sink stub → no throw |
| T-52-12 | DoS (stream flood) | remember bridge | accept | one fact = one emit (D-01); no loop, no fan-out; bounded by user typing speed, far below POLL_MS |
| T-52-13 | Injection | kind/seed values | mitigate | kind from a closed action switch; node ids are engine-minted newId()s written as bound params by the sink |
</threat_model>

<verification>
- `npx vitest run tests/remember-viz-bridge.test.ts` green (incl. zero-duplicate case + WARN-1 cross-seam assertion).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
`recense remember` lights the brain with action-correct kinds; a contradiction yields one magenta reconsolidation on the existing node and zero duplicate blips; the reconsolidation row shape is pinned to Plan 03's consumer; the bridge is provably incapable of perturbing the remember write.
</success_criteria>

<output>
Create `.planning/phases/52-brain-viz-honest-traces/52-05-SUMMARY.md` when done.
</output>
