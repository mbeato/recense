---
phase: 52-brain-viz-honest-traces
plan: 06
type: execute
wave: 3
depends_on: [52-02, 52-03, 52-04, 52-05]
files_modified: []
autonomous: false
requirements: [D-08-visual, SPEC-SC2, SPEC-SC3]
must_haves:
  truths:
    - "A live `recense viz` recall fires only seeds + real 1-hop neighbors with score-tracking brightness and the hold/fade tail — visibly not a 4-hop wall"
    - "A live `recense remember` of a new fact shows a green blip; a contradiction shows the magenta in-place flash with the arriving green evidence and no duplicate; an oscillation shows the orange wavering pulse"
    - "Color / motion / decay feel is founder-approved (or tuning notes captured for the named constants)"
  artifacts: []
  key_links: []
---

<objective>
Founder visual verification of the honest-traces rework (D-08 — structural tests are automated in Plans
02/04/05; this closes the loop on the parts only a human can judge: color, motion, decay feel, and the
full reconsolidation choreography). The machine-checkable honesty guarantees are already locked; this
checkpoint validates that the rendered result reads as MEANING rather than theater, and captures any
tuning of the named decay/palette constants.

Purpose: the decay envelope numbers (D-06) and palette only matter against the rendered feel — tune at
the window, not in the abstract.
Output: founder sign-off, or a punch-list of constant tweaks (DECAY_*/KIND_COLOR) to apply.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-brain-viz-honest-traces/52-CONTEXT.md
@docs/superpowers/specs/2026-06-29-brain-viz-honest-traces-design.md
@.planning/phases/52-brain-viz-honest-traces/52-02-SUMMARY.md
@.planning/phases/52-brain-viz-honest-traces/52-03-SUMMARY.md
@.planning/phases/52-brain-viz-honest-traces/52-04-SUMMARY.md
@.planning/phases/52-brain-viz-honest-traces/52-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Founder visual review — recall honesty, decay feel, ingestion vocabulary, reconsolidation hero</name>
  <what-built>
    - Recall animation now driven by the server's real scored 1-hop payload (BFS deleted); seed/edge
      brightness tracks retrieval score; lit set holds then fades on the D-06 attack/hold/exp-fade tail.
    - Ingestion bridged onto the same SSE stream with a per-kind color/motion vocabulary: new_node green
      blip, oscillation orange wavering pulse, reconsolidation magenta in-place flash with a green arriving
      evidence blip — and a muted-neutral color for background consolidation.
    - Automated guarantees already green: exactly-N-edges/no-BFS (Plan 02), event_type→kind cascade
      (Plan 04), zero-duplicate-on-contradiction (Plan 05).
  </what-built>
  <how-to-verify>
    1. Start the viz with tracing enabled: `recense viz` (this sets `viz_trace_enabled=1` for the window).
    2. RECALL honesty: trigger a few real recalls (use the app, or `recense recall "<query>"` in another
       shell, or the in-page test-trace button). Confirm: the firing is a tight seed cluster + ONE wave of
       real neighbors — NOT a fanned-out 4-hop wall. Strong hits read brighter than weak ones. The lit set
       briefly holds, then fades smoothly to a low floor (no abrupt linear cutoff).
    3. NEW FACT: in another shell run `recense remember "<a clearly new, unrelated fact>"`. Confirm a quiet
       GREEN blip at one node — no spreading.
    4. RECONSOLIDATION (the hero): `recense remember "<a fact that contradicts an existing belief>"`.
       Confirm: a subordinate GREEN evidence blip travels INTO the existing node, which error-FLASHES
       MAGENTA and settles — and NO second/duplicate node appears.
    5. OSCILLATION: assert a value, then assert its opposite back-and-forth to trip the oscillation guard.
       Confirm an ORANGE wavering/strobing pulse that reads as unsettled.
    6. Note whether the decay timing (attack/hold/fade) and any palette color feel right. The tunable
       constants are `DECAY_ATTACK_MS`/`DECAY_HOLD_MS`/`DECAY_FADE_MS`/`DECAY_FLOOR` and `KIND_COLOR.*` in
       `src/viz/modules/constants.js` — call out any values to nudge.
  </how-to-verify>
  <acceptance_criteria>
    - Founder confirms recall reads as honest (seed + 1 real hop wave, score-graded), not a topology wall.
    - Founder confirms the three ingestion kinds render their intended color + motion, and reconsolidation
      shows the in-place magenta update with no duplicate node.
    - Either "approved" OR a captured punch-list of constant tweaks (decay timings / palette hexes).
  </acceptance_criteria>
  <resume-signal>Type "approved", or list the constant tweaks to apply (e.g. "DECAY_FADE_MS 2500→2000, neutral too blue").</resume-signal>
</task>

</tasks>

<verification>
Human-in-the-loop. The automated honesty guarantees (Plans 02/04/05) must already be green before this checkpoint runs.
</verification>

<success_criteria>
The honest-traces rework is founder-approved as rendered (or a precise constant-tweak punch-list is captured for a fast follow-up edit). No engine/scoring/traversal behavior was touched.
</success_criteria>

<output>
Create `.planning/phases/52-brain-viz-honest-traces/52-06-SUMMARY.md` when done (record approval or the tuning punch-list).
</output>
