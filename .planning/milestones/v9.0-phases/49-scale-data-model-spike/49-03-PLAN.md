---
phase: 49-scale-data-model-spike
plan: 03
type: execute
wave: 3
depends_on: ["49-02"]
files_modified:
  - .planning/phases/49-scale-data-model-spike/49-SPIKE-FINDINGS.md
autonomous: true
requirements: [SCALE-01, SCALE-02]
user_setup: []

must_haves:
  truths:
    - "The findings doc carries the Phase-41-style frontmatter (phase, plan, artifact, requirement, sut_commit, date, harness, results)"
    - "§ SCALE-01 reports per-rung measured tables traced to the results JSON, and a written go/no-go verdict per the D-04 gate"
    - "A defer/no-go outcome is presented as an explicitly valid result — the doc does NOT force an ANN-adoption conclusion"
    - "§ SCALE-02 gives a written Zep-vs-DCPM-vs-tombstone recommendation with a better-sqlite3 migration-cost estimate from the schema delta (no migration code) (D-05)"
    - "Every number in the doc is traceable to the results JSON or marked unmeasured-here — no fabricated metrics"
  artifacts:
    - path: ".planning/phases/49-scale-data-model-spike/49-SPIKE-FINDINGS.md"
      provides: "The single SCALE-01 + SCALE-02 deliverable (D-06)"
      contains: "SCALE-01"
  key_links:
    - from: "49-SPIKE-FINDINGS.md § SCALE-01"
      to: "scripts/eval/results/49-crossover-spike.json"
      via: "measured tables transcribe the JSON per-rung numbers"
      pattern: "recall"
    - from: "49-SPIKE-FINDINGS.md § SCALE-02"
      to: "src/db/schema.ts (prev_value/prev_ts/tombstoned + SCHEMA_VERSION)"
      via: "migration-cost estimate measured against the current schema delta"
      pattern: "schema"
---

<objective>
Write the single `49-SPIKE-FINDINGS.md` deliverable (D-06): § SCALE-01 transcribes the
benchmark's per-rung numbers into Phase-41-style measured tables and renders the go/no-go
verdict per the D-04 gate; § SCALE-02 is a desk-analysis written recommendation on bi-temporal
(Zep) / supersedes-chain (DCPM) vs the current tombstone reconsolidation model, with a
better-sqlite3 migration-cost estimate derived from the schema delta — NO migration code.

Purpose: Discharge the decision half of SCALE-01 and all of SCALE-02. This is the phase's
sole written artifact; it must honor the "defer/no-go is a valid outcome" framing and the
no-fabrication honesty discipline.

Output: `.planning/phases/49-scale-data-model-spike/49-SPIKE-FINDINGS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-scale-data-model-spike/49-CONTEXT.md
@.planning/phases/49-scale-data-model-spike/49-PATTERNS.md
@.planning/phases/41-vector-index-and-hot-path-latency/41-SPIKE-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: § SCALE-01 — measured tables + go/no-go verdict</name>
  <files>.planning/phases/49-scale-data-model-spike/49-SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/41-vector-index-and-hot-path-latency/41-SPIKE-FINDINGS.md (format precedent — frontmatter lines 1-11, measured-table format 40-50, explicit `## Decision` block 96-105, closing provenance line 137-138)
    - scripts/eval/results/49-crossover-spike.json (THE numbers being reported — produced by Plan 02)
    - .planning/phases/49-scale-data-model-spike/49-02-SUMMARY.md (Plan 02's headline result + whether ANN loaded)
    - .planning/phases/49-scale-data-model-spike/49-CONTEXT.md (D-03 metrics, D-04 gate, D-06 format)
  </read_first>
  <action>
    Create `49-SPIKE-FINDINGS.md` with the Phase-41 YAML frontmatter shape: `phase:
    49-scale-data-model-spike`, `plan: 01`, `artifact: SPIKE-FINDINGS`,
    `requirement: [SCALE-01, SCALE-02]`, `sut_commit:` (from `git rev-parse --short HEAD` /
    the results JSON `meta.commit`), `date: 2026-06-28`, `harness:
    scripts/eval/49-crossover-spike.cjs`, `results: scripts/eval/results/49-crossover-spike.json
    (gitignored, local-only)`.

    Write `## SCALE-01 — Exact flat-buffer cosine vs HNSW crossover`. Transcribe the results JSON
    into a measured table per scale rung {11.3k live, 25k, 50k (100k if measured)} with columns:
    nodes, exact p50/p95 (ms), ANN p50/p95 (ms), recall@5, recall@10, build (ms), mem (MB). Bold
    the headline numbers. EVERY number must come from the JSON; if `ann_available.available ===
    false`, mark the ANN columns `unmeasured-here` and quote the load-error string exactly as
    Phase 41 recorded sqlite-vec "unavailable (module not installed)" — do NOT fabricate.

    Then a `## Decision (SCALE-01)` block stating the verdict in the first sentence per the D-04
    gate: ANN is **GO** ONLY IF the exact index's p95 measurably exceeds the felt-path budget
    (the Phase-40/41 warm baseline ≈ 45/46 ms over ~11.3k nodes) at realistic near-term scale
    AND HNSW holds recall@10 ≥ 0.95 vs exact at that scale. If exact p95 stays within budget
    across the sweep, OR the ANN lib was unmeasured-here, the honest verdict is **NO-GO / DEFER**
    — state that defer is an explicitly valid result (exact wins on simplicity + zero-dep +
    byte-exact correctness; ANN stays a future-requirement trigger). Identify the crossover point
    (the node scale, if any, where exact p95 first breaches the budget), or state explicitly that
    no crossover was observed within the swept range. Close the section with the read-only
    provenance line: "Real numbers, live brain, read-only. No writes to
    `~/.config/recense/recense.db` occurred."
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f='.planning/phases/49-scale-data-model-spike/49-SPIKE-FINDINGS.md';const s=fs.readFileSync(f,'utf8');const fm=/requirement:\s*\[SCALE-01,\s*SCALE-02\]/.test(s);const sec=/##\s*SCALE-01/.test(s);const dec=/##\s*Decision/i.test(s);const verdict=/(GO|NO-GO|DEFER)/.test(s);const prov=/read-only/i.test(s);process.exit(fm&&sec&&dec&&verdict&&prov?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter has `requirement: [SCALE-01, SCALE-02]`, `harness:`, and `results:` keys (grep-confirmed).
    - `## SCALE-01` section present with a per-rung measured table; numbers match the results JSON (or ANN cells marked `unmeasured-here` with the verbatim error string — no invented numbers).
    - A `## Decision` block states GO / NO-GO / DEFER per the D-04 gate, and explicitly frames defer as valid.
    - The crossover point is named, or "no crossover within swept range" is stated explicitly.
    - Read-only provenance line present.
  </acceptance_criteria>
  <done>§ SCALE-01 reports the measured crossover sweep and a D-04-gated go/no-go (defer-valid) verdict, fully traced to the results JSON.</done>
</task>

<task type="auto">
  <name>Task 2: § SCALE-02 — bi-temporal/supersedes recommendation + migration-cost estimate</name>
  <files>.planning/phases/49-scale-data-model-spike/49-SPIKE-FINDINGS.md</files>
  <read_first>
    - src/db/schema.ts (current node reconsolidation columns: prev_value/prev_ts/pending_contradictions/tombstoned around lines 50-53; SCHEMA_VERSION=14 line 11; additive ADD COLUMN guard ~222-235; CHECK-change full-table-recreation ~394-435; version-stamp/downgrade-guard ~607-623)
    - src/db/semantic-store.ts (single-writer setEmbedding / IMMEDIATE-transaction doctrine the migration must preserve)
    - .planning/phases/49-scale-data-model-spike/49-PATTERNS.md (lines 235-276 — the SCALE-02 migration-cost reference + invariants)
    - CLAUDE.md (§Constraints — never delete evidence-backed facts via decay; graph is source of truth; tombstone-always was the v1 choice)
  </read_first>
  <action>
    Append `## SCALE-02 — Reconsolidation data-model recommendation` to the same findings doc.
    This is desk analysis ONLY — NO migration code (D-05). Name the three options explicitly:
    Zep-style **bi-temporal validity intervals** (e.g. adding `valid_from`/`valid_to`/`tx_from`/
    `tx_to`), DCPM-style **doubly-linked supersedes chains** (e.g. `supersedes_id`/`superseded_by_id`
    + an index), versus the current **tombstone-always** model (`prev_value`/`prev_ts`/`tombstoned`/
    `pending_contradictions`).

    Estimate better-sqlite3 migration cost against the schema delta using the two established cost
    shapes in this codebase: (1) additive nullable `ADD COLUMN` is O(1) metadata-only — no row
    rewrite, no backfill (schema.ts ~222-235); (2) a CHECK-constraint change or typed/constrained
    reshape forces full table recreation rewriting all ~11.3k node rows + re-creating every index
    + rebuilding the `node_fts` virtual table (schema.ts ~394-435). State which option lands in
    which bucket: nullable interval/pointer columns are cheap-additive; backfilling
    `prev_value`/`prev_ts` INTO validity intervals or adding a constrained column is the expensive
    full-recreation path. Note the migration must bump `SCHEMA_VERSION` (currently 14) idempotently
    and rely on the existing downgrade guard.

    State which correctness invariants any new model MUST preserve (CLAUDE.md): decay never deletes
    an evidence-backed fact; graph is source of truth (vector is derived cache); single-writer —
    `node.embedding`/node mutation rides inside one IMMEDIATE transaction via `setEmbedding`. Close
    with a written recommendation whose first sentence is the verdict — adopt bi-temporal, adopt
    supersedes-chain, or **DEFER / keep tombstone-always** (defer is an explicitly valid outcome;
    actual adoption is a separate future phase regardless). Justify in terms of correctness-coverage
    gained vs migration cost incurred.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/49-scale-data-model-spike/49-SPIKE-FINDINGS.md','utf8');const sec=/##\s*SCALE-02/.test(s);const opts=/bi-temporal/i.test(s)&&/supersed/i.test(s)&&/tombstone/i.test(s);const cost=/(ADD COLUMN|table recreation|migration cost|SCHEMA_VERSION)/i.test(s);const rec=/(recommend|defer|adopt)/i.test(s);process.exit(sec&&opts&&cost&&rec?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `## SCALE-02` section present, naming all three options (bi-temporal / supersedes-chain / tombstone) — grep-confirmed.
    - Migration cost estimated via the two codebase cost shapes (additive O(1) ADD COLUMN vs full table-recreation) and ties each option to a bucket; references SCHEMA_VERSION bump.
    - The correctness invariants (decay-never-deletes, graph-source-of-truth, single-writer IMMEDIATE) are named as constraints the model must preserve.
    - A written recommendation with the verdict in the first sentence (adopt / defer); defer framed as valid; NO migration code is present (desk analysis only).
  </acceptance_criteria>
  <done>§ SCALE-02 delivers a written bi-temporal/supersedes/tombstone recommendation with a schema-delta migration-cost estimate, no code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| findings doc → committed repo | The written deliverable is committed; it must not leak private brain content or fabricated numbers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04 | Information disclosure | findings doc transcribing benchmark output | mitigate | The doc reports aggregate metrics (latency, recall, counts) only — not raw embedding vectors or private memory content. Raw per-id JSON stays gitignored under results/. |
| T-49-05 | Tampering | inflated/fabricated metrics in a committed doc | mitigate | Every number is traced to the results JSON or marked `unmeasured-here`; acceptance checks forbid invented numbers; honors the no-inflated-metrics project rule. |
| T-49-R | Repudiation | numbers not traceable to a run | accept→mitigate | Frontmatter stamps `sut_commit` + `harness` + `results` path so any reader can re-run. |
</threat_model>

<verification>
- `49-SPIKE-FINDINGS.md` exists with Phase-41-style frontmatter (requirement [SCALE-01, SCALE-02], harness, results).
- § SCALE-01 has a per-rung measured table + a GO/NO-GO/DEFER decision per the D-04 gate.
- § SCALE-02 has the three-option recommendation + schema-delta migration-cost estimate (no code).
- No fabricated metrics — every number traces to the results JSON or is marked unmeasured-here.
</verification>

<success_criteria>
- The single deliverable doc is complete (D-06) and discharges both SCALE-01 (go/no-go) and SCALE-02 (recommendation + migration cost).
- The defer/no-go-is-valid framing is preserved in both sections — neither forces adoption.
- The doc honors the no-inflated-metrics rule and the read-only provenance discipline.
</success_criteria>

<output>
Create `.planning/phases/49-scale-data-model-spike/49-03-SUMMARY.md` when done — note the SCALE-01
verdict (go/no-go/defer) and the SCALE-02 recommendation (adopt/defer) for the roadmap.
</output>
