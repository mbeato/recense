---
phase: 49-scale-data-model-spike
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - scripts/eval/49-crossover-spike.cjs
autonomous: true
requirements: [SCALE-01]
user_setup: []

must_haves:
  truths:
    - "The harness opens the live brain READ-ONLY and never writes ~/.config/recense/recense.db"
    - "The exact brute-force arm uses production cosineSimF32 as byte-identical ground truth"
    - "A real-distribution synthetic corpus is built by jitter+renormalize of live embeddings (NOT random vectors), anchored on the unmodified live brain (D-01)"
    - "The harness runs end-to-end and writes a results JSON with per-scale-rung recall@5/@10, exact+ANN p50/p95, build time, and memory footprint (D-03)"
    - "If the ANN lib fails to load/build, the harness records unmeasured-here and still completes — never fabricated numbers"
  artifacts:
    - path: "scripts/eval/49-crossover-spike.cjs"
      provides: "The crossover bench harness (exact vs HNSW over {11.3k live, 25k, 50k} nodes)"
      min_lines: 200
    - path: "scripts/eval/results/49-crossover-spike.json"
      provides: "Raw per-rung measurements (gitignored, local-only)"
      contains: "scales"
  key_links:
    - from: "scripts/eval/49-crossover-spike.cjs"
      to: "dist/src/retrieval/topk (cosineSimF32)"
      via: "require of compiled module for byte-exact ground truth"
      pattern: "require.*retrieval/topk"
    - from: "scripts/eval/49-crossover-spike.cjs"
      to: "~/.config/recense/recense.db"
      via: "Database open with { readonly: true, fileMustExist: true }"
      pattern: "readonly:\\s*true"
---

<objective>
Build and run the SCALE-01 crossover benchmark: the exact flat-buffer cosine index
(`src/retrieval/topk.ts`) versus an approximate HNSW ANN, over recense's real node
distribution scaled to {~11.3k live, 25k, 50k} nodes, measuring recall@5/@10, query
latency p50/p95, index build time, and memory footprint. This is a fork/extension of the
Phase-41 harness trio, not a greenfield build.

Purpose: Discharge the measurement half of SCALE-01 — produce the raw numbers that the
go/no-go gate (D-04) is decided against. The harness is the load-bearing instrument; Plan 03
only reports its output.

Output: `scripts/eval/49-crossover-spike.cjs` and the results JSON under
`scripts/eval/results/49-crossover-spike.json` (gitignored, local-only, D-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/49-scale-data-model-spike/49-CONTEXT.md
@.planning/phases/49-scale-data-model-spike/49-PATTERNS.md

<interfaces>
<!-- Contracts the harness reuses from compiled production code. Extracted from src/retrieval/topk.ts.
     The harness require()s dist/src/retrieval/topk — these are the live signatures. -->

From src/retrieval/topk.ts (compiled to dist/src/retrieval/topk.js):
```typescript
export function cosineSimF32(a: Float32Array, b: Float32Array): number;  // ground-truth cosine
export function buildVectorIndex(db: Database.Database, indexPath: string): number;
export function vectorIndexPath(dbPath: string): string;
export class CandidateRetriever { /* topk(queryVec, k) -> exact flat-buffer scan */ }
```

Canonical row filter (topk.ts:81 / :295, mirrored in all Phase-41 harnesses):
`SELECT id, embedding FROM node WHERE embedding IS NOT NULL AND tombstoned = 0`

Embedding decode (Pitfall 5 — sliced-Buffer-safe):
`new Float32Array(buf.buffer, buf.byteOffset, buf.byteLength / 4)`

Live DB default: `~/.config/recense/recense.db` ; embeddingDimensions = 1536.
Build before run: harness require()s dist/ — run `npm run build` (tsc) first.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harness scaffold — exact ground-truth arm + real-distribution corpus scale-up</name>
  <files>scripts/eval/49-crossover-spike.cjs</files>
  <read_first>
    - scripts/eval/41-index-spike.cjs (THE fork source — boilerplate lines 44-67, percentile 97-102, decodeEmbedding 109-113, exact flat arm 144-190, mockVector renormalize loop 126-130, results write 444-462, IIFE 326/463)
    - scripts/eval/41-latency-after.cjs (warm-timing loop with warm-up discard, lines 170-182)
    - scripts/eval/41-topk-equivalence.cjs (importing cosineSimF32 from DIST, line 46; set-overlap scoring 137-171)
    - src/retrieval/topk.ts (cosineSimF32 line 200; canonical row filter line 81/295)
    - .planning/phases/49-scale-data-model-spike/49-PATTERNS.md (lines 32-128 — the harness pattern assignments)
  </read_first>
  <action>
    Fork `scripts/eval/41-index-spike.cjs` into `scripts/eval/49-crossover-spike.cjs`. Copy the
    header boilerplate (`'use strict'`, CommonJS requires, `DIST = ../../dist/src`, arg parser,
    `DB_PATH = process.env.RECENSE_DB_PATH || ~/.config/recense/recense.db`, `DIMS = 1536`) and
    adjust the eval id to `49-crossover-spike`. Open the live brain ONLY with
    `{ readonly: true, fileMustExist: true }` — load-bearing safety invariant. Reuse verbatim:
    the `percentile` ceil-based helper, `decodeEmbedding` (byteOffset-safe), and `getCommitHash`.
    Import `cosineSimF32` directly from `require(DIST + '/retrieval/topk')` so the exact arm is
    byte-identical to production ground truth (NOT a re-derivation).

    Build the EXACT brute-force ground-truth arm: load all live `node.embedding` BLOBs using the
    canonical filter `WHERE embedding IS NOT NULL AND tombstoned = 0`, decode into a contiguous
    `Float32Array(n*DIMS)` with a parallel precomputed-norm array, and a `flatTopk(queryVec, k)`
    that returns the exact top-k ids — this is the recall denominator.

    Build the synthetic corpus scale-up (D-01, the spike's novel part — no analog): a function
    that takes the live embedding set and emits jittered+renormalized copies up to a target count.
    For each scale rung {live count (~11.3k), 25k, 50k} (100k optional if cheap): keep the
    unmodified live vectors as the anchor, then top up to the target by sampling a live vector,
    adding small Gaussian jitter, and L2-renormalizing (reuse the renormalize loop from `mockVector`,
    41-index-spike.cjs:126-130). Jitter magnitude is Claude's discretion (D-07) — small enough that
    the distribution stays realistic. Do NOT use random normalized vectors (HNSW recall is
    distribution-sensitive — D-01). Draw the query set from a held-out sample of REAL live
    embeddings (real-distribution queries), not random vectors. Add the warm-timing loop with a
    discarded warm-up pass (41-latency-after.cjs:170-182) and `REPEATS` for stable p50/p95.

    Do NOT inline the ANN arm yet — that is Task 2. Leave a clearly-marked seam.
  </action>
  <verify>
    <automated>npm run build >/dev/null 2>&1 && node -e "const s=require('fs').readFileSync('scripts/eval/49-crossover-spike.cjs','utf8'); const ok = /readonly:\s*true/.test(s) && /retrieval\/topk/.test(s) && /embedding IS NOT NULL AND tombstoned = 0/.test(s) && /byteOffset/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists and `node --check scripts/eval/49-crossover-spike.cjs` parses clean (exit 0).
    - Source contains the read-only open flags `readonly: true` (grep-confirmed by verify).
    - Source require()s `retrieval/topk` and uses the canonical row filter `embedding IS NOT NULL AND tombstoned = 0` (grep-confirmed).
    - Corpus scale-up jitters+renormalizes REAL live embeddings (no `Math.random()`-only vector generation as the corpus source) and anchors on the unmodified live set.
    - No write statement targets the live DB (no INSERT/UPDATE/DELETE/CREATE against the readonly handle).
  </acceptance_criteria>
  <done>The harness scaffold builds the exact ground-truth arm and the real-distribution synthetic corpus at rungs {11.3k, 25k, 50k}, read-only, with the ANN seam stubbed.</done>
</task>

<task type="auto">
  <name>Task 2: ANN HNSW arm + recall@k/build/memory measurement + results JSON, then run</name>
  <files>scripts/eval/49-crossover-spike.cjs</files>
  <read_first>
    - scripts/eval/49-crossover-spike.cjs (the file from Task 1 — fill the ANN seam)
    - scripts/eval/41-index-spike.cjs (trySqliteVec best-effort try/catch + tmpdir-copy pattern, lines 197-248; results JSON shape 444-462)
    - .planning/phases/49-scale-data-model-spike/49-01-SUMMARY.md (the loadability verdict — which lib loaded, or blocked)
    - .planning/phases/49-scale-data-model-spike/49-PATTERNS.md (lines 123-168 — recall@k adaptation, tryAnn, results JSON shape)
    - .planning/phases/41-vector-index-and-hot-path-latency/41-SPIKE-FINDINGS.md (unmeasured-here honesty precedent)
  </read_first>
  <action>
    Implement `tryAnn()` mirroring the `trySqliteVec` best-effort pattern (41-index-spike.cjs:197-248):
    `require('vectorlite')` first (the named primary); on any failure fall back to
    `require('hnswlib-node')`; on both failing return `{ available: false, error }` and let the
    run record the candidate `unmeasured-here` — NEVER fabricate numbers (D-02b). If the loaded lib
    is a sqlite loadable-extension (vectorlite) that must build a table, build it on a TMPDIR COPY
    of the live DB (`fs.copyFileSync` → scratch path, 41-index-spike.cjs:206-209) and clean up
    after — never touch the live file.

    For each scale rung where ANN is available: build the HNSW index over the scaled corpus
    (sweep reasonable HNSW params M / ef_construction / ef_search — Claude's discretion, D-07),
    timing `build_ms`; capture memory footprint via `process.memoryUsage().heapUsed` deltas around
    the build (or the lib's own reported size). Run the same query set through both arms at k∈{5,10}.
    Compute `recall@k = |ann_topk ∩ exact_topk| / k` against the exact arm as ground truth (adapt
    the set-overlap from 41-topk-equivalence.cjs:137-171; the TIE_EPS machinery is NOT needed —
    overlap fraction is the metric). Measure ANN query p50/p95 with the same warm-timing loop as
    the exact arm.

    Write the results JSON to `scripts/eval/results/49-crossover-spike.json` (mkdir recursive +
    `JSON.stringify(out,null,2)+'\n'`): `meta` { eval:'49-crossover-spike', date, commit, k, repeats,
    dims, embed_model }, a `scales` array (per rung: `{ n, exact:{p50_ms,p95_ms}, ann:{p50_ms,p95_ms,
    recall_at_5,recall_at_10,build_ms,mem_bytes,hnsw_params} }`), and `ann_available` ({available,error}
    or true+params). Include the Phase-40/41 warm felt-path reference (≈45/46 ms over ~11.3k nodes)
    in meta so Plan 03 can apply the D-04 gate. Wrap main in the top-level async IIFE with the
    `.catch(e => { console.error(...); process.exit(1) })` trap. Then RUN it (`npm run build` first
    so dist/ is fresh, then `node scripts/eval/49-crossover-spike.cjs`) and confirm the JSON lands.
  </action>
  <verify>
    <automated>npm run build >/dev/null 2>&1 && node scripts/eval/49-crossover-spike.cjs && node -e "const j=require('./scripts/eval/results/49-crossover-spike.json'); const ok=Array.isArray(j.scales)&&j.scales.length>=3&&'ann_available' in j; process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Harness runs to exit 0 and writes `scripts/eval/results/49-crossover-spike.json`.
    - JSON `scales` has >=3 rungs (11.3k, 25k, 50k); each rung has exact p50/p95.
    - Each rung either has ANN recall_at_5/recall_at_10 + build_ms + mem_bytes (lib loaded) OR `ann_available.available === false` with a non-empty error string (lib blocked — unmeasured-here, no invented numbers anywhere in the file).
    - Any vectorlite table build used a tmpdir copy; live DB opened readonly throughout (no live-DB mtime change across the run).
  </acceptance_criteria>
  <done>The crossover benchmark runs end-to-end read-only and emits per-rung exact-vs-ANN numbers (or an honest blocked record) for Plan 03 to report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench harness → live recense.db | The founder's private brain DB is read by the harness |
| bench harness → ANN lib table build | An ANN candidate may need to materialize its own index/table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | accidental write to live `~/.config/recense/recense.db` | mitigate | Every live-DB open uses `{ readonly: true, fileMustExist: true }`; any ANN that must build a table operates on a `fs.copyFileSync` tmpdir COPY and cleans up — the live file is never opened writable. Verified by no-mtime-change acceptance check. |
| T-49-02 | Information disclosure | results JSON derived from private embedding data | mitigate | Output lands under `scripts/eval/results/` which is gitignored (line 32) — local-only, never committed. JSON stores aggregate metrics + ids, not raw embedding vectors. |
| T-49-03 | Repudiation | numbers can't be traced to a commit | mitigate | `getCommitHash` stamps `meta.commit`; results path + harness path recorded in Plan 03 frontmatter. |
| T-49-DoS | Denial of Service | 50k-node corpus build memory pressure | accept | Bench is a one-shot local run on the founder's machine; rung count and 100k-optional are Claude's discretion to keep it cheap. |
</threat_model>

<verification>
- `node --check scripts/eval/49-crossover-spike.cjs` parses; harness runs to exit 0.
- `scripts/eval/results/49-crossover-spike.json` exists with >=3 scale rungs.
- Live DB mtime unchanged before/after the run (read-only invariant held).
- ANN arm reports real recall/build/mem OR an honest `unmeasured-here` blocked record — no fabricated numbers.
</verification>

<success_criteria>
- A runnable crossover measurement exists and has produced raw per-rung numbers (SCALE-01 measurement half).
- recall@5/@10 vs exact ground truth, p50/p95, build time, memory captured at {11.3k, 25k, 50k} (D-03).
- The "defer/no-go is valid" framing is preserved — the harness does not assume ANN wins; it records whatever the numbers (or load-failure) say.
</success_criteria>

<output>
Create `.planning/phases/49-scale-data-model-spike/49-02-SUMMARY.md` when done — note whether the
ANN lib loaded and the headline per-rung exact vs ANN numbers (or the blocked verdict).
</output>
