---
phase: 49-scale-data-model-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements: [SCALE-01]
user_setup: []

must_haves:
  truths:
    - "The HNSW ANN library is verified legitimate on its public registry before any install"
    - "The ANN library's loadability on THIS machine is recorded honestly — installed + requires/builds cleanly, OR recorded as blocked (unmeasured-here) (D-02b)"
    - "The ANN library lives under devDependencies only; the production dependency tree is untouched (D-02)"
  artifacts:
    - path: "package.json"
      provides: "ANN HNSW lib pinned under devDependencies (not dependencies)"
      contains: "hnswlib-node"
  key_links:
    - from: "package.json devDependencies"
      to: "hnswlib-node (and/or vectorlite)"
      via: "devDependency isolation — never added to dependencies"
      pattern: "devDependencies"
---

<objective>
Provision the approximate-nearest-neighbor (HNSW) library that SCALE-01's crossover
benchmark needs, as a bench-only devDependency, and verify EARLY whether it actually
loads on this machine (D-02b). Phase 41 found `sqlite-vec` unloadable here; this plan
de-risks SCALE-01 by confirming loadability up front rather than discovering it mid-benchmark.

Purpose: Honor D-02 ("ANN enters as a devDependency loaded ONLY in the bench harness;
production dependency tree stays untouched") and D-02b ("verify ANN-lib loadability EARLY;
a non-loadable lib is a recordable `unmeasured-here` result, never fabricated numbers").

Output: The HNSW lib pinned under `devDependencies` in package.json/package-lock.json, plus
a recorded loadability verdict (loads OR blocked) that Plan 02's harness will branch on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-scale-data-model-spike/49-CONTEXT.md
@.planning/phases/49-scale-data-model-spike/49-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for the ANN HNSW devDependency</name>
  <read_first>
    - .planning/phases/49-scale-data-model-spike/49-CONTEXT.md (D-02, D-02b — devDep-only, loadability-early)
    - .planning/phases/49-scale-data-model-spike/49-PATTERNS.md (lines 130-147 — tryAnn pattern, "neither vectorlite, hnswlib-node, nor sqlite-vec installed")
    - package.json (confirm current deps = better-sqlite3/openai/zod/etc; devDeps = tsx/typescript/vitest only)
  </read_first>
  <what-built>
    No code yet — this is the supply-chain gate that MUST clear before any package-manager
    install. The phase introduces ONE new third-party package (an HNSW implementation). No
    RESEARCH.md Package Legitimacy Audit table exists for this phase, so per the package
    legitimacy gate fallback both candidates are treated as [ASSUMED] and require human
    verification before install.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/hnswlib-node — confirm it is a real, maintained
       pure-node-addon HNSW binding (weekly downloads, repo link, recent publish), not a
       typosquat or [SLOP] package.
    2. Open https://www.npmjs.com/package/vectorlite — confirm legitimacy of the sqlite-extension
       HNSW option named in SCALE-01 (this is the requirement's named primary; it may not load
       here because it is a sqlite loadable extension like the unloadable sqlite-vec).
    3. Confirm both will be installed under `devDependencies` ONLY (D-02 — production tree untouched).
  </how-to-verify>
  <acceptance_criteria>
    - Both package pages reviewed by a human; neither is a typosquat/[SLOP] package.
    - Human approves installing the verified lib(s) as devDependencies only.
    - This checkpoint is NOT auto-approvable (workflow.auto_advance ignored — supply-chain gate).
  </acceptance_criteria>
  <resume-signal>Type "approved" to install the verified ANN lib(s), or name a different verified HNSW package.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the approved HNSW lib as a devDependency and record loadability</name>
  <files>
    - package.json
    - package-lock.json
  </files>
  <read_first>
    - package.json (the file being modified — add to devDependencies, never dependencies)
    - .planning/phases/49-scale-data-model-spike/49-PATTERNS.md (lines 130-147 — load-error → unmeasured-here discipline)
    - .planning/phases/41-vector-index-and-hot-path-latency/41-SPIKE-FINDINGS.md (lines 18-19, 48 — how Phase 41 recorded sqlite-vec as "unavailable, module not installed" rather than fabricating)
  </read_first>
  <action>
    Install `hnswlib-node` (pure node-addon HNSW — the D-02b fallback most likely to actually
    load and produce real numbers) as a devDependency via the project package manager with a
    save-dev flag, NOT as a production dependency. Optionally also install `vectorlite` (the
    requirement's named primary sqlite-extension HNSW) as a devDependency so the harness can
    attempt it first. Then run a loadability smoke-test: `require()` the installed lib in a
    throwaway node one-liner, instantiate a tiny HNSW index (a few unit vectors at the live
    `embeddingDimensions` = 1536) and add+query one point. Record the verdict — loads-and-builds
    OK, or the load/build error string — for Plan 02's harness to branch on (the tryAnn
    `{ available, error }` shape from PATTERNS lines 136-142). If the native build (node-gyp)
    fails on this machine, that is a recordable `unmeasured-here` result per D-02b — do NOT
    fabricate; leave the harness to report the candidate blocked. Confirm the lib appears under
    `devDependencies` and NOT under `dependencies` in package.json.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const ok=(p.devDependencies&&p.devDependencies['hnswlib-node'])&&!(p.dependencies&&p.dependencies['hnswlib-node']); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `hnswlib-node` present in package.json `devDependencies` and ABSENT from `dependencies` (exit 0 above).
    - Production `dependencies` list unchanged from pre-plan (better-sqlite3/openai/zod/etc — diff shows only devDependencies added).
    - A loadability verdict is recorded in the SUMMARY: either "HNSW loads + builds a tiny index on this machine" OR the exact node-gyp/require error string marked `unmeasured-here` (never an invented success).
  </acceptance_criteria>
  <done>The HNSW devDependency is installed (or recorded blocked), isolated from the production tree, and its load verdict is captured for Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → npm registry | A new third-party package crosses into the project's dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-SC | Tampering | new HNSW devDependency (hnswlib-node / vectorlite) install | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifies the package on npmjs.com before install; installed as devDependency ONLY so it never enters any production code path or shipped artifact. |
| T-49-EoP | Elevation of Privilege | native node-gyp build of hnswlib-node | accept | Native addon builds locally on the founder's own machine for a bench-only dep; no privileged install step beyond standard `npm`. Low-value, single-user, offline. |
</threat_model>

<verification>
- package.json/package-lock.json show the HNSW lib under devDependencies only.
- Production `dependencies` are byte-identical to pre-plan (net-zero new runtime deps — engine invariant).
- A loadability verdict (loads OR blocked-with-error-string) is recorded for Plan 02.
</verification>

<success_criteria>
- ANN HNSW lib provisioned as a bench-only devDependency (D-02 satisfied).
- Loadability verified EARLY on this machine, honestly recorded (D-02b satisfied).
- No production dependency added.
</success_criteria>

<output>
Create `.planning/phases/49-scale-data-model-spike/49-01-SUMMARY.md` when done — include the
loadability verdict (loads / blocked-with-error) so Plan 02 can branch correctly.
</output>
