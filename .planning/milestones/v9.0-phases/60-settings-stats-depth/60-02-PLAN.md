---
phase: 60-settings-stats-depth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/viz/modules/constants.js
  - src/viz/modules/charts.js
  - tests/viz-charts-geometry.test.ts
autonomous: true
requirements: [D-05, D-06, D-07, D-08, D-11]
must_haves:
  truths:
    - "COST_EVENTS and a NEUTRAL_SERIES_RAMP (4-step aubergine, no amber) exist as exported constants beside KIND_COLOR"
    - "charts.js exports pure geometry helpers (scale, nice-tick, nearest-point index) and SVG builders (line, bar, axis, legend) that emit createElementNS nodes with textContent-only labels"
    - "the SVG builders reuse KIND_COLOR/TYPE_COLOR for activity-kind series and NEUTRAL_SERIES_RAMP for non-activity series; no amber-family hex appears anywhere in charts.js or the new constants"
  artifacts:
    - path: "src/viz/modules/constants.js"
      provides: "COST_EVENTS array + NEUTRAL_SERIES_RAMP + chart-card flat token"
      contains: "COST_EVENTS"
    - path: "src/viz/modules/charts.js"
      provides: "hand-rolled inline-SVG chart helpers (line/bar/axis/legend/hover)"
      contains: "createElementNS"
    - path: "tests/viz-charts-geometry.test.ts"
      provides: "Node-side unit tests for the pure geometry helpers + amber-ban assertion"
      contains: "nearest"
  key_links:
    - from: "src/viz/modules/charts.js"
      to: "src/viz/modules/constants.js"
      via: "import KIND_COLOR / TYPE_COLOR / NEUTRAL_SERIES_RAMP"
      pattern: "from './constants.js'"
---

<objective>
Build the two net-new foundations the dashboard renders on: the shared constants
(COST_EVENTS markers + the non-activity neutral color ramp) and a hand-rolled
inline-SVG chart helper module (bars, lines, axes, legend, nearest-point hover) —
zero chart library, zero new runtime dependency (D-05). No in-repo SVG-chart
precedent exists, so this module is specified from scratch here.

Purpose: Give the dashboard plans reusable, testable chart primitives and a
locked color vocabulary so they only wire data, not invent rendering.
Output: constants.js additions, new charts.js, a geometry unit-test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-settings-stats-depth/60-CONTEXT.md
@.planning/phases/60-settings-stats-depth/60-PATTERNS.md
@.planning/phases/60-settings-stats-depth/60-UI-SPEC.md

<interfaces>
<!-- Existing constants the helpers reuse. Extracted from src/viz/modules/constants.js. -->
TYPE_COLOR (constants.js:84-88): entity 0x9c7080, fact 0x6d7890, schema 0x82698c
KIND_COLOR (constants.js:149-207): new_node 0x8fbf9e, reconsolidation 0xd9a0bd,
  oscillation 0xe89c9c, neutral 0xaab3c4, recall_hop 0x66d9ff, replay 0xb3ecf5,
  spontaneous 0xc9b8ff  (recall_seed 0xffb866 = AMBER — never used in charts)
HUD_CSS_TOKENS (constants.js:673-801): flat {key: cssValue} object;
  'surface-index-panel':'rgba(23, 15, 29, 0.97)' already present.
emitHudTokens() (css-tokens.js:20): builds :root{--key:value} from HUD_CSS_TOKENS.

Closest DOM-builder analog (reader.js:351-487 startGenProgress): createElement +
  style.width fill + textContent labels, returns an {stop,done,update} API object.
No createElementNS usage exists anywhere in src/viz/modules — this is net-new.

UI-SPEC contract: line 1.5px stroke no fill; bar 0.75-alpha fill + 1px stroke;
  Y-axis min 0, ~4-5 nice ticks; X tick "MMM D"; horizontal gridlines only;
  legend 8x8 swatch + 13px label; nearest-point = crosshair + 4px dot + tooltip
  "{MMM D} — {value}"; hover brightens opacity/stroke only, zero glow/box-shadow.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: constants.js — COST_EVENTS, NEUTRAL_SERIES_RAMP, chart-card token</name>
  <files>src/viz/modules/constants.js</files>
  <read_first>
    - src/viz/modules/constants.js (84-100 TYPE_COLOR/BG_COLOR; 149-207 KIND_COLOR; 663-801 HUD_CSS_TOKENS) — match the exported-const-with-JSDoc-@type and per-entry rationale-comment convention exactly
    - .planning/phases/60-settings-stats-depth/60-CONTEXT.md (D-08/D-11) and 60-UI-SPEC.md ("Usage tab neutral ramp" table + "Cost-event markers") — the exact ramp hexes and marker dates/labels
    - .planning/phases/57-viz-activity-palette-redesign/57-CONTEXT.md is referenced by CONTEXT for amber-exclusivity rationale (D-03(b)) — the ramp must not resemble accent-amber
  </read_first>
  <action>
    Add three exports beside KIND_COLOR following the file's section-banner +
    JSDoc convention. (1) `COST_EVENTS`: an array of `{date, label}` with the two
    known markers — `{date:'2026-07-03', label:'MAX_THINKING_TOKENS=0'}` and the
    Phase-42 consolSkipThreshold marker (resolve its exact date from git/CONTEXT;
    if unresolvable, use the honest month label the UI-SPEC allows) — with a leading
    comment stating new events are a one-line addition here (D-11). (2)
    `NEUTRAL_SERIES_RAMP`: a 4-entry array light→dark using the UI-SPEC hexes
    `#a99db3`, `#8b7090`, `#6d5a72`, `#574560` (first two are existing
    --text-body-mauve / --text-mauve-rest values; last two are the new darker steps
    in the same aubergine family), with a comment that it is the non-activity
    (tokens/models/features) series pool and must never resemble accent-amber (D-08).
    (3) Add a `'chart-card':'rgba(26, 18, 32, 0.4)'` entry to HUD_CSS_TOKENS so the
    dashboard card background is a CSS custom property like every other surface.
    Do not introduce any 0xffb866 / #ffb866 / accent-amber value.
  </action>
  <verify>
    <automated>node -e "import('./src/viz/modules/constants.js').then(m=>{if(!Array.isArray(m.COST_EVENTS)||m.NEUTRAL_SERIES_RAMP.length!==4)process.exit(1);const s=JSON.stringify([m.COST_EVENTS,m.NEUTRAL_SERIES_RAMP]);if(/ffb866/i.test(s))process.exit(2);console.log('ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - The node verify command prints `ok` (exports present, ramp length 4, no ffb866)
    - `grep -c "chart-card" src/viz/modules/constants.js` >= 1
    - `grep -niE "ffb866|accent-amber" src/viz/modules/constants.js` shows no match inside the new COST_EVENTS/NEUTRAL_SERIES_RAMP/chart-card additions (pre-existing amber tokens elsewhere untouched)
    - COST_EVENTS entries are `{date,label}` objects; NEUTRAL_SERIES_RAMP is `#a99db3,#8b7090,#6d5a72,#574560` in order
  </acceptance_criteria>
  <done>COST_EVENTS + NEUTRAL_SERIES_RAMP + chart-card token exported, amber-free, matching file conventions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: charts.js — hand-rolled inline-SVG chart helpers</name>
  <files>src/viz/modules/charts.js, tests/viz-charts-geometry.test.ts</files>
  <read_first>
    - src/viz/modules/reader.js (351-487 startGenProgress) — the only DOM-builder analog: build-tree, textContent labels, return an API object with update()
    - src/viz/modules/constants.js (post Task-1: KIND_COLOR, TYPE_COLOR, NEUTRAL_SERIES_RAMP) — the color sources the builders import
    - .planning/phases/60-settings-stats-depth/60-UI-SPEC.md ("Chart Construction Contract" table) — stroke widths, tick rules, gridline color token, legend size, hover spec, number formatting (fmtTokens reuse)
    - tests/viz-charts-geometry.test.ts does not exist yet — model the harness on any pure-function vitest file (e.g. a `describe`/`it`/`expect` importing the module)
  </read_first>
  <behavior>
    - niceTicks(min,max,count): returns ascending tick values starting at 0 (Y min always 0), ~4-5 nice steps covering max
    - linearScale(domain,range)(v): maps a value into pixel range; identity endpoints correct
    - nearestPointIndex(points, xPixel): returns the index of the data point whose x is closest to a hovered pixel (the D-07 hover core) — testable with no DOM
    - line(data, opts) / bar(data, opts) / axis(opts) / legend(series): return an SVGElement built via createElementNS, with any embedded <text> label set through textContent (never innerHTML)
    - a series color resolves from the caller-passed color (KIND_COLOR/TYPE_COLOR for activity kinds, NEUTRAL_SERIES_RAMP for non-activity); charts.js never hard-codes amber
  </behavior>
  <action>
    Create src/viz/modules/charts.js exporting the pure helpers (niceTicks,
    linearScale, nearestPointIndex, and a small MMM-D date formatter) and the SVG
    builders (line, bar, axis, legend, plus a hover attach helper that draws the
    1px crosshair + 4px dot and updates the existing `#tooltip` element with the
    "{MMM D} — {value}" string). Build every node with
    document.createElementNS('http://www.w3.org/2000/svg', ...) and setAttribute for
    numeric geometry; set any <text> content via `.textContent` only (T-44-19
    discipline — even numeric labels). Line = 1.5px stroke, no area fill; bar =
    0.75-alpha fill + 1px full-alpha stroke; horizontal gridlines only using the
    existing --section-divider-faint token; hover brightens opacity/stroke width
    only, no box-shadow/filter glow. Reuse settings.js's fmtTokens formatting rule
    for token counts (import or re-implement the same 1.2M/34.5k/plain thresholds).
    Keep the pure geometry helpers free of any `document` reference so they unit-test
    in Node. Create tests/viz-charts-geometry.test.ts asserting niceTicks starts at
    0 with sane steps, linearScale endpoints, nearestPointIndex picks the closest x,
    and a grep-style assertion that the charts.js source contains no `innerHTML` with
    interpolation and no `ffb866`/amber literal.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-charts-geometry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/viz-charts-geometry.test.ts` passes
    - niceTicks always returns a first tick of 0; nearestPointIndex returns the closest index in the unit tests
    - `grep -c "createElementNS" src/viz/modules/charts.js` >= 1
    - `grep -nE "innerHTML|ffb866" src/viz/modules/charts.js` returns no match
    - Pure helpers importable in Node without a DOM (the test file runs without jsdom for the geometry cases)
  </acceptance_criteria>
  <done>charts.js provides tested SVG line/bar/axis/legend/hover helpers built via createElementNS, textContent-only, amber-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server data → SVG DOM | numbers/dates/labels from /stats/* become chart geometry + text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04 | Tampering (XSS) | chart <text> / tooltip labels | mitigate | all label text set via .textContent; SVG nodes via createElementNS + setAttribute; no innerHTML-with-data anywhere in charts.js (grep-asserted) |
| T-60-06 | Integrity (palette) | series color selection | mitigate | colors sourced from KIND_COLOR/TYPE_COLOR/NEUTRAL_SERIES_RAMP only; amber-ban grep-asserted in both files |
| T-60-SC | Tampering | package installs | accept | zero new dependencies (hand-rolled SVG, no chart lib) — no install task |
</threat_model>

<verification>
- `npx vitest run tests/viz-charts-geometry.test.ts` green
- `node -e` constants import check prints ok
- `grep -nriE "ffb866|accent-amber" src/viz/modules/charts.js` empty
</verification>

<success_criteria>
Dashboard plans can import COST_EVENTS, NEUTRAL_SERIES_RAMP, and the charts.js
helpers and render every Phase-60 chart with no chart library, no new dependency,
no amber, and textContent-only labels.
</success_criteria>

<output>
Create `.planning/phases/60-settings-stats-depth/60-02-SUMMARY.md` when done
</output>
