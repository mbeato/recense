---
phase: 60-settings-stats-depth
plan: 04
type: execute
wave: 3
depends_on: ["60-03"]
files_modified:
  - src/viz/modules/stats-dashboard.js
autonomous: true
requirements: [D-03, D-06, D-07, D-08, D-13, D-14, D-15]
must_haves:
  truths:
    - "The Brain Health tab fetches /stats/brain-health and renders all six metric groups: node growth over time, kind mix, reconsolidations/day, tombstones/day, judge activity (fires + escalation rate), episodes pending vs consolidated, and a last-sleep-pass status tile"
    - "Every Brain-Health chart carries a Y-axis (min 0, ~4-5 nice ticks) + X-axis (MMM-D ticks on time-series) and a top-right legend per the D-06 full-polish contract; multi-series charts (kind-mix, judge dual-line) legend every series"
    - "Activity-kind series use their Phase-57 identity hue (reconsolidation magenta, new-node sage, tombstone coral, judge cyan, etc.); no amber appears"
    - "Node-growth carries the D-13 approximation disclosure caption"
    - "Last-sleep-pass tile renders the derived timestamp/duration and an honest status (never a fabricated success)"
    - "Empty brain (no consolidation_event) shows the Brain-Health empty state, not a broken chart"
  artifacts:
    - path: "src/viz/modules/stats-dashboard.js"
      provides: "Brain Health tab render path fetching /stats/brain-health"
      contains: "/stats/brain-health"
  key_links:
    - from: "src/viz/modules/stats-dashboard.js Brain-Health render"
      to: "/stats/brain-health"
      via: "fetch on first Brain-Health tab activation + refresh"
      pattern: "fetch\\('/stats/brain-health"
    - from: "Brain-Health series colors"
      to: "KIND_COLOR / TYPE_COLOR"
      via: "identity-hue import"
      pattern: "KIND_COLOR|TYPE_COLOR"
---

<objective>
Complete the second dashboard: the Brain Health tab. It fetches
`/stats/brain-health` (built in 60-01) and renders all six metric groups plus the
derived last-sleep-pass tile, using the charts.js helpers (60-02) and the
Phase-57 identity hues (D-08). This is the last data-rendering plan; after it the
phase is functionally complete pending navigation wiring (60-05).

Purpose: The brain-health dashboard — node growth, kind mix, recon/tombstones,
judge activity, episode backlog, and honest last-sleep-pass status.
Output: the Brain-Health render path added to stats-dashboard.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-settings-stats-depth/60-CONTEXT.md
@.planning/phases/60-settings-stats-depth/60-PATTERNS.md
@.planning/phases/60-settings-stats-depth/60-UI-SPEC.md

<interfaces>
<!-- The /stats/brain-health contract (60-01) this tab consumes. -->
Response: { node_growth:{points:[{date,count}],approximate:true},
  kind_mix:[{type,count}], reconsolidations_per_day:[{date,count}],
  tombstones_per_day:[{date,count}], judge_activity:{fires, escalation_rate},
  episodes:{pending, consolidated}, last_sleep_pass:{ts, duration_ms, status} }
  status ∈ {'unknown','none'} — never a fabricated success value.

<!-- Identity hues (constants.js KIND_COLOR/TYPE_COLOR) — UI-SPEC Brain-Health table -->
node growth line   → KIND_COLOR.new_node   0x8fbf9e (sage)
kind fact          → TYPE_COLOR.fact       0x6d7890 (slate)
kind entity        → TYPE_COLOR.entity     0x9c7080 (dusty rose)
kind schema        → TYPE_COLOR.schema     0x82698c (muted mauve)
kind doc           → KIND_COLOR.replay     0xb3ecf5 (ice-cyan, repurposed)
kind insight       → KIND_COLOR.spontaneous 0xc9b8ff (pastel lavender, repurposed)
reconsolidations   → KIND_COLOR.reconsolidation 0xd9a0bd (rose-mauve)
tombstones         → KIND_COLOR.oscillation 0xe89c9c (coral)
judge fires        → KIND_COLOR.recall_hop 0x66d9ff (cyan)
judge escalation % → KIND_COLOR.neutral    0xaab3c4 (slate, dashed secondary line)
episodes pending   → KIND_COLOR.neutral ; consolidated → KIND_COLOR.new_node
last-pass healthy  → --text-stat ; stale/none → --error-text
NO amber (0xffb866) anywhere.

<!-- charts.js (60-02): line/bar/axis/legend + hover helper + fmt. axis() and legend()
     are mandatory on every chart per the UI-SPEC Chart Construction Contract.
     stats-dashboard.js (60-03): shell with currentTab, load()/refresh, #stats-health-tab
     container + placeholder to replace, non-fatal fetch pattern, empty/error copy.
     The Usage tab (60-03 Task 3) already renders 3 axis+legend charts in this file —
     the grep-count criteria below are cumulative on top of those. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brain-Health fetch + node growth / kind mix / recon+tombstone charts</name>
  <files>src/viz/modules/stats-dashboard.js</files>
  <read_first>
    - src/viz/modules/stats-dashboard.js (60-03 shell: currentTab switching, load()/refresh, non-fatal fetch helper, #stats-health-tab placeholder, empty/error rendering, and the Usage-tab axis()/legend() call sites to mirror)
    - src/viz/modules/charts.js (line/bar/axis/legend/hover) — render API; axis() draws Y (min 0, nice ticks) + X (MMM-D), legend() draws the top-right swatch+label row
    - src/viz/modules/constants.js (KIND_COLOR, TYPE_COLOR) — identity hues
    - .planning/phases/60-settings-stats-depth/60-UI-SPEC.md (Chart Construction Contract — Axes + Legend rows; Chart Series Color Contract Brain-Health table; node-growth approximation caption copy; Empty-state copy) — exact axis/legend spec, hues + verbatim disclosure/empty text
    - .planning/phases/60-settings-stats-depth/60-CONTEXT.md (D-06/D-13/D-14)
  </read_first>
  <action>
    Add a Brain-Health fetch to the module: a non-fatal `fetch('/stats/brain-health')`
    (same try/catch-return-null helper as Usage) invoked when the Brain-Health tab is
    first activated and on refresh, sharing the same `as of` timestamp update. Replace
    the 60-03 placeholder in #stats-health-tab with rendered cards: (1) node-growth
    line chart via charts.js line() in KIND_COLOR.new_node with a caption verbatim
    `Derived from event history — approximate where older events were pruned.` when
    response.node_growth.approximate is true (D-13); declare node-growth (or kind-mix)
    the tab's focal anchor per the UI-checker flag — make it the top/largest card.
    (2) kind-mix grouped bar chart mapping fact/entity/schema/doc/insight to their
    TYPE_COLOR/KIND_COLOR hues from the interfaces table. (3) reconsolidations/day and
    tombstones/day as line/bar charts in KIND_COLOR.reconsolidation and
    KIND_COLOR.oscillation respectively. EVERY one of these four charts (node-growth,
    kind-mix, reconsolidations/day, tombstones/day) MUST call charts.js `axis(...)`
    for both a Y-axis (auto-scaled, minimum always 0, ~4-5 nice ticks) and an X-axis
    (MMM-D tick labels on the time-series; category labels for the kind-mix bars) and
    MUST call charts.js `legend(...)` for the top-right swatch+label row — the
    5-series kind-mix chart legends ALL five kinds (fact/entity/schema/doc/insight),
    no chart ships axis-less or legend-less. Wire nearest-point hover (D-07) on the
    time-series. Render the Brain-Health empty state (heading `No brain activity yet`,
    body `Metrics appear after the first sleep pass consolidates episodes into the
    brain.`) when the response is empty/zeroed, and the error state
    `could not load brain-health stats` when the fetch returned null. All values via
    .textContent; SVG via charts.js. No amber.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-frontend-static.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fetch('/stats/brain-health" src/viz/modules/stats-dashboard.js` matches
    - `grep -n "Derived from event history — approximate where older events were pruned." src/viz/modules/stats-dashboard.js` matches verbatim
    - `grep -nE "No brain activity yet|could not load brain-health stats" src/viz/modules/stats-dashboard.js` matches both
    - `grep -nE "KIND_COLOR|TYPE_COLOR" src/viz/modules/stats-dashboard.js` matches (identity hues used)
    - `grep -c "axis(" src/viz/modules/stats-dashboard.js` returns ≥7 (3 Usage charts from 60-03 + the 4 Brain-Health charts here, each Y+X axis)
    - `grep -c "legend(" src/viz/modules/stats-dashboard.js` returns ≥7 (top-right legend on every chart card incl. the 5-series kind-mix)
    - `grep -niE "ffb866|amber" src/viz/modules/stats-dashboard.js` returns no match
    - viz-frontend-static passes; tsc clean
  </acceptance_criteria>
  <done>Brain-Health tab fetches its route and renders node growth (with approximation caption + focal), kind mix, and recon/tombstone charts — each with axis + legend — in identity hues with empty/error states.</done>
</task>

<task type="auto">
  <name>Task 2: Judge activity, episode backlog, last-sleep-pass tile</name>
  <files>src/viz/modules/stats-dashboard.js</files>
  <read_first>
    - src/viz/modules/stats-dashboard.js (Task-1 Brain-Health render path incl. its axis()/legend() call sites)
    - src/viz/modules/charts.js (line/bar/axis/legend) and constants.js (KIND_COLOR)
    - .planning/phases/60-settings-stats-depth/60-UI-SPEC.md (Chart Construction Contract — Axes + Legend rows; Judge activity dual-line spec: fires solid cyan + escalation dashed slate; Episodes pending/consolidated colors; Last sleep-pass status colors + Copywriting rows for the status/duration strings)
    - .planning/phases/60-settings-stats-depth/60-PATTERNS.md ("No Analog Found" last-sleep-pass row) — status is honest 'unknown'/'none', never fabricated
  </read_first>
  <action>
    Complete the Brain-Health tab: (1) judge-activity chart showing fires (count,
    solid KIND_COLOR.recall_hop cyan) with escalation rate rendered as a dashed
    secondary line in KIND_COLOR.neutral on the same chart, with a `legend(...)`
    distinguishing the two series (fires vs escalation) and an `axis(...)` drawing
    Y (min 0) + X (MMM-D) (D-14 escalation = Sonnet share from the ledger model
    column). (2) episodes pending-vs-consolidated as a small paired bar
    (pending KIND_COLOR.neutral, consolidated KIND_COLOR.new_node) that also calls
    `axis(...)` (Y min 0, category X) and `legend(...)` for the two bars. (3) a
    last-sleep-pass readout tile (NOT a chart, so no axis/legend) rendering, from
    response.last_sleep_pass, `last sleep pass: {relative time} ({duration})` when ts
    is present, or `no sleep pass has run yet` when status is 'none'; color the tile
    with --text-stat when recent and --error-text when stale/none; surface the derived
    status honestly (the server sends 'unknown' — render it as an approximation, never
    as a fabricated success). Compute relative time and duration from the response
    ts/duration_ms locally, formatted for reading. All server values via .textContent;
    no amber.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-frontend-static.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "last sleep pass:|no sleep pass has run yet" src/viz/modules/stats-dashboard.js` matches both copy strings verbatim
    - `grep -n "escalation" src/viz/modules/stats-dashboard.js` matches (judge escalation rendered)
    - `grep -c "axis(" src/viz/modules/stats-dashboard.js` returns ≥9 (the 7 prior charts + judge-activity + episodes; the last-sleep-pass tile is a non-chart readout)
    - `grep -c "legend(" src/viz/modules/stats-dashboard.js` returns ≥9 (judge dual-series + episodes paired-bar legends added)
    - The last-sleep-pass render path reads `status`/`ts`/`duration_ms` and has no hard-coded 'ok'/'success' status literal (grep shows none)
    - `grep -niE "ffb866|amber" src/viz/modules/stats-dashboard.js` returns no match
    - viz-frontend-static passes; tsc clean
  </acceptance_criteria>
  <done>Brain-Health tab is complete: judge activity (fires + escalation, with axis+legend), episode backlog (with axis+legend), and an honest last-sleep-pass tile — all six D-14 metric groups rendered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /stats/brain-health JSON → DOM/SVG | server counts/dates/status become chart text + tiles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04 | Tampering (XSS) | metric labels, status tile, tooltips | mitigate | all server-sourced values via .textContent; SVG via charts.js createElementNS; grep-asserted no innerHTML-with-data |
| T-60-04b | Integrity (honesty) | last-sleep-pass status | mitigate | render server 'unknown'/'none' as-is; no client-side fabricated success; node-growth approximation disclosed (no-inflated-metrics) |
| T-60-06 | Integrity (palette) | Brain-Health series colors | mitigate | identity hues from KIND_COLOR/TYPE_COLOR only; amber-ban grep-asserted |
| T-60-08 | Availability | empty brain / fetch fail | mitigate | empty-state + error copy, never a broken chart |
| T-60-SC | Tampering | package installs | accept | zero new deps — no install task |
</threat_model>

<verification>
- `npx vitest run tests/viz-frontend-static.test.ts` green
- `npx tsc --noEmit` clean
- `grep -c "fetch('/stats/brain-health" src/viz/modules/stats-dashboard.js` == 1
- `grep -c "axis(" src/viz/modules/stats-dashboard.js` ≥9 (every Brain-Health chart carries axes)
</verification>

<success_criteria>
The Brain Health tab renders all six metric groups plus an honest last-sleep-pass
tile from real event-log/ledger data, in Phase-57 identity hues, with axes +
legends on every chart (D-06), the D-13 approximation disclosure, and empty/error
states — amber-free, textContent-only.
</success_criteria>

<output>
Create `.planning/phases/60-settings-stats-depth/60-04-SUMMARY.md` when done
</output>
</content>
</invoke>
