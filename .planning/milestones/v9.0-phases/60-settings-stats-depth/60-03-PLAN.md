---
phase: 60-settings-stats-depth
plan: 03
type: execute
wave: 2
depends_on: ["60-01", "60-02"]
files_modified:
  - src/viz/index.html
  - src/viz/css/styles.css
  - src/viz/modules/stats-dashboard.js
  - src/viz/modules/app.js
autonomous: true
requirements: [D-01, D-02, D-03, D-06, D-07, D-08, D-09, D-10, D-12, D-15]
must_haves:
  truths:
    - "Opening the stats view replaces the 3D brain full-window (like corpus) with a near-opaque flat surface holding a Usage | Brain Health tab bar, a range switcher, a refresh button with an 'as of' timestamp, and a close control"
    - "The Usage tab renders daily token burn with dated cost-event markers + before/after deltas, per-feature and per-model splits, and a retail-$ headline honestly labelled 'API-retail equivalent (subscription-billed: $0 marginal)'"
    - "Every Usage chart carries a Y-axis (min 0, ~4-5 nice ticks) + X-axis (MMM-D ticks) and a top-right legend per the D-06 full-polish contract"
    - "The range switcher (7d/30d/90d/all-time) re-fetches and rescopes charts; all-time renders weekly buckets"
    - "Refresh (on open + button) re-fetches and updates the 'as of {HH:MM:SS}' stamp; no SSE, no timer"
    - "Empty ledger shows 'no usage recorded yet' instead of a broken chart; fetch failure shows 'could not load usage stats'"
    - "ctx.openStatsDashboard/closeStatsDashboard/isStatsDashboardOpen exist and are wired in app.js"
  artifacts:
    - path: "src/viz/modules/stats-dashboard.js"
      provides: "full-window takeover shell + tab/range/refresh state + Usage-tab rendering"
      min_lines: 120
      contains: "openStatsDashboard"
    - path: "src/viz/index.html"
      provides: "#stats-view host markup with header (tabs, range pills, refresh, close)"
      contains: "stats-view"
    - path: "src/viz/css/styles.css"
      provides: "#stats-view flat surface + chart-card CSS + compact-hide (NO backdrop-filter)"
      contains: "#stats-view"
  key_links:
    - from: "src/viz/modules/stats-dashboard.js"
      to: "/stats/usage"
      via: "fetch on open + range change + refresh"
      pattern: "fetch\\('/stats/usage"
    - from: "src/viz/modules/app.js"
      to: "initStatsDashboard(ctx)"
      via: "init call after initSettings"
      pattern: "initStatsDashboard"
---

<objective>
Build the full-window stats takeover surface in Phase 59's HUD language: the
`#stats-view` host, its tab/range/refresh chrome, the open/close plumbing that
replaces the 3D brain (D-02), and the complete Usage tab (burn + cost markers +
per-feature/per-model + retail-$). Wire it into app.js so it is live and
self-verifiable. The Brain Health tab (60-04) and navigation entry points (60-05)
build on the shell this plan establishes.

Purpose: The primary dashboard surface and the Usage dashboard, reachable and
rendering real ledger data.
Output: index.html markup, styles.css, stats-dashboard.js, app.js wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-settings-stats-depth/60-CONTEXT.md
@.planning/phases/60-settings-stats-depth/60-PATTERNS.md
@.planning/phases/60-settings-stats-depth/60-UI-SPEC.md

<interfaces>
<!-- Precedents this plan mirrors. Extracted from the live codebase. -->
Full-window replace-the-graph pattern (corpus.js:525-616, styles.css:1228-1246):
  #corpus-graph position:fixed inset:0 z-index:5 display:none → .open{display:block};
  goToCorpus()/goToBrain() hide #graph, show the other; setTopicsSearchHidden()
  hides #hud-chip/#topics-rail while the takeover is up.
Overlay open/close + fetch-on-open + Escape (reader.js:140-197, settings.js:44-101):
  show(){classList.add('open'); if(!loaded)load()}; hide(){classList.remove('open')};
  Escape keydown closes when open; ctx.openX/closeX/isXOpen exposure convention.
Non-fatal fetch (settings.js:81-101): try{res=await fetch(p); if(!res.ok)return null;
  return res.json()}catch{return null}.
app.js wiring (app.js:35-40 imports, 252-256 init calls): initSettings(ctx) at 255,
  initPalette(ctx) LAST at 256 — insert initStatsDashboard(ctx) right after initSettings.
index.html markup neighbours (index.html:29 #graph, 34 #corpus-graph, 131-136
  #settings-panel, 140-146 #reader, 164 #tooltip).
Existing tokens (styles.css / HUD_CSS_TOKENS): --surface-index-panel
  rgba(23,15,29,0.97), --chart-card (added 60-02) rgba(26,18,32,0.4),
  --divider-faint, --accent-toggle-mauve, --motion-slow, --ease-out-soft,
  --glass-border-focused, --text-bright, --text-recede? (use UI-SPEC tokens).
charts.js (60-02): line/bar/axis/legend/hover + niceTicks/nearestPointIndex/fmt.
constants.js (60-02): COST_EVENTS, NEUTRAL_SERIES_RAMP.
D-14 invariant (tests/viz-activity-palette-invariants.test.ts): backdrop-filter
  ALLOWED_SELECTORS is a fixed set NOT including #stats-view → this surface must
  use NO backdrop-filter or the test fails.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: #stats-view markup + flat-surface CSS (no glass)</name>
  <files>src/viz/index.html, src/viz/css/styles.css</files>
  <read_first>
    - src/viz/index.html (29 #graph, 34 #corpus-graph, 125-165 settings/reader/palette/tooltip markup) — place #stats-view as a sibling of #corpus-graph
    - src/viz/css/styles.css (545-575 compact-hide @media; 707-745 #reader block; 1228-1266 #corpus-graph block) — copy the fixed/inset/display-none→.open{block} + opacity-fade pattern; copy the compact-hide list
    - tests/viz-activity-palette-invariants.test.ts (452-484 D-14-C backdrop-filter allow-list) — confirm #stats-view is NOT in the list, so it must never declare backdrop-filter
    - .planning/phases/60-settings-stats-depth/60-UI-SPEC.md (Full-Window Takeover table, Glass Discipline table, Spacing/Typography/Color) — exact tokens, z-index:6, backgrounds, tab/pill styling
  </read_first>
  <action>
    Add a `#stats-view` div sibling to `#corpus-graph` in index.html containing a
    header row with: two tab buttons (`Usage` / `Brain Health`), a range-switcher of
    four pills (`7d` `30d` `90d` `all-time`), a refresh icon button
    (aria-label `Refresh stats`, the circular-arrow outline SVG using the Phase-59
    stroke recipe viewBox 0 0 24 24 fill none stroke currentColor stroke-width 2),
    an `as of …` timestamp span, and a `×` close button mirroring #reader-close; plus
    two content containers (`#stats-usage-tab`, `#stats-health-tab`). All copy verbatim
    from the UI-SPEC Copywriting Contract. In styles.css add the `#stats-view` rule:
    position:fixed; inset:0; z-index:6; display:none; background:var(--surface-index-panel);
    opacity:0; transition:opacity var(--motion-slow) var(--ease-out-soft); with
    `#stats-view.open{display:block}` and an `.stats-in`/opacity:1 companion (mirror
    #corpus-graph). Style chart cards with background:var(--chart-card) +
    1px solid var(--divider-faint) and NO backdrop-filter anywhere. Style the active
    tab underline + active range pill with var(--accent-toggle-mauve); inactive/hover
    per UI-SPEC. Add `#stats-view` (and its trigger surfaces) to the compact-hide
    @media block at styles.css:545 so it is unreachable in tray/compact LOD.
    Do NOT add any backdrop-filter, box-shadow glow, or amber value.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-activity-palette-invariants.test.ts tests/viz-frontend-static.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass (D-14 backdrop-filter allow-list still green — proves #stats-view added no blur)
    - `grep -n "id=\"stats-view\"" src/viz/index.html` matches; the header contains the four range labels 7d/30d/90d/all-time and both tab labels
    - `grep -n "#stats-view" src/viz/css/styles.css` matches and the rule uses z-index:6 and var(--surface-index-panel)
    - `awk '/#stats-view/{f=1} f&&/backdrop-filter/{print}' src/viz/css/styles.css` prints nothing (no glass on this surface)
    - `#stats-view` appears in the `@media (max-width: 500px)` compact-hide block
  </acceptance_criteria>
  <done>#stats-view host + flat-surface/card CSS exist, glass-free, compact-hidden, D-14 invariant intact.</done>
</task>

<task type="auto">
  <name>Task 2: stats-dashboard.js shell + app.js wiring</name>
  <files>src/viz/modules/stats-dashboard.js, src/viz/modules/app.js</files>
  <read_first>
    - src/viz/modules/corpus.js (525-616 goToCorpus/goToBrain/openCorpus/isCorpusOpen + setTopicsSearchHidden) — the replace-the-graph toggle + HUD-hide behavior to mirror
    - src/viz/modules/reader.js (140-197 show/hide/Escape/ctx exposure) and settings.js (44-101 show/hide/load/non-fatal fetch) — open/close plumbing + fetch-on-open
    - src/viz/modules/app.js (35-40 imports, 252-256 init sequence) — where to import + call initStatsDashboard(ctx)
    - src/viz/index.html (#stats-view markup from Task 1) — element ids the module binds
    - .planning/phases/60-settings-stats-depth/60-UI-SPEC.md (Refresh D-15, Range switcher D-12, Empty/Error State Contract) — timestamp format, weekly-on-all rule, empty/error copy
  </read_first>
  <action>
    Create src/viz/modules/stats-dashboard.js exporting `initStatsDashboard(ctx)`.
    Bind #stats-view and its header controls. Implement show()/hide() that REPLACE
    the graph: on show, hide #graph (and #corpus-graph if open) and add `open`+fade
    class to #stats-view and call the same HUD-hide used by corpus (hide #hud-chip /
    #topics-rail); on hide, reverse and restore the brain. Expose
    ctx.openStatsDashboard(tab='usage'), ctx.closeStatsDashboard(),
    ctx.isStatsDashboardOpen(). Add Escape-to-close mirroring reader.js. Implement
    tab state (`currentTab` string, buttons toggling `active` via
    classList.toggle('active', tab===name), showing/hiding the two content
    containers). Implement range state (default '30d'; pills set it and re-fetch;
    passing window through to /stats/usage; the module trusts the server to switch
    to weekly buckets on 'all'). Implement refresh: a `load()` that fetches
    /stats/usage (non-fatal try/catch-return-null) and re-renders the active tab,
    called on open, on range change, and on the refresh button click, stamping the
    header `as of {HH:MM:SS}` from Date.now(). Render empty state
    ('no usage recorded yet') when buckets/totals are zero and error state
    ('could not load usage stats') when the fetch returned null — never a broken
    chart shell. Leave the Brain Health tab container populated with a placeholder
    that 60-04 replaces (it must not fetch /stats/brain-health yet). In app.js add
    `import { initStatsDashboard } from './stats-dashboard.js';` beside the other
    module imports and call `initStatsDashboard(ctx);` immediately after
    `initSettings(ctx);` (before initPalette). All server-sourced text via
    .textContent.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-frontend-static.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "initStatsDashboard" src/viz/modules/app.js` shows both the import and the call, and the call sits after `initSettings(ctx);`
    - `grep -nE "openStatsDashboard|closeStatsDashboard|isStatsDashboardOpen" src/viz/modules/stats-dashboard.js` matches all three
    - `grep -n "fetch('/stats/usage" src/viz/modules/stats-dashboard.js` matches; there is NO `/stats/brain-health` fetch in this file yet
    - `grep -n "no usage recorded yet" src/viz/modules/stats-dashboard.js` and `grep -n "could not load usage stats"` both match
    - `grep -n "as of" src/viz/modules/stats-dashboard.js` matches (refresh timestamp)
    - `grep -c "innerHTML" src/viz/modules/stats-dashboard.js` shows no innerHTML used with server data; tsc clean; viz-frontend-static passes
  </acceptance_criteria>
  <done>The takeover opens/closes as a graph-replacing surface with working tabs, range switcher, refresh, empty/error states, and ctx exposure wired in app.js.</done>
</task>

<task type="auto">
  <name>Task 3: Usage tab rendering — burn + cost markers + splits + retail-$</name>
  <files>src/viz/modules/stats-dashboard.js</files>
  <read_first>
    - src/viz/modules/stats-dashboard.js (the shell + load() from Task 2)
    - src/viz/modules/charts.js (60-02: line/bar/axis/legend/hover + fmt) — the render API to call; note the axis() and legend() signatures the UI-SPEC Chart Construction Contract mandates on every chart
    - src/viz/modules/constants.js (60-02: COST_EVENTS, NEUTRAL_SERIES_RAMP) — marker dates + non-activity series colors
    - src/viz/modules/settings.js (360-482 appendUsageLines/fmtTokens) — number-format precedent (fmtTokens; retail-$ toFixed(4))
    - .planning/phases/60-settings-stats-depth/60-UI-SPEC.md (Chart Construction Contract — Axes + Legend rows; Chart Series Color Contract Usage-tab table; Cost-event markers; Number formatting; Copywriting retail-$ label) — exact axis/legend spec, ramp step per series, verbatim retail-$ caption
  </read_first>
  <action>
    In stats-dashboard.js render the Usage tab from the /stats/usage response:
    (1) a headline retail-$ tile using toFixed(4) precision labelled verbatim
    `API-retail equivalent (subscription-billed: $0 marginal)` (D-09) plus a total-
    tokens Display-size number via fmtTokens; (2) the daily/weekly token-burn line
    chart via charts.js line() using NEUTRAL_SERIES_RAMP[0], with vertical dashed
    cost-event markers at each COST_EVENTS date (1px dashed neutral) whose hover
    shows the server-computed before/after avg-daily-burn delta in the shared
    #tooltip (D-10 — reuse the nearest-point hover, not a new tooltip); (3) a
    per-feature split (extract/judge/corpus_gen/schema_abstract → ramp[0..3]) and a
    per-model split (models → ramp cycled) as bar charts via charts.js bar().
    EVERY one of these three charts (burn line, per-feature bar, per-model bar) MUST
    call charts.js `axis(...)` to draw both a Y-axis (auto-scaled, minimum always 0,
    ~4-5 "nice" ticks per the UI-SPEC Chart Construction Contract) and an X-axis
    (`MMM D` tick labels, executor-chosen stride keeping ~5-8 visible ticks) — no
    chart ships axis-less — and MUST call charts.js `legend(...)` to render the
    top-right 8×8 swatch + Body-13px label row on the card header. The $-value axis
    ticks use the 2-decimal abbreviation (`$0.02`) and token axes use fmtTokens per
    the UI-SPEC Number-formatting row. Declare the daily-burn chart the primary focal
    anchor of the tab (largest card, top of the tab per the UI-checker flag). Wire
    the nearest-point hover (D-07) to every time-series via charts.js's hover helper
    rendering `{MMM D} — {value}`. All numbers/dates/labels via .textContent; SVG via
    charts.js (createElementNS). No amber; markers/series colors come only from
    NEUTRAL_SERIES_RAMP (non-activity) per the UI-SPEC Usage table.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-frontend-static.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "API-retail equivalent (subscription-billed: \$0 marginal)" src/viz/modules/stats-dashboard.js` matches verbatim
    - `grep -nE "NEUTRAL_SERIES_RAMP|COST_EVENTS" src/viz/modules/stats-dashboard.js` shows both imported/used
    - `grep -nE "import .*charts" src/viz/modules/stats-dashboard.js` matches (renders via the helper module, not inline SVG strings)
    - `grep -c "axis(" src/viz/modules/stats-dashboard.js` returns ≥3 (Y+X axes drawn on each of the three Usage charts — burn/per-feature/per-model)
    - `grep -c "legend(" src/viz/modules/stats-dashboard.js` returns ≥3 (top-right legend on each Usage chart card)
    - `grep -niE "ffb866|amber" src/viz/modules/stats-dashboard.js` returns no match
    - `grep -n "toFixed(4)" src/viz/modules/stats-dashboard.js` matches (retail-$ precision)
    - viz-frontend-static passes; tsc clean
  </acceptance_criteria>
  <done>The Usage tab renders the burn chart (focal), cost-event markers with before/after deltas, per-feature + per-model splits — each with axis + legend — and the honestly-labelled retail-$ headline — amber-free, textContent-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /stats/usage JSON → DOM/SVG | server numbers/dates/labels become chart text |
| CSS surface additions → D-14 invariant | new selectors must not extend the glass allow-list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04 | Tampering (XSS) | chart labels, retail-$, timestamps | mitigate | all server-sourced values via .textContent; SVG via charts.js createElementNS; grep-asserted no innerHTML-with-data |
| T-60-07 | Integrity (design lock) | #stats-view CSS | mitigate | no backdrop-filter on #stats-view (keeps D-14-C allow-list test green); no amber value on the surface |
| T-60-08 | Availability | fetch failure / empty DB | mitigate | non-fatal fetch → error copy; zeroed data → empty-state copy; never a broken chart shell |
| T-60-SC | Tampering | package installs | accept | zero new deps — no install task |
</threat_model>

<verification>
- `npx vitest run tests/viz-activity-palette-invariants.test.ts tests/viz-frontend-static.test.ts` green (D-14 intact)
- `npx tsc --noEmit` clean
- `grep -c "axis(" src/viz/modules/stats-dashboard.js` ≥3 (every Usage chart has axes)
- Manual/behavioral: opening via ctx.openStatsDashboard() replaces the brain and renders the Usage tab (covered by 60-05's live checkpoint)
</verification>

<success_criteria>
The stats takeover opens as a glass-free full-window surface with working tabs,
range switcher, refresh + "as of" stamp, and a complete Usage tab rendering real
ledger data (burn + markers + splits + retail-$) — every chart carrying axes +
legend per D-06 — wired live in app.js, D-14 invariant untouched.
</success_criteria>

<output>
Create `.planning/phases/60-settings-stats-depth/60-03-SUMMARY.md` when done
</output>
</content>
</invoke>
