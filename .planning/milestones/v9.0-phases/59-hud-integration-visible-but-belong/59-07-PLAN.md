---
phase: 59-hud-integration-visible-but-belong
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/viz/modules/app.js
  - tests/viz-detail-page-token-injection.test.ts
autonomous: true
requirements: [D-12]

must_haves:
  truths:
    - "On the /?detail=<id> tray-detail boot path, emitHudTokens() runs so the <style id=\"hud-tokens\"> :root block is injected — every var(--...) in styles.css resolves and the detail window renders themed (glass/borders/scrollbar), not an unstyled white page"
    - "emitHudTokens() is reachable on BOTH boot paths because its call precedes the DETAIL_ID mode branch (it is no longer nested inside the graph-boot else-branch)"
    - "A committed regression test fails if emitHudTokens() stops being reachable on the detail-page boot path, so CR-01 cannot silently regress"
  artifacts:
    - path: "src/viz/modules/app.js"
      provides: "emitHudTokens() call relocated ahead of the DETAIL_ID branch split (single call site, reached by both /?detail=<id> and graph-boot paths)"
      contains: "emitHudTokens()"
    - path: "tests/viz-detail-page-token-injection.test.ts"
      provides: "Regression lock: source-order assertion that emitHudTokens() precedes the DETAIL_ID branch + functional assertion that the injected #hud-tokens block carries resolved tokens"
      contains: "hud-tokens"
  key_links:
    - from: "src/viz/modules/app.js emitHudTokens() call"
      to: "the DETAIL_ID mode branch (if (DETAIL_ID !== null))"
      via: "call ordering — emission runs before the branch splits, so both paths inject the :root token block"
      pattern: "emitHudTokens\\(\\)[\\s\\S]*DETAIL_ID"
---

<objective>
Close the single BLOCKER gap CR-01 from 59-VERIFICATION.md: the `/?detail=<id>`
tray-detail boot path never injects the HUD `:root` token block, so the shipped
detail window renders as an unstyled white page (styles.css is 100% var(--token)
with zero literal fallbacks after Plan 02's migration).

Purpose: Restore truth #2 ("at viz boot a :root token block is injected") and
truth #6 / D-12 ("#detail carries the glass token language") for the detail-page
boot path — the exact surface `detail-page.js`'s own header documents as a real,
shipped tray feature. This is a guaranteed functional regression of a working
pre-phase surface, traced directly to this phase's own token migration.

Output: `emitHudTokens()` relocated ahead of the `DETAIL_ID` branch in app.js so
both boot paths inject tokens, plus a regression test that locks the detail-path
reachability so it cannot silently regress again.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-hud-integration-visible-but-belong/59-CONTEXT.md
@.planning/phases/59-hud-integration-visible-but-belong/59-VERIFICATION.md
@.planning/phases/59-hud-integration-visible-but-belong/59-REVIEW.md

<interfaces>
Root cause (independently reproduced by the verifier, confirmed via source read):

- src/viz/modules/app.js:
  - line 22-23: `import * as THREE from 'three';` and
    `import { emitHudTokens } from './css-tokens.js';` (import ALREADY present —
    only the CALL needs relocating, no import change)
  - line 46: `window.THREE = THREE;`
  - line 48-49: `// ── Detail mode branch …` then
    `const DETAIL_ID = new URLSearchParams(location.search).get('detail');`
  - line 51-54: `if (DETAIL_ID !== null) { … await renderDetailPage(DETAIL_ID); } else {`
    — the detail path RETURNS inside this `if` and never enters the `else`.
  - line 235 (INSIDE the `else` arm): `emitHudTokens();   // … inject :root HUD token block …`
    This is the ONLY current call site → unreachable on the detail path. THIS is the bug.

- src/viz/modules/css-tokens.js: `emitHudTokens()` is idempotent (guards on
  `document.getElementById('hud-tokens')`), imports only HUD_CSS_TOKENS from
  constants.js (no three.js), builds a `<style id="hud-tokens">` :root block via
  createElement + textContent, appends to document.head. It touches only
  `document.getElementById`, `document.createElement`, `document.head.appendChild`.

- Test harness convention (this repo has NO jsdom — vitest.config.ts is
  `environment: 'node'`): existing frontend regression tests
  (tests/viz-frontend-static.test.ts, tests/viz-activity-palette-invariants.test.ts)
  assert against source TEXT via `fs.readFileSync` + regex/index checks. The new
  regression test follows that pattern for boot-path reachability, and uses a
  tiny hand-rolled `globalThis.document` stub for the functional token-block check
  (do NOT add jsdom).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate emitHudTokens() ahead of the DETAIL_ID branch in app.js</name>
  <files>src/viz/modules/app.js</files>
  <read_first>
    - src/viz/modules/app.js (full boot flow — the import block lines 22-40, the
      `window.THREE = THREE;` assignment at line 46, the `DETAIL_ID` branch at
      lines 48-54, and the current `emitHudTokens();` call at line 235 inside the
      else-arm. See the Spike-001 load-order header comment lines 5-12.)
    - src/viz/modules/css-tokens.js (confirm emitHudTokens is idempotent and has no
      three.js dependency — safe to call before the UMD injection / before the branch)
    - .planning/phases/59-hud-integration-visible-but-belong/59-REVIEW.md (CR-01 fix
      option 1 — emit tokens BEFORE the mode branch)
  </read_first>
  <action>
    Move the single `emitHudTokens()` invocation so it runs unconditionally before
    the `DETAIL_ID` branch splits. Concretely: insert `emitHudTokens();` immediately
    after the `window.THREE = THREE;` statement (app.js line 46) and before the
    `// ── Detail mode branch` comment / the `const DETAIL_ID = new URLSearchParams(...)`
    line — carrying a one-line comment noting both boot paths need the :root token
    block (e.g. reference CR-01 / D-12). Then DELETE the now-redundant existing call
    and its inline comment at line 235 (the `emitHudTokens();   // Phase 59 Plan 01:
    inject :root HUD token block before any styled DOM renders` line inside the else
    arm) so there is exactly ONE call site, positioned before the branch. Do not
    reorder or alter the `window.THREE = THREE;` assignment or the UMD-injection block
    (Spike-001 load order is non-negotiable — emitHudTokens does not touch window.THREE).
    Leave the `import { emitHudTokens } from './css-tokens.js';` at line 23 unchanged
    (already present). Optionally update the module header's Detail-mode note (lines
    14-17) is NOT required — do not expand scope beyond the call relocation.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/viz/modules/app.js','utf8');const call=s.indexOf('emitHudTokens()');const branch=s.indexOf('DETAIL_ID !== null');const n=(s.match(/emitHudTokens\(\)/g)||[]).length;if(call>-1&&branch>-1&&call<branch&&n===1){console.log('ok')}else{console.error('call',call,'branch',branch,'count',n);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - app.js contains exactly ONE `emitHudTokens()` invocation (regex `emitHudTokens\(\)` matches once)
    - The character index of `emitHudTokens()` is LESS than the index of `DETAIL_ID !== null`
      (the call precedes the mode branch → reached on both boot paths)
    - The old call at former line 235 inside the `else` arm is gone (no `emitHudTokens()`
      appears after the `if (DETAIL_ID !== null)` branch)
    - `window.THREE = THREE;` still precedes the 3d-force-graph UMD `<script>` injection
      (Spike-001 order intact)
    - `npx tsc --noEmit -p .` is clean and `npm test` still passes
  </acceptance_criteria>
  <done>emitHudTokens() runs once, before the DETAIL_ID branch; the /?detail=<id> path now injects the :root token block.</done>
</task>

<task type="auto">
  <name>Task 2: Regression test locking emitHudTokens reachability on the detail-page boot path</name>
  <files>tests/viz-detail-page-token-injection.test.ts</files>
  <read_first>
    - src/viz/modules/app.js (post-Task-1 state — the relocated call site)
    - src/viz/modules/css-tokens.js (emitHudTokens: id 'hud-tokens', :root block,
      createElement/textContent/head.appendChild — the surface the functional stub drives)
    - tests/viz-frontend-static.test.ts (lines 29-60 — the fs.readFileSync + path.join
      corpus-setup convention and describe/it/expect style to mirror; environment is
      node, NOT jsdom)
    - vitest.config.ts (confirm `environment: 'node'` — do NOT introduce jsdom)
  </read_first>
  <action>
    Create tests/viz-detail-page-token-injection.test.ts with two describe blocks that
    together prove CR-01 stays fixed. Block A ("boot-path reachability") reads
    src/viz/modules/app.js via fs.readFileSync and asserts: (1) it contains exactly one
    `emitHudTokens()` call; (2) `app.js.indexOf('emitHudTokens()')` is strictly less than
    `app.js.indexOf('DETAIL_ID !== null')`, proving the call precedes the mode branch and
    thus runs on the /?detail=<id> path (not nested in the graph-boot else-arm). Include a
    comment tying the assertion to CR-01 / VERIFICATION truth #2 and #6. Block B
    ("injected #hud-tokens block resolves tokens") functionally exercises emitHudTokens:
    install a minimal `globalThis.document` stub before calling — `getElementById` returns
    null, `createElement` returns a plain object `{}`, `head.appendChild(el)` pushes to a
    captured array — then `await import('../src/viz/modules/css-tokens.js')`, call
    `emitHudTokens()`, and assert the appended element has `id === 'hud-tokens'` and its
    `textContent` contains `:root {` and at least one resolved custom property (e.g.
    `--glass-bg-ambient:`). Restore/delete the document stub in an afterAll so it does not
    leak (do NOT permanently clobber a global — mirror the IN-05 lesson). Use path.join(
    __dirname, '../src/viz/modules/...') for file paths.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-detail-page-token-injection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/viz-detail-page-token-injection.test.ts` exits 0
    - The file asserts `app.js.indexOf('emitHudTokens()') < app.js.indexOf('DETAIL_ID !== null')`
      (source-order reachability lock)
    - The file asserts the emitHudTokens-injected element has id `hud-tokens` and its
      textContent contains `:root {` and `--glass-bg-ambient:`
    - The test is proven to actually guard: temporarily reverting Task 1 (moving the call
      back inside the else-arm / after the branch) makes Block A FAIL — confirm this
      mentally by the index assertion, do not commit the revert
    - The `globalThis.document` stub is torn down (afterAll) — no global left clobbered
    - Full suite `npm test` remains green (2643 passed / 3 skipped baseline, +new tests)
  </acceptance_criteria>
  <done>Regression test green; detail-page token-injection reachability is machine-locked against silent regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viz server → browser | Loopback-bound, read-only static asset + JSON server; no accounts, no writes. This gap-closure relocates one existing client-side function call and adds a source-text/stub test — it introduces NO new trust boundary and NO new input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-07-01 | Tampering | app.js emitHudTokens() call relocation | accept | Same in-repo hardcoded HUD_CSS_TOKENS values, emitted via textContent (not innerHTML), guarded against double-injection. Moving the call site earlier changes only WHEN it runs, not WHAT it emits — no new injection surface. |
| T-59-07-SC | Tampering | package installs | N/A | Zero new packages (no jsdom, no deps) — no supply-chain surface. |

Spoofing / Repudiation / Info-Disclosure / DoS / Elevation: N/A — presentation-layer client-side CSS-token wiring fix, no auth, no persistence, no external calls.
</threat_model>

<verification>
- `npx vitest run tests/viz-detail-page-token-injection.test.ts` green (new regression lock)
- `npm test` full suite green (relocating a call in shared app.js must not break existing locks)
- `npx tsc --noEmit -p .` clean
- Manual (optional, matches the missed verifier check): load `recense viz` as
  `/?detail=<id>` and confirm a `<style id="hud-tokens">` is present in <head> and the
  #detail window renders themed (aubergine glass, hairline borders, themed scrollbar) —
  not an unstyled white page.
</verification>

<success_criteria>
- emitHudTokens() runs on BOTH boot paths (call precedes the DETAIL_ID branch; single call site)
- The /?detail=<id> tray detail window injects the :root token block and renders themed
- A committed regression test fails if emitHudTokens() stops being reachable on the detail-page path
- No WARNING/INFO findings (WR-02..WR-05, IN-01..IN-05) touched — scope held to CR-01 only
</success_criteria>

<output>
Create `.planning/phases/59-hud-integration-visible-but-belong/59-07-SUMMARY.md` when done
</output>
