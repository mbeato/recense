---
phase: 59-hud-integration-visible-but-belong
plan: 03
type: execute
wave: 3
depends_on: ["59-01", "59-02"]
files_modified:
  - src/viz/modules/palette.js
  - src/viz/modules/search.js
  - src/viz/modules/topics.js
  - src/viz/modules/hud.js
  - src/viz/modules/reader.js
  - src/viz/modules/app.js
  - src/viz/index.html
  - src/viz/css/styles.css
  - tests/viz-hud-palette.test.ts
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-02, D-03]

must_haves:
  truths:
    - "Pressing ⌘K (or Ctrl-K) opens a single-input palette with Nodes / Topics / Commands sections"
    - "Typing filters all three sections; selecting a node closes the palette and fly-tos via ctx.selectNode; selecting a command executes immediately"
    - "Node matching reuses the existing debounced /search BM25 fetch (with its sequence-guard); Topics/Commands use a client-side fuzzy/subsequence matcher"
    - "Event log and tombstone toggle are palette commands (ctx.toggleLog / ctx.toggleTombstones), not buttons"
    - "D-06: the palette works in brain/corpus/reader views — a node/topic fly-to first switches back to brain (ctx.showBrainFromCorpus if ctx.isCorpusOpen; close reader if reader-open), and the Commands section adapts to the current view"
  artifacts:
    - path: "src/viz/modules/palette.js"
      provides: "⌘K palette — input, per-section matcher, render, keyboard, command registry, ctx.openPalette"
      min_lines: 90
      exports: ["initPalette"]
    - path: "tests/viz-hud-palette.test.ts"
      provides: "unit test for the fuzzy/subsequence matcher"
      contains: "fuzzy"
    - path: "src/viz/index.html"
      provides: "#palette + #palette-backdrop DOM"
      contains: "id=\"palette\""
  key_links:
    - from: "src/viz/modules/palette.js"
      to: "/search?q= (via search.js exported fetch helper)"
      via: "reused debounced fetch + sequence-guard — not reimplemented"
      pattern: "search"
    - from: "src/viz/modules/palette.js"
      to: "ctx.selectNode"
      via: "select-and-go — damped camera fly-to through the existing entry point"
      pattern: "selectNode"
    - from: "src/viz/modules/palette.js"
      to: "ctx.showBrainFromCorpus / reader-open state (D-06 view-switch)"
      via: "before ctx.selectNode, if ctx.isCorpusOpen() -> ctx.showBrainFromCorpus(); if reader-open -> ctx.closeReader() (so the camera never flies on a hidden canvas)"
      pattern: "isCorpusOpen|showBrainFromCorpus|reader-open|isReaderOpen"
---

<objective>
Build the vanilla-JS ⌘K command palette (Raycast/Linear register) that unifies node search,
topic-region access, and dev/nav commands behind one input — reusing search.js's fetch and
topics.js's region-highlight verbatim, and re-homing the event-log and tombstone-toggle dev
tools as commands. Refactor search.js to expose its fetch helper (dropping its always-open DOM)
and expose hud.js's log/tombstone logic as ctx callables.

Purpose: D-03 demotes search to one discoverable surface; D-04/D-05/D-06/D-07 define the palette
mechanics; D-02 moves dev tools into it. This is the phase's one genuinely-new module — but it is
a composite of two proven modules, not a new subsystem.

Output: palette.js (new), search.js/topics.js/hud.js refactors, palette DOM + glass CSS, app.js
wiring, and a unit test for the matcher.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-hud-integration-visible-but-belong/59-CONTEXT.md
@.planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md
@.planning/phases/59-hud-integration-visible-but-belong/59-RESEARCH.md
@.planning/phases/59-hud-integration-visible-but-belong/59-PATTERNS.md

<interfaces>
Reuse (do NOT reimplement) — from PATTERNS.md:
- search.js fetch/debounce/sequence-guard (lines 32-33, 102-132): DEBOUNCE_MS=200, GET /search?q=,
  mySeq===seq stale-response guard (the one non-obvious correctness detail — copy exactly). Resolve
  ids via ctx.idMap. Use PALETTE_CAP_NODES=8 for display cap (from constants.js).
- search.js render (lines 58-80): textContent-only, createElement rows, ctx.selectNode(node) on pick.
- topics.js (lines 34-49, 70-73): schema list = ctx.allNodes filter type==='schema' && !tombstoned,
  memberCount via abstracts-edges, selection via ctx.selectNode(schemaNode) — engine-served region
  highlight (SC2 honesty invariant — never client-approximate membership).
- settings.js show/hide (lines 34-65): classList('open') + documentElement class + graceful
  `if (!el) return` guard + independent guarded Escape listener. Copy this open/close shape.
- hud.js (lines 96-158): tombstone-toggle closure state + log array + SSE wiring survive; expose
  toggleTombstones/toggleLog/showLog as ctx callables for the command registry.
- Guarded independent keydown listeners (reader.js:165, detail.js:730, settings.js:63) coexist
  safely — palette adds a FOURTH (⌘K + Escape guarded on state.open). NO central dispatcher
  (RESEARCH Pitfall 3).

Palette tunables (constants.js, from Plan 01): PALETTE_DEBOUNCE_MS=200, PALETTE_CAP_NODES=8,
PALETTE_CAP_TOPICS=6, PALETTE_CAP_COMMANDS=8, PALETTE_Z_INDEX=50, PALETTE_BACKDROP_Z_INDEX=49.
Matching = independent per-section fuzzy-sort+cap (RESEARCH Open Q2 recommendation).

app.js init order (lines 232-246): initPalette joins LAST — needs ctx.selectNode (initDetail/
initSearch), ctx.allNodes, settings/reader/corpus show(), hud toggles all wired.

Command registry (D-02/D-04, copy labels from UI-SPEC Copywriting Contract verbatim):
Show event log → ctx.showLog/toggleLog; Toggle tombstones → ctx.toggleTombstones; Open settings →
settings show(); Open reader → reader show(); Open corpus view → corpus open; Recenter view →
graph/camera recenter. Each registry entry is { id, label, run(ctx), visibleIn? } — visibleIn is an
optional array of view names ('brain'|'corpus'|'reader'); the Commands section filters by the CURRENT
view before fuzzy-matching so commands adapt per view (D-06 "commands adapt per view"). Entries with
no visibleIn show in every view. E.g. "Recenter view" is brain-only (visibleIn:['brain']); "Open
corpus view" shows in brain/reader (not while already in corpus). Phase-60 "Open usage stats" appends
without structural change (registry is an array).

D-06 view-switch contract (the load-bearing half of D-06 — a node/topic fly-to must not run on a
hidden canvas):
- Current-view detection uses EXISTING entry points — corpus: ctx.isCorpusOpen() (corpus.js:613);
  reader: document.documentElement.classList.contains('reader-open') (reader.js:132) OR a new
  ctx.isReaderOpen() exposed in Task 1.
- Before EVERY ctx.selectNode(node)/ctx.selectNode(schemaNode) call from the palette: if
  ctx.isCorpusOpen() → call ctx.showBrainFromCorpus() (corpus.js:602, existing); if reader is open →
  call ctx.closeReader() (the reader hide() equivalent — exposed as a ctx callable in Task 1, since
  reader.js has NO ctx close export today, only ctx.openReader). Only then fly to the node, so the
  damped camera never animates on a hidden brain canvas.

Copy (UI-SPEC Copywriting Contract): input placeholder "Search nodes, topics, commands…";
empty heading "No matches"; empty body "Try a different search term, or press Esc to close.";
search error reuse "search unavailable — try again".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose reusable pieces — search.js fetch helper, topics.js schema-list helper, hud.js command callables, reader close/state hooks</name>
  <files>src/viz/modules/search.js, src/viz/modules/topics.js, src/viz/modules/hud.js, src/viz/modules/reader.js</files>
  <read_first>
    - src/viz/modules/search.js (full — the debounced fetch, sequence-guard, renderResults, fly-to)
    - src/viz/modules/topics.js (full — schema-list building, memberCount, ctx.selectNode call)
    - src/viz/modules/hud.js (full — tombstone-toggle closure, log array, showToast, SSE wiring,
      the textContent security discipline in its header)
    - src/viz/modules/reader.js (lines ~130-155 — the internal show()/hide() pair; note reader.js
      today exposes ONLY ctx.openReader, no ctx close/state export; hide() already handles both the
      brain and corpus close paths, so Task 1 just wraps it)
    - .planning/phases/59-hud-integration-visible-but-belong/59-PATTERNS.md (the exact line refs
      and the "keep logic, change home" mapping)
  </read_first>
  <action>
    In search.js: extract the fetch+debounce+sequence-guard into an exported helper (e.g.
    ctx.searchNodes = async (query) => Node[] resolved via ctx.idMap, with the mySeq===seq guard and
    the "search unavailable — try again" error toast preserved verbatim). Keep the fly-to/select via
    ctx.selectNode reachable. Do NOT delete the #search-wrap/#search-results DOM references YET —
    guard them with the existing `if (!el) return` so search.js still boots after Plan 04 removes the
    DOM (the always-open UI is retired in Plan 04; here we only make the fetch reusable). In topics.js:
    factor the schema-list builder (filter type==='schema' && !tombstoned, memberCount via
    abstracts-edges, sort by count) into an exported helper (e.g. ctx.listTopics = () => {node,count}[])
    that both the topics rail (Plan 04) and the palette Topics section consume; keep selection via
    ctx.selectNode(schemaNode). In hud.js: expose the existing tombstone-toggle and log-show logic as
    ctx callables — ctx.toggleTombstones(), ctx.toggleLog() (or ctx.showLog()) — without changing the
    underlying closure state or SSE wiring; the button click handlers may still call these for now
    (removed in Plan 04). Preserve every textContent-only security note. In reader.js: expose the
    EXISTING internal hide() and open-state as ctx callables so the palette's D-06 view-switch can
    close the reader before a node fly-to — add ctx.closeReader = () => hide() and ctx.isReaderOpen =
    () => panel.classList.contains('open') (reader.js has no ctx close/state export today, only
    ctx.openReader). Do NOT change hide()'s brain/corpus branch logic or its liftGraphFocus/
    returnToCorpus behaviour — only surface it on ctx.
  </action>
  <verify>
    <automated>grep -q "ctx.searchNodes" src/viz/modules/search.js && grep -q "ctx.listTopics" src/viz/modules/topics.js && grep -qE "ctx.toggle(Tombstones|Log)|ctx.showLog" src/viz/modules/hud.js && grep -q "ctx.closeReader" src/viz/modules/reader.js && grep -q "ctx.isReaderOpen" src/viz/modules/reader.js && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - search.js exposes a reusable node-search helper on ctx (ctx.searchNodes or equivalent) that
      contains the `mySeq === seq` sequence-guard and the "search unavailable — try again" copy
    - topics.js exposes ctx.listTopics (schema list with memberCount) and still routes selection
      through ctx.selectNode(schemaNode)
    - hud.js exposes ctx.toggleTombstones and ctx.toggleLog/ctx.showLog; tombstone/log closure state
      and EventSource('/events') wiring unchanged
    - reader.js exposes ctx.closeReader (wraps the existing hide()) and ctx.isReaderOpen; hide()'s
      brain/corpus branch + liftGraphFocus/returnToCorpus behaviour unchanged
    - `npm test` green; loading `recense viz` still boots (existing search/topics/hud/reader still function)
  </acceptance_criteria>
  <done>Fetch, topic-list, and dev-tool logic are reusable ctx callables without behaviour change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: palette.js module + fuzzy matcher (unit-tested) + D-06 view-switch + DOM + app.js wiring</name>
  <files>src/viz/modules/palette.js, src/viz/index.html, src/viz/modules/app.js, tests/viz-hud-palette.test.ts</files>
  <read_first>
    - src/viz/modules/search.js (ctx.searchNodes from Task 1; renderResults textContent pattern)
    - src/viz/modules/topics.js (ctx.listTopics from Task 1)
    - src/viz/modules/corpus.js (ctx.isCorpusOpen / ctx.showBrainFromCorpus, lines ~602-615 — the
      D-06 view-switch entry points; do NOT modify corpus.js, only call these)
    - src/viz/modules/reader.js (ctx.closeReader / ctx.isReaderOpen from Task 1; reader-open class is
      document.documentElement.classList 'reader-open' at reader.js:132)
    - src/viz/modules/settings.js (show/hide/open-state/guarded-Escape shape to copy)
    - src/viz/modules/app.js (init order lines 232-246 + import block — where initPalette joins last)
    - src/viz/index.html (existing #settings-panel / #detail block conventions for the new #palette DOM)
    - src/viz/modules/constants.js (PALETTE_* tunables)
    - .planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md (Palette Tunables,
      Copywriting Contract, Typography — mono input, section headers)
    - .planning/phases/59-hud-integration-visible-but-belong/59-RESEARCH.md (Pitfall 3 — no central
      dispatcher; Security Domain — textContent-only, no innerHTML match-highlight)
  </read_first>
  <behavior>
    - fuzzyScore(query, candidate): returns a numeric score when query is a subsequence of candidate
      (higher for contiguous/earlier matches), and a falsy/negative sentinel when not a subsequence.
      Test: fuzzyScore('stg','settings') > 0; fuzzyScore('set','settings') > fuzzyScore('stg','settings')
      (contiguity/earlier wins); fuzzyScore('xyz','settings') is falsy; empty query returns all
      candidates (matches everything).
    - filterSection(query, items, cap): returns items scored > 0, sorted desc, sliced to cap.
    - Command registry is an array of {id,label,run,visibleIn?} — visibleIn is an optional view-name
      array; the Commands section filters by the current view before fuzzy-matching (D-06 "commands
      adapt per view"). Entries without visibleIn appear in every view. Extensible (Phase-60 "Open
      usage stats" appends without structural change).
    - currentView(): derive the active view from EXISTING state — 'corpus' if ctx.isCorpusOpen();
      'reader' if ctx.isReaderOpen() (or document.documentElement.classList.contains('reader-open'));
      else 'brain'. Pure read, no new state.
    - flyToNode(target): D-06 view-switch wrapper — if ctx.isCorpusOpen() call ctx.showBrainFromCorpus();
      if reader-open call ctx.closeReader(); THEN ctx.selectNode(target). Used for both node and topic
      selection so the damped camera never animates on a hidden brain canvas.
  </behavior>
  <action>
    Create palette.js exporting initPalette(ctx) with a header JSDoc (ctx fields consumed, a Security
    note: textContent-only, and a palette-invariant note: no amber fill/background). Implement:
    (a) the ~20-30 line fuzzy/subsequence matcher (fuzzyScore + filterSection) — pure functions,
    exported for the unit test; (b) open()/close() copying settings.js's classList('open') +
    documentElement class + graceful `if (!el) return` guard, plus ctx.openPalette export so the rail
    magnifier (Plan 04) can invoke it; (c) a fourth guarded keydown listener — (metaKey||ctrlKey)&&'k'
    toggles open (preventDefault), Escape closes when state.open — NO central dispatcher; (d) Nodes
    section via ctx.searchNodes (debounced PALETTE_DEBOUNCE_MS, cap PALETTE_CAP_NODES); Topics via
    ctx.listTopics (fuzzy, cap PALETTE_CAP_TOPICS); Commands via the static registry (fuzzy, cap
    PALETTE_CAP_COMMANDS); (e) render all rows via textContent/createElement only under section
    headers ("Nodes"/"Topics"/"Commands"); empty-state uses the UI-SPEC copy; (f) select-and-go
    via the flyToNode wrapper (D-06 view-switch, the load-bearing half): node → close + flyToNode(node);
    topic → close + flyToNode(schemaNode); where flyToNode FIRST returns to brain — if ctx.isCorpusOpen()
    call ctx.showBrainFromCorpus(), if the reader is open (ctx.isReaderOpen() OR
    document.documentElement.classList.contains('reader-open')) call ctx.closeReader() — and ONLY then
    ctx.selectNode(target), so the camera never flies on a hidden canvas; command → run() immediately;
    no preview-on-arrow, no auto-open detail (D-05). The Commands section filters the registry by
    currentView() (visibleIn predicate) before fuzzy-matching so commands adapt per view (D-06): e.g.
    "Recenter view" is brain-only, "Open corpus view" is hidden while already in corpus. Add #palette + #palette-backdrop DOM to index.html (mono input, z via
    PALETTE_Z_INDEX/PALETTE_BACKDROP_Z_INDEX applied in Plan 03 Task 3 CSS). Wire initPalette(ctx)
    LAST in app.js. Add tests/viz-hud-palette.test.ts asserting the fuzzyScore/filterSection behaviors
    above (import the exported pure functions).
  </action>
  <verify>
    <automated>bash -c 'set -e; npx vitest run tests/viz-hud-palette.test.ts; grep -q "isCorpusOpen" src/viz/modules/palette.js; grep -q "showBrainFromCorpus" src/viz/modules/palette.js; grep -qE "reader-open|isReaderOpen" src/viz/modules/palette.js; grep -qE "closeReader" src/viz/modules/palette.js; grep -q "visibleIn" src/viz/modules/palette.js; echo ok'</automated>
  </verify>
  <acceptance_criteria>
    - tests/viz-hud-palette.test.ts exits 0 and asserts: fuzzyScore('stg','settings')>0,
      fuzzyScore('set','settings')>fuzzyScore('stg','settings'), fuzzyScore('xyz','settings') falsy,
      empty-query matches all
    - palette.js exports initPalette and the pure matcher functions; contains a keydown listener
      matching (metaKey||ctrlKey) && key==='k' with preventDefault, and an Escape guard on state.open
    - palette.js Nodes section calls ctx.searchNodes; Topics calls ctx.listTopics; both selections
      route through ctx.selectNode; command rows call registry run()
    - D-06 view-switch (grep-verified): before ctx.selectNode, palette.js references ctx.isCorpusOpen +
      ctx.showBrainFromCorpus and reader-open state (ctx.isReaderOpen or the 'reader-open' class) +
      ctx.closeReader — a node/topic fly-to from corpus/reader switches back to brain first
    - D-06 commands-adapt-per-view (grep-verified): the command registry uses a visibleIn predicate and
      the Commands section filters by the current view before matching
    - palette.js renders exclusively via textContent/createElement — grep shows no `innerHTML =` with
      dynamic data
    - index.html contains id="palette" and id="palette-backdrop"; app.js calls initPalette(ctx) after
      initSettings(ctx)
    - `npm test` full suite green
  </acceptance_criteria>
  <done>⌘K opens a working mixed-section palette; matcher unit-tested; select-and-go routes through ctx.selectNode.</done>
</task>

<task type="auto">
  <name>Task 3: Palette glass CSS + compact-mode ⌘K gating</name>
  <files>src/viz/css/styles.css</files>
  <read_first>
    - src/viz/css/styles.css (the token-migrated stylesheet from Plan 02; the existing
      #settings-panel/.mode-window/compact @media block ~lines 420-435; the z-index map)
    - src/viz/modules/constants.js (PALETTE_Z_INDEX=50 / PALETTE_BACKDROP_Z_INDEX=49 — the values the
      #palette / #palette-backdrop rules must use)
    - .planning/phases/59-hud-integration-visible-but-belong/59-UI-SPEC.md (Glass Construction,
      Typography section headers, Spacing Scale, Palette Tunables z-index)
    - .planning/phases/59-hud-integration-visible-but-belong/59-RESEARCH.md (Pitfall 7 — compact
      gating; z-index map)
  </read_first>
  <action>
    Add #palette / #palette-backdrop rules to styles.css using the focused glass tier: background
    var(--glass-bg-focused), border var(--glass-border-focused), backdrop-filter blur(var(--glass-blur-md)),
    box-shadow var(--glass-specular), border-radius var(--radius-lg); z-index 50 for #palette and 49
    for #palette-backdrop (matching PALETTE_Z_INDEX/PALETTE_BACKDROP_Z_INDEX). Style the mono input
    (font-family 'JetBrains Mono HUD', 13px), the uppercase section headers (12px/600/0.06em
    letter-spacing per Typography), and result rows using the Spacing Scale (section padding md=16px,
    section gaps lg=24px). Open/close is opacity/visibility via var(--motion-slow)/var(--ease-out-soft)
    — no layout shift. Add the palette to the existing compact @media (max-width:500px),(max-height:500px)
    hide-list so it never shows in the tray popover (Pitfall 7). The ⌘K keydown gating behind
    .mode-window is enforced in palette.js; document in a CSS comment that the palette is window-mode only.
  </action>
  <verify>
    <automated>bash -c 'set -e; grep -qE "#palette\b" src/viz/css/styles.css; grep -q "z-index: 50" src/viz/css/styles.css; grep -q "JetBrains Mono HUD" src/viz/css/styles.css; grep -A40 "max-width: 500px" src/viz/css/styles.css | grep -q "palette" || { echo "palette not in compact hide-list"; exit 1; }; echo ok'</automated>
  </verify>
  <acceptance_criteria>
    - #palette rule uses var(--glass-bg-focused)/var(--glass-border-focused)/blur(var(--glass-blur-md))/
      var(--glass-specular)/var(--radius-lg) and z-index: 50; #palette-backdrop z-index: 49
    - The palette input uses font-family 'JetBrains Mono HUD'; section headers use 600 weight +
      0.06em letter-spacing + uppercase
    - The compact @media block hides #palette (or #palette-backdrop) — grep confirms
    - Palette open/close transitions are opacity/visibility only (no top/left/width/height animated)
    - `npx vitest run tests/viz-activity-palette-invariants.test.ts` still green (D-14 backdrop-filter
      allow-list now includes #palette)
  </acceptance_criteria>
  <done>Palette renders as focused-tier glass at z-50, mono input, window-mode only; invariants green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viz server → browser | /search?q= and /graph return server-sourced node/topic strings rendered into palette result rows — the one real untrusted-string surface this plan introduces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03 | Tampering | palette result rendering (node labels, topic names) | mitigate | textContent/createElement-only rendering (never innerHTML) — inherit the repo-wide T-10-12/T-44-19 discipline verbatim; if match-highlight is added, build via Text-node splitting, never string-concatenated innerHTML (RESEARCH Security Domain). |
| T-59-04 | Info Disclosure | /search reuse | accept | No new endpoint; palette reuses the existing read-only loopback /search — same exposure as today's search box. |
| T-59-SC | Tampering | package installs | N/A | Zero new packages (vanilla JS matcher, ~20-30 lines). |

Spoofing / Repudiation / DoS / Elevation: N/A — loopback, read-only, no auth.
</threat_model>

<verification>
- `npx vitest run tests/viz-hud-palette.test.ts` green (matcher)
- `npx vitest run tests/viz-activity-palette-invariants.test.ts` green (D-14 now includes #palette)
- `npm test` full suite green
- Manual: ⌘K opens palette; typing filters Nodes/Topics/Commands; node select fly-tos; "Toggle
  tombstones"/"Show event log" commands work; palette does not open in the tray popover
</verification>

<success_criteria>
- ⌘K palette with Nodes(BM25)/Topics(fuzzy)/Commands(fuzzy), select-and-go via ctx.selectNode
- Event log + tombstone toggle re-homed as commands; search.js fetch + topics list reused, not rebuilt
- Matcher unit-tested; textContent-only rendering; palette gated out of compact/popover mode
</success_criteria>

<output>
Create `.planning/phases/59-hud-integration-visible-but-belong/59-03-SUMMARY.md` when done
</output>
