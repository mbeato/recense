---
phase: 57-viz-activity-palette-redesign
plan: 08
type: execute
wave: 7
gap_closure: true
closes_gaps: [CR-01, CR-02]
depends_on: ["57-01", "57-06"]
files_modified:
  - src/viz/modules/trace.js
  - src/viz/modules/detail.js
  - src/viz/server.ts
  - tests/viz-ambient-liveliness.test.ts
autonomous: true
requirements: [VIZ-PAL-01, VIZ-PAL-02, VIZ-PAL-04]

must_haves:
  truths:
    - "A replayed ingestion-kind row (kind = oscillation | new_node | neutral | reconsolidation) never resolves to HOT_COLOR/amber — its seed/hop activation colour is KIND_COLORS.neutral, honouring the locked D-04 amber-exclusivity invariant"
    - "After an ingestion-kind trace's fade window elapses, ctx.traceNodes no longer contains the node ids that trace added — no permanent visibility leak defeating lod.js's overview LOD"
    - "focusNode's reveal lifetime is documented truthfully — the detail.js comment matches actual behaviour (no false 'the next trace's fade-back may re-hide it' claim)"
    - "The non-recall replay comment in trace.js matches actual behaviour (states neutral, and the code delivers neutral)"
  artifacts:
    - path: "src/viz/modules/trace.js"
      provides: "own-trace-scoped fade timers on all five _applyIngestion add-sites; non-recall replay seedColor/hopColor default to KIND_COLORS.neutral (not undefined->amber)"
      contains: "KIND_COLORS.neutral"
    - path: "src/viz/modules/detail.js"
      provides: "focusNode reveal-lifetime comment corrected to match actual (sticky-until-reload) behaviour"
    - path: "src/viz/server.ts"
      provides: "replay ring buffer kind filter — only recall/null-kind rows enter the replay buffer (defense-in-depth for D-04)"
    - path: "tests/viz-ambient-liveliness.test.ts"
      provides: "two behavioral regression tests: replayed ingestion-kind row never resolves HOT amber; ingestion-kind trace's traceNodes empty after fade"
  key_links:
    - from: "src/viz/modules/trace.js replay branch"
      to: "activate() __actColor"
      via: "seedColor = isRecallReplay ? KIND_COLORS.recall_seed : KIND_COLORS.neutral"
      pattern: "isRecallReplay \\? KIND_COLORS.recall_seed : KIND_COLORS.neutral"
    - from: "src/viz/modules/trace.js _applyIngestion add-sites"
      to: "ctx.traceNodes.delete"
      via: "scoped setTimeout deleting only the ids each branch added"
      pattern: "ctx\\.traceNodes\\.delete\\("
    - from: "ctx.traceNodes"
      to: "src/viz/modules/lod.js:192 nodeVisible predicate"
      via: "traceNodes.has(n.id)"
      pattern: "traceNodes\\.has"
---

<objective>
Close the two verified blocker gaps from 57-VERIFICATION.md, both introduced by the 57-06 WR-06 fix:

- CR-01 — ctx.traceNodes permanent leak: the global .clear() was removed but five _applyIngestion add-sites (trace.js) and focusNode (detail.js) never had their own scoped delete, so every sleep-pass consolidation event permanently reveals its node id and monotonically defeats the overview LOD on an all-day tray install.
- CR-02 — replayed non-recall rows flash HOT amber: the replay branch passes undefined seedColor/hopColor for non-recall rows, and activate()'s kindColor || HOT_COLOR fallback resolves that to live-amber at REPLAY_DIM, violating the locked D-04 amber-exclusivity invariant.

Purpose: restore the D-04 amber-exclusivity guarantee and the bounded-visibility property the phase's own must-haves assert, plus fix the two misleading comments that mask these defects.
Output: surgical fixes to trace.js/detail.js/server.ts and two behavioral regression tests closing the WR-03-class code-path-selection blindness for these two defects.

Scope discipline: this is gap closure only. Do NOT touch plans 57-01..57-07. Do NOT fix WR-01 (halo coalesce guard) or WR-02 (refcount fade clobber) — they are non-blocking review warnings, explicitly out of scope. No side-quest refactors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-viz-activity-palette-redesign/57-VERIFICATION.md
@.planning/phases/57-viz-activity-palette-redesign/57-CONTEXT.md
@.planning/phases/57-viz-activity-palette-redesign/57-06-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

trace.js — activate(node, level, kindColor, profile) (line ~317):
  - Sets node.__actColor = kindColor || HOT_COLOR (line 369). Passing undefined for kindColor resolves to HOT amber. This is the CR-02 fallback.
  - The halo path (lines 338-339, 348) also uses kindColor || HOT_COLOR.

trace.js — replay branch (lines ~682-760):
  - const isRecallReplay = !row.kind || row.kind === 'recall'; (line 705)
  - const seedColor = isRecallReplay ? KIND_COLORS.recall_seed : undefined; (line 731) — CR-02 bug
  - const hopColor  = isRecallReplay ? KIND_COLORS.recall_hop  : undefined; (line 732) — CR-02 bug
  - Misleading comment at lines 703-704 claims non-recall rows "keep the neutral default activation" (actual default is amber).
  - Already-correct scoped-fade pattern (the template to mirror for CR-01) at lines 748-759: capture addedIds = pathNodes.map(n => n.id), compute fadeMs, then setTimeout that does addedIds.forEach(id => ctx.traceNodes.delete(id)) plus a revealTrace re-hide.
  - Same scoped-fade template appears in the spontaneous branch (838-843) and the live-recall path (942-947).

trace.js — KIND_COLORS pre-built THREE.Color map (lines 263-280): has .recall_seed, .recall_hop, .neutral, .new_node, .oscillation, .reconsolidation, .spontaneous, .replay.

trace.js — _applyIngestion(row, kind) (lines ~958-1051) — the FIVE unscoped add-sites (CR-01):
  - line 973  new_node:        ctx.traceNodes.add(seedId) — single seed; markAnimating(DECAY_ATTACK_MS + DECAY_HOLD_MS + PULSE_MS)
  - line 988  oscillation:     ctx.traceNodes.add(seedId) — single seed; markAnimating(DECAY_ATTACK_MS + DECAY_HOLD_MS + PULSE_MS + 700); strobes at +280/+560ms
  - line 1014 reconsolidation: ctx.traceNodes.add(seedId) — seed; markAnimating(WF_SWEEP + DECAY_ATTACK_MS + DECAY_HOLD_MS + PULSE_MS)
  - line 1024 reconsolidation: ctx.traceNodes.add(candidateHop.node_id) — candidate (only when candidateNode present)
  - line 1047 neutral fallback: ctx.traceNodes.add(seedId) — single seed; markAnimating(DECAY_ATTACK_MS + DECAY_HOLD_MS + PULSE_MS)
  - Each branch also calls ctx.revealTrace([seedNode], []) on add. None has a matching delete anywhere.

detail.js — focusNode(node) (lines ~428-436):
  - if (ctx.nodeVisible && !ctx.nodeVisible(node)) { ctx.traceNodes.add(node.id); ctx.revealTrace([node], []); } (lines 429-432)
  - False comment at lines 417-419/426: "reveal it through the LOD if hidden (trace semantics — the next trace's fade-back may re-hide it)".

server.ts — replay buffer push (line 488): if (Array.isArray(seeds) && seeds.length > 0) { replayBuffer.push({ ..., kind: row.kind ?? null }); ... } — no kind filter. Live SSE payload emission is separate (lines 494-507) and must stay untouched.

lod.js — nodeVisible (line 192): member node visible iff expanded.has(n.__schemaId) || traceNodes.has(n.id) || densityRevealed.has(n.id). This is why a leaked traceNodes id stays revealed forever.

Test harness (tests/viz-ambient-liveliness.test.ts):
  - vi.mock('three') stub (lines 18-76): Color stores r/g/b in [0,1]; .copy/.clone/.lerp work.
  - file-level setTimeout no-op stub at line 96 — override locally with a capture array to fire fade callbacks (existing WR-06 test does this at lines 652-654).
  - makeCtx(nodes, hazeNodes) (lines 166-194) builds ctx with traceNodes: new Set(), revealTrace/markAnimating/logEvent as vi.fn(), pulseGroup: null (spawnPulse no-ops), calls initTrace(ctx), returns { ctx, ticks, setColorAtSpy }.
  - makeRegularNode(id) (lines 127-143) returns a node WITH __mat — so the activate() __actColor assignment (line 369) runs.
  - existing WR-06 behavioral regression test at lines 647-680 is the exact pattern to mirror (capture scheduled setTimeout, fire it, assert traceNodes membership).
  - imports available: initTrace from trace.js; ACT_SCALE_GAIN, REPLAY_DIM, DECAY_ATTACK_MS from constants.js (line 102) — add HOT and KIND_COLOR to that import for CR-02 assertions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CR-01 — scope the six leaked traceNodes add-sites</name>
  <files>src/viz/modules/trace.js, src/viz/modules/detail.js</files>
  <read_first>
    - src/viz/modules/trace.js (the three already-fixed scoped-fade branches at 748-759, 838-843, 942-947 — this is the exact pattern to mirror; and _applyIngestion at 958-1051)
    - src/viz/modules/detail.js (focusNode at 416-436 — the comment and the add-site)
    - src/viz/modules/lod.js (nodeVisible at 182-193 — confirm traceNodes.has is the visibility predicate the fade must reverse)
    - .planning/phases/57-viz-activity-palette-redesign/57-VERIFICATION.md (CR-01 gap block: missing items 1-2)
  </read_first>
  <action>
    In src/viz/modules/trace.js _applyIngestion, give each of the five add-sites its own scoped fade timer that mirrors the three already-correct branches — capture the exact ids that branch added into a local array, then a setTimeout that runs ids.forEach(id => ctx.traceNodes.delete(id)) followed by an "if (ctx.revealTrace) ctx.revealTrace(revealedNodes, [])" re-hide, placed just before each branch's return:
      - new_node branch (~966-978): delete [seedId]; fadeMs = DECAY_ATTACK_MS + DECAY_HOLD_MS + PULSE_MS + 500; reveal [seedNode].
      - oscillation branch (~981-995): delete [seedId]; fadeMs = DECAY_ATTACK_MS + DECAY_HOLD_MS + PULSE_MS + 700 + 500 (covers the +560ms strobe tail); reveal [seedNode].
      - reconsolidation branch (~1008-1037): collect an ids array = [seedId] plus candidateHop.node_id when candidateNode was present (i.e. when line ~1024's add ran); fadeMs = WF_SWEEP + DECAY_ATTACK_MS + DECAY_HOLD_MS + PULSE_MS + 500 (covers the delayed WF_SWEEP magenta flash); reveal the nodes that were revealed.
      - neutral fallback (~1042-1050): delete [seedId]; fadeMs = DECAY_ATTACK_MS + DECAY_HOLD_MS + PULSE_MS + 500; reveal [seedNode].
    Each fadeMs expression reuses that branch's own markAnimating duration plus a 500ms tail, matching the "<attack> + DECAY_HOLD_MS + 500" convention of the three fixed branches. Do NOT global-clear; delete only that branch's own ids. Do NOT touch ctx.traceLinks (this module never adds to it — confirmed in 57-06).
    In src/viz/modules/detail.js focusNode, DECIDE the reveal lifetime as sticky-until-reload — user-initiated focus intentionally keeps the focused node visible; a scoped timer would hide a node the user deliberately flew the camera to. Rewrite the false comment at lines 417-419/426 so it states the truth: the reveal persists until page reload; it grows ctx.traceNodes only on explicit user focus of an LOD-hidden node (bounded, user-driven — NOT the automatic monotonic leak CR-01 targets), and nothing re-hides it. Remove the "the next trace's fade-back may re-hide it" claim. Leave the focusNode add-site code itself unchanged.
  </action>
  <verify>
    <automated>grep -c 'ctx.traceNodes.delete(' src/viz/modules/trace.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'ctx.traceNodes.delete(' src/viz/modules/trace.js returns >= 7 (was 3; five ingestion branches add at least four new scoped delete call-sites — reconsolidation's seed+candidate may be deleted in one forEach over a 2-element array).
    - grep -c 'ctx.traceNodes.clear()' src/viz/modules/trace.js still returns 0 (no regression of the WR-06 fix).
    - Each of the five _applyIngestion branches (new_node, oscillation, reconsolidation, neutral) has a setTimeout whose body deletes only that branch's own added id(s) from ctx.traceNodes — verified by reading each branch.
    - grep -c 'next trace' src/viz/modules/detail.js returns 0; the focusNode doc comment states the reveal persists until reload.
    - npx tsc --noEmit exits 0.
  </acceptance_criteria>
  <done>All five _applyIngestion add-sites have own-trace-scoped fade deletion; focusNode's comment truthfully describes its sticky-until-reload reveal; no global clear reintroduced; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: CR-02 — non-recall replay rows resolve to neutral, never amber</name>
  <files>src/viz/modules/trace.js, src/viz/server.ts</files>
  <read_first>
    - src/viz/modules/trace.js (replay branch 682-760, especially lines 703-705 comment and 731-732 seedColor/hopColor; activate() fallback at 369; KIND_COLORS map at 263-280)
    - src/viz/modules/constants.js (KIND_COLOR + HOT at 106, 149-183; the D-04 lock comment at 123-130)
    - src/viz/server.ts (replay buffer push at 486-493)
    - .planning/phases/57-viz-activity-palette-redesign/57-VERIFICATION.md (CR-02 gap block: missing items 1-2)
  </read_first>
  <action>
    In src/viz/modules/trace.js replay branch: change line 731 so the non-recall case resolves to KIND_COLORS.neutral instead of undefined — final form: const seedColor = isRecallReplay ? KIND_COLORS.recall_seed : KIND_COLORS.neutral;. Apply the identical change to line 732 hopColor — final form: const hopColor = isRecallReplay ? KIND_COLORS.recall_hop : KIND_COLORS.neutral;. This makes activate()'s kindColor || HOT_COLOR fallback never fire for replayed non-recall rows — they now paint the slate neutral hue (KIND_COLOR.neutral = 0xaab3c4, D-04-compliant), not HOT amber.
    Fix the misleading comment at lines 703-704: it currently claims non-recall replay rows "keep the neutral default activation" — make it true by stating the non-recall replay case explicitly resolves seed/hop colour to KIND_COLORS.neutral (previously an unintended amber fallback via kindColor||HOT_COLOR — the CR-02 defect this closes).
    In src/viz/server.ts, add defense-in-depth at line 488: extend the replay-buffer admission guard so only recall (or null-kind, back-compat) rows enter the buffer — change "if (Array.isArray(seeds) && seeds.length > 0)" to also require "(row.kind == null || row.kind === 'recall')". This matches what TRIGGER-STEPS.md promises the founder (replay echoes recalls only) and keeps ingestion-kind rows out of the replay stream entirely; the client neutral fallback above remains as the belt-and-suspenders guard against any future regression. Do NOT alter the live SSE payload emission (lines 494-507) — only the replayBuffer.push admission condition.
  </action>
  <verify>
    <automated>grep -c 'KIND_COLORS.recall_seed : KIND_COLORS.neutral' src/viz/modules/trace.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'KIND_COLORS.recall_seed : KIND_COLORS.neutral' src/viz/modules/trace.js returns 1 and grep -c 'KIND_COLORS.recall_hop : KIND_COLORS.neutral' src/viz/modules/trace.js returns 1.
    - grep -c ': undefined;' on the two replay seedColor/hopColor lines returns 0 (no undefined-resolving-to-amber path remains for replay).
    - src/viz/server.ts line ~488 admission guard includes a kind check restricting the replay buffer to null/recall kinds; grep -n "row.kind === 'recall'" src/viz/server.ts finds it inside the replayBuffer.push guard.
    - The trace.js comment at 703-704 no longer claims the default is neutral-by-inaction; it names KIND_COLORS.neutral as the explicit non-recall replay colour.
    - npx tsc --noEmit exits 0; npx vitest run tests/viz-ambient-liveliness.test.ts tests/viz-activity-palette-invariants.test.ts stays green (existing suites unaffected).
  </acceptance_criteria>
  <done>Replayed non-recall rows resolve seed and hop colour to KIND_COLORS.neutral; the server replay buffer admits only recall/null-kind rows; the stale comment is corrected; existing suites still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regression tests locking both defects (WR-03 class)</name>
  <files>tests/viz-ambient-liveliness.test.ts</files>
  <read_first>
    - tests/viz-ambient-liveliness.test.ts (THREE mock 18-76; makeCtx 166-194; makeRegularNode 127-143; the existing WR-06 behavioral test 647-680 — the setTimeout-capture pattern to mirror; the constants import line 102)
    - src/viz/modules/trace.js (replay branch 682-760 and _applyIngestion 958-1051 — the exact code paths under test)
    - src/viz/modules/constants.js (HOT at 106; KIND_COLOR at 149-183)
  </read_first>
  <behavior>
    - CR-02 test: a replayed ingestion-kind row (seeds present, replay:true, kind:'oscillation') activates its seed node with __actColor equal to KIND_COLOR.neutral (0xaab3c4) and NOT equal to HOT (0xffb866) — asserts the D-04 amber-exclusivity path is honoured for non-recall replay.
    - CR-01 test: an ingestion-kind trace (kind:'new_node' or a neutral-fallback kind such as 'confirm', single seed) adds its seed id to ctx.traceNodes on fire, and after its scheduled fade callback runs, ctx.traceNodes no longer contains that id (size back to 0) — asserts the leak is closed.
  </behavior>
  <action>
    Add a new describe block to tests/viz-ambient-liveliness.test.ts (place it after the existing "WR-06 / D-11 — own-trace-scoped fades" block, ~line 682), titled for CR-01/CR-02 gap closure. Extend the constants import at line 102 to also bring in HOT and KIND_COLOR.
    CR-02 test: build a single regular node via makeRegularNode, makeCtx([node]); call ctx.applyTrace with { seeds: [{ node_id, score: 0.9 }], hops: [], replay: true, kind: 'oscillation' }; then assert node.__actColor is defined and its r/g/b match new Color(KIND_COLOR.neutral) (compare each channel), and assert it does NOT match new Color(HOT) (at least one channel differs). Use the vi.mock('three') Color stub already in the file — no real THREE needed.
    CR-01 test: mirror the WR-06 test's setTimeout capture — save realSetTimeout, replace globalThis.setTimeout with (fn) => { scheduled.push(fn); return 0; } inside a try/finally that restores it. Build one node, makeCtx([node]); call ctx.applyTrace with { seeds: [{ node_id, score: 0.6 }], hops: [], kind: 'new_node' }; assert ctx.traceNodes.has(id) is true and ctx.traceNodes.size === 1; then invoke the scheduled fade callback (the new_node branch schedules exactly one setTimeout — the scoped fade); assert ctx.traceNodes.has(id) is false and ctx.traceNodes.size === 0. Restore setTimeout in finally.
    Both tests are pure runtime assertions against applyTrace behaviour (the WR-03 gap: existing invariant tests lock constant VALUES, not which constant the code path selects — these two lock the path selection).
  </action>
  <verify>
    <automated>npx vitest run tests/viz-ambient-liveliness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both new tests are present in tests/viz-ambient-liveliness.test.ts and reference applyTrace with replay:true/kind:'oscillation' (CR-02) and kind:'new_node' (CR-01) respectively.
    - Reverting Task 2's trace.js change (seedColor back to undefined) makes the CR-02 test FAIL (it asserts __actColor == neutral, which would resolve to HOT amber under the old code) — the test genuinely locks the defect, not a tautology. (Executor confirms by reasoning about the assertion; no need to actually revert in the committed tree.)
    - Reverting Task 1's new_node scoped fade makes the CR-01 test FAIL (traceNodes.size would stay 1 with no fade callback deleting the id).
    - npx vitest run tests/viz-ambient-liveliness.test.ts passes with 2 more tests than the pre-plan count (48/48 or higher, all green).
    - npx tsc --noEmit exits 0.
  </acceptance_criteria>
  <done>Two behavioral regression tests lock CR-01 (traceNodes bounded after an ingestion-kind fade) and CR-02 (replayed ingestion-kind never resolves HOT amber); full viz-ambient-liveliness suite green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sleep-pass -> activation_trace DB | Local sleep pass writes trace rows (kind, seeds, hops) into a local SQLite table on the founder's single-user machine — trusted local writer, no network ingress |
| server.ts -> SSE -> viz client | Local viz server reads those rows and streams them to the local browser tab — same-origin, single user, no auth surface |

No new inputs, no new endpoints, no auth paths, no file-access or schema changes. This is a presentation-layer render/fade fix (faithfulness clause: viz is decorative chrome).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-08-A | Information Disclosure | replay branch colour resolution (trace.js) | mitigate | A malformed/unexpected non-recall kind must not paint the reserved live-amber (a false "live retrieval" signal). Task 2 makes the non-recall replay branch resolve to KIND_COLORS.neutral, so the kindColor||HOT_COLOR amber fallback can never fire for a replayed non-recall row — closing the D-04 violation for any current or future kind value. |
| T-57-08-B | Denial of Service | ctx.traceNodes unbounded growth (trace.js/lod.js) | mitigate | Unbounded traceNodes growth degrades the overview LOD over long uptime (soft self-DoS of the visualization). Task 1 scopes every automatic (_applyIngestion) add-site to a bounded fade window; the only remaining growth is user-initiated focusNode reveals (bounded by explicit user action, documented honestly). |
| T-57-08-SC | Tampering | package installs | accept | No package-manager installs in this plan (no new deps); the phase's package legitimacy surface is unchanged. No new supply-chain exposure. |
</threat_model>

<verification>
Phase-level checks after all three tasks:
- npx vitest run tests/viz-ambient-liveliness.test.ts tests/viz-activity-palette-invariants.test.ts — all green (2 new regression tests added; 77 prior invariant/liveliness tests unaffected).
- npx tsc --noEmit — exit 0.
- grep -c 'ctx.traceNodes.clear()' src/viz/modules/trace.js — 0 (WR-06 fix not regressed).
- grep -c 'ctx.traceNodes.delete(' src/viz/modules/trace.js — >= 7 (3 prior + new scoped ingestion fades).
- Full suite sanity: npm test — no new failures vs the 57-06 baseline.
</verification>

<success_criteria>
- CR-01 closed: every _applyIngestion branch deletes its own added ids after a bounded fade; focusNode's reveal lifetime is documented truthfully; no global clear reintroduced.
- CR-02 closed: replayed non-recall rows resolve to KIND_COLORS.neutral (never HOT amber); server replay buffer admits only recall/null-kind rows; D-04 amber-exclusivity holds.
- Both misleading comments (trace.js ~703-704, detail.js ~426) now match actual behaviour.
- Two behavioral regression tests lock the two defects (the WR-03-class code-path-selection blindness is closed for these two paths specifically).
- tsc clean; existing suites green.
</success_criteria>

<human_verification>
Routed to human (not executor-plannable — per 57-VERIFICATION.md human_verification block):
- Founder re-review of Stage-1/Stage-2 approvals given the CR-01/CR-02 fixes change the visual behaviour they approved. Once this plan lands, the founder should observe (a) an idle replay of a consolidation event renders in the neutral slate hue, never amber, and (b) previously-revealed nodes/links return to LOD-hidden after their fade window on a long-running tray. The founder either re-approves or explicitly accepts.
- OPTIONAL executor add-on (not required to close the gaps): the executor MAY append these two observation scenarios to docs/superpowers/evidence/57-stage2-system/TRIGGER-STEPS.md so the founder has a concrete walkthrough. The actual sign-off stays human.
</human_verification>

<output>
Create .planning/phases/57-viz-activity-palette-redesign/57-08-SUMMARY.md when done.
</output>
