---
phase: 57-viz-activity-palette-redesign
plan: 04
type: execute
wave: 4
depends_on: [57-03]
files_modified:
  - src/viz/modules/constants.js
  - tests/viz-activity-palette-invariants.test.ts
  - tests/viz-ambient-liveliness.test.ts
autonomous: true
requirements: [VIZ-PAL-02, VIZ-PAL-05]

must_haves:
  truths:
    - "Each layer (live, replay, spontaneous, twinkle) has a named motion profile: attack, halo/scale, pulse thickness, cadence, density"
    - "Live's founder-tuned constants are preserved pixel-equivalent as the top profile"
    - "SC3 ordering holds monotonically on the salient channels (attack sharpness, halo/scale)"
    - "Every dim factor has a hard floor ≥ the provisional floor bound; ordering 0.6 ≤ SPONT_DIM ≤ REPLAY_DIM < 1 is test-locked"
  artifacts:
    - path: "src/viz/modules/constants.js"
      provides: "Four per-layer motion-profile constant blocks (D-06) with re-homed live values (D-08) and floored dim factors (D-05)"
      contains: "motion profile"
    - path: "tests/viz-activity-palette-invariants.test.ts"
      provides: "Monotonic SC3 ordering invariants + dim-floor invariants + migrated WR-02/REPLAY_DIM locks"
      contains: "monotonic"
  key_links:
    - from: "tests/viz-activity-palette-invariants.test.ts"
      to: "src/viz/modules/constants.js"
      via: "parses per-layer attack/halo consts and asserts monotonic ordering + floors"
      pattern: "ATTACK|HALO|DIM"
---

<objective>
Author the four per-layer motion profiles (D-06) as the PRIMARY salience-ordering channels, re-home the live layer's founder-tuned constants pixel-equivalent (D-08), floor every dim factor (D-05), and lock the SC3 ordering + floor invariants in the D-12 file — migrating the scattered WR-02/REPLAY_DIM locks in.

Purpose: Salience ordering (live > replay > spontaneous > twinkle) moves off brightness-of-a-saturated-hue and onto motion/scale. Each layer gets a designed "character" (live: sharp attack + big halo; replay: soft echo; spontaneous: slow drift; twinkle: micro-breathe), with brightness demoted to a floored secondary cue. The machine-checkable ordering invariants move from color constants to motion/scale constants. This plan authors the constants + locks; 57-06 wires trace.js to consume them.

Output: motion-profile blocks in constants.js (values PROVISIONAL, ratcheted at Stage 2); ordering + floor invariants in the D-12 file; the old REPLAY_DIM<1 lock migrated out of viz-ambient-liveliness.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/57-viz-activity-palette-redesign/57-CONTEXT.md
@.planning/phases/57-viz-activity-palette-redesign/57-PATTERNS.md
@.planning/phases/56-spontaneous-1-hop-idle-activation/56-REVIEW.md

<interfaces>
<!-- Live constants to RE-HOME pixel-equivalent (D-08) — keep names + values -->
ACT_SCALE_GAIN=1.05, ACT_BRIGHTEN_GAIN=0.95, ACT_HAZE_LERP=0.95 (constants.js:337-343)
DECAY_ATTACK_MS=80, DECAY_HOLD_MS=600, DECAY_FADE_MS=2500, DECAY_FLOOR=0.04 (constants.js:116-129)

<!-- Subordinate-layer dim factors to FLOOR (D-05: floor ≥ ~0.6) -->
REPLAY_DIM=0.4 → raise to provisional ≥0.6 (subordination now from motion/scale, not brightness)
SPONT_DIM=0.3 → raise to provisional ≥0.6; keep SPONT_DIM ≤ REPLAY_DIM (WR-02)
TWINKLE_COUNT=220, TWINKLE_PERIOD_MS=2000, TWINKLE_AMP=0.42 (twinkle profile source)

<!-- Salient channels that MUST order monotonically (D-06) -->
attack sharpness (sharper = smaller ms): LIVE < REPLAY < SPONT < TWINKLE (ms)
halo/scale (bigger = more salient): LIVE > REPLAY > SPONT > TWINKLE
cadence + density: free to vary for feel (NOT ordering-constrained)

<!-- Migrating locks -->
viz-ambient-liveliness.test.ts:482-489 REPLAY_DIM<1 source-parse lock → move into D-12 file
56-REVIEW WR-02: SPONT_DIM < REPLAY_DIM ordering lock (never written) → write into D-12 file
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author four per-layer motion profiles in constants.js (D-06/D-08/D-05)</name>
  <read_first>
    - src/viz/modules/constants.js (108-129 decay envelope; 327-398 the Phase-54/56 layer blocks; the named-tunable JSDoc convention)
    - .planning/phases/57-viz-activity-palette-redesign/57-CONTEXT.md (D-05/D-06/D-07/D-08)
    - .planning/phases/57-viz-activity-palette-redesign/57-PATTERNS.md (Layer-N sub-header convention; individually-JSDoc'd named consts, never object literals)
  </read_first>
  <action>
    Restructure the layer-constants region into four named motion-profile blocks under `// Layer N — <name>` sub-headers, following the existing individually-JSDoc'd named-`export const` convention (NOT object literals). Live (Layer 1): re-home ACT_SCALE_GAIN(1.05)/ACT_BRIGHTEN_GAIN(0.95)/ACT_HAZE_LERP(0.95) and the decay envelope (DECAY_ATTACK_MS 80/HOLD 600/FADE 2500/FLOOR 0.04) — KEEP the existing names and exact values (D-08 pixel-equivalent; downstream tests import these names), just group them as the top profile and ADD the missing salient-channel consts (a live halo/scale const and a live pulse-thickness const) named for the profile. For replay/spontaneous/twinkle, add the per-layer channel consts: attack ms, halo/scale, pulse thickness, cadence (reuse existing REPLAY_CADENCE_MS/SPONT_CADENCE_MS/TWINKLE_PERIOD_MS), density (reuse TWINKLE_COUNT for twinkle; add density consts where meaningful). Set the salient channels to satisfy the ordering: attack ms LIVE(80) < REPLAY < SPONT < TWINKLE; halo/scale LIVE > REPLAY > SPONT > TWINKLE. Floor the dim factors: raise REPLAY_DIM 0.4→0.7 provisional, SPONT_DIM 0.3→0.6 provisional (both ≥0.6, SPONT_DIM ≤ REPLAY_DIM < 1). Every value carries a JSDoc stating layer character + PROVISIONAL + Stage-2 ratchet. Do NOT change live's re-homed values. Keep DECAY_* consuming shape intact so trace.js fade math (used by all branches) still resolves.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/viz/modules/constants.js','utf8'); const g=n=>parseFloat((s.match(new RegExp('export const '+n+'\\\\s*=\\\\s*([\\\\d.]+)'))||[])[1]); if(g('ACT_SCALE_GAIN')!==1.05||g('DECAY_ATTACK_MS')!==80||g('DECAY_HOLD_MS')!==600)throw new Error('live values not pixel-equivalent'); if(!(g('SPONT_DIM')>=0.6&&g('SPONT_DIM')<=g('REPLAY_DIM')&&g('REPLAY_DIM')<1))throw new Error('dim floor/order violated'); console.log('profiles authored, live preserved, floors ok')"</automated>
  </verify>
  <done>Four motion-profile blocks exist; live values are pixel-equivalent; dim factors floored ≥0.6 with SPONT_DIM ≤ REPLAY_DIM < 1.</done>
  <acceptance_criteria>
    - ACT_SCALE_GAIN=1.05, ACT_BRIGHTEN_GAIN=0.95, ACT_HAZE_LERP=0.95, DECAY_ATTACK_MS=80, DECAY_HOLD_MS=600, DECAY_FADE_MS=2500, DECAY_FLOOR=0.04 unchanged
    - REPLAY_DIM ≥ 0.6, SPONT_DIM ≥ 0.6, SPONT_DIM ≤ REPLAY_DIM, REPLAY_DIM < 1
    - Per-layer attack-ms and halo/scale consts exist for all four layers, named under Layer-N headers, each JSDoc'd with layer character + PROVISIONAL/Stage-2 note
    - No object-literal profile blocks (named consts only, per convention)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock SC3 ordering + dim floors; migrate WR-02/REPLAY_DIM locks (D-12)</name>
  <read_first>
    - tests/viz-activity-palette-invariants.test.ts (existing D-10 + D-02 sections; add a D-06/D-05 section)
    - tests/viz-ambient-liveliness.test.ts (482-489 REPLAY_DIM<1 lock to migrate out; note it also imports REPLAY_DIM at line 102 for behavioral tests — leave the import, only move the source-parse lock)
    - src/viz/modules/constants.js (profile consts from Task 1)
    - .planning/phases/56-spontaneous-1-hop-idle-activation/56-REVIEW.md (WR-02 fix sketch, lines 96-102)
  </read_first>
  <behavior>
    - Monotonic attack: parse the four per-layer attack-ms consts; assert LIVE < REPLAY < SPONT < TWINKLE (strictly increasing ms = decreasing sharpness).
    - Monotonic halo/scale: parse the four per-layer halo/scale consts; assert LIVE > REPLAY > SPONT > TWINKLE.
    - Dim floors: assert REPLAY_DIM ≥ FLOOR_BOUND and SPONT_DIM ≥ FLOOR_BOUND (FLOOR_BOUND = 0.6 provisional, ratchets Stage 2).
    - WR-02 ordering: assert SPONT_DIM < REPLAY_DIM (or ≤, matching the authored relation) AND REPLAY_DIM < 1.
    - Cadence/density are NOT asserted for ordering (free to vary — D-06).
  </behavior>
  <action>
    Add a `describe('D-06 motion-profile SC3 ordering + D-05 dim floors', ...)` section to the invariants file implementing the behaviors above via the source-parse helpers. Declare `FLOOR_BOUND = 0.6` as a named provisional constant with a Stage-2-ratchet comment. Then remove the `REPLAY_DIM < 1` source-parse test from viz-ambient-liveliness.test.ts (lines 482-489), leaving a one-line comment there noting the lock moved to viz-activity-palette-invariants.test.ts (do NOT remove the `REPLAY_DIM` import at line 102 — the behavioral replay-peak tests still need it). Confirm the migrated + new locks pass against the Task-1 provisional values.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-activity-palette-invariants.test.ts 2>&1 | tail -15 && npx vitest run tests/viz-ambient-liveliness.test.ts 2>&1 | tail -8</automated>
  </verify>
  <done>Ordering + floor invariants pass; the REPLAY_DIM<1 lock lives only in the D-12 file; viz-ambient-liveliness.test.ts stays green.</done>
  <acceptance_criteria>
    - `grep -c "REPLAY_DIM" tests/viz-ambient-liveliness.test.ts` still >0 (import + behavioral tests remain) but the `toBeLessThan(1)` source-parse assertion no longer lives there
    - The D-12 file asserts monotonic attack (LIVE<REPLAY<SPONT<TWINKLE), monotonic halo (LIVE>REPLAY>SPONT>TWINKLE), floors (≥0.6), and SPONT_DIM ≤ REPLAY_DIM < 1
    - Both test files pass; `npm test` shows no new failures
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client ↔ localhost SSE | unchanged; motion constants are client-render only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-05 | Tampering | SC3 ordering invariant | mitigate | Ordering moved to test-locked motion consts; a future retune that inverts salience fails the D-12 monotonic test before shipping |
| T-57-SC | Tampering | package installs | n/a | No installs (net-zero deps) |
</threat_model>

<verification>
- D-12 invariants (ordering + floors) pass on provisional values
- viz-ambient-liveliness.test.ts stays green after lock migration
- `npx tsc --noEmit` clean; `npm test` no new failures (2558-test suite stays green)
- Live re-home is pixel-equivalent (D-08) — no behavioral change to the live layer
</verification>

<success_criteria>
- Four named motion profiles with monotonic SC3 ordering on salient channels
- Dim factors floored ≥0.6; WR-02 + REPLAY_DIM<1 locks consolidated in the D-12 file
- Live values preserved pixel-equivalent
</success_criteria>

<output>
Create `.planning/phases/57-viz-activity-palette-redesign/57-04-SUMMARY.md` when done
</output>
