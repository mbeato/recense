---
phase: 57-viz-activity-palette-redesign
plan: 03
type: execute
wave: 3
depends_on: [57-02]
files_modified:
  - src/viz/modules/constants.js
  - tests/viz-activity-palette-invariants.test.ts
  - docs/superpowers/evidence/57-stage1-palette/
autonomous: false
requirements: [VIZ-PAL-01, VIZ-PAL-07]

must_haves:
  truths:
    - "The founder has seen every identity hue rendered over the real hull and signed off (or tuned) each"
    - "The founder-approved hues are the locked values in constants.js"
    - "The luminance band bounds are ratcheted to the approved palette (± tolerance)"
  artifacts:
    - path: "docs/superpowers/evidence/57-stage1-palette/"
      provides: "Captured screenshots of the identity palette over the hull — the durable Stage-1 approval record (D-15)"
    - path: "src/viz/modules/constants.js"
      provides: "Founder-approved (locked) identity hues replacing provisional ones"
    - path: "tests/viz-activity-palette-invariants.test.ts"
      provides: "Ratcheted luminance band bounds (D-09)"
  key_links:
    - from: "tests/viz-activity-palette-invariants.test.ts"
      to: "src/viz/modules/constants.js"
      via: "band bounds tightened to enclose the approved palette"
      pattern: "Y_MIN|Y_MAX"
---

<objective>
Stage-1 founder checkpoint (D-16): render every identity hue over the real hull, get the founder's hue sign-off BEFORE any motion-profile work builds on the palette, then ratchet the provisional hues and band bounds to the approved values (D-09).

Purpose: Color is the load-bearing decision this phase revises; the founder is the judge of hue taste (D-02/D-15). Locking the palette here means the motion-profile plans (57-04/57-06) build on a stable, approved color system rather than churning hues later. This is the first time viz approval leaves durable artifacts (D-15).

Output: Approved+locked hues in constants.js; ratcheted band in the invariants test; Stage-1 screenshots captured as the approval record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/57-viz-activity-palette-redesign/57-CONTEXT.md
@.planning/phases/57-viz-activity-palette-redesign/57-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dist and prepare the palette-on-hull render</name>
  <read_first>
    - scripts/copy-viz-assets.cjs (postbuild copy of modules/ into dist)
    - src/viz/modules/constants.js (provisional palette from 57-02)
    - package.json (build + viz launch scripts)
  </read_first>
  <action>
    Run the project build so the compiled server + copied modules reflect the 57-02 palette (`npm run build` or the documented build script, which runs tsc then copy-viz-assets). Confirm `dist/src/viz/modules/constants.js` contains the new eight-hue palette. Launch `recense viz` locally so the founder can view live. Prepare a way to exercise each activity kind over the hull (live recall, replay echo, spontaneous, ingestion kinds, twinkle) — reuse any existing trace-injection/demo path if one exists; otherwise document the exact steps the founder runs to trigger each kind. Do NOT auto-tune hues — that is the founder's call at the checkpoint.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && node -e "const s=require('fs').readFileSync('dist/src/viz/modules/constants.js','utf8'); if(!/replay:/.test(s))throw new Error('dist palette stale'); console.log('dist palette current')"</automated>
  </verify>
  <done>dist reflects the new palette; the viz is launchable and each activity kind is triggerable over the hull.</done>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `dist/src/viz/modules/constants.js` contains the replay identity hue and the retuned reconsolidation/oscillation/neutral hues
    - Written steps exist for triggering each of the eight identity kinds live
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task: Stage-1 founder checkpoint — palette hue sign-off</name>
  <action>BLOCKING human checkpoint (autonomous: false) — the founder reviews every identity hue rendered over the real hull and signs off, or supplies the approved hex per kind. The executor does NOT auto-tune hues; it presents the live render and waits for the resume signal, then Task 2 ratchets the approved values. See <what-built>/<how-to-verify>/<resume-signal>.</action>
  <what-built>The full luminance-equalized identity palette (eight hues incl. the new replay ice-cyan, retuned reconsolidation/oscillation/neutral) rendered live over the real hull, ready for founder hue sign-off before motion work builds on it.</what-built>
  <how-to-verify>
    1. Open the running `recense viz` in the browser.
    2. Trigger each activity kind in turn (steps from Task 1): live recall (amber-gold), replay echo (ice-cyan), spontaneous wander (lavender), new_node / reconsolidation / oscillation / neutral ingestion kinds, ambient twinkle.
    3. For EACH hue, confirm it reads as its intended identity over the dark additive hull and does not vanish. Confirm oscillation no longer looks amber (D-03b). Confirm replay reads as a cooler sibling of live cyan, distinguishable from live.
    4. Adjust any hue to taste — tell the executor the exact approved hex per kind (or "approved as-is").
    5. Capture a screenshot of each kind firing over the hull for the durable record.
  </how-to-verify>
  <resume-signal>Reply "approved: &lt;per-kind hexes or 'as-is'&gt;" or list the hues to change. This gate is a real founder decision — not auto-approvable.</resume-signal>
  <verify><human-check>Founder has viewed each of the eight identity hues over the hull and replied with approved hexes or "as-is".</human-check></verify>
  <done>Founder has signed off every identity hue (approved hexes or "as-is" captured) and per-kind screenshots taken; Task 2 ratchet can proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Ratchet approved hues + band bounds; store screenshots (D-09/D-15)</name>
  <read_first>
    - src/viz/modules/constants.js (provisional hues to replace with approved)
    - tests/viz-activity-palette-invariants.test.ts (provisional Y_MIN/Y_MAX to ratchet)
    - .planning/phases/57-viz-activity-palette-redesign/57-CONTEXT.md (D-09 ratchet rule, tolerance is Claude's discretion)
  </read_first>
  <action>
    Apply the founder-approved hex for each kind into constants.js KIND_COLOR (replace provisional values; keep the JSDoc, update it to record "founder-approved at Stage-1 57-03"). Ratchet the invariants band: set Y_MIN/Y_MAX to enclose the approved palette with a small documented tolerance (pick tolerance per discretion, e.g. ± a few Y units around the approved min/max), and update the comment from "PROVISIONAL" to "locked at Stage-1 57-03 (± tolerance)". Save the founder's per-kind screenshots into `docs/superpowers/evidence/57-stage1-palette/` (create the dir) with descriptive filenames (one per kind). Re-run the band invariant to confirm the approved palette passes the ratcheted band.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-activity-palette-invariants.test.ts -t "luminance-band" 2>&1 | tail -10 && ls docs/superpowers/evidence/57-stage1-palette/ | head</automated>
  </verify>
  <done>Approved hues locked in constants.js; band bounds ratcheted; Stage-1 screenshots stored.</done>
  <acceptance_criteria>
    - constants.js hues match the founder-approved hexes; JSDoc records "founder-approved at Stage-1 57-03"
    - Y_MIN/Y_MAX are ratcheted with a "locked at Stage-1" comment and the band test passes
    - `docs/superpowers/evidence/57-stage1-palette/` contains at least one screenshot per activity kind
    - `npm test` shows no new failures
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder ↔ local viz install | manual visual review on 127.0.0.1; no external surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-04 | Repudiation | palette approval | mitigate | Stage-1 screenshots captured as durable approval evidence (D-15) — the approval is recorded, not just verbal |
| T-57-SC | Tampering | package installs | n/a | No installs (net-zero deps) |
</threat_model>

<verification>
- Band invariant passes with ratcheted bounds enclosing the approved palette
- Screenshots stored under docs/superpowers/evidence/57-stage1-palette/
- `npm test` green (no regression)
</verification>

<success_criteria>
- Founder has signed off every identity hue over the real hull
- Approved hues + ratcheted band are locked in source and test
- Durable Stage-1 evidence exists
</success_criteria>

<output>
Create `.planning/phases/57-viz-activity-palette-redesign/57-03-SUMMARY.md` when done
</output>
