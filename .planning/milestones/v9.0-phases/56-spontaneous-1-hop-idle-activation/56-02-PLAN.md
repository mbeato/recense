---
phase: 56-spontaneous-1-hop-idle-activation
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - src/viz/server.ts
autonomous: true
requirements: [SPONT-01, SPONT-03, SPONT-04]
must_haves:
  truths:
    - "With an empty replay buffer and an elapsed idle gap, the viz server emits SSE `trace` events tagged kind:'spontaneous'"
    - "Every emitted spontaneous hop is a real PRED_SET kind='relation' out-edge of its live seed, carries its real src, and has score:null"
    - "Spontaneous stays silent while replayBuffer is non-empty (replay preempts) or live activity is within the idle gap (live preempts) — SC3 ordering by construction"
    - "The viz server writes no activation_trace row and opens no new Database() when emitting spontaneous — read-only/LLM-free/single-writer invariants hold"
  artifacts:
    - path: "src/viz/server.ts"
      provides: "eligible-seed pool builder, pickSpontaneousSeeds pure fn, spontaneous setInterval emitter, server-authoritative SPONT constants, teardown clearInterval"
      contains: "kind: 'spontaneous'"
  key_links:
    - from: "src/viz/server.ts"
      to: "src/retrieval/honest-trace.ts"
      via: "import buildHonestOneHopTrace + read-only reader over the readonly db handle"
      pattern: "buildHonestOneHopTrace\\("
    - from: "src/viz/server.ts spontaneous emitter"
      to: "replayBuffer / lastLiveRow"
      via: "gate replayBuffer.length === 0 && Date.now()-lastLiveRow >= idle gap"
      pattern: "replayBuffer\\.length === 0"
---

<objective>
Add a read-only, SSE-only spontaneous idle emitter to `src/viz/server.ts`, mirroring the Phase-54 replay scheduler exactly (`setInterval`, idle-gap gate, `.unref()`, teardown `clearInterval`, no new `Database()`, no LLM, no network). When the replay buffer is EMPTY and the idle gap has elapsed, it samples 2-3 distinct live seeds from a pre-filtered eligible pool, builds their honest 1-hop out-edge spread via the shared `buildHonestOneHopTrace` helper (Plan 01), and broadcasts an SSE `trace` event tagged `kind: 'spontaneous'` with `score:null` hops carrying their real `src`. It never writes an `activation_trace` row.

Purpose: Make the brain wander with genuinely-new real pathways even when there is nothing recent to replay (SPONT-01/03/04), without violating the single-writer, read-only/LLM-free viz invariants.
Output: `server.ts` with an eligible-seed pool, a `pickSpontaneousSeeds` pure function, server-authoritative SPONT constants, the spontaneous emitter interval, and its teardown.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-spontaneous-1-hop-idle-activation/56-CONTEXT.md
@.planning/phases/56-spontaneous-1-hop-idle-activation/56-01-SUMMARY.md
@src/retrieval/honest-trace.ts

<interfaces>
<!-- Extracted from codebase — implement against these; no exploration needed. -->

Shared helper (from Plan 01, src/retrieval/honest-trace.ts):
  export interface HonestTraceReader {
    getOutEdgesWithRel(id): Array<{ dst, rel, w, kind }>;
    getNode(id): { tombstoned: number } | undefined;
  }
  export function buildHonestOneHopTrace(
    seeds: Array<{ node_id: string; score: number }>,
    reader: HonestTraceReader,
    topN: number,
  ): { seeds; hops: Array<{ node_id, src, score: null, hop: 1 }> };

Phase-54 replay scheduler this mirrors (src/viz/server.ts:450-479):
  const replayInterval = setInterval(() => {
    if (clients.size === 0) return;
    if (replayBuffer.length === 0) return;
    if (Date.now() - lastLiveRow < REPLAY_IDLE_GAP_MS) return;
    ... res.write(`event: trace\ndata: ${JSON.stringify({..., replay: true})}\n\n`);
  }, REPLAY_CADENCE_MS);
  replayInterval.unref();

Closure state available in startViz scope: `db` (readonly Database), `clients: Set`, `replayBuffer: ReplayRow[]`, `lastLiveRow: number`, `REPLAY_IDLE_GAP_MS` (server.ts:65).
Server-authoritative replay constants precedent: server.ts:64-67 (REPLAY_IDLE_GAP_MS/REPLAY_CADENCE_MS/REPLAY_HISTORY_N, kept in sync with constants.js).
Teardown: server.ts:1215-1219 (server 'close' → clearInterval(pollInterval); clearInterval(replayInterval); db.close()).
DB handle: server.ts:186 — `const db = new Database(dbPath, { readonly: true });`
edge table columns: (src, dst, rel, w, last_access, kind). node column: tombstoned (0/1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-authoritative SPONT constants + eligible-seed pool + pickSpontaneousSeeds</name>
  <read_first>
    - src/viz/server.ts:60-67 (POLL_MS / REPLAY_* server-authoritative constant block — add SPONT constants here in the same style)
    - src/viz/server.ts:186 (readonly db handle) and src/viz/server.ts:389-397 (replayBuffer / lastLiveRow closure state)
    - src/db/semantic-store.ts:216-220 (edge table columns) and src/model/typed-predicates.ts:35 (PRED_SET — the pool must require ≥1 PRED_SET edge per seed)
  </read_first>
  <action>
    In `src/viz/server.ts` add a server-authoritative SPONT constant block next to the REPLAY constants (~line 67), with a "keep in sync with constants.js" comment mirroring the REPLAY precedent: `SPONT_CADENCE_MS` (idle broadcast cadence, starting 2500), `SPONT_SEED_COUNT` (starting 3 — the 2-3 distinct-seeds density tunable, D-03), `SPONT_HOP_TOPN` (starting 6, reuse the AMBIENT top-N density), and `SPONT_POOL_REFRESH_MS` (how often the eligible pool is rebuilt, starting 60000). Reuse `REPLAY_IDLE_GAP_MS` as the idle signal (D-01a) — do NOT add a second idle constant. Inside `startViz` (after the replay scheduler), add a read-only eligible-seed pool: prepare a single `SELECT DISTINCT e.src, e.rel FROM edge e JOIN node n ON n.id = e.src WHERE e.kind = 'relation' AND n.tombstoned = 0` statement over the readonly `db`, run it, filter rows to `PRED_SET.has(rel)` in JS, and collect the distinct `src` ids into a `spontaneousPool: string[]` (guarantees every seed has ≥1 honest edge — no dud ticks, D-02). Cache the pool with a `poolBuiltAt` timestamp and rebuild lazily at most once per `SPONT_POOL_REFRESH_MS`. Add a pure exported (for the guard test) helper `pickSpontaneousSeeds(pool: string[], count: number, rng: () => number): Array<{ node_id: string; score: number }>` that samples `count` DISTINCT ids uniformly (score arbitrary passthrough — the helper sets hop score:null; use 0 or a fixed value), returning fewer only when the pool is smaller than count. Import `PRED_SET` from `../model/typed-predicates`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "server.ts" | grep -qx 0 && echo TSC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "SPONT_CADENCE_MS|SPONT_SEED_COUNT|SPONT_HOP_TOPN|SPONT_POOL_REFRESH_MS" src/viz/server.ts` shows all four constants defined server-side.
    - The pool query is a single SELECT (no INSERT/UPDATE/DELETE); `grep -nE "INSERT|UPDATE|DELETE" src/viz/server.ts` shows no new write statement introduced by this task.
    - `pickSpontaneousSeeds` is exported and returns DISTINCT ids (seeds within a tick are distinct — D-03).
    - The pool rows are filtered by `PRED_SET.has` so every eligible seed has ≥1 semantic edge.
    - `tsc` clean.
  </acceptance_criteria>
  <done>SPONT server constants exist, the eligible pool builds read-only from PRED_SET semantic edges of live nodes, and pickSpontaneousSeeds samples distinct seeds deterministically given an rng.</done>
</task>

<task type="auto">
  <name>Task 2: Spontaneous SSE emitter interval (gated, read-only, unref'd, torn down)</name>
  <read_first>
    - src/viz/server.ts:450-479 (replay scheduler — copy structure exactly, swap row source for live-seed spontaneous hops, gate additionally on replayBuffer.length===0)
    - src/viz/server.ts:1213-1220 (server 'close' teardown — add clearInterval(spontaneousInterval))
    - src/retrieval/honest-trace.ts (buildHonestOneHopTrace + HonestTraceReader from Plan 01)
  </read_first>
  <action>
    In `startViz`, after `replayInterval.unref()`, build a read-only `HonestTraceReader` over the existing readonly `db` handle: two prepared SELECTs — `getOutEdgesWithRel(id)` → `SELECT dst, rel, w, kind FROM edge WHERE src = ?`, and `getNode(id)` → `SELECT tombstoned FROM node WHERE id = ?` (return `{ tombstoned }` or undefined). Do NOT open a new `Database()` and do NOT construct a write-capable SemanticStore. Add `const spontaneousInterval = setInterval(...)` with cadence `SPONT_CADENCE_MS` whose body, in order, returns early when: `clients.size === 0`; `replayBuffer.length !== 0` (replay owns the idle gap — D-01 fallback-only); `Date.now() - lastLiveRow < REPLAY_IDLE_GAP_MS` (live preempts — SC3). Then refresh the pool if stale, `pickSpontaneousSeeds(spontaneousPool, SPONT_SEED_COUNT, Math.random)`, call `buildHonestOneHopTrace(seeds, reader, SPONT_HOP_TOPN)`, and if `hops.length > 0` broadcast an SSE `event: trace` payload `{ seeds, hops, kind: 'spontaneous' }` to every client via `res.write` — no `id`/`ts`/`query_id`, no `replay` flag, no DB row written. Call `spontaneousInterval.unref()`. Add `clearInterval(spontaneousInterval)` to the server 'close' handler alongside the replay/poll clears. This emitter must never touch `cursor` and never write.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "server.ts" | grep -qx 0 && echo TSC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "kind: 'spontaneous'" src/viz/server.ts` shows the SSE wire tag on the emitted payload.
    - `grep -n "replayBuffer.length === 0\|replayBuffer.length !== 0" src/viz/server.ts` shows the fallback-only gate (D-01) in the spontaneous body.
    - `grep -n "REPLAY_IDLE_GAP_MS" src/viz/server.ts` shows the shared idle-gap gate reused (D-01a) in the spontaneous body.
    - `grep -n "spontaneousInterval.unref()" src/viz/server.ts` and `grep -c "clearInterval(spontaneousInterval)" src/viz/server.ts` (== 1) prove unref + teardown.
    - `grep -nE "new Database|INSERT INTO activation_trace|\\.run\\(" src/viz/server.ts` shows NO new Database() and NO write introduced by the emitter (reader uses only prepared SELECT `.all`/`.get`).
    - The emitter imports and calls `buildHonestOneHopTrace` (no reimplemented filter — grep proves single source).
    - `tsc` clean; existing viz-ambient-liveliness tests still pass: `npx vitest run tests/viz-ambient-liveliness.test.ts`.
  </acceptance_criteria>
  <done>A gated, read-only, unref'd, torn-down spontaneous interval emits SSE kind:'spontaneous' events with honest hops only when the replay buffer is empty and the idle gap elapsed; no DB write, no new Database().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viz server → DB | Read-only handle (`readonly: true`); the emitter must not become a second writer. |
| viz server → SSE clients | Outbound event stream; payload must carry only real edges + score:null (no fabrication). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-02 | Tampering/Elevation | spontaneous emitter → DB | mitigate | db opened readonly:true; reader uses only prepared SELECT .all/.get; no new Database(); grep gate asserts no INSERT/UPDATE/DELETE/.run in the emitter path; single-writer invariant preserved. |
| T-56-03 | Spoofing/Info | SSE payload honesty | mitigate | Hops sourced only via shared buildHonestOneHopTrace (PRED_SET + kind='relation' + liveness); score:null always; pool requires a real PRED_SET edge so no dud/fabricated tick. Cross-checked by Plan 04 guard. |
| T-56-04 | Denial of Service | pool build read | mitigate | Pool is a single bounded DISTINCT query, cached and rebuilt at most once per SPONT_POOL_REFRESH_MS (not per tick); seed count and top-N bounded constants. |
| T-56-05 | Denial of Service | interval leak | mitigate | spontaneousInterval.unref() + clearInterval on server 'close' (mirrors replayInterval). |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — package-legitimacy gate N/A. |

No high-severity threats: no new network input is accepted, no writer is added, and no new Database() handle is opened.
</threat_model>

<verification>
- `tsc --noEmit` clean.
- viz-ambient-liveliness test suite green (replay/twinkle layers not regressed).
- Grep gates: `kind: 'spontaneous'` present; `replayBuffer.length` gate present; `.unref()` + teardown present; no new Database() / no write in emitter.
</verification>

<success_criteria>
The viz server emits honest spontaneous 1-hop SSE events only in the empty-replay-buffer idle window, is provably read-only (no new Database, no write), reuses the shared honest builder, and cleans up its interval on close.
</success_criteria>

<output>
Create `.planning/phases/56-spontaneous-1-hop-idle-activation/56-02-SUMMARY.md` when done.
</output>
