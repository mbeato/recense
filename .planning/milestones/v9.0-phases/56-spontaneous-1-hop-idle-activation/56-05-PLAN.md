---
phase: 56-spontaneous-1-hop-idle-activation
plan: 05
type: execute
wave: 4
depends_on: ["56-02", "56-03", "56-04"]
files_modified:
  - src/viz/modules/constants.js
  - src/viz/server.ts
autonomous: false
requirements: [SPONT-06]
must_haves:
  truths:
    - "The founder confirms spontaneous density, hue, cadence, and dim read correctly on screen during an idle session with an empty replay buffer (SC1/SC4)"
    - "Any tuned constant still honors SPONT_DIM < REPLAY_DIM and keeps the server-authoritative copy in sync with constants.js"
  artifacts:
    - path: "src/viz/modules/constants.js"
      provides: "founder-tuned spontaneous hue / SPONT_DIM / density (if adjusted)"
      contains: "SPONT_DIM"
  key_links:
    - from: "constants.js SPONT constants"
      to: "server.ts SPONT_CADENCE_MS / SPONT_SEED_COUNT / SPONT_HOP_TOPN"
      via: "server-authoritative copies kept in sync after tuning"
      pattern: "SPONT_CADENCE_MS"
---

<objective>
Founder visual checkpoint (D-06, mirrors Phase-54 Plan-05): with everything wired (emitter + client render + guard), the founder views the live `recense viz` during an idle session and confirms the spontaneous default-mode wandering reads correctly — distinct dim-indigo hue, calm cadence, density that fills idle emptiness without clutter, strictly subordinate to replay. Any tuning is applied to the named constants (client `constants.js` + the server-authoritative copies in `server.ts`), preserving `SPONT_DIM < REPLAY_DIM` and cross-file sync.

Purpose: The numbers only matter on screen (D-05/D-06) — this closes SC1/SC4 and finalizes the density tunables (SPONT-06).
Output: Founder-approved (and possibly tuned) spontaneous constants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-spontaneous-1-hop-idle-activation/56-CONTEXT.md
@.planning/phases/56-spontaneous-1-hop-idle-activation/56-02-SUMMARY.md
@.planning/phases/56-spontaneous-1-hop-idle-activation/56-03-SUMMARY.md
@src/viz/modules/constants.js
@src/viz/server.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Founder confirms spontaneous density / hue / cadence / dim on screen</name>
  <read_first>
    - src/viz/modules/constants.js (SPONT_DIM, SPONT_CADENCE_MS, SPONT_HOP_TOPN, KIND_COLOR.spontaneous — the client tunables)
    - src/viz/server.ts:64-67 area (SPONT server-authoritative constants — SPONT_CADENCE_MS / SPONT_SEED_COUNT / SPONT_HOP_TOPN — keep in sync when tuning)
  </read_first>
  <what-built>
    A read-only SSE-only spontaneous idle emitter that, when the replay buffer is empty and the idle gap has elapsed, fires genuinely-new real 1-hop semantic spreads (2-3 seeds × top-N honest out-edges) in a dim indigo/violet "default-mode" hue under kind:'spontaneous'. The tray stays at rest on it; the honesty guard (SPONT-06) is green; no activation_trace row is written.
  </what-built>
  <how-to-verify>
    1. Start the viz: run `recense viz` (or the project's viz launch command) and open the reader URL it prints.
    2. Leave the brain idle with an EMPTY replay buffer (fresh session / no recent recalls) past the idle gap (~5s). Confirm spontaneous 1-hop pathways appear in the distinct dim indigo/violet hue — clearly NOT recall amber and NOT replay cyan (SC1).
    3. Confirm the density feels alive (fills the idle emptiness) without reading busy/cluttered; confirm cadence feels calm; confirm spontaneous reads strictly dimmer/subordinate to a replay echo (trigger a recall first to seed replay, then compare).
    4. Confirm the tray icon does NOT pulse during spontaneous activity, but DOES pulse on a live recall and on a sleep-pass row (SC3).
    5. If any of hue / cadence (SPONT_CADENCE_MS) / dim (SPONT_DIM) / density (SPONT_SEED_COUNT / SPONT_HOP_TOPN) needs adjustment, state the new values. The executor edits both the client `constants.js` and the server-authoritative `server.ts` copies together, keeping SPONT_DIM < REPLAY_DIM, then you re-verify.
  </how-to-verify>
  <acceptance_criteria>
    - Founder confirms spontaneous pathways render in the distinct default-mode hue with an empty replay buffer (SC1).
    - Founder confirms density/cadence/dim feel right (SC4); spontaneous reads subordinate to replay (SC3).
    - Founder confirms the tray stays at rest on spontaneous while live/sleep-pass still pulse (SC3).
    - If tuned: `SPONT_DIM < REPLAY_DIM` still holds, and the server-authoritative SPONT constants in server.ts match the constants.js values (cross-file sync).
  </acceptance_criteria>
  <resume-signal>Type "approved" or state the exact hue / SPONT_DIM / SPONT_CADENCE_MS / SPONT_SEED_COUNT / SPONT_HOP_TOPN values to apply.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder tuning → constants | Only tunes presentation magnitudes; no logic/honesty change. The SPONT_DIM < REPLAY_DIM invariant bounds the tuning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-10 | Tampering | tuning breaks SPONT_DIM < REPLAY_DIM or cross-file sync | mitigate | Acceptance criterion re-asserts the invariant; server-authoritative copy kept in sync with constants.js (same requirement as REPLAY constants). |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | No package installs — package-legitimacy gate N/A. |

No high-severity threats: presentation tuning only; no writer, no new input, no new DB handle.
</threat_model>

<verification>
- Founder sign-off recorded in the SUMMARY.
- If tuned: `node -e "import('./src/viz/modules/constants.js').then(m=>{if(!(m.SPONT_DIM<m.REPLAY_DIM))throw 0;console.log('OK')})"` and a grep confirming server.ts SPONT constants match constants.js.
</verification>

<success_criteria>
The founder confirms the spontaneous default-mode wandering reads correctly on screen (SC1/SC4), spontaneous is subordinate to replay (SC3), the tray stays honest, and any tuned constants preserve SPONT_DIM < REPLAY_DIM with server/client copies in sync.
</success_criteria>

<output>
Create `.planning/phases/56-spontaneous-1-hop-idle-activation/56-05-SUMMARY.md` when done.
</output>
