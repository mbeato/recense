---
phase: 56-spontaneous-1-hop-idle-activation
plan: 04
type: execute
wave: 3
depends_on: ["56-01", "56-02"]
files_modified:
  - tests/spontaneous-idle-activation.test.ts
autonomous: true
requirements: [SPONT-06]
must_haves:
  truths:
    - "A deterministic test proves every emitted spontaneous hop (src→dst) is a real kind='relation' PRED_SET out-edge of its seed in the store, and dst is live"
    - "A test proves the spontaneous emitter writes no activation_trace row and opens no new Database() — read-only/single-writer preserved (D-10/D-11)"
    - "Hops carry score:null and their real src seed (no fabricated magnitude, no guessed attribution)"
  artifacts:
    - path: "tests/spontaneous-idle-activation.test.ts"
      provides: "SPONT-06 honesty guard (edge cross-check) + no-DB-write guard, deterministic via injected rng / fixed pool"
      exports: []
      min_lines: 40
  key_links:
    - from: "tests/spontaneous-idle-activation.test.ts"
      to: "pickSpontaneousSeeds + buildHonestOneHopTrace"
      via: "deterministic seed pick → honest hops cross-checked against getOutEdgesWithRel"
      pattern: "buildHonestOneHopTrace|pickSpontaneousSeeds"
---

<objective>
Add the SPONT-06 machine guard (the regression lock, D-10): a deterministic test that (1) cross-checks every emitted spontaneous hop against the store — each `(src→dst)` is a real `kind='relation'` PRED_SET out-edge of its seed and `dst` is live, hop `score:null` — and (2) proves the spontaneous path writes no `activation_trace` row and opens no new `Database()`. Determinism comes from a fixed eligible pool + injected RNG into `pickSpontaneousSeeds` (Plan 02) so the run is reproducible (D-10 discretion).

Purpose: Lock the honesty invariant (no fabricated edge, no fabricated magnitude) and the read-only/single-writer invariant (D-11 replay-ring exclusion is automatic because no row is written) against regression.
Output: `tests/spontaneous-idle-activation.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-spontaneous-1-hop-idle-activation/56-CONTEXT.md
@.planning/phases/56-spontaneous-1-hop-idle-activation/56-02-SUMMARY.md
@src/retrieval/honest-trace.ts
@src/viz/server.ts

<interfaces>
<!-- Test seams from Plans 01/02 + the Phase-55 guard style to mirror. -->

From src/retrieval/honest-trace.ts (Plan 01):
  buildHonestOneHopTrace(seeds, reader, topN) → { seeds, hops: [{node_id, src, score:null, hop:1}] }
From src/viz/server.ts (Plan 02):
  pickSpontaneousSeeds(pool: string[], count: number, rng: () => number) → Array<{node_id, score}>
  (the eligible pool is distinct src of live kind='relation' PRED_SET edges)

Phase-55 guard style to mirror (tests/activation-trace-wiring.test.ts:435-460):
  - builds a fixture store, calls the honest builder, asserts every hop.kind/rel via PRED_SET.has,
    tombstoned dst absent, top-N cap, score:null, hop:1.
Test helpers: makeRetrievalDeps(db) pattern in tests/activation-trace-wiring.test.ts (seed fixture nodes + edges).
PRED_SET: src/model/typed-predicates.ts. edge columns (src,dst,rel,w,last_access,kind); node.tombstoned.
Test runner: `vitest run`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Honesty guard — every spontaneous hop is a real live PRED_SET out-edge (deterministic)</name>
  <read_first>
    - tests/activation-trace-wiring.test.ts:435-475 (Phase-55 guard fixture + assertion style to mirror)
    - src/retrieval/honest-trace.ts (buildHonestOneHopTrace) and src/viz/server.ts (pickSpontaneousSeeds seam)
    - src/model/typed-predicates.ts:35 (PRED_SET)
  </read_first>
  <action>
    Create `tests/spontaneous-idle-activation.test.ts`. Build an in-memory fixture DB with: seed nodes that each have ≥1 PRED_SET `kind='relation'` out-edge to LIVE dsts, at least one structural `extends`/`links_to` edge (must be excluded), at least one PRED_SET edge to a TOMBSTONED dst (must be excluded), and at least one seed with >SPONT_HOP_TOPN live semantic edges (top-N truncation). Construct a read-only reader over the fixture (same `{getOutEdgesWithRel, getNode}` shape the server builds) and a fixed eligible pool. Call `pickSpontaneousSeeds(pool, count, rng)` with an INJECTED deterministic rng (e.g. a fixed sequence) so the picked seeds are reproducible, then `buildHonestOneHopTrace(seeds, reader, SPONT_HOP_TOPN)`. Assert: every hop's `(src→dst)` is present in `reader.getOutEdgesWithRel(hop.src)` with `kind==='relation'` and `PRED_SET.has(rel)`; every hop `dst` node is live (`getNode(dst).tombstoned !== 1`); no hop references the excluded structural or tombstoned targets; every `hop.score` is `null` and `hop.hop === 1`; every `hop.src` equals one of the picked seed ids (real attribution, not a guessed seeds[0]); picked seeds are distinct.
  </action>
  <verify>
    <automated>npx vitest run tests/spontaneous-idle-activation.test.ts -t "honest"</automated>
  </verify>
  <acceptance_criteria>
    - The test cross-checks each hop against `getOutEdgesWithRel(hop.src)` (real-edge assertion, not a trust-the-payload check).
    - Structural (`extends`/`links_to`) and tombstoned-dst edges are asserted ABSENT from hops.
    - Every hop asserts `score === null`, `hop === 1`, and `src` ∈ picked seed ids.
    - The run is deterministic (injected rng / fixed pool) — re-running produces identical seeds/hops.
    - Test passes: exit 0.
  </acceptance_criteria>
  <done>A deterministic test proves every spontaneous hop is a real live PRED_SET out-edge of its seed with score:null and honest src.</done>
</task>

<task type="auto">
  <name>Task 2: No-DB-write guard + read-only static gate (D-10/D-11)</name>
  <read_first>
    - src/viz/server.ts (the spontaneous emitter path + readonly db handle; teardown)
    - CLAUDE.md (project — Consolidator is SOLE graph writer; viz is read-only + fire-and-forget)
  </read_first>
  <action>
    In the same test file, add a no-write guard: open a fixture DB, record the `activation_trace` row count (and max id), exercise the spontaneous emit path (either by starting the viz server with the idle gap forced elapsed and an empty replay buffer so a spontaneous tick fires, or by directly invoking the extracted emit-payload construction against the reader — whichever seam Plan 02 exposed), then assert the `activation_trace` row count and max id are UNCHANGED (no row written — SPONT-03/D-11: replay-ring exclusion is automatic because nothing is written). Add a static read-only gate asserting the emitter source introduces no writer: read `src/viz/server.ts` as text and assert the spontaneous emitter region contains no `new Database(`, no `INSERT INTO activation_trace`, and the reader uses only prepared SELECT reads. If the server is started in-test, ensure it is closed in an afterEach so `clearInterval(spontaneousInterval)` + `db.close()` run (no interval leak).
  </action>
  <verify>
    <automated>npx vitest run tests/spontaneous-idle-activation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - After a spontaneous emit, `SELECT COUNT(*) FROM activation_trace` and `MAX(id)` are unchanged vs. before (no DB write).
    - A static assertion proves the spontaneous path opens no `new Database(` and writes no `activation_trace` row.
    - If the server is started, it is torn down in afterEach (no leaked interval / open handle).
    - Full test file passes: exit 0.
  </acceptance_criteria>
  <done>Tests prove the spontaneous emitter writes no activation_trace row and opens no new Database(), locking the read-only/single-writer invariant (D-11 exclusion holds by construction).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → engine/viz internals | Guard exercises the real emit path against a fixture store; no production data touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-08 | Repudiation/Tampering | honesty regression slips through | mitigate | This test IS the regression lock — real-edge cross-check + no-write assertion; runs in CI (vitest run). |
| T-56-09 | Denial of Service | leaked interval/db handle in test | mitigate | afterEach closes any started server (clearInterval + db.close); prefer the pure emit-payload seam over a live server where possible. |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | No package installs — package-legitimacy gate N/A. |

No high-severity threats: test-only additions, no production behavior change.
</threat_model>

<verification>
- `npx vitest run tests/spontaneous-idle-activation.test.ts` exits 0.
- Honesty guard cross-checks hops against the store; no-write guard asserts activation_trace unchanged.
</verification>

<success_criteria>
SPONT-06 is machine-verified: every spontaneous hop is a real live PRED_SET out-edge with score:null, and the emitter provably writes no activation_trace row / opens no new Database().
</success_criteria>

<output>
Create `.planning/phases/56-spontaneous-1-hop-idle-activation/56-04-SUMMARY.md` when done.
</output>
