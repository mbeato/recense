---
phase: 56-spontaneous-1-hop-idle-activation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/viz/modules/constants.js
  - src/viz/modules/trace.js
  - apps/tray/src/tray-icon.ts
autonomous: true
requirements: [SPONT-02, SPONT-05]
must_haves:
  truths:
    - "The client renders kind:'spontaneous' traces as a seed→hop 1-hop pathway in a dim indigo/violet default-mode hue, distinct from recall amber and replay cyan"
    - "Spontaneous renders strictly subordinate to replay — SPONT_DIM < REPLAY_DIM (0.4) is a checkable invariant"
    - "The tray icon does NOT pulse on kind:'spontaneous' events (idle, not live), while a malformed trace still pulses"
  artifacts:
    - path: "src/viz/modules/constants.js"
      provides: "SPONT named constants (KIND_COLOR.spontaneous hue, SPONT_DIM, SPONT_CADENCE_MS, SPONT_HOP_TOPN) mirroring the Phase-54/55 layer-constants block"
      contains: "SPONT_DIM"
    - path: "src/viz/modules/trace.js"
      provides: "spontaneous render branch + pre-dimmed SPONT_HOP_COLOR"
      contains: "spontaneous"
    - path: "apps/tray/src/tray-icon.ts"
      provides: "pulse suppression extended to kind==='spontaneous'"
      contains: "spontaneous"
  key_links:
    - from: "src/viz/modules/trace.js applyTrace"
      to: "KIND_COLOR.spontaneous / SPONT_DIM"
      via: "spontaneous branch renders seed→hop pathway at SPONT_DIM in the indigo hue"
      pattern: "spontaneous"
    - from: "apps/tray/src/tray-icon.ts trace listener"
      to: "kind==='spontaneous'"
      via: "suppress pulse (return) same as replay===true"
      pattern: "spontaneous"
---

<objective>
Render the Phase-56 spontaneous wire event on the client: add the named "default-mode" constants (dim indigo/violet hue, `SPONT_DIM`, cadence, density) to `constants.js` with the hard invariant `SPONT_DIM < REPLAY_DIM`; add a `kind:'spontaneous'` render branch to `trace.js` that draws the honest seed→hop 1-hop pathway in the dim indigo hue (subordinate to replay); and extend the tray-icon pulse-suppression to `kind==='spontaneous'` so the icon stays at rest during idle wandering (SPONT-02, SPONT-05).

Purpose: Make spontaneous activity visually unmistakable (never mistaken for a live query result) and keep the tray honest about liveness. Builds against the wire contract `{ seeds, hops: [{node_id, src, score:null, hop:1}], kind:'spontaneous' }` defined in this plan — no dependency on the server plan's files.
Output: SPONT constants, a spontaneous render branch, and tray suppression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-spontaneous-1-hop-idle-activation/56-CONTEXT.md

<interfaces>
<!-- Wire contract (defined by this phase) + existing render patterns to mirror. -->

Spontaneous SSE event shape (the render target):
  { seeds: Array<{node_id, score}>, hops: Array<{node_id, src, score: null, hop: 1}>, kind: 'spontaneous' }
  (no `replay` flag, no id/ts/query_id)

constants.js layer-constants block to extend (src/viz/modules/constants.js:336-361):
  export const REPLAY_DIM = 0.4;              // spontaneous MUST be < this
  export const KIND_COLOR = { recall_seed: 0xffb866, recall_hop: 0x66d9ff, ... };  // ~line 148

trace.js render patterns to mirror (src/viz/modules/trace.js):
  - applyTrace(row) dispatch: `if (row.replay === true) {...replay branch...}` (line 612) THEN
    `if (kind && kind !== 'recall') { _applyIngestion(row, kind); return; }` (line 685) THEN recall path.
    NOTE: kind:'spontaneous' would fall into _applyIngestion by default — the new branch must intercept it BEFORE that dispatch.
  - Replay branch (612-682) is the structural template: resolve seeds via normalizeSeed, hops via traceEdgesFromHops, activate(node, intensity * DIM, color), spawnPulse(srcNode, node, HOP_COLOR), fade via setTimeout.
  - REPLAY_HOP_COLOR construction (247-257): pre-dim a hex by multiplying each channel by the DIM factor and rebuilding the int (works under the minimal test colour stub — do NOT use multiplyScalar).
  - KIND_COLORS map (236-245): per-kind THREE.Color built once at init.

tray-icon.ts suppress block to extend (apps/tray/src/tray-icon.ts:154-170) — the git-tracked TypeScript SOURCE that `tsc` compiles to dist/ (the app runs `dist/src/main.js` per package.json "main"):
  es.addEventListener('trace', (e: MessageEvent) => {
    isDim = false;
    let isReplay = false;
    try { isReplay = JSON.parse(e.data)?.replay === true; } catch { isReplay = false; }
    if (isReplay) { log('replay trace event received — not pulsing ...'); return; }
    log('trace event received — pulsing icon'); pulse();
  });
  NOTE: there is a stray untracked `apps/tray/tray-icon.js` at the app root that is byte-identical to compiled `dist/src/tray-icon.js` — it is an orphaned build artifact sitting outside gitignored `dist/`. It is NOT the source, editing it does nothing (tsc regenerates dist from the .ts), and it MUST NOT be edited or `git add`-ed. Flagged for founder deletion (see Task 3 note).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SPONT named constants + default-mode hue (SPONT_DIM < REPLAY_DIM)</name>
  <read_first>
    - src/viz/modules/constants.js:336-361 (Phase-54/55 layer-constants block — add the SPONT block here in the same doc-commented style)
    - src/viz/modules/constants.js:134-152 (KIND_COLOR map — add the `spontaneous` hue alongside recall_seed/recall_hop)
  </read_first>
  <action>
    In `src/viz/modules/constants.js`, add a "Layer 4 — spontaneous default-mode" constants block after the twinkle block (~line 361): `SPONT_DIM` (starting value strictly less than REPLAY_DIM=0.4, e.g. 0.3 — this is the hard SC3 invariant), `SPONT_CADENCE_MS` (starting 2500, comment "keep in sync with server.ts SPONT_CADENCE_MS — server is authoritative"), and `SPONT_HOP_TOPN` (starting 6). Add `spontaneous:` to the `KIND_COLOR` map (~line 148) with a dim indigo/violet hex (starting direction ~0x8a7fff / muted violet — NOT amber 0xffb866, NOT cyan 0x66d9ff), doc-commented as the default-mode-network wandering hue tuned at the founder checkpoint. Values are starting directions only (D-04/D-06 — tuned on screen).
  </action>
  <verify>
    <automated>node -e "import('./src/viz/modules/constants.js').then(m=>{if(!(m.SPONT_DIM<m.REPLAY_DIM))throw new Error('SPONT_DIM must be < REPLAY_DIM');if(!m.KIND_COLOR.spontaneous)throw new Error('missing spontaneous hue');if(m.KIND_COLOR.spontaneous===0xffb866||m.KIND_COLOR.spontaneous===0x66d9ff)throw new Error('spontaneous hue must differ from amber/cyan');console.log('CONSTANTS_OK')})"</automated>
  </verify>
  <acceptance_criteria>
    - `constants.js` exports `SPONT_DIM`, `SPONT_CADENCE_MS`, `SPONT_HOP_TOPN`.
    - `SPONT_DIM < REPLAY_DIM` (0.4) — asserted by the verify command (hard invariant, SC3).
    - `KIND_COLOR.spontaneous` exists and is distinct from `0xffb866` (amber) and `0x66d9ff` (cyan).
  </acceptance_criteria>
  <done>SPONT constants + default-mode hue exist, distinct from recall/replay colors, with SPONT_DIM strictly below REPLAY_DIM.</done>
</task>

<task type="auto">
  <name>Task 2: trace.js spontaneous render branch (dim indigo seed→hop pathway)</name>
  <read_first>
    - src/viz/modules/trace.js:602-700 (applyTrace: the replay branch template + the kind dispatch that must be intercepted before _applyIngestion)
    - src/viz/modules/trace.js:236-257 (KIND_COLORS map + REPLAY_HOP_COLOR pre-dim construction to mirror for SPONT_HOP_COLOR)
    - src/viz/modules/constants.js (SPONT_DIM, KIND_COLOR.spontaneous from Task 1)
  </read_first>
  <action>
    In `src/viz/modules/trace.js`: import `SPONT_DIM` and `KIND_COLOR.spontaneous` (via the existing constants import). At init (near line 245-257), add `KIND_COLORS.spontaneous = new THREE.Color(KIND_COLOR.spontaneous)` and build a pre-dimmed `SPONT_HOP_COLOR` by scaling each channel of the indigo hex by `SPONT_DIM` and rebuilding the int (mirror the REPLAY_HOP_COLOR bit-shift construction exactly — no multiplyScalar, so the test colour stub works). In `applyTrace`, add a spontaneous branch that intercepts `row.kind === 'spontaneous'` BEFORE the `if (kind && kind !== 'recall')` ingestion dispatch (place it right after the replay branch). The branch reuses the replay-branch structure (resolve seeds via normalizeSeed, hops via traceEdgesFromHops, markAnimating, revealTrace, fade timeout) but activates seeds and hops at `intensity * SPONT_DIM` in `KIND_COLORS.spontaneous`, and draws seed→hop pathway pulses with `SPONT_HOP_COLOR` only when `srcNode` is present (honest src edge). score:null hops fall back to the existing mid-intensity path (WR-02). Return at the end of the branch so it never reaches _applyIngestion.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-ambient-liveliness.test.ts tests/trace-honest-recall.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "kind === 'spontaneous'\|'spontaneous'" src/viz/modules/trace.js` shows the intercept branch placed BEFORE the `kind && kind !== 'recall'` ingestion dispatch.
    - `grep -n "SPONT_HOP_COLOR\|SPONT_DIM\|KIND_COLORS.spontaneous" src/viz/modules/trace.js` shows the dim-indigo activation + pre-dimmed pulse color.
    - Existing replay/recall render tests still pass (spontaneous branch does not regress the shared applyTrace path).
    - The branch activates at `* SPONT_DIM` (strictly dimmer than replay's `* REPLAY_DIM`).
  </acceptance_criteria>
  <done>kind:'spontaneous' renders as a dim-indigo seed→hop pathway subordinate to replay, intercepted before the ingestion dispatch, with existing render tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Tray-icon pulse suppression on kind==='spontaneous'</name>
  <read_first>
    - apps/tray/src/tray-icon.ts:154-172 (the git-tracked TS SOURCE trace event listener + the `isReplay = JSON.parse(e.data)?.replay === true` suppress block to extend — line 154 `es.addEventListener('trace', ...)`, 165 the parse, 169 `if (isReplay) { ...; return; }`)
    - apps/tray/package.json ("main": "dist/src/main.js" — confirms the app runs the tsc-compiled output of this .ts, NOT the stray root .js)
  </read_first>
  <action>
    In `apps/tray/src/tray-icon.ts` trace listener (~line 154-175), extend the existing idle-suppression branch: after the `isReplay = JSON.parse(e.data)?.replay === true` parse, also derive whether the trace is spontaneous (e.g. `const parsed = JSON.parse(e.data); parsed?.kind === 'spontaneous'`, done inside the same try so a parse throw leaves both false). Suppress the pulse (log + `return` without calling `pulse()`) when EITHER `replay === true` OR `kind === 'spontaneous'` — the same "idle, not live" rule as replay (D-09). PRESERVE the current fail-toward-liveness behavior: a malformed/unparseable trace (parse throws → both flags false) still falls through to `pulse()`. Do not change the `isDim = false` liveness clear at the top of the listener (any received trace, of any kind, still proves the server alive). Edit ONLY the git-tracked `.ts` source — `tsc` regenerates `dist/`. Do NOT touch the stray untracked `apps/tray/tray-icon.js`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('apps/tray/src/tray-icon.ts','utf8'); if(!/kind\s*===\s*'spontaneous'/.test(s))throw new Error('no spontaneous suppress in TS source'); if(!/isDim\s*=\s*false/.test(s))throw new Error('liveness clear removed'); if(!/replay\s*===\s*true/.test(s))throw new Error('replay suppress lost'); console.log('TRAY_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "kind === 'spontaneous'" apps/tray/src/tray-icon.ts` shows the suppress condition in the git-tracked TypeScript source (NOT the stray root .js).
    - The suppression path `return`s without calling `pulse()`; the parse-failure path still calls `pulse()` (fail toward liveness — unchanged).
    - `isDim = false` liveness clear and the existing `replay === true` suppress are both retained.
    - The stray untracked `apps/tray/tray-icon.js` is left unedited and unstaged.
  </acceptance_criteria>
  <note>
    STRAY FILE — FLAG FOR FOUNDER: `apps/tray/tray-icon.js` (repo root of the tray app) is an accidental, untracked build artifact — byte-identical to compiled `dist/src/tray-icon.js` but sitting outside the gitignored `dist/` dir. It is not the source, editing it is a no-op (the app runs the tsc output of `src/tray-icon.ts` per package.json `"main": "dist/src/main.js"`), and it must NOT be `git add`-ed. Out of scope for this task's edits — recommend the founder delete it during cleanup.
  </note>
  <done>The tray does not pulse on kind==='spontaneous' (same as replay), while malformed traces still pulse — change made in the git-tracked `apps/tray/src/tray-icon.ts` source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream → client render | Untrusted-shaped event; render must fail safe (missing nodes → no-op, never throw). |
| SSE stream → tray icon | Malformed event must still fail toward the liveness signal (pulse), per existing behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-06 | Spoofing | spontaneous mistaken for live recall | mitigate | Distinct default-mode hue (≠ amber/cyan) + SPONT_DIM < REPLAY_DIM enforced by verify assertion; tray suppressed so no live pulse. |
| T-56-07 | Denial of Service | malformed SSE row in render/tray | mitigate | Render reuses replay-branch guards (missing idMap node → skip); tray keeps fail-toward-liveness on parse error (no crash, no silent-dead icon). |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — package-legitimacy gate N/A. |

No high-severity threats: client-side render/tray changes accept no new privileged input and perform no writes.
</threat_model>

<verification>
- `SPONT_DIM < REPLAY_DIM` asserted at module load.
- `KIND_COLOR.spontaneous` distinct from amber/cyan.
- Existing viz render + tray behavior tests green.
- Grep gates confirm the spontaneous branch precedes ingestion dispatch and the tray suppress covers spontaneous in the git-tracked `apps/tray/src/tray-icon.ts` source.
</verification>

<success_criteria>
Spontaneous traces render in a distinct dim-indigo default-mode hue strictly subordinate to replay, and the tray icon stays at rest on them while still pulsing on live/malformed events.
</success_criteria>

<output>
Create `.planning/phases/56-spontaneous-1-hop-idle-activation/56-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
