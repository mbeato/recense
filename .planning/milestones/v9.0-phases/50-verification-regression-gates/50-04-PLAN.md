---
phase: 50-verification-regression-gates
plan: 04
type: execute
wave: 3
depends_on: ["50-03"]
files_modified:
  - docs/evals.md
autonomous: true
requirements: [GATE-03]
user_setup: []

must_haves:
  truths:
    - "docs/evals.md records the v9.0-final numbers (fresh cheap axes + fresh EVAL-02 + reused KU/LoCoMo-J/counter)"
    - "Every REUSED number carries its source commit + date and is NOT presented as a fresh same-day re-run (D-12 reuse recorded runs with honest provenance, D-15 pristine KU reuse via 46-recon03)"
    - "The 368-contradicts-on-14-clean-cases judge-validation evidence is recorded with methodology (Phase-46 win proven) (D-08)"
    - "Before/after deltas vs the pre-v9.0 baseline are documented for each axis"
    - "The PERF-03(b) 3-harness discharge mapping (correctness + KU + latency) is documented (D-16)"
  artifacts:
    - path: "docs/evals.md"
      provides: "v9.0-final results-of-record with honest provenance + judge-fire evidence + deferred-check discharge"
      contains: "v9.0-final"
  key_links:
    - from: "docs/evals.md"
      to: "scripts/eval/results/gate-baseline.json"
      via: "cites fresh cheap-axis v9.0-final numbers"
      pattern: "v9.0-final"
    - from: "docs/evals.md"
      to: "scripts/eval/results/46-recon03-ku-bm25on.json"
      via: "cites reused KU + 368 contradicts with Jun-28 provenance"
      pattern: "368"
---

<objective>
Update `docs/evals.md` — the results-of-record (GATE-03) — with the v9.0-final numbers: the fresh cheap-axis figures and fresh EVAL-02 from Plan 50-03, plus the deliberately-reused KU finish-78, LoCoMo-J, and judge-fire counter, each carrying honest source-commit + date provenance (D-12). Record the 368-contradicts-on-14-clean-cases judge-validation evidence (D-08), document before/after deltas, and write the deferred-check discharge mapping for RECON-04, the pristine KU re-run, and PERF-03(b) (D-14, D-15, D-16).

Purpose: Close GATE-03's documentation requirement honestly. The no-inflated-metrics rule applies to reading our OWN old runs: reused numbers must state the commit + date they came from and must NOT imply a fresh same-day v9.0-final re-run.

Output: updated `docs/evals.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-verification-regression-gates/50-CONTEXT.md
@.planning/phases/50-verification-regression-gates/50-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record v9.0-final numbers + provenance + judge-fire evidence in docs/evals.md</name>
  <read_first>
    - docs/evals.md (the FULL file being modified — match the existing EVAL-01..EVAL-04 section structure, results-table format, and honesty-disclosure style before editing)
    - scripts/eval/results/gate-baseline.json (fresh v9.0-final cheap-axis numbers from Plan 50-03)
    - scripts/eval/results/correctness-v90final.json (fresh EVAL-02 number from Plan 50-03)
    - scripts/eval/results/correctness-PENDING.json (pre-46 EVAL-02 84.6% @ f779bfb / Jun-27 — the before-delta reference)
    - scripts/eval/results/46-recon03-ku-bm25on.json (reused KU finish-78: ku_correct 7/18, total_contradicts 368, commit f4de897 / Jun-28)
    - scripts/eval/results/locomo-d41d5c8.json (reused LoCoMo-J headline 86.0%, commit d41d5c8 / Jun-24)
    - .planning/phases/46-reconsolidation-candidate-broadening/46-02-SUMMARY.md (368-contradicts methodology — the judge-fire proof source)
  </read_first>
  <action>
    Add a `v9.0-final` results section (or update the existing results tables) in `docs/evals.md` recording, per axis:

    - FRESH (Plan 50-03, this phase): LoCoMo R@5/R@10, latency p50/p95, token-structural injected_tokens, and EVAL-02 belief-correction — each tagged with the fresh date + commit from gate-baseline.json / correctness-v90final.json. State these are freshly re-measured on the shipped subscription headless stack (Haiku extract / Sonnet judge), D-09.
    - REUSED (NOT re-run): KU finish-78 (ku_correct 7/18, commit f4de897, Jun-28), LoCoMo-J headline (86.0%, commit d41d5c8, Jun-24), and the judge-fire counter (368). For EACH reused number write an explicit "source commit + date; reused, not a fresh same-day re-run" provenance note (D-12, no-inflated-metrics rule). Do NOT present any reused number as a v9.0-final fresh re-run.

    Record the judge-fire validation evidence (D-08): `368 contradicts on 14 clean cases vs pre-46 zero`, with the methodology — the `queryJudgeEngagement()` SQLite count over `consolidation_event` `contradict_*` rows (cite 46-02-SUMMARY.md), and that the gate asserts the binary `> 0` form to be robust to LLM run-to-run variance (D-06).

    Add a before/after delta line per axis vs the pre-v9.0 baseline (the v8.0-final figures in STATE.md: LoCoMo J=86.0%, R@5/R@10=77.3/82.2%, p50/p95=45/46ms, marginal write ~7,118 tok/turn; and EVAL-02 pre-46 84.6% @ f779bfb).

    Add a "Deferred-check discharge" subsection mapping: RECON-04 → fresh EVAL-02 (correctness-v90final.json); pristine KU re-run → reused 46-recon03-ku-bm25on.json with provenance; PERF-03(b) 3-harness end-to-end → the three harnesses are correctness (EVAL-02) + KU (replay/longmemeval) + latency, satisfied by fresh latency + fresh EVAL-02 + reused KU (D-16). State the regression-gate is the on-demand `npm run gate` (LLM-free) + opt-in `npm run gate:accuracy`, NOT a CI merge-block (D-01) — to keep docs consistent with the founder reframe.

    Match existing markdown section/table style; do not restructure unrelated sections (surgical edit).
  </action>
  <verify>
    <automated>grep -q "v9.0-final" docs/evals.md && grep -q "368" docs/evals.md && grep -q "f4de897" docs/evals.md && grep -q "RECON-04" docs/evals.md && grep -qi "reused" docs/evals.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - docs/evals.md contains a `v9.0-final` results section with the fresh cheap-axis + fresh EVAL-02 numbers tagged with their fresh date + commit
    - Every reused number (KU 7/18, LoCoMo-J 86.0%, 368 contradicts) carries an explicit source commit + date provenance note and is marked "reused, not a fresh re-run"
    - The `368 contradicts on 14 clean cases vs pre-46 zero` judge-validation evidence + methodology is recorded
    - A before/after delta per axis vs the pre-v9.0 (v8.0-final) baseline is present
    - A deferred-check discharge subsection maps RECON-04, pristine KU re-run, and PERF-03(b) (3-harness = correctness + KU + latency)
    - No number is presented as a fresh same-day re-run when it is in fact reused (no-inflated-metrics rule)
  </acceptance_criteria>
  <done>docs/evals.md records v9.0-final with honest provenance, the judge-fire evidence, before/after deltas, and the three deferred-check discharges — GATE-03 documentation complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded results JSON → docs/evals.md | The only real risk is misattributing a reused number as a fresh same-day re-run, or inflating a figure beyond what the result JSON supports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-07 | Tampering / metric inflation | docs/evals.md reused numbers | mitigate | Each reused number carries source commit + date and an explicit "reused, not a fresh re-run" label; no figure exceeds what its source result JSON reports (no-inflated-metrics rule, verified by acceptance criteria). |
| T-50-08 | Repudiation | judge-fire claim | mitigate | The 368-contradicts evidence cites its result JSON (46-recon03, f4de897/Jun-28) and methodology (46-02-SUMMARY.md queryJudgeEngagement), making the Phase-46 win independently checkable. |
| T-50-SC | Tampering | npm installs | accept | Docs-only edit; no dependencies, no install task. |
</threat_model>

<verification>
- grep confirms v9.0-final, 368, the reused-source commit (f4de897), RECON-04, and "reused" all present in docs/evals.md
- Manual read confirms provenance notes and the deferred-check discharge mapping are honest and complete
</verification>

<success_criteria>
- docs/evals.md is updated with v9.0-final numbers, honest provenance, judge-validation evidence, and before/after deltas (GATE-03)
- The three deferred checks (RECON-04, pristine KU, PERF-03(b)) are documented as discharged
</success_criteria>

<output>
Create `.planning/phases/50-verification-regression-gates/50-04-SUMMARY.md` when done
</output>
