---
phase: 50-verification-regression-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/eval/gate-runner.cjs
  - scripts/eval/results/gate-baseline.json
  - package.json
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03]
user_setup: []

must_haves:
  truths:
    - "`node scripts/eval/gate-runner.cjs` with no mode flag prints usage and exits non-zero (mode-guard)"
    - "Delivered as an on-demand local `npm run gate` with NO CI / merge-blocking enforcement; the only consumer is Phase 51's cheap re-runnable latency/recall check (D-01 on-demand-not-CI, D-02 Phase-51 consumer)"
    - "Gate exits 0 when every deterministic axis is within floor/ceiling vs the baseline (D-03 LLM-free deterministic axes only, D-04 floor/ceiling)"
    - "Gate exits non-zero when any deterministic axis breaches its floor/ceiling (D-04)"
    - "Gate fails-closed (non-zero) when the baseline JSON or a required result JSON is missing"
    - "Gate asserts scores.total_contradicts > 0 read from the frozen 46-recon03 JSON (LLM-free judge-fire proof) (D-06 binary >0 assertion, D-07 candidate-escalation counter is LLM-free)"
    - "`--update-baseline` rewrites the committed gate-baseline.json deliberately (re-baseline is explicit, not silent) (D-05)"
  artifacts:
    - path: "scripts/eval/gate-runner.cjs"
      provides: "Cheap LLM-free deterministic regression gate + floor/ceiling comparator + re-baseline path"
      min_lines: 120
    - path: "scripts/eval/results/gate-baseline.json"
      provides: "Committed v9.0-final baseline schema (scores + thresholds + recorded_from provenance)"
      contains: "thresholds"
    - path: "package.json"
      provides: "gate / gate:baseline / gate:accuracy npm scripts"
      contains: "\"gate\":"
  key_links:
    - from: "scripts/eval/gate-runner.cjs"
      to: "scripts/eval/results/gate-baseline.json"
      via: "reads thresholds block"
      pattern: "thresholds"
    - from: "scripts/eval/gate-runner.cjs"
      to: "scripts/eval/results/46-recon03-ku-bm25on.json"
      via: "reads scores.total_contradicts, asserts > 0"
      pattern: "total_contradicts"
    - from: "package.json"
      to: "scripts/eval/gate-runner.cjs"
      via: "gate script invokes runner --run"
      pattern: "gate-runner.cjs --run"
---

<objective>
Build the cheap, on-demand, LLM-free regression gate (`npm run gate`) over the deterministic axes — LoCoMo R@5/R@10, latency p50/p95, token-structural, and the binary judge-fire contradiction assertion — with floor/ceiling thresholds vs a committed baseline JSON, a fail-closed contract, and an explicit `--update-baseline` re-baseline mechanism. This is the load-bearing GATE-01 deliverable and the only consumer Phase 51 needs (D-02).

Purpose: Give the founder a fast, $0-against-subscription, reproducible guard that exits non-zero on any deterministic regression (D-01, D-03, D-04), and an intentional re-baseline path so v9.0-final numbers are never overwritten by silent drift (D-05, GATE-03).

Output: `scripts/eval/gate-runner.cjs`, the `scripts/eval/results/gate-baseline.json` schema (thresholds + provenance), and the `gate` / `gate:baseline` / `gate:accuracy` npm scripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-verification-regression-gates/50-CONTEXT.md
@.planning/phases/50-verification-regression-gates/50-PATTERNS.md

<interfaces>
<!-- Result-JSON shapes the gate reads. Extracted from committed result files. Use directly. -->

LoCoMo scorer output (scripts/eval/results/locomo-d41d5c8.json):
  scores.rAtK.r5  = 0.7727...   (R@5, fraction)
  scores.rAtK.r10 = 0.8220...   (R@10, fraction)

Latency output (scripts/eval/results/41-latency-after.json):
  warm.indexed.p50_ms = 13
  warm.indexed.p95_ms = 14

Token-structural output (injection-efficiency-harness.cjs, charsToTokens proxy, NO api key):
  injected_tokens  (deterministic char/4 proxy, LLM-free)

Frozen judge-fire counter (scripts/eval/results/46-recon03-ku-bm25on.json):
  scores.total_contradicts = 368   (assert > 0; LLM-free per 50-PATTERNS.md §"Judge-Fire Counter")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the cheap deterministic gate runner</name>
  <read_first>
    - scripts/eval/gate-runner.cjs (the file being created — confirm absent first)
    - scripts/eval/correctness-harness.cjs (CLI arg parser lines 35-36, commit resolution lines 305-307, non-zero-exit / key-guard shape lines 77-86, results-envelope write lines 400-403)
    - scripts/eval/locomo-harness.cjs (mode-guard lines 84-98, `--retrieval-only` line 77, `--run` line 75)
    - scripts/eval/41-latency-after.cjs (percentile util lines 76-80, warm.indexed shape, OUT default line 47)
    - scripts/eval/injection-efficiency-harness.cjs (charsToTokens proxy line 47, injected_tokens lines 153/178 — the LLM-free token-structural axis)
    - scripts/eval/results/46-recon03-ku-bm25on.json (scores.total_contradicts surface)
    - scripts/eval/results/locomo-d41d5c8.json + scripts/eval/results/41-latency-after.json (real result JSONs the comparator reads — usable as deterministic test fixtures)
    - .planning/phases/50-verification-regression-gates/50-PATTERNS.md (§gate-runner.cjs pattern assignments + §Judge-Fire Counter answer)
  </read_first>
  <action>
    Create `scripts/eval/gate-runner.cjs` (CommonJS, `'use strict'`). Reuse the `arg(k,d)` parser and `execSync('git rev-parse --short HEAD')` commit-resolution from correctness-harness.cjs, and the `percentile()` util from 41-latency-after.cjs. Implement a mode-guard mirroring locomo-harness.cjs lines 84-98: refuse to run without `--run` or `--update-baseline` (print usage, `process.exit(1)`).

    Flags: `--run` (re-run cheap axes then compare), `--update-baseline` (re-record then rewrite the committed baseline, exit 0), `--baseline <path>` (default `scripts/eval/results/gate-baseline.json`), and per-axis result overrides `--locomo <path>` `--latency <path>` `--injection <path>` `--contradicts <path>` (default `scripts/eval/results/46-recon03-ku-bm25on.json`) so the floor/ceiling comparator is testable against committed result JSONs without spawning harnesses.

    Cheap-axis sources (LLM-free; the only network dependency is OpenAI embeddings for LoCoMo R@K, NOT the claude judge): (a) LoCoMo R@K = run `node scripts/eval/locomo-harness.cjs --run --retrieval-only` then `node scripts/eval/locomo-scorer.cjs` and read `scores.rAtK.r5` / `scores.rAtK.r10`; (b) latency = run `scripts/eval/41-latency-after.cjs` and read `warm.indexed.p50_ms` / `warm.indexed.p95_ms`; (c) token-structural = run `scripts/eval/injection-efficiency-harness.cjs` and read `injected_tokens`; (d) contradicts = READ the frozen `--contradicts` JSON `scores.total_contradicts` (no fresh consolidation, no LLM).

    Comparator (D-04 floor/ceiling, read thresholds from `baseline.thresholds`): fail if `locomo_r5 < locomo_r5_floor`; `locomo_r10 < locomo_r10_floor`; `lat_p95_ms > lat_p95_ceiling_ms`; `injected_tokens > injected_tokens_ceiling`; `total_contradicts < contradicts_floor` (contradicts_floor = 1, the D-06 binary assertion). Accumulate a `failures[]` array, print one `GATE FAIL: <axis> <actual> vs <bound>` line per breach, and `if (failures.length > 0) process.exit(1)` after all checks (copy the exit shape from correctness-harness.cjs lines 77-86). On all-pass print `GATE PASS` and exit 0.

    Fail-closed (the one real threat surface, per <threat_model>): if the `--baseline` file is missing/unparseable, OR any required cheap-axis result JSON is missing/unreadable/missing-its-expected-key, print `GATE FAIL: <reason>` and `process.exit(1)` — NEVER exit 0 on absent/corrupt inputs. Do not let a missing harness output be treated as a pass.

    `--update-baseline` path: after running the cheap axes, write `scripts/eval/results/gate-baseline.json` following the 50-PATTERNS.md target schema — `meta` (eval: "gate-baseline", version: "v9.0-final", ISO date, short commit, engine_version from package.json, `recorded_from` provenance map), `scores` (locomo_r5, locomo_r10, lat_p50_ms, lat_p95_ms, injected_tokens, total_contradicts), and a `thresholds` block. Print `Baseline updated -> <path>` and exit 0. Do NOT fabricate scores — write only measured values; mark any axis whose harness did not produce output as a hard error (fail-closed), never a placeholder number (no-inflated-metrics rule).
  </action>
  <verify>
    <automated>node -c scripts/eval/gate-runner.cjs && node scripts/eval/gate-runner.cjs 2>&1 | grep -qi usage; echo "mode-guard exit=$?"; node scripts/eval/gate-runner.cjs --run --baseline /no/such/baseline.json; test $? -ne 0 && echo "fail-closed-missing-baseline OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node -c scripts/eval/gate-runner.cjs` succeeds (valid syntax)
    - Invoked with no mode flag → prints usage and exits non-zero (mode-guard)
    - Invoked `--run --baseline <nonexistent>` → exits non-zero (fail-closed on missing baseline)
    - Reading a `--contradicts` JSON with `scores.total_contradicts = 0` → exits non-zero (D-06 binary floor)
    - With a baseline whose `locomo_r5_floor` exceeds the result's r5 (e.g. floor 1.1 vs locomo-d41d5c8.json r5=0.773) → exits non-zero with a `GATE FAIL: locomo_r5` line
    - With a baseline whose floors/ceilings are satisfied by the committed result JSONs → exits 0 with `GATE PASS`
    - `--update-baseline` writes a gate-baseline.json with `meta.recorded_from`, `scores`, and `thresholds` blocks; never writes a placeholder/fabricated score
  </acceptance_criteria>
  <done>gate-runner.cjs exists, mode-guarded, fails-closed on missing baseline/inputs and contradicts=0, exits non-zero on any floor/ceiling breach and 0 when clean, and supports `--update-baseline`.</done>
</task>

<task type="auto">
  <name>Task 2: Add gate npm scripts + committed baseline schema</name>
  <read_first>
    - package.json (the `eval:*` script block ~lines 45-52 — exact `npm run build &&` prefix convention; the file being modified)
    - scripts/eval/results/gate-baseline.json (the file being created — confirm absent first)
    - scripts/eval/results/locomo-d41d5c8.json + 41-latency-after.json + 46-recon03-ku-bm25on.json (source numbers for the initial schema)
    - .planning/phases/50-verification-regression-gates/50-PATTERNS.md (§gate-baseline.json target schema + §package.json new scripts)
  </read_first>
  <action>
    Add three scripts to package.json after the existing `eval:cost-benefit` entry, matching the `npm run build &&` convention (the gate runner imports compiled dist via the harnesses it spawns): `"gate": "npm run build && node scripts/eval/gate-runner.cjs --run"`, `"gate:baseline": "npm run build && node scripts/eval/gate-runner.cjs --run --update-baseline"`, and `"gate:accuracy": "npm run build && node scripts/eval/gate-accuracy-runner.cjs --run"` (the gate:accuracy target file is authored in Plan 50-02; the script entry is inert until that file exists, which is fine).

    Create `scripts/eval/results/gate-baseline.json` as the committed schema scaffold following the 50-PATTERNS.md target schema: `meta` (eval "gate-baseline", version "v9.0-final", `recorded_from` provenance map pointing at locomo-d41d5c8.json / 41-latency-after.json / 46-recon03-ku-bm25on.json), `scores` seeded from those committed files (locomo_r5 0.773, locomo_r10 0.822, lat_p50_ms 13, lat_p95_ms 14, total_contradicts 368; injected_tokens from the injection-efficiency result), and `thresholds` with `locomo_r5_floor`, `locomo_r10_floor`, `lat_p95_ceiling_ms`, `injected_tokens_ceiling`, `contradicts_floor: 1`. Set floors at baseline − epsilon (recall epsilon ~0.02), latency ceiling = baseline p95 × ~1.15 rounded up, injected_tokens ceiling = baseline × ~1.10. Mark in `meta` that scores here are pre-Plan-50-03 provenance-tagged values (D-12 honest provenance) and will be re-recorded fresh by `npm run gate:baseline` in Plan 50-03 — do NOT present these seed numbers as a same-day fresh re-run.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); ['gate','gate:baseline','gate:accuracy'].forEach(k=>{if(!p.scripts[k])throw new Error('missing '+k)}); const b=require('./scripts/eval/results/gate-baseline.json'); if(!b.thresholds||b.thresholds.contradicts_floor!==1)throw new Error('bad thresholds'); if(!b.meta.recorded_from)throw new Error('no provenance'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"gate":`, `"gate:baseline":`, and `"gate:accuracy":` scripts
    - `gate` and `gate:baseline` scripts invoke `scripts/eval/gate-runner.cjs` with `--run` / `--run --update-baseline`
    - `scripts/eval/results/gate-baseline.json` parses and contains `meta.recorded_from`, `scores`, and a `thresholds` block with `contradicts_floor: 1`
    - `meta` notes these seed scores carry provenance and will be re-recorded by Plan 50-03 (no implied fresh same-day re-run)
  </acceptance_criteria>
  <done>The three npm scripts exist; gate-baseline.json holds a parseable v9.0-final schema with thresholds and honest provenance, ready for the fresh re-record in Plan 50-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → gate-runner | Gate reads baseline + frozen result JSONs; all local, founder-authored. No untrusted network input on the cheap path (OpenAI embedding call is outbound only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Denial-of-service / fail-open | gate-runner reading baseline + result JSONs | mitigate | Fail-closed: missing/corrupt baseline OR any missing result JSON / missing expected key → print `GATE FAIL` and `process.exit(1)`. Never exit 0 on absent input. (Verified in Task 1 acceptance.) |
| T-50-02 | Tampering / metric inflation | gate-baseline.json scores | mitigate | `--update-baseline` writes only measured values; any axis without a real harness output is a hard error, never a placeholder. Provenance recorded in `meta.recorded_from`. |
| T-50-SC | Tampering | npm installs | accept | This plan adds zero new dependencies (net-zero invariant); only package.json `scripts` entries change. No install task. |
</threat_model>

<verification>
- `node -c scripts/eval/gate-runner.cjs` passes
- Mode-guard, fail-closed-on-missing-baseline, contradicts=0 breach, floor breach, clean-pass, and `--update-baseline` write all behave per acceptance criteria
- The three npm scripts and the baseline JSON schema validate
</verification>

<success_criteria>
- `npm run gate` (via gate-runner --run) is a single command that exits non-zero on any deterministic-axis regression and 0 when clean (GATE-01)
- The gate covers latency p50/p95, R@5/R@10, token-structural, and the binary judge-fire contradiction assertion (GATE-02 deterministic portion)
- An explicit `--update-baseline` / `npm run gate:baseline` re-baseline mechanism exists (GATE-03 re-baseline, D-05)
- No `.github/workflows/` enforcement added (D-01)
</success_criteria>

<output>
Create `.planning/phases/50-verification-regression-gates/50-01-SUMMARY.md` when done
</output>
