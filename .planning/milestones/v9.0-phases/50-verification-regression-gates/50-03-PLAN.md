---
phase: 50-verification-regression-gates
plan: 03
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - scripts/eval/results/gate-baseline.json
  - scripts/eval/results/correctness-v90final.json
autonomous: false
requirements: [GATE-02, GATE-03]
user_setup:
  - service: openai
    why: "Fresh LoCoMo R@K cheap-axis run needs question embeddings (LLM-free judge, but embeddings are real)"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI dashboard -> API keys"
  - service: anthropic
    why: "Fresh EVAL-02 (n=13) discharges RECON-04 on the shipped subscription headless stack (Haiku extract / Sonnet judge via claude -p)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API keys (or the founder's claude -p Max-subscription transport)"

must_haves:
  truths:
    - "gate-baseline.json holds FRESH v9.0-final cheap-axis numbers (post-Phase-47 R@K, latency, token-structural) with a fresh date + commit (D-09 subscription headless stack, D-10 fresh cheap-axis run becomes the committed baseline)"
    - "A fresh EVAL-02 (n=13) post-46–48 belief-correction number is recorded — discharging RECON-04 with a genuine number, not the pre-46 f779bfb run (D-11 fresh EVAL-02 exception, D-14 RECON-04 discharge)"
    - "No multi-hour fresh KU / LoCoMo-J re-run is performed; those are reused from recent recorded runs, and PERF-03(b)'s 3 harnesses map to correctness + KU + latency (D-13 no multi-hour re-run, D-16 PERF-03(b) mapping)"
    - "`npm run gate` exits 0 against the freshly-recorded baseline (the gate is green at v9.0-final)"
    - "gate-baseline.json thresholds block contains numeric eval02_floor, locomo_j_floor, and ku_floor so `npm run gate:accuracy` ENFORCES (not SKIPs) all three accuracy axes — GATE-02's accuracy tier is a real floor, not a silent no-op"
  artifacts:
    - path: "scripts/eval/results/gate-baseline.json"
      provides: "Re-recorded v9.0-final committed baseline (fresh cheap-axis scores + provenance + enforced accuracy floors)"
      contains: "v9.0-final"
    - path: "scripts/eval/results/correctness-v90final.json"
      provides: "Fresh EVAL-02 n=13 result envelope (RECON-04 discharge, post-46–48)"
      contains: "meta"
  key_links:
    - from: "npm run gate:baseline"
      to: "scripts/eval/results/gate-baseline.json"
      via: "fresh cheap-axis run rewrites scores intentionally"
      pattern: "v9.0-final"
    - from: "npm run eval:correctness"
      to: "scripts/eval/results/correctness-v90final.json"
      via: "fresh n=13 belief-correction run on shipped stack"
      pattern: "correctness"
    - from: "npm run gate:accuracy"
      to: "scripts/eval/results/gate-baseline.json"
      via: "reads eval02_floor / locomo_j_floor / ku_floor for the enforced accuracy-tier floor comparison"
      pattern: "eval02_floor"
---

<objective>
Record the genuine v9.0-final numbers on the shipped subscription headless stack (D-09): re-run the cheap LLM-free axes NOW and commit them as the gate baseline (D-10), run a fresh EVAL-02 (n=13) that discharges the deferred RECON-04 with a real post-46–48 belief-correction number (D-11, D-14), and write the three accuracy floors into the baseline so `npm run gate:accuracy` actually enforces them (closing GATE-02's accuracy tier — without this step the accuracy runner SKIPs every axis and the tier is a silent no-op). Phase 47 changed hybrid retrieval, so R@K MUST be re-recorded fresh — the pre-46 `correctness-PENDING.json` (f779bfb, Jun-27) cannot serve as the post-pipeline evidence.

Purpose: Turn the Plan 50-01 baseline scaffold into honestly-measured v9.0-final figures, close RECON-04, and arm the accuracy-tier floors so GATE-02 enforces rather than informs. These are the numbers Plan 50-04 records in docs/evals.md.

Output: re-recorded `scripts/eval/results/gate-baseline.json` (cheap-axis scores + accuracy floors) and a fresh `scripts/eval/results/correctness-v90final.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-verification-regression-gates/50-CONTEXT.md
@.planning/phases/50-verification-regression-gates/50-PATTERNS.md
@.planning/phases/50-verification-regression-gates/50-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Record fresh v9.0-final cheap-axis baseline</name>
  <read_first>
    - scripts/eval/gate-runner.cjs (the `--update-baseline` path authored in Plan 50-01)
    - scripts/eval/results/gate-baseline.json (the scaffold to be re-recorded; the file being modified)
    - scripts/eval/README.md (harness catalog — confirm the live DB / LoCoMo dataset prerequisites)
    - scripts/eval/results/locomo-d41d5c8.json (prior R@K, for the before/after delta in 50-04)
  </read_first>
  <what-built>
    Plan 50-01 produced `gate-runner.cjs --update-baseline` and the `npm run gate:baseline` script. This checkpoint records the fresh, honestly-measured cheap-axis numbers on the live brain + shipped stack, because they depend on the founder's `OPENAI_API_KEY` and live recense.db (customer-zero) and should be eyeballed before being committed as the baseline of record.
  </what-built>
  <how-to-verify>
    1. Ensure `OPENAI_API_KEY` is set and the live recense.db + LoCoMo dataset are present (the cheap gate uses OpenAI embeddings for R@K — LLM-free judge, but embeddings are real).
    2. Run: `npm run gate:baseline`
    3. Confirm `scripts/eval/results/gate-baseline.json` now shows a FRESH `meta.date` (today, 2026-06-29 or later) and `meta.commit` (current HEAD short sha), with `scores.locomo_r5` / `locomo_r10` reflecting the post-Phase-47 hybrid retrieval (re-measured, not the old 0.773/0.822 unless they genuinely re-measure there), fresh `lat_p50_ms` / `lat_p95_ms`, fresh `injected_tokens`, and `total_contradicts` carried from the frozen 46-recon03 JSON.
    4. Run `npm run gate` and confirm it exits 0 (`GATE PASS`) against the freshly-recorded baseline.
    5. Sanity-check the numbers are interview-defensible (no rounding up, no inflation) before approving.
  </how-to-verify>
  <acceptance_criteria>
    - `gate-baseline.json` `meta.date` is the recording day and `meta.commit` is the current HEAD short sha (fresh, not the seed provenance dates)
    - `scores` contains freshly-measured locomo_r5, locomo_r10, lat_p50_ms, lat_p95_ms, injected_tokens, and total_contradicts > 0
    - `npm run gate` exits 0 against the freshly-recorded baseline
    - Every score is a measured value (no placeholder/fabricated number)
  </acceptance_criteria>
  <resume-signal>Type "approved" once gate-baseline.json holds fresh v9.0-final numbers and `npm run gate` is green, or describe the issue.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run fresh EVAL-02 (n=13) — discharge RECON-04</name>
  <read_first>
    - scripts/eval/correctness-harness.cjs (the EVAL-02 belief-correction harness; default `--cases` line 38, `--out` line 39, scores aggregation lines 281-302)
    - scripts/eval/cases/correctness-cases.json (the n=13 case set — confirm contradiction-case count)
    - scripts/eval/results/correctness-PENDING.json (the PRE-46 f779bfb/Jun-27 run — delta reference ONLY, not the v9.0-final number)
    - docs/evals.md (EVAL-02 methodology section — match the recorded shape)
  </read_first>
  <what-built>
    The EVAL-02 belief-correction harness already exists. RECON-04 (clean-case no-regression after Phases 46–48) requires a GENUINE post-pipeline number; the only recorded EVAL-02 is pre-46 (f779bfb, Jun-27) so it cannot serve as the evidence. This is the one mandated fresh accuracy run (D-11) — ~25 min, $0 local / ~$2 API.
  </what-built>
  <how-to-verify>
    1. Ensure `ANTHROPIC_API_KEY` (or the claude -p subscription transport) and `OPENAI_API_KEY` are available — fresh numbers must be on the shipped subscription headless stack (Haiku extract / Sonnet judge), D-09.
    2. Run the fresh EVAL-02 writing to a NEW result file (do not overwrite the pre-46 PENDING run): `node scripts/eval/correctness-harness.cjs --out scripts/eval/results/correctness-v90final.json` (after `npm run build`).
    3. Confirm `correctness-v90final.json` has `meta` with today's date + current commit (post-46–48) and a `scores` block with the belief-correction / clean-case-no-regression rate over n=13.
    4. Compare against the pre-46 `correctness-PENDING.json` (84.6% @ f779bfb) as a before/after delta — confirm no regression (RECON-04). If there IS a regression, STOP and flag the founder (do not silently record it as a pass).
  </how-to-verify>
  <acceptance_criteria>
    - `scripts/eval/results/correctness-v90final.json` exists with `meta.date` = today, `meta.commit` = current HEAD (post-46–48), and a `scores` block
    - The fresh number is recorded separately from `correctness-PENDING.json` (the pre-46 run is preserved as the delta reference)
    - RECON-04 is dischargeable: the fresh clean-case no-regression result vs the pre-46 84.6% baseline is captured for the 50-04 delta table
  </acceptance_criteria>
  <resume-signal>Type "approved" once correctness-v90final.json holds the fresh post-46–48 EVAL-02 number, or describe the regression/issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Arm the accuracy-tier floors in gate-baseline.json (close GATE-02)</name>
  <read_first>
    - scripts/eval/results/gate-baseline.json (the baseline re-recorded in Task 1 — the file being modified; read its existing `thresholds` block so the new keys are added, not overwritten)
    - scripts/eval/results/correctness-v90final.json (the FRESH EVAL-02 from Task 2 — source of the eval02 clean-case number)
    - scripts/eval/results/46-recon03-ku-bm25on.json (REUSED KU number per D-12, e.g. ku_correct 7/18 ≈ 0.389 — source of ku_floor anchor)
    - scripts/eval/results/locomo-d41d5c8.json (REUSED LoCoMo-J headline per D-12 — source of locomo_j_floor anchor; read `scores.headline`)
    - .planning/phases/50-verification-regression-gates/50-CONTEXT.md (D-03/D-11/D-12 — accuracy tier + honest provenance) and 50-PATTERNS.md (gate-baseline.json thresholds schema)
  </read_first>
  <action>
    AFTER Task 2 has recorded the fresh `correctness-v90final.json` and Task 1 has re-recorded the cheap-axis baseline, write three accuracy-floor keys into `scripts/eval/results/gate-baseline.json`'s existing `thresholds` block — `eval02_floor`, `locomo_j_floor`, `ku_floor` — so `gate-accuracy-runner.cjs` (50-02) enforces every accuracy axis instead of printing `SKIP: <axis> (no baseline floor)`. The cheap `npm run gate:baseline` script only records the cheap-axis thresholds, so these accuracy floors are a separate, deliberate write (do this as a small in-place edit of the JSON's `thresholds` object — do NOT touch the cheap-axis floors Task 1 already wrote, and do NOT touch the `scores` block).

    Use the SAME floor convention as the cheap axes (floor = recorded value − epsilon):
    - `eval02_floor` = the FRESH clean-case score from `correctness-v90final.json` (Task 2) − 0.05.
    - `locomo_j_floor` = the REUSED recorded LoCoMo-J headline (`locomo-d41d5c8.json` `scores.headline`, D-12) − epsilon (~0.05).
    - `ku_floor` = the REUSED recorded KU score (`46-recon03-ku-bm25on.json`, ku_correct 7/18 ≈ 0.389, D-12) − epsilon (~0.05).

    Honest provenance (no-inflated-metrics rule + D-12): `locomo_j_floor` and `ku_floor` are derived from REUSED recorded numbers, NOT fresh same-day re-runs — record that distinction in `meta` (e.g. extend `meta.recorded_from` or add a `meta.accuracy_floor_provenance` map naming each floor's source file + commit/date, and noting eval02_floor is anchored to the fresh Task-2 run while locomo_j_floor / ku_floor are anchored to reused runs). Never present the reused-anchored floors as fresh measurements.
  </action>
  <verify>
    <automated>node -e "const b=require('./scripts/eval/results/gate-baseline.json'); const t=b.thresholds||{}; ['eval02_floor','locomo_j_floor','ku_floor'].forEach(k=>{if(typeof t[k]!=='number')throw new Error('missing/non-numeric '+k)}); if(!b.meta.recorded_from && !b.meta.accuracy_floor_provenance)throw new Error('no accuracy-floor provenance'); console.log('accuracy floors armed OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `gate-baseline.json` `thresholds` block contains `eval02_floor`, `locomo_j_floor`, and `ku_floor`, each a numeric value
    - The cheap-axis thresholds Task 1 wrote (locomo_r5_floor, locomo_r10_floor, lat_p95_ceiling_ms, injected_tokens_ceiling, contradicts_floor) and the `scores` block are unchanged
    - `npm run gate:accuracy` ENFORCES (does not `SKIP`) all three accuracy axes against the committed floors — no axis prints `SKIP: <axis> (no baseline floor)`
    - `meta` honestly records that `locomo_j_floor` and `ku_floor` are derived from REUSED recorded numbers (provenance: source file + commit/date), distinct from the fresh-Task-2-anchored `eval02_floor`
  </acceptance_criteria>
  <done>gate-baseline.json's thresholds block carries numeric eval02_floor / locomo_j_floor / ku_floor with honest reuse provenance, so `npm run gate:accuracy` enforces every accuracy axis (GATE-02 accuracy tier is a real floor, not a silent no-op).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder run → committed baseline/result JSON | The risk is recording a number that is not interview-defensible (inflation) or recording a regression as a pass. Both are human-in-the-loop gated here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05 | Tampering / metric inflation | fresh baseline + EVAL-02 numbers | mitigate | Both runs are blocking human-verify checkpoints; founder eyeballs interview-defensibility before approving. `--update-baseline` writes only measured values (no placeholders). |
| T-50-06 | Repudiation / stale evidence | RECON-04 discharge + reused accuracy floors | mitigate | Fresh run written to a NEW file (`correctness-v90final.json`); the pre-46 PENDING run is preserved with its f779bfb/Jun-27 provenance. Reused-anchored accuracy floors (locomo_j_floor, ku_floor) carry explicit source-file + commit/date provenance in `meta` (D-12), never presented as fresh. |
| T-50-SC | Tampering | npm installs | accept | No new dependencies; only result JSONs are written. No install task. |
</threat_model>

<verification>
- `gate-baseline.json` carries fresh date + commit and `npm run gate` exits 0 against it
- `correctness-v90final.json` carries fresh date + commit and a scores block; RECON-04 delta vs the pre-46 run captured
- `gate-baseline.json` thresholds block contains numeric eval02_floor / locomo_j_floor / ku_floor with honest reuse provenance; `npm run gate:accuracy` enforces (not SKIPs) all three accuracy axes
</verification>

<success_criteria>
- The committed baseline is intentionally re-set to genuine v9.0-final cheap-axis figures (GATE-03 re-baseline, D-10)
- RECON-04 is discharged with a real post-46–48 EVAL-02 number (D-11, D-14)
- The accuracy-tier floors (eval02_floor / locomo_j_floor / ku_floor) are written so `npm run gate:accuracy` enforces every accuracy axis — GATE-02's accuracy tier is a real floor, not a silent no-op
- No multi-hour fresh KU / LoCoMo-J re-run was performed (D-13 respected — those are reused in 50-04, and their reuse anchors the accuracy floors with honest provenance per D-12)
</success_criteria>

<output>
Create `.planning/phases/50-verification-regression-gates/50-03-SUMMARY.md` when done
</output>
