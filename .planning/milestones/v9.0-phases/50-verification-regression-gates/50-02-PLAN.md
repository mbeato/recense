---
phase: 50-verification-regression-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/eval/gate-accuracy-runner.cjs
autonomous: true
requirements: [GATE-01, GATE-02]
user_setup:
  - service: anthropic
    why: "gate:accuracy spends real subscription/API tokens (Sonnet judge + Haiku extract) — deliberate opt-in only"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API keys (or the headless claude -p subscription transport on the founder machine)"
      - name: OPENAI_API_KEY
        source: "OpenAI dashboard -> API keys (question embeddings)"

must_haves:
  truths:
    - "`node scripts/eval/gate-accuracy-runner.cjs` with no mode flag prints usage and exits non-zero (mode-guard)"
    - "The accuracy runner is a SEPARATE command — it is never invoked by gate-runner.cjs / the cheap `npm run gate` (D-03 accuracy axes live in a separate deliberate gate:accuracy)"
    - "Without ANTHROPIC_API_KEY / OPENAI_API_KEY a real run exits non-zero with a clear key-missing error (fail-closed, no silent skip)"
    - "`--probe` prints a token/cost estimate and exits 0 without running the full paid suite"
  artifacts:
    - path: "scripts/eval/gate-accuracy-runner.cjs"
      provides: "Opt-in paid accuracy-tier gate wrapping LoCoMo-J + EVAL-02 belief-correction + KU, with floor checks vs baseline accuracy thresholds"
      min_lines: 90
  key_links:
    - from: "scripts/eval/gate-accuracy-runner.cjs"
      to: "scripts/eval/correctness-harness.cjs"
      via: "spawns EVAL-02 belief-correction run"
      pattern: "correctness-harness"
    - from: "scripts/eval/gate-accuracy-runner.cjs"
      to: "scripts/eval/results/gate-baseline.json"
      via: "reads accuracy thresholds for floor comparison"
      pattern: "gate-baseline"
---

<objective>
Build the deliberate, opt-in, paid accuracy-tier gate (`npm run gate:accuracy`) that wraps the LLM-routed axes — LoCoMo-J, EVAL-02 belief-correction, and KU — behind a hard key-guard and a cost probe, comparing them against accuracy floors in the baseline JSON. This is intentionally NEVER part of the cheap default `npm run gate` (D-03): real token spend stays explicit.

Purpose: Complete GATE-02's coverage of the accuracy + belief-correction axes that cannot run LLM-free, while keeping them off the cheap reproducible path the founder runs before merging Phase 51 (D-02).

Output: `scripts/eval/gate-accuracy-runner.cjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-verification-regression-gates/50-CONTEXT.md
@.planning/phases/50-verification-regression-gates/50-PATTERNS.md

<interfaces>
<!-- Paid harnesses this runner wraps (do NOT rebuild — they exist). -->
LoCoMo-J:     node scripts/eval/locomo-harness.cjs --run        -> locomo-scorer.cjs scores.headline
EVAL-02:      node scripts/eval/correctness-harness.cjs         -> scores (belief-correction rate)
KU finish-78: node scripts/eval/longmemeval-harness.cjs / replay-ku-harness.cjs

Cost-probe pattern (locomo-harness.cjs line 75, --probe runs ONE case + prints cost, exits 0).
Key-guard pattern (correctness-harness.cjs lines 77-86, missing keys -> process.exit(1)).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the opt-in accuracy-tier gate runner</name>
  <read_first>
    - scripts/eval/gate-accuracy-runner.cjs (the file being created — confirm absent first)
    - scripts/eval/correctness-harness.cjs (exact API-key guard lines 77-86, arg parser lines 35-36, commit resolution lines 305-307)
    - scripts/eval/locomo-harness.cjs (mode-guard lines 84-98, `--probe` cost-estimation pattern lines 75/103-104, `--run` requirement)
    - scripts/eval/results/gate-baseline.json (created in Plan 50-01, accuracy floors written in Plan 50-03 — read its thresholds block for accuracy floors)
    - .planning/phases/50-verification-regression-gates/50-PATTERNS.md (§gate-accuracy-runner.cjs exact-shape assignment)
  </read_first>
  <action>
    Create `scripts/eval/gate-accuracy-runner.cjs` (CommonJS, `'use strict'`) reusing the `arg(k,d)` parser and commit-resolution. Apply the locomo-harness mode-guard: refuse to run without `--run` or `--probe` (print usage, `process.exit(1)`).

    Hard key-guard (copy correctness-harness.cjs lines 77-86, but ALWAYS required — there is no dry-run that skips the LLM here): if `--run` and either `ANTHROPIC_API_KEY` or `OPENAI_API_KEY` is absent, print a clear `ERROR: missing API keys for a paid accuracy run: ...` plus the `--probe` hint and `process.exit(1)`. Never silently skip an axis on a missing key (fail-closed).

    `--probe`: print the estimated token/cost envelope for the full accuracy suite (reuse the cost-probe approach — run/estimate ONE representative case, report cost + case count) and exit 0 WITHOUT running the full paid suite. State explicitly that subscription marginal cost ≈ $0 but tokens are real and count against limits (per CLAUDE.md sleep-pass cost note).

    `--run` orchestration (paid): invoke the existing accuracy harnesses — `correctness-harness.cjs` (EVAL-02 belief-correction), `locomo-harness.cjs --run` + `locomo-scorer.cjs` (LoCoMo-J headline), and the KU harness — collect their `scores`, and compare against accuracy floors read from `scripts/eval/results/gate-baseline.json` `thresholds` (`eval02_floor`, `locomo_j_floor`, `ku_floor` — written into the baseline by Plan 50-03 Task 3; if a key is absent treat its axis as informational-only, do NOT fail-open silently — print a clear `SKIP: <axis> (no baseline floor)` notice). Accumulate `failures[]`, print `GATE FAIL: <axis> <actual> < floor` per breach, and `process.exit(1)` if any breach; else `GATE PASS (accuracy tier)` exit 0.

    Add a banner at the top of `--run` output making explicit this is the deliberate paid tier and is NOT what `npm run gate` runs (D-03 separation).
  </action>
  <verify>
    <automated>node -c scripts/eval/gate-accuracy-runner.cjs && node scripts/eval/gate-accuracy-runner.cjs 2>&1 | grep -qi usage; echo "mode-guard exit=$?"; env -u ANTHROPIC_API_KEY -u OPENAI_API_KEY node scripts/eval/gate-accuracy-runner.cjs --run; test $? -ne 0 && echo "key-guard fail-closed OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node -c scripts/eval/gate-accuracy-runner.cjs` succeeds (valid syntax)
    - No mode flag → prints usage, exits non-zero (mode-guard)
    - `--run` with ANTHROPIC_API_KEY and OPENAI_API_KEY both unset → exits non-zero with a key-missing error (fail-closed, no silent axis skip)
    - `--probe` exits 0 and prints a token/cost estimate without running the full paid suite
    - The runner is standalone, asserted from its OWN always-present file (order-independent vs the Wave-1 sibling 50-01): `grep -n 'gate-runner' scripts/eval/gate-accuracy-runner.cjs` returns nothing — i.e. `gate-accuracy-runner.cjs` contains no spawn/require of `gate-runner.cjs`. (Reciprocal check: IF `scripts/eval/gate-runner.cjs` already exists at verify time, additionally grep it for `gate-accuracy-runner` and confirm no match; ELSE defer that half — do NOT add a 50-02→50-01 dependency.)
  </acceptance_criteria>
  <done>gate-accuracy-runner.cjs exists, mode-guarded, hard-key-guarded, has a cost probe, compares accuracy axes vs baseline floors, and is never invoked by the cheap gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate-accuracy-runner → claude -p / OpenAI | Outbound paid LLM + embedding calls; real token spend. The boundary risk is *unintended* spend (running the paid tier by accident) and *silent fail-open* (an axis skipped on a missing key counted as pass). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-03 | Elevation / unintended spend | gate-accuracy-runner `--run` | mitigate | Mode-guard requires explicit `--run`; `--probe` available for a zero-suite estimate. Banner states this is the paid tier. |
| T-50-04 | Denial-of-service / fail-open | missing API key | mitigate | Hard key-guard exits non-zero; an axis is never silently skipped-and-passed. Missing baseline floor → explicit `SKIP` notice, not a pass. |
| T-50-SC | Tampering | npm installs | accept | Zero new dependencies; one new script file only. No install task. |
</threat_model>

<verification>
- `node -c` passes; mode-guard and key-guard fail-closed behaviors verified
- grep on the always-present `gate-accuracy-runner.cjs` (the file this plan creates) confirms it does not spawn/require `gate-runner.cjs` — order-independent; the reciprocal check on `gate-runner.cjs` runs only if that Wave-1 sibling already exists at verify time
</verification>

<success_criteria>
- A separate `npm run gate:accuracy` exists for the LLM-routed accuracy + belief-correction axes (GATE-02 accuracy portion), deliberately decoupled from the cheap gate (D-03)
- It fails closed on missing keys and offers a `--probe` cost estimate before any spend
</success_criteria>

<output>
Create `.planning/phases/50-verification-regression-gates/50-02-SUMMARY.md` when done
</output>
</output>
