---
phase: 61-corpus-chrome-index-column-project-browsing
plan: 14
type: execute
wave: 3
depends_on: ["61-10", "61-11", "61-12", "61-13"]
gap_closure: true
gaps: ["GAP-5", "GAP-6", "GAP-7", "GAP-8"]
requirements: ["GAP-5", "GAP-6", "GAP-7", "GAP-8"]
files_modified:
  - src/viz/modules/constants.js
  - src/viz/css/styles.css
autonomous: false
user_setup: []
must_haves:
  truths:
    - "Project rows have no curved/rounded borders — row hover/active surfaces are square-edged (or full-bleed) within the rail."
    - "The corpus view opens with the index rail closed; the graph gets the full canvas by default and the rail opens only on explicit user action (left-edge handle), then docks/reflows as built."
    - "Exiting project focus (active-row toggle, Esc, canvas click) plays the same zoom/frame animation as focusing, in reverse — same duration/easing — instead of snapping."
    - "A schema row under a project is immediately legible: a human-readable title (never a raw UUID) and a presentation that makes clear what these docs are at first glance."
    - "The round-2 live-install sign-off is re-run: GAP-5..8 resolved, GAP-1..4 still resolved (no regression), D1-D4 still resolved (no regression), and the four CORPUS_* feel constants sit at founder-approved values."
  artifacts:
    - path: "src/viz/modules/constants.js"
      provides: "CORPUS_FOCUS_DIM_OPACITY / CORPUS_HOVER_DIM_OPACITY / CORPUS_LABEL_ZOOM_THRESHOLD / CORPUS_FOCUS_TRANSITION_MS at founder-approved values"
      contains: "CORPUS_FOCUS_TRANSITION_MS"
  key_links:
    - from: "founder live-install feedback (post round-2 gap closure)"
      to: "constants.js CORPUS_* tunables + any residual feel tuning"
      via: "closing checkpoint ratchet (54/56/57/58/59 pattern)"
      pattern: "CORPUS_"
---

<objective>
Round-2 closing founder live-install sign-off AFTER the GAP-5..GAP-8 closure — this RE-RUNS the 61-09 checkpoint
(which superseded the still-open 61-04 sign-off) and now additionally verifies the four round-2 gaps are resolved.
Per 61-UAT.md Notes: "61-09 (and the 61-04 sign-off it supersedes) remains open — re-run the live-install sign-off
after GAP-5..GAP-8 close." This is that re-run.

Purpose: GAP-5..8 were closed autonomously (square-edged rows, default-closed rail, animated unfocus, human-readable
schema titles + relabeled section) but their resolution is visual/interactive — only the founder's eye on the live
install can confirm them, no gap regressed, and the four provisional feel constants (untuned after two structural
rounds) can finally be ratcheted.
Output: founder-confirmed resolution of GAP-5..8, no regression of GAP-1..4 and D1-D4, with the four CORPUS_*
constants (and any residual feel values) at their approved settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-CONTEXT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UI-SPEC.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-09-PLAN.md
@src/viz/modules/constants.js
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Founder round-2 live-install sign-off — GAP-5..8 resolved + GAP-1..4/D1-D4 no-regression + ratchet feel values</name>
  <files>src/viz/modules/constants.js, src/viz/css/styles.css</files>
  <read_first>
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md (the four round-2 gap truths GAP-5..8
      AND the round-1 GAP-1..4 truths — the pass/fail bar for this checkpoint)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-09-PLAN.md (the sign-off this re-runs — its
      GAP-1..4 + D1-D4 verification steps and the four CORPUS_* constants)
    - src/viz/modules/constants.js (CORPUS_FOCUS_DIM_OPACITY=0.18 ~670, CORPUS_HOVER_DIM_OPACITY=0.30 ~675,
      CORPUS_LABEL_ZOOM_THRESHOLD=1.2 ~681, CORPUS_FOCUS_TRANSITION_MS=500 ~685, and the HUD_CSS_TOKENS
      index-row-active-bg key — the only values eligible for tuning here)
  </read_first>
  <action>
    Pause for the founder. Serve the viz on the live install and drive the verification steps below. Iteratively edit
    ONLY feel values — the four CORPUS_* tunables in constants.js, and (if the founder asks) the index-row-active-bg
    token strength / dock divider in styles.css, and (if requested at 61-12's checkpoint) a small muted descriptor
    line under the Notes/schema section heading (needs a token-routed CSS rule — add it here since 61-14 owns
    styles.css; no raw literal, no amber). Do NOT change logic, add color literals, or alter the schema-title /
    focus-toggle / unfocus-animation / default-closed behavior. Note: CORPUS_FOCUS_TRANSITION_MS now also drives the
    unfocus animation (GAP-7) — tuning it changes BOTH focus and unfocus speed by design. After any edit, re-run the
    invariants suite + tsc. Record the final approved values in the SUMMARY. If the founder reports a STRUCTURAL
    defect (not a feel value) in any gap, capture it as a NEW UAT gap rather than hand-patching here.
  </action>
  <what-built>
    - GAP-5 (61-10): index row surfaces (.index-row, .index-entry) are square-edged — the rounded bottom border is
      gone; hover/active backgrounds + the active left-accent bar are otherwise unchanged. Durable no-rounded-rows
      rule recorded (CSS comment + recense memory).
    - GAP-6 (61-13): the corpus opens with the rail CLOSED — full-width graph + a slim left-edge reopen handle;
      clicking the handle docks/reflows the rail as before; returning to the brain hides both.
    - GAP-7 (61-11): exiting focus (active-row toggle, Esc, canvas click) now ANIMATES a zoom-out over the full
      visible set at CORPUS_FOCUS_TRANSITION_MS — the inverse of focus, not a snap.
    - GAP-8 (61-12): every /index label is human-readable — a UUID slug is replaced by the doc's title (never
      leaked); the nested-schema section renders under the founder-chosen label (default "Notes") that reads as
      one-off docs.
    - The four provisional feel constants remain at their 61-02 shipped values (CORPUS_FOCUS_DIM_OPACITY=0.18,
      CORPUS_HOVER_DIM_OPACITY=0.30, CORPUS_LABEL_ZOOM_THRESHOLD=1.2, CORPUS_FOCUS_TRANSITION_MS=500) pending this ratchet.
  </what-built>
  <how-to-verify>
    Start the viz on the live install and open the corpus view (`recense viz` or the project's viz-serve command,
    then open the printed URL and switch to the corpus view):
    1. GAP-6 (default-closed rail): the corpus should open with the index rail CLOSED — the graph fills the whole
       canvas and only the slim left-edge reopen handle is visible. Click the handle → the rail docks and the graph
       reflows to its right (as built). Collapse (◀) → graph returns to full width, handle reappears. Switch to the
       brain and back → the corpus opens closed again (rail does not auto-open).
    2. GAP-5 (square rows): open the rail. Project and doc rows must have NO rounded corners — hover/active
       backgrounds are square-edged (full-bleed within the rail). No curved bottom border on any project label.
    3. GAP-7 (animated unfocus): click a project name → the graph animates in to focus (zoom + dim). Exit three
       ways and confirm each ANIMATES back out (no snap) at the same speed/easing as focusing: (a) click the active
       row again, (b) press Esc, (c) click empty canvas.
    4. GAP-8 (legible schema rows): expand a project (and/or scroll the nested-schema section). Each schema row must
       show a human-readable title — NO raw UUIDs. The section heading should read as one-off docs (the label chosen
       at 61-12's checkpoint, e.g. "Notes"), making it clear what these docs are at a glance.
    5. GAP-1..4 no-regression: re-confirm the round-1 truths still hold — docked rail reflow (once opened), rows read
       as intentionally designed, active-row + click-to-unfocus toggle with Esc/canvas-click sync, and schemas nested
       under their related project (not free-floating peers).
    6. D1-D4 no-regression (from 61-04/61-09): D1 glass navigable tree, D2 project focus browsing, D3 declutter
       (no at-rest label pile-up; hover dims + amber-highlights the subtree; chapters appear only when browsing their
       project), D4 reader round-trip (doc click → reader → back; Esc closes the reader FIRST while focused and focus
       persists; a second Esc with the reader closed exits focus).
    7. Feel tuning: if the focus dim, hover dim, subject-label zoom-in point, or focus/unfocus zoom speed feels off,
       tell the executor the direction; it edits ONLY the CORPUS_* constants (and, if asked, index-row-active-bg /
       the divider / an optional section descriptor) and you re-check until it feels right.
  </how-to-verify>
  <verify>
    <automated>cd /Users/vtx/brain-memory && npx tsc --noEmit 2>/dev/null; npx vitest run tests/viz-activity-palette-invariants.test.ts tests/viz-index-route.test.ts tests/viz-corpus-graph.test.ts tests/viz-frontend-static.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Founder confirms GAP-5, GAP-6, GAP-7, GAP-8 resolved on the live install (each gap truth observed).
    - Founder confirms GAP-1..4 no-regression and D1-D4 no-regression (including Esc-close-reader-while-focused keeping focus).
    - Any tuned feel values are recorded in constants.js (and styles.css if the active-bg/divider/descriptor was tuned);
      no logic, behavior, or color-literal change was made during tuning.
    - After the final edit: `npx tsc --noEmit` clean and `npx vitest run tests/viz-activity-palette-invariants.test.ts
      tests/viz-index-route.test.ts tests/viz-corpus-graph.test.ts tests/viz-frontend-static.test.ts` all green.
  </acceptance_criteria>
  <done>Founder confirms GAP-5..8 resolved, GAP-1..4 and D1-D4 not regressed; the four CORPUS_* constants (and any residual feel values) sit at approved settings; tsc clean and the four viz suites green.</done>
  <resume-signal>Type "approved" (optionally with final constant values), or describe what still feels off (which gap/defect, which direction to tune).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder feedback → constants.js / styles.css | Only feel values (CORPUS_* tunables, active-bg token, divider, optional descriptor) change; no logic, no behavior, no raw color literal. |
| viz server → browser | Presentation-only; no server surface touched by this checkpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-26 | Tampering | tuning edits | mitigate | Only named tunables / token values change; re-run invariants + index-route + corpus-graph + frontend-static + tsc after any edit; structural defects captured as NEW UAT gaps, not hand-patched. |
</threat_model>

<verification>
- Founder types "approved" for GAP-5..8 resolved + GAP-1..4 / D1-D4 no-regression on the live install.
- After any edit: `npx tsc --noEmit` clean; the four viz suites (invariants, index-route, corpus-graph, frontend-static) green.
- Final approved values recorded in constants.js/styles.css and the SUMMARY.
</verification>

<success_criteria>
- GAP-5..8 confirmed resolved; GAP-1..4 and D1-D4 confirmed not regressed by the founder on the live install.
- The four CORPUS_* constants (and any residual feel values) ratcheted to approved settings.
- Invariants / index-route / corpus-graph / frontend-static suites green; tsc clean.
</success_criteria>

<output>
Create `.planning/phases/61-corpus-chrome-index-column-project-browsing/61-14-SUMMARY.md` when done, recording the final approved feel values and the founder's GAP-5..8 + GAP-1..4 + D1-D4 sign-off. Note that this SUMMARY supersedes the still-open 61-09 sign-off (which itself superseded 61-04) — those need not be executed separately.
</output>
