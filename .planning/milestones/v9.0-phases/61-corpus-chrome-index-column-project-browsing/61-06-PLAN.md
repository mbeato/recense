---
phase: 61-corpus-chrome-index-column-project-browsing
plan: 06
type: execute
wave: 2
depends_on: ["61-05"]
gap_closure: true
gaps: ["GAP-2"]
requirements: ["GAP-2"]
files_modified:
  - src/viz/css/styles.css
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Project rows keep chevron + doc-count structure but look intentionally designed — spacing, alignment, hover/active states — not default file-explorer pills."
  artifacts:
    - path: "src/viz/css/styles.css"
      provides: "Refined .index-row / .index-chevron / .index-count / .index-entry typography, spacing, alignment and hover states — token-only"
      contains: ".index-row"
  key_links:
    - from: ".index-row (chevron + name + count)"
      to: "Phase-59 token vocabulary"
      via: "var(--token) color/spacing only, zero raw literals"
      pattern: "var(--"
---

<objective>
Close GAP-2: the project rows keep the right structure (chevron + name + doc-count badge) but read as
generic "AI-slop" file-explorer detailing. Refine the row design — typography, spacing, alignment, and
interaction states — so rows read as intentionally designed and consistent with the Phase-59 token
vocabulary. This is a CSS-only refinement pass; the row STRUCTURE and the JS render are unchanged.

Purpose: GAP-2 is the "rows look default" defect. Founder direction: keep chevron/count structure, fix the
detailing. It depends on GAP-1 (docked layout) since the final rail width/context is what the rows sit in.
Output: Rows that read as designed — deliberate vertical rhythm, aligned chevron/name/count, quiet-but-clear
hover — with zero new raw color literals.

Constraint (hard): token-only. No raw hex/rgba literal may enter styles.css (D-14-A `findRawColorLiteralLines`
lock). No amber anywhere (D-14-B amber-exclusivity). Reuse existing HUD_CSS_TOKENS keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-CONTEXT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UI-SPEC.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md

<interfaces>
<!-- Current row CSS (extracted) + the token palette available for reuse -->

styles.css current row blocks:
  .index-row { display:flex; align-items:center; gap:4px; color:var(--text-recede); border-radius:6px;
               cursor:pointer; transition: background .12s, color .12s; padding-left/right set inline by JS. }
  .index-row:hover { color: var(--text-bright); background: var(--index-search-focus-bg); }
  .index-chevron { width:18px; height:18px; ... color:var(--text-recede); }
  .index-chevron:hover { color: var(--text-bright-mauve); }
  .index-chevron svg { transition: transform var(--motion-fast) var(--ease-out-soft); }
  .index-chevron.expanded svg { transform: rotate(90deg); }
  .index-count { font-family:'JetBrains Mono HUD'; font-size:11px; color:var(--text-stat); margin-left:auto; }
  .index-entry { display:block; font-size:13px; line-height:1.35; color:var(--text-recede);
                 padding:5px 8px; border-radius:6px; transition: background .12s, color .12s; }
  .index-entry:hover { color: var(--text-bright); background: var(--index-search-focus-bg); }

Reusable tokens (HUD_CSS_TOKENS, constants.js — DO NOT add raw literals, reuse these):
  --text-recede #9a8aa4, --text-bright #e7dfec, --text-bright-mauve #c8bcd0, --text-mauve-rest #8b7090,
  --text-stat #7fae93, --text-data-value #9aa4b0,
  --index-search-focus-bg rgba(139,112,144,.12), --index-hairline rgba(139,112,144,.22),
  --radius-xs 4px, --radius-sm 7px, --motion-fast 120ms, --ease-out-soft.
UI-SPEC spacing scale: xs 4px (chevron→label gap), sm 8px (row h-padding base), tree indent formula 8+depth*14,
row v-padding 5px (existing). Count badge is mono (JetBrains Mono HUD), bare integer, per UI-SPEC.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine project-row typography, spacing and alignment (token-only)</name>
  <files>src/viz/css/styles.css</files>
  <read_first>
    - src/viz/css/styles.css (the .index-row / .index-row:hover / .index-chevron / .index-count / .index-entry
      blocks ~1409-1471 — read all so the refinement extends the exact current declarations)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UI-SPEC.md (Typography table,
      Spacing Scale, and Copywriting Contract → Doc-count badge = bare integer mono, plus Color rules that
      forbid amber on chevron/count/row)
    - src/viz/modules/constants.js (HUD_CSS_TOKENS ~700-828 — the ONLY source of color/spacing tokens; confirm
      every value you reference exists as a key)
  </read_first>
  <action>
    Refine the four row classes so rows read as designed, keeping the flex chevron+name+count structure:
    1. Vertical rhythm: give `.index-row` an explicit vertical padding matching the existing `.index-entry` 5px
       (currently .index-row has no v-padding, so project rows and doc rows sit at different heights — align them).
       Keep the JS-set inline `padding-left`/`padding-right` (the `8 + depth*14` indent formula and 8px right pad)
       intact — do NOT hardcode horizontal padding in CSS that would fight the inline indent.
    2. Chevron alignment: shrink the chevron hit box from 18px toward the UI-SPEC ~11-12px glyph in a tidy target
       (keep it visually centered on the row's baseline via `align-items:center`), and set the chevron→label gap to
       the xs token (4px) via the existing `.index-row` `gap`. Keep the CSS rotate on `.index-chevron.expanded svg`.
    3. Name typography: ensure the project name anchor inside `.index-row` uses the same 13px/1.35 body face as
       `.index-entry` (it already reuses `.index-entry doc-ref`); add a subtle weight/tracking distinction so a
       project row reads as a heavier "folder" than its leaf docs — e.g. font-weight 500 on the name inside
       `.index-row` (a `.index-row .index-entry` selector), NOT on leaf rows. No color change beyond tokens.
    4. Count badge: keep the mono face and bare integer; align it hard-right (margin-left:auto is already there),
       give it a slightly recessed color (`var(--text-data-value)` or keep `var(--text-stat)` — pick the quieter
       of the two so the count reads as a readout, not a loud pill) and add small horizontal breathing room
       (padding via a token, e.g. 0 var(--radius-xs)). No background pill unless it stays token-routed and quiet.
    5. Hover state: keep the existing `.index-row:hover` background (`var(--index-search-focus-bg)`) + brightened
       text; make the transition consistent with `--motion-fast`. Leave room for GAP-3 to add an `.active` state
       (do NOT add active styling here — that is 61-07's scope) but do not use a hover treatment so strong that a
       later active state can't out-rank it.
    Every color must be a `var(--token)` from HUD_CSS_TOKENS. Zero raw hex/rgba. No amber. No structural HTML change.
  </action>
  <verify>
    <automated>cd /Users/vtx/brain-memory && npx vitest run tests/viz-activity-palette-invariants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.index-row` and `.index-entry` render at the same row height (both carry a 5px vertical padding) — source
      assertion in styles.css.
    - The chevron→label gap uses the 4px xs value and the count badge stays right-aligned (`margin-left:auto`).
    - A `.index-row .index-entry` (or equivalent) rule distinguishes the project name weight from leaf-doc rows.
    - `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(' src/viz/css/styles.css` shows NO new raw literal was added inside the
      touched row blocks (all colors via var(--token)); the D-14-A `findRawColorLiteralLines` test stays green.
    - `npx vitest run tests/viz-activity-palette-invariants.test.ts` passes (45/45), including the amber-exclusivity
      lock (no new amber key/literal).
  </acceptance_criteria>
  <done>Project rows read as intentionally designed (aligned chevron/name/count, deliberate rhythm, quiet hover) while keeping the chevron + count structure; all colors token-routed; invariants suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS token source → styles.css | Every color/spacing value must resolve through var(--token); no raw literal, no amber outside AMBER_ALLOWED_KEYS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-13 | Tampering | styles.css row refinement | mitigate | Token-only; run `npx vitest run tests/viz-activity-palette-invariants.test.ts` (D-14-A literal ban + D-14-B amber lock) after the edit. |
</threat_model>

<verification>
- Automated: invariants suite green (45/45); no raw literal introduced.
- Behavioral (deferred to 61-09 founder checkpoint): rows read as designed rather than default file-explorer pills.
</verification>

<success_criteria>
- Rows keep chevron + count structure and read as intentionally designed (spacing, alignment, hover).
- Zero raw color literals; no amber; invariants suite green.
</success_criteria>

<output>
Create `.planning/phases/61-corpus-chrome-index-column-project-browsing/61-06-SUMMARY.md` when done.
</output>
