---
phase: 61-corpus-chrome-index-column-project-browsing
plan: 18
type: execute
wave: 2
depends_on: ["61-17"]
files_modified:
  - src/viz/css/styles.css
  - src/viz/modules/index.js
  - src/viz/index.html
autonomous: false
gap_closure: true
requirements: ["GAP-10"]

must_haves:
  truths:
    - "The corpus index is a docked LEFT panel/column that lives outside the graph canvas, attached to the main app window (side-by-side) — not a floating overlay (GAP-10)"
    - "Opening the index reflows #corpus-graph to sit BESIDE the panel; closing it returns the graph to the full canvas"
    - "The index still opens closed-by-default via the reopen handle (GAP-6 preserved); tree/focus/active-row/filter/Esc all still work"
    - "Index PROJECT rows remain square-edged — no rounded/curved borders (GAP-5 hard rule preserved)"
  artifacts:
    - path: "src/viz/css/styles.css"
      provides: "#index-panel restyled as a docked full-height left column (flush left edge, edge-only border, no floating offset/rounded-window chrome); .index-docked #corpus-graph reflow rule reinstated"
      contains: "index-docked #corpus-graph"
    - path: "src/viz/modules/index.js"
      provides: "openSidebar/hidePanel toggle body.index-docked + call ctx.refitCorpus; header drag wiring + panelPos state removed"
  key_links:
    - from: "src/viz/modules/index.js openSidebar/hidePanel"
      to: "#corpus-graph canvas width"
      via: "body.classList.add/remove('index-docked') + ctx.refitCorpus()"
      pattern: "index-docked"
    - from: ".index-docked (body)"
      to: "#corpus-graph left offset"
      via: "CSS reflow rule"
      pattern: "index-docked #corpus-graph"
---

<objective>
Close GAP-10 (BLOCKER) — the founder's twice-recorded, dated, verbatim rejection of every non-docked
index layout this phase has shipped. Founder verdict at the 61-16 Task 3 live-install sign-off
(2026-07-16): "index on top of graph is still out of place needs to be outside the window as a left
panel but still attached to the main window."

History: GAP-1 built a docked rail WITH reflow → GAP-9 un-docked it into a floating draggable panel
(61-16) → the founder rejected the floating-over-canvas result and now demands a docked LEFT panel
OUTSIDE the graph viewport, attached to the main window, with the graph reflowing beside it. This
REVERSES 61-16's "graph keeps full canvas, no reflow" premise. Rework 61-16's floating-panel CSS/JS
into the docked side-by-side layout (per 61-16-SUMMARY.md's own next-steps note — the floating build
is the substrate, not a from-scratch rebuild).

Key point about resurrected code: `ctx.refitCorpus()` (corpus.js ~723) and the `.index-docked`
docking comments in corpus.js still EXIST (dead after 61-16). GAP-10 makes them LIVE again — index.js
re-wires them. What GAP-10 ORPHANS is the 61-16 drag machinery (pointer handlers, panelPos, dragging
class, grab cursor, floating offset/rounded-window chrome) — remove that.

Purpose: end Phase 61 with the layout the founder actually asked for. Output: a docked left index
column with a reflowing corpus graph, founder-verified on the live install.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-VERIFICATION.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UI-SPEC.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-16-SUMMARY.md

<interfaces>
<!-- Extracted from live source — executor uses these directly; no exploration needed. -->

DOM (index.html): `<div id="corpus-graph"></div>` (line 34) and `<div id="index-panel"></div>`
(line 157) are BOTH direct children of <body>, both position:fixed. The reflow is a body-class
descendant selector (`.index-docked #corpus-graph`), so NO DOM restructuring is needed.

styles.css current floating chrome (1273-1320, from 61-16 — the substrate to REWORK):
- comment block 1273-1282 documents the "DETACHED FLOATING WINDOW … DO NOT re-dock" paradigm — this comment must be REPLACED (its instruction is now inverted by the founder).
- `:root { --index-width: 270px; }` (1283) — keep; it will drive BOTH the panel width AND the graph left-offset again.
- `#index-panel` (1284-1305): `position:fixed; top:56px; left:16px; width:var(--index-width); max-width:60vw; max-height:calc(100vh - 56px - 16px); z-index:8; display:none→flex; flex-direction:column;` glass-focused tier (`--glass-bg-focused`, `border:1px solid var(--glass-border-focused)`, `border-radius:var(--radius-lg)`, `backdrop-filter:blur(var(--glass-blur-md))` + -webkit twin, `box-shadow:var(--glass-specular)`), `opacity` fade via `.shown`.
- `.index-sidebar-header` (1311-1319): `cursor: grab;` — the drag-grip cursor to REMOVE.
- `#index-panel.dragging .index-sidebar-header { cursor: grabbing; }` (1320) — the drag rule to REMOVE.
- #corpus-graph (1200-1218): `position:fixed; inset:0; z-index:5; opacity fade`. inset:0 sets left:0/right:0 — the reflow rule overrides `left` to `var(--index-width)`.
- The pre-GAP-9 docked geometry to restore: `top:0; left:0; bottom:0; width:var(--index-width); border-right:1px solid var(--glass-border-focused);` (no floating offset, no max-height, no rounded outer container).
- The reflow rule to REINSTATE: `.index-docked #corpus-graph { left: var(--index-width); }`.
- GAP-5 square-edge block (`.index-row`/`.index-entry`, ~1427-1497) — DO NOT touch; `grep border-radius` scoped there must stay ZERO hits.

index.js current floating mount (lines 40-160, 447-504 — the substrate to REWORK):
- module header comment (~8) says "dragging the panel never reflows #corpus-graph" — REPLACE (now inverted).
- `let panelPos = null;` (66) + its comment (63-66) — REMOVE (drag state).
- `ensureChrome()` header drag wiring (88-126): `dragStart`, `header.addEventListener('pointerdown'|'pointermove'|'pointerup'|'pointercancel')`, `setPointerCapture`, `container.classList.add('dragging')`, the clamp math — REMOVE all of it (the header is no longer a drag grip).
- `openSidebar()` (447-463): currently sets display:flex, applies panelPos, double-rAF `.shown`, `prepareIndex()`. RE-ADD `document.body.classList.add('index-docked')` and `ctx.refitCorpus()` (guarded); REMOVE the panelPos apply block (453-456).
- `hidePanel()` (465-477): currently removes `.shown`, hides after transitionend/timeout, toggles handle. RE-ADD `document.body.classList.remove('index-docked')` and a guarded `ctx.refitCorpus()` so the graph re-expands to full width.
- `ctx.openIndex` (487) + `ctx.openIndexSidebar` (485): `ctx.openIndex` is a dead alias (zero callers per 61-REVIEW.md IN-02) — remove it. `ctx.openIndexSidebar` is referenced by index.html/comments — keep.

corpus.js re-wired contracts (NO corpus.js edit needed — these already exist and become live again):
- `ctx.refitCorpus` (723-726): `sizeCorpusGraph(); fitAndClamp();` — the reflow re-fit index.js will call.
- `sizeCorpusGraph()` (563-571): reads `container.clientWidth` which reflects the `.index-docked` left offset — its comment (565-567) becomes ACCURATE again; leave as-is.
- `goToCorpus()` (668-672): "then it docks/reflows as built" comment becomes accurate again; leave as-is.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework #index-panel from floating window to docked left column + reinstate the canvas reflow (CSS)</name>
  <files>src/viz/css/styles.css</files>
  <read_first>
    - src/viz/css/styles.css (lines 1194-1218 — #corpus-graph fixed/inset:0 host, the reflow target)
    - src/viz/css/styles.css (lines 1273-1344 — the 61-16 floating #index-panel + .index-sidebar-header + drag chrome to rework)
    - src/viz/css/styles.css (lines 1427-1497 — the .index-row/.index-entry GAP-5 square-edge ban + durable comment; DO NOT modify)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md (GAP-10 founder verbatim + scope_hint; GAP-5 hard rule)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UI-SPEC.md (glass-tier + color token rationale)
  </read_first>
  <action>
    Convert `#index-panel` (styles.css ~1284) from the 61-16 floating window back into a docked
    full-height LEFT column attached to the window edge, and reinstate the graph reflow:
    - Anchoring: replace `top:56px; left:16px; max-height:calc(100vh - 56px - 16px)` with the docked
      geometry `top:0; left:0; bottom:0` (full-height, flush to the window's left edge). Keep
      `width: var(--index-width)`. REMOVE `max-width: 60vw` (a full-height dock is not width-constrained
      that way) — or keep only if it does not shrink the 270px column. Keep `display:none`→flex,
      `flex-direction:column`, `box-sizing:border-box`, the `opacity` `.shown` fade, and z-index.
    - Chrome: REMOVE `border-radius: var(--radius-lg)` (a flush-edge docked column is NOT a rounded
      window) and replace the full `border: 1px solid var(--glass-border-focused)` with an edge-only
      `border-right: 1px solid var(--glass-border-focused)`. KEEP `background: var(--glass-bg-focused)`,
      `backdrop-filter: blur(var(--glass-blur-md))` (+ `-webkit-` twin), and `box-shadow: var(--glass-specular)`
      so the Phase-59 glass vocabulary (D1) and the D-14 invariants stay intact. ZERO new raw color
      literals (D-14-A), NO amber (D-14-B). `#index-panel`/`#index-reopen` are already on the
      backdrop-filter allow-list — no test change needed.
    - Header: REMOVE `cursor: grab` from `.index-sidebar-header` (~1318) and DELETE the
      `#index-panel.dragging .index-sidebar-header { cursor: grabbing; }` rule (~1320). The header is a
      static title bar now (title + collapse button), not a drag grip.
    - Reflow: REINSTATE `.index-docked #corpus-graph { left: var(--index-width); }` so the corpus graph
      cedes width to the docked column (side-by-side, graph starts at x=270 while the panel occupies
      0–270). Place it adjacent to the #corpus-graph rules or the #index-panel block.
    - Comment: REPLACE the 1273-1282 "DETACHED FLOATING WINDOW … DO NOT re-dock" comment with a durable
      GAP-10 note documenting the docked-left-column paradigm (index is OUTSIDE the graph, attached to
      the main window, graph reflows beside it via `.index-docked`) so a future reader does not
      re-float it. Keep `:root { --index-width: 270px; }`.
    Do NOT touch `.index-row`/`.index-entry`/`.index-chevron`/`.index-count` or the GAP-5 square-edge
    comment. Do NOT touch `#index-reopen`.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-activity-palette-invariants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `#index-panel` uses docked geometry: `grep -n "top: 0" src/viz/css/styles.css` near #index-panel confirms `top:0; left:0; bottom:0`; the `top:56px`/`left:16px` floating offset and `max-height:calc(...)` are gone.
    - `grep -n "index-docked #corpus-graph" src/viz/css/styles.css` returns the reinstated reflow rule.
    - `grep -n "cursor: grab" src/viz/css/styles.css` returns NOTHING (drag-grip cursor removed); the `#index-panel.dragging` rule is gone.
    - `#index-panel` has `border-right` (edge-only), NOT `border-radius` — the rounded-window container chrome is removed.
    - GAP-5 preserved: `grep -n "border-radius" src/viz/css/styles.css` still shows ZERO hits scoped within the `.index-row`/`.index-entry` rules.
    - D-14 invariants intact: `npx vitest run tests/viz-activity-palette-invariants.test.ts` green (45; no new raw color literal, amber allow-list intact, backdrop-filter allow-list intact).
    - `npx vitest run tests/viz-frontend-static.test.ts` green.
  </acceptance_criteria>
  <done>#index-panel reads as a docked full-height glass left column flush to the window edge (edge-only border, no rounded-window chrome, no drag cursor); the `.index-docked #corpus-graph` reflow rule is reinstated so the graph sits beside it.</done>
</task>

<task type="auto">
  <name>Task 2: Re-wire the dock reflow trigger and remove the floating drag machinery (JS)</name>
  <files>src/viz/modules/index.js, src/viz/index.html</files>
  <read_first>
    - src/viz/modules/index.js (lines 1-15 — module header comment describing the floating paradigm to invert)
    - src/viz/modules/index.js (lines 45-160 — init state incl. panelPos; ensureChrome header + drag wiring to remove)
    - src/viz/modules/index.js (lines 441-504 — openSidebar/hidePanel/collapseSidebar/closeIndexSidebar/ctx hooks/showIndexHandle)
    - src/viz/modules/corpus.js (lines 720-726 — ctx.refitCorpus, the existing hook to call; do NOT edit corpus.js)
    - src/viz/index.html (lines 150-157 — the #index-panel host comment describing the floating paradigm)
    - src/viz/css/styles.css (the docked #index-panel rules from Task 1 — the JS must not fight them)
  </read_first>
  <action>
    Re-wire index.js so opening/closing the docked panel reflows the graph, and delete the 61-16 drag
    machinery orphaned by the docked layout. Do NOT edit corpus.js — `ctx.refitCorpus` already exists.
    - `openSidebar()` (~447): RE-ADD `document.body.classList.add('index-docked')` (before or with the
      display:flex), and after revealing, call `ctx.refitCorpus()` guarded by
      `typeof ctx.refitCorpus === 'function'` so the canvas resizes to the remaining width. REMOVE the
      `if (panelPos) { container.style.left/top … }` apply block (453-456) — a docked column has a fixed
      CSS position, no restored drag offset. Keep `showReopenHandle(false)`, `container.style.display='flex'`,
      the double-rAF `.shown` fade, and `prepareIndex()`.
    - `hidePanel(showHandleAfter)` (~465): RE-ADD `document.body.classList.remove('index-docked')` and a
      guarded `ctx.refitCorpus()` so the graph re-expands to the full canvas when the panel closes. Keep
      the `.shown` removal, the transitionend/timeout `display:none` hide, and `showReopenHandle(showHandleAfter)`.
    - REMOVE the drag machinery in `ensureChrome()` (88-126): the `dragStart` variable, all four
      `header.addEventListener('pointerdown'|'pointermove'|'pointerup'|'pointercancel', …)` handlers, the
      `setPointerCapture`/`releasePointerCapture` calls, `container.classList.add/remove('dragging')`, and
      the clamp math. Keep the header, title, and collapse button and its click→collapseSidebar wiring.
    - REMOVE `let panelPos = null;` and its comment (63-66) — dead once the drag handlers are gone.
    - REMOVE the dead `ctx.openIndex = openSidebar;` alias (~487, zero callers per 61-REVIEW.md IN-02).
      Keep `ctx.openIndexSidebar` and `ctx.closeIndexSidebar`.
    - Update the module header comment (~8) and the index.html #index-panel host comment (~150-157) from
      the "floating panel, never reflows #corpus-graph" description back to the docked-left-column
      paradigm (index docks at the left edge outside the graph; the graph reflows beside it via
      `.index-docked`; opened explicitly via the reopen handle, GAP-6 default-closed unchanged).
    Preserve ALL inner behavior: collapsible tree, project name-click focus (WR-04 single-writer path),
    active row, filter, hover highlight, Esc-exit, the reopen handle, and GAP-6 default-closed
    (`ctx.showIndexHandle` reveals only the handle). Do NOT alter renderSections/makeProjectRow/
    computeVisible/syncCorpusFocus logic.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-frontend-static.test.ts tests/viz-index-route.test.ts tests/viz-corpus-graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "index-docked" src/viz/modules/index.js` shows the class added in openSidebar and removed in hidePanel (reflow re-wired).
    - `grep -n "refitCorpus" src/viz/modules/index.js` shows guarded calls in BOTH openSidebar and hidePanel.
    - Drag machinery gone: `grep -nE "pointer(down|move|up|cancel)|setPointerCapture|panelPos|classList.add\('dragging'\)|dragging" src/viz/modules/index.js` returns NOTHING.
    - `grep -n "ctx.openIndex\b" src/viz/modules/index.js` returns nothing (dead alias removed); `ctx.openIndexSidebar` still present.
    - Collapse still works: the collapse button's `click → collapseSidebar` wiring is intact (`grep -n "collapse.addEventListener('click', collapseSidebar)" src/viz/modules/index.js`).
    - `grep -n "localStorage" src/viz/modules/index.js` returns nothing (position state fully removed, no persistence added).
    - `npx vitest run tests/viz-frontend-static.test.ts tests/viz-index-route.test.ts tests/viz-corpus-graph.test.ts` green; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Opening the index adds body.index-docked + refits the corpus graph to the remaining width; closing removes the class + re-expands the graph; the floating drag handlers, panelPos, and dead ctx.openIndex alias are gone; all tree/focus/filter behaviors and GAP-6 default-closed are intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Founder live-install sign-off on the docked left index panel (GAP-10)</name>
  <files>(none — verification checkpoint)</files>
  <read_first>
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md (GAP-10 founder verbatim direction; GAP-1..GAP-8 no-regression list)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-16-SUMMARY.md (round-3 sign-off protocol: rebuild before verify; capture structural feedback as new gaps, no hand-patching)
  </read_first>
  <action>Checkpoint — do NOT auto-advance (this is a founder feel + no-regression gate, and the index layout has been rejected twice this phase). After Tasks 1-2 merge and `npm run build` runs, PAUSE and present the docked-left-panel build to the founder using the how-to-verify steps below. Plan 61-17 (GAP-7 animated clamp + GAP-8 /graph UUID scrub) is a prerequisite of this plan, so those fixes are also live — the no-regression pass should confirm them. Capture any remaining issues as new UAT gaps rather than hand-patching at the checkpoint (Rule 4 — structural feel changes get their own follow-up plan).</action>
  <what-built>
    #index-panel is now a docked full-height glass LEFT column flush to the window's left edge
    (edge-only border, no rounded-window chrome, no drag), OUTSIDE the graph canvas. Opening it adds
    body.index-docked and reflows #corpus-graph to sit BESIDE it (graph starts at x=270); closing it
    returns the graph to the full canvas. The index still opens closed-by-default via the left-edge
    reopen handle (GAP-6). All prior behaviors (collapsible tree, project focus flow, active row,
    Esc-exit, animated unfocus, square-edged rows, human-readable Notes titles, filter) are preserved.
    The GAP-7 animated-clamp fix and GAP-8 /graph UUID-scrub from plan 61-17 are also live.
  </what-built>
  <how-to-verify>
    Prerequisite: run `npm run build` first so the packed viz assets reflect this work (prior sign-offs
    hit stale assets — rebuild before verifying).
    1. Open the app, enter the corpus view. Confirm the index opens CLOSED by default (only the
       left-edge reopen handle shows, graph on the full canvas).
    2. Click the reopen handle. Confirm the index docks as a LEFT PANEL/COLUMN outside the graph,
       attached to the window's left edge — NOT floating on top of the graph — and the graph REFLOWS to
       sit beside it (the graph visibly cedes width; nothing is hidden underneath the panel). This is
       the exact thing rejected twice: confirm "index on top of graph" is gone.
    3. Collapse the panel (header collapse button). Confirm the graph re-expands to the full canvas.
    4. No-regression pass: expand a project (chevron), click a project name (graph focuses + dims +
       reveals chapters, active row paints), click the active row again / press Esc / click empty canvas
       (ANIMATED zoom-out, no snap — GAP-7), confirm rows are square-edged (no rounded corners — GAP-5),
       hover nodes and Notes rows show human-readable titles (no UUIDs — GAP-8), and the filter box works.
    5. Resize the app window with the panel open; confirm the graph re-fits to the remaining width and
       the panel stays anchored to the left edge.
  </how-to-verify>
  <resume-signal>Type "approved" if the index reads as a docked left panel outside the graph with the graph reflowing beside it and nothing regressed, or describe the issues to capture as follow-up gaps.</resume-signal>
  <verify><human-check>Founder confirms the index is a docked left column outside the graph attached to the main window, the graph reflows beside it, GAP-6 default-closed holds, and nothing from GAP-1..8 / D1-D4 regressed.</human-check></verify>
  <done>Founder types "approved" on the live install, or issues are captured as new UAT gaps for a follow-up plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB-sourced strings → DOM | Index tree labels/slugs render in the docked panel; must stay `.textContent`-only (T-39-08). This plan changes layout/positioning only — no rendering-path change. |
| CSS class state → layout | `body.index-docked` toggles the graph reflow; a pure presentational class, no data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-18-01 | Injection (XSS) | Docked panel row rendering | mitigate | This plan touches positioning/chrome + open/close wiring only; all DB strings keep `.textContent` (T-39-08). No `innerHTML` with DB data introduced (innerHTML receives only static SVG icon constants, unchanged). |
| T-61-18-02 | Tampering | CSS token/glass invariants | mitigate | No new raw color literals or amber (D-14-A/B); backdrop-filter stays on the already-allow-listed `#index-panel`; the invariants suite must stay green. |
| T-61-18-03 | Denial of service (self) | dock reflow re-fit | accept | `ctx.refitCorpus()` runs on explicit open/close/resize only; `fitAndClamp` guards the zoom; guarded `typeof` check keeps it non-fatal if the corpus graph isn't built. |
| T-61-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs (removing drag code, CSS layout only; net-negative deps surface). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run tests/viz-activity-palette-invariants.test.ts` — green (D-14 raw-literal/amber/backdrop-filter invariants intact).
- `npx vitest run tests/viz-frontend-static.test.ts tests/viz-index-route.test.ts tests/viz-corpus-graph.test.ts` — green.
- `npx tsc --noEmit` — clean.
- `npx vitest run` — full suite (180 files / 2699 tests) stays green.
- `grep -n "index-docked #corpus-graph" src/viz/css/styles.css` — reflow rule present.
- `grep -nE "pointer(down|move|up|cancel)|panelPos" src/viz/modules/index.js` — no match (drag machinery removed).
- `grep -n "border-radius" src/viz/css/styles.css` scoped to `.index-row`/`.index-entry` — zero hits (GAP-5).
- Founder live-install sign-off recorded (Task 3).
</verification>

<success_criteria>
- The corpus index is a docked left panel/column outside the graph canvas, attached to the main app window; the graph reflows beside it (GAP-10 founder verdict satisfied).
- Opening reflows the graph; closing returns it to the full canvas; GAP-6 default-closed is preserved.
- The floating drag machinery is fully removed; the GAP-5 square-edge rule and all GAP-1..8 / D1-D4 behaviors hold (with GAP-7/GAP-8 fixes from 61-17 live).
- The founder confirms the docked-left layout on the live install.
</success_criteria>

<output>
Create `.planning/phases/61-corpus-chrome-index-column-project-browsing/61-18-SUMMARY.md` when done.
</output>
