---
phase: 61-corpus-chrome-index-column-project-browsing
plan: 09
type: execute
wave: 4
depends_on: ["61-05", "61-06", "61-07", "61-08"]
gap_closure: true
gaps: ["GAP-1", "GAP-2", "GAP-3", "GAP-4"]
requirements: ["GAP-1", "GAP-2", "GAP-3", "GAP-4"]
files_modified:
  - src/viz/modules/constants.js
  - src/viz/css/styles.css
autonomous: false
user_setup: []
must_haves:
  truths:
    - "The index is a docked full-height sidebar; the graph reflows beside it instead of being overlaid."
    - "Project rows keep chevron + doc-count structure but look intentionally designed — spacing, alignment, hover/active states — not default file-explorer pills."
    - "The focused project's index row shows a clear active state, and clicking it again exits focus; Esc and canvas-click keep working."
    - "Schemas are nested under their related project in the index tree and graph grouping instead of rendering as free-floating peer nodes."
    - "The 61-04 live-install sign-off is re-run: D1-D4 still resolved (no regression) and the four CORPUS_* feel constants sit at founder-approved values."
  artifacts:
    - path: "src/viz/modules/constants.js"
      provides: "CORPUS_FOCUS_DIM_OPACITY / CORPUS_HOVER_DIM_OPACITY / CORPUS_LABEL_ZOOM_THRESHOLD / CORPUS_FOCUS_TRANSITION_MS at founder-approved values"
      contains: "CORPUS_FOCUS_DIM_OPACITY"
  key_links:
    - from: "founder live-install feedback (post-gap-closure)"
      to: "constants.js CORPUS_* tunables + any residual feel tuning"
      via: "closing checkpoint ratchet (54/56/57/58/59 pattern)"
      pattern: "CORPUS_"
---

<objective>
Closing founder live-install sign-off AFTER gap closure — this RE-RUNS the 61-04 checkpoint (which remained
open; its scope was the four CORPUS_* tunables) and additionally verifies the four UAT gaps (GAP-1..GAP-4) are
resolved. Per 61-UAT.md Notes: "Re-run the 61-04 live-install sign-off after gap closure." This is that re-run.

Purpose: gaps GAP-1..4 were closed autonomously (docked sidebar, refined rows, active-row toggle, schema
nesting) but their resolution is visual/interactive — only the founder's eye on the live install can confirm
them, and the four provisional feel constants (dim opacities, label zoom threshold, focus timing) plus any
residual feel values (active-row strength, dock divider) may need a final ratchet.
Output: founder-confirmed resolution of GAP-1..4 and no regression of D1-D4, with the four CORPUS_* constants
(and any residual feel values) at their approved settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-CONTEXT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UI-SPEC.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-04-PLAN.md
@src/viz/modules/constants.js
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Founder live-install sign-off on GAP-1..GAP-4 + D1-D4 no-regression + ratchet feel values</name>
  <files>src/viz/modules/constants.js, src/viz/css/styles.css</files>
  <read_first>
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md (the four gap truths — the
      pass/fail bar for this checkpoint)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-04-PLAN.md (the sign-off this re-runs —
      its D1-D4 verification steps and the four CORPUS_* constants)
    - src/viz/modules/constants.js (the CORPUS_FOCUS_DIM_OPACITY / CORPUS_HOVER_DIM_OPACITY /
      CORPUS_LABEL_ZOOM_THRESHOLD / CORPUS_FOCUS_TRANSITION_MS block and the HUD_CSS_TOKENS index-row-active-bg
      key — the only values eligible for tuning here)
  </read_first>
  <action>
    Pause for the founder. Serve the viz on the live install and drive the verification steps below. Iteratively
    edit ONLY feel values — the four CORPUS_* tunables in constants.js, and (if the founder asks) the
    index-row-active-bg token strength and the dock divider treatment in styles.css — until each reads right. Do
    NOT change logic, add color literals, or alter the schema-resolution / focus-toggle behavior. After any edit,
    re-run the invariants suite and tsc. Record the final approved values in the SUMMARY. If the founder reports a
    STRUCTURAL defect (not a feel value) in any gap, capture it as a new UAT gap rather than hand-patching here.
  </action>
  <what-built>
    - GAP-1 (61-05): the index is now a flush, full-height docked rail — the corpus canvas reflows to sit BESIDE
      it (not underneath); collapsing returns the full canvas width; the floating-drawer cue and the stale
      "does NOT reflow" comment are gone.
    - GAP-2 (61-06): project rows keep the chevron + doc-count structure but are refined — aligned chevron/name/
      count, deliberate vertical rhythm, quiet-but-clear hover, token-only (no raw literals, no amber).
    - GAP-3 (61-07): the focused project's index row shows a clear non-amber active state; clicking the active
      row again exits focus (toggle); Esc and empty-canvas click still exit and now also clear the row's active
      state (two-way sync via ctx.syncCorpusFocus).
    - GAP-4 (61-08): schemas with a resolvable D-37-gated project scope now nest under their related project in
      the index tree and group under that project in the graph (dim/focus/reveal) — no longer free-floating peers;
      unresolvable schemas stay in the Schemas section as before.
    - The four provisional feel constants remain at their 61-02 shipped values (CORPUS_FOCUS_DIM_OPACITY=0.18,
      CORPUS_HOVER_DIM_OPACITY=0.30, CORPUS_LABEL_ZOOM_THRESHOLD=1.2, CORPUS_FOCUS_TRANSITION_MS=500) pending this
      ratchet.
  </what-built>
  <how-to-verify>
    Start the viz on the live install and open the corpus view:
    1. Run `recense viz` (or the project's viz-serve command) and open the browser at the printed URL; switch to
       the corpus view.
    2. GAP-1 (docked sidebar): confirm the index reads as a docked full-height rail with the corpus graph sitting
       to its RIGHT (not overlaid underneath). Collapse the rail (◀) — the graph should expand to full width.
       Reopen via the left-edge handle — it should re-dock and the graph re-reflow. It should NOT feel like a
       floating drawer bolted over the graph.
    3. GAP-2 (rows): confirm project rows read as intentionally designed — chevron, name, and count aligned, tidy
       spacing, a quiet hover — not default file-explorer pills.
    4. GAP-3 (active + unfocus): click a project name — its row should show a clear active state and the graph
       should focus (zoom + dim). Click the SAME active row again — focus should exit and the active state clear
       (toggle). Re-focus, then press Esc — focus exits and the active state clears. Re-focus, then click empty
       canvas — same. Focus a different project — the active state should move.
    5. GAP-4 (schemas): confirm schemas appear NESTED under their related project in the index tree (expand a
       project to see them) and group with that project's cluster in the graph — not as a separate free-floating
       peer section/cluster. (Schemas with no resolvable project may still appear under the Schemas section — that
       is the accepted fallback.)
    6. D1-D4 no-regression (re-run of 61-04): re-confirm D1 (glass navigable tree), D2 (project focus browsing),
       D3 (declutter: no at-rest label pile-up, hover dims + amber-highlights the subtree, chapters appear only
       when browsing their project), and D4 (reader round-trip: doc click → reader → back still works; Esc closes
       the reader FIRST while a project is focused and focus persists; a second Esc with the reader closed exits
       focus).
    7. Feel tuning: if the focus dim, hover dim, subject-label zoom-in point, focus zoom speed, active-row
       strength, or the dock divider feels off, tell the executor the direction; it edits ONLY the CORPUS_*
       constants (and, if asked, index-row-active-bg / the divider) and you re-check until it feels right.
  </how-to-verify>
  <verify>
    <automated>cd /Users/vtx/brain-memory && npx tsc --noEmit 2>/dev/null; npx vitest run tests/viz-activity-palette-invariants.test.ts tests/viz-index-route.test.ts tests/viz-corpus-graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Founder confirms GAP-1, GAP-2, GAP-3, GAP-4 resolved on the live install (each gap truth observed).
    - Founder confirms D1-D4 no-regression (including Esc-close-reader-while-focused keeping focus).
    - Any tuned feel values are recorded in constants.js (and styles.css if the active-bg/divider was tuned); no
      logic, behavior, or color-literal change was made during tuning.
    - After the final edit: `npx tsc --noEmit` clean and `npx vitest run tests/viz-activity-palette-invariants.test.ts
      tests/viz-index-route.test.ts tests/viz-corpus-graph.test.ts` all green.
  </acceptance_criteria>
  <done>Founder confirms GAP-1..4 resolved and D1-D4 not regressed; the four CORPUS_* constants (and any residual feel values) sit at approved settings; tsc clean and the three viz test suites green.</done>
  <resume-signal>Type "approved" (optionally with final constant values), or describe what still feels off (which gap/defect, which direction to tune).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder feedback → constants.js / styles.css | Only feel values (CORPUS_* tunables, active-bg token, divider) change; no logic, no behavior, no raw color literal. |
| viz server → browser | Presentation-only; no server surface touched by this checkpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-19 | Tampering | tuning edits | mitigate | Only named tunables / token values change; re-run invariants + tsc after any edit; structural defects are captured as new UAT gaps, not hand-patched. |
</threat_model>

<verification>
- Founder types "approved" for GAP-1..4 and D1-D4 no-regression on the live install.
- After any edit: `npx tsc --noEmit` clean; the three viz suites (invariants, index-route, corpus-graph) green.
- Final approved values recorded in constants.js/styles.css and the SUMMARY.
</verification>

<success_criteria>
- GAP-1..4 confirmed resolved and D1-D4 confirmed not regressed by the founder on the live install.
- The four CORPUS_* constants (and any residual feel values) ratcheted to approved settings.
- Invariants / index-route / corpus-graph suites green; tsc clean.
</success_criteria>

<output>
Create `.planning/phases/61-corpus-chrome-index-column-project-browsing/61-09-SUMMARY.md` when done, recording the final approved feel values and the founder's GAP-1..4 + D1-D4 sign-off. Note that this SUMMARY supersedes the still-open 61-04 sign-off (61-04 need not be executed separately).
</output>
