---
phase: 61-corpus-chrome-index-column-project-browsing
plan: 16
type: execute
wave: 2
depends_on: ["61-15"]
files_modified:
  - src/viz/modules/index.js
  - src/viz/css/styles.css
autonomous: false
gap_closure: true
requirements: ["GAP-9"]

must_haves:
  truths:
    - "The corpus index reads as a free-floating, draggable panel with its own chrome/header — visually separate from the graph, not a docked rail and not a flush canvas overlay (GAP-9)"
    - "The corpus graph keeps the full canvas — it no longer reflows to make room for the index"
    - "The closed corpus view no longer feels bare (founder-confirmed at the checkpoint)"
    - "No regression: collapsible tree, project focus flow, active row, Esc-exit, animated unfocus, square-edged rows, and human-readable Notes titles all still work inside the floating panel"
  artifacts:
    - path: "src/viz/css/styles.css"
      provides: "#index-panel restyled as a floating panel (rounded panel chrome, full glass border, grab-cursor drag header, bounded height, default offset position); .index-docked reflow removed/neutralized"
      contains: "#index-panel"
    - path: "src/viz/modules/index.js"
      provides: "pointer-drag on the header; floating mount (no canvas reflow); in-memory panel position; default presentation decided at checkpoint"
  key_links:
    - from: "src/viz/modules/index.js header pointerdown"
      to: "#index-panel position/transform"
      via: "pointer drag handler"
      pattern: "pointer(down|move|up)"
    - from: "src/viz/modules/index.js openSidebar/hidePanel"
      to: "#corpus-graph canvas width"
      via: "no longer toggles .index-docked reflow"
      pattern: "index-docked"
---

<objective>
Close GAP-9 (founder-raised at the 61-14 round-2 sign-off, captured in 61-UAT.md with status:
failed). The corpus index must stop being a docked rail (61-05/61-13 behavior) OR a flush canvas
overlay, and become a DETACHED FLOATING PANEL: own chrome/header, draggable, visually separated
from the graph — "similar in spirit to the collapsed tray-app node view" (founder's words). The
closed corpus view must no longer feel bare.

Founder direction (verbatim, 61-UAT.md GAP-9): "maybe it would be good similar to the collapsed
tray app node view as like a separate window kinda cause it also feels bare without the index
drawer but having on top of the corpus graph still feels wrong." Clarified: a free-floating,
window-like panel inside the viz page, not docked into the layout, not overlaid flush on the canvas.

This is a STRUCTURAL/architectural change (new panel paradigm), which is exactly why it was carried
to its own plan instead of hand-patched at the checkpoint. It supersedes GAP-1's docked-reflow
specifically — the founder explicitly changed direction away from docking. All INNER behaviors
(tree, focus, active row, filter, Esc, square rows, Notes titles) must be preserved unchanged.

HARD RULE (founder, non-negotiable): no curved/rounded corners on the index PROJECT ROWS. The
panel container itself may be rounded (it is a floating window); the row-surface ban
(`.index-row`/`.index-entry` border-radius) stays in force — do not touch it.

Purpose: resolve the last open item of Phase 61. Output: a floating, draggable index panel that
reads as a distinct window over the corpus graph, verified live by the founder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UI-SPEC.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-PATTERNS.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-14-SUMMARY.md

<interfaces>
<!-- Extracted from live source — executor uses these directly. -->

styles.css (current docked-rail block, lines 1273-1309):
- `:root { --index-width: 270px; }` (line 1282) — the rail width, also drives the reflow offset.
- `#index-panel` (1283-1304): `position:fixed; top:0; left:0; bottom:0; width:var(--index-width);`
  glass-focused tier (`--glass-bg-focused`, `blur(--glass-blur-md)`, `--glass-specular`),
  `border-right: 1px solid var(--glass-border-focused)` (edge-only border), `opacity` fade via `.shown`.
- `.index-docked #corpus-graph { left: var(--index-width); }` (line 1309) — the canvas reflow to REMOVE.
- `.index-sidebar-header` (1312-1319): the header (title + collapse button) — becomes the drag grip.
- Glass/radius tokens available (constants.js HUD_CSS_TOKENS): `--glass-bg-focused`,
  `--glass-border-focused`, `--glass-blur-md`, `--glass-specular`, `--radius-lg` (12px), `--radius-md` (11px).
- backdrop-filter allow-list (tests/viz-activity-palette-invariants.test.ts ALLOWED_SELECTORS,
  lines 457-470) ALREADY includes `#index-panel` and `#index-reopen` — no test change needed unless a NEW selector gains backdrop-filter.

index.js (current dock mount, lines 385-424):
- `openSidebar()` (385-400): sets display:flex, adds `document.body.classList.add('index-docked')`,
  double-rAF adds `.shown` + calls `ctx.refitCorpus()`.
- `hidePanel(showHandleAfter)` (402-419): removes `.shown`, removes `.index-docked`, calls
  `ctx.refitCorpus()`, hides after transitionend/timeout, toggles the reopen handle.
- `ensureChrome()` (63-102): builds `.index-sidebar-header` (title 'Index' + `.index-collapse` button).
- `ctx.showIndexHandle` (433-437): reveals only the left-edge reopen handle without docking (GAP-6).
- `container = document.getElementById('index-panel')`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle #index-panel from docked rail to a floating window; stop the canvas reflow (CSS)</name>
  <files>src/viz/css/styles.css</files>
  <read_first>
    - src/viz/css/styles.css (lines 1242-1310 — #index-reopen, #index-panel, .index-docked reflow)
    - src/viz/css/styles.css (lines 1311-1343 — .index-sidebar-header / .index-sidebar-title / .index-collapse, the future drag grip)
    - src/viz/css/styles.css (lines 1427-1497 — the .index-row / .index-entry square-edge ban block and the durable GAP-5 comment; DO NOT modify these row rules)
    - src/viz/css/styles.css (lines 51-70 — #detail floating focused-glass recipe, the analog for a rounded floating panel)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UI-SPEC.md (Glass-tier rationale + Color sections)
  </read_first>
  <action>
    Convert `#index-panel` from a flush full-height rail into a floating window, reusing the `#detail`
    focused-glass recipe (lines 51-70) as the analog:
    - Replace the `top:0; left:0; bottom:0; width:var(--index-width)` full-height anchoring with a
      FLOATING position: a default offset from the top-left corner (e.g. `top: 56px; left: 16px`),
      a bounded height (`max-height: calc(100vh - <top>px - 16px)`), and keep `width: var(--index-width)`
      (or a slightly narrower floating width) with `max-width` retained. Keep `display:none`→flex,
      `flex-direction:column`, the opacity `.shown` fade, and `box-sizing:border-box`.
    - Give the panel WINDOW chrome: replace the edge-only `border-right` with a full
      `border: 1px solid var(--glass-border-focused)` and add `border-radius: var(--radius-lg)`
      (the row-corner ban is on `.index-row`/`.index-entry` ONLY — the floating window container is
      allowed and SHOULD be rounded). Keep the existing `--glass-bg-focused` background,
      `backdrop-filter: blur(var(--glass-blur-md))` (+ `-webkit-` twin), and `box-shadow: var(--glass-specular)`.
      Optionally strengthen the drop shadow to read as a lifted window, but only via existing tokens
      or opacity — ZERO new raw color literals (D-14-A) and NO amber (D-14-B).
    - Make the header read as a title bar / drag grip: give `.index-sidebar-header` `cursor: grab`
      (and a `.index-panel.dragging` / `:active` `cursor: grabbing` companion), and ensure the header
      visually reads as a handle (the existing bottom divider is fine). Do not remove the title or
      collapse button.
    - REMOVE the canvas reflow: delete (or neutralize) the `.index-docked #corpus-graph { left: var(--index-width); }`
      rule so the graph keeps the full canvas while the panel floats over it. If any child rule still
      relies on `--index-width`, keep the variable declared (it can still size the panel width); only
      the `#corpus-graph` offset is removed.
    - Add a durable comment above `#index-panel` documenting the GAP-9 paradigm (floating detached
      window, not a docked rail; graph no longer reflows) so a future reader does not re-dock it.
    Leave `#index-reopen` as-is (it already carries ambient glass). Do NOT touch the `.index-row`/
    `.index-entry`/`.index-chevron`/`.index-count` rules or the GAP-5 square-edge comment.
  </action>
  <verify>
    <automated>npx vitest run tests/viz-activity-palette-invariants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `#index-panel` no longer uses `top:0; left:0; bottom:0` full-height anchoring; it has a floating offset + `max-height` + `border-radius: var(--radius-lg)` + a full `border` (not just `border-right`).
    - `grep -n "index-docked #corpus-graph" src/viz/css/styles.css` returns nothing (reflow rule removed).
    - `grep -n "cursor: grab" src/viz/css/styles.css` returns the header drag-grip cursor.
    - No new raw color literals: `npx vitest run tests/viz-activity-palette-invariants.test.ts` stays green (findRawColorLiteralLines == [], amber allow-list intact, backdrop-filter allow-list intact — #index-panel already listed).
    - The `.index-row`/`.index-entry` blocks are unchanged: `grep -n "border-radius" src/viz/css/styles.css` still shows ZERO hits scoped within the `.index-row`/`.index-entry` rules (GAP-5 hard rule preserved).
    - `npx vitest run tests/viz-frontend-static.test.ts` green.
  </acceptance_criteria>
  <done>The index panel reads as a rounded, bordered, glass floating window with a grab-cursor header; the corpus graph keeps the full canvas.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the panel as a floating window and make the header draggable (JS)</name>
  <files>src/viz/modules/index.js</files>
  <read_first>
    - src/viz/modules/index.js (lines 45-115 — init, ensureChrome header build, ensureReopenHandle)
    - src/viz/modules/index.js (lines 379-446 — openSidebar / hidePanel / collapseSidebar / closeIndexSidebar / ctx hooks / showIndexHandle / syncCorpusFocus)
    - src/viz/modules/graph.js (lines 860-870 and 1120-1195 — the existing pointerdown/pointerup pointer-capture idiom to mirror for drag, net-zero-deps)
    - src/viz/css/styles.css (the #index-panel floating rules from Task 1 — the JS must not fight them)
  </read_first>
  <action>
    Floating mount (index.js openSidebar/hidePanel): stop docking + reflowing.
    - In `openSidebar()`: remove the `document.body.classList.add('index-docked')` line and the
      `ctx.refitCorpus()` call (the graph no longer reflows, so no re-fit is needed on open). Keep
      `container.style.display = 'flex'`, the double-rAF `.shown` add for the opacity fade, and
      `prepareIndex()`.
    - In `hidePanel()`: remove the `document.body.classList.remove('index-docked')` line and the
      `ctx.refitCorpus()` re-fit calls. Keep the `.shown` removal, the transitionend/timeout
      display:none hide, and `showReopenHandle(showHandleAfter)`.
    - The panel now floats at its CSS default position; opening/closing is a pure opacity fade over a
      fixed floating window.

    Header drag (index.js): make the panel draggable by its header, mirroring graph.js's
    pointerdown/pointermove/pointerup + setPointerCapture idiom (net-zero deps — no library).
    - In `ensureChrome()`, after the `.index-sidebar-header` is built, wire a pointer drag: on
      `pointerdown` on the header (but NOT on the `.index-collapse` button — check `ev.target.closest('.index-collapse')`
      and bail so the collapse click still works), record the pointer start and the panel's current
      offset, `setPointerCapture`, add a `dragging` class to the panel. On `pointermove`, update the
      panel's `left`/`top` (inline style) by the pointer delta, CLAMPED so the panel stays within the
      viewport (keep at least the header visible — do not let it be dragged fully off-screen). On
      `pointerup`/`pointercancel`, release capture and remove the `dragging` class.
    - Persist the dragged position IN-MEMORY only (a module-scoped `panelPos` object, like the existing
      `expandedIds`/`activeScope` session state) so the position survives a corpus close/reopen within
      the session and resets on hard reload (never localStorage — matches the D-01 expand-state rule).
      On `openSidebar`, if a stored position exists, apply it to the panel's inline style before showing.

    Preserve ALL inner behavior unchanged: the collapsible tree, project name-click focus (which now
    goes through the WR-04 single-writer path from Plan 61-15), the active row, the filter, hover
    highlight, Esc-exit, and the reopen handle. Do not alter renderSections/makeProjectRow/syncCorpusFocus
    logic. The module header comment still describing a "docked LEFT rail" should be updated to the
    floating-panel paradigm (doc accuracy).
  </action>
  <verify>
    <automated>npx vitest run tests/viz-frontend-static.test.ts tests/viz-index-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "index-docked" src/viz/modules/index.js` returns nothing (no docking/reflow in openSidebar/hidePanel).
    - `grep -n "refitCorpus" src/viz/modules/index.js` shows the open/hide reflow re-fits removed (any remaining refitCorpus reference, if kept for collapse-only, is documented — expected: none needed on the floating path).
    - `grep -nE "pointer(down|move|up)|setPointerCapture" src/viz/modules/index.js` shows the header drag wiring.
    - Drag ignores clicks on `.index-collapse` (a `closest('.index-collapse')` bail is present) so collapse still closes the panel.
    - Panel position persists in a module-scoped variable (not localStorage): `grep -n "localStorage" src/viz/modules/index.js` returns nothing.
    - `npx vitest run tests/viz-frontend-static.test.ts` and `tests/viz-index-route.test.ts` green; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The index panel is a floating window draggable by its header, position persisted in-session; opening/closing no longer reflows the graph; all tree/focus/filter behaviors are intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Founder live-install sign-off on the detached floating index panel (GAP-9)</name>
  <files>(none — verification checkpoint)</files>
  <read_first>
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md (GAP-9 founder direction and acceptance)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-14-SUMMARY.md (round-2 sign-off protocol: rebuild before verify; capture structural feedback as new gaps, no hand-patching)
  </read_first>
  <action>Checkpoint — do NOT auto-advance. After Tasks 1-2 merge and `npm run build` runs, PAUSE and present the floating-panel build to the founder for live-install sign-off using the how-to-verify steps below. This is a founder feel + no-regression gate for GAP-9. Capture any issues (position, default-open presentation, drag feel) as new UAT gaps rather than hand-patching at the checkpoint (Rule 4 — structural feel changes get their own follow-up plan).</action>
  <what-built>
    #index-panel is now a detached, draggable, own-chrome floating window (rounded glass container,
    full border, grab-cursor header) over the corpus graph. The graph keeps the full canvas (no
    reflow). Position is drag-adjustable and persists within the session. All prior behaviors
    (collapsible tree, project focus flow, active row, Esc-exit, animated unfocus, square-edged rows,
    human-readable Notes titles, filter) are preserved. The WR-01/03/04 predicate fixes from Plan
    61-15 are also live.
  </what-built>
  <how-to-verify>
    Prerequisite: run `npm run build` first so the packed viz assets reflect this work (the 61-14
    sign-off hit stale assets — rebuild before verifying).
    1. Open the app, enter the corpus view. Confirm the index reads as a FREE-FLOATING WINDOW
       (own chrome/header, visually separate from the graph) — NOT a docked rail and NOT a flush
       canvas overlay. Confirm the corpus view "no longer feels bare."
    2. Drag the panel by its header around the viewport; confirm it moves, stays on-screen, and the
       collapse button in the header still closes it (drag does not swallow the collapse click).
    3. Confirm the graph keeps the full canvas (no reflow/offset when the panel is open).
    4. No-regression pass: expand a project (chevron), click a project name (graph focuses + dims +
       reveals chapters, active row paints), click the active row again / press Esc / click empty
       canvas (animated zoom-out), confirm rows are square-edged (no rounded corners), Notes titles
       are human-readable (no UUIDs), and the filter box still works.
    5. Decision to confirm: is the floating panel's DEFAULT presentation right (shown-by-default
       floating vs. handle-first / closed-by-default)? State your preference — this is the one open
       design choice; the executor defaulted to a concrete build target and will adjust to your call.
  </how-to-verify>
  <resume-signal>Type "approved" if the floating panel reads right and nothing regressed, or describe the issues (position/default-open/feel) to capture as follow-up gaps.</resume-signal>
  <verify><human-check>Founder confirms the detached floating panel reads right, drags, keeps the graph full-canvas, no longer feels bare, and nothing from GAP-1..8 / D1-D4 regressed.</human-check></verify>
  <done>Founder types "approved" on the live install, or issues are captured as new UAT gaps for a follow-up plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB-sourced strings → DOM | Index tree labels/slugs render in the floating panel; must stay `.textContent`-only (T-39-08). |
| user pointer input → panel position | Drag deltas mutate inline style only; no eval, no DOM injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-16-01 | Injection (XSS) | Floating panel row rendering | mitigate | This plan changes panel positioning/chrome only; all DB strings keep `.textContent` (T-39-08). No `innerHTML` with DB data introduced. |
| T-61-16-02 | Denial of service (self) | Drag off-screen | mitigate | Pointermove clamps the panel within the viewport so it can never be dragged fully off-screen and lost. |
| T-61-16-03 | Tampering | CSS token/glass invariants | mitigate | No new raw color literals or amber (D-14-A/B); backdrop-filter stays on the already-allow-listed `#index-panel`; invariants suite must stay green. |
| T-61-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs (net-zero deps; native Pointer Events only). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run tests/viz-activity-palette-invariants.test.ts` — green (raw-literal ban, amber exclusivity, backdrop-filter allow-list intact).
- `npx vitest run tests/viz-frontend-static.test.ts` — green.
- `npx vitest run tests/viz-index-route.test.ts` — green.
- `npx tsc --noEmit` — clean.
- `grep -n "index-docked #corpus-graph" src/viz/css/styles.css` — no match (reflow removed).
- Row square-edge rule preserved (no border-radius on `.index-row`/`.index-entry`).
- Founder live-install sign-off recorded (Task 3).
</verification>

<success_criteria>
- The index is a detached, draggable, own-chrome floating panel visually separate from the graph — not a docked rail, not a flush overlay.
- The corpus graph keeps the full canvas; opening/closing the panel does not reflow it.
- The founder confirms the closed corpus view no longer feels bare and nothing from GAP-1..8 / D1-D4 regressed.
- The GAP-5 hard rule holds: index project rows remain square-edged.
</success_criteria>

<output>
Create `.planning/phases/61-corpus-chrome-index-column-project-browsing/61-16-SUMMARY.md` when done.
</output>
