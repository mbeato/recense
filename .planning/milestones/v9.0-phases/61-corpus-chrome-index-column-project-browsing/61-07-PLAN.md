---
phase: 61-corpus-chrome-index-column-project-browsing
plan: 07
type: execute
wave: 3
depends_on: ["61-06", "61-08"]
gap_closure: true
gaps: ["GAP-3"]
requirements: ["GAP-3"]
files_modified:
  - src/viz/modules/index.js
  - src/viz/modules/corpus.js
  - src/viz/css/styles.css
  - src/viz/modules/constants.js
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The focused project's index row shows a clear active state, and clicking it again exits focus; Esc and canvas-click keep working."
  artifacts:
    - path: "src/viz/modules/index.js"
      provides: "Active-project tracking; project-name click toggles focus on/off; renders .active on the focused project row"
      contains: "syncCorpusFocus"
    - path: "src/viz/modules/corpus.js"
      provides: "ctx.syncCorpusFocus notified on every focus change (focus/switch/Esc/background-click) so the index active state stays in sync"
      contains: "syncCorpusFocus"
    - path: "src/viz/css/styles.css"
      provides: ".index-row.active visible active state (token-only, no amber)"
      contains: ".index-row.active"
    - path: "src/viz/modules/constants.js"
      provides: "HUD_CSS_TOKENS entry for the active-row background (token, not raw literal)"
      contains: "index-row-active"
  key_links:
    - from: "corpus.js focus change (focus/switch/Esc/onBackgroundClick)"
      to: "index.js active-row state"
      via: "ctx.syncCorpusFocus(focusedScope|null)"
      pattern: "syncCorpusFocus"
    - from: "index.js project-name click on the active row"
      to: "ctx.focusCorpusProject(null)"
      via: "toggle when activeScope === entry.slug"
      pattern: "focusCorpusProject\\(null\\)"
---

<objective>
Close GAP-3: focusing a project has no discoverable exit and no visible active state — Esc and canvas-click
work but are invisible (UAT test 2). Founder direction: make the focused project's index row visibly ACTIVE,
and clicking that active row again UNFOCUSES (toggle); Esc and canvas-click keep working; no extra chrome on
the graph. This makes the focus/unfocus loop discoverable through the index itself.

Purpose: the browsing path (D2) works but lacks a clear way back out. An active row + click-to-toggle gives a
visible, reversible affordance without adding a back button or graph chrome.
Output: the focused project's row shows an active state; clicking it toggles focus off; canvas-click/Esc also
clear focus AND the index active state (state stays in sync across both directions).

Constraint: no amber on the active state (D-14-B amber-exclusivity — amber is activation/hover-only, and focus
is expressed WITHOUT amber per 61-02). Active-state color is a non-amber token. No new graph chrome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-CONTEXT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md
@.planning/phases/61-corpus-chrome-index-column-project-browsing/61-PATTERNS.md

<interfaces>
<!-- Current focus wiring (extracted) -->
index.js makeProjectRow name-click (~212-217): const scope = entry.slug;
  if (!expandedIds.has(entry.id)) setProjectExpanded(entry, true);
  if (typeof ctx.focusCorpusProject === 'function') ctx.focusCorpusProject(scope);
index.js registers ctx hooks (~411-413): ctx.openIndexSidebar / ctx.closeIndexSidebar / ctx.openIndex.

corpus.js ctx.focusCorpusProject(scope|null) (~759-780): scope===null → focusedScope=null + reframe; else
  guard `projectScopes.has(scope)` then focusedScope=scope + animated zoomToFit to that project's nodes.
corpus.js .onBackgroundClick (~424): if (focusedScope) ctx.focusCorpusProject(null).
corpus.js Esc listener (~797-802): if focusedScope && corpus open && reader/palette closed → focusCorpusProject(null).
corpus.js is the OWNER of focusCorpusProject; index.js CALLS it (ctx-hook convention — never a direct import).

Reusable non-amber tokens (HUD_CSS_TOKENS): --text-bright #e7dfec, --text-bright-mauve #c8bcd0,
  --text-mauve-rest #8b7090, --index-search-focus-bg rgba(139,112,144,.12), --index-hairline.
emitHudTokens() (css-tokens.js) auto-emits every HUD_CSS_TOKENS key as a :root var — adding a key is the
tokenized path (no raw literal in styles.css).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Active-project tracking, click-to-toggle-unfocus, and focus-state sync (index.js + corpus.js)</name>
  <files>src/viz/modules/index.js, src/viz/modules/corpus.js</files>
  <read_first>
    - src/viz/modules/index.js (makeProjectRow ~185-226, renderTreeSection/emit ~284-302, renderSections
      ~305-318, and the ctx-hook registration ~411-413 — so the new hook is registered the same way)
    - src/viz/modules/corpus.js (ctx.focusCorpusProject ~759-780, onBackgroundClick ~424-426, the Esc listener
      ~797-803 — the four places focusedScope changes and must notify the index)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-PATTERNS.md (Ctx-hook cross-module wiring
      section — owner defines `ctx.fn`, consumer calls it guarded)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UAT.md (GAP-3 founder_direction)
  </read_first>
  <action>
    Wire a two-way focus/active sync between index.js and corpus.js:
    1. index.js — track the active project. Add a module-scoped `activeScope` (string|null, the slug of the
       currently focused project). In makeProjectRow's name-click handler: if `activeScope === entry.slug`
       (clicking the already-active row), call `ctx.focusCorpusProject(null)` and set `activeScope = null`
       (toggle OFF) — do NOT also collapse the row (collapse stays chevron-owned). Otherwise keep the current
       behavior (auto-expand if collapsed + `ctx.focusCorpusProject(entry.slug)`) and set `activeScope = entry.slug`.
       After changing activeScope call renderSections() so the `.active` class re-applies.
    2. index.js — render the active class. In makeProjectRow, when `activeScope === entry.slug`, add the `active`
       class to the `.index-row` element (e.g. `row.className = 'index-row' + (isActive ? ' active' : '')`), and
       set `aria-current="true"` on the row for a11y. Pass the active flag through emit/makeProjectRow.
    3. index.js — own a sync hook. Register `ctx.syncCorpusFocus = function syncCorpusFocus(scope) { activeScope =
       scope || null; renderSections(); }` next to the other ctx registrations. This lets corpus.js push the
       current focus back into the index (guarded-call convention — corpus.js checks `typeof ctx.syncCorpusFocus
       === 'function'`).
    4. corpus.js — notify on every focus change. In ctx.focusCorpusProject, after setting focusedScope (both the
       null/clear branch AND the set branch, including the ignored-unrecognized-scope early return path where
       focus does NOT change — skip notify only when nothing changed), call `if (typeof ctx.syncCorpusFocus ===
       'function') ctx.syncCorpusFocus(focusedScope)`. Also ensure the onBackgroundClick handler and the Esc
       listener route through ctx.focusCorpusProject(null) (they already do) so the notify fires from those paths
       too — no separate notify needed there if they call focusCorpusProject.
    Guardrails: do NOT add a second Esc listener or new graph chrome. Do NOT recolor nodes (focus stays dim-only,
    no amber). The chevron remains an independent expand/collapse target (stopPropagation intact).
  </action>
  <verify>
    <automated>cd /Users/vtx/brain-memory && npx tsc --noEmit 2>/dev/null; grep -q "syncCorpusFocus" src/viz/modules/index.js && grep -q "syncCorpusFocus" src/viz/modules/corpus.js && grep -q "focusCorpusProject(null)" src/viz/modules/index.js</automated>
  </verify>
  <acceptance_criteria>
    - index.js: clicking a project row whose slug === activeScope calls `ctx.focusCorpusProject(null)` and sets
      activeScope null (toggle off) — source assertion; clicking a non-active row sets activeScope to that slug
      and calls focus(scope).
    - index.js: the `.index-row` for the active project gets the `active` class and `aria-current="true"` during
      render (source assertion in makeProjectRow).
    - index.js registers `ctx.syncCorpusFocus`; corpus.js calls it (guarded) whenever focusedScope changes,
      including via onBackgroundClick and the Esc listener (which route through focusCorpusProject).
    - No second `document.addEventListener('keydown'` for focus was added in corpus.js beyond the existing one;
      no node recolor / amber added.
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>The focused project's row is marked active, clicking it toggles focus off, and canvas-click/Esc clear both graph focus and the index active state via ctx.syncCorpusFocus; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Active-row visual state (styles.css + constants.js token)</name>
  <files>src/viz/css/styles.css, src/viz/modules/constants.js</files>
  <read_first>
    - src/viz/css/styles.css (the `.index-row` / `.index-row:hover` block — as refined by 61-06 — so the active
      state out-ranks hover and reuses the same idiom)
    - src/viz/modules/constants.js (HUD_CSS_TOKENS ~700-828 — add the new token key here; confirm the naming/quote
      style of adjacent index-* keys like --index-search-focus-bg / --index-hairline)
    - src/viz/modules/css-tokens.js (emitHudTokens ~20-30 — confirm a new HUD_CSS_TOKENS key is auto-emitted as a
      :root var, so styles.css can reference var(--index-row-active-bg) with no raw literal)
    - .planning/phases/61-corpus-chrome-index-column-project-browsing/61-UI-SPEC.md (Color — amber is forbidden on
      row backgrounds/text; focus/active is expressed by luminance/opacity, never a warm hue)
  </read_first>
  <action>
    Give the active row a clear, non-amber active state:
    1. In constants.js HUD_CSS_TOKENS, add one key `'index-row-active-bg'` with a mauve tint stronger than
       `--index-search-focus-bg` (0.12) so active out-reads hover — e.g. `rgba(139, 112, 144, 0.22)` (same hue
       family as the existing index-* tokens; NOT amber). Place it beside the other index-* token entries.
    2. In styles.css, add `.index-row.active` (and `.index-row.active:hover`) after the `.index-row`/`.index-row:hover`
       rules: background `var(--index-row-active-bg)`, text `var(--text-bright)`, and a clear-but-quiet marker such
       as a 2px left accent bar via `box-shadow: inset 2px 0 0 var(--text-mauve-rest)` OR
       `border-left: 2px solid var(--text-bright-mauve)` (pick one; keep it token-routed). Ensure `.active` visually
       out-ranks `:hover` so hovering the active row doesn't erase the active read.
    3. The active chevron/count inherit fine; do not add amber to either. No raw hex/rgba literal in styles.css —
       every color via var(--token), including the new key.
    Zero amber. Zero raw literals in styles.css.
  </action>
  <verify>
    <automated>cd /Users/vtx/brain-memory && grep -q "index-row-active-bg" src/viz/modules/constants.js && grep -q "\.index-row\.active" src/viz/css/styles.css && npx vitest run tests/viz-activity-palette-invariants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `HUD_CSS_TOKENS` has an `index-row-active-bg` key with a non-amber mauve-family value (source assertion).
    - styles.css defines `.index-row.active` using `var(--index-row-active-bg)` + `var(--text-bright)` + a
      token-routed left-accent marker; no raw hex/rgba literal added.
    - The active state out-ranks `.index-row:hover` (an `.index-row.active:hover` rule or specificity ensures the
      active background persists on hover).
    - `npx vitest run tests/viz-activity-palette-invariants.test.ts` passes (45/45) — D-14-A literal ban and
      D-14-B amber-exclusivity both green (no new amber key/literal).
  </acceptance_criteria>
  <done>The active project row reads clearly (stronger mauve background + accent bar + brighter text), out-ranks hover, uses only tokens, and adds no amber; invariants suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus.js ↔ index.js | Focus state crosses via ctx hooks only (no direct import); both directions guarded by typeof checks. |
| CSS token source → styles.css | Active-state color via var(--token); no raw literal, no amber outside AMBER_ALLOWED_KEYS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-17 | Tampering | styles.css active-state + new token | mitigate | Token-only; run `npx vitest run tests/viz-activity-palette-invariants.test.ts` (D-14-A/B) after the edit. |
| T-61-18 | Denial of Service | focus/active re-render loop | accept | syncCorpusFocus → renderSections is a bounded full re-render (existing pattern); notify is skipped when focusedScope is unchanged, so no ping-pong. |
</threat_model>

<verification>
- Automated: invariants suite green (45/45); `npx tsc --noEmit` clean; grep gates pass.
- Behavioral (deferred to 61-09 founder checkpoint): the focused project's row shows an active state; clicking it
  again exits focus; pressing Esc or clicking empty canvas also clears both the graph focus and the row's active
  state; switching projects moves the active state.
</verification>

<success_criteria>
- Active row visible; click-to-toggle-unfocus works; Esc/canvas-click keep working and clear the active state.
- No amber; no new graph chrome; invariants suite green; tsc clean.
</success_criteria>

<output>
Create `.planning/phases/61-corpus-chrome-index-column-project-browsing/61-07-SUMMARY.md` when done.
</output>
